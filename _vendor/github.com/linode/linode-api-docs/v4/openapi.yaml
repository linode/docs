openapi: 3.0.1
info:
  description: |
    Add a Cloud Computing instance so you can build, release, and scale applications faster with virtual machines.
  title: 'Akamai: Linode API'
  version: 4.176.0
  contact:
    name: Linode
    email: support@linode.com
    url: https://linode.com
  license:
    name: Apache 2.0
    url: https://www.apache.org/licenses/LICENSE-2.0.html
servers:
- url: https://api.linode.com/v4
- url: https://api.linode.com/v4beta
tags:
- name: Account
  description: |
    Use the Account endpoints to manage user settings, billing, and payments. You can also initiate and maintain OAuth client
    application authentication, enable the Linode Managed service, and create new
    users on your account.
- name: Beta programs
  description: |
    Use the Beta Programs endpoint to view information about available beta programs. To sign up for eligible beta programs,
    run the _Enroll in a Beta program_ operation.
- name: Databases
  description: |
    Managed Databases is Linode's fully-managed, high-performance database service. Use the Managed Databases endpoints to create
    and manage database clusters.
- name: Domains
  description: |
    Use the Domains endpoints to create and manage domains and domain records on your account.
- name: Images
  description: |
    Use the Images endpoints to capture, store, and manage custom Linode images.
- name: Linode instances
  description: |
    Use the Linode Instances endpoints to create, configure, and manage your Linode instances. You can also manage the Linode
    Backup service; maintain and manage configuration profiles; create and maintain
    disks, initiate a host migration; view Linode Instance statistics; and more.
- name: Linode types
  description: |
    Use the Linode Types endpoints to retrieve information about Linode plan types, including pricing information, hardware resources,
    and network transfer allotment.
- name: Linode Kubernetes Engine (LKE)
  description: |
    Linode Kubernetes Engine (LKE) is Linode's managed Kubernetes service. Use the LKE endpoints to create and manage Kubernetes
    clusters and their associated Node Pools.
- name: Longview
  description: |
    Longview is Linode's system-level monitoring and graphing service. Use the Longview endpoints to manage your Longview subscription
    and plan and to create and maintain Longview clients.
- name: Managed
  description: |
    Managed is Linode's incident response service. Use the Managed endpoints to register a service to be monitored by the
    Managed Service team, provide secure access to your managed services,  view information
    about detected issues, and more.
- name: Networking
  description: |
    Use the Networking endpoints to view all IP addresses on your account, reorganize assigned IPv4 addresses, update RDNS,
    and configure IP sharing.
- name: NodeBalancers
  description: |
    NodeBalancers is Linode's load balancing service. Use the NodeBalancers endpoints to create and manage NodeBalancers. You
    can also create and maintain configurations; create and maintain nodes, and view
    statistics.
- name: Object storage
  description: |
    Object Storage is Linode's S3-compatible data storage service. Use the Object Storage endpoints to create and maintaining
    buckets, add and remove objects from buckets, create and maintain Object Storage
    keys, and cancel the Object Storage service.
- name: Profile
  description: |
    Use the Profile endpoints to manage your Linode user profile preferences and security settings. This includes creating
    and maintaining personal access tokens, creating and maintaining SSH keys, confirming
    and enabling two-factor authentication, and updating user and profile preferences.
- name: Regions
  description: |
    Use the Regions endpoints to view information about the various Linode data center regions, including the service capabilities
    for each region, country, status, and more.
- name: StackScripts
  description: |
    Linode StackScripts allow you to create reusable scripts to configure new Linode instances. Use the StackScripts endpoints
    to create and manage StackScripts on your account.
- name: Support
  description: |
    Use the Support endpoints to open, view, and close Linode Support tickets. You can also create and manage your Support
    ticket replies.
- name: Tags
  description: |
    Tags allow you to organize and group your various Linode services. Use the Tags endpoints to create, assign, and delete
    your account tags.
- name: Volumes
  description: |
    Volumes is Linode's block storage service. Use the Volumes endpoints to create, attach, and manage your account Volumes.
- name: VPCs
  description: |
    Use the VPCs endpoints to view, create, and manage Virtual Private Cloud (VPC) and VPC Subnet resources.
x-readme:
  samples-languages:
  - curl
  - python
  - node
paths:
  /nodebalancers/{nodeBalancerId}/configs/{configId}:
    x-akamai:
      file-path: paths/node-balancer-config.yaml
      path-info: /nodebalancers/{nodeBalancerId}/configs/{configId}
    parameters:
    - name: nodeBalancerId
      in: path
      description: |
        The ID of the NodeBalancer to access.
      example: '{{nodeBalancerId}}'
      x-akamai:
        file-path: parameters/node-balancer-id.yaml
      required: true
      schema:
        type: integer
    - name: configId
      in: path
      description: |
        The ID of the Config to access
      example: '{{configId}}'
      x-akamai:
        file-path: parameters/config-id.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-node-balancer-config
      summary: Get a config
      tags:
      - Configurations
      description: |-
        Returns configuration information for a single port of this NodeBalancer.


        ---


        - __CLI__.

            ```
            linode-cli nodebalancers config-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            nodebalancers:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli nodebalancers config-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: nodebalancers:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The requested NodeBalancer config.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A NodeBalancer config represents the configuration of this NodeBalancer on a single port.  For example,
                  a NodeBalancer Config on port 80 would typically represent how this
                  NodeBalancer response to HTTP requests. NodeBalancer configs have
                  a list of backends, called "nodes," that they forward requests between
                  based on their configuration.
                x-akamai:
                  file-path: schemas/node-balancer-config.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This config's unique ID
                    example: 4567
                    readOnly: true
                    x-linode-cli-display: 1
                  algorithm:
                    type: string
                    description: |
                      What algorithm this NodeBalancer should use for routing traffic to backends.
                    example: roundrobin
                    default: roundrobin
                    enum:
                    - roundrobin
                    - leastconn
                    - source
                    x-linode-cli-display: 4
                  check:
                    type: string
                    description: |-
                      The type of check to perform against backends to ensure they are serving requests. This is used to determine if backends are up or down.

                      - If `none` no check is performed.
                      - `connection` requires only a connection to the backend to succeed.
                      - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
                    example: http_body
                    default: none
                    enum:
                    - none
                    - connection
                    - http
                    - http_body
                  check_attempts:
                    type: integer
                    description: |
                      How many times to attempt a check before considering a backend to be down.
                    example: 3
                    default: 3
                    minimum: 1
                    maximum: 30
                  check_body:
                    type: string
                    description: |
                      This value must be present in the response body of the check in order for it to
                      pass. If this value is not present in the response body of a
                      check request, the backend is considered to be down.
                    example: it works
                  check_interval:
                    type: integer
                    description: |-
                      How often, in seconds, to check that backends are up and serving requests.

                      Must be greater than `check_timeout`.
                    example: 90
                    default: 31
                  check_passive:
                    type: boolean
                    description: |
                      If true, any response from this backend with a `5xx` status code will be enough for
                      it to be considered unhealthy and taken out of rotation.
                    example: true
                    default: true
                    x-linode-cli-display: 6
                  check_path:
                    type: string
                    description: |
                      The URL path to check on each backend. If the backend does not respond to this request
                      it is considered to be down.
                    example: /test
                    pattern: ^[a-zA-Z0-9\/\-%?&=.]*$
                  check_timeout:
                    type: integer
                    description: |-
                      How long, in seconds, to wait for a check attempt before considering it failed.

                      Must be less than `check_interval`.
                    example: 10
                    default: 30
                    minimum: 1
                    maximum: 30
                  cipher_suite:
                    type: string
                    description: |-
                      What ciphers to use for SSL connections served by this NodeBalancer.

                      - `legacy` is considered insecure and should only be used if necessary.
                    example: recommended
                    default: recommended
                    enum:
                    - recommended
                    - legacy
                    x-linode-cli-color:
                      default_: white
                      legacy: red
                    x-linode-cli-display: 7
                  nodebalancer_id:
                    type: integer
                    description: |
                      __Read-only__ The ID for the NodeBalancer this config belongs to.
                    example: 12345
                    readOnly: true
                  nodes_status:
                    type: object
                    description: |
                      __Read-only__ A structure containing information about the health of the backends for
                      this port.  This information is updated periodically as checks
                      are performed against backends.
                    additionalProperties: false
                    readOnly: true
                    x-linode-cli-display: 10
                    properties:
                      down:
                        type: integer
                        description: |
                          __Read-only__ The number of backends considered to be "DOWN" and unhealthy.  These
                          are not in rotation, and not serving requests.
                        example: 0
                        readOnly: true
                      up:
                        type: integer
                        description: |
                          __Read-only__ The number of backends considered to be "UP" and healthy, and
                          that are serving requests.
                        example: 4
                        readOnly: true
                  port:
                    type: integer
                    description: |
                      The port this Config is for. These values must be unique across configs on a single
                      NodeBalancer (you can't have two configs for port 80, for example).  While
                      some ports imply some protocols, no enforcement is done and
                      you may configure your NodeBalancer however is useful to you.
                      For example, while port 443 is generally used for HTTPS, you
                      do not need SSL configured to have a NodeBalancer listening
                      on port 443.
                    example: 80
                    default: 80
                    minimum: 1
                    maximum: 65535
                    x-linode-cli-display: 2
                  protocol:
                    type: string
                    description: |-
                      The protocol this port is configured to serve.

                      - The `http` and `tcp` protocols do not support `ssl_cert` and `ssl_key`.

                      - The `https` protocol is mutually required with `ssl_cert` and `ssl_key`.

                      Review our guide on [Available Protocols](https://www.linode.com/docs/products/networking/nodebalancers/guides/protocols/) for information on protocol features.
                    example: http
                    default: http
                    enum:
                    - http
                    - https
                    - tcp
                    x-linode-cli-display: 3
                  proxy_protocol:
                    type: string
                    description: |-
                      ProxyProtocol is a TCP extension that sends initial TCP connection information such as source/destination IPs and ports to backend devices. This information would be lost otherwise. Backend devices must be configured to work with ProxyProtocol if enabled.

                      - If omitted, or set to `none`, the NodeBalancer doesn't send any auxiliary data over TCP connections. This is the default.
                      - If set to `v1`, the human-readable header format (Version 1) is used. Requires `tcp` protocol.
                      - If set to `v2`, the binary header format (Version 2) is used. Requires `tcp` protocol.
                    example: none
                    default: none
                    enum:
                    - none
                    - v1
                    - v2
                  ssl_cert:
                    type: string
                    description: |2-

                      The PEM-formatted public SSL certificate (or the combined PEM-formatted SSL certificate and Certificate Authority chain) that should be served on this NodeBalancerConfig's port.

                      Line breaks must be represented as "\n" in the string for requests (but not when using the Linode CLI).

                      [Diffie-Hellman Parameters](https://www.linode.com/docs/products/networking/nodebalancers/guides/ssl-termination/#diffie-hellman-parameters) can be included in this value to enable forward secrecy.

                      The contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.

                      The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.
                    example: <REDACTED>
                    nullable: true
                    format: ssl-cert
                  ssl_commonname:
                    type: string
                    description: |
                      __Read-only__ The read-only common name automatically derived from the SSL certificate assigned
                      to this NodeBalancerConfig. Please refer to this field to verify
                      that the appropriate certificate is assigned to your NodeBalancerConfig.
                    example: www.example.com
                    readOnly: true
                    x-linode-cli-display: 8
                  ssl_fingerprint:
                    type: string
                    description: |
                      __Read-only__ The read-only SHA1-encoded fingerprint automatically derived from the SSL
                      certificate assigned to this NodeBalancerConfig. Please refer
                      to this field to verify that the appropriate certificate is
                      assigned to your NodeBalancerConfig.
                    example: 00:01:02:03:04:05:06:07:08:09:0A:0B:0C:0D:0E:0F:10:11:12:13
                    readOnly: true
                    x-linode-cli-display: 9
                  ssl_key:
                    type: string
                    description: |-
                      The PEM-formatted private key for the SSL certificate set in the `ssl_cert` field.

                      Line breaks must be represented as "\n" in the string for requests (but not when using the Linode CLI).

                      The contents of this field will not be shown in any responses that display
                      the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field
                      appears.

                      The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig
                      response are automatically derived from your certificate. Please refer to these fields to
                      verify that the appropriate certificate was assigned to your NodeBalancerConfig.
                    example: <REDACTED>
                    nullable: true
                    format: ssl-key
                  stickiness:
                    type: string
                    description: |-
                      Controls how session stickiness is handled on this port.

                      - If set to `none` connections will always be assigned a backend based on the algorithm configured.
                      - If set to `table` sessions from the same remote address will be routed to the same backend.
                      - For HTTP or HTTPS clients, `http_cookie` allows sessions to be routed to the same backend based on a cookie set by the NodeBalancer.
                    example: http_cookie
                    default: none
                    enum:
                    - none
                    - table
                    - http_cookie
                    x-linode-cli-display: 5
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-node-balancer-config
      security:
      - personalAccessToken: []
      - oauth:
        - nodebalancers:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/nodebalancers/12345/configs/4567
      - lang: CLI
        source: |-
          linode-cli nodebalancers config-view \
            12345 4567
      x-linode-cli-action: config-view
      x-linode-grant: read_only
      x-original-op-id: getNodeBalancerConfig
      x-original-op-title: Config View
    put:
      operationId: put-node-balancer-config
      summary: Update a config
      tags:
      - Configurations
      description: |-
        Updates the configuration for a single port on a NodeBalancer.


        ---


        - __CLI__.

            ```
            linode-cli nodebalancers config-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            nodebalancers:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli nodebalancers config-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: nodebalancers:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The fields to update.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                A NodeBalancer config represents the configuration of this NodeBalancer on a single port.  For example,
                a NodeBalancer Config on port 80 would typically represent how this
                NodeBalancer response to HTTP requests. NodeBalancer configs have
                a list of backends, called "nodes," that they forward requests between
                based on their configuration.
              x-akamai:
                file-path: schemas/node-balancer-config.yaml
              additionalProperties: false
              properties:
                id:
                  type: integer
                  description: |
                    __Read-only__ This config's unique ID
                  example: '{{id}}'
                  readOnly: true
                  x-linode-cli-display: 1
                algorithm:
                  type: string
                  description: |
                    What algorithm this NodeBalancer should use for routing traffic to backends.
                  example: '{{algorithm}}'
                  default: roundrobin
                  enum:
                  - roundrobin
                  - leastconn
                  - source
                  x-linode-cli-display: 4
                check:
                  type: string
                  description: |-
                    The type of check to perform against backends to ensure they are serving requests. This is used to determine if backends are up or down.

                    - If `none` no check is performed.
                    - `connection` requires only a connection to the backend to succeed.
                    - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
                  example: '{{check}}'
                  default: none
                  enum:
                  - none
                  - connection
                  - http
                  - http_body
                check_attempts:
                  type: integer
                  description: |
                    How many times to attempt a check before considering a backend to be down.
                  example: '{{check_attempts}}'
                  default: 3
                  minimum: 1
                  maximum: 30
                check_body:
                  type: string
                  description: |
                    This value must be present in the response body of the check in order for it to pass. If
                    this value is not present in the response body of a check request,
                    the backend is considered to be down.
                  example: '{{check_body}}'
                check_interval:
                  type: integer
                  description: |-
                    How often, in seconds, to check that backends are up and serving requests.

                    Must be greater than `check_timeout`.
                  example: '{{check_interval}}'
                  default: 31
                check_passive:
                  type: boolean
                  description: |
                    If true, any response from this backend with a `5xx` status code will be enough for
                    it to be considered unhealthy and taken out of rotation.
                  example: '{{check_passive}}'
                  default: true
                  x-linode-cli-display: 6
                check_path:
                  type: string
                  description: |
                    The URL path to check on each backend. If the backend does not respond to this request
                    it is considered to be down.
                  example: '{{check_path}}'
                  pattern: ^[a-zA-Z0-9\/\-%?&=.]*$
                check_timeout:
                  type: integer
                  description: |-
                    How long, in seconds, to wait for a check attempt before considering it failed.

                    Must be less than `check_interval`.
                  example: '{{check_timeout}}'
                  default: 30
                  minimum: 1
                  maximum: 30
                cipher_suite:
                  type: string
                  description: |-
                    What ciphers to use for SSL connections served by this NodeBalancer.

                    - `legacy` is considered insecure and should only be used if necessary.
                  example: '{{cipher_suite}}'
                  default: recommended
                  enum:
                  - recommended
                  - legacy
                  x-linode-cli-color:
                    default_: white
                    legacy: red
                  x-linode-cli-display: 7
                nodebalancer_id:
                  type: integer
                  description: |
                    __Read-only__ The ID for the NodeBalancer this config belongs to.
                  example: '{{nodebalancer_id}}'
                  readOnly: true
                nodes_status:
                  type: object
                  description: |
                    __Read-only__ A structure containing information about the health of the backends for this
                    port.  This information is updated periodically as checks are
                    performed against backends.
                  additionalProperties: false
                  readOnly: true
                  properties:
                    down:
                      type: integer
                      description: |
                        __Read-only__ The number of backends considered to be "DOWN" and unhealthy.  These
                        are not in rotation, and not serving requests.
                      example: 0
                      readOnly: true
                    up:
                      type: integer
                      description: |
                        __Read-only__ The number of backends considered to be "UP" and healthy, and
                        that are serving requests.
                      example: 4
                      readOnly: true
                  x-linode-cli-display: 10
                port:
                  type: integer
                  description: |
                    The port this Config is for. These values must be unique across configs on a single
                    NodeBalancer (you can't have two configs for port 80, for example).  While
                    some ports imply some protocols, no enforcement is done and you
                    may configure your NodeBalancer however is useful to you. For
                    example, while port 443 is generally used for HTTPS, you do not
                    need SSL configured to have a NodeBalancer listening on port 443.
                  example: '{{port}}'
                  default: 80
                  minimum: 1
                  maximum: 65535
                  x-linode-cli-display: 2
                protocol:
                  type: string
                  description: |-
                    The protocol this port is configured to serve.

                    - The `http` and `tcp` protocols do not support `ssl_cert` and `ssl_key`.

                    - The `https` protocol is mutually required with `ssl_cert` and `ssl_key`.

                    Review our guide on [Available Protocols](https://www.linode.com/docs/products/networking/nodebalancers/guides/protocols/) for information on protocol features.
                  example: '{{protocol}}'
                  default: http
                  enum:
                  - http
                  - https
                  - tcp
                  x-linode-cli-display: 3
                proxy_protocol:
                  type: string
                  description: |-
                    ProxyProtocol is a TCP extension that sends initial TCP connection information such as source/destination IPs and ports to backend devices. This information would be lost otherwise. Backend devices must be configured to work with ProxyProtocol if enabled.

                    - If omitted, or set to `none`, the NodeBalancer doesn't send any auxiliary data over TCP connections. This is the default.
                    - If set to `v1`, the human-readable header format (Version 1) is used. Requires `tcp` protocol.
                    - If set to `v2`, the binary header format (Version 2) is used. Requires `tcp` protocol.
                  example: '{{proxy_protocol}}'
                  default: none
                  enum:
                  - none
                  - v1
                  - v2
                ssl_cert:
                  type: string
                  description: |2-

                    The PEM-formatted public SSL certificate (or the combined PEM-formatted SSL certificate and Certificate Authority chain) that should be served on this NodeBalancerConfig's port.

                    Line breaks must be represented as "\n" in the string for requests (but not when using the Linode CLI).

                    [Diffie-Hellman Parameters](https://www.linode.com/docs/products/networking/nodebalancers/guides/ssl-termination/#diffie-hellman-parameters) can be included in this value to enable forward secrecy.

                    The contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.

                    The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.
                  example: '{{ssl_cert}}'
                  nullable: true
                  format: ssl-cert
                ssl_commonname:
                  type: string
                  description: |
                    __Read-only__ The read-only common name automatically derived from the SSL certificate assigned
                    to this NodeBalancerConfig. Please refer to this field to verify
                    that the appropriate certificate is assigned to your NodeBalancerConfig.
                  example: '{{ssl_commonname}}'
                  readOnly: true
                  x-linode-cli-display: 8
                ssl_fingerprint:
                  type: string
                  description: |
                    __Read-only__ The read-only SHA1-encoded fingerprint automatically derived from the SSL certificate
                    assigned to this NodeBalancerConfig. Please refer to this field
                    to verify that the appropriate certificate is assigned to your
                    NodeBalancerConfig.
                  example: '{{ssl_fingerprint}}'
                  readOnly: true
                  x-linode-cli-display: 9
                ssl_key:
                  type: string
                  description: |-
                    The PEM-formatted private key for the SSL certificate set in the `ssl_cert` field.

                    Line breaks must be represented as "\n" in the string for requests (but not when using the Linode CLI).

                    The contents of this field will not be shown in any responses that display
                    the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field
                    appears.

                    The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig
                    response are automatically derived from your certificate. Please refer to these fields to
                    verify that the appropriate certificate was assigned to your NodeBalancerConfig.
                  example: '{{ssl_key}}'
                  nullable: true
                  format: ssl-key
                stickiness:
                  type: string
                  description: |-
                    Controls how session stickiness is handled on this port.

                    - If set to `none` connections will always be assigned a backend based on the algorithm configured.
                    - If set to `table` sessions from the same remote address will be routed to the same backend.
                    - For HTTP or HTTPS clients, `http_cookie` allows sessions to be routed to the same backend based on a cookie set by the NodeBalancer.
                  example: '{{stickiness}}'
                  default: none
                  enum:
                  - none
                  - table
                  - http_cookie
                  x-linode-cli-display: 5
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Config updated successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A NodeBalancer config represents the configuration of this NodeBalancer on a single port.  For example,
                  a NodeBalancer Config on port 80 would typically represent how this
                  NodeBalancer response to HTTP requests. NodeBalancer configs have
                  a list of backends, called "nodes," that they forward requests between
                  based on their configuration.
                x-akamai:
                  file-path: schemas/node-balancer-config.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This config's unique ID
                    example: 4567
                    readOnly: true
                    x-linode-cli-display: 1
                  algorithm:
                    type: string
                    description: |
                      What algorithm this NodeBalancer should use for routing traffic to backends.
                    example: roundrobin
                    default: roundrobin
                    enum:
                    - roundrobin
                    - leastconn
                    - source
                    x-linode-cli-display: 4
                  check:
                    type: string
                    description: |-
                      The type of check to perform against backends to ensure they are serving requests. This is used to determine if backends are up or down.

                      - If `none` no check is performed.
                      - `connection` requires only a connection to the backend to succeed.
                      - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
                    example: http_body
                    default: none
                    enum:
                    - none
                    - connection
                    - http
                    - http_body
                  check_attempts:
                    type: integer
                    description: |
                      How many times to attempt a check before considering a backend to be down.
                    example: 3
                    default: 3
                    minimum: 1
                    maximum: 30
                  check_body:
                    type: string
                    description: |
                      This value must be present in the response body of the check in order for it to
                      pass. If this value is not present in the response body of a
                      check request, the backend is considered to be down.
                    example: it works
                  check_interval:
                    type: integer
                    description: |-
                      How often, in seconds, to check that backends are up and serving requests.

                      Must be greater than `check_timeout`.
                    example: 90
                    default: 31
                  check_passive:
                    type: boolean
                    description: |
                      If true, any response from this backend with a `5xx` status code will be enough for
                      it to be considered unhealthy and taken out of rotation.
                    example: true
                    default: true
                    x-linode-cli-display: 6
                  check_path:
                    type: string
                    description: |
                      The URL path to check on each backend. If the backend does not respond to this request
                      it is considered to be down.
                    example: /test
                    pattern: ^[a-zA-Z0-9\/\-%?&=.]*$
                  check_timeout:
                    type: integer
                    description: |-
                      How long, in seconds, to wait for a check attempt before considering it failed.

                      Must be less than `check_interval`.
                    example: 10
                    default: 30
                    minimum: 1
                    maximum: 30
                  cipher_suite:
                    type: string
                    description: |-
                      What ciphers to use for SSL connections served by this NodeBalancer.

                      - `legacy` is considered insecure and should only be used if necessary.
                    example: recommended
                    default: recommended
                    enum:
                    - recommended
                    - legacy
                    x-linode-cli-color:
                      default_: white
                      legacy: red
                    x-linode-cli-display: 7
                  nodebalancer_id:
                    type: integer
                    description: |
                      __Read-only__ The ID for the NodeBalancer this config belongs to.
                    example: 12345
                    readOnly: true
                  nodes_status:
                    type: object
                    description: |
                      __Read-only__ A structure containing information about the health of the backends for
                      this port.  This information is updated periodically as checks
                      are performed against backends.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      down:
                        type: integer
                        description: |
                          __Read-only__ The number of backends considered to be "DOWN" and unhealthy.  These
                          are not in rotation, and not serving requests.
                        example: 0
                        readOnly: true
                      up:
                        type: integer
                        description: |
                          __Read-only__ The number of backends considered to be "UP" and healthy, and
                          that are serving requests.
                        example: 4
                        readOnly: true
                    x-linode-cli-display: 10
                  port:
                    type: integer
                    description: |
                      The port this Config is for. These values must be unique across configs on a single
                      NodeBalancer (you can't have two configs for port 80, for example).  While
                      some ports imply some protocols, no enforcement is done and
                      you may configure your NodeBalancer however is useful to you.
                      For example, while port 443 is generally used for HTTPS, you
                      do not need SSL configured to have a NodeBalancer listening
                      on port 443.
                    example: 80
                    default: 80
                    minimum: 1
                    maximum: 65535
                    x-linode-cli-display: 2
                  protocol:
                    type: string
                    description: |-
                      The protocol this port is configured to serve.

                      - The `http` and `tcp` protocols do not support `ssl_cert` and `ssl_key`.

                      - The `https` protocol is mutually required with `ssl_cert` and `ssl_key`.

                      Review our guide on [Available Protocols](https://www.linode.com/docs/products/networking/nodebalancers/guides/protocols/) for information on protocol features.
                    example: http
                    default: http
                    enum:
                    - http
                    - https
                    - tcp
                    x-linode-cli-display: 3
                  proxy_protocol:
                    type: string
                    description: |-
                      ProxyProtocol is a TCP extension that sends initial TCP connection information such as source/destination IPs and ports to backend devices. This information would be lost otherwise. Backend devices must be configured to work with ProxyProtocol if enabled.

                      - If omitted, or set to `none`, the NodeBalancer doesn't send any auxiliary data over TCP connections. This is the default.
                      - If set to `v1`, the human-readable header format (Version 1) is used. Requires `tcp` protocol.
                      - If set to `v2`, the binary header format (Version 2) is used. Requires `tcp` protocol.
                    example: none
                    default: none
                    enum:
                    - none
                    - v1
                    - v2
                  ssl_cert:
                    type: string
                    description: |2-

                      The PEM-formatted public SSL certificate (or the combined PEM-formatted SSL certificate and Certificate Authority chain) that should be served on this NodeBalancerConfig's port.

                      Line breaks must be represented as "\n" in the string for requests (but not when using the Linode CLI).

                      [Diffie-Hellman Parameters](https://www.linode.com/docs/products/networking/nodebalancers/guides/ssl-termination/#diffie-hellman-parameters) can be included in this value to enable forward secrecy.

                      The contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.

                      The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.
                    example: <REDACTED>
                    nullable: true
                    format: ssl-cert
                  ssl_commonname:
                    type: string
                    description: |
                      __Read-only__ The read-only common name automatically derived from the SSL certificate assigned
                      to this NodeBalancerConfig. Please refer to this field to verify
                      that the appropriate certificate is assigned to your NodeBalancerConfig.
                    example: www.example.com
                    readOnly: true
                    x-linode-cli-display: 8
                  ssl_fingerprint:
                    type: string
                    description: |
                      __Read-only__ The read-only SHA1-encoded fingerprint automatically derived from the SSL
                      certificate assigned to this NodeBalancerConfig. Please refer
                      to this field to verify that the appropriate certificate is
                      assigned to your NodeBalancerConfig.
                    example: 00:01:02:03:04:05:06:07:08:09:0A:0B:0C:0D:0E:0F:10:11:12:13
                    readOnly: true
                    x-linode-cli-display: 9
                  ssl_key:
                    type: string
                    description: |-
                      The PEM-formatted private key for the SSL certificate set in the `ssl_cert` field.

                      Line breaks must be represented as "\n" in the string for requests (but not when using the Linode CLI).

                      The contents of this field will not be shown in any responses that display
                      the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field
                      appears.

                      The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig
                      response are automatically derived from your certificate. Please refer to these fields to
                      verify that the appropriate certificate was assigned to your NodeBalancerConfig.
                    example: <REDACTED>
                    nullable: true
                    format: ssl-key
                  stickiness:
                    type: string
                    description: |-
                      Controls how session stickiness is handled on this port.

                      - If set to `none` connections will always be assigned a backend based on the algorithm configured.
                      - If set to `table` sessions from the same remote address will be routed to the same backend.
                      - For HTTP or HTTPS clients, `http_cookie` allows sessions to be routed to the same backend based on a cookie set by the NodeBalancer.
                    example: http_cookie
                    default: none
                    enum:
                    - none
                    - table
                    - http_cookie
                    x-linode-cli-display: 5
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-node-balancer-config
      security:
      - personalAccessToken: []
      - oauth:
        - nodebalancers:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "port": 443,
                "protocol": "https",
                "algorithm": "roundrobin",
                "stickiness": "http_cookie",
                "check": "http_body",
                "check_interval": 90,
                "check_timeout": 10,
                "check_attempts": 3,
                "check_path": "/test",
                "check_body": "it works",
                "check_passive": true,
                "proxy_protocol": "none",
                "ssl_cert": "-----BEGIN CERTIFICATE-----\nCERTIFICATE_INFORMATION\n-----END CERTIFICATE-----",
                "ssl_key": "-----BEGIN PRIVATE KEY-----\nPRIVATE_KEY_INFORMATION\n-----END PRIVATE KEY-----",
                "cipher_suite": "recommended"
              }' \
              https://api.linode.com/v4/nodebalancers/12345/configs/4567
      - lang: CLI
        source: |-
          linode-cli nodebalancers config-update \
            12345 4567 \
            --port 443 \
            --protocol https \
            --algorithm roundrobin \
            --stickiness http_cookie \
            --check http_body \
            --check_interval 90 \
            --check_timeout 10 \
            --check_attempts 3 \
            --check_path "/test" \
            --check_body "it works" \
            --check_passive true \
            --proxy_protocol "none" \
            --ssl_cert "-----BEGIN CERTIFICATE-----
                        CERTIFICATE_INFORMATION
                        -----END CERTIFICATE-----" \
            --ssl_key "-----BEGIN PRIVATE KEY-----
                       PRIVATE_KEY_INFORMATION
                       -----END PRIVATE KEY-----" \
            --cipher_suite recommended
      x-linode-cli-action: config-update
      x-linode-grant: read_write
      x-original-op-id: updateNodeBalancerConfig
      x-original-op-title: Config Update
    delete:
      operationId: delete-node-balancer-config
      summary: Delete a config
      tags:
      - Configurations
      description: |-
        Deletes the Config for a port of this NodeBalancer.

        __This cannot be undone.__

        Once completed, this NodeBalancer will no longer respond to requests on the given port. This also deletes all associated NodeBalancerNodes, but the Linodes they were routing traffic to will be unchanged and will not be removed.


        ---


        - __CLI__.

            ```
            linode-cli nodebalancers config-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            nodebalancers:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli nodebalancers config-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: nodebalancers:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            NodeBalancer Config deleted successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-node-balancer-config
      security:
      - personalAccessToken: []
      - oauth:
        - nodebalancers:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/nodebalancers/12345/configs/4567
      - lang: CLI
        source: |-
          linode-cli nodebalancers config-delete \
            12345 4567
      x-linode-cli-action: config-delete
      x-linode-grant: read_write
      x-original-op-id: deleteNodeBalancerConfig
      x-original-op-title: Config Delete
    x-linode-cli-command: nodebalancers
  /nodebalancers/{nodeBalancerId}/configs/{configId}/nodes:
    x-akamai:
      file-path: paths/nodes.yaml
      path-info: /nodebalancers/{nodeBalancerId}/configs/{configId}/nodes
    parameters:
    - name: nodeBalancerId
      in: path
      description: |
        The ID of the NodeBalancer to access.
      example: '{{nodeBalancerId}}'
      x-akamai:
        file-path: parameters/node-balancer-id.yaml
      required: true
      schema:
        type: integer
    - name: configId
      in: path
      description: |
        The ID of the NodeBalancer config to access.
      example: '{{configId}}'
      x-akamai:
        file-path: parameters/config-id-node-balancer.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-node-balancer-node
      summary: Create a node
      tags:
      - Nodes
      description: |-
        Creates a NodeBalancer Node, a backend that can accept traffic for this NodeBalancer Config. Nodes are routed requests on the configured port based on their status.


        ---


        - __CLI__.

            ```
            linode-cli nodebalancers node-create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            nodebalancers:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli nodebalancers node-create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: nodebalancers:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the Node to create.
        required: true
        content:
          application/json:
            schema:
              required:
              - label
              - address
              allOf:
              - type: object
                description: |
                  A NodeBalancerNode represents a single backend serving requests for a single port of a NodeBalancer.  Nodes
                  are specific to NodeBalancer Configs, and serve traffic over their
                  private IP.  If the same Linode is serving traffic for more than
                  one port on the same NodeBalancer, one NodeBalancer Node is required
                  for each config (port) it should serve requests on.  For example,
                  if you have four backends, and each should response to both HTTP
                  and HTTPS requests, you will need two NodeBalancerConfigs (port
                  80 and port 443) and four backends each - one for each of the Linodes
                  serving requests for that port.
                x-akamai:
                  file-path: schemas/node-balancer-node.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This node's unique ID.
                    example: 54321
                    readOnly: true
                    x-linode-cli-display: 1
                  address:
                    type: string
                    description: |
                      The private IP Address where this backend can be reached. This _must_ be a private
                      IP address.
                    example: 192.168.210.120:80
                    format: ip
                    x-linode-cli-display: 3
                  config_id:
                    type: integer
                    description: |
                      __Read-only__ The NodeBalancer Config ID that this Node belongs to.
                    example: 4567
                    readOnly: true
                  label:
                    type: string
                    description: |
                      The label for this node.  This is for display purposes only.
                    example: node54321
                    minLength: 3
                    maxLength: 32
                    pattern: '[a-zA-Z0-9-_.]{3,32}'
                    x-linode-cli-display: 2
                  mode:
                    type: string
                    description: |-
                      The mode this NodeBalancer should use when sending traffic to this backend.

                      - If set to `accept` this backend is accepting traffic.
                      - If set to `reject` this backend will not receive traffic.
                      - If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.
                      - If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
                    example: accept
                    enum:
                    - accept
                    - reject
                    - drain
                    - backup
                    x-linode-cli-display: 6
                  nodebalancer_id:
                    type: integer
                    description: |
                      __Read-only__ The NodeBalancer ID that this Node belongs to.
                    example: 12345
                    readOnly: true
                  status:
                    type: string
                    description: |
                      __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer
                      Config.
                    example: UP
                    readOnly: true
                    enum:
                    - unknown
                    - UP
                    - DOWN
                    x-linode-cli-color:
                      DOWN: red
                      UP: green
                      default_: white
                      unknown: yellow
                    x-linode-cli-display: 4
                  weight:
                    type: integer
                    description: |
                      Used when picking a backend to serve a request and is not pinned to a single backend
                      yet.  Nodes with a higher weight will receive more traffic.
                    example: 50
                    minimum: 1
                    maximum: 255
                    x-linode-cli-display: 5
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Node created successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A NodeBalancerNode represents a single backend serving requests for a single port of a NodeBalancer.  Nodes
                  are specific to NodeBalancer Configs, and serve traffic over their
                  private IP.  If the same Linode is serving traffic for more than
                  one port on the same NodeBalancer, one NodeBalancer Node is required
                  for each config (port) it should serve requests on.  For example,
                  if you have four backends, and each should response to both HTTP
                  and HTTPS requests, you will need two NodeBalancerConfigs (port
                  80 and port 443) and four backends each - one for each of the Linodes
                  serving requests for that port.
                x-akamai:
                  file-path: schemas/node-balancer-node.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This node's unique ID.
                    example: 54321
                    readOnly: true
                    x-linode-cli-display: 1
                  address:
                    type: string
                    description: |
                      The private IP Address where this backend can be reached. This _must_ be a private
                      IP address.
                    example: 192.168.210.120:80
                    format: ip
                    x-linode-cli-display: 3
                  config_id:
                    type: integer
                    description: |
                      __Read-only__ The NodeBalancer Config ID that this Node belongs to.
                    example: 4567
                    readOnly: true
                  label:
                    type: string
                    description: |
                      The label for this node.  This is for display purposes only.
                    example: node54321
                    minLength: 3
                    maxLength: 32
                    pattern: '[a-zA-Z0-9-_.]{3,32}'
                    x-linode-cli-display: 2
                  mode:
                    type: string
                    description: |-
                      The mode this NodeBalancer should use when sending traffic to this backend.

                      - If set to `accept` this backend is accepting traffic.
                      - If set to `reject` this backend will not receive traffic.
                      - If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.
                      - If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
                    example: accept
                    enum:
                    - accept
                    - reject
                    - drain
                    - backup
                    x-linode-cli-display: 6
                  nodebalancer_id:
                    type: integer
                    description: |
                      __Read-only__ The NodeBalancer ID that this Node belongs to.
                    example: 12345
                    readOnly: true
                  status:
                    type: string
                    description: |
                      __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer
                      Config.
                    example: UP
                    readOnly: true
                    enum:
                    - unknown
                    - UP
                    - DOWN
                    x-linode-cli-color:
                      DOWN: red
                      UP: green
                      default_: white
                      unknown: yellow
                    x-linode-cli-display: 4
                  weight:
                    type: integer
                    description: |
                      Used when picking a backend to serve a request and is not pinned to a single backend
                      yet.  Nodes with a higher weight will receive more traffic.
                    example: 50
                    minimum: 1
                    maximum: 255
                    x-linode-cli-display: 5
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-node-balancer-node
      security:
      - personalAccessToken: []
      - oauth:
        - nodebalancers:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "address": "192.168.210.120:80",
                "label": "node54321",
                "weight": 50,
                "mode": "accept"
              }' \
              https://api.linode.com/v4/nodebalancers/12345/configs/4567/nodes
      - lang: CLI
        source: |-
          linode-cli nodebalancers node-create \
            12345 4567 \
            --address 192.168.210.120:80 \
            --label node54321 \
            --weight 50 \
            --mode accept
      x-linode-cli-action: node-create
      x-linode-grant: read_write
      x-original-op-id: createNodeBalancerNode
      x-original-op-title: Node Create
    get:
      operationId: get-node-balancer-config-nodes
      summary: List nodes
      tags:
      - Nodes
      description: |-
        Returns a paginated list of NodeBalancer nodes associated with this Config. These are the backends that will be sent traffic for this port.


        ---


        - __CLI__.

            ```
            linode-cli nodebalancers nodes-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            nodebalancers:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli nodebalancers nodes-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: nodebalancers:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of NodeBalancer nodes.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        A NodeBalancerNode represents a single backend serving requests for a single
                        port of a NodeBalancer.  Nodes are specific to NodeBalancer
                        Configs, and serve traffic over their private IP.  If the
                        same Linode is serving traffic for more than one port on the
                        same NodeBalancer, one NodeBalancer Node is required for each
                        config (port) it should serve requests on.  For example, if
                        you have four backends, and each should response to both HTTP
                        and HTTPS requests, you will need two NodeBalancerConfigs
                        (port 80 and port 443) and four backends each - one for each
                        of the Linodes serving requests for that port.
                      x-akamai:
                        file-path: schemas/node-balancer-node.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ This node's unique ID.
                          example: 54321
                          readOnly: true
                          x-linode-cli-display: 1
                        address:
                          type: string
                          description: |
                            The private IP Address where this backend can be reached. This _must_
                            be a private IP address.
                          example: 192.168.210.120:80
                          format: ip
                          x-linode-cli-display: 3
                        config_id:
                          type: integer
                          description: |
                            __Read-only__ The NodeBalancer Config ID that this Node belongs to.
                          example: 4567
                          readOnly: true
                        label:
                          type: string
                          description: |
                            The label for this node.  This is for display purposes only.
                          example: node54321
                          minLength: 3
                          maxLength: 32
                          pattern: '[a-zA-Z0-9-_.]{3,32}'
                          x-linode-cli-display: 2
                        mode:
                          type: string
                          description: |-
                            The mode this NodeBalancer should use when sending traffic to this backend.

                            - If set to `accept` this backend is accepting traffic.
                            - If set to `reject` this backend will not receive traffic.
                            - If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.
                            - If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
                          example: accept
                          enum:
                          - accept
                          - reject
                          - drain
                          - backup
                          x-linode-cli-display: 6
                        nodebalancer_id:
                          type: integer
                          description: |
                            __Read-only__ The NodeBalancer ID that this Node belongs to.
                          example: 12345
                          readOnly: true
                        status:
                          type: string
                          description: |
                            __Read-only__ The current status of this node, based on the configured
                            checks of its NodeBalancer Config.
                          example: UP
                          readOnly: true
                          enum:
                          - unknown
                          - UP
                          - DOWN
                          x-linode-cli-color:
                            DOWN: red
                            UP: green
                            default_: white
                            unknown: yellow
                          x-linode-cli-display: 4
                        weight:
                          type: integer
                          description: |
                            Used when picking a backend to serve a request and is not pinned to a single
                            backend yet.  Nodes with a higher weight will receive
                            more traffic.
                          example: 50
                          minimum: 1
                          maximum: 255
                          x-linode-cli-display: 5
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-node-balancer-config-nodes
      security:
      - personalAccessToken: []
      - oauth:
        - nodebalancers:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/nodebalancers/12345/configs/4567/nodes
      - lang: CLI
        source: linode-cli nodebalancers nodes-list 12345 4567
      x-linode-cli-action: nodes-list
      x-linode-grant: read_only
      x-original-op-id: getNodeBalancerConfigNodes
      x-original-op-title: Nodes List
    x-linode-cli-command: nodebalancers
  /nodebalancers/{nodeBalancerId}/configs/{configId}/nodes/{nodeId}:
    x-akamai:
      file-path: paths/config-node.yaml
      path-info: /nodebalancers/{nodeBalancerId}/configs/{configId}/nodes/{nodeId}
    parameters:
    - name: nodeBalancerId
      in: path
      description: |
        The ID of the NodeBalancer to access.
      example: '{{nodeBalancerId}}'
      x-akamai:
        file-path: parameters/node-balancer-id.yaml
      required: true
      schema:
        type: integer
    - name: configId
      in: path
      description: |
        The ID of the Config to access
      example: '{{configId}}'
      x-akamai:
        file-path: parameters/config-id.yaml
      required: true
      schema:
        type: integer
    - name: nodeId
      in: path
      description: |
        The ID of the Node to access
      example: '{{nodeId}}'
      x-akamai:
        file-path: parameters/node-id.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-node-balancer-node
      summary: Get a node balancer's node
      tags:
      - Nodes
      description: |-
        Returns information about a single Node, a backend for this NodeBalancer's configured port.


        ---


        - __CLI__.

            ```
            linode-cli nodebalancers node-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            nodebalancers:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli nodebalancers node-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: nodebalancers:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of NodeBalancer nodes.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A NodeBalancerNode represents a single backend serving requests for a single port of a NodeBalancer.  Nodes
                  are specific to NodeBalancer Configs, and serve traffic over their
                  private IP.  If the same Linode is serving traffic for more than
                  one port on the same NodeBalancer, one NodeBalancer Node is required
                  for each config (port) it should serve requests on.  For example,
                  if you have four backends, and each should response to both HTTP
                  and HTTPS requests, you will need two NodeBalancerConfigs (port
                  80 and port 443) and four backends each - one for each of the Linodes
                  serving requests for that port.
                x-akamai:
                  file-path: schemas/node-balancer-node.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This node's unique ID.
                    example: 54321
                    readOnly: true
                    x-linode-cli-display: 1
                  address:
                    type: string
                    description: |
                      The private IP Address where this backend can be reached. This _must_ be a private
                      IP address.
                    example: 192.168.210.120:80
                    format: ip
                    x-linode-cli-display: 3
                  config_id:
                    type: integer
                    description: |
                      __Read-only__ The NodeBalancer Config ID that this Node belongs to.
                    example: 4567
                    readOnly: true
                  label:
                    type: string
                    description: |
                      The label for this node.  This is for display purposes only.
                    example: node54321
                    minLength: 3
                    maxLength: 32
                    pattern: '[a-zA-Z0-9-_.]{3,32}'
                    x-linode-cli-display: 2
                  mode:
                    type: string
                    description: |-
                      The mode this NodeBalancer should use when sending traffic to this backend.

                      - If set to `accept` this backend is accepting traffic.
                      - If set to `reject` this backend will not receive traffic.
                      - If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.
                      - If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
                    example: accept
                    enum:
                    - accept
                    - reject
                    - drain
                    - backup
                    x-linode-cli-display: 6
                  nodebalancer_id:
                    type: integer
                    description: |
                      __Read-only__ The NodeBalancer ID that this Node belongs to.
                    example: 12345
                    readOnly: true
                  status:
                    type: string
                    description: |
                      __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer
                      Config.
                    example: UP
                    readOnly: true
                    enum:
                    - unknown
                    - UP
                    - DOWN
                    x-linode-cli-color:
                      DOWN: red
                      UP: green
                      default_: white
                      unknown: yellow
                    x-linode-cli-display: 4
                  weight:
                    type: integer
                    description: |
                      Used when picking a backend to serve a request and is not pinned to a single backend
                      yet.  Nodes with a higher weight will receive more traffic.
                    example: 50
                    minimum: 1
                    maximum: 255
                    x-linode-cli-display: 5
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-node-balancer-node
      security:
      - personalAccessToken: []
      - oauth:
        - nodebalancers:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/nodebalancers/12345/configs/4567/nodes/54321
      - lang: CLI
        source: linode-cli nodebalancers node-view 12345 4567 54321
      x-linode-cli-action: node-view
      x-linode-grant: read_only
      x-original-op-id: getNodeBalancerNode
      x-original-op-title: Node View
    put:
      operationId: put-node-balancer-node
      summary: Update a node
      tags:
      - Nodes
      description: |-
        Updates information about a Node, a backend for this NodeBalancer's configured port.


        ---


        - __CLI__.

            ```
            linode-cli nodebalancers node-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            nodebalancers:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli nodebalancers node-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: nodebalancers:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The fields to update.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                A NodeBalancerNode represents a single backend serving requests for a single port of a NodeBalancer.  Nodes
                are specific to NodeBalancer Configs, and serve traffic over their
                private IP.  If the same Linode is serving traffic for more than one
                port on the same NodeBalancer, one NodeBalancer Node is required for
                each config (port) it should serve requests on.  For example, if you
                have four backends, and each should response to both HTTP and HTTPS
                requests, you will need two NodeBalancerConfigs (port 80 and port
                443) and four backends each - one for each of the Linodes serving
                requests for that port.
              x-akamai:
                file-path: schemas/node-balancer-node.yaml
              additionalProperties: false
              properties:
                id:
                  type: integer
                  description: |
                    __Read-only__ This node's unique ID.
                  example: '{{id}}'
                  readOnly: true
                  x-linode-cli-display: 1
                address:
                  type: string
                  description: |
                    The private IP Address where this backend can be reached. This _must_ be a private IP
                    address.
                  example: '{{address}}'
                  format: ip
                  x-linode-cli-display: 3
                config_id:
                  type: integer
                  description: |
                    __Read-only__ The NodeBalancer Config ID that this Node belongs to.
                  example: '{{config_id}}'
                  readOnly: true
                label:
                  type: string
                  description: |
                    The label for this node.  This is for display purposes only.
                  example: '{{label}}'
                  minLength: 3
                  maxLength: 32
                  pattern: '[a-zA-Z0-9-_.]{3,32}'
                  x-linode-cli-display: 2
                mode:
                  type: string
                  description: |-
                    The mode this NodeBalancer should use when sending traffic to this backend.

                    - If set to `accept` this backend is accepting traffic.
                    - If set to `reject` this backend will not receive traffic.
                    - If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.
                    - If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
                  example: '{{mode}}'
                  enum:
                  - accept
                  - reject
                  - drain
                  - backup
                  x-linode-cli-display: 6
                nodebalancer_id:
                  type: integer
                  description: |
                    __Read-only__ The NodeBalancer ID that this Node belongs to.
                  example: '{{nodebalancer_id}}'
                  readOnly: true
                status:
                  type: string
                  description: |
                    __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer
                    Config.
                  example: '{{status}}'
                  readOnly: true
                  enum:
                  - unknown
                  - UP
                  - DOWN
                  x-linode-cli-color:
                    DOWN: red
                    UP: green
                    default_: white
                    unknown: yellow
                  x-linode-cli-display: 4
                weight:
                  type: integer
                  description: |
                    Used when picking a backend to serve a request and is not pinned to a single backend yet.  Nodes
                    with a higher weight will receive more traffic.
                  example: '{{weight}}'
                  minimum: 1
                  maximum: 255
                  x-linode-cli-display: 5
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Node updated successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A NodeBalancerNode represents a single backend serving requests for a single port of a NodeBalancer.  Nodes
                  are specific to NodeBalancer Configs, and serve traffic over their
                  private IP.  If the same Linode is serving traffic for more than
                  one port on the same NodeBalancer, one NodeBalancer Node is required
                  for each config (port) it should serve requests on.  For example,
                  if you have four backends, and each should response to both HTTP
                  and HTTPS requests, you will need two NodeBalancerConfigs (port
                  80 and port 443) and four backends each - one for each of the Linodes
                  serving requests for that port.
                x-akamai:
                  file-path: schemas/node-balancer-node.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This node's unique ID.
                    example: 54321
                    readOnly: true
                    x-linode-cli-display: 1
                  address:
                    type: string
                    description: |
                      The private IP Address where this backend can be reached. This _must_ be a private
                      IP address.
                    example: 192.168.210.120:80
                    format: ip
                    x-linode-cli-display: 3
                  config_id:
                    type: integer
                    description: |
                      __Read-only__ The NodeBalancer Config ID that this Node belongs to.
                    example: 4567
                    readOnly: true
                  label:
                    type: string
                    description: |
                      The label for this node.  This is for display purposes only.
                    example: node54321
                    minLength: 3
                    maxLength: 32
                    pattern: '[a-zA-Z0-9-_.]{3,32}'
                    x-linode-cli-display: 2
                  mode:
                    type: string
                    description: |-
                      The mode this NodeBalancer should use when sending traffic to this backend.

                      - If set to `accept` this backend is accepting traffic.
                      - If set to `reject` this backend will not receive traffic.
                      - If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.
                      - If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
                    example: accept
                    enum:
                    - accept
                    - reject
                    - drain
                    - backup
                    x-linode-cli-display: 6
                  nodebalancer_id:
                    type: integer
                    description: |
                      __Read-only__ The NodeBalancer ID that this Node belongs to.
                    example: 12345
                    readOnly: true
                  status:
                    type: string
                    description: |
                      __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer
                      Config.
                    example: UP
                    readOnly: true
                    enum:
                    - unknown
                    - UP
                    - DOWN
                    x-linode-cli-color:
                      DOWN: red
                      UP: green
                      default_: white
                      unknown: yellow
                    x-linode-cli-display: 4
                  weight:
                    type: integer
                    description: |
                      Used when picking a backend to serve a request and is not pinned to a single backend
                      yet.  Nodes with a higher weight will receive more traffic.
                    example: 50
                    minimum: 1
                    maximum: 255
                    x-linode-cli-display: 5
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-node-balancer-node
      security:
      - personalAccessToken: []
      - oauth:
        - nodebalancers:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "address": "192.168.210.120:80",
                "label": "node54321",
                "weight": 50,
                "mode": "accept"
              }' \
              https://api.linode.com/v4/nodebalancers/12345/configs/4567/nodes/54321
      - lang: CLI
        source: |-
          linode-cli nodebalancers node-update \
            12345 4567 54321 \
            --address 192.168.210.120:80 \
            --label node54321 \
            --weight 50 \
            --mode accept
      x-linode-cli-action: node-update
      x-linode-grant: read_write
      x-original-op-id: updateNodeBalancerNode
      x-original-op-title: Node Update
    delete:
      operationId: delete-node-balancer-config-node
      summary: Delete a node balancer's node
      tags:
      - Nodes
      description: |-
        Deletes a Node from this Config. This backend will no longer receive traffic for the configured port of this NodeBalancer.

        This does not change or remove the Linode whose address was used in the creation of this Node.


        ---


        - __CLI__.

            ```
            linode-cli nodebalancers node-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            nodebalancers:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli nodebalancers node-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: nodebalancers:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Node deleted successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-node-balancer-config-node
      security:
      - personalAccessToken: []
      - oauth:
        - nodebalancers:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/nodebalancers/12345/configs/4567/nodes/54321
      - lang: CLI
        source: |-
          linode-cli nodebalancers node-delete \
            12345 4567 54321
      x-linode-cli-action: node-delete
      x-linode-grant: read_write
      x-original-op-id: deleteNodeBalancerConfigNode
      x-original-op-title: Node Delete
    x-linode-cli-command: nodebalancers
  /nodebalancers/{nodeBalancerId}/configs/{configId}/rebuild:
    x-akamai:
      file-path: paths/config-rebuild.yaml
      path-info: /nodebalancers/{nodeBalancerId}/configs/{configId}/rebuild
    parameters:
    - name: nodeBalancerId
      in: path
      description: |
        The ID of the NodeBalancer to access.
      example: '{{nodeBalancerId}}'
      x-akamai:
        file-path: parameters/node-balancer-id.yaml
      required: true
      schema:
        type: integer
    - name: configId
      in: path
      description: |
        The ID of the Config to access
      example: '{{configId}}'
      x-akamai:
        file-path: parameters/config-id.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-rebuild-node-balancer-config
      summary: Rebuild a config
      tags:
      - Configurations
      description: |-
        Rebuilds a NodeBalancer Config and its Nodes that you have permission to modify.

        Use this operation to update a NodeBalancer's Config and Nodes with a single request.


        ---


        - __CLI__.

            ```
            linode-cli nodebalancers config-rebuild
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            nodebalancers:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli nodebalancers config-rebuild
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: nodebalancers:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the NodeBalancer Config to rebuild.
        required: true
        content:
          application/json:
            schema:
              allOf:
              - type: object
                description: |
                  A NodeBalancer config represents the configuration of this NodeBalancer on a single port.  For example,
                  a NodeBalancer Config on port 80 would typically represent how this
                  NodeBalancer response to HTTP requests. NodeBalancer configs have
                  a list of backends, called "nodes," that they forward requests between
                  based on their configuration.
                x-akamai:
                  file-path: schemas/node-balancer-config.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This config's unique ID
                    example: 4567
                    readOnly: true
                    x-linode-cli-display: 1
                  algorithm:
                    type: string
                    description: |
                      What algorithm this NodeBalancer should use for routing traffic to backends.
                    example: roundrobin
                    default: roundrobin
                    enum:
                    - roundrobin
                    - leastconn
                    - source
                    x-linode-cli-display: 4
                  check:
                    type: string
                    description: |-
                      The type of check to perform against backends to ensure they are serving requests. This is used to determine if backends are up or down.

                      - If `none` no check is performed.
                      - `connection` requires only a connection to the backend to succeed.
                      - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
                    example: http_body
                    default: none
                    enum:
                    - none
                    - connection
                    - http
                    - http_body
                  check_attempts:
                    type: integer
                    description: |
                      How many times to attempt a check before considering a backend to be down.
                    example: 3
                    default: 3
                    minimum: 1
                    maximum: 30
                  check_body:
                    type: string
                    description: |
                      This value must be present in the response body of the check in order for it to
                      pass. If this value is not present in the response body of a
                      check request, the backend is considered to be down.
                    example: it works
                  check_interval:
                    type: integer
                    description: |-
                      How often, in seconds, to check that backends are up and serving requests.

                      Must be greater than `check_timeout`.
                    example: 90
                    default: 31
                  check_passive:
                    type: boolean
                    description: |
                      If true, any response from this backend with a `5xx` status code will be enough for
                      it to be considered unhealthy and taken out of rotation.
                    example: true
                    default: true
                    x-linode-cli-display: 6
                  check_path:
                    type: string
                    description: |
                      The URL path to check on each backend. If the backend does not respond to this request
                      it is considered to be down.
                    example: /test
                    pattern: ^[a-zA-Z0-9\/\-%?&=.]*$
                  check_timeout:
                    type: integer
                    description: |-
                      How long, in seconds, to wait for a check attempt before considering it failed.

                      Must be less than `check_interval`.
                    example: 10
                    default: 30
                    minimum: 1
                    maximum: 30
                  cipher_suite:
                    type: string
                    description: |-
                      What ciphers to use for SSL connections served by this NodeBalancer.

                      - `legacy` is considered insecure and should only be used if necessary.
                    example: recommended
                    default: recommended
                    enum:
                    - recommended
                    - legacy
                    x-linode-cli-color:
                      default_: white
                      legacy: red
                    x-linode-cli-display: 7
                  nodebalancer_id:
                    type: integer
                    description: |
                      __Read-only__ The ID for the NodeBalancer this config belongs to.
                    example: 12345
                    readOnly: true
                  nodes_status:
                    type: object
                    description: |
                      __Read-only__ A structure containing information about the health of the backends for
                      this port.  This information is updated periodically as checks
                      are performed against backends.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      down:
                        type: integer
                        description: |
                          __Read-only__ The number of backends considered to be "DOWN" and unhealthy.  These
                          are not in rotation, and not serving requests.
                        example: 0
                        readOnly: true
                      up:
                        type: integer
                        description: |
                          __Read-only__ The number of backends considered to be "UP" and healthy, and
                          that are serving requests.
                        example: 4
                        readOnly: true
                    x-linode-cli-display: 10
                  port:
                    type: integer
                    description: |
                      The port this Config is for. These values must be unique across configs on a single
                      NodeBalancer (you can't have two configs for port 80, for example).  While
                      some ports imply some protocols, no enforcement is done and
                      you may configure your NodeBalancer however is useful to you.
                      For example, while port 443 is generally used for HTTPS, you
                      do not need SSL configured to have a NodeBalancer listening
                      on port 443.
                    example: 80
                    default: 80
                    minimum: 1
                    maximum: 65535
                    x-linode-cli-display: 2
                  protocol:
                    type: string
                    description: |-
                      The protocol this port is configured to serve.

                      - The `http` and `tcp` protocols do not support `ssl_cert` and `ssl_key`.

                      - The `https` protocol is mutually required with `ssl_cert` and `ssl_key`.

                      Review our guide on [Available Protocols](https://www.linode.com/docs/products/networking/nodebalancers/guides/protocols/) for information on protocol features.
                    example: http
                    default: http
                    enum:
                    - http
                    - https
                    - tcp
                    x-linode-cli-display: 3
                  proxy_protocol:
                    type: string
                    description: |-
                      ProxyProtocol is a TCP extension that sends initial TCP connection information such as source/destination IPs and ports to backend devices. This information would be lost otherwise. Backend devices must be configured to work with ProxyProtocol if enabled.

                      - If omitted, or set to `none`, the NodeBalancer doesn't send any auxiliary data over TCP connections. This is the default.
                      - If set to `v1`, the human-readable header format (Version 1) is used. Requires `tcp` protocol.
                      - If set to `v2`, the binary header format (Version 2) is used. Requires `tcp` protocol.
                    example: none
                    default: none
                    enum:
                    - none
                    - v1
                    - v2
                  ssl_cert:
                    type: string
                    description: |2-

                      The PEM-formatted public SSL certificate (or the combined PEM-formatted SSL certificate and Certificate Authority chain) that should be served on this NodeBalancerConfig's port.

                      Line breaks must be represented as "\n" in the string for requests (but not when using the Linode CLI).

                      [Diffie-Hellman Parameters](https://www.linode.com/docs/products/networking/nodebalancers/guides/ssl-termination/#diffie-hellman-parameters) can be included in this value to enable forward secrecy.

                      The contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.

                      The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.
                    example: <REDACTED>
                    nullable: true
                    format: ssl-cert
                  ssl_commonname:
                    type: string
                    description: |
                      __Read-only__ The read-only common name automatically derived from the SSL certificate assigned
                      to this NodeBalancerConfig. Please refer to this field to verify
                      that the appropriate certificate is assigned to your NodeBalancerConfig.
                    example: www.example.com
                    readOnly: true
                    x-linode-cli-display: 8
                  ssl_fingerprint:
                    type: string
                    description: |
                      __Read-only__ The read-only SHA1-encoded fingerprint automatically derived from the SSL
                      certificate assigned to this NodeBalancerConfig. Please refer
                      to this field to verify that the appropriate certificate is
                      assigned to your NodeBalancerConfig.
                    example: 00:01:02:03:04:05:06:07:08:09:0A:0B:0C:0D:0E:0F:10:11:12:13
                    readOnly: true
                    x-linode-cli-display: 9
                  ssl_key:
                    type: string
                    description: |-
                      The PEM-formatted private key for the SSL certificate set in the `ssl_cert` field.

                      Line breaks must be represented as "\n" in the string for requests (but not when using the Linode CLI).

                      The contents of this field will not be shown in any responses that display
                      the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field
                      appears.

                      The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig
                      response are automatically derived from your certificate. Please refer to these fields to
                      verify that the appropriate certificate was assigned to your NodeBalancerConfig.
                    example: <REDACTED>
                    nullable: true
                    format: ssl-key
                  stickiness:
                    type: string
                    description: |-
                      Controls how session stickiness is handled on this port.

                      - If set to `none` connections will always be assigned a backend based on the algorithm configured.
                      - If set to `table` sessions from the same remote address will be routed to the same backend.
                      - For HTTP or HTTPS clients, `http_cookie` allows sessions to be routed to the same backend based on a cookie set by the NodeBalancer.
                    example: http_cookie
                    default: none
                    enum:
                    - none
                    - table
                    - http_cookie
                    x-linode-cli-display: 5
              - type: object
                additionalProperties: false
                required:
                - nodes
                properties:
                  nodes:
                    type: array
                    description: |-
                      The NodeBalancer Node(s) that serve this Config.

                      Some considerations for Nodes when rebuilding a config:

                        - Current Nodes excluded from the request body will be deleted from the Config.
                        - Current Nodes (identified by their Node ID) will be updated.
                        - New Nodes (included without a Node ID) will be created.
                    items:
                      type: object
                      description: |
                        NodeBalancer Node request object including ID.
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The unique ID of the Node to update.
                          example: 54321
                        address:
                          type: string
                          description: |
                            The private IP Address where this backend can be reached. This _must_
                            be a private IP address.
                          example: 192.168.210.120:80
                          format: ip
                          x-linode-cli-display: 3
                        label:
                          type: string
                          description: |
                            The label for this node.  This is for display purposes only.
                          example: node54321
                          minLength: 3
                          maxLength: 32
                          pattern: '[a-zA-Z0-9-_.]{3,32}'
                          x-linode-cli-display: 2
                        mode:
                          type: string
                          description: |-
                            The mode this NodeBalancer should use when sending traffic to this backend.

                            - If set to `accept` this backend is accepting traffic.
                            - If set to `reject` this backend will not receive traffic.
                            - If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.
                            - If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
                          example: accept
                          enum:
                          - accept
                          - reject
                          - drain
                          - backup
                          x-linode-cli-display: 6
                        weight:
                          type: integer
                          description: |
                            Used when picking a backend to serve a request and is not pinned to a single
                            backend yet.  Nodes with a higher weight will receive
                            more traffic.
                          example: 50
                          minimum: 1
                          maximum: 255
                          x-linode-cli-display: 5
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            NodeBalancer created successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A NodeBalancer config represents the configuration of this NodeBalancer on a single port.  For example,
                  a NodeBalancer Config on port 80 would typically represent how this
                  NodeBalancer response to HTTP requests. NodeBalancer configs have
                  a list of backends, called "nodes," that they forward requests between
                  based on their configuration.
                x-akamai:
                  file-path: schemas/node-balancer-config.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This config's unique ID
                    example: 4567
                    readOnly: true
                    x-linode-cli-display: 1
                  algorithm:
                    type: string
                    description: |
                      What algorithm this NodeBalancer should use for routing traffic to backends.
                    example: roundrobin
                    default: roundrobin
                    enum:
                    - roundrobin
                    - leastconn
                    - source
                    x-linode-cli-display: 4
                  check:
                    type: string
                    description: |-
                      The type of check to perform against backends to ensure they are serving requests. This is used to determine if backends are up or down.

                      - If `none` no check is performed.
                      - `connection` requires only a connection to the backend to succeed.
                      - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
                    example: http_body
                    default: none
                    enum:
                    - none
                    - connection
                    - http
                    - http_body
                  check_attempts:
                    type: integer
                    description: |
                      How many times to attempt a check before considering a backend to be down.
                    example: 3
                    default: 3
                    minimum: 1
                    maximum: 30
                  check_body:
                    type: string
                    description: |
                      This value must be present in the response body of the check in order for it to
                      pass. If this value is not present in the response body of a
                      check request, the backend is considered to be down.
                    example: it works
                  check_interval:
                    type: integer
                    description: |-
                      How often, in seconds, to check that backends are up and serving requests.

                      Must be greater than `check_timeout`.
                    example: 90
                    default: 31
                  check_passive:
                    type: boolean
                    description: |
                      If true, any response from this backend with a `5xx` status code will be enough for
                      it to be considered unhealthy and taken out of rotation.
                    example: true
                    default: true
                    x-linode-cli-display: 6
                  check_path:
                    type: string
                    description: |
                      The URL path to check on each backend. If the backend does not respond to this request
                      it is considered to be down.
                    example: /test
                    pattern: ^[a-zA-Z0-9\/\-%?&=.]*$
                  check_timeout:
                    type: integer
                    description: |-
                      How long, in seconds, to wait for a check attempt before considering it failed.

                      Must be less than `check_interval`.
                    example: 10
                    default: 30
                    minimum: 1
                    maximum: 30
                  cipher_suite:
                    type: string
                    description: |-
                      What ciphers to use for SSL connections served by this NodeBalancer.

                      - `legacy` is considered insecure and should only be used if necessary.
                    example: recommended
                    default: recommended
                    enum:
                    - recommended
                    - legacy
                    x-linode-cli-color:
                      default_: white
                      legacy: red
                    x-linode-cli-display: 7
                  nodebalancer_id:
                    type: integer
                    description: |
                      __Read-only__ The ID for the NodeBalancer this config belongs to.
                    example: 12345
                    readOnly: true
                  nodes_status:
                    type: object
                    description: |
                      __Read-only__ A structure containing information about the health of the backends for
                      this port.  This information is updated periodically as checks
                      are performed against backends.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      down:
                        type: integer
                        description: |
                          __Read-only__ The number of backends considered to be "DOWN" and unhealthy.  These
                          are not in rotation, and not serving requests.
                        example: 0
                        readOnly: true
                      up:
                        type: integer
                        description: |
                          __Read-only__ The number of backends considered to be "UP" and healthy, and
                          that are serving requests.
                        example: 4
                        readOnly: true
                    x-linode-cli-display: 10
                  port:
                    type: integer
                    description: |
                      The port this Config is for. These values must be unique across configs on a single
                      NodeBalancer (you can't have two configs for port 80, for example).  While
                      some ports imply some protocols, no enforcement is done and
                      you may configure your NodeBalancer however is useful to you.
                      For example, while port 443 is generally used for HTTPS, you
                      do not need SSL configured to have a NodeBalancer listening
                      on port 443.
                    example: 80
                    default: 80
                    minimum: 1
                    maximum: 65535
                    x-linode-cli-display: 2
                  protocol:
                    type: string
                    description: |-
                      The protocol this port is configured to serve.

                      - The `http` and `tcp` protocols do not support `ssl_cert` and `ssl_key`.

                      - The `https` protocol is mutually required with `ssl_cert` and `ssl_key`.

                      Review our guide on [Available Protocols](https://www.linode.com/docs/products/networking/nodebalancers/guides/protocols/) for information on protocol features.
                    example: http
                    default: http
                    enum:
                    - http
                    - https
                    - tcp
                    x-linode-cli-display: 3
                  proxy_protocol:
                    type: string
                    description: |-
                      ProxyProtocol is a TCP extension that sends initial TCP connection information such as source/destination IPs and ports to backend devices. This information would be lost otherwise. Backend devices must be configured to work with ProxyProtocol if enabled.

                      - If omitted, or set to `none`, the NodeBalancer doesn't send any auxiliary data over TCP connections. This is the default.
                      - If set to `v1`, the human-readable header format (Version 1) is used. Requires `tcp` protocol.
                      - If set to `v2`, the binary header format (Version 2) is used. Requires `tcp` protocol.
                    example: none
                    default: none
                    enum:
                    - none
                    - v1
                    - v2
                  ssl_cert:
                    type: string
                    description: |2-

                      The PEM-formatted public SSL certificate (or the combined PEM-formatted SSL certificate and Certificate Authority chain) that should be served on this NodeBalancerConfig's port.

                      Line breaks must be represented as "\n" in the string for requests (but not when using the Linode CLI).

                      [Diffie-Hellman Parameters](https://www.linode.com/docs/products/networking/nodebalancers/guides/ssl-termination/#diffie-hellman-parameters) can be included in this value to enable forward secrecy.

                      The contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.

                      The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.
                    example: <REDACTED>
                    nullable: true
                    format: ssl-cert
                  ssl_commonname:
                    type: string
                    description: |
                      __Read-only__ The read-only common name automatically derived from the SSL certificate assigned
                      to this NodeBalancerConfig. Please refer to this field to verify
                      that the appropriate certificate is assigned to your NodeBalancerConfig.
                    example: www.example.com
                    readOnly: true
                    x-linode-cli-display: 8
                  ssl_fingerprint:
                    type: string
                    description: |
                      __Read-only__ The read-only SHA1-encoded fingerprint automatically derived from the SSL
                      certificate assigned to this NodeBalancerConfig. Please refer
                      to this field to verify that the appropriate certificate is
                      assigned to your NodeBalancerConfig.
                    example: 00:01:02:03:04:05:06:07:08:09:0A:0B:0C:0D:0E:0F:10:11:12:13
                    readOnly: true
                    x-linode-cli-display: 9
                  ssl_key:
                    type: string
                    description: |-
                      The PEM-formatted private key for the SSL certificate set in the `ssl_cert` field.

                      Line breaks must be represented as "\n" in the string for requests (but not when using the Linode CLI).

                      The contents of this field will not be shown in any responses that display
                      the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field
                      appears.

                      The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig
                      response are automatically derived from your certificate. Please refer to these fields to
                      verify that the appropriate certificate was assigned to your NodeBalancerConfig.
                    example: <REDACTED>
                    nullable: true
                    format: ssl-key
                  stickiness:
                    type: string
                    description: |-
                      Controls how session stickiness is handled on this port.

                      - If set to `none` connections will always be assigned a backend based on the algorithm configured.
                      - If set to `table` sessions from the same remote address will be routed to the same backend.
                      - For HTTP or HTTPS clients, `http_cookie` allows sessions to be routed to the same backend based on a cookie set by the NodeBalancer.
                    example: http_cookie
                    default: none
                    enum:
                    - none
                    - table
                    - http_cookie
                    x-linode-cli-display: 5
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-rebuild-node-balancer-config
      security:
      - personalAccessToken: []
      - oauth:
        - nodebalancers:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "port": 1234,
                "protocol": "https",
                "algorithm": "roundrobin",
                "stickiness": "http_cookie",
                "check": "http_body",
                "check_interval": 90,
                "check_timeout": 10,
                "check_attempts": 3,
                "check_path": "/test",
                "check_body": "it works",
                "check_passive": true,
                "proxy_protocol": "none",
                "cipher_suite": "recommended",
                "ssl_cert": "-----BEGIN CERTIFICATE-----\nCERTIFICATE_INFORMATION\n-----END CERTIFICATE-----",
                "ssl_key": "-----BEGIN PRIVATE KEY-----\nPRIVATE_KEY_INFORMATION\n-----END PRIVATE KEY-----",
                "nodes": [
                  {
                    "id": 54321,
                    "address": "192.168.210.120:80",
                    "label": "node1",
                    "weight": 50,
                    "mode": "accept"
                  },
                  {
                    "address": "192.168.210.122:81",
                    "label": "node2",
                    "weight": 50,
                    "mode": "accept"
                  }
                ]
              }' \
              https://api.linode.com/v4/nodebalancers/12345/configs/4567/rebuild
      - lang: CLI
        source: |-
          linode-cli nodebalancers config-rebuild \
            12345 4567 \
            --port 443 \
            --protocol https \
            --algorithm roundrobin \
            --stickiness http_cookie \
            --check http_body \
            --check_interval 90 \
            --check_timeout 10 \
            --check_attempts 3 \
            --check_path "/test" \
            --check_body "it works" \
            --check_passive true \
            --proxy_protocol "none" \
            --ssl_cert "-----BEGIN CERTIFICATE-----
                        CERTIFICATE_INFORMATION
                        -----END CERTIFICATE-----" \
            --ssl_key "-----BEGIN PRIVATE KEY-----
                       PRIVATE_KEY_INFORMATION
                       -----END PRIVATE KEY-----" \
            --cipher_suite recommended \
            --nodes '{"address":"192.168.210.120:80","label":"node1","weight":50,"mode":"accept"}' \
            --nodes '{"address":"192.168.210.122:80","label":"node2","weight":50,"mode":"accept"}'
      x-linode-cli-action: config-rebuild
      x-linode-grant: add_nodebalancers
      x-original-op-id: rebuildNodeBalancerConfig
      x-original-op-title: Config Rebuild
    x-linode-cli-command: nodebalancers
  /nodebalancers/{nodeBalancerId}/firewalls:
    x-akamai:
      file-path: paths/node-balancer-firewalls.yaml
      path-info: /nodebalancers/{nodeBalancerId}/firewalls
    parameters:
    - name: nodeBalancerId
      in: path
      description: |
        The ID of the NodeBalancer to access.
      example: '{{nodeBalancerId}}'
      x-akamai:
        file-path: parameters/node-balancer-id.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-node-balancer-firewalls
      summary: List node balancer firewalls
      tags:
      - Firewalls
      description: |-
        View information for Firewalls assigned to this NodeBalancer.


        ---


        - __CLI__.

            ```
            linode-cli nodebalancers firewalls
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            nodebalancers:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli nodebalancers firewalls
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: nodebalancers:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated list of Firewalls assigned to this NodeBalancer.
          content:
            application/json:
              schema:
                allOf:
                - type: object
                  description: |
                    An envelope for paginated response. When accessing a collection through a GET endpoint,
                    the results are wrapped in this envelope which includes metadata
                    about those results. Results are presented within a `data` array.
                    See [Pagination](https://techdocs.akamai.com/linode-api/reference/pagination)
                    for more information.
                  x-akamai:
                    file-path: schemas/pagination-envelope.yaml
                  additionalProperties: false
                  properties:
                    page:
                      type: integer
                      description: |
                        __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    pages:
                      type: integer
                      description: |
                        __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    results:
                      type: integer
                      description: |
                        __Read-only__ The total number of results.
                      example: 1
                      readOnly: true
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        type: object
                        description: |
                          A resource that controls incoming and outgoing network traffic to a compute
                          service. Only one enabled Firewall can be attached to a
                          particular service at any given time. [Create a firewall
                          device](https://techdocs.akamai.com/linode-api/reference/post-firewall-device)
                          to assign a Firewall to a service. Currently, Firewalls
                          can assigned to Linode compute instances and NodeBalancers.
                        x-akamai:
                          file-path: schemas/firewall.yaml
                        additionalProperties: false
                        properties:
                          id:
                            type: integer
                            description: |
                              __Read-only__ The Firewall's unique ID.
                            example: 123
                            readOnly: true
                            x-linode-cli-display: 1
                            x-linode-filterable: true
                          tags:
                            type: array
                            description: |
                              An array of tags applied to this object. Tags are for organizational
                              purposes only.
                            example:
                            - example tag
                            - another example
                            items:
                              type: string
                            x-linode-filterable: true
                          created:
                            type: string
                            description: |
                              __Read-only__ When this Firewall was created.
                            example: '2018-01-01T00:01:01'
                            readOnly: true
                            format: date-time
                            x-linode-cli-display: 4
                            x-linode-filterable: true
                          label:
                            type: string
                            description: |-
                              The Firewall's label, for display purposes only.

                              Firewall labels have the following constraints:

                                - Must begin and end with an alphanumeric character.
                                - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                                - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
                                - Must be between 3 and 32 characters.
                                - Must be unique.
                            example: firewall123
                            minLength: 3
                            maxLength: 32
                            pattern: ^[a-zA-Z]((?!--|__|..)[a-zA-Z0-9-_.])+$
                            x-linode-cli-display: 2
                            x-linode-filterable: true
                          rules:
                            type: object
                            description: |-
                              The inbound and outbound access rules to apply to the Firewall.

                              A Firewall may have up to 25 rules across its inbound and outbound rulesets.

                              Multiple rules are applied in order. If two rules conflict, the first rule takes precedence. For example, if the first rule accepts inbound traffic from an address, and the second rule drops inbound traffic the same address, the first rule applies and inbound traffic from that address is accepted.
                            additionalProperties: false
                            properties:
                              inbound:
                                type: array
                                description: |
                                  The inbound rules for the firewall, as a JSON array.
                                items:
                                  type: object
                                  description: |
                                    One of a Firewall's inbound or outbound access rules.
                                    The `ports` property can be used to allow traffic
                                    on a comma-separated list of different ports.
                                  x-akamai:
                                    file-path: schemas/firewall-rule-config.yaml
                                  additionalProperties: false
                                  properties:
                                    description:
                                      type: string
                                      description: |
                                        Used to describe this rule. For display purposes
                                        only.
                                      example: An example firewall rule description.
                                      minLength: 1
                                      maxLength: 100
                                    action:
                                      type: string
                                      description: |
                                        Controls whether traffic is accepted or dropped by
                                        this rule. Overrides the Firewall's `inbound_policy`
                                        if this is an inbound rule, or the `outbound_policy`
                                        if this is an outbound rule.
                                      example: ACCEPT
                                      enum:
                                      - ACCEPT
                                      - DROP
                                    addresses:
                                      type: object
                                      description: |-
                                        The IPv4 and/or IPv6 addresses affected by this rule. A Rule can have up to 255 total addresses or networks listed across its IPv4 and IPv6 arrays. A network and a single IP are treated as equivalent when accounting for this limit.

                                        Must contain `ipv4`, `ipv6`, or both.
                                      additionalProperties: false
                                      properties:
                                        ipv4:
                                          type: array
                                          description: |-
                                            A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.

                                            If "0.0.0.0/0" is included in this list, all IPv4 addresses are affected by this rule.
                                          example:
                                          - 192.0.2.0/24
                                          - 198.51.100.2/32
                                          items:
                                            type: string
                                        ipv6:
                                          type: array
                                          description: |-
                                            A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.

                                            If "::/0" is included in this list, all IPv6 addresses are affected by this rule.
                                          example:
                                          - 2001:DB8::/128
                                          items:
                                            type: string
                                    label:
                                      type: string
                                      description: |
                                        Used to identify this rule. For display purposes
                                        only.
                                      example: firewallrule123
                                      minLength: 3
                                      maxLength: 32
                                    ports:
                                      type: string
                                      description: |-
                                        A string representing the port or ports affected by this rule:

                                        - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                                        - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                                        - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port "080" is not allowed.
                                        - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                                        - If no ports are configured, all ports are affected.
                                        - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                                      example: 22-24, 80, 443
                                      nullable: true
                                    protocol:
                                      type: string
                                      description: |
                                        The type of network traffic affected by this rule.
                                      example: TCP
                                      enum:
                                      - TCP
                                      - UDP
                                      - ICMP
                                      - IPENCAP
                                x-linode-cli-format: json
                              inbound_policy:
                                type: string
                                description: |
                                  The default behavior for inbound traffic. This setting can be
                                  overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules)
                                  the `inbound.action` property of the Firewall Rule.
                                example: DROP
                                enum:
                                - ACCEPT
                                - DROP
                              outbound:
                                type: array
                                description: |
                                  The outbound rules for the firewall, as a JSON array.
                                items:
                                  type: object
                                  description: |
                                    One of a Firewall's inbound or outbound access rules.
                                    The `ports` property can be used to allow traffic
                                    on a comma-separated list of different ports.
                                  x-akamai:
                                    file-path: schemas/firewall-rule-config.yaml
                                  additionalProperties: false
                                  properties:
                                    description:
                                      type: string
                                      description: |
                                        Used to describe this rule. For display purposes
                                        only.
                                      example: An example firewall rule description.
                                      minLength: 1
                                      maxLength: 100
                                    action:
                                      type: string
                                      description: |
                                        Controls whether traffic is accepted or dropped by
                                        this rule. Overrides the Firewall's `inbound_policy`
                                        if this is an inbound rule, or the `outbound_policy`
                                        if this is an outbound rule.
                                      example: ACCEPT
                                      enum:
                                      - ACCEPT
                                      - DROP
                                    addresses:
                                      type: object
                                      description: |-
                                        The IPv4 and/or IPv6 addresses affected by this rule. A Rule can have up to 255 total addresses or networks listed across its IPv4 and IPv6 arrays. A network and a single IP are treated as equivalent when accounting for this limit.

                                        Must contain `ipv4`, `ipv6`, or both.
                                      additionalProperties: false
                                      properties:
                                        ipv4:
                                          type: array
                                          description: |-
                                            A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.

                                            If "0.0.0.0/0" is included in this list, all IPv4 addresses are affected by this rule.
                                          example:
                                          - 192.0.2.0/24
                                          - 198.51.100.2/32
                                          items:
                                            type: string
                                        ipv6:
                                          type: array
                                          description: |-
                                            A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.

                                            If "::/0" is included in this list, all IPv6 addresses are affected by this rule.
                                          example:
                                          - 2001:DB8::/128
                                          items:
                                            type: string
                                    label:
                                      type: string
                                      description: |
                                        Used to identify this rule. For display purposes
                                        only.
                                      example: firewallrule123
                                      minLength: 3
                                      maxLength: 32
                                    ports:
                                      type: string
                                      description: |-
                                        A string representing the port or ports affected by this rule:

                                        - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                                        - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                                        - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port "080" is not allowed.
                                        - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                                        - If no ports are configured, all ports are affected.
                                        - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                                      example: 22-24, 80, 443
                                      nullable: true
                                    protocol:
                                      type: string
                                      description: |
                                        The type of network traffic affected by this rule.
                                      example: TCP
                                      enum:
                                      - TCP
                                      - UDP
                                      - ICMP
                                      - IPENCAP
                                x-linode-cli-format: json
                              outbound_policy:
                                type: string
                                description: |
                                  The default behavior for outbound traffic. This setting can
                                  be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules)
                                  the `outbound.action` property of the Firewall Rule.
                                example: DROP
                                enum:
                                - ACCEPT
                                - DROP
                          status:
                            type: string
                            description: |-
                              __Read-only__ The status of this Firewall.

                                - When a Firewall is first created its status is `enabled`.
                                - RUn the [Update a firewall](https://techdocs.akamai.com/linode-api/reference/put-firewall) operation to set a Firewall's status to `enabled` or `disabled`.
                                - RUn the [Delete a firewall](https://techdocs.akamai.com/linode-api/reference/delete-firewall) operation to delete a Firewall.
                            example: enabled
                            readOnly: true
                            enum:
                            - enabled
                            - disabled
                            - deleted
                            x-linode-cli-display: 3
                          updated:
                            type: string
                            description: |
                              __Read-only__ When this Firewall was last updated.
                            example: '2018-01-02T00:01:01'
                            readOnly: true
                            format: date-time
                            x-linode-cli-display: 5
                            x-linode-filterable: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-node-balancer-firewalls
      security:
      - personalAccessToken: []
      - oauth:
        - nodebalancers:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl https://api.linode.com/v4/nodebalancers/$nodeBalancerId/firewalls \
            -H "Authorization: Bearer $TOKEN"
      - lang: CLI
        source: linode-cli nodebalancers firewalls $nodeBalancerId
      x-linode-cli-action: firewalls
      x-linode-grant: read_only
      x-original-op-id: getNodeBalancerFirewalls
      x-original-op-title: Firewalls List
    x-linode-cli-command: nodebalancers
  /nodebalancers/{nodeBalancerId}/stats:
    x-akamai:
      file-path: paths/stats.yaml
      path-info: /nodebalancers/{nodeBalancerId}/stats
    parameters:
    - name: nodeBalancerId
      in: path
      description: |
        The ID of the NodeBalancer to access.
      example: '{{nodeBalancerId}}'
      x-akamai:
        file-path: parameters/node-balancer-id.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-node-balancer-stats
      summary: Get node balancer statistics
      tags:
      - Statistics
      description: |-
        Returns detailed statistics about the requested NodeBalancer.


        ---


        - __CLI__.

            ```
            linode-cli nodebalancers stats
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            nodebalancers:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli nodebalancers stats
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: nodebalancers:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The requested stats.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Stats for this NodeBalancer.
                x-akamai:
                  file-path: schemas/node-balancer-stats.yaml
                additionalProperties: false
                properties:
                  title:
                    type: string
                    description: |
                      The title for the statistics generated in this response.
                    example: linode.com - balancer12345 (12345) - day (5 min avg)
                  data:
                    type: object
                    description: |
                      The data returned about this NodeBalancers.
                    additionalProperties: false
                    properties:
                      connections:
                        type: array
                        description: |
                          An array of key/value pairs representing unix timestamp and reading for connections
                          to this NodeBalancer.
                        items:
                          type: number
                          example:
                          - 1526391300000
                          - 0
                      traffic:
                        type: object
                        description: |
                          Traffic statistics for this NodeBalancer.
                        additionalProperties: false
                        properties:
                          in:
                            type: array
                            description: |
                              An array of key/value pairs representing unix timestamp and reading
                              for inbound traffic.
                            items:
                              type: number
                              example:
                              - 1526391300000
                              - 631.21
                          out:
                            type: array
                            description: |
                              An array of key/value pairs representing unix timestamp and reading
                              for outbound traffic.
                            items:
                              type: number
                              example:
                              - 1526391300000
                              - 103.44
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-node-balancer-stats
      security:
      - personalAccessToken: []
      - oauth:
        - nodebalancers:read_only
      x-linode-cli-action: stats
      x-linode-cli-skip: true
      x-linode-grant: read_only
      x-original-op-id: get-node-balancer-stats
      x-original-op-title: NodeBalancer Statistics
        View
    x-linode-cli-command: nodebalancers
  /object-storage/buckets:
    x-akamai:
      file-path: paths/buckets.yaml
      path-info: /object-storage/buckets
    post:
      servers:
      - url: https://api.linode.com/v4
      operationId: post-object-storage-bucket
      summary: Create an object storage bucket
      tags:
      - Buckets
      description: |-
        Creates an Object Storage Bucket in the specified cluster.

        Accounts with negative balances cannot access this operation.

        If the bucket already exists and is owned by you, this operation returns a `200` response with that bucket as if it had just been created.

        This operation is available for convenience. It is recommended that instead you use the more [fully-featured S3 API](https://docs.ceph.com/en/latest/radosgw/s3/bucketops/#put-bucket) directly.


        ---


        - __OAuth__.

            ```
            object_storage:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: object_storage:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the bucket you want to create.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - label
              - cluster
              properties:
                acl:
                  type: string
                  description: |
                    The Access Control Level of the bucket using a canned ACL string. For more fine-grained
                    control of ACLs, use the S3 API directly.
                  example: '{{acl}}'
                  default: private
                  enum:
                  - private
                  - public-read
                  - authenticated-read
                  - public-read-write
                cluster:
                  type: string
                  description: |
                    The ID of the Object Storage Cluster where this bucket should be created.
                  example: '{{cluster}}'
                cors_enabled:
                  type: boolean
                  description: |
                    If true, the bucket will be created with CORS enabled for all origins. For more fine-grained
                    controls of CORS, use the S3 API directly.
                  example: '{{cors_enabled}}'
                  default: false
                label:
                  type: string
                  description: |
                    The name for this bucket. Must be unique in the cluster you are creating the bucket in,
                    or an error will be returned. Labels will be reserved only for
                    the cluster that active buckets are created and stored in. If
                    you want to reserve this bucket's label in another cluster, you
                    must create a new bucket with the same label in the new cluster.
                  example: '{{label}}'
                  pattern: ^[a-z0-09][a-z0-9-]*[a-z0-9]?$
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The bucket created successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An Object Storage Bucket. This should be accessed primarily through the S3 API; [click here
                  for more information](https://docs.ceph.com/en/latest/radosgw/s3/#api).
                x-akamai:
                  file-path: schemas/object-storage-bucket.yaml
                additionalProperties: false
                properties:
                  cluster:
                    type: string
                    description: |
                      The ID of the Object Storage Cluster this bucket is in.
                    example: us-east-1
                  created:
                    type: string
                    description: |
                      When this bucket was created.
                    example: '2019-01-01T01:23:45'
                    format: date-time
                  hostname:
                    type: string
                    description: |
                      The hostname where this bucket can be accessed. This hostname can be accessed through
                      a browser if the bucket is made public.
                    example: example-bucket.us-east-1.linodeobjects.com
                  label:
                    type: string
                    description: |
                      The name of this bucket.
                    example: example-bucket
                  objects:
                    type: integer
                    description: |
                      The number of objects stored in this bucket.
                    example: 4
                  size:
                    type: integer
                    description: |
                      The size of the bucket in bytes.
                    example: 188318981
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-object-storage-bucket
      security:
      - personalAccessToken: []
      - oauth:
        - object_storage:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "label": "example-bucket",
                "cluster": "us-east-1",
                "cors_enabled": true,
                "acl": "private"
              }' \
            https://api.linode.com/v4/object-storage/buckets/
      x-linode-cli-skip: true
      x-original-op-id: createObjectStorageBucket
      x-original-op-title: Object Storage Bucket Create
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-object-storage-buckets
      summary: List object storage buckets
      tags:
      - Buckets
      description: |-
        Returns a paginated list of all Object Storage Buckets that you own.

        This operation is available for convenience. It is recommended that instead you use the more [fully-featured S3 API](https://docs.ceph.com/en/latest/radosgw/s3/serviceops/#list-buckets) directly.


        ---


        - __OAuth__.

            ```
            object_storage:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: object_storage:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of buckets you own.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        An Object Storage Bucket. This should be accessed primarily through the S3 API;
                        [click here for more information](https://docs.ceph.com/en/latest/radosgw/s3/#api).
                      x-akamai:
                        file-path: schemas/object-storage-bucket.yaml
                      additionalProperties: false
                      properties:
                        cluster:
                          type: string
                          description: |
                            The ID of the Object Storage Cluster this bucket is in.
                          example: us-east-1
                        created:
                          type: string
                          description: |
                            When this bucket was created.
                          example: '2019-01-01T01:23:45'
                          format: date-time
                        hostname:
                          type: string
                          description: |
                            The hostname where this bucket can be accessed. This hostname can be
                            accessed through a browser if the bucket is made public.
                          example: example-bucket.us-east-1.linodeobjects.com
                        label:
                          type: string
                          description: |
                            The name of this bucket.
                          example: example-bucket
                        objects:
                          type: integer
                          description: |
                            The number of objects stored in this bucket.
                          example: 4
                        size:
                          type: integer
                          description: |
                            The size of the bucket in bytes.
                          example: 188318981
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-object-storage-buckets
      security:
      - personalAccessToken: []
      - oauth:
        - object_storage:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
            https://api.linode.com/v4/object-storage/buckets/
      x-linode-cli-skip: true
      x-original-op-id: getObjectStorageBuckets
      x-original-op-title: Object Storage Buckets List
  /object-storage/buckets/{clusterId}:
    x-akamai:
      file-path: paths/buckets-cluster.yaml
      path-info: /object-storage/buckets/{clusterId}
    parameters:
    - name: clusterId
      in: path
      description: |
        ID of the Kubernetes cluster to look up.
      example: '{{clusterId}}'
      x-akamai:
        file-path: parameters/cluster-id-path.yaml
      required: true
      schema:
        type: integer
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-object-storage-bucketin-cluster
      summary: List object storage buckets in cluster
      tags:
      - Buckets
      description: |-
        Returns a list of Buckets in this cluster belonging to this Account.

        This operation is available for convenience. It is recommended that instead you use the more [fully-featured S3 API](https://docs.ceph.com/en/latest/radosgw/s3/bucketops/#get-bucket) directly.


        ---


        - __OAuth__.

            ```
            object_storage:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: object_storage:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of buckets you own in this cluster.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        An Object Storage Bucket. This should be accessed primarily through the S3 API;
                        [click here for more information](https://docs.ceph.com/en/latest/radosgw/s3/#api).
                      x-akamai:
                        file-path: schemas/object-storage-bucket.yaml
                      additionalProperties: false
                      properties:
                        cluster:
                          type: string
                          description: |
                            The ID of the Object Storage Cluster this bucket is in.
                          example: us-east-1
                        created:
                          type: string
                          description: |
                            When this bucket was created.
                          example: '2019-01-01T01:23:45'
                          format: date-time
                        hostname:
                          type: string
                          description: |
                            The hostname where this bucket can be accessed. This hostname can be
                            accessed through a browser if the bucket is made public.
                          example: example-bucket.us-east-1.linodeobjects.com
                        label:
                          type: string
                          description: |
                            The name of this bucket.
                          example: example-bucket
                        objects:
                          type: integer
                          description: |
                            The number of objects stored in this bucket.
                          example: 4
                        size:
                          type: integer
                          description: |
                            The size of the bucket in bytes.
                          example: 188318981
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-object-storage-bucketin-cluster
      security:
      - personalAccessToken: []
      - oauth:
        - object_storage:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
            https://api.linode.com/v4/object-storage/buckets/us-east-1
      x-linode-cli-skip: true
      x-original-op-id: getObjectStorageBucketinCluster
      x-original-op-title: Object Storage Buckets in
        Cluster List
  /object-storage/buckets/{clusterId}/{bucket}:
    x-akamai:
      file-path: paths/bucket.yaml
      path-info: /object-storage/buckets/{clusterId}/{bucket}
    parameters:
    - name: clusterId
      in: path
      description: |
        ID of the Kubernetes cluster to look up.
      example: '{{clusterId}}'
      x-akamai:
        file-path: parameters/cluster-id-path.yaml
      required: true
      schema:
        type: integer
    - name: bucket
      in: path
      description: |
        The bucket name.
      example: '{{bucket}}'
      x-akamai:
        file-path: parameters/bucket.yaml
      required: true
      schema:
        type: string
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-object-storage-bucket
      summary: Get an object storage bucket
      tags:
      - Buckets
      description: |-
        Returns a single Object Storage Bucket.

        This operation is available for convenience. It is recommended that instead you use the more [fully-featured S3 API](https://docs.ceph.com/en/latest/radosgw/s3/bucketops/#get-bucket) directly.


        ---


        - __OAuth__.

            ```
            object_storage:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: object_storage:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The requested bucket.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An Object Storage Bucket. This should be accessed primarily through the S3 API; [click here
                  for more information](https://docs.ceph.com/en/latest/radosgw/s3/#api).
                x-akamai:
                  file-path: schemas/object-storage-bucket.yaml
                additionalProperties: false
                properties:
                  cluster:
                    type: string
                    description: |
                      The ID of the Object Storage Cluster this bucket is in.
                    example: us-east-1
                  created:
                    type: string
                    description: |
                      When this bucket was created.
                    example: '2019-01-01T01:23:45'
                    format: date-time
                  hostname:
                    type: string
                    description: |
                      The hostname where this bucket can be accessed. This hostname can be accessed through
                      a browser if the bucket is made public.
                    example: example-bucket.us-east-1.linodeobjects.com
                  label:
                    type: string
                    description: |
                      The name of this bucket.
                    example: example-bucket
                  objects:
                    type: integer
                    description: |
                      The number of objects stored in this bucket.
                    example: 4
                  size:
                    type: integer
                    description: |
                      The size of the bucket in bytes.
                    example: 188318981
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-object-storage-bucket
      security:
      - personalAccessToken: []
      - oauth:
        - object_storage:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
            https://api.linode.com/v4/object-storage/buckets/us-east-1/example-bucket
      x-linode-cli-skip: true
      x-original-op-id: getObjectStorageBucket
      x-original-op-title: Object Storage Bucket View
    delete:
      servers:
      - url: https://api.linode.com/v4
      operationId: delete-object-storage-bucket
      summary: Remove an object storage bucket
      tags:
      - Buckets
      description: |-
        Removes a single bucket.

        Bucket objects must be removed prior to removing the bucket. While buckets containing objects _may_ be deleted using the [s3cmd command-line tool](https://www.linode.com/docs/products/storage/object-storage/guides/s3cmd/#delete-a-bucket), such operations can fail if the bucket contains too many objects. The recommended way to empty large buckets is to use the [S3 API to configure lifecycle policies](https://docs.ceph.com/en/latest/radosgw/bucketpolicy/#) that remove all objects, then delete the bucket.

        This operation is available for convenience. It is recommended that instead you use the more [fully-featured S3 API](https://docs.ceph.com/en/latest/radosgw/s3/bucketops/#delete-bucket) directly.


        ---


        - __OAuth__.

            ```
            object_storage:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: object_storage:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Bucket deleted successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-object-storage-bucket
      security:
      - personalAccessToken: []
      - oauth:
        - object_storage:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
            -X DELETE \
            https://api.linode.com/v4/object-storage/buckets/us-east-1/example-bucket
      x-linode-cli-skip: true
      x-original-op-id: deleteObjectStorageBucket
      x-original-op-title: Object Storage Bucket Remove
  /object-storage/buckets/{clusterId}/{bucket}/access:
    x-akamai:
      file-path: paths/access.yaml
      path-info: /object-storage/buckets/{clusterId}/{bucket}/access
    parameters:
    - name: clusterId
      in: path
      description: |
        The ID of the cluster this bucket exists in.
      example: '{{clusterId}}'
      x-akamai:
        file-path: parameters/cluster-id.yaml
      required: true
      schema:
        type: string
    - name: bucket
      in: path
      description: |
        The bucket name.
      example: '{{bucket}}'
      x-akamai:
        file-path: parameters/bucket.yaml
      required: true
      schema:
        type: string
    post:
      servers:
      - url: https://api.linode.com/v4
      operationId: post-object-storage-bucket-access
      summary: Modify access to an object storage bucket
      tags:
      - Bucket access
      description: |-
        Allows changing basic Cross-origin Resource Sharing (CORS) and Access Control Level (ACL) settings. Only allows enabling/disabling CORS for all origins, and/or setting canned ACLs.

        For more fine-grained control of both systems, please use the more [fully-featured S3 API](https://docs.ceph.com/en/latest/radosgw/s3/bucketops/#put-bucket-acl) directly.


        ---


        - __OAuth__.

            ```
            object_storage:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: object_storage:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The changes to make to the bucket's access controls.
        content:
          application/json:
            schema:
              additionalProperties: false
              properties:
                acl:
                  type: string
                  description: |
                    The Access Control Level of the bucket, as a canned ACL string. For more fine-grained control
                    of ACLs, use the S3 API directly.
                  example: private
                  enum:
                  - private
                  - public-read
                  - authenticated-read
                  - public-read-write
                  - custom
                cors_enabled:
                  type: boolean
                  description: |
                    If true, the bucket will be created with CORS enabled for all origins. For more fine-grained
                    controls of CORS, use the S3 API directly.
                  example: true
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Access controls updated.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-object-storage-bucket-access
      security:
      - personalAccessToken: []
      - oauth:
        - object_storage:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \ -H "Authorization: Bearer $TOKEN" \
            -X POST -d '{
                "cors_enabled": true,
                "acl": "private"
              }' \
            https://api.linode.com/v4/object-storage/buckets/us-east-1/example-bucket/access
      x-linode-cli-skip: true
      x-original-op-id: modifyObjectStorageBucketAccess
      x-original-op-title: Object Storage Bucket Access
        Modify
    put:
      servers:
      - url: https://api.linode.com/v4
      operationId: put-storage-bucket-access
      summary: Update access to an object storage bucket
      tags:
      - Bucket access
      description: |-
        Allows changing basic Cross-origin Resource Sharing (CORS) and Access Control Level (ACL) settings. Only allows enabling/disabling CORS for all origins, and/or setting canned ACLs.

        For more fine-grained control of both systems, please use the more [fully-featured S3 API](https://docs.ceph.com/en/latest/radosgw/s3/bucketops/#put-bucket-acl) directly.


        ---


        - __OAuth__.

            ```
            object_storage:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: object_storage:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The changes to make to the bucket's access controls.
        content:
          application/json:
            schema:
              additionalProperties: false
              properties:
                acl:
                  type: string
                  description: |
                    The Access Control Level of the bucket, as a canned ACL string. For more fine-grained control
                    of ACLs, use the S3 API directly.
                  example: private
                  enum:
                  - private
                  - public-read
                  - authenticated-read
                  - public-read-write
                  - custom
                cors_enabled:
                  type: boolean
                  description: |
                    If true, the bucket will be created with CORS enabled for all origins. For more fine-grained
                    controls of CORS, use the S3 API directly.
                  example: true
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Access controls updated.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-storage-bucket-access
      security:
      - personalAccessToken: []
      - oauth:
        - object_storage:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \ -H "Authorization: Bearer $TOKEN" \
            -X PUT -d '{
                "cors_enabled": true,
                "acl": "private"
              }' \
            https://api.linode.com/v4/object-storage/buckets/us-east-1/example-bucket/access
      x-linode-cli-skip: true
      x-original-op-id: updateObjectStorageBucketAccess
      x-original-op-title: Object Storage Bucket Access
        Update
  /object-storage/buckets/{clusterId}/{bucket}/object-acl:
    x-akamai:
      file-path: paths/object-acl.yaml
      path-info: /object-storage/buckets/{clusterId}/{bucket}/object-acl
    parameters:
    - name: clusterId
      in: path
      description: |
        The ID of the cluster this bucket exists in.
      example: '{{clusterId}}'
      x-akamai:
        file-path: parameters/cluster-id-path-7b27f6d9.yaml
      required: true
      schema:
        type: string
    - name: bucket
      in: path
      description: |
        The bucket name.
      example: '{{bucket}}'
      x-akamai:
        file-path: parameters/bucket.yaml
      required: true
      schema:
        type: string
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-object-storage-bucket-acl
      summary: Get an object storage object ACL config
      tags:
      - ACL configurations
      description: |-
        View an Object's configured Access Control List (ACL) in this Object Storage bucket. ACLs define who can access your buckets and objects and specify the level of access granted to those users.

        This operation is available for convenience. It is recommended that instead you use the more [fully-featured S3 API](https://docs.ceph.com/en/latest/radosgw/s3/objectops/#get-object-acl) directly.


        ---


        - __OAuth__.

            ```
            object_storage:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: object_storage:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: name
        in: query
        description: |
          The `name` of the object for which to retrieve its Access Control List (ACL). Run the [List object storage
          bucket contents](https://techdocs.akamai.com/linode-api/reference/get-object-storage-bucket-content)
          operation to access all object names in a bucket.
        example: '{{name}}'
        x-akamai:
          file-path: parameters/name-query.yaml
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The Object's canned ACL and policy.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  acl:
                    type: string
                    description: |
                      The Access Control Level of the bucket, as a canned ACL string. For more fine-grained
                      control of ACLs, use the S3 API directly.
                    example: public-read
                    enum:
                    - private
                    - public-read
                    - authenticated-read
                    - public-read-write
                    - custom
                  acl_xml:
                    type: string
                    description: |
                      The full XML of the object's ACL policy.
                    example: <AccessControlPolicy>...</AccessControlPolicy>
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-object-storage-bucket-acl
      security:
      - personalAccessToken: []
      - oauth:
        - object_storage:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
            https://api.linode.com/v4/object-storage/buckets/us-east-1/example-bucket/object-acl?name=example.txt
      x-linode-cli-skip: true
      x-original-op-id: viewObjectStorageBucketACL
      x-original-op-title: Object Storage Object ACL
        Config View
    put:
      servers:
      - url: https://api.linode.com/v4
      operationId: put-object-storage-bucket-acl
      summary: Update an object's ACL config
      tags:
      - ACL configurations
      description: |-
        Update an Object's configured Access Control List (ACL) in this Object Storage bucket. ACLs define who can access your buckets and objects and specify the level of access granted to those users.

        This operation is available for convenience. It is recommended that instead you use the more [fully-featured S3 API](https://docs.ceph.com/en/latest/radosgw/s3/objectops/#set-object-acl) directly.


        ---


        - __OAuth__.

            ```
            object_storage:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: object_storage:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The changes to make to this Object's access controls.
        content:
          application/json:
            schema:
              additionalProperties: false
              required:
              - acl
              - name
              properties:
                name:
                  type: string
                  description: |
                    The `name` of the object for which to update its Access Control List (ACL). Run the [List
                    object storage bucket contents](https://techdocs.akamai.com/linode-api/reference/get-object-storage-bucket-content)
                    operation to access all object names in a bucket.
                acl:
                  type: string
                  description: |
                    The Access Control Level of the bucket, as a canned ACL string. For more fine-grained control
                    of ACLs, use the S3 API directly.
                  example: public-read
                  enum:
                  - private
                  - public-read
                  - authenticated-read
                  - public-read-write
                  - custom
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The Object's canned ACL and policy.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  acl:
                    type: string
                    description: |
                      The Access Control Level of the bucket, as a canned ACL string. For more fine-grained
                      control of ACLs, use the S3 API directly.
                    example: public-read
                    enum:
                    - private
                    - public-read
                    - authenticated-read
                    - public-read-write
                    - custom
                  acl_xml:
                    type: string
                    description: |
                      The full XML of the object's ACL policy.
                    example: <AccessControlPolicy>...</AccessControlPolicy>
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-object-storage-bucket-acl
      security:
      - personalAccessToken: []
      - oauth:
        - object_storage:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \ -H "Authorization: Bearer $TOKEN" \
            -X PUT -d '{
              "acl": "public-read",
              "name": "example.txt"
            }' \
            https://api.linode.com/v4/object-storage/buckets/us-east-1/example-bucket/object-acl
      x-linode-cli-skip: true
      x-original-op-id: updateObjectStorageBucketACL
      x-original-op-title: Object Storage Object ACL
        Config Update
  /object-storage/buckets/{clusterId}/{bucket}/object-list:
    x-akamai:
      file-path: paths/object-list.yaml
      path-info: /object-storage/buckets/{clusterId}/{bucket}/object-list
    parameters:
    - name: clusterId
      in: path
      description: |
        The ID of the cluster this bucket exists in.
      example: '{{clusterId}}'
      x-akamai:
        file-path: parameters/cluster-id-path-7b27f6d9.yaml
      required: true
      schema:
        type: string
    - name: bucket
      in: path
      description: |
        The bucket name.
      example: '{{bucket}}'
      x-akamai:
        file-path: parameters/bucket.yaml
      required: true
      schema:
        type: string
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-object-storage-bucket-content
      summary: List object storage bucket contents
      tags:
      - Bucket contents
      description: |-
        Returns the contents of a bucket. The contents are paginated using a `marker`, which is the name of the last object on the previous page.  Objects may be filtered by `prefix` and `delimiter` as well; see Query Parameters for more information.

        This operation is available for convenience. It is recommended that instead you use the more [fully-featured S3 API](https://docs.ceph.com/en/latest/radosgw/s3/objectops/#get-object) directly.


        ---


        - __OAuth__.

            ```
            object_storage:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: object_storage:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: marker
        in: query
        description: |
          The "marker" for this request, which can be used to paginate through large buckets. Its value should be
          the value of the `next_marker` property returned with the last page. Listing
          bucket contents _does not_ support arbitrary page access. See the `next_marker`
          property in the responses section for more details.
        example: '{{marker}}'
        x-akamai:
          file-path: parameters/marker-query.yaml
        required: false
        schema:
          type: string
      - name: delimiter
        in: query
        description: |
          The delimiter for object names; if given, object names will be returned up to the first occurrence of this
          character. This is most commonly used with the `/` character to allow bucket
          transversal in a manner similar to a filesystem, however any delimiter may
          be used. Use in conjunction with `prefix` to see object names past the first
          occurrence of the delimiter.
        example: '{{delimiter}}'
        x-akamai:
          file-path: parameters/delimiter-query.yaml
        required: false
        schema:
          type: string
      - name: prefix
        in: query
        description: |
          Filters objects returned to only those whose name start with the given prefix. Commonly used in conjunction
          with `delimiter` to allow transversal of bucket contents in a manner similar
          to a filesystem.
        example: '{{prefix}}'
        x-akamai:
          file-path: parameters/prefix-query.yaml
        required: false
        schema:
          type: string
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            One page of the requested bucket's contents.
          content:
            application/json:
              schema:
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        An Object in Object Storage, or a "prefix" that contains one or more objects when
                        a `delimiter` is used.
                      x-akamai:
                        file-path: schemas/object-storage-object.yaml
                      additionalProperties: false
                      properties:
                        name:
                          type: string
                          description: |
                            The name of this object or prefix.
                          example: example
                        etag:
                          type: string
                          description: |
                            An MD-5 hash of the object. `null` if this object represents a prefix.
                          example: 9f254c71e28e033bf9e0e5262e3e72ab
                        last_modified:
                          type: string
                          description: |
                            The date and time this object was last modified. `null` if this object
                            represents a prefix.
                          example: '2019-01-01T01:23:45'
                          format: date-time
                        owner:
                          type: string
                          description: |
                            The owner of this object, as a UUID. `null` if this object represents
                            a prefix.
                          example: bfc70ab2-e3d4-42a4-ad55-83921822270c
                        size:
                          type: integer
                          description: |
                            The size of this object, in bytes. `null` if this object represents a prefix.
                          example: 123
                  is_truncated:
                    type: boolean
                    description: |
                      Designates if there is another page of bucket objects.
                    example: true
                  next_marker:
                    type: string
                    description: |
                      Returns the value you should pass to the `marker` query parameter to get the next
                      page of objects. If there is no next page, `null` will be returned.
                    example: bd021c21-e734-4823-97a4-58b41c2cd4c8.892602.184
                    nullable: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-object-storage-bucket-content
      security:
      - personalAccessToken: []
      - oauth:
        - object_storage:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
            https://api.linode.com/v4/object-storage/buckets/us-east-1/example-bucket/object-list
      x-linode-cli-skip: true
      x-original-op-id: getObjectStorageBucketContent
      x-original-op-title: Object Storage Bucket Contents
        List
  /object-storage/buckets/{clusterId}/{bucket}/object-url:
    x-akamai:
      file-path: paths/object-url.yaml
      path-info: /object-storage/buckets/{clusterId}/{bucket}/object-url
    parameters:
    - name: clusterId
      in: path
      description: |
        The ID of the cluster this bucket exists in.
      example: '{{clusterId}}'
      x-akamai:
        file-path: parameters/cluster-id-path-7b27f6d9.yaml
      required: true
      schema:
        type: string
    - name: bucket
      in: path
      description: |
        The bucket name.
      example: '{{bucket}}'
      x-akamai:
        file-path: parameters/bucket.yaml
      required: true
      schema:
        type: string
    post:
      servers:
      - url: https://api.linode.com/v4
      operationId: post-object-storage-object-url
      summary: Create a URL for an object
      tags:
      - Buckets
      description: |-
        Creates a pre-signed URL to access a single Object in a bucket. This can be used to share objects, and also to create/delete objects by using the appropriate HTTP method in your request body's `method` parameter.

        This operation is available for convenience. It is recommended that instead you
        use the more [fully-featured S3 API](https://docs.ceph.com/en/latest/radosgw/s3/)
        directly.


        ---


        - __OAuth__.

            ```
            object_storage:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: object_storage:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the request to sign.
        content:
          application/json:
            schema:
              additionalProperties: false
              required:
              - name
              - method
              properties:
                name:
                  type: string
                  description: |
                    The name of the object that will be accessed with the pre-signed URL. This object need
                    not exist, and no error will be returned if it doesn't. This behavior
                    is useful for generating pre-signed URLs to upload new objects
                    to by setting the `method` to "PUT".
                  example: example
                content_type:
                  type: string
                  description: |
                    The expected `Content-type` header of the request this signed URL will be valid for.  If provided,
                    the `Content-type` header _must_ be sent with the request when
                    this URL is used, and _must_ be the same as it was when the signed
                    URL was created. Required for all methods _except_ "GET" or "DELETE".
                  example: null
                expires_in:
                  type: integer
                  description: |
                    How long this signed URL will be valid for, in seconds.  If omitted, the URL will be
                    valid for 3600 seconds (1 hour).
                  example: null
                  default: 3600
                  minimum: 360
                  maximum: 86400
                method:
                  type: string
                  description: |
                    The HTTP method allowed to be used with the pre-signed URL.
                  example: GET
                  default: GET
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The URL with which to access your object.
          content:
            application/json:
              schema:
                additionalProperties: false
                properties:
                  url:
                    type: string
                    description: |
                      The signed URL to perform the request at.
                    example: https://us-east-1.linodeobjects.com/example-bucket/example?Signature=qr98TEucCntPgEG%2BsZQGDsJg93c%3D&Expires=1567609905&AWSAccessKeyId=G4YAF81XWY61DQM94SE0
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-object-storage-object-url
      security:
      - personalAccessToken: []
      - oauth:
        - object_storage:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \ -H "Authorization: Bearer $TOKEN" \
            -X POST -d '{
                "method": "GET",
                "name": "example"
              }' \
            https://api.linode.com/v4/object-storage/buckets/us-east-1/example-bucket/object-url
      x-linode-cli-skip: true
      x-original-op-id: createObjectStorageObjectURL
      x-original-op-title: Object Storage Object URL
        Create
  /object-storage/buckets/{clusterId}/{bucket}/ssl:
    x-akamai:
      file-path: paths/bucket-ssl.yaml
      path-info: /object-storage/buckets/{clusterId}/{bucket}/ssl
    parameters:
    - name: clusterId
      in: path
      description: |
        ID of the Kubernetes cluster to look up.
      example: '{{clusterId}}'
      x-akamai:
        file-path: parameters/cluster-id-path.yaml
      required: true
      schema:
        type: integer
    - name: bucket
      in: path
      description: |
        The bucket name.
      example: '{{bucket}}'
      x-akamai:
        file-path: parameters/bucket.yaml
      required: true
      schema:
        type: string
    post:
      servers:
      - url: https://api.linode.com/v4
      operationId: post-object-storage-ssl
      summary: Upload an object storage TLS/SSL certificate
      tags:
      - TLS/SSL certificates
      description: |-
        Upload a TLS/SSL certificate and private key to be served when you visit your Object Storage bucket via HTTPS. Your TLS/SSL certificate and private key are stored encrypted at rest.

        To replace an expired certificate, [delete your current certificates](https://techdocs.akamai.com/linode-api/reference/delete-object-storage-ssl) and upload a new one.


        ---


        - __CLI__.

            ```
            linode-cli object-storage ssl-upload
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            object_storage:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli object-storage ssl-upload
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: object_storage:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Upload this TLS/SSL certificate with its corresponding secret key.
        content:
          application/json:
            schema:
              type: object
              description: |
                Upload a TLS/SSL certificate and private key to be served when you visit your Object Storage bucket
                via HTTPS.
              x-akamai:
                file-path: schemas/object-storage-ssl.yaml
              additionalProperties: false
              required:
              - certificate
              - private_key
              properties:
                certificate:
                  type: string
                  description: |-
                    Your Base64 encoded and PEM formatted SSL certificate.

                    Line breaks must be represented as "\n" in the string for requests (but not when using the Linode CLI)
                  example: '{{certificate}}'
                private_key:
                  type: string
                  description: |-
                    The private key associated with this TLS/SSL certificate.

                    Line breaks must be represented as "\n" in the string for requests (but not when using the Linode CLI)
                  example: '{{private_key}}'
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns whether this bucket has a corresponding TLS/SSL certificate that was uploaded by a user.
          content:
            application/json:
              schema:
                type: object
                description: |
                  If this Object Storage bucket has a corresponding TLS/SSL Certificate.
                x-akamai:
                  file-path: schemas/object-storage-ssl-response.yaml
                additionalProperties: false
                properties:
                  ssl:
                    type: boolean
                    description: |
                      __Read-only__ A boolean indicating if this Bucket has a corresponding TLS/SSL certificate
                      that was uploaded by an Account user.
                    example: true
                    readOnly: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-object-storage-ssl
      security:
      - personalAccessToken: []
      - oauth:
        - object_storage:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                  "certificate": "-----BEGIN CERTIFICATE-----\nCERTIFICATE_INFORMATION\n-----END CERTIFICATE-----",
                  "private_key": "-----BEGIN PRIVATE KEY-----\nPRIVATE_KEY_INFORMATION\n-----END PRIVATE KEY-----"
                }' \
              https://api.linode.com/v4/object-storage/buckets/us-east-1/example-bucket/ssl
      - lang: CLI
        source: |-
          linode-cli object-storage ssl-upload \
            us-east-1 example-bucket \
            --certificate "-----BEGIN CERTIFICATE-----
                           CERTIFICATE_INFORMATION
                           -----END CERTIFICATE-----" \
            --private_key "-----BEGIN PRIVATE KEY-----
                           PRIVATE_KEY_INFORMATION
                           -----END PRIVATE KEY-----"
      x-linode-cli-action: ssl-upload
      x-original-op-id: createObjectStorageSSL
      x-original-op-title: Object Storage TLS/SSL Cert
        Upload
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-object-storage-ssl
      summary: Get an object storage TLS/SSL certificate
      tags:
      - TLS/SSL certificates
      description: |-
        Returns a boolean value indicating if this bucket has a corresponding TLS/SSL certificate that was uploaded by an Account user.


        ---


        - __CLI__.

            ```
            linode-cli object-storage ssl-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            object_storage:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli object-storage ssl-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: object_storage:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a boolean value indicating if this bucket has a corresponding TLS/SSL certificate that was uploaded
            by an Account user.
          content:
            application/json:
              schema:
                type: object
                description: |
                  If this Object Storage bucket has a corresponding TLS/SSL Certificate.
                x-akamai:
                  file-path: schemas/object-storage-ssl-response.yaml
                additionalProperties: false
                properties:
                  ssl:
                    type: boolean
                    description: |
                      __Read-only__ A boolean indicating if this Bucket has a corresponding TLS/SSL certificate
                      that was uploaded by an Account user.
                    example: true
                    readOnly: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-object-storage-ssl
      security:
      - personalAccessToken: []
      - oauth:
        - object_storage:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/object-storage/buckets/us-east-1/example-bucket/ssl
      - lang: CLI
        source: |-
          linode-cli object-storage ssl-view \
            us-east-1 example-bucket
      x-linode-cli-action: ssl-view
      x-original-op-id: getObjectStorageSSL
      x-original-op-title: Object Storage TLS/SSL Cert
        View
    delete:
      servers:
      - url: https://api.linode.com/v4
      operationId: delete-object-storage-ssl
      summary: Delete an object storage TLS/SSL certificate
      tags:
      - TLS/SSL certificates
      description: |-
        Deletes this Object Storage bucket's user uploaded TLS/SSL certificate and private key.


        ---


        - __CLI__.

            ```
            linode-cli object-storage ssl-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            object_storage:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli object-storage ssl-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: object_storage:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Deletes this Object Storage bucket's user uploaded TLS/SSL certificate and private key.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-object-storage-ssl
      security:
      - personalAccessToken: []
      - oauth:
        - object_storage:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/object-storage/buckets/us-east-1/example-bucket/ssl
      - lang: CLI
        source: |-
          linode-cli object-storage ssl-delete \
            us-east-1 example-bucket
      x-linode-cli-action: ssl-delete
      x-original-op-id: deleteObjectStorageSSL
      x-original-op-title: Object Storage TLS/SSL Cert
        Delete
    x-linode-cli-command: object-storage
  /object-storage/cancel:
    x-akamai:
      file-path: paths/object-storage-cancel.yaml
      path-info: /object-storage/cancel
    post:
      servers:
      - url: https://api.linode.com/v4
      operationId: post-cancel-object-storage
      summary: Cancel object storage
      tags:
      - Object storage
      description: |-
        Cancel Object Storage on an Account.

        __Warning__: Removes all buckets and their contents from your Account. This data is irretrievable once removed.


        ---


        - __CLI__.

            ```
            linode-cli object-storage cancel
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            object_storage:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli object-storage cancel
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: object_storage:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Object Storage cancellation successful.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-cancel-object-storage
      security:
      - personalAccessToken: []
      - oauth:
        - object_storage:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
            -X POST \
            https://api.linode.com/v4/object-storage/cancel
      - lang: CLI
        source: linode-cli object-storage cancel
      x-linode-cli-action: cancel
      x-original-op-id: cancelObjectStorage
      x-original-op-title: Object Storage Cancel
    x-linode-cli-command: object-storage
  /object-storage/clusters:
    x-akamai:
      file-path: paths/object-storage-clusters.yaml
      path-info: /object-storage/clusters
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-object-storage-clusters
      summary: List clusters
      tags:
      - Clusters
      description: |
        Returns a paginated list of Object Storage Clusters that are available for use.  Users can connect to the clusters
        with third party clients to create buckets and upload objects.
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of available clusters.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        An Object Storage Cluster
                      x-akamai:
                        file-path: schemas/object-storage-cluster.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: string
                          description: |
                            The unique ID for this cluster.
                          example: us-east-1
                        domain:
                          type: string
                          description: |
                            The base URL for this cluster, used for connecting with third-party
                            clients.
                          example: us-east-1.linodeobjects.com
                        region:
                          type: string
                          description: |
                            The region where this cluster is located.
                          example: us-east
                        static_site_domain:
                          type: string
                          description: |
                            The base URL for this cluster used when hosting static sites.
                          example: website-us-east-1.linodeobjects.com
                        status:
                          type: string
                          description: |
                            This cluster's status.
                          example: available
                          enum:
                          - available
                          - unavailable
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-object-storage-clusters
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
            https://api.linode.com/v4/object-storage/clusters
      - lang: CLI
        source: linode-cli object-storage clusters-list
      x-linode-cli-action: clusters-list
      x-original-op-id: getObjectStorageClusters
      x-original-op-title: Clusters List
    x-linode-cli-command: object-storage
  /object-storage/clusters/{clusterId}:
    x-akamai:
      file-path: paths/object-storage-cluster.yaml
      path-info: /object-storage/clusters/{clusterId}
    parameters:
    - name: clusterId
      in: path
      description: |
        The ID of the Cluster.
      example: '{{clusterId}}'
      x-akamai:
        file-path: parameters/cluster-id-path-f8f10b45.yaml
      required: true
      schema:
        type: string
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-object-storage-cluster
      summary: Get a cluster
      tags:
      - Clusters
      description: |
        Returns a single Object Storage Cluster.
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The requested Cluster
          content:
            application/json:
              schema:
                type: object
                description: |
                  An Object Storage Cluster
                x-akamai:
                  file-path: schemas/object-storage-cluster.yaml
                additionalProperties: false
                properties:
                  id:
                    type: string
                    description: |
                      The unique ID for this cluster.
                    example: us-east-1
                  domain:
                    type: string
                    description: |
                      The base URL for this cluster, used for connecting with third-party clients.
                    example: us-east-1.linodeobjects.com
                  region:
                    type: string
                    description: |
                      The region where this cluster is located.
                    example: us-east
                  static_site_domain:
                    type: string
                    description: |
                      The base URL for this cluster used when hosting static sites.
                    example: website-us-east-1.linodeobjects.com
                  status:
                    type: string
                    description: |
                      This cluster's status.
                    example: available
                    enum:
                    - available
                    - unavailable
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-object-storage-cluster
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
            https://api.linode.com/v4/object-storage/clusters/us-east-1
      - lang: CLI
        source: linode-cli object-storage clusters-view us-east-1
      x-linode-cli-action: clusters-view
      x-original-op-id: getObjectStorageCluster
      x-original-op-title: Cluster View
    x-linode-cli-command: object-storage
  /object-storage/keys:
    x-akamai:
      file-path: paths/keys.yaml
      path-info: /object-storage/keys
    post:
      servers:
      - url: https://api.linode.com/v4
      operationId: post-object-storage-keys
      summary: Create an object storage key
      tags:
      - Keys
      description: |-
        Provisions a new Object Storage Key on your account.

        - Accounts with negative balances cannot access this operation.
        - A successful request triggers an `obj_access_key_create` event.
        - To create a Limited Access Key with specific permissions, send a `bucket_access` array.
        - To create a Limited Access Key without access to any buckets, send an empty `bucket_access` array.
        - To create an Access Key with unlimited access to all clusters and all buckets, omit the `bucket_access` array.


        ---


        - __CLI__.

            ```
            linode-cli object-storage keys-create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            object_storage:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli object-storage keys-create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: object_storage:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The label of the key to create. This is used to identify the created key.
        content:
          application/json:
            schema:
              type: object
              description: |
                A keypair used to communicate with the Object Storage S3 API.
              x-akamai:
                file-path: schemas/object-storage-key.yaml
              additionalProperties: false
              properties:
                id:
                  type: integer
                  description: |
                    __Read-only__ This keypair's unique ID
                  example: '{{id}}'
                  readOnly: true
                access_key:
                  type: string
                  description: |
                    __Read-only__ This keypair's access key. This is not secret.
                  example: '{{access_key}}'
                  readOnly: true
                bucket_access:
                  type: array
                  description: |-
                    Defines this key as a Limited Access Key. Limited Access Keys restrict this Object Storage key's access to only the bucket(s) declared in this array and define their bucket-level permissions.

                      Limited Access Keys can:

                      - [list all buckets](https://techdocs.akamai.com/linode-api/reference/get-object-storage-buckets) available on this Account, but cannot perform any actions on a bucket unless it has access to the bucket.

                      - [create new buckets](https://techdocs.akamai.com/linode-api/reference/post-object-storage-bucket), but do not have any access to the buckets it creates, unless explicitly given access to them.

                      __Note:__ You can create an Object Storage Limited Access Key without access to any buckets.
                      This is achieved by sending a request with an empty `bucket_access` array.

                      __Note:__ If this field is omitted, a regular unlimited access key is issued.
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      bucket_name:
                        type: string
                        description: |
                          The unique label of the bucket to which the key will grant limited access.
                        example: example-bucket
                      cluster:
                        type: string
                        description: |
                          The Object Storage cluster where a bucket to which the key is granting access
                          is hosted.
                        example: ap-south-1
                      permissions:
                        type: string
                        description: |
                          This Limited Access Key's permissions for the selected bucket.
                        example: read_only
                        enum:
                        - read_write
                        - read_only
                label:
                  type: string
                  description: |
                    The label given to this key. For display purposes only.
                  example: '{{label}}'
                limited:
                  type: boolean
                  description: |
                    __Read-only__ Whether or not this key is a limited access key. Will return `false` if
                    this key grants full access to all buckets on the user's account.
                  example: '{{limited}}'
                  readOnly: true
                secret_key:
                  type: string
                  description: |
                    __Read-only__ This keypair's secret key. Only returned on key creation.
                  example: '{{secret_key}}'
                  readOnly: true
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The new keypair.  __This is the only time__ the secret key is returned.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A keypair used to communicate with the Object Storage S3 API.
                x-akamai:
                  file-path: schemas/object-storage-key.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This keypair's unique ID
                    example: 123
                    readOnly: true
                  access_key:
                    type: string
                    description: |
                      __Read-only__ This keypair's access key. This is not secret.
                    example: KVAKUTGBA4WTR2NSJQ81
                    readOnly: true
                  bucket_access:
                    type: array
                    description: |-
                      Defines this key as a Limited Access Key. Limited Access Keys restrict this Object Storage key's access to only the bucket(s) declared in this array and define their bucket-level permissions.

                        Limited Access Keys can:

                        - [list all buckets](https://techdocs.akamai.com/linode-api/reference/get-object-storage-buckets) available on this Account, but cannot perform any actions on a bucket unless it has access to the bucket.

                        - [create new buckets](https://techdocs.akamai.com/linode-api/reference/post-object-storage-bucket), but do not have any access to the buckets it creates, unless explicitly given access to them.

                        __Note:__ You can create an Object Storage Limited Access Key without access to any buckets.
                        This is achieved by sending a request with an empty `bucket_access` array.

                        __Note:__ If this field is omitted, a regular unlimited access key is issued.
                    items:
                      type: object
                      additionalProperties: false
                      properties:
                        bucket_name:
                          type: string
                          description: |
                            The unique label of the bucket to which the key will grant limited access.
                          example: example-bucket
                        cluster:
                          type: string
                          description: |
                            The Object Storage cluster where a bucket to which the key is granting access
                            is hosted.
                          example: ap-south-1
                        permissions:
                          type: string
                          description: |
                            This Limited Access Key's permissions for the selected bucket.
                          example: read_only
                          enum:
                          - read_write
                          - read_only
                  label:
                    type: string
                    description: |
                      The label given to this key. For display purposes only.
                    example: my-key
                  limited:
                    type: boolean
                    description: |
                      __Read-only__ Whether or not this key is a limited access key. Will return `false`
                      if this key grants full access to all buckets on the user's
                      account.
                    example: true
                    readOnly: true
                  secret_key:
                    type: string
                    description: |
                      __Read-only__ This keypair's secret key. Only returned on key creation.
                    example: OiA6F5r0niLs3QA2stbyq7mY5VCV7KqOzcmitmHw
                    readOnly: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-object-storage-keys
      security:
      - personalAccessToken: []
      - oauth:
        - object_storage:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "label": "my-object-storage-key",
                "bucket_access": [
                  {
                    "cluster": "ap-south-1",
                    "bucket_name": "bucket-example-1",
                    "permissions": "read_write"
                  },
                  {
                    "cluster": "us-east-1",
                    "bucket_name": "bucket-example-2",
                    "permissions": "read_only"
                  }
                ]
              }' \
            https://api.linode.com/v4/object-storage/keys
      - lang: CLI
        source: |-
          linode-cli object-storage keys-create \
            --label "my-object-storage-key" \
            --bucket_access '[{"cluster": "ap-south-1", "bucket_name": "bucket-example-1", "permissions": "read_write" }]'
      x-linode-cli-action: keys-create
      x-original-op-id: createObjectStorageKeys
      x-original-op-title: Object Storage Key Create
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-object-storage-keys
      summary: List object storage keys
      tags:
      - Keys
      description: |-
        Returns a paginated list of Object Storage Keys for authenticating to the Object Storage S3 API.


        ---


        - __CLI__.

            ```
            linode-cli object-storage keys-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            object_storage:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli object-storage keys-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: object_storage:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of Object Storage Keys
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        A keypair used to communicate with the Object Storage S3 API.
                      x-akamai:
                        file-path: schemas/object-storage-key.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ This keypair's unique ID
                          example: 123
                          readOnly: true
                        access_key:
                          type: string
                          description: |
                            __Read-only__ This keypair's access key. This is not secret.
                          example: KVAKUTGBA4WTR2NSJQ81
                          readOnly: true
                        bucket_access:
                          type: array
                          description: |-
                            Defines this key as a Limited Access Key. Limited Access Keys restrict this Object Storage key's access to only the bucket(s) declared in this array and define their bucket-level permissions.

                              Limited Access Keys can:

                              - [list all buckets](https://techdocs.akamai.com/linode-api/reference/get-object-storage-buckets) available on this Account, but cannot perform any actions on a bucket unless it has access to the bucket.

                              - [create new buckets](https://techdocs.akamai.com/linode-api/reference/post-object-storage-bucket), but do not have any access to the buckets it creates, unless explicitly given access to them.

                              __Note:__ You can create an Object Storage Limited Access Key without access to any buckets.
                              This is achieved by sending a request with an empty `bucket_access` array.

                              __Note:__ If this field is omitted, a regular unlimited access key is issued.
                          items:
                            type: object
                            additionalProperties: false
                            properties:
                              bucket_name:
                                type: string
                                description: |
                                  The unique label of the bucket to which the key will grant
                                  limited access.
                                example: example-bucket
                              cluster:
                                type: string
                                description: |
                                  The Object Storage cluster where a bucket to which the key
                                  is granting access is hosted.
                                example: ap-south-1
                              permissions:
                                type: string
                                description: |
                                  This Limited Access Key's permissions for the selected bucket.
                                example: read_only
                                enum:
                                - read_write
                                - read_only
                        label:
                          type: string
                          description: |
                            The label given to this key. For display purposes only.
                          example: my-key
                        limited:
                          type: boolean
                          description: |
                            __Read-only__ Whether or not this key is a limited access key. Will
                            return `false` if this key grants full access to all buckets
                            on the user's account.
                          example: true
                          readOnly: true
                        secret_key:
                          type: string
                          description: |
                            __Read-only__ This keypair's secret key. Only returned on key creation.
                          example: OiA6F5r0niLs3QA2stbyq7mY5VCV7KqOzcmitmHw
                          readOnly: true
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-object-storage-keys
      security:
      - personalAccessToken: []
      - oauth:
        - object_storage:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
            https://api.linode.com/v4/object-storage/keys
      - lang: CLI
        source: linode-cli object-storage keys-list
      x-linode-cli-action: keys-list
      x-original-op-id: getObjectStorageKeys
      x-original-op-title: Object Storage Keys List
    x-linode-cli-command: object-storage
  /object-storage/keys/{keyId}:
    x-akamai:
      file-path: paths/key.yaml
      path-info: /object-storage/keys/{keyId}
    parameters:
    - name: keyId
      in: path
      description: |
        The key to look up.
      example: '{{keyId}}'
      x-akamai:
        file-path: parameters/key-id-path.yaml
      required: true
      schema:
        type: integer
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-object-storage-key
      summary: Get an object storage key
      tags:
      - Keys
      description: |-
        Returns a single Object Storage Key provisioned for your account.


        ---


        - __CLI__.

            ```
            linode-cli object-storage keys-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            object_storage:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli object-storage keys-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: object_storage:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The keypair
          content:
            application/json:
              schema:
                type: object
                description: |
                  A keypair used to communicate with the Object Storage S3 API.
                x-akamai:
                  file-path: schemas/object-storage-key.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This keypair's unique ID
                    example: 123
                    readOnly: true
                  access_key:
                    type: string
                    description: |
                      __Read-only__ This keypair's access key. This is not secret.
                    example: KVAKUTGBA4WTR2NSJQ81
                    readOnly: true
                  bucket_access:
                    type: array
                    description: |-
                      Defines this key as a Limited Access Key. Limited Access Keys restrict this Object Storage key's access to only the bucket(s) declared in this array and define their bucket-level permissions.

                        Limited Access Keys can:

                        - [list all buckets](https://techdocs.akamai.com/linode-api/reference/get-object-storage-buckets) available on this Account, but cannot perform any actions on a bucket unless it has access to the bucket.

                        - [create new buckets](https://techdocs.akamai.com/linode-api/reference/post-object-storage-bucket), but do not have any access to the buckets it creates, unless explicitly given access to them.

                        __Note:__ You can create an Object Storage Limited Access Key without access to any buckets.
                        This is achieved by sending a request with an empty `bucket_access` array.

                        __Note:__ If this field is omitted, a regular unlimited access key is issued.
                    items:
                      type: object
                      additionalProperties: false
                      properties:
                        bucket_name:
                          type: string
                          description: |
                            The unique label of the bucket to which the key will grant limited access.
                          example: example-bucket
                        cluster:
                          type: string
                          description: |
                            The Object Storage cluster where a bucket to which the key is granting access
                            is hosted.
                          example: ap-south-1
                        permissions:
                          type: string
                          description: |
                            This Limited Access Key's permissions for the selected bucket.
                          example: read_only
                          enum:
                          - read_write
                          - read_only
                  label:
                    type: string
                    description: |
                      The label given to this key. For display purposes only.
                    example: my-key
                  limited:
                    type: boolean
                    description: |
                      __Read-only__ Whether or not this key is a limited access key. Will return `false`
                      if this key grants full access to all buckets on the user's
                      account.
                    example: true
                    readOnly: true
                  secret_key:
                    type: string
                    description: |
                      __Read-only__ This keypair's secret key. Only returned on key creation.
                    example: OiA6F5r0niLs3QA2stbyq7mY5VCV7KqOzcmitmHw
                    readOnly: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-object-storage-key
      security:
      - personalAccessToken: []
      - oauth:
        - object_storage:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
            https://api.linode.com/v4/object-storage/keys/12345
      - lang: CLI
        source: |-
          linode-cli object-storage keys-view \
            --keyId 12345
      x-linode-cli-action: keys-view
      x-original-op-id: getObjectStorageKey
      x-original-op-title: Object Storage Key View
    put:
      servers:
      - url: https://api.linode.com/v4
      operationId: put-object-storage-key
      summary: Update an object storage key
      tags:
      - Keys
      description: |-
        Updates an Object Storage Key on your account.

        - A successful request triggers an `obj_access_key_update` event.


        ---


        - __CLI__.

            ```
            linode-cli object-storage keys-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            object_storage:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli object-storage keys-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: object_storage:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The fields to update.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                label:
                  type: string
                  description: |
                    The label for this keypair, for display purposes only.
                  example: '{{label}}'
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Update Successful
          content:
            application/json:
              schema:
                type: object
                description: |
                  A keypair used to communicate with the Object Storage S3 API.
                x-akamai:
                  file-path: schemas/object-storage-key.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This keypair's unique ID
                    example: 123
                    readOnly: true
                  access_key:
                    type: string
                    description: |
                      __Read-only__ This keypair's access key. This is not secret.
                    example: KVAKUTGBA4WTR2NSJQ81
                    readOnly: true
                  bucket_access:
                    type: array
                    description: |-
                      Defines this key as a Limited Access Key. Limited Access Keys restrict this Object Storage key's access to only the bucket(s) declared in this array and define their bucket-level permissions.

                        Limited Access Keys can:

                        - [list all buckets](https://techdocs.akamai.com/linode-api/reference/get-object-storage-buckets) available on this Account, but cannot perform any actions on a bucket unless it has access to the bucket.

                        - [create new buckets](https://techdocs.akamai.com/linode-api/reference/post-object-storage-bucket), but do not have any access to the buckets it creates, unless explicitly given access to them.

                        __Note:__ You can create an Object Storage Limited Access Key without access to any buckets.
                        This is achieved by sending a request with an empty `bucket_access` array.

                        __Note:__ If this field is omitted, a regular unlimited access key is issued.
                    items:
                      type: object
                      additionalProperties: false
                      properties:
                        bucket_name:
                          type: string
                          description: |
                            The unique label of the bucket to which the key will grant limited access.
                          example: example-bucket
                        cluster:
                          type: string
                          description: |
                            The Object Storage cluster where a bucket to which the key is granting access
                            is hosted.
                          example: ap-south-1
                        permissions:
                          type: string
                          description: |
                            This Limited Access Key's permissions for the selected bucket.
                          example: read_only
                          enum:
                          - read_write
                          - read_only
                  label:
                    type: string
                    description: |
                      The label given to this key. For display purposes only.
                    example: my-key
                  limited:
                    type: boolean
                    description: |
                      __Read-only__ Whether or not this key is a limited access key. Will return `false`
                      if this key grants full access to all buckets on the user's
                      account.
                    example: true
                    readOnly: true
                  secret_key:
                    type: string
                    description: |
                      __Read-only__ This keypair's secret key. Only returned on key creation.
                    example: OiA6F5r0niLs3QA2stbyq7mY5VCV7KqOzcmitmHw
                    readOnly: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-object-storage-key
      security:
      - personalAccessToken: []
      - oauth:
        - object_storage:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \ -H "Authorization: Bearer $TOKEN" \
            -X PUT -d '{
              "label": "my-object-storage-key"
            }' \
            https://api.linode.com/v4/object-storage/keys/12345
      - lang: CLI
        source: |-
          linode-cli object-storage keys-update \
            --keyId 12345
            --label "my-object-storage-key"
      x-linode-cli-action: keys-update
      x-original-op-id: updateObjectStorageKey
      x-original-op-title: Object Storage Key Update
    delete:
      servers:
      - url: https://api.linode.com/v4
      operationId: delete-object-storage-key
      summary: Revoke an object storage key
      tags:
      - Keys
      description: |-
        Revokes an Object Storage Key. This keypair will no longer be usable by third-party clients.

        - A successful request triggers an `obj_access_key_delete` event.


        ---


        - __CLI__.

            ```
            linode-cli object-storage keys-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            object_storage:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli object-storage keys-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: object_storage:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Deletion successful
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-object-storage-key
      security:
      - personalAccessToken: []
      - oauth:
        - object_storage:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
            -X DELETE \
            https://api.linode.com/v4/object-storage/keys/12345
      - lang: CLI
        source: linode-cli object-storage keys-delete 12345
      x-linode-cli-action: keys-delete
      x-original-op-id: deleteObjectStorageKey
      x-original-op-title: Object Storage Key Revoke
    x-linode-cli-command: object-storage
  /object-storage/transfer:
    x-akamai:
      file-path: paths/object-storage-transfer.yaml
      path-info: /object-storage/transfer
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-object-storage-transfer
      summary: Get object storage transfer data
      tags:
      - Object storage
      description: |-
        The amount of outbound data transfer used by your account's Object Storage buckets. Object Storage adds 1 terabyte of outbound data transfer to your data transfer pool. See the [Object Storage Overview](https://www.linode.com/docs/products/storage/object-storage/#pricing) guide for details on Object Storage transfer quotas.


        ---


        - __OAuth__.

            ```
            object_storage:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: object_storage:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns the amount of outbound data transfer used by your account's Object Storage buckets.
          content:
            application/json:
              schema:
                additionalProperties: false
                properties:
                  used:
                    type: integer
                    description: |
                      The amount of outbound data transfer used by your account's Object Storage buckets,
                      in bytes, for the current month's billing cycle.
                    example: 12956600198
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-object-storage-transfer
      security:
      - personalAccessToken: []
      - oauth:
        - object_storage:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
            https://api.linode.com/v4/object-storage/transfer/
      x-linode-cli-skip: true
      x-original-op-id: getObjectStorageTransfer
      x-original-op-title: Object Storage Transfer
        View
  /placement/groups:
    x-akamai:
      file-path: paths/groups.yaml
      path-info: /placement/groups
    post:
      x-linode charge: true
      x-linode grant: add_placement
      operationId: post-placement-group
      summary: Create placement group
      tags:
      - Placement groups
      description: |-
        Creates a placement group on your account. Placement groups let you set the physical location of your compute instances in a data center (region) to best suit your need.

        - For this request to complete successfully, your user must have the `add_placement` grant.
        - We offer an [example API workflow](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/#create-a-placement-group) you can follow to create a placement group.

        __Note__. With the beta release, this service is only available in [select regions](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/#availability).


        ---


        - __CLI__.

            ```
            linode-cli placement group-create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli placement group-create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The requested initial state of the new placement group.
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - affinity_type
              - is_strict
              - label
              - region
              allOf:
              - type: object
                description: |
                  Common properties for creating and rebuilding placement groups.
                x-akamai:
                  file-path: schemas/placement-group-request.yaml
                additionalProperties: false
                properties:
                  affinity_type:
                    type: string
                    description: |-
                      Determine how compute instances are to be distributed in your placement group. Set to `anti-affinity:local` to place compute instances in separate fault domains, but still in the same region. This better supports a high-availability model.

                      __Note__: With the limited availability release, only `anti_affinity:local` is available for `affinity_type`.
                    example:
                    - anti_affinity:local
                    enum:
                    - anti_affinity:local
                    x-linode-cli-display: 1
                  is_strict:
                    type: boolean
                    description: |-
                      How the API enforces your `affinity_type`:

                      - Set to `true`, your group is strict, You can't add more compute instances to your placement group if your preferred container lacks capacity or is unavailable.
                      - Set to `false`, your group is flexible. You can add more compute instances to it even if they violate the `affinity_type`. If you violate the `affinity_type` your placement group becomes non-compliant and you need to wait for our assistance.

                      __Note__: Once you create a placement group, you can't change its `is_strict` setting.
                    example: true
                  label:
                    type: string
                    description: |-
                      A unique name for the placement group. A placement group `label` has the following constraints:

                      - It needs to begin and end with an alphanumeric character.
                      - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`), or periods (`.`).
                    example: PG_Miami_failover
                    minLength: 1
                  region:
                    type: string
                    description: |
                      The data center that houses the compute instances you want to add to your placement
                      group. Run the [List Linodes](https://techdocs.akamai.com/linode-api/reference/get-linode-instances)
                      operation to view your compute instances and store the `region`
                      identifier.
                    example: us-iad
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
        x-linode-cli-allowed-defaults:
        - affinity_type
        - is_strict
        - label
        - region
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A new placement group is being created.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/placement-group.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      The placement group's ID. You need to provide it for all operations impacting it.
                    example: 528
                    nullable: false
                    x-linode-cli-display: 1
                  affinity_type:
                    type: string
                    description: |-
                      __Read-only__ How compute instances are distributed in your placement group. `anti-affinity:local` places compute instances in separate fault domains, but still in the same region.

                      __Note__: With the limited availability release, only `anti_affinity:local` is available for `affinity_type`.
                    example: anti-affinity:local
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  is_compliant:
                    type: boolean
                    description: |
                      The availability of the placement group. `true` indicates the group is available
                      and all the compute instances in your group meet your `affinity_type`
                      and `is_strict` enforcement setting. If `false`, the group is
                      unavailable. One or more compute instances violate your `affinity_type`
                      and `is_strict` settings. You need to wait for our assistance
                      to physically move compute instances to make the group compliant.
                    example: true
                    nullable: false
                  is_strict:
                    type: boolean
                    description: |-
                      How the API enforces your `affinity_type`:

                      - Set to `true`, your group is strict. You can't add more compute instances to your placement group if your preferred container lacks capacity or is unavailable.
                      - Set to `false`, your group is flexible. You can add more compute instances to it even if they violate the `affinity_type`. If you violate the `affinity_type` your placement group becomes non-compliant and you need to wait for our assistance.
                    example: true
                    nullable: false
                  label:
                    type: string
                    description: |-
                      The unique name set for the placement group. A label has these constraints:

                      - It needs to begin and end with an alphanumeric character.
                      - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                    example: PG_Miami_failover
                    minLength: 1
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  members:
                    type: array
                    description: |
                      An array of compute instances included in the placement group.
                    items:
                      type: object
                      additionalProperties: false
                      properties:
                        is_compliant:
                          type: boolean
                          description: |
                            Whether or not the compute instance meets your group's `affinity_type`
                            and `is_strict` settings. If `false`, the compute instance
                            makes the group non-compliant. You need to wait for our
                            assistance to physically move it to meet your model, once
                            applicable space is available.
                          example: true
                        linode_id:
                          type: integer
                          description: |
                            __Read-only__ The unique identifier for a compute instance included
                            in the placement group.
                          example: 123
                          readOnly: true
                  region:
                    type: string
                    description: |
                      __Read-only__ The [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the placement group was deployed.
                    example: us-mia
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-placement-group
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "label": "PG_Miami_failover",
                "region": "us-mia",
                "affinity_type": "anti-affinity:local",
                "is_strict": true
              }' \
              https://api.linode.com/v4/placement/groups
      - lang: CLI
        source: |-
          linode-cli placement create \
            --label PG_Miami_failover \
            --region us-mia \
            --affinity_type "anti-affinity:local" \
            --is_strict true
      x-linode-cli-action: group-create
      x-original-op-id: createPlacementGroup
      x-original-op-title: Placement Group Create
    get:
      operationId: get-placement-groups
      summary: List placement groups
      tags:
      - Placement groups
      description: |-
        Returns a paginated list of placement groups you have permission to view.


        ---


        - __OAuth__.

            ```
            placement:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: placement:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns an array of all placement groups on your Account.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      x-akamai:
                        file-path: schemas/placement-group.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The placement group's ID. You need to provide it for all operations
                            impacting it.
                          example: 528
                          nullable: false
                          x-linode-cli-display: 1
                        affinity_type:
                          type: string
                          description: |-
                            __Read-only__ How compute instances are distributed in your placement group. `anti-affinity:local` places compute instances in separate fault domains, but still in the same region.

                            __Note__: With the limited availability release, only `anti_affinity:local` is available for `affinity_type`.
                          example: anti-affinity:local
                          readOnly: true
                          x-linode-cli-display: 1
                          x-linode-filterable: true
                        is_compliant:
                          type: boolean
                          description: |
                            The availability of the placement group. `true` indicates the group is
                            available and all the compute instances in your group
                            meet your `affinity_type` and `is_strict` enforcement
                            setting. If `false`, the group is unavailable. One or
                            more compute instances violate your `affinity_type` and
                            `is_strict` settings. You need to wait for our assistance
                            to physically move compute instances to make the group
                            compliant.
                          example: true
                          nullable: false
                        is_strict:
                          type: boolean
                          description: |-
                            How the API enforces your `affinity_type`:

                            - Set to `true`, your group is strict. You can't add more compute instances to your placement group if your preferred container lacks capacity or is unavailable.
                            - Set to `false`, your group is flexible. You can add more compute instances to it even if they violate the `affinity_type`. If you violate the `affinity_type` your placement group becomes non-compliant and you need to wait for our assistance.
                          example: true
                          nullable: false
                        label:
                          type: string
                          description: |-
                            The unique name set for the placement group. A label has these constraints:

                            - It needs to begin and end with an alphanumeric character.
                            - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                          example: PG_Miami_failover
                          minLength: 1
                          x-linode-cli-display: 1
                          x-linode-filterable: true
                        members:
                          type: array
                          description: |
                            An array of compute instances included in the placement group.
                          items:
                            type: object
                            additionalProperties: false
                            properties:
                              is_compliant:
                                type: boolean
                                description: |
                                  Whether or not the compute instance meets your group's `affinity_type`
                                  and `is_strict` settings. If `false`, the compute
                                  instance makes the group non-compliant. You need
                                  to wait for our assistance to physically move it
                                  to meet your model, once applicable space is available.
                                example: true
                              linode_id:
                                type: integer
                                description: |
                                  __Read-only__ The unique identifier for a compute instance included
                                  in the placement group.
                                example: 123
                                readOnly: true
                        region:
                          type: string
                          description: |
                            __Read-only__ The [region](https://techdocs.akamai.com/linode-api/reference/get-regions)
                            where the placement group was deployed.
                          example: us-mia
                          readOnly: true
                          x-linode-cli-display: 1
                          x-linode-filterable: true
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-placement-groups
      security:
      - personalAccessToken: []
      - oauth:
        - placement:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/placement/groups
      - lang: CLI
        source: linode-cli placement-list
      x-linode-cli-action:
      - groups-list
      - groups-ls
      x-original-op-id: getPlacementGroups
      x-original-op-title: Placement Groups List
    x-linode-cli-command: placement
  /placement/groups/{groupId}:
    x-akamai:
      file-path: paths/group.yaml
      path-info: /placement/groups/{groupId}
    parameters:
    - name: groupId
      in: path
      description: |
        ID of the placement group to look up. Run the [List placement groups](https://techdocs.akamai.com/linode-api/reference/get-placement-groups)
        operation and store the `id` for the applicable placement group.
      example: '{{groupId}}'
      x-akamai:
        file-path: parameters/group-id-path-043ea725.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-placement-group
      summary: Get a placement group
      tags:
      - Placement groups
      description: |-
        View a specific placement group by ID.


        ---


        - __CLI__.

            ```
            linode-cli placement group-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli placement group-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        200:
          description: |
            Returns a single placement group object.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/placement-group.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      The placement group's ID. You need to provide it for all operations impacting it.
                    example: 528
                    nullable: false
                    x-linode-cli-display: 1
                  affinity_type:
                    type: string
                    description: |-
                      __Read-only__ How compute instances are distributed in your placement group. `anti-affinity:local` places compute instances in separate fault domains, but still in the same region.

                      __Note__: With the limited availability release, only `anti_affinity:local` is available for `affinity_type`.
                    example: anti-affinity:local
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  is_compliant:
                    type: boolean
                    description: |
                      The availability of the placement group. `true` indicates the group is available
                      and all the compute instances in your group meet your `affinity_type`
                      and `is_strict` enforcement setting. If `false`, the group is
                      unavailable. One or more compute instances violate your `affinity_type`
                      and `is_strict` settings. You need to wait for our assistance
                      to physically move compute instances to make the group compliant.
                    example: true
                    nullable: false
                  is_strict:
                    type: boolean
                    description: |-
                      How the API enforces your `affinity_type`:

                      - Set to `true`, your group is strict. You can't add more compute instances to your placement group if your preferred container lacks capacity or is unavailable.
                      - Set to `false`, your group is flexible. You can add more compute instances to it even if they violate the `affinity_type`. If you violate the `affinity_type` your placement group becomes non-compliant and you need to wait for our assistance.
                    example: true
                    nullable: false
                  label:
                    type: string
                    description: |-
                      The unique name set for the placement group. A label has these constraints:

                      - It needs to begin and end with an alphanumeric character.
                      - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                    example: PG_Miami_failover
                    minLength: 1
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  members:
                    type: array
                    description: |
                      An array of compute instances included in the placement group.
                    items:
                      type: object
                      additionalProperties: false
                      properties:
                        is_compliant:
                          type: boolean
                          description: |
                            Whether or not the compute instance meets your group's `affinity_type`
                            and `is_strict` settings. If `false`, the compute instance
                            makes the group non-compliant. You need to wait for our
                            assistance to physically move it to meet your model, once
                            applicable space is available.
                          example: true
                        linode_id:
                          type: integer
                          description: |
                            __Read-only__ The unique identifier for a compute instance included
                            in the placement group.
                          example: 123
                          readOnly: true
                  region:
                    type: string
                    description: |
                      __Read-only__ The [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the placement group was deployed.
                    example: us-mia
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-placement-group
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X GET \
              https://api.linode.com/v4/placement/groups/528
      - lang: CLI
        source: linode-cli placement view 528
      x-linode-cli-action: group-view
      x-linode-grant: read-only
      x-original-op-id: getPlacementGroup
      x-original-op-title: Placement Group View
    put:
      operationId: put-placement-group
      summary: Update a placement group
      tags:
      - Placement groups
      description: |-
        Change the `label` for a specific placement group. This is the only value you can update. However, you can [add](#placement-group-add-linode) more compute instances or [remove](#placement-group-remove-linode) existing ones.


        ---


        - __CLI__.

            ```
            linode-cli placement group-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli placement group-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Include the `label` parameter to update the name of your placement group.
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                label:
                  type: string
                  description: |-
                    A unique name for the placement group. A placement group `label` has the following constraints:

                    - It needs to begin and end with an alphanumeric character.
                    - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`), or periods (`.`).
                  example: '{{label}}'
                  minLength: 1
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The updated placement group.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/placement-group.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      The placement group's ID. You need to provide it for all operations impacting it.
                    example: 528
                    nullable: false
                    x-linode-cli-display: 1
                  affinity_type:
                    type: string
                    description: |-
                      __Read-only__ How compute instances are distributed in your placement group. `anti-affinity:local` places compute instances in separate fault domains, but still in the same region.

                      __Note__: With the limited availability release, only `anti_affinity:local` is available for `affinity_type`.
                    example: anti-affinity:local
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  is_compliant:
                    type: boolean
                    description: |
                      The availability of the placement group. `true` indicates the group is available
                      and all the compute instances in your group meet your `affinity_type`
                      and `is_strict` enforcement setting. If `false`, the group is
                      unavailable. One or more compute instances violate your `affinity_type`
                      and `is_strict` settings. You need to wait for our assistance
                      to physically move compute instances to make the group compliant.
                    example: true
                    nullable: false
                  is_strict:
                    type: boolean
                    description: |-
                      How the API enforces your `affinity_type`:

                      - Set to `true`, your group is strict. You can't add more compute instances to your placement group if your preferred container lacks capacity or is unavailable.
                      - Set to `false`, your group is flexible. You can add more compute instances to it even if they violate the `affinity_type`. If you violate the `affinity_type` your placement group becomes non-compliant and you need to wait for our assistance.
                    example: true
                    nullable: false
                  label:
                    type: string
                    description: |-
                      The unique name set for the placement group. A label has these constraints:

                      - It needs to begin and end with an alphanumeric character.
                      - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                    example: PG_Miami_failover
                    minLength: 1
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  members:
                    type: array
                    description: |
                      An array of compute instances included in the placement group.
                    items:
                      type: object
                      additionalProperties: false
                      properties:
                        is_compliant:
                          type: boolean
                          description: |
                            Whether or not the compute instance meets your group's `affinity_type`
                            and `is_strict` settings. If `false`, the compute instance
                            makes the group non-compliant. You need to wait for our
                            assistance to physically move it to meet your model, once
                            applicable space is available.
                          example: true
                        linode_id:
                          type: integer
                          description: |
                            __Read-only__ The unique identifier for a compute instance included
                            in the placement group.
                          example: 123
                          readOnly: true
                  region:
                    type: string
                    description: |
                      __Read-only__ The [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the placement group was deployed.
                    example: us-mia
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-placement-group
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "label": "PG_Miami_failover_update"
              }' \
              https://api.linode.com/v4/placement/groups/528
      - lang: CLI
        source: |-
          linode-cli placement update 528 \
            --label PG_Miami_failover_update
      x-linode-cli-action: group-update
      x-linode-grant: read-write
      x-original-op-id: updatePlacementGroup
      x-original-op-title: Placement Group Update
    delete:
      operationId: delete-placement-group
      summary: Delete a placement group
      tags:
      - Placement groups
      description: |-
        Deletes a placement group you have permission to `read_write`.

        - Deleting a placement group can't be undone.
        - All compute instances need to be [removed](https://techdocs.akamai.com/linode-api/reference/post-group-unassign) before you can delete a placement group.
        - If your placement group is non-compliant, you can't delete it. You need to wait for our help to bring it [compliant](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/#non-compliance-precedence).


        ---


        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Delete successful
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-placement-group
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/placement/groups/528
      - lang: CLI
        source: linode-cli placement delete 528
      x-linode-cli-action:
      - group-delete
      - group-rm
      x-linode-grant: read_write
      x-original-op-id: deletePlacementGroup
      x-original-op-title: Placement Group Delete
    x-linode-cli-command: placement
  /placement/groups/{groupId}/assign:
    x-akamai:
      file-path: paths/group-assign.yaml
      path-info: /placement/groups/{groupId}/assign
    parameters:
    - name: groupId
      in: path
      description: |-
        ID of the placement group to look up. Run the [List placement groups](https://techdocs.akamai.com/linode-api/reference/get-placement-groups) operation and store the `id`
        for the applicable placement group.
      example: '{{groupId}}'
      x-akamai:
        file-path: parameters/group-id-path.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-group-add-linode
      summary: Assign a placement group
      tags:
      - Placement groups
      description: |-
        Add one or more compute instances to your placement group. Check out our [example API workflow](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/#create-a-placement-group) to create a placement group and add compute instances.


        ---


        - __CLI__.

            ```
            linode-cli placement assign-linode
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli placement assign-linode
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The compute instances you want to add to your placement group.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                The compute instances included in a placement group.
              x-akamai:
                file-path: schemas/placement-group-compute-instances.yaml
              additionalProperties: false
              properties:
                linodes:
                  type: array
                  description: |
                    The `linodeId` values for individual compute instances included in the placement group.
                  example: 528
                  items:
                    type: integer
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The compute instance was added successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/placement-group.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      The placement group's ID. You need to provide it for all operations impacting it.
                    example: 528
                    nullable: false
                    x-linode-cli-display: 1
                  affinity_type:
                    type: string
                    description: |-
                      __Read-only__ How compute instances are distributed in your placement group. `anti-affinity:local` places compute instances in separate fault domains, but still in the same region.

                      __Note__: With the limited availability release, only `anti_affinity:local` is available for `affinity_type`.
                    example: anti-affinity:local
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  is_compliant:
                    type: boolean
                    description: |
                      The availability of the placement group. `true` indicates the group is available
                      and all the compute instances in your group meet your `affinity_type`
                      and `is_strict` enforcement setting. If `false`, the group is
                      unavailable. One or more compute instances violate your `affinity_type`
                      and `is_strict` settings. You need to wait for our assistance
                      to physically move compute instances to make the group compliant.
                    example: true
                    nullable: false
                  is_strict:
                    type: boolean
                    description: |-
                      How the API enforces your `affinity_type`:

                      - Set to `true`, your group is strict. You can't add more compute instances to your placement group if your preferred container lacks capacity or is unavailable.
                      - Set to `false`, your group is flexible. You can add more compute instances to it even if they violate the `affinity_type`. If you violate the `affinity_type` your placement group becomes non-compliant and you need to wait for our assistance.
                    example: true
                    nullable: false
                  label:
                    type: string
                    description: |-
                      The unique name set for the placement group. A label has these constraints:

                      - It needs to begin and end with an alphanumeric character.
                      - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                    example: PG_Miami_failover
                    minLength: 1
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  members:
                    type: array
                    description: |
                      An array of compute instances included in the placement group.
                    items:
                      type: object
                      additionalProperties: false
                      properties:
                        is_compliant:
                          type: boolean
                          description: |
                            Whether or not the compute instance meets your group's `affinity_type`
                            and `is_strict` settings. If `false`, the compute instance
                            makes the group non-compliant. You need to wait for our
                            assistance to physically move it to meet your model, once
                            applicable space is available.
                          example: true
                        linode_id:
                          type: integer
                          description: |
                            __Read-only__ The unique identifier for a compute instance included
                            in the placement group.
                          example: 123
                          readOnly: true
                  region:
                    type: string
                    description: |
                      __Read-only__ The [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the placement group was deployed.
                    example: us-mia
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-group-add-linode
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "linode": [123, 456],
                "non-compliant": false
              }' \
              https://api.linode.com/v4/placement/groups/528/assign
      - lang: CLI
        source: |-
          linode-cli placement assignLinode 528 \
            --linodes 123 456 \
            --non-compliant true
      x-linode-cli-action: assign-linode
      x-linode-grant: read-write
      x-original-op-id: placementGroupAddLinode
      x-original-op-title: Placement Group Assign
    x-linode-cli-command: placement
  /placement/groups/{groupId}/unassign:
    x-akamai:
      file-path: paths/unassign.yaml
      path-info: /placement/groups/{groupId}/unassign
    parameters:
    - name: groupId
      in: path
      description: |
        ID of the placement group to look up. Run the [List placement groups](https://techdocs.akamai.com/linode-api/reference/get-placement-groups)
        operation and store the `id` for the applicable placement group.
      example: '{{groupId}}'
      x-akamai:
        file-path: parameters/group-id-path-0d373f63.yaml
      required: true
      schema:
        type: integer
        x-ref: ../schemas/added-tbd.yaml
    post:
      operationId: post-group-unassign
      summary: Unassign a placement group
      tags:
      - Placement groups
      description: |-
        Remove one or more compute instances from your placement group.


        ---


        - __CLI__.

            ```
            linode-cli placement unassign-linode
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli placement unassign-linode
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The compute instances you want to remove from your placement group.
        required: true
        content:
          application/json:
            schema:
              type: array
              description: |
                The `linodeId` values for individual compute instances included in the placement group.
              example: 528
              items:
                type: integer
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The compute instance was removed successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/placement-group.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      The placement group's ID. You need to provide it for all operations impacting it.
                    example: 528
                    nullable: false
                    x-linode-cli-display: 1
                  affinity_type:
                    type: string
                    description: |-
                      __Read-only__ How compute instances are distributed in your placement group. `anti-affinity:local` places compute instances in separate fault domains, but still in the same region.

                      __Note__: With the limited availability release, only `anti_affinity:local` is available for `affinity_type`.
                    example: anti-affinity:local
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  is_compliant:
                    type: boolean
                    description: |
                      The availability of the placement group. `true` indicates the group is available
                      and all the compute instances in your group meet your `affinity_type`
                      and `is_strict` enforcement setting. If `false`, the group is
                      unavailable. One or more compute instances violate your `affinity_type`
                      and `is_strict` settings. You need to wait for our assistance
                      to physically move compute instances to make the group compliant.
                    example: true
                    nullable: false
                  is_strict:
                    type: boolean
                    description: |-
                      How the API enforces your `affinity_type`:

                      - Set to `true`, your group is strict. You can't add more compute instances to your placement group if your preferred container lacks capacity or is unavailable.
                      - Set to `false`, your group is flexible. You can add more compute instances to it even if they violate the `affinity_type`. If you violate the `affinity_type` your placement group becomes non-compliant and you need to wait for our assistance.
                    example: true
                    nullable: false
                  label:
                    type: string
                    description: |-
                      The unique name set for the placement group. A label has these constraints:

                      - It needs to begin and end with an alphanumeric character.
                      - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                    example: PG_Miami_failover
                    minLength: 1
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  members:
                    type: array
                    description: |
                      An array of compute instances included in the placement group.
                    items:
                      type: object
                      additionalProperties: false
                      properties:
                        is_compliant:
                          type: boolean
                          description: |
                            Whether or not the compute instance meets your group's `affinity_type`
                            and `is_strict` settings. If `false`, the compute instance
                            makes the group non-compliant. You need to wait for our
                            assistance to physically move it to meet your model, once
                            applicable space is available.
                          example: true
                        linode_id:
                          type: integer
                          description: |
                            __Read-only__ The unique identifier for a compute instance included
                            in the placement group.
                          example: 123
                          readOnly: true
                  region:
                    type: string
                    description: |
                      __Read-only__ The [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the placement group was deployed.
                    example: us-mia
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-group-unassign
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "linode": [123, 456]
              }' \
              https://api.linode.com/v4/placement/groups/528/unassign
      - lang: CLI
        source: |-
          linode-cli placement unassignLinode 528 \
            --linode 123 456
      x-linode-cli-action: unassign-linode
      x-linode-grant: read-write
      x-original-op-id: post-group-unassign
      x-original-op-title: Placement Group Unassign
    x-linode-cli-command: placement
  /profile:
    x-akamai:
      file-path: paths/profile.yaml
      path-info: /profile
    get:
      operationId: get-profile
      summary: Get a profile
      tags:
      - Profile
      description: |-
        Returns information about the current User. This can be used to see who is acting in applications where more than one token is managed. For example, in third-party OAuth applications.

        This operation is always accessible, no matter what OAuth scopes the acting token has.
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Profile response.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A Profile represents your User in our system. This is where you can change information about
                  your User. This information is available to any OAuth Client regardless
                  of requested scopes, and can be used to populate User information
                  in third-party applications.
                x-akamai:
                  file-path: schemas/profile.yaml
                additionalProperties: false
                properties:
                  authentication_type:
                    type: string
                    description: |-
                      __Read-only__ This account's Cloud Manager authentication type. Authentication types are chosen through Cloud Manager and authorized when logging into your account. These authentication types are either the user's password (in conjunction with their username), or the name of their identity provider such as GitHub. For example, if a user:

                      - Has never used Third-Party Authentication, their authentication type will be `password`.
                      - Is using Third-Party Authentication, their authentication type will be the name of their Identity Provider (eg. `github`).
                      - Has used Third-Party Authentication and has since revoked it, their authentication type will be `password`.

                      __Note:__ This functionality may not yet be available in Cloud Manager. See the [Cloud Manager Changelog](https://www.linode.com/docs/products/tools/cloud-manager/release-notes/) for the latest updates.
                    example: password
                    readOnly: true
                    enum:
                    - password
                    - github
                  authorized_keys:
                    type: array
                    description: |
                      The list of SSH Keys authorized to use Lish for your User. This value is ignored
                      if `lish_auth_method` is "disabled."
                    example: null
                    nullable: true
                    items:
                      type: string
                      format: ssh-key
                  email:
                    type: string
                    description: |
                      Your email address.  This address will be used for communication with Linode as necessary.
                    example: example-user@gmail.com
                    format: email
                    x-linode-cli-display: 2
                  email_notifications:
                    type: boolean
                    description: |
                      If true, you will receive email notifications about account activity.  If false, you may
                      still receive business-critical communications through email.
                    example: true
                  ip_whitelist_enabled:
                    type: boolean
                    description: |
                      If true, logins for your User will only be allowed from whitelisted IPs. This setting
                      is currently deprecated, and cannot be enabled. If you disable
                      this setting, you will not be able to re-enable it.
                    example: false
                    deprecated: true
                  lish_auth_method:
                    type: string
                    description: |-
                      The authentication methods that are allowed when connecting to [the Linode Shell (Lish)](https://www.linode.com/docs/guides/lish/).

                      - `keys_only` is the most secure if you intend to use Lish.
                      - `disabled` is recommended if you do not intend to use Lish at all.
                      - If this account's Cloud Manager authentication type is set to a Third-Party Authentication method, `password_keys` cannot be used as your Lish authentication method. To view this account's Cloud Manager `authentication_type` field, send a request to the [Get a profile](https://techdocs.akamai.com/linode-api/reference/get-profile) operation.
                    example: keys_only
                    enum:
                    - password_keys
                    - keys_only
                    - disabled
                  referrals:
                    type: object
                    description: |-
                      __Read-only__ Information about your status in our referral program.

                      This information becomes accessible after this Profile's Account has established at least $25.00 USD of total payments.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      code:
                        type: string
                        description: |
                          __Read-only__ Your referral code.  If others use this when signing up for Linode,
                          you will receive account credit.
                        example: 871be32f49c1411b14f29f618aaf0c14637fb8d3
                        readOnly: true
                      completed:
                        type: integer
                        description: |
                          __Read-only__ The number of completed signups with your referral code.
                        example: 0
                        readOnly: true
                      credit:
                        type: integer
                        description: |
                          __Read-only__ The amount of account credit in US Dollars issued to you through
                          the referral program.
                        example: 0
                        readOnly: true
                      pending:
                        type: integer
                        description: |
                          __Read-only__ The number of pending signups with your referral code.  You
                          will not receive credit for these signups until they are
                          completed.
                        example: 0
                        readOnly: true
                      total:
                        type: integer
                        description: |
                          __Read-only__ The number of users who have signed up with your referral code.
                        example: 0
                        readOnly: true
                      url:
                        type: string
                        description: |
                          __Read-only__ Your referral url, used to direct others to sign up for Linode
                          with your referral code.
                        example: https://www.linode.com/?r=871be32f49c1411b14f29f618aaf0c14637fb8d3
                        readOnly: true
                        format: url
                  restricted:
                    type: boolean
                    description: |
                      If true, your User has restrictions on what can be accessed on your Account. To
                      get details on what entities/actions you can access/perform,
                      run [List grants](https://techdocs.akamai.com/linode-api/reference/get-profile-grants).
                    example: false
                    x-linode-cli-display: 3
                  timezone:
                    type: string
                    description: |
                      The timezone you prefer to see times in. This is not used by the API directly. It is
                      provided for the benefit of clients such as the Linode Cloud
                      Manager and other clients built on the API. All times returned
                      by the API are in UTC.
                    example: US/Eastern
                  two_factor_auth:
                    type: boolean
                    description: |
                      If true, logins from untrusted computers will require Two Factor Authentication.  Run
                      [Create a two factor secret](https://techdocs.akamai.com/linode-api/reference/post-tfa-enable)
                      to enable Two Factor Authentication.
                    example: true
                    x-linode-cli-display: 4
                  uid:
                    type: integer
                    description: |
                      __Read-only__ Your unique ID in our system. This value will never change, and can
                      safely be used to identify your User.
                    example: 1234
                    readOnly: true
                  username:
                    type: string
                    description: |
                      __Read-only__ Your username, used for logging in to our system.
                    example: exampleUser
                    readOnly: true
                    x-linode-cli-display: 1
                  verified_phone_number:
                    type: string
                    description: |-
                      __Read-only__ The phone number verified for this Profile with the [Verify a phone number](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number-verify) operation.

                      `null` if this Profile has no verified phone number.
                    example: '+5555555555'
                    nullable: true
                    readOnly: true
                    format: phone
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-profile
      security:
      - personalAccessToken: []
      - oauth: []
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/profile
      - lang: CLI
        source: linode-cli profile view
      x-linode-cli-action: view
      x-original-op-id: getProfile
      x-original-op-title: Profile View
    put:
      operationId: put-profile
      summary: Update a profile
      tags:
      - Profile
      description: |-
        Update information in your Profile.  This operation requires the "account:read_write" OAuth Scope.

        __Parent and child accounts__

        In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, the following apply:

        - You can't edit the `email` for a child account parent user (proxy user). This value is fixed and set when you provision this environment.


        ---


        - __CLI__.

            ```
            linode-cli profile update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli profile update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The fields to update.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                A Profile represents your User in our system. This is where you can change information about
                your User. This information is available to any OAuth Client regardless
                of requested scopes, and can be used to populate User information
                in third-party applications.
              x-akamai:
                file-path: schemas/profile.yaml
              additionalProperties: false
              properties:
                authentication_type:
                  type: string
                  description: |-
                    __Read-only__ This account's Cloud Manager authentication type. Authentication types are chosen through Cloud Manager and authorized when logging into your account. These authentication types are either the user's password (in conjunction with their username), or the name of their identity provider such as GitHub. For example, if a user:

                    - Has never used Third-Party Authentication, their authentication type will be `password`.
                    - Is using Third-Party Authentication, their authentication type will be the name of their Identity Provider (eg. `github`).
                    - Has used Third-Party Authentication and has since revoked it, their authentication type will be `password`.

                    __Note:__ This functionality may not yet be available in Cloud Manager. See the [Cloud Manager Changelog](https://www.linode.com/docs/products/tools/cloud-manager/release-notes/) for the latest updates.
                  example: '{{authentication_type}}'
                  readOnly: true
                  enum:
                  - password
                  - github
                authorized_keys:
                  type: array
                  description: |
                    The list of SSH Keys authorized to use Lish for your User. This value is ignored if
                    `lish_auth_method` is "disabled."
                  example: null
                  nullable: true
                  items:
                    type: string
                    format: ssh-key
                email:
                  type: string
                  description: |
                    Your email address.  This address will be used for communication with Linode as necessary.
                  example: '{{email}}'
                  format: email
                  x-linode-cli-display: 2
                email_notifications:
                  type: boolean
                  description: |
                    If true, you will receive email notifications about account activity.  If false, you may
                    still receive business-critical communications through email.
                  example: '{{email_notifications}}'
                ip_whitelist_enabled:
                  type: boolean
                  description: |
                    If true, logins for your User will only be allowed from whitelisted IPs. This setting is
                    currently deprecated, and cannot be enabled. If you disable this
                    setting, you will not be able to re-enable it.
                  example: '{{ip_whitelist_enabled}}'
                  deprecated: true
                lish_auth_method:
                  type: string
                  description: |-
                    The authentication methods that are allowed when connecting to [the Linode Shell (Lish)](https://www.linode.com/docs/guides/lish/).

                    - `keys_only` is the most secure if you intend to use Lish.
                    - `disabled` is recommended if you do not intend to use Lish at all.
                    - If this account's Cloud Manager authentication type is set to a Third-Party Authentication method, `password_keys` cannot be used as your Lish authentication method. To view this account's Cloud Manager `authentication_type` field, send a request to the [Get a profile](https://techdocs.akamai.com/linode-api/reference/get-profile) operation.
                  example: '{{lish_auth_method}}'
                  enum:
                  - password_keys
                  - keys_only
                  - disabled
                referrals:
                  type: object
                  description: |-
                    __Read-only__ Information about your status in our referral program.

                    This information becomes accessible after this Profile's Account has established at least $25.00 USD of total payments.
                  additionalProperties: false
                  readOnly: true
                  properties:
                    code:
                      type: string
                      description: |
                        __Read-only__ Your referral code.  If others use this when signing up for Linode,
                        you will receive account credit.
                      example: 871be32f49c1411b14f29f618aaf0c14637fb8d3
                      readOnly: true
                    completed:
                      type: integer
                      description: |
                        __Read-only__ The number of completed signups with your referral code.
                      example: 0
                      readOnly: true
                    credit:
                      type: integer
                      description: |
                        __Read-only__ The amount of account credit in US Dollars issued to you through
                        the referral program.
                      example: 0
                      readOnly: true
                    pending:
                      type: integer
                      description: |
                        __Read-only__ The number of pending signups with your referral code.  You will
                        not receive credit for these signups until they are completed.
                      example: 0
                      readOnly: true
                    total:
                      type: integer
                      description: |
                        __Read-only__ The number of users who have signed up with your referral code.
                      example: 0
                      readOnly: true
                    url:
                      type: string
                      description: |
                        __Read-only__ Your referral url, used to direct others to sign up for Linode
                        with your referral code.
                      example: https://www.linode.com/?r=871be32f49c1411b14f29f618aaf0c14637fb8d3
                      readOnly: true
                      format: url
                restricted:
                  type: boolean
                  description: |
                    If true, your User has restrictions on what can be accessed on your Account. To get details
                    on what entities/actions you can access/perform, run [List grants](https://techdocs.akamai.com/linode-api/reference/get-profile-grants).
                  example: '{{restricted}}'
                  x-linode-cli-display: 3
                timezone:
                  type: string
                  description: |
                    The timezone you prefer to see times in. This is not used by the API directly. It is provided
                    for the benefit of clients such as the Linode Cloud Manager and
                    other clients built on the API. All times returned by the API
                    are in UTC.
                  example: '{{timezone}}'
                two_factor_auth:
                  type: boolean
                  description: |
                    If true, logins from untrusted computers will require Two Factor Authentication.  Run
                    [Create a two factor secret](https://techdocs.akamai.com/linode-api/reference/post-tfa-enable)
                    to enable Two Factor Authentication.
                  example: '{{two_factor_auth}}'
                  x-linode-cli-display: 4
                uid:
                  type: integer
                  description: |
                    __Read-only__ Your unique ID in our system. This value will never change, and can safely
                    be used to identify your User.
                  example: '{{uid}}'
                  readOnly: true
                username:
                  type: string
                  description: |
                    __Read-only__ Your username, used for logging in to our system.
                  example: '{{username}}'
                  readOnly: true
                  x-linode-cli-display: 1
                verified_phone_number:
                  type: string
                  description: |-
                    __Read-only__ The phone number verified for this Profile with the [Verify a phone number](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number-verify) operation.

                    `null` if this Profile has no verified phone number.
                  example: '{{verified_phone_number}}'
                  nullable: true
                  readOnly: true
                  format: phone
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Profile updated successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A Profile represents your User in our system. This is where you can change information about
                  your User. This information is available to any OAuth Client regardless
                  of requested scopes, and can be used to populate User information
                  in third-party applications.
                x-akamai:
                  file-path: schemas/profile.yaml
                additionalProperties: false
                properties:
                  authentication_type:
                    type: string
                    description: |-
                      __Read-only__ This account's Cloud Manager authentication type. Authentication types are chosen through Cloud Manager and authorized when logging into your account. These authentication types are either the user's password (in conjunction with their username), or the name of their identity provider such as GitHub. For example, if a user:

                      - Has never used Third-Party Authentication, their authentication type will be `password`.
                      - Is using Third-Party Authentication, their authentication type will be the name of their Identity Provider (eg. `github`).
                      - Has used Third-Party Authentication and has since revoked it, their authentication type will be `password`.

                      __Note:__ This functionality may not yet be available in Cloud Manager. See the [Cloud Manager Changelog](https://www.linode.com/docs/products/tools/cloud-manager/release-notes/) for the latest updates.
                    example: password
                    readOnly: true
                    enum:
                    - password
                    - github
                  authorized_keys:
                    type: array
                    description: |
                      The list of SSH Keys authorized to use Lish for your User. This value is ignored
                      if `lish_auth_method` is "disabled."
                    example: null
                    nullable: true
                    items:
                      type: string
                      format: ssh-key
                  email:
                    type: string
                    description: |
                      Your email address.  This address will be used for communication with Linode as necessary.
                    example: example-user@gmail.com
                    format: email
                    x-linode-cli-display: 2
                  email_notifications:
                    type: boolean
                    description: |
                      If true, you will receive email notifications about account activity.  If false, you may
                      still receive business-critical communications through email.
                    example: true
                  ip_whitelist_enabled:
                    type: boolean
                    description: |
                      If true, logins for your User will only be allowed from whitelisted IPs. This setting
                      is currently deprecated, and cannot be enabled. If you disable
                      this setting, you will not be able to re-enable it.
                    example: false
                    deprecated: true
                  lish_auth_method:
                    type: string
                    description: |-
                      The authentication methods that are allowed when connecting to [the Linode Shell (Lish)](https://www.linode.com/docs/guides/lish/).

                      - `keys_only` is the most secure if you intend to use Lish.
                      - `disabled` is recommended if you do not intend to use Lish at all.
                      - If this account's Cloud Manager authentication type is set to a Third-Party Authentication method, `password_keys` cannot be used as your Lish authentication method. To view this account's Cloud Manager `authentication_type` field, send a request to the [Get a profile](https://techdocs.akamai.com/linode-api/reference/get-profile) operation.
                    example: keys_only
                    enum:
                    - password_keys
                    - keys_only
                    - disabled
                  referrals:
                    type: object
                    description: |-
                      __Read-only__ Information about your status in our referral program.

                      This information becomes accessible after this Profile's Account has established at least $25.00 USD of total payments.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      code:
                        type: string
                        description: |
                          __Read-only__ Your referral code.  If others use this when signing up for Linode,
                          you will receive account credit.
                        example: 871be32f49c1411b14f29f618aaf0c14637fb8d3
                        readOnly: true
                      completed:
                        type: integer
                        description: |
                          __Read-only__ The number of completed signups with your referral code.
                        example: 0
                        readOnly: true
                      credit:
                        type: integer
                        description: |
                          __Read-only__ The amount of account credit in US Dollars issued to you through
                          the referral program.
                        example: 0
                        readOnly: true
                      pending:
                        type: integer
                        description: |
                          __Read-only__ The number of pending signups with your referral code.  You
                          will not receive credit for these signups until they are
                          completed.
                        example: 0
                        readOnly: true
                      total:
                        type: integer
                        description: |
                          __Read-only__ The number of users who have signed up with your referral code.
                        example: 0
                        readOnly: true
                      url:
                        type: string
                        description: |
                          __Read-only__ Your referral url, used to direct others to sign up for Linode
                          with your referral code.
                        example: https://www.linode.com/?r=871be32f49c1411b14f29f618aaf0c14637fb8d3
                        readOnly: true
                        format: url
                  restricted:
                    type: boolean
                    description: |
                      If true, your User has restrictions on what can be accessed on your Account. To
                      get details on what entities/actions you can access/perform,
                      run [List grants](https://techdocs.akamai.com/linode-api/reference/get-profile-grants).
                    example: false
                    x-linode-cli-display: 3
                  timezone:
                    type: string
                    description: |
                      The timezone you prefer to see times in. This is not used by the API directly. It is
                      provided for the benefit of clients such as the Linode Cloud
                      Manager and other clients built on the API. All times returned
                      by the API are in UTC.
                    example: US/Eastern
                  two_factor_auth:
                    type: boolean
                    description: |
                      If true, logins from untrusted computers will require Two Factor Authentication.  Run
                      [Create a two factor secret](https://techdocs.akamai.com/linode-api/reference/post-tfa-enable)
                      to enable Two Factor Authentication.
                    example: true
                    x-linode-cli-display: 4
                  uid:
                    type: integer
                    description: |
                      __Read-only__ Your unique ID in our system. This value will never change, and can
                      safely be used to identify your User.
                    example: 1234
                    readOnly: true
                  username:
                    type: string
                    description: |
                      __Read-only__ Your username, used for logging in to our system.
                    example: exampleUser
                    readOnly: true
                    x-linode-cli-display: 1
                  verified_phone_number:
                    type: string
                    description: |-
                      __Read-only__ The phone number verified for this Profile with the [Verify a phone number](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number-verify) operation.

                      `null` if this Profile has no verified phone number.
                    example: '+5555555555'
                    nullable: true
                    readOnly: true
                    format: phone
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-profile
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "email": "example-user@gmail.com",
                "timezone": "US/Eastern",
                "email_notifications": true,
                "lish_auth_method": "keys_only",
                "authorized_keys": null,
                "two_factor_auth": true,
                "restricted": false
              }' \
              https://api.linode.com/v4/profile
      - lang: CLI
        source: |-
          linode-cli profile update \
            --email example-user@gmail.com \
            --timezone US/Eastern \
            --email_notifications true \
            --list_auth_method keys_only \
            --two_factor_auth true \
            --restricted false
      x-linode-cli-action: update
      x-original-op-id: updateProfile
      x-original-op-title: Profile Update
    x-linode-cli-command: profile
  /profile/apps:
    x-akamai:
      file-path: paths/apps.yaml
      path-info: /profile/apps
    get:
      operationId: get-profile-apps
      summary: List authorized apps
      tags:
      - Apps
      description: |-
        This is a collection of OAuth apps that you've given access to your Account, and includes the level of access granted.


        ---


        - __CLI__.

            ```
            linode-cli profile apps-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli profile apps-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of apps you've authorized.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        An application you have authorized access to your Account through OAuth.
                      x-akamai:
                        file-path: schemas/authorized-app.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ This authorization's ID, used for revoking access.
                          example: 123
                          readOnly: true
                          x-linode-cli-display: 1
                        created:
                          type: string
                          description: |
                            __Read-only__ When this app was authorized.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                          x-linode-cli-display: 5
                          x-linode-filterable: true
                        expiry:
                          type: string
                          description: |
                            __Read-only__ When the app's access to your account expires. If `null`,
                            the app's access must be revoked manually.
                          example: '2018-01-15T00:01:01'
                          nullable: true
                          readOnly: true
                          format: date-time
                          x-linode-cli-display: 6
                          x-linode-filterable: true
                        label:
                          type: string
                          description: |
                            __Read-only__ The name of the application you've authorized.
                          example: example-app
                          readOnly: true
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        scopes:
                          type: string
                          description: |
                            __Read-only__ The OAuth scopes this app was authorized with.  This defines
                            what parts of your Account the app is allowed to access.
                          example: linodes:read_only
                          readOnly: true
                          format: oauth-scopes
                          x-linode-cli-display: 3
                        thumbnail_url:
                          type: string
                          description: |
                            __Read-only__ The URL at which this app's thumbnail may be accessed.
                          example: null
                          readOnly: true
                          format: url
                        website:
                          type: string
                          description: |
                            __Read-only__ The website where you can get more information about this
                            app.
                          example: example.org
                          readOnly: true
                          format: url
                          x-linode-cli-display: 4
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-profile-apps
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/profile/apps
      - lang: CLI
        source: linode-cli profile apps-list
      x-linode-cli-action: apps-list
      x-original-op-id: getProfileApps
      x-original-op-title: Authorized Apps List
    x-linode-cli-command: profile
  /profile/apps/{appId}:
    x-akamai:
      file-path: paths/app.yaml
      path-info: /profile/apps/{appId}
    parameters:
    - name: appId
      in: path
      description: |
        The authorized app ID to manage.
      example: '{{appId}}'
      x-akamai:
        file-path: parameters/app-id-path.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-profile-app
      summary: Get an authorized app
      tags:
      - Apps
      description: |-
        Returns information about a single app you've authorized to access your Account.


        ---


        - __CLI__.

            ```
            linode-cli profile app-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli profile app-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The app requested.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An application you have authorized access to your Account through OAuth.
                x-akamai:
                  file-path: schemas/authorized-app.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This authorization's ID, used for revoking access.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                  created:
                    type: string
                    description: |
                      __Read-only__ When this app was authorized.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 5
                    x-linode-filterable: true
                  expiry:
                    type: string
                    description: |
                      __Read-only__ When the app's access to your account expires. If `null`, the app's
                      access must be revoked manually.
                    example: '2018-01-15T00:01:01'
                    nullable: true
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 6
                    x-linode-filterable: true
                  label:
                    type: string
                    description: |
                      __Read-only__ The name of the application you've authorized.
                    example: example-app
                    readOnly: true
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  scopes:
                    type: string
                    description: |
                      __Read-only__ The OAuth scopes this app was authorized with.  This defines what parts
                      of your Account the app is allowed to access.
                    example: linodes:read_only
                    readOnly: true
                    format: oauth-scopes
                    x-linode-cli-display: 3
                  thumbnail_url:
                    type: string
                    description: |
                      __Read-only__ The URL at which this app's thumbnail may be accessed.
                    example: null
                    readOnly: true
                    format: url
                  website:
                    type: string
                    description: |
                      __Read-only__ The website where you can get more information about this app.
                    example: example.org
                    readOnly: true
                    format: url
                    x-linode-cli-display: 4
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-profile-app
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/profile/apps/123
      - lang: CLI
        source: linode-cli profile app-view 1234
      x-linode-cli-action: app-view
      x-original-op-id: getProfileApp
      x-original-op-title: Authorized App View
    delete:
      operationId: delete-profile-app
      summary: Revoke app access
      tags:
      - Apps
      description: |-
        Expires this app token. This token may no longer be used to access your Account.


        ---


        - __CLI__.

            ```
            linode-cli profile app-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli profile app-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            App's authorization has been revoked.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-profile-app
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/profile/apps/123
      - lang: CLI
        source: linode-cli profile app-delete 123
      x-linode-cli-action: app-delete
      x-original-op-id: deleteProfileApp
      x-original-op-title: App Access Revoke
    x-linode-cli-command: profile
  /profile/devices:
    x-akamai:
      file-path: paths/profile-devices.yaml
      path-info: /profile/devices
    get:
      operationId: get-devices
      summary: List trusted devices
      tags:
      - Devices
      description: |-
        Returns a paginated list of active TrustedDevices for your User. Browsers with an active Remember Me Session are logged into your account until the session expires or is revoked.


        ---


        - __CLI__.

            ```
            linode-cli profile devices-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli profile devices-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated list of TrustedDevice objects.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        A trusted device object represents an active Remember Me session with [login.linode.com](https://login.linode.com).
                      x-akamai:
                        file-path: schemas/trusted-device.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ The unique ID for this TrustedDevice
                          example: 123
                          readOnly: true
                        created:
                          type: string
                          description: |
                            __Read-only__ When this Remember Me session was started.  This corresponds
                            to the time of login with the "Remember Me" box checked.
                          example: '2018-01-01T01:01:01'
                          readOnly: true
                          format: date-time
                        expiry:
                          type: string
                          description: |
                            __Read-only__ When this TrustedDevice session expires.  Sessions typically
                            last 30 days.
                          example: '2018-01-31T01:01:01'
                          readOnly: true
                          format: date-time
                        last_authenticated:
                          type: string
                          description: |
                            __Read-only__ The last time this TrustedDevice was successfully used
                            to authenticate to [login.linode.com](https://login.linode.com).
                          example: '2018-01-05T12:57:12'
                          readOnly: true
                          format: date-time
                        last_remote_addr:
                          type: string
                          description: |
                            __Read-only__ The last IP Address to successfully authenticate with
                            this TrustedDevice.
                          example: 203.0.113.1
                          readOnly: true
                        user_agent:
                          type: string
                          description: |
                            __Read-only__ The User Agent of the browser that created this TrustedDevice
                            session.
                          example: Mozilla/5.0 (Macintosh; Intel Mac OS X 10_13_3) AppleWebKit/537.36 (KHTML, like
                            Gecko) Chrome/70.0.3538.77 Safari/537.36 Vivaldi/2.1.1337.36
                          readOnly: true
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-devices
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/profile/devices
      - lang: CLI
        source: linode-cli profile devices-list
      x-linode-cli-action: devices-list
      x-original-op-id: getDevices
      x-original-op-title: Trusted Devices List
    x-linode-cli-command: profile
  /profile/devices/{deviceId}:
    x-akamai:
      file-path: paths/profile-device.yaml
      path-info: /profile/devices/{deviceId}
    parameters:
    - name: deviceId
      in: path
      description: |
        The ID of the TrustedDevice
      example: '{{deviceId}}'
      x-akamai:
        file-path: parameters/device-id-path-b3b3e692.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-trusted-device
      summary: Get a trusted device
      tags:
      - Devices
      description: |-
        Returns a single active TrustedDevice for your User.


        ---


        - __CLI__.

            ```
            linode-cli profile device-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli profile device-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The requested TrustedDevice object
          content:
            application/json:
              schema:
                type: object
                description: |
                  A trusted device object represents an active Remember Me session with [login.linode.com](https://login.linode.com).
                x-akamai:
                  file-path: schemas/trusted-device.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID for this TrustedDevice
                    example: 123
                    readOnly: true
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Remember Me session was started.  This corresponds to the
                      time of login with the "Remember Me" box checked.
                    example: '2018-01-01T01:01:01'
                    readOnly: true
                    format: date-time
                  expiry:
                    type: string
                    description: |
                      __Read-only__ When this TrustedDevice session expires.  Sessions typically last
                      30 days.
                    example: '2018-01-31T01:01:01'
                    readOnly: true
                    format: date-time
                  last_authenticated:
                    type: string
                    description: |
                      __Read-only__ The last time this TrustedDevice was successfully used to authenticate
                      to [login.linode.com](https://login.linode.com).
                    example: '2018-01-05T12:57:12'
                    readOnly: true
                    format: date-time
                  last_remote_addr:
                    type: string
                    description: |
                      __Read-only__ The last IP Address to successfully authenticate with this TrustedDevice.
                    example: 203.0.113.1
                    readOnly: true
                  user_agent:
                    type: string
                    description: |
                      __Read-only__ The User Agent of the browser that created this TrustedDevice session.
                    example: Mozilla/5.0 (Macintosh; Intel Mac OS X 10_13_3) AppleWebKit/537.36 (KHTML, like Gecko)
                      Chrome/70.0.3538.77 Safari/537.36 Vivaldi/2.1.1337.36
                    readOnly: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-trusted-device
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/profile/devices/123
      - lang: CLI
        source: linode-cli profile device-view 123
      x-linode-cli-action: device-view
      x-original-op-id: getTrustedDevice
      x-original-op-title: Trusted Device View
    delete:
      operationId: delete-trusted-device
      summary: Revoke a trusted device
      tags:
      - Devices
      description: |-
        Revoke an active TrustedDevice for your User.  Once a TrustedDevice is revoked, this device will have to log in again before accessing your Linode account.


        ---


        - __CLI__.

            ```
            linode-cli profile device-revoke
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli profile device-revoke
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Session revoked successfully
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-trusted-device
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/profile/devices/123
      - lang: CLI
        source: linode-cli profile device-revoke 123
      x-linode-cli-action: device-revoke
      x-original-op-id: revokeTrustedDevice
      x-original-op-title: Trusted Device Revoke
    x-linode-cli-command: profile
  /profile/grants:
    x-akamai:
      file-path: paths/profile-grants.yaml
      path-info: /profile/grants
    get:
      operationId: get-profile-grants
      summary: List grants
      tags:
      - Grants
      description: |-
        This returns a GrantsResponse describing what the acting User has been granted access to.  For unrestricted users, this will return a  204 and no body because unrestricted users have access to everything without grants.  This will not return information about entities you do not have access to.  This operation is useful when writing third-party OAuth applications to see what options you should present to the acting User.

        For example, if they do not have `global.add_linodes`, you might not display a button to deploy a new Linode.

        Any client may run this operation; no OAuth scopes are required.
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            GrantsResponse
          content:
            application/json:
              schema:
                type: object
                description: |
                  A structure representing all grants a restricted User has on the Account. Not available for
                  unrestricted users, as they have access to everything without grants.
                  If retrieved from the `/profile/grants` endpoint, entities to which
                  a User has no access will be omitted.
                x-akamai:
                  file-path: schemas/grants-response.yaml
                additionalProperties: false
                properties:
                  database:
                    type: array
                    description: |
                      The grants this User has for each Database that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  domain:
                    type: array
                    description: |
                      The grants this User has for each Domain that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  global:
                    type: object
                    description: |
                      A structure containing the Account-level grants a User has.
                    additionalProperties: false
                    properties:
                      account_access:
                        type: string
                        description: |-
                          The level of access this User has to Account-level actions, like billing information. A restricted User will never be able to manage users.

                          __Parent and child accounts__

                          In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, this grant can be added to a child account user, to give the user `read-write` access. This gives the child user unrestricted access to expected management operations, such as creating other child users. However, child users don't have write access to billing operations. The API issues a specific error message if a write operation is attempted by a child user.
                        example: read_only
                        nullable: true
                        enum:
                        - read_only
                        - read_write
                      add_databases:
                        type: boolean
                        description: |
                          If true, this User may add Managed Databases.
                        example: true
                      add_domains:
                        type: boolean
                        description: |
                          If true, this User may add Domains.
                        example: true
                      add_firewalls:
                        type: boolean
                        description: |
                          If true, this User may add Firewalls.
                        example: true
                      add_images:
                        type: boolean
                        description: |
                          If true, this User may add Images.
                        example: true
                      add_linodes:
                        type: boolean
                        description: |
                          If true, this User may create Linodes.
                        example: true
                      add_loadbalancers:
                        type: boolean
                        description: |
                          If true, this User may add Cloud Load Balancers.
                        example: true
                      add_longview:
                        type: boolean
                        description: |
                          If true, this User may create Longview clients and view the current plan.
                        example: true
                      add_nodebalancers:
                        type: boolean
                        description: |
                          If true, this User may add NodeBalancers.
                        example: true
                      add_stackscripts:
                        type: boolean
                        description: |
                          If true, this User may add StackScripts.
                        example: true
                      add_volumes:
                        type: boolean
                        description: |
                          If true, this User may add Volumes.
                        example: true
                      add_vpcs:
                        type: boolean
                        description: |
                          If true, this User may add VPCs.
                        example: true
                      cancel_account:
                        type: boolean
                        description: |
                          If true, this User may cancel the entire Account.
                        example: false
                      child_account_access:
                        type: boolean
                        description: |
                          In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/)
                          environment, this gives a parent account access to endpoints
                          that can be used to manage child accounts. Unrestricted
                          parent account users have access to this grant, while restricted
                          parent users don't. An unrestricted parent user can set
                          this to `true` to add this grant to a restricted parent
                          user. Displayed as `null` for all non-parent accounts.
                        example: true
                        nullable: true
                      longview_subscription:
                        type: boolean
                        description: |
                          If true, this User may manage the Account's Longview subscription.
                        example: true
                  image:
                    type: array
                    description: |
                      The grants this User has for each Image that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  linode:
                    type: array
                    description: |
                      The grants this User has for each Linode that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  loadbalancer:
                    type: array
                    description: |
                      The grants this User has for each Cloud Load Balancer that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  longview:
                    type: array
                    description: |
                      The grants this User has for each Longview Client that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  nodebalancer:
                    type: array
                    description: |
                      The grants this User has for each NodeBalancer that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  stackscript:
                    type: array
                    description: |
                      The grants this User has for each StackScript that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  volume:
                    type: array
                    description: |
                      The grants this User has for each Block Storage Volume that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  vpc:
                    type: array
                    description: |
                      The grants this User has for each VPC that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
              x-example:
                x-ref: ../examples/tbd.json
        204:
          description: |
            This is an unrestricted User, who has no grants. This User can access everything on the Account.
          content:
            application/json: {}
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-profile-grants
      security:
      - personalAccessToken: []
      - oauth: []
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/profile/grants
      x-linode-cli-action: grants
      x-linode-cli-skip: true
      x-original-op-id: getProfileGrants
      x-original-op-title: Grants List
    x-linode-cli-command: profile
  /profile/logins:
    x-akamai:
      file-path: paths/profile-logins.yaml
      path-info: /profile/logins
    get:
      operationId: get-profile-logins
      summary: List logins
      tags:
      - Logins
      description: |-
        Returns a collection of successful account logins from this user during the last 90 days.


        ---


        - __CLI__.

            ```
            linode-cli profile logins-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli profile logins-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            An array of successful account logins from this user during the last 90 days.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        An object representing a previous successful login for a User.
                      x-akamai:
                        file-path: schemas/login.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ The unique ID of this login object.
                          example: 1234
                          readOnly: true
                          x-linode-cli-display: 1
                        datetime:
                          type: string
                          description: |
                            __Read-only__ When the login was initiated.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                          x-linode-cli-display: 2
                        ip:
                          type: string
                          description: |
                            __Read-only__ The remote IP address that requested the login.
                          example: 192.0.2.0
                          readOnly: true
                          format: ip
                          x-linode-cli-display: 3
                        restricted:
                          type: boolean
                          description: |
                            __Read-only__ True if the User that attempted the login was a restricted
                            User, false otherwise.
                          example: true
                          readOnly: true
                          x-linode-cli-display: 6
                        status:
                          type: string
                          description: |
                            __Read-only__ Whether the login attempt succeeded or failed.
                          example: successful
                          readOnly: true
                          enum:
                          - successful
                          - failed
                          x-linode-cli-display: 5
                        username:
                          type: string
                          description: |
                            __Read-only__ The username of the User that attempted the login.
                          example: example_user
                          readOnly: true
                          x-linode-cli-display: 4
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-profile-logins
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/profile/logins
      - lang: CLI
        source: linode-cli profile logins-list
      x-linode-cli-action: logins-list
      x-original-op-id: getProfileLogins
      x-original-op-title: Logins List
    x-linode-cli-command: profile
  /profile/logins/{loginId}:
    x-akamai:
      file-path: paths/profile-login.yaml
      path-info: /profile/logins/{loginId}
    parameters:
    - name: loginId
      in: path
      description: |
        The ID of the login object to access.
      example: '{{loginId}}'
      x-akamai:
        file-path: parameters/login-id-path.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-profile-login
      summary: Get a profile's login
      tags:
      - Logins
      description: |-
        Returns a login object displaying information about a successful account login from this user.


        ---


        - __CLI__.

            ```
            linode-cli profile login-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli profile login-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The requested login object.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An object representing a previous successful login for a User.
                x-akamai:
                  file-path: schemas/login.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of this login object.
                    example: 1234
                    readOnly: true
                    x-linode-cli-display: 1
                  datetime:
                    type: string
                    description: |
                      __Read-only__ When the login was initiated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 2
                  ip:
                    type: string
                    description: |
                      __Read-only__ The remote IP address that requested the login.
                    example: 192.0.2.0
                    readOnly: true
                    format: ip
                    x-linode-cli-display: 3
                  restricted:
                    type: boolean
                    description: |
                      __Read-only__ True if the User that attempted the login was a restricted User,
                      false otherwise.
                    example: true
                    readOnly: true
                    x-linode-cli-display: 6
                  status:
                    type: string
                    description: |
                      __Read-only__ Whether the login attempt succeeded or failed.
                    example: successful
                    readOnly: true
                    enum:
                    - successful
                    - failed
                    x-linode-cli-display: 5
                  username:
                    type: string
                    description: |
                      __Read-only__ The username of the User that attempted the login.
                    example: example_user
                    readOnly: true
                    x-linode-cli-display: 4
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-profile-login
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/profile/logins/1234
      - lang: CLI
        source: linode-cli profile login-view 1234
      x-linode-cli-action: login-view
      x-original-op-id: getProfileLogin
      x-original-op-title: Login View
    x-linode-cli-command: profile
  /profile/phone-number:
    x-akamai:
      file-path: paths/phone-number.yaml
      path-info: /profile/phone-number
    post:
      operationId: post-profile-phone-number
      summary: Send a phone number verification code
      tags:
      - Phone number
      description: |-
        Send a one-time verification code via SMS message to the submitted phone number. Providing your phone number helps ensure you can securely access your Account in case other ways to connect are lost. Your phone number is only used to verify your identity by sending an SMS message. Standard carrier messaging fees may apply.

        - By accessing this operation you are opting in to receive SMS messages. You can opt out of SMS messages by running the [Delete a phone number](https://techdocs.akamai.com/linode-api/reference/delete-profile-phone-number) operation after your phone number is verified.

        - Verification codes are valid for 10 minutes after they are sent.

        - Subsequent requests made prior to code expiration result in sending the same code.

        Once a verification code is received, verify your phone number with the [Verify a phone number](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number-verify) operation.


        ---


        - __CLI__.

            ```
            linode-cli phone sms-code-send
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli phone sms-code-send
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Enter a phone number and country code for verification.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - iso_code
              - phone_number
              properties:
                iso_code:
                  type: string
                  description: |
                    The two-letter ISO 3166 country code associated with the phone number.
                  example: '{{iso_code}}'
                phone_number:
                  type: string
                  description: |
                    A valid phone number.
                  example: '{{phone_number}}'
                  format: phone
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Phone number verification code request successful.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
            -H "Authorization: Bearer $TOKEN" \
            -X POST -d '{
              "iso_code": "US",
              "phone_number": "555-555-5555"
            }' \
            https://api.linode.com/v4/profile/phone-number
      - lang: CLI
        source: |-
          linode-cli phone sms-code-send \
            --iso-code US \
            --phone-number 555-555-5555
      x-linode-cli-action: sms-code-send
      x-linode-grant: read_write
      x-original-op-id: postProfilePhoneNumber
      x-original-op-title: Phone Number Verification
        Code Send
    delete:
      operationId: delete-profile-phone-number
      summary: Delete a phone number
      tags:
      - Phone number
      description: |-
        Delete the verified phone number for the User making this request.

        Use this operation to opt out of SMS messages for the requesting User after a phone number has been verified with the [Verify a phone number](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number-verify) operation.


        ---


        - __CLI__.

            ```
            linode-cli phone delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli phone delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Phone number deletion request successful.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-profile-phone-number
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
            -X DELETE \
            https://api.linode.com/v4/profile/phone-number
      - lang: CLI
        source: linode-cli phone delete
      x-linode-cli-action: delete
      x-linode-grant: read_write
      x-original-op-id: deleteProfilePhoneNumber
      x-original-op-title: Phone Number Delete
    x-linode-cli-command: phone
  /profile/phone-number/verify:
    x-akamai:
      file-path: paths/verify.yaml
      path-info: /profile/phone-number/verify
    post:
      operationId: post-profile-phone-number-verify
      summary: Verify a phone number
      tags:
      - Phone number
      description: |-
        Verify a phone number by confirming the one-time code received via SMS message after running the [Send a phone number verification code](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number) operation.

        - Verification codes are valid for 10 minutes after they are sent.

        - Only the same User that made the verification code request can use that code with this operation.

        Once completed, the verified phone number is assigned to the User making the request. To change the verified phone number for a User, first run the [Delete a phone number](https://techdocs.akamai.com/linode-api/reference/delete-profile-phone-number) operation, then begin the verification process again with the [Send a phone number verification code](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number) operation.


        ---


        - __CLI__.

            ```
            linode-cli phone verify
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli phone verify
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Enter a phone verification code for confirmation.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - otp_code
              properties:
                otp_code:
                  type: string
                  description: |
                    The one-time code received via SMS message after running the [Send a phone number verification
                    code](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number)
                    operation.
                  example: '{{otp_code}}'
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Phone number verification successful.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number-verify
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
            -H "Authorization: Bearer $TOKEN" \
            -X POST -d '{
              "otp_code": "123456"
            }' \
            https://api.linode.com/v4/profile/phone-number/verify
      - lang: CLI
        source: |-
          linode-cli phone verify \
            --otp_code 123456
      x-linode-cli-action: verify
      x-linode-grant: read_write
      x-original-op-id: postProfilePhoneNumberVerify
      x-original-op-title: Phone Number Verify
    x-linode-cli-command: phone
  /profile/preferences:
    x-akamai:
      file-path: paths/preferences.yaml
      path-info: /profile/preferences
    get:
      operationId: get-user-preferences
      summary: Get user preferences
      tags:
      - Preferences
      description: |-
        View a list of user preferences tied to the OAuth client that generated the token making the request. The user preferences endpoints allow consumers of the API to store arbitrary JSON data, such as a user's font size preference or preferred display name. User preferences are available for each OAuth client registered to your account, and as such an account can have multiple user preferences.


        ---


        - __CLI__.

            ```
            linode-cli profile preferences-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli profile preferences-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns an object of user preferences.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A dictionary of user preferences.
                example:
                  key1: value1
                  key2: value2
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-user-preferences
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/profile/preferences
      x-linode-cli-action: preferences-view
      x-linode-cli-skip: true
      x-linode-grant: read_only
      x-original-op-id: getUserPreferences
      x-original-op-title: User Preferences View
    put:
      operationId: put-user-preferences
      summary: Update a user's preferences
      tags:
      - Preferences
      description: |-
        Updates a user's preferences. These preferences are tied to the OAuth client that generated the token making the request. The user preferences endpoints allow consumers of the API to store arbitrary JSON data, such as a user's font size preference or preferred display name. An account may have multiple preferences. Preferences, and the pertaining request body, may contain any arbitrary JSON data that the user would like to store.


        ---


        - __CLI__.

            ```
            linode-cli profile preferences-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli profile preferences-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The user preferences to update or store.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |-
                Arbitrary JSON of your choosing. Overwrites any existing preferences for this user.

                Total length cannot exceed 65,535 characters.
              maxLength: 65535
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns an object of user preferences.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An object of user preferences.
                example:
                  key1: value1
                  key2: value2
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-user-preferences
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "key1": "value1",
                "key2": "value2"
              }' \
              https://api.linode.com/v4/profile/preferences
      x-linode-cli-action: preferences-update
      x-linode-cli-skip: true
      x-original-op-id: updateUserPreferences
      x-original-op-title: User Preferences Update
    x-linode-cli-command: profile
  /profile/security-questions:
    x-akamai:
      file-path: paths/security-questions.yaml
      path-info: /profile/security-questions
    post:
      operationId: post-security-questions
      summary: Answer security questions
      tags:
      - Security questions
      description: |-
        Adds security question responses for your User.

        Requires exactly three unique questions.

        Previous responses are overwritten if answered or reset to `null` if unanswered.

        __Note__. Security questions must be answered for your User prior to accessing the [Create a two factor secret](https://techdocs.akamai.com/linode-api/reference/post-tfa-enable) operation.


        ---


        - __CLI__.

            ```
            linode-cli security-questions answer
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli security-questions answer
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Answer Security Questions
        content:
          application/json:
            schema:
              type: object
              description: |
                Security questions and responses object for POST operation.
              x-akamai:
                file-path: schemas/security-questions-post.yaml
              additionalProperties: false
              properties:
                security_questions:
                  type: array
                  description: |
                    Security questions and response objects.
                  items:
                    type: object
                    description: |
                      Single security question and response object for POST operation.
                    additionalProperties: false
                    properties:
                      question_id:
                        type: integer
                        description: |
                          The ID representing the security question.
                        example: 1
                      response:
                        type: string
                        description: |
                          The security question response.
                        example: Gotham City
                        minLength: 3
                        maxLength: 17
                      security_question:
                        type: string
                        description: |
                          __Read-only__ The security question.
                        example: In what city were you born?
                        readOnly: true
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Security Questions answered successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Security questions and responses object for POST operation.
                x-akamai:
                  file-path: schemas/security-questions-post.yaml
                additionalProperties: false
                properties:
                  security_questions:
                    type: array
                    description: |
                      Security questions and response objects.
                    items:
                      type: object
                      description: |
                        Single security question and response object for POST operation.
                      additionalProperties: false
                      properties:
                        question_id:
                          type: integer
                          description: |
                            The ID representing the security question.
                          example: 1
                        response:
                          type: string
                          description: |
                            The security question response.
                          example: Gotham City
                          minLength: 3
                          maxLength: 17
                        security_question:
                          type: string
                          description: |
                            __Read-only__ The security question.
                          example: In what city were you born?
                          readOnly: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-security-questions
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                  "security_questions": [
                      {
                          "question_id": 1,
                          "response": "secret answer 1"
                      },
                      {
                          "question_id": 2,
                          "response": "secret answer 2"
                      },
                      {
                          "question_id": 11,
                          "response": "secret answer 3"
                      }
                  ]
              }' \
              https://api.linode.com/v4/profile/security-questions
      x-linode-cli-action: answer
      x-linode-cli-skip: true
      x-original-op-id: postSecurityQuestions
      x-original-op-title: Security Questions Answer
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-security-questions
      summary: List security questions
      tags:
      - Security questions
      description: |-
        Returns a collection of security questions and their responses, if any, for your User Profile.


        ---


        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a list of security questions.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Security questions and responses object for GET operation.
                x-akamai:
                  file-path: schemas/security-questions-get.yaml
                additionalProperties: false
                properties:
                  security_questions:
                    type: array
                    description: |
                      Security questions and response objects.
                    items:
                      type: object
                      description: |
                        Single security question and response object for GET operation.
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID representing the security question.
                          example: 1
                        question:
                          type: string
                          description: |
                            __Read-only__ The security question.
                          example: In what city were you born?
                          readOnly: true
                        response:
                          type: string
                          description: |
                            The security question response.
                          example: Gotham City
                          minLength: 3
                          maxLength: 17
              x-example:
                x-ref: ../examples/tbd.json
              x-linode-cli-nested-list: security_questions
              x-linode-cli-use-schema:
                type: object
                additionalProperties: false
                properties:
                  security_questions.id:
                    x-linode-cli-display: 1
                  security_questions.question:
                    x-linode-cli-display: 2
                  security_questions.response:
                    x-linode-cli-display: 3
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-security-questions
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/profile/security-questions
      - lang: CLI
        source: linode-cli security-questions list
      x-linode-cli-action:
      - list
      - ls
      x-linode-grant: read_only
      x-original-op-id: getSecurityQuestions
      x-original-op-title: Security Questions List
    x-linode-cli-command: security-questions
  /profile/sshkeys:
    x-akamai:
      file-path: paths/profile-ssh-keys.yaml
      path-info: /profile/sshkeys
    post:
      operationId: post-add-ssh-key
      summary: Add an SSH key
      tags:
      - SSH keys
      description: |-
        Adds an SSH Key to your Account profile.


        ---


        - __CLI__.

            ```
            linode-cli sshkeys create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli sshkeys create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Add SSH Key
        content:
          application/json:
            schema:
              type: object
              description: |
                A credential object for authenticating a User's secure shell connection to a Linode.
              x-akamai:
                file-path: schemas/ssh-key.yaml
              additionalProperties: false
              properties:
                id:
                  type: integer
                  description: |
                    __Read-only__ The unique identifier of an SSH Key object.
                  example: '{{id}}'
                  readOnly: true
                created:
                  type: string
                  description: |
                    __Read-only__ The date this key was added.
                  example: '{{created}}'
                  readOnly: true
                  format: date-time
                label:
                  type: string
                  description: |
                    A label for the SSH Key.
                  example: '{{label}}'
                  minLength: 0
                  maxLength: 64
                ssh_key:
                  type: string
                  description: |-
                    The public SSH Key, which is used to authenticate to the root user of the Linodes you deploy.

                    Accepted formats:

                    - ssh-dss
                    - ssh-rsa
                    - ecdsa-sha2-nistp
                    - ssh-ed25519
                    - sk-ecdsa-sha2-nistp256 (Akamai-specific)
                  example: '{{ssh_key}}'
                  format: ssh-key
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            SSH Key associated successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A credential object for authenticating a User's secure shell connection to a Linode.
                x-akamai:
                  file-path: schemas/ssh-key.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique identifier of an SSH Key object.
                    example: 42
                    readOnly: true
                  created:
                    type: string
                    description: |
                      __Read-only__ The date this key was added.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  label:
                    type: string
                    description: |
                      A label for the SSH Key.
                    example: My SSH Key
                    minLength: 0
                    maxLength: 64
                  ssh_key:
                    type: string
                    description: |-
                      The public SSH Key, which is used to authenticate to the root user of the Linodes you deploy.

                      Accepted formats:

                      - ssh-dss
                      - ssh-rsa
                      - ecdsa-sha2-nistp
                      - ssh-ed25519
                      - sk-ecdsa-sha2-nistp256 (Akamai-specific)
                    example: ssh-rsa AAAA_valid_public_ssh_key_123456785== user@their-computer
                    format: ssh-key
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-add-ssh-key
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                  "label": "My SSH Key",
                  "ssh_key": "ssh-rsa AAAA_valid_public_ssh_key_123456785== user@their-computer"
              }' \
              https://api.linode.com/v4/profile/sshkeys
      - lang: CLI
        source: |-
          linode-cli sshkeys create \
            --label "My SSH Key" \
            --ssh_key "ssh-rsa AAAA_valid_public_ssh_key_123456785== user@their-computer"
      x-linode-cli-action: create
      x-original-op-id: addSSHKey
      x-original-op-title: SSH Key Add
    get:
      operationId: get-ssh-keys
      summary: List SSH keys
      tags:
      - SSH keys
      description: |-
        Returns a collection of SSH Keys you've added to your Profile.


        ---


        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated list of SSH Key objects.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        A credential object for authenticating a User's secure shell connection to a Linode.
                      x-akamai:
                        file-path: schemas/ssh-key.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ The unique identifier of an SSH Key object.
                          example: 42
                          readOnly: true
                        created:
                          type: string
                          description: |
                            __Read-only__ The date this key was added.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                        label:
                          type: string
                          description: |
                            A label for the SSH Key.
                          example: My SSH Key
                          minLength: 0
                          maxLength: 64
                        ssh_key:
                          type: string
                          description: |-
                            The public SSH Key, which is used to authenticate to the root user of the Linodes you deploy.

                            Accepted formats:

                            - ssh-dss
                            - ssh-rsa
                            - ecdsa-sha2-nistp
                            - ssh-ed25519
                            - sk-ecdsa-sha2-nistp256 (Akamai-specific)
                          example: ssh-rsa AAAA_valid_public_ssh_key_123456785== user@their-computer
                          format: ssh-key
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-ssh-keys
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/profile/sshkeys
      - lang: CLI
        source: linode-cli sshkeys list
      x-linode-cli-action:
      - list
      - ls
      x-linode-grant: read_only
      x-original-op-id: getSSHKeys
      x-original-op-title: SSH Keys List
    x-linode-cli-command: sshkeys
  /profile/sshkeys/{sshKeyId}:
    x-akamai:
      file-path: paths/profile-ssh-key.yaml
      path-info: /profile/sshkeys/{sshKeyId}
    parameters:
    - name: sshKeyId
      in: path
      description: |
        The ID of the SSHKey
      example: '{{sshKeyId}}'
      x-akamai:
        file-path: parameters/ssh-key-id-path.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-ssh-key
      summary: Get an SSH key
      tags:
      - SSH keys
      description: |-
        Returns a single SSH Key object identified by `id` that you have access to view.


        ---


        - __CLI__.

            ```
            linode-cli sshkeys view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli sshkeys view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            An SSH Key object
          content:
            application/json:
              schema:
                type: object
                description: |
                  A credential object for authenticating a User's secure shell connection to a Linode.
                x-akamai:
                  file-path: schemas/ssh-key.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique identifier of an SSH Key object.
                    example: 42
                    readOnly: true
                  created:
                    type: string
                    description: |
                      __Read-only__ The date this key was added.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  label:
                    type: string
                    description: |
                      A label for the SSH Key.
                    example: My SSH Key
                    minLength: 0
                    maxLength: 64
                  ssh_key:
                    type: string
                    description: |-
                      The public SSH Key, which is used to authenticate to the root user of the Linodes you deploy.

                      Accepted formats:

                      - ssh-dss
                      - ssh-rsa
                      - ecdsa-sha2-nistp
                      - ssh-ed25519
                      - sk-ecdsa-sha2-nistp256 (Akamai-specific)
                    example: ssh-rsa AAAA_valid_public_ssh_key_123456785== user@their-computer
                    format: ssh-key
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-ssh-key
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/profile/sshkeys/42
      - lang: CLI
        source: linode-cli sshkeys view 42
      x-linode-cli-action: view
      x-original-op-id: getSSHKey
      x-original-op-title: SSH Key View
    put:
      operationId: put-ssh-key
      summary: Update an SSH key
      tags:
      - SSH keys
      description: |-
        Updates an SSH Key that you have permission to `read_write`.

        Only SSH key labels can be updated.


        ---


        - __CLI__.

            ```
            linode-cli sshkeys update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli sshkeys update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The fields to update.
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                label:
                  type: string
                  description: |
                    A label for the SSH Key.
                  example: '{{label}}'
                  minLength: 0
                  maxLength: 64
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            SSH Key updated successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A credential object for authenticating a User's secure shell connection to a Linode.
                x-akamai:
                  file-path: schemas/ssh-key.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique identifier of an SSH Key object.
                    example: 42
                    readOnly: true
                  created:
                    type: string
                    description: |
                      __Read-only__ The date this key was added.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  label:
                    type: string
                    description: |
                      A label for the SSH Key.
                    example: My SSH Key
                    minLength: 0
                    maxLength: 64
                  ssh_key:
                    type: string
                    description: |-
                      The public SSH Key, which is used to authenticate to the root user of the Linodes you deploy.

                      Accepted formats:

                      - ssh-dss
                      - ssh-rsa
                      - ecdsa-sha2-nistp
                      - ssh-ed25519
                      - sk-ecdsa-sha2-nistp256 (Akamai-specific)
                    example: ssh-rsa AAAA_valid_public_ssh_key_123456785== user@their-computer
                    format: ssh-key
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-ssh-key
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "label": "my laptop"
              }' \
              https://api.linode.com/v4/profile/sshkeys/42
      - lang: CLI
        source: |-
          linode-cli sshkeys update 42 \
            --label "my laptop"
      x-linode-cli-action: update
      x-original-op-id: updateSSHKey
      x-original-op-title: SSH Key Update
    delete:
      operationId: delete-ssh-key
      summary: Delete an SSH key
      tags:
      - SSH keys
      description: |-
        Deletes an SSH Key you have access to.

        __Note__. deleting an SSH Key will _not_ remove it from any Linode or Disk that was deployed with `authorized_keys`. In those cases, the keys must be manually deleted on the Linode or Disk. This operation will only delete the key's association from your Profile.


        ---


        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            SSH Key deleted successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-ssh-key
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/profile/sshkeys/42
      - lang: CLI
        source: linode-cli sshkeys delete 42
      x-linode-cli-action:
      - delete
      - rm
      x-original-op-id: deleteSSHKey
      x-original-op-title: SSH Key Delete
    x-linode-cli-command: sshkeys
  /profile/tfa-disable:
    x-akamai:
      file-path: paths/tfa-disable.yaml
      path-info: /profile/tfa-disable
    post:
      operationId: post-tfa-disable
      summary: Disable two factor authentication
      tags:
      - Two factor authentication
      description: |-
        Disables Two Factor Authentication for your User. Once successful, login attempts from untrusted computers will only require a password before being successful. This is less secure, and is discouraged.


        ---


        - __CLI__.

            ```
            linode-cli profile tfa-disable
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli profile tfa-disable
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            TFA disabled.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-tfa-disable
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST \
              https://api.linode.com/v4/profile/tfa-disable
      - lang: CLI
        source: linode-cli profile tfa-disable
      x-linode-cli-action: tfa-disable
      x-original-op-id: tfaDisable
      x-original-op-title: Two Factor Authentication
        Disable
    x-linode-cli-command: profile
  /profile/tfa-enable:
    x-akamai:
      file-path: paths/tfa-enable.yaml
      path-info: /profile/tfa-enable
    post:
      operationId: post-tfa-enable
      summary: Create a two factor secret
      tags:
      - Two factor authentication
      description: |-
        Generates a Two Factor secret for your User. To enable TFA for your User, enter the secret obtained from this operation with the [Enable two factor authentication](https://techdocs.akamai.com/linode-api/reference/post-tfa-confirm) operation. Once enabled, logins from untrusted computers are required to provide a TFA code before they are successful.

        Run the [Answer security questions](https://techdocs.akamai.com/linode-api/reference/post-security-questions) operation.


        ---


        - __CLI__.

            ```
            linode-cli profile tfa-enable
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli profile tfa-enable
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Two Factor secret generated
          content:
            application/json:
              schema:
                additionalProperties: false
                properties:
                  expiry:
                    type: string
                    description: |
                      When this Two Factor secret expires.
                    example: '2018-03-01T00:01:01.000Z'
                    format: date-time
                  secret:
                    type: string
                    description: |
                      Your Two Factor secret. This is used to generate time-based two factor codes required
                      for logging in. Doing this will be required to confirm TFA an
                      actually enable it.
                    example: 5FXX6KLACOC33GTC
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-tfa-enable
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST \
              https://api.linode.com/v4/profile/tfa-enable
      - lang: CLI
        source: linode-cli profile tfa-enable
      x-linode-cli-action: tfa-enable
      x-original-op-id: tfaEnable
      x-original-op-title: Two Factor Secret Create
    x-linode-cli-command: profile
  /profile/tfa-enable-confirm:
    x-akamai:
      file-path: paths/tfa-enable-confirm.yaml
      path-info: /profile/tfa-enable-confirm
    post:
      operationId: post-tfa-confirm
      summary: Enable two factor authentication
      tags:
      - Two factor authentication
      description: |-
        Confirms that you can successfully generate Two Factor codes and enables TFA on your Account. Once this is complete, login attempts from untrusted computers will be required to provide a Two Factor code before they are successful.


        ---


        - __CLI__.

            ```
            linode-cli profile tfa-confirm
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli profile tfa-confirm
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The Two Factor code you generated with your Two Factor secret.
        required: true
        content:
          application/json:
            schema:
              additionalProperties: false
              properties:
                tfa_code:
                  type: string
                  description: |
                    The Two Factor code you generated with your Two Factor secret. These codes are time-based,
                    so be sure it is current.
                  example: '213456'
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            TFA enabled successfully
          content:
            application/json:
              schema:
                additionalProperties: false
                properties:
                  scratch:
                    type: string
                    description: |
                      A one-use code that can be used in place of your Two Factor code, in case you are
                      unable to generate one.  Keep this in a safe place to avoid
                      being locked out of your Account.
                    example: sample two factor scratch
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-tfa-confirm
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "tfa_code": "213456"
              }' \
              https://api.linode.com/v4/profile/tfa-enable-confirm
      - lang: CLI
        source: |-
          linode-cli profile tfa-confirm \
            --tfa_code 213456
      x-linode-cli-action: tfa-confirm
      x-original-op-id: tfaConfirm
      x-original-op-title: Two Factor Authentication
        Confirm/Enable
    x-linode-cli-command: profile
  /profile/tokens:
    x-akamai:
      file-path: paths/profile-tokens.yaml
      path-info: /profile/tokens
    post:
      operationId: post-personal-access-token
      summary: Create a personal access token
      tags:
      - Personal access tokens
      description: |-
        Creates a Personal Access Token for your User. The raw token will be returned in the response, but will never be returned again afterward so be sure to take note of it. You may create a token with _at most_ the scopes of your current token. The created token will be able to access your Account until the given expiry, or until it is revoked. __Parent and child accounts__ In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, the following apply:

        - If you're using a child account parent user (proxy user), you can't create this form of token. The only token available to a proxy user is one that lets you run operations in a child account. These are created with the [Create a proxy user token](https://techdocs.akamai.com/linode-api/reference/post-child-account-token) operation.


        ---


        - __CLI__.

            ```
            linode-cli profile token-create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli profile token-create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the requested token.
        required: true
        content:
          application/json:
            schema:
              additionalProperties: false
              properties:
                expiry:
                  type: string
                  description: |
                    When this token should be valid until.  If omitted, the new token will be valid until
                    it is manually revoked.
                  example: null
                  format: date-time
                label:
                  type: string
                  description: |
                    This token's label.  This is for display purposes only, but can be used to more easily track
                    what you're using each token for.
                  example: linode-cli
                  minLength: 1
                  maxLength: 100
                  x-linode-cli-display: 2
                  x-linode-filterable: true
                scopes:
                  type: string
                  description: |-
                    The access [scopes](https://techdocs.akamai.com/linode-api/reference/oauth) to grant to the created token. These cannot be changed after creation, and may not exceed the scopes of the acting token.

                    If omitted or entered with a wildcard character (`*`), the new token will have the same scopes as the acting token.

                    Multiple scopes are separated by a space character (` `).

                    For example, `linodes:read_write account:read_only`.
                  example: '*'
                  format: oauth-scope
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Token created successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A Personal Access Token is a token generated manually to access the API without going through
                  an OAuth login.  Personal Access Tokens can have scopes just like
                  OAuth tokens do, and are commonly used to give access to command-line
                  tools like the Linode CLI, or when writing your own integrations.
                x-akamai:
                  file-path: schemas/personal-access-token.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This token's unique ID, which can be used to revoke it.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                  created:
                    type: string
                    description: |
                      __Read-only__ The date and time this token was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  expiry:
                    type: string
                    description: |
                      __Read-only__ When this token will expire.  Personal Access Tokens cannot be renewed,
                      so after this time the token will be completely unusable and
                      a new token will need to be generated.  Tokens may be created
                      with "null" as their expiry and will never expire unless revoked.
                    example: '2018-01-01T13:46:32'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 6
                  label:
                    type: string
                    description: |
                      This token's label.  This is for display purposes only, but can be used to more easily
                      track what you're using each token for.
                    example: linode-cli
                    minLength: 1
                    maxLength: 100
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  scopes:
                    type: string
                    description: |
                      __Read-only__ The scopes this token was created with. These define what parts of the
                      Account the token can be used to access. Many command-line tools,
                      such as the [Linode CLI](https://github.com/linode/linode-cli),
                      require tokens with access to `*`. Tokens with more restrictive
                      scopes are generally more secure.
                    example: '*'
                    readOnly: true
                    format: oauth-scopes
                    x-linode-cli-display: 3
                  token:
                    type: string
                    description: |
                      __Read-only__ The token used to access the API.  When the token is created, the full
                      token is returned here.  Otherwise, only the first 16 characters
                      are returned.
                    example: abcdefghijklmnop
                    readOnly: true
                    x-linode-cli-display: 5
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-personal-access-token
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "scopes": "*",
                "expiry": "2018-01-01T13:46:32",
                "label": "linode-cli"
              }' \
              https://api.linode.com/v4/profile/tokens
      - lang: CLI
        source: |-
          linode-cli profile token-create \
            --scopes '*' \
            --expiry '2018-01-01T13:46:32' \
            --label linode-cli
      x-linode-cli-action: token-create
      x-original-op-id: createPersonalAccessToken
      x-original-op-title: Personal Access Token Create
    get:
      operationId: get-personal-access-tokens
      summary: List personal access tokens
      tags:
      - Personal access tokens
      description: |-
        Returns a paginated list of Personal Access Tokens currently active for your User.


        ---


        - __CLI__.

            ```
            linode-cli profile tokens-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli profile tokens-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of active tokens.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        A Personal Access Token is a token generated manually to access the API without
                        going through an OAuth login.  Personal Access Tokens can
                        have scopes just like OAuth tokens do, and are commonly used
                        to give access to command-line tools like the Linode CLI,
                        or when writing your own integrations.
                      x-akamai:
                        file-path: schemas/personal-access-token.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ This token's unique ID, which can be used to revoke it.
                          example: 123
                          readOnly: true
                          x-linode-cli-display: 1
                        created:
                          type: string
                          description: |
                            __Read-only__ The date and time this token was created.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                          x-linode-cli-display: 4
                          x-linode-filterable: true
                        expiry:
                          type: string
                          description: |
                            __Read-only__ When this token will expire.  Personal Access Tokens cannot
                            be renewed, so after this time the token will be completely
                            unusable and a new token will need to be generated.  Tokens
                            may be created with "null" as their expiry and will never
                            expire unless revoked.
                          example: '2018-01-01T13:46:32'
                          readOnly: true
                          format: date-time
                          x-linode-cli-display: 6
                        label:
                          type: string
                          description: |
                            This token's label.  This is for display purposes only, but can be
                            used to more easily track what you're using each token
                            for.
                          example: linode-cli
                          minLength: 1
                          maxLength: 100
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        scopes:
                          type: string
                          description: |
                            __Read-only__ The scopes this token was created with. These define what
                            parts of the Account the token can be used to access.
                            Many command-line tools, such as the [Linode CLI](https://github.com/linode/linode-cli),
                            require tokens with access to `*`. Tokens with more restrictive
                            scopes are generally more secure.
                          example: '*'
                          readOnly: true
                          format: oauth-scopes
                          x-linode-cli-display: 3
                        token:
                          type: string
                          description: |
                            __Read-only__ The token used to access the API.  When the token is created,
                            the full token is returned here.  Otherwise, only the
                            first 16 characters are returned.
                          example: abcdefghijklmnop
                          readOnly: true
                          x-linode-cli-display: 5
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-personal-access-tokens
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/profile/tokens
      - lang: CLI
        source: linode-cli profile tokens-list
      x-linode-cli-action: tokens-list
      x-original-op-id: getPersonalAccessTokens
      x-original-op-title: Personal Access Tokens List
    x-linode-cli-command: profile
  /profile/tokens/{tokenId}:
    x-akamai:
      file-path: paths/profile-token.yaml
      path-info: /profile/tokens/{tokenId}
    parameters:
    - name: tokenId
      in: path
      description: |
        The ID of the token to access.
      example: '{{tokenId}}'
      x-akamai:
        file-path: parameters/token-id-path.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-personal-access-token
      summary: Get a personal access token
      tags:
      - Personal access tokens
      description: |-
        Returns a single Personal Access Token.


        ---


        - __CLI__.

            ```
            linode-cli profile token-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli profile token-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The requested token.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A Personal Access Token is a token generated manually to access the API without going through
                  an OAuth login.  Personal Access Tokens can have scopes just like
                  OAuth tokens do, and are commonly used to give access to command-line
                  tools like the Linode CLI, or when writing your own integrations.
                x-akamai:
                  file-path: schemas/personal-access-token.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This token's unique ID, which can be used to revoke it.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                  created:
                    type: string
                    description: |
                      __Read-only__ The date and time this token was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  expiry:
                    type: string
                    description: |
                      __Read-only__ When this token will expire.  Personal Access Tokens cannot be renewed,
                      so after this time the token will be completely unusable and
                      a new token will need to be generated.  Tokens may be created
                      with "null" as their expiry and will never expire unless revoked.
                    example: '2018-01-01T13:46:32'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 6
                  label:
                    type: string
                    description: |
                      This token's label.  This is for display purposes only, but can be used to more easily
                      track what you're using each token for.
                    example: linode-cli
                    minLength: 1
                    maxLength: 100
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  scopes:
                    type: string
                    description: |
                      __Read-only__ The scopes this token was created with. These define what parts of the
                      Account the token can be used to access. Many command-line tools,
                      such as the [Linode CLI](https://github.com/linode/linode-cli),
                      require tokens with access to `*`. Tokens with more restrictive
                      scopes are generally more secure.
                    example: '*'
                    readOnly: true
                    format: oauth-scopes
                    x-linode-cli-display: 3
                  token:
                    type: string
                    description: |
                      __Read-only__ The token used to access the API.  When the token is created, the full
                      token is returned here.  Otherwise, only the first 16 characters
                      are returned.
                    example: abcdefghijklmnop
                    readOnly: true
                    x-linode-cli-display: 5
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-personal-access-token
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/profile/tokens/123
      - lang: CLI
        source: linode-cli profile token-view 123
      x-linode-cli-action: token-view
      x-original-op-id: getPersonalAccessToken
      x-original-op-title: Personal Access Token View
    put:
      operationId: put-personal-access-token
      summary: Update a personal access token
      tags:
      - Personal access tokens
      description: |-
        Updates a Personal Access Token.


        ---


        - __CLI__.

            ```
            linode-cli profile token-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli profile token-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The fields to update.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                A Personal Access Token is a token generated manually to access the API without going through
                an OAuth login.  Personal Access Tokens can have scopes just like
                OAuth tokens do, and are commonly used to give access to command-line
                tools like the Linode CLI, or when writing your own integrations.
              x-akamai:
                file-path: schemas/personal-access-token.yaml
              additionalProperties: false
              properties:
                id:
                  type: integer
                  description: |
                    __Read-only__ This token's unique ID, which can be used to revoke it.
                  example: '{{id}}'
                  readOnly: true
                  x-linode-cli-display: 1
                created:
                  type: string
                  description: |
                    __Read-only__ The date and time this token was created.
                  example: '{{created}}'
                  readOnly: true
                  format: date-time
                  x-linode-cli-display: 4
                  x-linode-filterable: true
                expiry:
                  type: string
                  description: |
                    __Read-only__ When this token will expire.  Personal Access Tokens cannot be renewed,
                    so after this time the token will be completely unusable and a
                    new token will need to be generated.  Tokens may be created with
                    "null" as their expiry and will never expire unless revoked.
                  example: '{{expiry}}'
                  readOnly: true
                  format: date-time
                  x-linode-cli-display: 6
                label:
                  type: string
                  description: |
                    This token's label.  This is for display purposes only, but can be used to more easily track
                    what you're using each token for.
                  example: '{{label}}'
                  minLength: 1
                  maxLength: 100
                  x-linode-cli-display: 2
                  x-linode-filterable: true
                scopes:
                  type: string
                  description: |
                    __Read-only__ The scopes this token was created with. These define what parts of the Account
                    the token can be used to access. Many command-line tools, such
                    as the [Linode CLI](https://github.com/linode/linode-cli), require
                    tokens with access to `*`. Tokens with more restrictive scopes
                    are generally more secure.
                  example: '{{scopes}}'
                  readOnly: true
                  format: oauth-scopes
                  x-linode-cli-display: 3
                token:
                  type: string
                  description: |
                    __Read-only__ The token used to access the API.  When the token is created, the full token
                    is returned here.  Otherwise, only the first 16 characters are
                    returned.
                  example: '{{token}}'
                  readOnly: true
                  x-linode-cli-display: 5
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Token updated successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A Personal Access Token is a token generated manually to access the API without going through
                  an OAuth login.  Personal Access Tokens can have scopes just like
                  OAuth tokens do, and are commonly used to give access to command-line
                  tools like the Linode CLI, or when writing your own integrations.
                x-akamai:
                  file-path: schemas/personal-access-token.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This token's unique ID, which can be used to revoke it.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                  created:
                    type: string
                    description: |
                      __Read-only__ The date and time this token was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  expiry:
                    type: string
                    description: |
                      __Read-only__ When this token will expire.  Personal Access Tokens cannot be renewed,
                      so after this time the token will be completely unusable and
                      a new token will need to be generated.  Tokens may be created
                      with "null" as their expiry and will never expire unless revoked.
                    example: '2018-01-01T13:46:32'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 6
                  label:
                    type: string
                    description: |
                      This token's label.  This is for display purposes only, but can be used to more easily
                      track what you're using each token for.
                    example: linode-cli
                    minLength: 1
                    maxLength: 100
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  scopes:
                    type: string
                    description: |
                      __Read-only__ The scopes this token was created with. These define what parts of the
                      Account the token can be used to access. Many command-line tools,
                      such as the [Linode CLI](https://github.com/linode/linode-cli),
                      require tokens with access to `*`. Tokens with more restrictive
                      scopes are generally more secure.
                    example: '*'
                    readOnly: true
                    format: oauth-scopes
                    x-linode-cli-display: 3
                  token:
                    type: string
                    description: |
                      __Read-only__ The token used to access the API.  When the token is created, the full
                      token is returned here.  Otherwise, only the first 16 characters
                      are returned.
                    example: abcdefghijklmnop
                    readOnly: true
                    x-linode-cli-display: 5
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-personal-access-token
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "label": "linode-cli"
              }' \
              https://api.linode.com/v4/profile/tokens/123
      - lang: CLI
        source: |-
          linode-cli profile token-update 123 \
            --label linode-cli
      x-linode-cli-action: token-update
      x-original-op-id: updatePersonalAccessToken
      x-original-op-title: Personal Access Token Update
    delete:
      operationId: delete-personal-access-token
      summary: Revoke a personal access token
      tags:
      - Personal access tokens
      description: |-
        Revokes a Personal Access Token. The token will be invalidated immediately, and requests using that token will fail with a 401. It is possible to revoke access to the token making the request to revoke a token, but keep in mind that doing so could lose you access to the api and require you to create a new token through some other means.


        ---


        - __CLI__.

            ```
            linode-cli profile token-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli profile token-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Token revoked successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-personal-access-token
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/profile/tokens/123
      - lang: CLI
        source: linode-cli profile token-delete 123
      x-linode-cli-action: token-delete
      x-original-op-id: deletePersonalAccessToken
      x-original-op-title: Personal Access Token Revoke
    x-linode-cli-command: profile
  /regions:
    x-akamai:
      file-path: paths/regions.yaml
      path-info: /regions
    get:
      operationId: get-regions
      summary: List regions
      tags:
      - Regions
      description: |
        Lists the Regions available for Linode services. Not all services are guaranteed to be available in all Regions.
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns an array of Regions.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        An area where Linode services are available.
                      x-akamai:
                        file-path: schemas/region.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: string
                          description: |
                            __Read-only__ The unique ID of this Region.
                          example: us-east
                          readOnly: true
                          x-linode-cli-display: 1
                        capabilities:
                          type: array
                          description: |
                            __Read-only__ A list of capabilities of this region.
                          example:
                          - Linodes
                          - NodeBalancers
                          - Block Storage
                          - Object Storage
                          - Placement Groups
                          readOnly: true
                          items:
                            type: string
                          x-linode-cli-display: 4
                        country:
                          type: string
                          description: |
                            __Read-only__ The country where this Region resides.
                          example: us
                          readOnly: true
                          x-linode-cli-display: 3
                        label:
                          type: string
                          description: |
                            __Read-only__ Detailed location information for this Region, including
                            city, state or region, and country.
                          example: Newark, NJ, USA
                          readOnly: true
                          x-linode-cli-display: 2
                        placement_group_limits:
                          type: object
                          description: |
                            __Read-only__ The limits for [placement groups](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/)
                            in this region.
                          additionalProperties: false
                          readOnly: true
                          properties:
                            maximum_linodes_per_pg:
                              type: integer
                              description: |
                                __Read-only__ The maximum number of compute instances you can include
                                in a single placement group in this region.
                              example: 5
                              readOnly: true
                            maximum_pgs_per_customer:
                              type: integer
                              description: |
                                __Read-only__ The maximum number of placement groups you can have
                                in this region. Displayed as `null` if you don't have
                                a limit.
                              example: 10
                              nullable: true
                              readOnly: true
                          x-linode-cli-display: 7
                        resolvers:
                          type: object
                          additionalProperties: false
                          readOnly: true
                          properties:
                            ipv4:
                              type: string
                              description: |
                                __Read-only__ The IPv4 addresses for this region's DNS resolvers,
                                separated by commas.
                              example: 192.0.2.0,192.0.2.1
                              readOnly: true
                            ipv6:
                              type: string
                              description: |
                                __Read-only__ The IPv6 addresses for this region's DNS resolvers,
                                separated by commas.
                              example: 2001:0db8::,2001:0db8::1
                              readOnly: true
                          x-linode-cli-display: 6
                        status:
                          type: string
                          description: |
                            __Read-only__ This region's current operational status.
                          example: ok
                          readOnly: true
                          enum:
                          - ok
                          - outage
                          x-linode-cli-display: 5
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-regions
      x-code-samples:
      - lang: Shell
        source: curl https://api.linode.com/v4/regions
      - lang: CLI
        source: linode-cli regions list
      x-linode-cli-action:
      - list
      - ls
      x-linode-redoc-load-ids: true
      x-original-op-id: getRegions
      x-original-op-title: Regions List
    x-linode-cli-command: regions
  /regions/availability:
    x-akamai:
      file-path: paths/regions-availability.yaml
      path-info: /regions/availability
    get:
      operationId: get-regions-availability
      summary: List regions' availability
      tags:
      - Regions
      description: |-
        Returns availability data for all Regions.

        Currently, this operation returns availability of select premium and GPU plans for select regions.
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a Region Availability object.
          content:
            application/json:
              schema:
                allOf:
                - type: object
                  description: |
                    An envelope for paginated response. When accessing a collection through a GET endpoint,
                    the results are wrapped in this envelope which includes metadata
                    about those results. Results are presented within a `data` array.
                    See [Pagination](https://techdocs.akamai.com/linode-api/reference/pagination)
                    for more information.
                  x-akamai:
                    file-path: schemas/pagination-envelope.yaml
                  additionalProperties: false
                  properties:
                    page:
                      type: integer
                      description: |
                        __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    pages:
                      type: integer
                      description: |
                        __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    results:
                      type: integer
                      description: |
                        __Read-only__ The total number of results.
                      example: 1
                      readOnly: true
                - properties:
                    data:
                      type: array
                      items:
                        type: object
                        description: |
                          Compute instance availability information by [Type](https://techdocs.akamai.com/linode-api/reference/get-linode-types)
                          and [Region](https://techdocs.akamai.com/linode-api/reference/get-regions).
                        x-akamai:
                          file-path: schemas/region-availability.yaml
                        additionalProperties: false
                        properties:
                          available:
                            type: boolean
                            description: |
                              Whether the compute instance type is available in the region.
                            example: true
                            x-linode-cli-display: 3
                            x-linode-filterable: true
                          plan:
                            type: string
                            description: |
                              The compute instance [Type](https://techdocs.akamai.com/linode-api/reference/get-linode-types)
                              ID.
                            example: gpu-rtx6000-1.1
                            x-linode-cli-display: 2
                            x-linode-filterable: true
                          region:
                            type: string
                            description: |
                              The [Region](https://techdocs.akamai.com/linode-api/reference/get-regions) ID.
                            example: us-east
                            x-linode-cli-display: 1
                            x-linode-filterable: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-regions-availability
      x-code-samples:
      - lang: Shell
        source: curl https://api.linode.com/v4/regions/availability
      - lang: CLI
        source: linode-cli regions list-avail
      x-linode-cli-action: list-avail
      x-original-op-id: getRegionsAvailability
      x-original-op-title: Regions Availability List
    x-linode-cli-command: regions
  /regions/{regionId}:
    x-akamai:
      file-path: paths/region.yaml
      path-info: /regions/{regionId}
    parameters:
    - name: regionId
      in: path
      description: |
        ID of the Region to look up.
      example: '{{regionId}}'
      x-akamai:
        file-path: parameters/region-id-path.yaml
      required: true
      schema:
        type: string
    get:
      operationId: get-region
      summary: Get a region
      tags:
      - Regions
      description: |
        Returns a single Region.
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A single Region object.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An area where Linode services are available.
                x-akamai:
                  file-path: schemas/region.yaml
                additionalProperties: false
                properties:
                  id:
                    type: string
                    description: |
                      __Read-only__ The unique ID of this Region.
                    example: us-east
                    readOnly: true
                    x-linode-cli-display: 1
                  capabilities:
                    type: array
                    description: |
                      __Read-only__ A list of capabilities of this region.
                    example:
                    - Linodes
                    - NodeBalancers
                    - Block Storage
                    - Object Storage
                    - Placement Groups
                    readOnly: true
                    items:
                      type: string
                    x-linode-cli-display: 4
                  country:
                    type: string
                    description: |
                      __Read-only__ The country where this Region resides.
                    example: us
                    readOnly: true
                    x-linode-cli-display: 3
                  label:
                    type: string
                    description: |
                      __Read-only__ Detailed location information for this Region, including city, state
                      or region, and country.
                    example: Newark, NJ, USA
                    readOnly: true
                    x-linode-cli-display: 2
                  placement_group_limits:
                    type: object
                    description: |
                      __Read-only__ The limits for [placement groups](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/)
                      in this region.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      maximum_linodes_per_pg:
                        type: integer
                        description: |
                          __Read-only__ The maximum number of compute instances you can include in a
                          single placement group in this region.
                        example: 5
                        readOnly: true
                      maximum_pgs_per_customer:
                        type: integer
                        description: |
                          __Read-only__ The maximum number of placement groups you can have in this
                          region. Displayed as `null` if you don't have a limit.
                        example: 10
                        nullable: true
                        readOnly: true
                    x-linode-cli-display: 7
                  resolvers:
                    type: object
                    additionalProperties: false
                    readOnly: true
                    properties:
                      ipv4:
                        type: string
                        description: |
                          __Read-only__ The IPv4 addresses for this region's DNS resolvers, separated
                          by commas.
                        example: 192.0.2.0,192.0.2.1
                        readOnly: true
                      ipv6:
                        type: string
                        description: |
                          __Read-only__ The IPv6 addresses for this region's DNS resolvers, separated
                          by commas.
                        example: 2001:0db8::,2001:0db8::1
                        readOnly: true
                    x-linode-cli-display: 6
                  status:
                    type: string
                    description: |
                      __Read-only__ This region's current operational status.
                    example: ok
                    readOnly: true
                    enum:
                    - ok
                    - outage
                    x-linode-cli-display: 5
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-region
      x-code-samples:
      - lang: Shell
        source: curl https://api.linode.com/v4/regions/us-east
      - lang: CLI
        source: linode-cli regions view us-east
      x-linode-cli-action: view
      x-original-op-id: getRegion
      x-original-op-title: Region View
    x-linode-cli-command: regions
  /regions/{regionId}/availability:
    x-akamai:
      file-path: paths/region-availability.yaml
      path-info: /regions/{regionId}/availability
    parameters:
    - name: regionId
      in: path
      description: |
        ID of the Region to look up.
      example: '{{regionId}}'
      x-akamai:
        file-path: parameters/region-id-path.yaml
      required: true
      schema:
        type: string
    get:
      operationId: get-region-availability
      summary: Get a region's availability
      tags:
      - Regions
      description: |
        Returns availability data for a single Region.
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The availability data for a single Region.
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  description: |
                    Compute instance availability information by [Type](https://techdocs.akamai.com/linode-api/reference/get-linode-types)
                    and [Region](https://techdocs.akamai.com/linode-api/reference/get-regions).
                  x-akamai:
                    file-path: schemas/region-availability.yaml
                  additionalProperties: false
                  properties:
                    available:
                      type: boolean
                      description: |
                        Whether the compute instance type is available in the region.
                      example: true
                      x-linode-cli-display: 3
                      x-linode-filterable: true
                    plan:
                      type: string
                      description: |
                        The compute instance [Type](https://techdocs.akamai.com/linode-api/reference/get-linode-types)
                        ID.
                      example: gpu-rtx6000-1.1
                      x-linode-cli-display: 2
                      x-linode-filterable: true
                    region:
                      type: string
                      description: |
                        The [Region](https://techdocs.akamai.com/linode-api/reference/get-regions) ID.
                      example: us-east
                      x-linode-cli-display: 1
                      x-linode-filterable: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-region-availability
      x-code-samples:
      - lang: Shell
        source: curl https://api.linode.com/v4/regions/us-east/availability
      - lang: CLI
        source: linode-cli regions view-avail us-east
      x-linode-cli-action: view-avail
      x-original-op-id: getRegionAvailability
      x-original-op-title: Region Availability View
    x-linode-cli-command: regions
  /support/tickets:
    x-akamai:
      file-path: paths/tickets.yaml
      path-info: /support/tickets
    post:
      operationId: post-ticket
      summary: Open a support ticket
      tags:
      - Support tickets
      description: |-
        Open a Support Ticket. Only one of the ID attributes (`linode_id`, `domain_id`, etc.) can be set on a single Support Ticket.


        ---


        - __CLI__.

            ```
            linode-cli tickets create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli tickets create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Open a Support Ticket.
        content:
          application/json:
            schema:
              type: object
              description: |
                An object representing a created Support Ticket - a question or issue and request for help
                from the Linode support team. Only one of the ID attributes (`linode_id`,
                `domain_id`, etc.) can be set on a single Support Ticket.
              x-akamai:
                file-path: schemas/support-ticket-request.yaml
              additionalProperties: false
              required:
              - summary
              - description
              properties:
                summary:
                  type: string
                  description: |
                    The summary or title for this SupportTicket.
                  example: '{{summary}}'
                  minLength: 1
                  maxLength: 64
                description:
                  type: string
                  description: |
                    The full details of the issue or question.
                  example: '{{description}}'
                  minLength: 1
                  maxLength: 65000
                database_id:
                  type: integer
                  description: |
                    The ID of the Managed Database this ticket is regarding, if relevant.
                  example: '{{database_id}}'
                domain_id:
                  type: integer
                  description: |
                    The ID of the Domain this ticket is regarding, if relevant.
                  example: '{{domain_id}}'
                firewall_id:
                  type: integer
                  description: |
                    The ID of the Firewall this ticket is regarding, if relevant.
                  example: '{{firewall_id}}'
                linode_id:
                  type: integer
                  description: |
                    The ID of the Linode this ticket is regarding, if relevant.
                  example: '{{linode_id}}'
                lkecluster_id:
                  type: integer
                  description: |
                    The ID of the Kubernetes cluster this ticket is regarding, if relevant.
                  example: '{{lkecluster_id}}'
                longviewclient_id:
                  type: integer
                  description: |
                    The ID of the Longview client this ticket is regarding, if relevant.
                  example: '{{longviewclient_id}}'
                managed_issue:
                  type: boolean
                  description: |-
                    Designates if this ticket is related to a [Managed service](https://www.linode.com/products/managed/). If `true`, the following constraints will apply:

                    - No ID attributes (i.e. `linode_id`, `domain_id`, etc.) should be provided with this request.
                    - Your account must have a managed service [enabled](https://techdocs.akamai.com/linode-api/reference/post-enable-managed-service).
                  example: '{{managed_issue}}'
                nodebalancer_id:
                  type: integer
                  description: |
                    The ID of the NodeBalancer this ticket is regarding, if relevant.
                  example: '{{nodebalancer_id}}'
                region:
                  type: string
                  description: |-
                    The [Region](https://techdocs.akamai.com/linode-api/reference/get-regions) ID for the associated VLAN this ticket is regarding.

                    Only allowed when submitting a VLAN ticket.
                  example: '{{region}}'
                vlan:
                  type: string
                  description: |-
                    The label of the VLAN this ticket is regarding, if relevant. To view your VLANs, run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans)) operation.

                    Requires a specified `region` to identify the VLAN.
                  example: '{{vlan}}'
                volume_id:
                  type: integer
                  description: |
                    The ID of the Volume this ticket is regarding, if relevant.
                  example: '{{volume_id}}'
                vpc_id:
                  type: integer
                  description: |
                    The ID of the VPC this ticket is regarding, if relevant.
                  example: '{{vpc_id}}'
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Support Ticket opened.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A Support Ticket opened on your Account.
                x-akamai:
                  file-path: schemas/support-ticket.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The ID of the Support Ticket.
                    example: 11223344
                    readOnly: true
                    x-linode-cli-display: 1
                  summary:
                    type: string
                    description: |
                      __Read-only__ The summary or title for this Ticket.
                    example: Having trouble resetting root password on my Linode
                    readOnly: true
                    minLength: 1
                    maxLength: 64
                    x-linode-cli-display: 2
                  description:
                    type: string
                    description: |
                      __Read-only__ The full details of the issue or question.
                    example: I am having trouble setting the root password on my Linode. I tried following the instructions
                      but something is not working. Can you please help me figure
                      out how I can reset it?
                    readOnly: true
                    minLength: 1
                    maxLength: 65000
                    x-linode-cli-display: 5
                  attachments:
                    type: array
                    description: |
                      __Read-only__ A list of filenames representing attached files associated with this
                      Ticket.
                    readOnly: true
                    items:
                      type: string
                      example:
                      - screenshot.jpg
                      - screenshot.txt
                  closable:
                    type: boolean
                    description: |
                      Whether the Support Ticket may be closed.
                    example: false
                  closed:
                    type: string
                    description: |
                      __Read-only__ The date and time this Ticket was closed.
                    example: '2015-06-04T16:07:03'
                    nullable: true
                    readOnly: true
                    format: date-time
                    x-linode-filterable: true
                  entity:
                    type: object
                    description: |
                      __Read-only__ The entity this Ticket was opened for.
                    additionalProperties: false
                    nullable: true
                    readOnly: true
                    properties:
                      id:
                        type: integer
                        description: |
                          __Read-only__ The unique ID for this Ticket's entity.
                        example: 10400
                        readOnly: true
                      type:
                        type: string
                        description: |
                          __Read-only__ The type of entity this is related to.
                        example: linode
                        readOnly: true
                      label:
                        type: string
                        description: |
                          __Read-only__ The current label of this entity.
                        example: linode123456
                        readOnly: true
                      url:
                        type: string
                        description: |
                          __Read-only__ The URL where you can access the object this event is for. If
                          a relative URL, it is relative to the domain you retrieved
                          the entity from.
                        example: /v4/linode/instances/123456
                        readOnly: true
                    x-linode-cli-display: 6
                  gravatar_id:
                    type: string
                    description: |
                      __Read-only__ The Gravatar ID of the User who opened this Ticket.
                    example: 474a1b7373ae0be4132649e69c36ce30
                    readOnly: true
                  opened:
                    type: string
                    description: |
                      __Read-only__ The date and time this Ticket was created.
                    example: '2015-06-04T14:16:44'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  opened_by:
                    type: string
                    description: |
                      __Read-only__ The User who opened this Ticket.
                    example: some_user
                    readOnly: true
                    x-linode-cli-display: 3
                  status:
                    type: string
                    description: |
                      __Read-only__ The current status of this Ticket.
                    example: open
                    readOnly: true
                    enum:
                    - closed
                    - new
                    - open
                  updated:
                    type: string
                    description: |
                      __Read-only__ The date and time this Ticket was last updated.
                    example: '2015-06-04T16:07:03'
                    readOnly: true
                    format: date-time
                    x-linode-filterable: true
                  updated_by:
                    type: string
                    description: |
                      __Read-only__ The User who last updated this Ticket.
                    example: some_other_user
                    nullable: true
                    readOnly: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-ticket
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "description": "I'm having trouble setting the root password on my Linode. I tried following the instructions but something is not working and I'm not sure what I'm doing wrong. Can you please help me figure out how I can reset it?",
                "linode_id": 123,
                "summary": "Having trouble resetting root password on my Linode"
              }' \
              https://api.linode.com/v4/support/tickets
      - lang: CLI
        source: |-
          linode-cli tickets create \
            --description "I'm having trouble setting the root password on my Linode. I tried following the instructions but something is not working and I'm not sure what I'm doing wrong. Can you please help me figure out how I can reset it?" \
            --linode_id 123 \
            --summary "Having trouble resetting root password on my Linode"
      x-linode-cli-action: create
      x-linode-grant: read_write
      x-original-op-id: createTicket
      x-original-op-title: Support Ticket Open
    get:
      operationId: get-tickets
      summary: List support tickets
      tags:
      - Support tickets
      description: |-
        Returns a collection of Support Tickets on your Account. Support Tickets can be both tickets you open with Linode for support, as well as tickets generated by Linode regarding your Account. This collection includes all Support Tickets generated on your Account, with open tickets returned first.


        ---


        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated list of SupportTicket objects.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        A Support Ticket opened on your Account.
                      x-akamai:
                        file-path: schemas/support-ticket.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ The ID of the Support Ticket.
                          example: 11223344
                          readOnly: true
                          x-linode-cli-display: 1
                        summary:
                          type: string
                          description: |
                            __Read-only__ The summary or title for this Ticket.
                          example: Having trouble resetting root password on my Linode
                          readOnly: true
                          minLength: 1
                          maxLength: 64
                          x-linode-cli-display: 2
                        description:
                          type: string
                          description: |
                            __Read-only__ The full details of the issue or question.
                          example: I am having trouble setting the root password on my Linode. I tried following
                            the instructions but something is not working. Can you
                            please help me figure out how I can reset it?
                          readOnly: true
                          minLength: 1
                          maxLength: 65000
                          x-linode-cli-display: 5
                        attachments:
                          type: array
                          description: |
                            __Read-only__ A list of filenames representing attached files associated
                            with this Ticket.
                          readOnly: true
                          items:
                            type: string
                            example:
                            - screenshot.jpg
                            - screenshot.txt
                        closable:
                          type: boolean
                          description: |
                            Whether the Support Ticket may be closed.
                          example: false
                        closed:
                          type: string
                          description: |
                            __Read-only__ The date and time this Ticket was closed.
                          example: '2015-06-04T16:07:03'
                          nullable: true
                          readOnly: true
                          format: date-time
                          x-linode-filterable: true
                        entity:
                          type: object
                          description: |
                            __Read-only__ The entity this Ticket was opened for.
                          additionalProperties: false
                          nullable: true
                          readOnly: true
                          properties:
                            id:
                              type: integer
                              description: |
                                __Read-only__ The unique ID for this Ticket's entity.
                              example: 10400
                              readOnly: true
                            type:
                              type: string
                              description: |
                                __Read-only__ The type of entity this is related to.
                              example: linode
                              readOnly: true
                            label:
                              type: string
                              description: |
                                __Read-only__ The current label of this entity.
                              example: linode123456
                              readOnly: true
                            url:
                              type: string
                              description: |
                                __Read-only__ The URL where you can access the object this event
                                is for. If a relative URL, it is relative to the domain
                                you retrieved the entity from.
                              example: /v4/linode/instances/123456
                              readOnly: true
                          x-linode-cli-display: 6
                        gravatar_id:
                          type: string
                          description: |
                            __Read-only__ The Gravatar ID of the User who opened this Ticket.
                          example: 474a1b7373ae0be4132649e69c36ce30
                          readOnly: true
                        opened:
                          type: string
                          description: |
                            __Read-only__ The date and time this Ticket was created.
                          example: '2015-06-04T14:16:44'
                          readOnly: true
                          format: date-time
                          x-linode-cli-display: 4
                          x-linode-filterable: true
                        opened_by:
                          type: string
                          description: |
                            __Read-only__ The User who opened this Ticket.
                          example: some_user
                          readOnly: true
                          x-linode-cli-display: 3
                        status:
                          type: string
                          description: |
                            __Read-only__ The current status of this Ticket.
                          example: open
                          readOnly: true
                          enum:
                          - closed
                          - new
                          - open
                        updated:
                          type: string
                          description: |
                            __Read-only__ The date and time this Ticket was last updated.
                          example: '2015-06-04T16:07:03'
                          readOnly: true
                          format: date-time
                          x-linode-filterable: true
                        updated_by:
                          type: string
                          description: |
                            __Read-only__ The User who last updated this Ticket.
                          example: some_other_user
                          nullable: true
                          readOnly: true
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-tickets
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/support/tickets
      - lang: CLI
        source: linode-cli tickets list
      x-linode-cli-action:
      - list
      - ls
      x-linode-grant: read_only
      x-original-op-id: getTickets
      x-original-op-title: Support Tickets List
    x-linode-cli-command: tickets
  /support/tickets/{ticketId}:
    x-akamai:
      file-path: paths/ticket.yaml
      path-info: /support/tickets/{ticketId}
    parameters:
    - name: ticketId
      in: path
      description: |
        The ID of the Support Ticket.
      example: '{{ticketId}}'
      x-akamai:
        file-path: parameters/ticket-id.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-ticket
      summary: Get a support ticket
      tags:
      - Support tickets
      description: |-
        Returns a Support Ticket under your Account.


        ---


        - __CLI__.

            ```
            linode-cli tickets view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli tickets view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a single SupportTicket object.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A Support Ticket opened on your Account.
                x-akamai:
                  file-path: schemas/support-ticket.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The ID of the Support Ticket.
                    example: 11223344
                    readOnly: true
                    x-linode-cli-display: 1
                  summary:
                    type: string
                    description: |
                      __Read-only__ The summary or title for this Ticket.
                    example: Having trouble resetting root password on my Linode
                    readOnly: true
                    minLength: 1
                    maxLength: 64
                    x-linode-cli-display: 2
                  description:
                    type: string
                    description: |
                      __Read-only__ The full details of the issue or question.
                    example: I am having trouble setting the root password on my Linode. I tried following the instructions
                      but something is not working. Can you please help me figure
                      out how I can reset it?
                    readOnly: true
                    minLength: 1
                    maxLength: 65000
                    x-linode-cli-display: 5
                  attachments:
                    type: array
                    description: |
                      __Read-only__ A list of filenames representing attached files associated with this
                      Ticket.
                    readOnly: true
                    items:
                      type: string
                      example:
                      - screenshot.jpg
                      - screenshot.txt
                  closable:
                    type: boolean
                    description: |
                      Whether the Support Ticket may be closed.
                    example: false
                  closed:
                    type: string
                    description: |
                      __Read-only__ The date and time this Ticket was closed.
                    example: '2015-06-04T16:07:03'
                    nullable: true
                    readOnly: true
                    format: date-time
                    x-linode-filterable: true
                  entity:
                    type: object
                    description: |
                      __Read-only__ The entity this Ticket was opened for.
                    additionalProperties: false
                    nullable: true
                    readOnly: true
                    properties:
                      id:
                        type: integer
                        description: |
                          __Read-only__ The unique ID for this Ticket's entity.
                        example: 10400
                        readOnly: true
                      type:
                        type: string
                        description: |
                          __Read-only__ The type of entity this is related to.
                        example: linode
                        readOnly: true
                      label:
                        type: string
                        description: |
                          __Read-only__ The current label of this entity.
                        example: linode123456
                        readOnly: true
                      url:
                        type: string
                        description: |
                          __Read-only__ The URL where you can access the object this event is for. If
                          a relative URL, it is relative to the domain you retrieved
                          the entity from.
                        example: /v4/linode/instances/123456
                        readOnly: true
                    x-linode-cli-display: 6
                  gravatar_id:
                    type: string
                    description: |
                      __Read-only__ The Gravatar ID of the User who opened this Ticket.
                    example: 474a1b7373ae0be4132649e69c36ce30
                    readOnly: true
                  opened:
                    type: string
                    description: |
                      __Read-only__ The date and time this Ticket was created.
                    example: '2015-06-04T14:16:44'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  opened_by:
                    type: string
                    description: |
                      __Read-only__ The User who opened this Ticket.
                    example: some_user
                    readOnly: true
                    x-linode-cli-display: 3
                  status:
                    type: string
                    description: |
                      __Read-only__ The current status of this Ticket.
                    example: open
                    readOnly: true
                    enum:
                    - closed
                    - new
                    - open
                  updated:
                    type: string
                    description: |
                      __Read-only__ The date and time this Ticket was last updated.
                    example: '2015-06-04T16:07:03'
                    readOnly: true
                    format: date-time
                    x-linode-filterable: true
                  updated_by:
                    type: string
                    description: |
                      __Read-only__ The User who last updated this Ticket.
                    example: some_other_user
                    nullable: true
                    readOnly: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-ticket
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/support/tickets/11223344
      - lang: CLI
        source: linode-cli tickets view 11223344
      x-linode-cli-action: view
      x-linode-grant: read_only
      x-original-op-id: getTicket
      x-original-op-title: Support Ticket View
    x-linode-cli-command: tickets
  /support/tickets/{ticketId}/attachments:
    x-akamai:
      file-path: paths/attachments.yaml
      path-info: /support/tickets/{ticketId}/attachments
    parameters:
    - name: ticketId
      in: path
      description: |
        The ID of the Support Ticket.
      example: '{{ticketId}}'
      x-akamai:
        file-path: parameters/ticket-id.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-ticket-attachment
      summary: Create a support ticket attachment
      tags:
      - Attachments
      description: |-
        Adds a file attachment to an existing Support Ticket on your Account. File attachments are used to assist our Support team in resolving your Ticket. Examples of attachments are screen shots and text files that provide additional information.

        The file attachment is submitted in the request as multipart/form-data.

        __Note__. Accepted file extensions include: .gif, .jpg, .jpeg, .pjpg, .pjpeg, .tif, .tiff, .png, .pdf, or .txt.


        ---


        - __CLI__.

            ```
            linode-cli tickets upload-attachment
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli tickets upload-attachment
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Add an attachment.
        required: true
        content:
          application/json: {}
          multipart/form-data:
            schema:
              additionalProperties: false
              required:
              - file
              properties:
                file:
                  type: string
                  description: |
                    The local, absolute path to the file you want to attach to your Support Ticket.
                  example: /Users/LinodeGuy/pictures/screen_shot.jpg
              x-ref: ../schemas/added-tbd.yaml
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Attachment created.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-ticket-attachment
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X POST \
              -F 'file=@/Users/LinodeGuy/pictures/screen_shot.jpg' \
              https://api.linode.com/v4/support/tickets/11223344/attachments
      x-linode-cli-action: upload-attachment
      x-linode-cli-skip: true
      x-linode-grant: read_write
      x-original-op-id: createTicketAttachment
      x-original-op-title: Support Ticket Attachment
        Create
    x-linode-cli-command: tickets
  /support/tickets/{ticketId}/close:
    x-akamai:
      file-path: paths/close.yaml
      path-info: /support/tickets/{ticketId}/close
    parameters:
    - name: ticketId
      in: path
      description: |
        The ID of the Support Ticket.
      example: '{{ticketId}}'
      x-akamai:
        file-path: parameters/ticket-id.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-close-ticket
      summary: Close a support ticket
      tags:
      - Support tickets
      description: |-
        Closes a Support Ticket you have access to modify.


        ---


        - __CLI__.

            ```
            linode-cli tickets close
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli tickets close
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Support Ticket closed successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-close-ticket
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X POST \
              https://api.linode.com/v4/support/tickets/11223344/close
      - lang: CLI
        source: linode-cli tickets close 11223344
      x-linode-cli-action: close
      x-linode-grant: read_write
      x-original-op-id: closeTicket
      x-original-op-title: Support Ticket Close
    x-linode-cli-command: tickets
  /support/tickets/{ticketId}/replies:
    x-akamai:
      file-path: paths/replies.yaml
      path-info: /support/tickets/{ticketId}/replies
    parameters:
    - name: ticketId
      in: path
      description: |
        The ID of the Support Ticket.
      example: '{{ticketId}}'
      x-akamai:
        file-path: parameters/ticket-id.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-ticket-reply
      summary: Create a reply
      tags:
      - Replies
      description: |-
        Adds a reply to an existing Support Ticket.


        ---


        - __CLI__.

            ```
            linode-cli tickets reply
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli tickets reply
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Add a reply.
        required: true
        content:
          application/json:
            schema:
              additionalProperties: false
              required:
              - description
              properties:
                description:
                  type: string
                  description: |
                    The content of your reply.
                  example: Thank you for your help. I was able to figure out what the problem was and I successfully
                    reset my password. You guys are the best!
                  minLength: 1
                  maxLength: 65535
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Reply created.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An object representing a reply to a Support Ticket.
                x-akamai:
                  file-path: schemas/support-ticket-reply.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of this Support Ticket reply.
                    example: 11223345
                    readOnly: true
                    x-linode-cli-display: 1
                  description:
                    type: string
                    description: |
                      __Read-only__ The body of this Support Ticket reply.
                    example: Hello,\nI'm sorry to hear that you are having trouble resetting the root password of your
                      Linode. Just to be sure, have you tried to follow the instructions
                      in our online documentation? The link is here:\n \nhttps://linode.com/docs/guides/reset-the-root-password-on-your-linode/
                      \n\nIf you have, please reply with any additional steps you
                      have also taken.\n\nRegards, Linode Support Team
                    readOnly: true
                  created:
                    type: string
                    description: |
                      __Read-only__ The date and time this Ticket reply was created.
                    example: '2015-06-02T14:31:41'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 3
                  created_by:
                    type: string
                    description: |
                      __Read-only__ The User who submitted this reply.
                    example: John Q. Linode
                    readOnly: true
                    x-linode-cli-display: 2
                  from_linode:
                    type: boolean
                    description: |
                      __Read-only__ If set to true, this reply came from a Linode employee.
                    example: true
                    readOnly: true
                  gravatar_id:
                    type: string
                    description: |
                      __Read-only__ The Gravatar ID of the User who created this reply.
                    example: 474a1b7373ae0be4132649e69c36ce30
                    readOnly: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-ticket-reply
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "description": "Thank you for your help. I was able to figure out what the problem was and I successfully reset my password. You guys are the best!"
              }' \
              https://api.linode.com/v4/support/tickets/11223344/replies
      - lang: CLI
        source: |-
          linode-cli tickets reply 11223344 \
            --description "Thank you for your help. I was able to figure out what the problem was and I successfully reset my password. You guys are the best!"
      x-linode-cli-action: reply
      x-linode-grant: read_write
      x-original-op-id: createTicketReply
      x-original-op-title: Reply Create
    get:
      operationId: get-ticket-replies
      summary: List replies
      tags:
      - Replies
      description: |-
        Returns a collection of replies to a Support Ticket on your Account.


        ---


        - __CLI__.

            ```
            linode-cli tickets replies
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli tickets replies
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated list of SupportTicketReply objects.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        An object representing a reply to a Support Ticket.
                      x-akamai:
                        file-path: schemas/support-ticket-reply.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ The unique ID of this Support Ticket reply.
                          example: 11223345
                          readOnly: true
                          x-linode-cli-display: 1
                        description:
                          type: string
                          description: |
                            __Read-only__ The body of this Support Ticket reply.
                          example: Hello,\nI'm sorry to hear that you are having trouble resetting the root
                            password of your Linode. Just to be sure, have you tried
                            to follow the instructions in our online documentation?
                            The link is here:\n \nhttps://linode.com/docs/guides/reset-the-root-password-on-your-linode/
                            \n\nIf you have, please reply with any additional steps
                            you have also taken.\n\nRegards, Linode Support Team
                          readOnly: true
                        created:
                          type: string
                          description: |
                            __Read-only__ The date and time this Ticket reply was created.
                          example: '2015-06-02T14:31:41'
                          readOnly: true
                          format: date-time
                          x-linode-cli-display: 3
                        created_by:
                          type: string
                          description: |
                            __Read-only__ The User who submitted this reply.
                          example: John Q. Linode
                          readOnly: true
                          x-linode-cli-display: 2
                        from_linode:
                          type: boolean
                          description: |
                            __Read-only__ If set to true, this reply came from a Linode employee.
                          example: true
                          readOnly: true
                        gravatar_id:
                          type: string
                          description: |
                            __Read-only__ The Gravatar ID of the User who created this reply.
                          example: 474a1b7373ae0be4132649e69c36ce30
                          readOnly: true
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-ticket-replies
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/support/tickets/11223344/replies
      - lang: CLI
        source: linode-cli tickets replies 11223344
      x-linode-cli-action: replies
      x-linode-grant: read_only
      x-original-op-id: getTicketReplies
      x-original-op-title: Replies List
    x-linode-cli-command: tickets
  /tags:
    x-akamai:
      file-path: paths/tags.yaml
      path-info: /tags
    post:
      operationId: post-tag
      summary: Create a tag
      tags:
      - Tags
      description: |-
        Creates a new Tag and optionally tags requested objects with it immediately.

        __Important__: You must be an unrestricted User in order to access, add, or modify Tags information.


        ---


        - __CLI__.

            ```
            linode-cli tags create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli tags create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The tag to create, and optionally the objects to tag.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - label
              properties:
                domains:
                  type: array
                  description: |
                    A list of Domain IDs to apply the new Tag to.  You must be allowed to `read_write`
                    all of the requested Domains, or the Tag will not be created and
                    an error will be returned.
                  example:
                  - 564
                  - 565
                  items:
                    type: integer
                label:
                  type: string
                  description: |
                    The new Tag.
                  example: '{{label}}'
                  minLength: 3
                  maxLength: 50
                linodes:
                  type: array
                  description: |
                    A list of Linode IDs to apply the new Tag to.  You must be allowed to `read_write`
                    all of the requested Linodes, or the Tag will not be created and
                    an error will be returned.
                  example:
                  - 123
                  - 456
                  items:
                    type: integer
                nodebalancers:
                  type: array
                  description: |
                    A list of NodeBalancer IDs to apply the new Tag to. You must be allowed to `read_write`
                    all of the requested NodeBalancers, or the Tag will not be created
                    and an error will be returned.
                  example:
                  - 10301
                  - 10501
                  items:
                    type: integer
                volumes:
                  type: array
                  description: |
                    A list of Volume IDs to apply the new Tag to.  You must be allowed to `read_write`
                    all of the requested Volumes, or the Tag will not be created and
                    an error will be returned.
                  example:
                  - 9082
                  - 10003
                  items:
                    type: integer
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The new Tag.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A tag that has been applied to an object on your Account. Tags are currently for organizational
                  purposes only.
                x-akamai:
                  file-path: schemas/tag.yaml
                additionalProperties: false
                properties:
                  label:
                    type: string
                    description: |
                      A Label used for organization of objects on your Account.
                    example: example tag
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-tag
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                  "label": "example tag",
                  "linodes": [123,456],
                  "volumes": [9082,10003]
              }' \
              https://api.linode.com/v4/tags
      - lang: CLI
        source: |-
          linode-cli tags create \
            --label 'example tag' \
            --linodes 123 \
            --linodes 456 \
            --volumes 9082 \
            --volumes 10003
      x-linode-cli-action: create
      x-linode-grant: unrestricted only
      x-original-op-id: createTag
      x-original-op-title: New Tag Create
    get:
      operationId: get-tags
      summary: List tags
      tags:
      - Tags
      description: |-
        Tags are User-defined labels attached to objects in your Account, such as Linodes. They are used for specifying and grouping attributes of objects that are relevant to the User.

        This operation returns a paginated list of Tags on your account.

        __Important__: You must be an unrestricted User in order to access, add, or modify Tags information.


        ---


        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of Tags
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        A tag that has been applied to an object on your Account. Tags are currently for
                        organizational purposes only.
                      x-akamai:
                        file-path: schemas/tag.yaml
                      additionalProperties: false
                      properties:
                        label:
                          type: string
                          description: |
                            A Label used for organization of objects on your Account.
                          example: example tag
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-tags
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/tags
      x-linode-cli-action:
      - list
      - ls
      x-linode-grant: unrestricted only
      x-original-op-id: getTags
      x-original-op-title: Tags List
    x-linode-cli-command: tags
  /tags/{tagLabel}:
    x-akamai:
      file-path: paths/tag-label.yaml
      path-info: /tags/{tagLabel}
    parameters:
    - name: tagLabel
      in: path
      example: '{{tagLabel}}'
      x-akamai:
        file-path: parameters/tag-label-path.yaml
      required: true
      schema:
        type: string
        description: |
          The `label` of the Tag to access.
    get:
      operationId: get-tagged-objects
      summary: List tagged objects
      tags:
      - Tags
      description: |-
        Returns a paginated list of all objects you've tagged with the requested Tag. This is a mixed collection of all object types.

        __Important__: You must be an unrestricted User in order to access, add, or modify Tags information.


        ---


        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of objects, organized by type, that have been tagged with the requested Tag.
          content:
            application/json:
              schema:
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      additionalProperties: false
                      properties:
                        type:
                          type: string
                          example: linode
                        data:
                          discriminator:
                            propertyName: type
                          oneOf:
                          - type: object
                            x-akamai:
                              file-path: schemas/linode.yaml
                            title: Linode
                            additionalProperties: false
                            properties:
                              id:
                                type: integer
                                description: |
                                  __Read-only__ This Linode's ID which must be provided for all
                                  operations impacting this Linode.
                                example: 123
                                readOnly: true
                                x-linode-cli-display: 1
                                x-linode-filterable: true
                              tags:
                                type: array
                                description: |
                                  An array of tags applied to this object.  Tags are for organizational
                                  purposes only.
                                example:
                                - example tag
                                - another example
                                items:
                                  type: string
                                x-linode-filterable: true
                              type:
                                type: string
                                description: |
                                  __Read-only__ This is the [Linode type](https://techdocs.akamai.com/linode-api/reference/get-linode-types)
                                  that this Linode was deployed with. To change a
                                  Linode's Type, use [Resize a Linode](https://techdocs.akamai.com/linode-api/reference/post-resize-linode-instance).
                                example: g6-standard-1
                                readOnly: true
                                x-linode-cli-display: 4
                              alerts:
                                type: object
                                additionalProperties: false
                                properties:
                                  cpu:
                                    type: integer
                                    description: |-
                                      The percentage of CPU usage required to trigger an alert. If the average CPU usage over two hours exceeds this value, we'll send you an alert. Your Linode's total CPU capacity is represented as 100%, multiplied by its number of cores.

                                      For example, a two core Linode's CPU capacity is represented as 200%. If you want to be alerted at 90% of a two core Linode's CPU capacity, set the alert value to `180`.

                                      The default value is 90% multiplied by the number of cores.

                                      If the value is set to `0` (zero), the alert is disabled.
                                    example: 180
                                  io:
                                    type: integer
                                    description: |
                                      The amount of disk IO operation per second required
                                      to trigger an alert. If the average disk IO
                                      over two hours exceeds this value, we'll send
                                      you an alert. If set to `0` (zero), this alert
                                      is disabled.
                                    example: 10000
                                  network_in:
                                    type: integer
                                    description: |
                                      The amount of incoming traffic, in Mbit/s, required
                                      to trigger an alert. If the average incoming
                                      traffic over two hours exceeds this value, we'll
                                      send you an alert. If this is set to `0` (zero),
                                      the alert is disabled.
                                    example: 10
                                  network_out:
                                    type: integer
                                    description: |
                                      The amount of outbound traffic, in Mbit/s, required
                                      to trigger an alert. If the average outbound
                                      traffic over two hours exceeds this value, we'll
                                      send you an alert. If this is set to `0` (zero),
                                      the alert is disabled.
                                    example: 10
                                  transfer_quota:
                                    type: integer
                                    description: |
                                      The percentage of network transfer that may be used
                                      before an alert is triggered. When this value
                                      is exceeded, we'll alert you. If this is set
                                      to `0` (zero), the alert is disabled.
                                    example: 80
                              backups:
                                type: object
                                description: |
                                  Information about this Linode's backups status. For information
                                  about available backups, run [List backups](https://techdocs.akamai.com/linode-api/reference/get-backups).
                                additionalProperties: false
                                properties:
                                  available:
                                    type: boolean
                                    description: |-
                                      __Read-only__ Whether Backups for this Linode are available for restoration.

                                      Backups undergoing maintenance are not available for restoration.
                                    example: true
                                    readOnly: true
                                  enabled:
                                    type: boolean
                                    description: |
                                      __Read-only__ If this Linode has the Backup service enabled.
                                      To enable backups, run [Enable backups](https://techdocs.akamai.com/linode-api/reference/post-enable-backups).
                                    example: true
                                    readOnly: true
                                  last_successful:
                                    type: string
                                    description: |
                                      __Read-only__ The last successful backup date. 'null' if there
                                      was no previous backup.
                                    example: '2018-01-01T00:01:01'
                                    readOnly: true
                                    format: date-time
                                  schedule:
                                    type: object
                                    additionalProperties: false
                                    properties:
                                      day:
                                        type: string
                                        description: |-
                                          The day of the week that your Linode's weekly Backup is taken. If not set manually, a day will be chosen for you. Backups are taken every day, but backups taken on this day are preferred when selecting backups to retain for a longer period.

                                          If not set manually, then when backups are initially enabled, this may come back as `Scheduling` until the `day` is automatically selected.
                                        example: Saturday
                                        nullable: true
                                        enum:
                                        - Scheduling
                                        - Sunday
                                        - Monday
                                        - Tuesday
                                        - Wednesday
                                        - Thursday
                                        - Friday
                                        - Saturday
                                      window:
                                        type: string
                                        description: |-
                                          The window in which your backups will be taken, in UTC. A backups window is a two-hour span of time in which the backup may occur.

                                          For example, `W10` indicates that your backups should be taken between 10:00 and 12:00. If you do not choose a backup window, one will be selected for you automatically.

                                          If not set manually, when backups are initially enabled this may come back as `Scheduling` until the `window` is automatically selected.
                                        example: W22
                                        nullable: true
                                        enum:
                                        - Scheduling
                                        - W0
                                        - W2
                                        - W4
                                        - W6
                                        - W8
                                        - W10
                                        - W12
                                        - W14
                                        - W16
                                        - W18
                                        - W20
                                        - W22
                              created:
                                type: string
                                description: |
                                  __Read-only__ When this Linode was created.
                                example: '2018-01-01T00:01:01'
                                readOnly: true
                                format: date-time
                              disk_encryption:
                                type: string
                                description: |
                                  Local disk encryption ensures that your data stored on compute
                                  instances is encrypted. Encryption converts the
                                  data on the compute instance into unreadable code.
                                  Decryption of the the disk requires other systems
                                  within the datacenter. This requirement protects
                                  against data leakage if the disk is removed from
                                  a datacenter, lost, stolen, recycled or disposed.
                                example: enabled
                                default: enabled
                                enum:
                                - enabled
                                - disabled
                              group:
                                type: string
                                description: |
                                  A deprecated property denoting a group label for this Linode.
                                example: Linode-Group
                                deprecated: true
                                x-linode-filterable: true
                              has_user_data:
                                type: boolean
                                description: |
                                  __Read-only__ Whether this compute instance was provisioned utilizing
                                  `user_data` provided via the Metadata service. See
                                  the [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
                                  description for more information on Metadata.
                                example: true
                                readOnly: true
                              host_uuid:
                                type: string
                                description: |
                                  __Read-only__ The Linode's host machine, as a UUID.
                                example: 3a3ddd59d9a78bb8de041391075df44de62bfec8
                                readOnly: true
                                format: uuid
                              hypervisor:
                                type: string
                                description: |
                                  __Read-only__ The virtualization software powering this Linode.
                                example: kvm
                                readOnly: true
                                enum:
                                - kvm
                              image:
                                example: linode/debian10
                                nullable: true
                                readOnly: true
                                allOf:
                                - type: string
                                  description: |-
                                    An Image ID to deploy the Linode Disk from.

                                    Run the [List images](https://techdocs.akamai.com/linode-api/reference/get-images) operation with authentication to view all available Images. Official Linode Images start with `linode/`, while your Account's Images start with `private/`. Creating a disk from a Private Image requires `read_only` or `read_write` permissions for that Image. Run the [Update a user's grants](https://techdocs.akamai.com/linode-api/reference/put-user-grants) operation to adjust permissions for an Account Image.
                                  example: linode/debian9
                                x-linode-cli-display: 5
                                x-linode-filterable: true
                              ipv4:
                                type: array
                                description: |-
                                  __Read-only__ This Linode's IPv4 Addresses. Each Linode is assigned a single public IPv4 address upon creation, and may get a single private IPv4 address if needed. You may need to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) to get additional IPv4 addresses.

                                  IPv4 addresses may be reassigned between your Linodes, or shared with other Linodes. See the [networking](https://techdocs.akamai.com/linode-api/reference/post-firewalls) operations for details.
                                example:
                                - 203.0.113.1
                                - 192.0.2.1
                                readOnly: true
                                format: ipv4
                                items:
                                  type: string
                                x-linode-cli-display: 7
                                x-linode-filterable: true
                              ipv6:
                                type: string
                                description: |
                                  __Read-only__ This Linode's IPv6 SLAAC address. This address is
                                  specific to a Linode, and may not be shared. If
                                  the Linode has not been assigned an IPv6 address,
                                  the return value will be `null`.
                                example: c001:d00d::1337/128
                                nullable: true
                                readOnly: true
                                format: ipv6/128
                              label:
                                type: string
                                description: |-
                                  The Linode's label is for display purposes only. If no label is provided for a Linode, a default will be assigned.

                                  Linode labels have the following constraints:

                                  - Must begin and end with an alphanumeric character.
                                  - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                                  - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
                                example: linode123
                                minLength: 3
                                maxLength: 64
                                pattern: ^[a-zA-Z]((?!--|__|..)[a-zA-Z0-9-_.])+$
                                x-linode-cli-display: 2
                                x-linode-filterable: true
                              placement_group:
                                type: object
                                description: |
                                  __Read-only__ Details on the [placement group](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/)
                                  that this Linode belongs to. Empty if the Linode
                                  isn't in a placement group.
                                additionalProperties: false
                                nullable: true
                                readOnly: true
                                properties:
                                  id:
                                    type: integer
                                    description: |
                                      The placement group's ID. You need to provide it
                                      for all operations impacting it.
                                    example: 528
                                    nullable: false
                                    x-linode-cli-display: 1
                                  affinity_type:
                                    type: string
                                    description: |-
                                      __Read-only__ How compute instances are distributed in your placement group. `anti-affinity:local` places compute instances in separate fault domains, but still in the same region.

                                      __Note__: With the limited availability release, only `anti_affinity:local` is available for `affinity_type`.
                                    example: anti-affinity:local
                                    readOnly: true
                                    x-linode-cli-display: 1
                                    x-linode-filterable: true
                                  is_strict:
                                    type: boolean
                                    description: |-
                                      How the API enforces your `affinity_type`:

                                      - Set to `true`, your group is strict. You can't add more compute instances to your placement group if your preferred container lacks capacity or is unavailable.
                                      - Set to `false`, your group is flexible. You can add more compute instances to it even if they violate the `affinity_type`. If you violate the `affinity_type` your placement group becomes non-compliant and you need to wait for our assistance.
                                    example: true
                                    nullable: false
                                  label:
                                    type: string
                                    description: |-
                                      The unique name set for the placement group. A label has these constraints:

                                      - It needs to begin and end with an alphanumeric character.
                                      - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                                    example: PG_Miami_failover
                                    minLength: 1
                                    x-linode-cli-display: 1
                                    x-linode-filterable: true
                              region:
                                type: string
                                description: |
                                  __Read-only__ This is the [region](https://techdocs.akamai.com/linode-api/reference/get-regions)
                                  where the Linode was deployed. A Linode's region
                                  can only be changed by initiating a [cross data
                                  center migration](https://techdocs.akamai.com/linode-api/reference/post-migrate-linode-instance).
                                example: us-east
                                readOnly: true
                                x-linode-cli-display: 3
                                x-linode-filterable: true
                              specs:
                                type: object
                                description: |
                                  __Read-only__ Information about the resources available to this
                                  Linode.
                                additionalProperties: false
                                readOnly: true
                                properties:
                                  disk:
                                    type: integer
                                    description: |
                                      __Read-only__ The amount of storage space, in MB, this Linode
                                      has access to. A typical Linode will divide
                                      this space between a primary disk with an `image`
                                      deployed to it, and a swap disk, usually 512
                                      MB. This is the default configuration created
                                      when deploying a Linode with an `image` through
                                      [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance).
                                      While this configuration is suitable for 99%
                                      of use cases, if you need finer control over
                                      your Linode's disks, see the [List disks](https://techdocs.akamai.com/linode-api/reference/get-linode-disks)
                                      operations.
                                    example: 81920
                                    readOnly: true
                                  gpus:
                                    type: integer
                                    description: |
                                      __Read-only__ The number of gpus this Linode has access to.
                                    example: 0
                                    readOnly: true
                                  memory:
                                    type: integer
                                    description: |-
                                      __Read-only__ The amount of RAM, in MB, this Linode has access to.

                                      Typically, a Linode boots with all of its available RAM, but this can be configured in a Config profile. See the [List config profiles](https://techdocs.akamai.com/linode-api/reference/get-linode-configs) operations and the LinodeConfig object for more information.
                                    example: 4096
                                    readOnly: true
                                  transfer:
                                    type: integer
                                    description: |
                                      __Read-only__ The amount of network transfer this Linode is
                                      allotted each month.
                                    example: 4000
                                    readOnly: true
                                  vcpus:
                                    type: integer
                                    description: |
                                      __Read-only__ The number of vcpus this Linode has access to.
                                    example: 2
                                    readOnly: true
                              status:
                                type: string
                                description: |
                                  __Read-only__ A brief description of this Linode's current state.
                                  This field may change without direct action from
                                  you. For example, when a Linode goes into maintenance
                                  mode its status will display "stopped".
                                example: running
                                readOnly: true
                                enum:
                                - running
                                - offline
                                - booting
                                - rebooting
                                - shutting_down
                                - provisioning
                                - deleting
                                - migrating
                                - rebuilding
                                - cloning
                                - restoring
                                - stopped
                                - billing_suspension
                                x-linode-cli-color:
                                  billing_suspension: red
                                  default_: yellow
                                  offline: red
                                  running: green
                                x-linode-cli-display: 6
                              updated:
                                type: string
                                description: |
                                  __Read-only__ When this Linode was last updated.
                                example: '2018-01-01T00:01:01'
                                readOnly: true
                                format: date-time
                              watchdog_enabled:
                                type: boolean
                                description: |
                                  The watchdog, named Lassie, is a Shutdown Watchdog that monitors
                                  your Linode and will reboot it if it powers off
                                  unexpectedly. It works by issuing a boot job when
                                  your Linode powers off without a shutdown job being
                                  responsible. To prevent a loop, Lassie will give
                                  up if there have been more than 5 boot jobs issued
                                  within 15 minutes.
                                example: true
                            x-linode-ref-name: linode
                          - type: object
                            description: |
                              A domain zonefile in our DNS system.  You must own the domain name and
                              tell your registrar to use Linode's nameservers in order
                              for a domain in our system to be treated as authoritative.
                            x-akamai:
                              file-path: schemas/domain.yaml
                            title: Domain
                            additionalProperties: false
                            properties:
                              id:
                                type: integer
                                description: |
                                  __Read-only__ This Domain's unique ID
                                example: 1234
                                readOnly: true
                                x-linode-cli-display: 1
                              tags:
                                type: array
                                description: |
                                  An array of tags applied to this object.  Tags are for organizational
                                  purposes only.
                                example:
                                - example tag
                                - another example
                                items:
                                  type: string
                                x-linode-filterable: true
                              type:
                                type: string
                                description: |
                                  Whether this Domain represents the authoritative source
                                  of information for the domain it describes ("master"),
                                  or whether it is a read-only copy of a master ("slave").
                                example: master
                                enum:
                                - master
                                - slave
                                x-linode-cli-display: 3
                              description:
                                type: string
                                description: |
                                  A description for this Domain. This is for display purposes only.
                                example: null
                                minLength: 1
                                maxLength: 253
                              axfr_ips:
                                type: array
                                description: |-
                                  The list of IPs that may perform a zone transfer for this Domain. The total combined length of all data within this array cannot exceed 1000 characters.

                                  __Note__: This is potentially dangerous, and should be set to an empty list unless you intend to use it.
                                example: []
                                items:
                                  type: string
                                  format: ip
                              domain:
                                type: string
                                description: |
                                  The domain this Domain represents. Domain labels cannot be
                                  longer than 63 characters and must conform to [RFC1035](https://tools.ietf.org/html/rfc1035).
                                  Domains must be unique on Linode's platform, including
                                  across different Linode accounts; there cannot be
                                  two Domains representing the same domain.
                                example: example.org
                                minLength: 1
                                maxLength: 253
                                pattern: ^(\*\.)?([a-zA-Z0-9-_]{1,63}\.)+([a-zA-Z]{2,3}\.)?([a-zA-Z]{2,16}|xn--[a-zA-Z0-9]+)$
                                x-linode-cli-display: 2
                                x-linode-filterable: true
                              expire_sec:
                                type: integer
                                description: |-
                                  The amount of time in seconds that may pass before this Domain is no longer authoritative.

                                  - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                                  - Any other value is rounded up to the nearest valid value.

                                  - A value of 0 is equivalent to the default value of 1209600.
                                example: 300
                                default: 0
                              group:
                                type: string
                                description: |
                                  The group this Domain belongs to.  This is for display purposes
                                  only.
                                example: null
                                minLength: 1
                                maxLength: 50
                                deprecated: true
                                x-linode-filterable: true
                              master_ips:
                                type: array
                                description: |
                                  The IP addresses representing the master DNS for this Domain.
                                  At least one value is required for `type` slave
                                  Domains. The total combined length of all data within
                                  this array cannot exceed 1000 characters.
                                example: []
                                items:
                                  type: string
                                  format: ip
                              refresh_sec:
                                type: integer
                                description: |-
                                  The amount of time in seconds before this Domain should be refreshed.

                                  - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                                  - Any other value is rounded up to the nearest valid value.

                                  - A value of 0 is equivalent to the default value of 14400.
                                example: 300
                                default: 0
                              retry_sec:
                                type: integer
                                description: |-
                                  The interval, in seconds, at which a failed refresh should be retried.

                                  - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                                  - Any other value is rounded up to the nearest valid value.

                                  - A value of 0 is equivalent to the default value of 14400.
                                example: 300
                                default: 0
                              soa_email:
                                type: string
                                description: |
                                  Start of Authority email address. This is required for `type`
                                  master Domains.
                                example: admin@example.org
                                format: email
                                x-linode-cli-display: 5
                              status:
                                type: string
                                description: |
                                  Used to control whether this Domain is currently being rendered.
                                example: active
                                default: active
                                enum:
                                - disabled
                                - active
                                x-linode-cli-color:
                                  active: green
                                  default_: red
                                  disabled: yellow
                                  edit_mode: yellow
                                x-linode-cli-display: 4
                              ttl_sec:
                                type: integer
                                description: |-
                                  "Time to Live" - the amount of time in seconds that this Domain's records may be cached by resolvers or other domain servers.

                                  - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
                                  - Any other value is rounded up to the nearest valid value.
                                  - A value of 0 is equivalent to the default value of 86400.
                                example: 300
                                default: 0
                            x-linode-ref-name: domain
                          - type: object
                            description: |
                              A Block Storage Volume associated with your Account.
                            x-akamai:
                              file-path: schemas/volume.yaml
                            title: Volume
                            additionalProperties: false
                            properties:
                              id:
                                type: integer
                                description: |
                                  __Read-only__ The unique ID of this Volume.
                                example: 12345
                                readOnly: true
                                x-linode-cli-display: 1
                              tags:
                                type: array
                                description: |
                                  An array of Tags applied to this object.  Tags are for organizational
                                  purposes only.
                                example:
                                - example tag
                                - another example
                                items:
                                  type: string
                                x-linode-filterable: true
                              created:
                                type: string
                                description: |
                                  __Read-only__ When this Volume was created.
                                example: '2018-01-01T00:01:01'
                                readOnly: true
                                format: date-time
                              filesystem_path:
                                type: string
                                description: |
                                  __Read-only__ The full filesystem path for the Volume based on
                                  the Volume's label. Path is /dev/disk/by-id/scsi-0Linode_Volume_
                                  + Volume label.
                                example: /dev/disk/by-id/scsi-0Linode_Volume_my-volume
                                readOnly: true
                              hardware_type:
                                type: string
                                description: |
                                  __Read-only__ The storage type of this Volume.
                                example: nvme
                                readOnly: true
                                enum:
                                - hdd
                                - nvme
                              label:
                                type: string
                                description: |
                                  The Volume's label is for display purposes only.
                                example: my-volume
                                minLength: 1
                                maxLength: 32
                                pattern: ^[a-zA-Z]((?!--|__)[a-zA-Z0-9-_])+$
                                x-linode-cli-display: 2
                                x-linode-filterable: true
                              linode_id:
                                type: integer
                                description: |
                                  If a Volume is attached to a specific Linode, the ID of that
                                  Linode will be displayed here.
                                example: 12346
                                nullable: true
                                x-linode-cli-display: 6
                              linode_label:
                                type: string
                                description: |
                                  __Read-only__ If a Volume is attached to a specific Linode, the
                                  label of that Linode will be displayed here.
                                example: linode123
                                nullable: true
                                readOnly: true
                                x-linode-cli-display: 7
                              region:
                                type: string
                                description: |
                                  __Read-only__ The unique ID of this Region.
                                example: us-east
                                readOnly: true
                                x-linode-cli-display: 5
                              size:
                                type: integer
                                description: |
                                  The Volume's size, in GiB.
                                example: 30
                                maximum: 10240
                                x-linode-cli-display: 4
                              status:
                                type: string
                                description: |-
                                  __Read-only__ The current status of the volume.  Can be one of:

                                    - `creating` - the Volume is being created and is not yet available
                                      for use.
                                    - `active` - the Volume is online and available for use.
                                    - `resizing` - the Volume is in the process of upgrading
                                      its current capacity.
                                example: active
                                readOnly: true
                                enum:
                                - creating
                                - active
                                - resizing
                                x-linode-cli-color:
                                  active: green
                                  contact_support: red
                                  default_: yellow
                                x-linode-cli-display: 3
                              updated:
                                type: string
                                description: |
                                  __Read-only__ When this Volume was last updated.
                                example: '2018-01-01T00:01:01'
                                readOnly: true
                                format: date-time
                            x-linode-ref-name: volume
                          - type: object
                            description: |
                              Linode's load balancing solution.  Can handle multiple ports, SSL
                              termination, and any number of backends.  NodeBalancer
                              ports are configured with NodeBalancer Configs, and
                              each config is given one or more NodeBalancer Node that
                              accepts traffic.  The traffic should be routed to the  NodeBalancer's
                              ip address, the NodeBalancer will handle routing individual
                              requests to backends.
                            x-akamai:
                              file-path: schemas/node-balancer.yaml
                            title: NodeBalancer
                            additionalProperties: false
                            properties:
                              id:
                                type: integer
                                description: |
                                  __Read-only__ This NodeBalancer's unique ID.
                                example: 12345
                                readOnly: true
                                x-linode-cli-display: 1
                              tags:
                                type: array
                                description: |
                                  An array of Tags applied to this object.  Tags are for organizational
                                  purposes only.
                                example:
                                - example tag
                                - another example
                                items:
                                  type: string
                                x-linode-filterable: true
                              client_conn_throttle:
                                type: integer
                                description: |
                                  Throttle connections per second.  Set to 0 (zero) to disable
                                  throttling.
                                example: 0
                                minimum: 0
                                maximum: 20
                                x-linode-cli-display: 6
                              created:
                                type: string
                                description: |
                                  __Read-only__ When this NodeBalancer was created.
                                example: '2018-01-01T00:01:01'
                                readOnly: true
                                format: date-time
                              host:
                                type: string
                                description: |
                                  __Read-only__ This NodeBalancer's hostname, beginning with its
                                  IP address and ending with _.ip.linodeusercontent.com_.
                                example: 192.0.2.1.ip.linodeusercontent.com
                                readOnly: true
                                x-linode-cli-display: 4
                              ipv4:
                                type: string
                                description: |
                                  __Read-only__ This NodeBalancer's public IPv4 address.
                                example: 203.0.113.1
                                readOnly: true
                                format: ip
                                x-linode-cli-display: 5
                                x-linode-filterable: true
                              ipv6:
                                type: string
                                description: |
                                  __Read-only__ This NodeBalancer's public IPv6 address.
                                example: null
                                nullable: true
                                readOnly: true
                                format: ip
                                x-linode-cli-display: 6
                              label:
                                type: string
                                description: |
                                  This NodeBalancer's label. These must be unique on your Account.
                                example: balancer12345
                                minLength: 3
                                maxLength: 32
                                pattern: '[a-zA-Z0-9-_]{3,32}'
                                x-linode-cli-display: 2
                                x-linode-filterable: true
                              region:
                                type: string
                                description: |
                                  __Read-only__ The Region where this NodeBalancer is located. NodeBalancers
                                  only support backends in the same Region.
                                example: us-east
                                readOnly: true
                                x-linode-cli-display: 3
                                x-linode-filterable: true
                              transfer:
                                type: object
                                description: |
                                  __Read-only__ Information about the amount of transfer this NodeBalancer
                                  has had so far this month.
                                additionalProperties: false
                                readOnly: true
                                properties:
                                  in:
                                    type: number
                                    description: |
                                      __Read-only__ The total outbound transfer, in MB, used for
                                      this NodeBalancer this month.
                                    example: 28.91200828552246
                                    nullable: true
                                    readOnly: true
                                  out:
                                    type: number
                                    description: |
                                      __Read-only__ The total inbound transfer, in MB, used for
                                      this NodeBalancer this month.
                                    example: 3.5487728118896484
                                    nullable: true
                                    readOnly: true
                                  total:
                                    type: number
                                    description: |
                                      __Read-only__ The total transfer, in MB, used by this NodeBalancer
                                      this month.
                                    example: 32.46078109741211
                                    nullable: true
                                    readOnly: true
                              updated:
                                type: string
                                description: |
                                  __Read-only__ When this NodeBalancer was last updated.
                                example: '2018-03-01T00:01:01'
                                readOnly: true
                                format: date-time
                            x-linode-ref-name: nodeBalancer
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-tagged-objects
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/tags/$tagLabel
      x-linode-cli-command: view
      x-linode-cli-skip: true
      x-original-op-id: getTaggedObjects
      x-original-op-title: Tagged Objects List
    delete:
      operationId: delete-ag
      summary: Delete a tag
      tags:
      - Tags
      description: |-
        Remove a Tag from all objects and delete it.

        __Important__: You must be an unrestricted User in order to access, add, or modify Tags information.


        ---


        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Tag deleted.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-ag
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-linode-cli-action:
      - delete
      - rm
      x-original-op-id: deleteTag
      x-original-op-title: Tag Delete
    x-linode-cli-command: tags
  /volumes:
    x-akamai:
      file-path: paths/volumes.yaml
      path-info: /volumes
    post:
      operationId: post-volume
      summary: Create a volume
      tags:
      - Volumes
      description: |-
        Creates a Volume on your Account. In order for this to complete successfully, your User must have the `add_volumes` grant. Creating a new Volume will start accruing additional charges on your account.


        ---


        - __CLI__.

            ```
            linode-cli volumes create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            volumes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli volumes create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: volumes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The requested initial state of a new Volume.
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - label
              properties:
                tags:
                  type: array
                  description: |
                    An array of Tags applied to this object.  Tags are for organizational purposes only.
                  example:
                  - example tag
                  - another example
                  items:
                    type: string
                  x-linode-filterable: true
                config_id:
                  type: integer
                  description: |-
                    When creating a Volume attached to a Linode, the ID of the Linode Config to include the new Volume in. This Config must belong to the Linode referenced by `linode_id`. Must _not_ be provided if `linode_id` is not sent. If a `linode_id` is sent without a `config_id`, the volume will be attached:

                      - to the Linode's only config if it only has one config.
                      - to the Linode's last used config, if possible.

                    If no config can be selected for attachment, an error will be returned.
                  example: '{{config_id}}'
                label:
                  type: string
                  description: |
                    The Volume's label, which is also used in the `filesystem_path` of the resulting volume.
                  example: '{{label}}'
                  minLength: 1
                  maxLength: 32
                  pattern: ^[a-zA-Z]((?!--|__)[a-zA-Z0-9-_])+$
                linode_id:
                  type: integer
                  description: |
                    The Linode this volume should be attached to upon creation. If not given, the volume
                    will be created without an attachment.
                  example: '{{linode_id}}'
                region:
                  type: string
                  description: |
                    The Region to deploy this Volume in. This is only required if a linode_id is not given.
                  example: '{{region}}'
                size:
                  type: integer
                  description: |
                    The initial size of this volume, in GB.  Be aware that volumes may only be resized up after
                    creation.
                  example: '{{size}}'
                  default: 20
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
        x-linode-cli-allowed-defaults:
        - region
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Creating Volume.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A Block Storage Volume associated with your Account.
                x-akamai:
                  file-path: schemas/volume.yaml
                title: Volume
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of this Volume.
                    example: 12345
                    readOnly: true
                    x-linode-cli-display: 1
                  tags:
                    type: array
                    description: |
                      An array of Tags applied to this object.  Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Volume was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  filesystem_path:
                    type: string
                    description: |
                      __Read-only__ The full filesystem path for the Volume based on the Volume's label.
                      Path is /dev/disk/by-id/scsi-0Linode_Volume_ + Volume label.
                    example: /dev/disk/by-id/scsi-0Linode_Volume_my-volume
                    readOnly: true
                  hardware_type:
                    type: string
                    description: |
                      __Read-only__ The storage type of this Volume.
                    example: nvme
                    readOnly: true
                    enum:
                    - hdd
                    - nvme
                  label:
                    type: string
                    description: |
                      The Volume's label is for display purposes only.
                    example: my-volume
                    minLength: 1
                    maxLength: 32
                    pattern: ^[a-zA-Z]((?!--|__)[a-zA-Z0-9-_])+$
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  linode_id:
                    type: integer
                    description: |
                      If a Volume is attached to a specific Linode, the ID of that Linode will be displayed
                      here.
                    example: 12346
                    nullable: true
                    x-linode-cli-display: 6
                  linode_label:
                    type: string
                    description: |
                      __Read-only__ If a Volume is attached to a specific Linode, the label of that Linode
                      will be displayed here.
                    example: linode123
                    nullable: true
                    readOnly: true
                    x-linode-cli-display: 7
                  region:
                    type: string
                    description: |
                      __Read-only__ The unique ID of this Region.
                    example: us-east
                    readOnly: true
                    x-linode-cli-display: 5
                  size:
                    type: integer
                    description: |
                      The Volume's size, in GiB.
                    example: 30
                    maximum: 10240
                    x-linode-cli-display: 4
                  status:
                    type: string
                    description: |-
                      __Read-only__ The current status of the volume.  Can be one of:

                        - `creating` - the Volume is being created and is not yet available
                          for use.
                        - `active` - the Volume is online and available for use.
                        - `resizing` - the Volume is in the process of upgrading
                          its current capacity.
                    example: active
                    readOnly: true
                    enum:
                    - creating
                    - active
                    - resizing
                    x-linode-cli-color:
                      active: green
                      contact_support: red
                      default_: yellow
                    x-linode-cli-display: 3
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Volume was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-volume
      security:
      - personalAccessToken: []
      - oauth:
        - volumes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "label": "my-volume",
                "size": 20,
                "linode_id": 12346
              }' \
              https://api.linode.com/v4/volumes
      - lang: CLI
        source: |-
          linode-cli volumes create \
            --label my-volume \
            --size 20 \
            --linode_id 12346 \
            --no-defaults
      x-linode-charge: true
      x-linode-cli-action: create
      x-linode-grant: add_volumes
      x-original-op-id: createVolume
      x-original-op-title: Volume Create
    get:
      operationId: get-volumes
      summary: List volumes
      tags:
      - Volumes
      description: |-
        Returns a paginated list of Volumes you have permission to view.


        ---


        - __OAuth__.

            ```
            volumes:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: volumes:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns an array of all Volumes on your Account.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        A Block Storage Volume associated with your Account.
                      x-akamai:
                        file-path: schemas/volume.yaml
                      title: Volume
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ The unique ID of this Volume.
                          example: 12345
                          readOnly: true
                          x-linode-cli-display: 1
                        tags:
                          type: array
                          description: |
                            An array of Tags applied to this object.  Tags are for organizational purposes
                            only.
                          example:
                          - example tag
                          - another example
                          items:
                            type: string
                          x-linode-filterable: true
                        created:
                          type: string
                          description: |
                            __Read-only__ When this Volume was created.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                        filesystem_path:
                          type: string
                          description: |
                            __Read-only__ The full filesystem path for the Volume based on the Volume's
                            label. Path is /dev/disk/by-id/scsi-0Linode_Volume_ +
                            Volume label.
                          example: /dev/disk/by-id/scsi-0Linode_Volume_my-volume
                          readOnly: true
                        hardware_type:
                          type: string
                          description: |
                            __Read-only__ The storage type of this Volume.
                          example: nvme
                          readOnly: true
                          enum:
                          - hdd
                          - nvme
                        label:
                          type: string
                          description: |
                            The Volume's label is for display purposes only.
                          example: my-volume
                          minLength: 1
                          maxLength: 32
                          pattern: ^[a-zA-Z]((?!--|__)[a-zA-Z0-9-_])+$
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        linode_id:
                          type: integer
                          description: |
                            If a Volume is attached to a specific Linode, the ID of that Linode will
                            be displayed here.
                          example: 12346
                          nullable: true
                          x-linode-cli-display: 6
                        linode_label:
                          type: string
                          description: |
                            __Read-only__ If a Volume is attached to a specific Linode, the label
                            of that Linode will be displayed here.
                          example: linode123
                          nullable: true
                          readOnly: true
                          x-linode-cli-display: 7
                        region:
                          type: string
                          description: |
                            __Read-only__ The unique ID of this Region.
                          example: us-east
                          readOnly: true
                          x-linode-cli-display: 5
                        size:
                          type: integer
                          description: |
                            The Volume's size, in GiB.
                          example: 30
                          maximum: 10240
                          x-linode-cli-display: 4
                        status:
                          type: string
                          description: |-
                            __Read-only__ The current status of the volume.  Can be one of:

                              - `creating` - the Volume is being created and is not yet available
                                for use.
                              - `active` - the Volume is online and available for use.
                              - `resizing` - the Volume is in the process of upgrading
                                its current capacity.
                          example: active
                          readOnly: true
                          enum:
                          - creating
                          - active
                          - resizing
                          x-linode-cli-color:
                            active: green
                            contact_support: red
                            default_: yellow
                          x-linode-cli-display: 3
                        updated:
                          type: string
                          description: |
                            __Read-only__ When this Volume was last updated.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-volumes
      security:
      - personalAccessToken: []
      - oauth:
        - volumes:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/volumes
      - lang: CLI
        source: linode-cli volumes list
      x-linode-cli-action:
      - list
      - ls
      x-linode-grant: read_only
      x-original-op-id: getVolumes
      x-original-op-title: Volumes List
    x-linode-cli-command: volumes
  /volumes/{volumeId}:
    x-akamai:
      file-path: paths/volume.yaml
      path-info: /volumes/{volumeId}
    parameters:
    - name: volumeId
      in: path
      description: |
        ID of the Volume to look up.
      example: '{{volumeId}}'
      x-akamai:
        file-path: parameters/volume-id-path-a4b5473e.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-volume
      summary: Get a volume
      tags:
      - Volumes
      description: |-
        Get information about a single Volume.


        ---


        - __CLI__.

            ```
            linode-cli volumes view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            volumes:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli volumes view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: volumes:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a single Volume object.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A Block Storage Volume associated with your Account.
                x-akamai:
                  file-path: schemas/volume.yaml
                title: Volume
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of this Volume.
                    example: 12345
                    readOnly: true
                    x-linode-cli-display: 1
                  tags:
                    type: array
                    description: |
                      An array of Tags applied to this object.  Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Volume was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  filesystem_path:
                    type: string
                    description: |
                      __Read-only__ The full filesystem path for the Volume based on the Volume's label.
                      Path is /dev/disk/by-id/scsi-0Linode_Volume_ + Volume label.
                    example: /dev/disk/by-id/scsi-0Linode_Volume_my-volume
                    readOnly: true
                  hardware_type:
                    type: string
                    description: |
                      __Read-only__ The storage type of this Volume.
                    example: nvme
                    readOnly: true
                    enum:
                    - hdd
                    - nvme
                  label:
                    type: string
                    description: |
                      The Volume's label is for display purposes only.
                    example: my-volume
                    minLength: 1
                    maxLength: 32
                    pattern: ^[a-zA-Z]((?!--|__)[a-zA-Z0-9-_])+$
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  linode_id:
                    type: integer
                    description: |
                      If a Volume is attached to a specific Linode, the ID of that Linode will be displayed
                      here.
                    example: 12346
                    nullable: true
                    x-linode-cli-display: 6
                  linode_label:
                    type: string
                    description: |
                      __Read-only__ If a Volume is attached to a specific Linode, the label of that Linode
                      will be displayed here.
                    example: linode123
                    nullable: true
                    readOnly: true
                    x-linode-cli-display: 7
                  region:
                    type: string
                    description: |
                      __Read-only__ The unique ID of this Region.
                    example: us-east
                    readOnly: true
                    x-linode-cli-display: 5
                  size:
                    type: integer
                    description: |
                      The Volume's size, in GiB.
                    example: 30
                    maximum: 10240
                    x-linode-cli-display: 4
                  status:
                    type: string
                    description: |-
                      __Read-only__ The current status of the volume.  Can be one of:

                        - `creating` - the Volume is being created and is not yet available
                          for use.
                        - `active` - the Volume is online and available for use.
                        - `resizing` - the Volume is in the process of upgrading
                          its current capacity.
                    example: active
                    readOnly: true
                    enum:
                    - creating
                    - active
                    - resizing
                    x-linode-cli-color:
                      active: green
                      contact_support: red
                      default_: yellow
                    x-linode-cli-display: 3
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Volume was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
          links:
            attach:
              operationId: post-attach-volume
              parameters:
                volumeID: $request.body#/id
            clone:
              operationId: post-clone-volume
              parameters:
                volumeId: $request.body#/id
            detach:
              operationId: post-attach-volume
              parameters:
                volumeId: $request.body#/id
            resize:
              operationId: post-resize-volume
              parameters:
                volumeId: $request.body#/id
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-volume
      security:
      - personalAccessToken: []
      - oauth:
        - volumes:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/volumes/12345
      - lang: CLI
        source: linode-cli volumes view 12345
      x-linode-cli-action: view
      x-linode-grant: read_only
      x-original-op-id: getVolume
      x-original-op-title: Volume View
    put:
      operationId: put-volume
      summary: Update a volume
      tags:
      - Volumes
      description: |-
        Updates a Volume that you have permission to `read_write`.


        ---


        - __CLI__.

            ```
            linode-cli volumes update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            volumes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli volumes update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: volumes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          If any updated field fails to pass validation, the Volume will not be updated.
        required: true
        content:
          application/json:
            schema:
              allOf:
              - type: object
                description: |
                  A Block Storage Volume associated with your Account.
                x-akamai:
                  file-path: schemas/volume.yaml
                title: Volume
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of this Volume.
                    example: 12345
                    readOnly: true
                    x-linode-cli-display: 1
                  tags:
                    type: array
                    description: |
                      An array of Tags applied to this object.  Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Volume was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  filesystem_path:
                    type: string
                    description: |
                      __Read-only__ The full filesystem path for the Volume based on the Volume's label.
                      Path is /dev/disk/by-id/scsi-0Linode_Volume_ + Volume label.
                    example: /dev/disk/by-id/scsi-0Linode_Volume_my-volume
                    readOnly: true
                  hardware_type:
                    type: string
                    description: |
                      __Read-only__ The storage type of this Volume.
                    example: nvme
                    readOnly: true
                    enum:
                    - hdd
                    - nvme
                  label:
                    type: string
                    description: |
                      The Volume's label is for display purposes only.
                    example: my-volume
                    minLength: 1
                    maxLength: 32
                    pattern: ^[a-zA-Z]((?!--|__)[a-zA-Z0-9-_])+$
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  linode_id:
                    type: integer
                    description: |
                      If a Volume is attached to a specific Linode, the ID of that Linode will be displayed
                      here.
                    example: 12346
                    nullable: true
                    x-linode-cli-display: 6
                  linode_label:
                    type: string
                    description: |
                      __Read-only__ If a Volume is attached to a specific Linode, the label of that Linode
                      will be displayed here.
                    example: linode123
                    nullable: true
                    readOnly: true
                    x-linode-cli-display: 7
                  region:
                    type: string
                    description: |
                      __Read-only__ The unique ID of this Region.
                    example: us-east
                    readOnly: true
                    x-linode-cli-display: 5
                  size:
                    type: integer
                    description: |
                      The Volume's size, in GiB.
                    example: 30
                    maximum: 10240
                    x-linode-cli-display: 4
                  status:
                    type: string
                    description: |-
                      __Read-only__ The current status of the volume.  Can be one of:

                        - `creating` - the Volume is being created and is not yet available
                          for use.
                        - `active` - the Volume is online and available for use.
                        - `resizing` - the Volume is in the process of upgrading
                          its current capacity.
                    example: active
                    readOnly: true
                    enum:
                    - creating
                    - active
                    - resizing
                    x-linode-cli-color:
                      active: green
                      contact_support: red
                      default_: yellow
                    x-linode-cli-display: 3
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Volume was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
              - type: object
                properties:
                  linode_id:
                    readOnly: true
                  size:
                    readOnly: true
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The updated Volume.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A Block Storage Volume associated with your Account.
                x-akamai:
                  file-path: schemas/volume.yaml
                title: Volume
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of this Volume.
                    example: 12345
                    readOnly: true
                    x-linode-cli-display: 1
                  tags:
                    type: array
                    description: |
                      An array of Tags applied to this object.  Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Volume was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  filesystem_path:
                    type: string
                    description: |
                      __Read-only__ The full filesystem path for the Volume based on the Volume's label.
                      Path is /dev/disk/by-id/scsi-0Linode_Volume_ + Volume label.
                    example: /dev/disk/by-id/scsi-0Linode_Volume_my-volume
                    readOnly: true
                  hardware_type:
                    type: string
                    description: |
                      __Read-only__ The storage type of this Volume.
                    example: nvme
                    readOnly: true
                    enum:
                    - hdd
                    - nvme
                  label:
                    type: string
                    description: |
                      The Volume's label is for display purposes only.
                    example: my-volume
                    minLength: 1
                    maxLength: 32
                    pattern: ^[a-zA-Z]((?!--|__)[a-zA-Z0-9-_])+$
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  linode_id:
                    type: integer
                    description: |
                      If a Volume is attached to a specific Linode, the ID of that Linode will be displayed
                      here.
                    example: 12346
                    nullable: true
                    x-linode-cli-display: 6
                  linode_label:
                    type: string
                    description: |
                      __Read-only__ If a Volume is attached to a specific Linode, the label of that Linode
                      will be displayed here.
                    example: linode123
                    nullable: true
                    readOnly: true
                    x-linode-cli-display: 7
                  region:
                    type: string
                    description: |
                      __Read-only__ The unique ID of this Region.
                    example: us-east
                    readOnly: true
                    x-linode-cli-display: 5
                  size:
                    type: integer
                    description: |
                      The Volume's size, in GiB.
                    example: 30
                    maximum: 10240
                    x-linode-cli-display: 4
                  status:
                    type: string
                    description: |-
                      __Read-only__ The current status of the volume.  Can be one of:

                        - `creating` - the Volume is being created and is not yet available
                          for use.
                        - `active` - the Volume is online and available for use.
                        - `resizing` - the Volume is in the process of upgrading
                          its current capacity.
                    example: active
                    readOnly: true
                    enum:
                    - creating
                    - active
                    - resizing
                    x-linode-cli-color:
                      active: green
                      contact_support: red
                      default_: yellow
                    x-linode-cli-display: 3
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Volume was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-volume
      security:
      - personalAccessToken: []
      - oauth:
        - volumes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "label": "my-volume"
              }' \
              https://api.linode.com/v4/volumes/12345
      - lang: CLI
        source: |-
          linode-cli volumes update 12345 \
            --label my_volume
      x-linode-cli-action: update
      x-linode-grant: read_write
      x-original-op-id: updateVolume
      x-original-op-title: Volume Update
    delete:
      operationId: delete-volume
      summary: Delete a volume
      tags:
      - Volumes
      description: |-
        Deletes a Volume you have permission to `read_write`.

        - __Deleting a Volume is a destructive action and cannot be undone.__

        - Deleting stops billing for the Volume. You will be billed for time used within the billing period the Volume was active.

        - Volumes that are migrating cannot be deleted until the migration is finished.


        ---


        - __OAuth__.

            ```
            volumes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: volumes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Volume deletion successful.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-volume
      security:
      - personalAccessToken: []
      - oauth:
        - volumes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/volumes/12345
      - lang: CLI
        source: linode-cli volumes delete 12345
      x-linode-cli-action:
      - delete
      - rm
      x-linode-grant: read_write
      x-original-op-id: deleteVolume
      x-original-op-title: Volume Delete
    x-linode-cli-command: volumes
  /volumes/{volumeId}/attach:
    x-akamai:
      file-path: paths/attach.yaml
      path-info: /volumes/{volumeId}/attach
    parameters:
    - name: volumeId
      in: path
      description: |
        ID of the Volume to attach.
      example: '{{volumeId}}'
      x-akamai:
        file-path: parameters/volume-id-path-62b4501e.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-attach-volume
      summary: Attach a volume
      tags:
      - Volumes
      description: |-
        Attaches a Volume on your Account to an existing Linode on your Account. In order for this request to complete successfully, your User must have `read_write` permission to the Volume and `read_write` permission to the Linode. Additionally, the Volume and Linode must be located in the same Region.


        ---


        - __CLI__.

            ```
            linode-cli volumes attach
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            volumes:read_write
        linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli volumes attach
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: |-
            volumes:read_write
            linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Volume to attach to a Linode.
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - linode_id
              properties:
                config_id:
                  type: integer
                  description: |
                    The ID of the Linode Config to include this Volume in. Must belong to the Linode referenced
                    by `linode_id`. If not given, the last booted Config will be chosen.
                  example: '{{config_id}}'
                linode_id:
                  type: integer
                  description: |
                    The ID of the Linode to attach the volume to.
                  example: '{{linode_id}}'
                persist_across_boots:
                  type: boolean
                  description: |
                    Defaults to true, if false is provided, the Volume will not be attached to the Linode
                    Config. In this case more than 8 Volumes may be attached to a
                    Linode if a Linode has 16GB of RAM or more. The number of volumes
                    that can be attached is equal to the number of GB of RAM that
                    the Linode has, up to a maximum of 64. `config_id` should not
                    be passed if this is set to false and linode_id must be passed.
                    The Linode must be running.
                  example: '{{persist_across_boots}}'
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Volume was attached to a Linode.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A Block Storage Volume associated with your Account.
                x-akamai:
                  file-path: schemas/volume.yaml
                title: Volume
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of this Volume.
                    example: 12345
                    readOnly: true
                    x-linode-cli-display: 1
                  tags:
                    type: array
                    description: |
                      An array of Tags applied to this object.  Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Volume was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  filesystem_path:
                    type: string
                    description: |
                      __Read-only__ The full filesystem path for the Volume based on the Volume's label.
                      Path is /dev/disk/by-id/scsi-0Linode_Volume_ + Volume label.
                    example: /dev/disk/by-id/scsi-0Linode_Volume_my-volume
                    readOnly: true
                  hardware_type:
                    type: string
                    description: |
                      __Read-only__ The storage type of this Volume.
                    example: nvme
                    readOnly: true
                    enum:
                    - hdd
                    - nvme
                  label:
                    type: string
                    description: |
                      The Volume's label is for display purposes only.
                    example: my-volume
                    minLength: 1
                    maxLength: 32
                    pattern: ^[a-zA-Z]((?!--|__)[a-zA-Z0-9-_])+$
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  linode_id:
                    type: integer
                    description: |
                      If a Volume is attached to a specific Linode, the ID of that Linode will be displayed
                      here.
                    example: 12346
                    nullable: true
                    x-linode-cli-display: 6
                  linode_label:
                    type: string
                    description: |
                      __Read-only__ If a Volume is attached to a specific Linode, the label of that Linode
                      will be displayed here.
                    example: linode123
                    nullable: true
                    readOnly: true
                    x-linode-cli-display: 7
                  region:
                    type: string
                    description: |
                      __Read-only__ The unique ID of this Region.
                    example: us-east
                    readOnly: true
                    x-linode-cli-display: 5
                  size:
                    type: integer
                    description: |
                      The Volume's size, in GiB.
                    example: 30
                    maximum: 10240
                    x-linode-cli-display: 4
                  status:
                    type: string
                    description: |-
                      __Read-only__ The current status of the volume.  Can be one of:

                        - `creating` - the Volume is being created and is not yet available
                          for use.
                        - `active` - the Volume is online and available for use.
                        - `resizing` - the Volume is in the process of upgrading
                          its current capacity.
                    example: active
                    readOnly: true
                    enum:
                    - creating
                    - active
                    - resizing
                    x-linode-cli-color:
                      active: green
                      contact_support: red
                      default_: yellow
                    x-linode-cli-display: 3
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Volume was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-attach-volume
      security:
      - personalAccessToken: []
      - oauth:
        - volumes:read_write
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "linode_id": 12346,
                "config_id": 23456
              }' \
              https://api.linode.com/v4/volumes/12345/attach
      - lang: CLI
        source: |-
          linode-cli volumes attach 12345 \
            --linode_id 12346 \
            --config_id 23456
      x-linode-cli-action: attach
      x-original-op-id: attachVolume
      x-original-op-title: Volume Attach
    x-linode-cli-command: volumes
  /volumes/{volumeId}/clone:
    x-akamai:
      file-path: paths/volume-clone.yaml
      path-info: /volumes/{volumeId}/clone
    parameters:
    - name: volumeId
      in: path
      description: |
        ID of the Volume to clone.
      example: '{{volumeId}}'
      x-akamai:
        file-path: parameters/volume-id-path-1f2bbcd1.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-clone-volume
      summary: Clone a volume
      tags:
      - Volumes
      description: |-
        Creates a Volume on your Account. In order for this request to complete successfully, your User must have the `add_volumes` grant. The new Volume will have the same size and data as the source Volume. Creating a new Volume will incur a charge on your Account.

        - Only Volumes with a `status` of "active" can be cloned.


        ---


        - __CLI__.

            ```
            linode-cli volumes clone
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            volumes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli volumes clone
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: volumes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The requested state your Volume will be cloned into.
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - label
              properties:
                label:
                  type: string
                  description: |
                    The Volume's label is for display purposes only.
                  example: '{{label}}'
                  minLength: 1
                  maxLength: 32
                  pattern: ^[a-zA-Z]((?!--|__)[a-zA-Z0-9-_])+$
                  x-linode-cli-display: 2
                  x-linode-filterable: true
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Clone started.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A Block Storage Volume associated with your Account.
                x-akamai:
                  file-path: schemas/volume.yaml
                title: Volume
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of this Volume.
                    example: 12345
                    readOnly: true
                    x-linode-cli-display: 1
                  tags:
                    type: array
                    description: |
                      An array of Tags applied to this object.  Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Volume was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  filesystem_path:
                    type: string
                    description: |
                      __Read-only__ The full filesystem path for the Volume based on the Volume's label.
                      Path is /dev/disk/by-id/scsi-0Linode_Volume_ + Volume label.
                    example: /dev/disk/by-id/scsi-0Linode_Volume_my-volume
                    readOnly: true
                  hardware_type:
                    type: string
                    description: |
                      __Read-only__ The storage type of this Volume.
                    example: nvme
                    readOnly: true
                    enum:
                    - hdd
                    - nvme
                  label:
                    type: string
                    description: |
                      The Volume's label is for display purposes only.
                    example: my-volume
                    minLength: 1
                    maxLength: 32
                    pattern: ^[a-zA-Z]((?!--|__)[a-zA-Z0-9-_])+$
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  linode_id:
                    type: integer
                    description: |
                      If a Volume is attached to a specific Linode, the ID of that Linode will be displayed
                      here.
                    example: 12346
                    nullable: true
                    x-linode-cli-display: 6
                  linode_label:
                    type: string
                    description: |
                      __Read-only__ If a Volume is attached to a specific Linode, the label of that Linode
                      will be displayed here.
                    example: linode123
                    nullable: true
                    readOnly: true
                    x-linode-cli-display: 7
                  region:
                    type: string
                    description: |
                      __Read-only__ The unique ID of this Region.
                    example: us-east
                    readOnly: true
                    x-linode-cli-display: 5
                  size:
                    type: integer
                    description: |
                      The Volume's size, in GiB.
                    example: 30
                    maximum: 10240
                    x-linode-cli-display: 4
                  status:
                    type: string
                    description: |-
                      __Read-only__ The current status of the volume.  Can be one of:

                        - `creating` - the Volume is being created and is not yet available
                          for use.
                        - `active` - the Volume is online and available for use.
                        - `resizing` - the Volume is in the process of upgrading
                          its current capacity.
                    example: active
                    readOnly: true
                    enum:
                    - creating
                    - active
                    - resizing
                    x-linode-cli-color:
                      active: green
                      contact_support: red
                      default_: yellow
                    x-linode-cli-display: 3
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Volume was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-clone-volume
      security:
      - personalAccessToken: []
      - oauth:
        - volumes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "label": "my-volume"
              }' \
              https://api.linode.com/v4/volumes/12345/clone
      - lang: CLI
        source: |-
          linode-cli volumes clone 12345 \
            --label my-volume
      x-linode-charge: true
      x-linode-cli-action: clone
      x-linode-grant: add_volumes
      x-original-op-id: cloneVolume
      x-original-op-title: Volume Clone
    x-linode-cli-command: volumes
  /volumes/{volumeId}/detach:
    x-akamai:
      file-path: paths/detach.yaml
      path-info: /volumes/{volumeId}/detach
    parameters:
    - name: volumeId
      in: path
      description: |
        ID of the Volume to detach.
      example: '{{volumeId}}'
      x-akamai:
        file-path: parameters/volume-id-path-e125a1de.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-detach-volume
      summary: Detach a volume
      tags:
      - Volumes
      description: |-
        Detaches a Volume on your Account from a Linode on your Account. In order for this request to complete successfully, your User must have `read_write` access to the Volume and `read_write` access to the Linode.

        Volumes are automatically detached from deleted Linodes.


        ---


        - __CLI__.

            ```
            linode-cli volumes detach
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            volumes:read_write
        linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli volumes detach
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: |-
            volumes:read_write
            linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Volume was detached from a Linode.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-detach-volume
      security:
      - personalAccessToken: []
      - oauth:
        - volumes:read_write
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST \
              https://api.linode.com/v4/volumes/12345/detach
      - lang: CLI
        source: linode-cli volumes detach 12345
      x-linode-cli-action: detach
      x-original-op-id: detachVolume
      x-original-op-title: Volume Detach
    x-linode-cli-command: volumes
  /volumes/{volumeId}/resize:
    x-akamai:
      file-path: paths/volume-resize.yaml
      path-info: /volumes/{volumeId}/resize
    parameters:
    - name: volumeId
      in: path
      description: |
        ID of the Volume to resize.
      example: '{{volumeId}}'
      x-akamai:
        file-path: parameters/volume-id-path-e4f0f1b5.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-resize-volume
      summary: Resize a volume
      tags:
      - Volumes
      description: |-
        Resize an existing Volume on your Account. In order for this request to complete successfully, your User must have the `read_write` permissions to the Volume.

        - Volumes can only be resized up.
        - Only Volumes with a `status` of "active" can be resized.


        ---


        - __CLI__.

            ```
            linode-cli volumes resize
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            volumes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli volumes resize
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: volumes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The requested size to increase your Volume to.
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - size
              properties:
                size:
                  type: integer
                  description: |
                    The Volume's size, in GiB.
                  example: '{{size}}'
                  maximum: 10240
                  x-linode-cli-display: 4
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Volume resize started.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A Block Storage Volume associated with your Account.
                x-akamai:
                  file-path: schemas/volume.yaml
                title: Volume
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of this Volume.
                    example: 12345
                    readOnly: true
                    x-linode-cli-display: 1
                  tags:
                    type: array
                    description: |
                      An array of Tags applied to this object.  Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Volume was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  filesystem_path:
                    type: string
                    description: |
                      __Read-only__ The full filesystem path for the Volume based on the Volume's label.
                      Path is /dev/disk/by-id/scsi-0Linode_Volume_ + Volume label.
                    example: /dev/disk/by-id/scsi-0Linode_Volume_my-volume
                    readOnly: true
                  hardware_type:
                    type: string
                    description: |
                      __Read-only__ The storage type of this Volume.
                    example: nvme
                    readOnly: true
                    enum:
                    - hdd
                    - nvme
                  label:
                    type: string
                    description: |
                      The Volume's label is for display purposes only.
                    example: my-volume
                    minLength: 1
                    maxLength: 32
                    pattern: ^[a-zA-Z]((?!--|__)[a-zA-Z0-9-_])+$
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  linode_id:
                    type: integer
                    description: |
                      If a Volume is attached to a specific Linode, the ID of that Linode will be displayed
                      here.
                    example: 12346
                    nullable: true
                    x-linode-cli-display: 6
                  linode_label:
                    type: string
                    description: |
                      __Read-only__ If a Volume is attached to a specific Linode, the label of that Linode
                      will be displayed here.
                    example: linode123
                    nullable: true
                    readOnly: true
                    x-linode-cli-display: 7
                  region:
                    type: string
                    description: |
                      __Read-only__ The unique ID of this Region.
                    example: us-east
                    readOnly: true
                    x-linode-cli-display: 5
                  size:
                    type: integer
                    description: |
                      The Volume's size, in GiB.
                    example: 30
                    maximum: 10240
                    x-linode-cli-display: 4
                  status:
                    type: string
                    description: |-
                      __Read-only__ The current status of the volume.  Can be one of:

                        - `creating` - the Volume is being created and is not yet available
                          for use.
                        - `active` - the Volume is online and available for use.
                        - `resizing` - the Volume is in the process of upgrading
                          its current capacity.
                    example: active
                    readOnly: true
                    enum:
                    - creating
                    - active
                    - resizing
                    x-linode-cli-color:
                      active: green
                      contact_support: red
                      default_: yellow
                    x-linode-cli-display: 3
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Volume was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-resize-volume
      security:
      - personalAccessToken: []
      - oauth:
        - volumes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "size": 30
              }' \
              https://api.linode.com/v4/volumes/12345/resize
      - lang: CLI
        source: |-
          linode-cli volumes resize 12345 \
            --size 30
      x-linode-charge: true
      x-linode-cli-action: resize
      x-original-op-id: resizeVolume
      x-original-op-title: Volume Resize
    x-linode-cli-command: volumes
  /vpcs:
    x-akamai:
      file-path: paths/vpcs.yaml
      path-info: /vpcs
    post:
      servers:
      - url: https://api.linode.com/v4
      operationId: post-vpc
      summary: Create a VPC
      tags:
      - VPCs
      description: |-
        Create a new VPC and optionally associated VPC Subnets.

        - Users must have the `add_vpc` grant to access this operation.
        - A successful request triggers a `vpc_create` event and `subnet_create` events for any created VPC Subnets.

        Once a VPC is created, it can be attached to a Linode by assigning a VPC Subnet to one of the Linode's Configuration Profile Interfaces. This step can be accomplished with the following operations:

        - [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
        - [Create a config profile](https://techdocs.akamai.com/linode-api/reference/post-add-linode-config)
        - [Update a config profile](https://techdocs.akamai.com/linode-api/reference/put-linode-config)
        - [Add a configuration profile interface](https://techdocs.akamai.com/linode-api/reference/post-linode-config-interface)


        ---


        - __CLI__.

            ```
            linode-cli vpcs create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            vpc:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli vpcs create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: vpc:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          VPC Create request object.
        required: true
        content:
          application/json:
            schema:
              allOf:
              - required:
                - label
                - region
              - properties:
                  subnets:
                    required:
                    - ipv4
                    - label
              - type: object
                description: |
                  An object describing a VPC belonging to the Account.
                x-akamai:
                  file-path: schemas/vpc.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of the VPC.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  description:
                    type: string
                    description: |
                      A written description to help distinguish the VPC.
                    example: A description of my VPC.
                    default: ''
                    maxLength: 255
                    x-linode-cli-display: 3
                  created:
                    type: string
                    description: |
                      __Read-only__ The date-time of VPC creation.
                    example: '2023-07-11T00:00:00'
                    readOnly: true
                    format: date-time
                    x-linode-filterable: true
                  label:
                    type: string
                    description: |-
                      The VPC's label, for display purposes only.

                      - Needs to be unique among the Account's VPCs.
                      - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                    example: cool-vpc
                    minLength: 1
                    maxLength: 64
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  region:
                    type: string
                    description: |
                      The Region for the VPC.
                    example: us-east
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  subnets:
                    type: array
                    description: |
                      A list of subnets associated with the VPC.
                    items:
                      type: object
                      description: |
                        An object describing a VPC Subnet.
                      x-akamai:
                        file-path: schemas/vpc-subnet.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ The unique ID of the VPC Subnet.
                          example: 456
                          readOnly: true
                          x-linode-cli-display: 1
                          x-linode-filterable: true
                        created:
                          type: string
                          description: |
                            __Read-only__ The date-time of VPC Subnet creation.
                          example: '2023-07-11T00:00:00'
                          readOnly: true
                          format: date-time
                          x-linode-filterable: true
                        ipv4:
                          type: string
                          description: |-
                            IPv4 range in CIDR canonical form.

                            - The range must belong to a private address space as defined in [RFC1918](https://datatracker.ietf.org/doc/html/rfc1918).
                            - Allowed prefix lengths: 1-29.
                            - The range must not overlap with 192.168.128.0/17.
                            - The range must not overlap with other Subnets on the same VPC.
                          example: 10.0.1.0/24
                          format: ip
                          x-linode-cli-display: 3
                        label:
                          type: string
                          description: |-
                            The VPC Subnet's label, for display purposes only.

                            - Must be unique among the VPC's Subnets.
                            - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                          example: cool-vpc-subnet
                          minLength: 1
                          maxLength: 64
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        linodes:
                          type: array
                          description: |-
                            __Read-only__ An array of Linode IDs assigned to the VPC Subnet.

                            A Linode is assigned to a VPC Subnet if it has a Configuration Profile with a `vpc` purpose interface with the subnet's `subnet_id`. Even if the Configuration Profile is not active, meaning the Linode does not have access to the Subnet, the Linode still appears in this array.
                          readOnly: true
                          items:
                            type: object
                            additionalProperties: false
                            properties:
                              id:
                                type: integer
                                description: |
                                  ID of a Linode assigned to the VPC Subnet.
                                example: 111
                              interfaces:
                                type: array
                                description: |
                                  An array of Interfaces assigned to the Linode.
                                items:
                                  type: object
                                  additionalProperties: false
                                  properties:
                                    id:
                                      type: integer
                                      description: |
                                        ID of the interface
                                      example: 421
                                    active:
                                      type: boolean
                                      description: |
                                        Returns `true` if the Interface is in use, meaning
                                        that the Linode was powered on using the Configuration
                                        Profile to which the Interface belongs. Otherwise
                                        returns `false`.
                                      example: true
                          x-linode-cli-display: 5
                        updated:
                          type: string
                          description: |
                            __Read-only__ The date-time of the most recent VPC Subnet update.
                          example: '2023-09-11T00:00:00'
                          nullable: true
                          readOnly: true
                          format: date-time
                          x-linode-filterable: true
                  updated:
                    type: string
                    description: |
                      __Read-only__ The date-time of the most recent VPC update.
                    example: '2023-09-11T00:00:00'
                    nullable: true
                    readOnly: true
                    format: date-time
                    x-linode-filterable: true
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The new VPC.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An object describing a VPC belonging to the Account.
                x-akamai:
                  file-path: schemas/vpc.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of the VPC.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  description:
                    type: string
                    description: |
                      A written description to help distinguish the VPC.
                    example: A description of my VPC.
                    default: ''
                    maxLength: 255
                    x-linode-cli-display: 3
                  created:
                    type: string
                    description: |
                      __Read-only__ The date-time of VPC creation.
                    example: '2023-07-11T00:00:00'
                    readOnly: true
                    format: date-time
                    x-linode-filterable: true
                  label:
                    type: string
                    description: |-
                      The VPC's label, for display purposes only.

                      - Needs to be unique among the Account's VPCs.
                      - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                    example: cool-vpc
                    minLength: 1
                    maxLength: 64
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  region:
                    type: string
                    description: |
                      The Region for the VPC.
                    example: us-east
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  subnets:
                    type: array
                    description: |
                      A list of subnets associated with the VPC.
                    items:
                      type: object
                      description: |
                        An object describing a VPC Subnet.
                      x-akamai:
                        file-path: schemas/vpc-subnet.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ The unique ID of the VPC Subnet.
                          example: 456
                          readOnly: true
                          x-linode-cli-display: 1
                          x-linode-filterable: true
                        created:
                          type: string
                          description: |
                            __Read-only__ The date-time of VPC Subnet creation.
                          example: '2023-07-11T00:00:00'
                          readOnly: true
                          format: date-time
                          x-linode-filterable: true
                        ipv4:
                          type: string
                          description: |-
                            IPv4 range in CIDR canonical form.

                            - The range must belong to a private address space as defined in [RFC1918](https://datatracker.ietf.org/doc/html/rfc1918).
                            - Allowed prefix lengths: 1-29.
                            - The range must not overlap with 192.168.128.0/17.
                            - The range must not overlap with other Subnets on the same VPC.
                          example: 10.0.1.0/24
                          format: ip
                          x-linode-cli-display: 3
                        label:
                          type: string
                          description: |-
                            The VPC Subnet's label, for display purposes only.

                            - Must be unique among the VPC's Subnets.
                            - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                          example: cool-vpc-subnet
                          minLength: 1
                          maxLength: 64
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        linodes:
                          type: array
                          description: |-
                            __Read-only__ An array of Linode IDs assigned to the VPC Subnet.

                            A Linode is assigned to a VPC Subnet if it has a Configuration Profile with a `vpc` purpose interface with the subnet's `subnet_id`. Even if the Configuration Profile is not active, meaning the Linode does not have access to the Subnet, the Linode still appears in this array.
                          readOnly: true
                          items:
                            type: object
                            additionalProperties: false
                            properties:
                              id:
                                type: integer
                                description: |
                                  ID of a Linode assigned to the VPC Subnet.
                                example: 111
                              interfaces:
                                type: array
                                description: |
                                  An array of Interfaces assigned to the Linode.
                                items:
                                  type: object
                                  additionalProperties: false
                                  properties:
                                    id:
                                      type: integer
                                      description: |
                                        ID of the interface
                                      example: 421
                                    active:
                                      type: boolean
                                      description: |
                                        Returns `true` if the Interface is in use, meaning
                                        that the Linode was powered on using the Configuration
                                        Profile to which the Interface belongs. Otherwise
                                        returns `false`.
                                      example: true
                          x-linode-cli-display: 5
                        updated:
                          type: string
                          description: |
                            __Read-only__ The date-time of the most recent VPC Subnet update.
                          example: '2023-09-11T00:00:00'
                          nullable: true
                          readOnly: true
                          format: date-time
                          x-linode-filterable: true
                  updated:
                    type: string
                    description: |
                      __Read-only__ The date-time of the most recent VPC update.
                    example: '2023-09-11T00:00:00'
                    nullable: true
                    readOnly: true
                    format: date-time
                    x-linode-filterable: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-vpc
      security:
      - personalAccessToken: []
      - oauth:
        - vpc:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl https://api.linode.com/v4/vpcs \
            -H "Authorization: Bearer $TOKEN" \
            -H "Content-Type: application/json" \
            -X POST -d '{
              "description": "A description of my VPC.",
              "label": "cool-vpc",
              "region": "us-east",
              "subnets": [
                {
                  "label": "cool-vpc-subnet",
                  "ipv4": "10.0.1.0/24"
                }
              ]
            }'
      - lang: CLI
        source: |-
          linode-cli vpcs create \
            --description "A description of my VPC." \
            --label cool-vpc \
            --region us-east \
            --subnets.label cool-vpc-subnet \
            --subnets.ipv4 10.0.1.0/24
      x-linode-cli-action: create
      x-linode-grant: add_vpcs
      x-original-op-id: createVPC
      x-original-op-title: VPC Create
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-vpcs
      summary: List VPCs
      tags:
      - VPCs
      description: |
        Display all VPCs on your account.
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of VPC objects.
          content:
            application/json:
              schema:
                allOf:
                - type: object
                  description: |
                    An envelope for paginated response. When accessing a collection through a GET endpoint,
                    the results are wrapped in this envelope which includes metadata
                    about those results. Results are presented within a `data` array.
                    See [Pagination](https://techdocs.akamai.com/linode-api/reference/pagination)
                    for more information.
                  x-akamai:
                    file-path: schemas/pagination-envelope.yaml
                  additionalProperties: false
                  properties:
                    page:
                      type: integer
                      description: |
                        __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    pages:
                      type: integer
                      description: |
                        __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    results:
                      type: integer
                      description: |
                        __Read-only__ The total number of results.
                      example: 1
                      readOnly: true
                - properties:
                    data:
                      type: array
                      items:
                        type: object
                        description: |
                          An object describing a VPC belonging to the Account.
                        x-akamai:
                          file-path: schemas/vpc.yaml
                        additionalProperties: false
                        properties:
                          id:
                            type: integer
                            description: |
                              __Read-only__ The unique ID of the VPC.
                            example: 123
                            readOnly: true
                            x-linode-cli-display: 1
                            x-linode-filterable: true
                          description:
                            type: string
                            description: |
                              A written description to help distinguish the VPC.
                            example: A description of my VPC.
                            default: ''
                            maxLength: 255
                            x-linode-cli-display: 3
                          created:
                            type: string
                            description: |
                              __Read-only__ The date-time of VPC creation.
                            example: '2023-07-11T00:00:00'
                            readOnly: true
                            format: date-time
                            x-linode-filterable: true
                          label:
                            type: string
                            description: |-
                              The VPC's label, for display purposes only.

                              - Needs to be unique among the Account's VPCs.
                              - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                            example: cool-vpc
                            minLength: 1
                            maxLength: 64
                            x-linode-cli-display: 2
                            x-linode-filterable: true
                          region:
                            type: string
                            description: |
                              The Region for the VPC.
                            example: us-east
                            x-linode-cli-display: 4
                            x-linode-filterable: true
                          subnets:
                            type: array
                            description: |
                              A list of subnets associated with the VPC.
                            items:
                              type: object
                              description: |
                                An object describing a VPC Subnet.
                              x-akamai:
                                file-path: schemas/vpc-subnet.yaml
                              additionalProperties: false
                              properties:
                                id:
                                  type: integer
                                  description: |
                                    __Read-only__ The unique ID of the VPC Subnet.
                                  example: 456
                                  readOnly: true
                                  x-linode-cli-display: 1
                                  x-linode-filterable: true
                                created:
                                  type: string
                                  description: |
                                    __Read-only__ The date-time of VPC Subnet creation.
                                  example: '2023-07-11T00:00:00'
                                  readOnly: true
                                  format: date-time
                                  x-linode-filterable: true
                                ipv4:
                                  type: string
                                  description: |-
                                    IPv4 range in CIDR canonical form.

                                    - The range must belong to a private address space as defined in [RFC1918](https://datatracker.ietf.org/doc/html/rfc1918).
                                    - Allowed prefix lengths: 1-29.
                                    - The range must not overlap with 192.168.128.0/17.
                                    - The range must not overlap with other Subnets on the same VPC.
                                  example: 10.0.1.0/24
                                  format: ip
                                  x-linode-cli-display: 3
                                label:
                                  type: string
                                  description: |-
                                    The VPC Subnet's label, for display purposes only.

                                    - Must be unique among the VPC's Subnets.
                                    - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                                  example: cool-vpc-subnet
                                  minLength: 1
                                  maxLength: 64
                                  x-linode-cli-display: 2
                                  x-linode-filterable: true
                                linodes:
                                  type: array
                                  description: |-
                                    __Read-only__ An array of Linode IDs assigned to the VPC Subnet.

                                    A Linode is assigned to a VPC Subnet if it has a Configuration Profile with a `vpc` purpose interface with the subnet's `subnet_id`. Even if the Configuration Profile is not active, meaning the Linode does not have access to the Subnet, the Linode still appears in this array.
                                  readOnly: true
                                  items:
                                    type: object
                                    additionalProperties: false
                                    properties:
                                      id:
                                        type: integer
                                        description: |
                                          ID of a Linode assigned to the VPC Subnet.
                                        example: 111
                                      interfaces:
                                        type: array
                                        description: |
                                          An array of Interfaces assigned to the Linode.
                                        items:
                                          type: object
                                          additionalProperties: false
                                          properties:
                                            id:
                                              type: integer
                                              description: |
                                                ID of the interface
                                              example: 421
                                            active:
                                              type: boolean
                                              description: |
                                                Returns `true` if the Interface is in use,
                                                meaning that the Linode was powered
                                                on using the Configuration Profile
                                                to which the Interface belongs. Otherwise
                                                returns `false`.
                                              example: true
                                  x-linode-cli-display: 5
                                updated:
                                  type: string
                                  description: |
                                    __Read-only__ The date-time of the most recent VPC Subnet update.
                                  example: '2023-09-11T00:00:00'
                                  nullable: true
                                  readOnly: true
                                  format: date-time
                                  x-linode-filterable: true
                          updated:
                            type: string
                            description: |
                              __Read-only__ The date-time of the most recent VPC update.
                            example: '2023-09-11T00:00:00'
                            nullable: true
                            readOnly: true
                            format: date-time
                            x-linode-filterable: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-vpcs
      security:
      - personalAccessToken: []
      - oauth: []
      x-code-samples:
      - lang: Shell
        source: |-
          curl https://api.linode.com/v4/vpcs \
              -H "Authorization: Bearer $TOKEN"
      - lang: CLI
        source: linode-cli vpcs list
      x-linode-cli-action:
      - list
      - ls
      x-original-op-id: getVPCs
      x-original-op-title: VPCs List
    x-linode-cli-command: vpcs
  /vpcs/ips:
    x-akamai:
      file-path: paths/vpcs-ips.yaml
      path-info: /vpcs/ips
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-vpcs-ips
      summary: List VPC IP addresses
      tags:
      - IP addresses
      description: |-
        Returns a paginated list of all VPC IP addresses and address ranges on your account.

        __Note__. If a Linode has several configuration profiles that include a VPC interface, address information for all of them is listed in the response. Since VPCs can use the same address space, you may see duplicate IP addresses.


        ---


        - __OAuth__.

            ```
            ips:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: ips:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of VPC interface IP addresses.
          content:
            application/json:
              schema:
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      description: |
                        The response data for the VPC IP Addresses List and View operations.
                      x-akamai:
                        file-path: schemas/ip-addresses-vpc.yaml
                      allOf:
                      - type: object
                        description: |
                          An envelope for paginated response. When accessing a collection through
                          a GET endpoint, the results are wrapped in this envelope
                          which includes metadata about those results. Results are
                          presented within a `data` array. See [Pagination](https://techdocs.akamai.com/linode-api/reference/pagination)
                          for more information.
                        x-akamai:
                          file-path: schemas/pagination-envelope.yaml
                        additionalProperties: false
                        properties:
                          page:
                            type: integer
                            description: |
                              __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                            example: 1
                            readOnly: true
                          pages:
                            type: integer
                            description: |
                              __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                            example: 1
                            readOnly: true
                          results:
                            type: integer
                            description: |
                              __Read-only__ The total number of results.
                            example: 1
                            readOnly: true
                      - type: object
                        description: |
                          A VPC IP address that exists in Linode's system, specific to the response
                          for the [List VPC IP addresses](https://techdocs.akamai.com/linode-api/reference/get-vpcs-ips)
                          operation. Returned as an empty set for Linodes that are
                          not part of a VPC.
                        properties:
                          active:
                            type: boolean
                            description: |
                              __Read-only__ Returns `true` if the VPC interface is in use, meaning
                              that the Linode was powered on using the `config_id`
                              to which the interface belongs. Otherwise returns `false`.
                            example: true
                            readOnly: true
                            x-linode-filterable: true
                          address:
                            type: string
                            description: |
                              __Read-only__ An IPv4 address configured for this VPC interface. These
                              follow the [RFC 1918](https://datatracker.ietf.org/doc/html/rfc1918)
                              private address format. Displayed as `null` if an `address_range`.
                            example: 192.0.2.141
                            nullable: true
                            readOnly: true
                            format: ip
                            x-linode-cli-display: 1
                          address_range:
                            type: string
                            description: |
                              __Read-only__ A range of IPv4 addresses configured for this VPC interface.
                              Displayed as `null` if a single `address`.
                            nullable: true
                            readOnly: true
                          config_id:
                            type: integer
                            description: |
                              __Read-only__ The globally general entity identifier for the Linode
                              configuration profile where the VPC is included.
                            example: 4567
                            readOnly: true
                            x-linode-filterable: true
                          gateway:
                            type: string
                            description: |
                              __Read-only__ The default gateway for the VPC subnet that the IP or
                              IP range belongs to.
                            example: 192.0.2.1
                            nullable: true
                            readOnly: true
                            format: ip
                          interface_id:
                            type: integer
                            description: |
                              __Read-only__ The globally general API entity identifier for the Linode
                              interface.
                            example: 2435
                            readOnly: true
                          linode_id:
                            type: integer
                            description: |
                              __Read-only__ The identifier for the Linode the VPC interface currently
                              belongs to.
                            example: 123
                            readOnly: true
                            x-linode-cli-display: 6
                            x-linode-filterable: true
                          nat_1_1:
                            type: string
                            description: |
                              __Read-only__ The public IP address used for NAT 1:1 with the VPC.
                              This is empty if NAT 1:1 isn't used.
                            example: 192.168.0.42
                            readOnly: true
                            format: ip
                          prefix:
                            type: integer
                            description: |
                              __Read-only__ The number of bits set in the `subnet_mask`.
                            example: 24
                            nullable: true
                            readOnly: true
                          region:
                            type: string
                            description: |
                              __Read-only__ The region of the VPC.
                            example: us-east
                            readOnly: true
                            x-linode-cli-display: 5
                            x-linode-filterable: true
                          subnet_id:
                            type: integer
                            description: |
                              The `id` of the VPC Subnet for this interface.
                            example: 101
                            nullable: false
                          subnet_mask:
                            type: string
                            description: |
                              __Read-only__ The mask that separates host bits from network bits
                              for the `address` or `address_range`.
                            example: 255.255.255.0
                            readOnly: true
                            format: ip
                          vpc_id:
                            type: integer
                            description: |
                              __Read-only__ The unique globally general API entity identifier for
                              the VPC.
                            example: 7654
                            readOnly: true
                            x-linode-filterable: true
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-vpcs-ips
      security:
      - personalAccessToken: []
      - oauth:
        - ips:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/networking/vpcs/ips
      - lang: CLI
        source: linode-cli vpcs ip list
      x-linode-cli-action:
      - ip-list
      - ip-ls
      x-linode-grant: read_only
      x-original-op-id: getAllVPCIPs
      x-original-op-title: VPC IP Addresses List
  /vpcs/{vpcId}:
    x-akamai:
      file-path: paths/vpc.yaml
      path-info: /vpcs/{vpcId}
    parameters:
    - name: vpcId
      in: path
      description: |
        The `id` of the VPC.
      example: '{{vpcId}}'
      x-akamai:
        file-path: parameters/vpc-id.yaml
      required: true
      schema:
        type: integer
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-vpc
      summary: Get a VPC
      tags:
      - VPCs
      description: |
        Get information about a single VPC.
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A VPC object.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An object describing a VPC belonging to the Account.
                x-akamai:
                  file-path: schemas/vpc.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of the VPC.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  description:
                    type: string
                    description: |
                      A written description to help distinguish the VPC.
                    example: A description of my VPC.
                    default: ''
                    maxLength: 255
                    x-linode-cli-display: 3
                  created:
                    type: string
                    description: |
                      __Read-only__ The date-time of VPC creation.
                    example: '2023-07-11T00:00:00'
                    readOnly: true
                    format: date-time
                    x-linode-filterable: true
                  label:
                    type: string
                    description: |-
                      The VPC's label, for display purposes only.

                      - Needs to be unique among the Account's VPCs.
                      - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                    example: cool-vpc
                    minLength: 1
                    maxLength: 64
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  region:
                    type: string
                    description: |
                      The Region for the VPC.
                    example: us-east
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  subnets:
                    type: array
                    description: |
                      A list of subnets associated with the VPC.
                    items:
                      type: object
                      description: |
                        An object describing a VPC Subnet.
                      x-akamai:
                        file-path: schemas/vpc-subnet.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ The unique ID of the VPC Subnet.
                          example: 456
                          readOnly: true
                          x-linode-cli-display: 1
                          x-linode-filterable: true
                        created:
                          type: string
                          description: |
                            __Read-only__ The date-time of VPC Subnet creation.
                          example: '2023-07-11T00:00:00'
                          readOnly: true
                          format: date-time
                          x-linode-filterable: true
                        ipv4:
                          type: string
                          description: |-
                            IPv4 range in CIDR canonical form.

                            - The range must belong to a private address space as defined in [RFC1918](https://datatracker.ietf.org/doc/html/rfc1918).
                            - Allowed prefix lengths: 1-29.
                            - The range must not overlap with 192.168.128.0/17.
                            - The range must not overlap with other Subnets on the same VPC.
                          example: 10.0.1.0/24
                          format: ip
                          x-linode-cli-display: 3
                        label:
                          type: string
                          description: |-
                            The VPC Subnet's label, for display purposes only.

                            - Must be unique among the VPC's Subnets.
                            - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                          example: cool-vpc-subnet
                          minLength: 1
                          maxLength: 64
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        linodes:
                          type: array
                          description: |-
                            __Read-only__ An array of Linode IDs assigned to the VPC Subnet.

                            A Linode is assigned to a VPC Subnet if it has a Configuration Profile with a `vpc` purpose interface with the subnet's `subnet_id`. Even if the Configuration Profile is not active, meaning the Linode does not have access to the Subnet, the Linode still appears in this array.
                          readOnly: true
                          items:
                            type: object
                            additionalProperties: false
                            properties:
                              id:
                                type: integer
                                description: |
                                  ID of a Linode assigned to the VPC Subnet.
                                example: 111
                              interfaces:
                                type: array
                                description: |
                                  An array of Interfaces assigned to the Linode.
                                items:
                                  type: object
                                  additionalProperties: false
                                  properties:
                                    id:
                                      type: integer
                                      description: |
                                        ID of the interface
                                      example: 421
                                    active:
                                      type: boolean
                                      description: |
                                        Returns `true` if the Interface is in use, meaning
                                        that the Linode was powered on using the Configuration
                                        Profile to which the Interface belongs. Otherwise
                                        returns `false`.
                                      example: true
                          x-linode-cli-display: 5
                        updated:
                          type: string
                          description: |
                            __Read-only__ The date-time of the most recent VPC Subnet update.
                          example: '2023-09-11T00:00:00'
                          nullable: true
                          readOnly: true
                          format: date-time
                          x-linode-filterable: true
                  updated:
                    type: string
                    description: |
                      __Read-only__ The date-time of the most recent VPC update.
                    example: '2023-09-11T00:00:00'
                    nullable: true
                    readOnly: true
                    format: date-time
                    x-linode-filterable: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-vpc
      security:
      - personalAccessToken: []
      - oauth: []
      x-code-samples:
      - lang: Shell
        source: |-
          curl https://api.linode.com/v4/vpcs/$vpcId \
            -H "Authorization: Bearer $TOKEN"
      - lang: CLI
        source: linode-cli vpcs view $vpcId
      x-linode-cli-action: view
      x-original-op-id: getVPC
      x-original-op-title: VPC View
    put:
      servers:
      - url: https://api.linode.com/v4
      operationId: put-vpc
      summary: Update a VPC
      tags:
      - VPCs
      description: |-
        Update an existing VPC.

        - The User accessing this operation must have `read_write` grants to the VPC.
        - A successful request triggers a `vpc_update` event.

        To update a VPC's Subnet, run the [Update a VPC subnet](https://techdocs.akamai.com/linode-api/reference/put-vpc-subnet) operation.


        ---


        - __CLI__.

            ```
            linode-cli vpcs update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            vpc:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli vpcs update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: vpc:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          VPC Update request object.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                A VPC Update request object.
              additionalProperties: false
              properties:
                description:
                  type: string
                  description: |
                    A written description to help distinguish the VPC.
                  example: '{{description}}'
                  default: ''
                  maxLength: 255
                  x-linode-cli-display: 3
                label:
                  type: string
                  description: |-
                    The VPC's label, for display purposes only.

                    - Needs to be unique among the Account's VPCs.
                    - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                  example: '{{label}}'
                  minLength: 1
                  maxLength: 64
                  x-linode-cli-display: 2
                  x-linode-filterable: true
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The updated VPC.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An object describing a VPC belonging to the Account.
                x-akamai:
                  file-path: schemas/vpc.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of the VPC.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  description:
                    type: string
                    description: |
                      A written description to help distinguish the VPC.
                    example: A description of my VPC.
                    default: ''
                    maxLength: 255
                    x-linode-cli-display: 3
                  created:
                    type: string
                    description: |
                      __Read-only__ The date-time of VPC creation.
                    example: '2023-07-11T00:00:00'
                    readOnly: true
                    format: date-time
                    x-linode-filterable: true
                  label:
                    type: string
                    description: |-
                      The VPC's label, for display purposes only.

                      - Needs to be unique among the Account's VPCs.
                      - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                    example: cool-vpc
                    minLength: 1
                    maxLength: 64
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  region:
                    type: string
                    description: |
                      The Region for the VPC.
                    example: us-east
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  subnets:
                    type: array
                    description: |
                      A list of subnets associated with the VPC.
                    items:
                      type: object
                      description: |
                        An object describing a VPC Subnet.
                      x-akamai:
                        file-path: schemas/vpc-subnet.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ The unique ID of the VPC Subnet.
                          example: 456
                          readOnly: true
                          x-linode-cli-display: 1
                          x-linode-filterable: true
                        created:
                          type: string
                          description: |
                            __Read-only__ The date-time of VPC Subnet creation.
                          example: '2023-07-11T00:00:00'
                          readOnly: true
                          format: date-time
                          x-linode-filterable: true
                        ipv4:
                          type: string
                          description: |-
                            IPv4 range in CIDR canonical form.

                            - The range must belong to a private address space as defined in [RFC1918](https://datatracker.ietf.org/doc/html/rfc1918).
                            - Allowed prefix lengths: 1-29.
                            - The range must not overlap with 192.168.128.0/17.
                            - The range must not overlap with other Subnets on the same VPC.
                          example: 10.0.1.0/24
                          format: ip
                          x-linode-cli-display: 3
                        label:
                          type: string
                          description: |-
                            The VPC Subnet's label, for display purposes only.

                            - Must be unique among the VPC's Subnets.
                            - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                          example: cool-vpc-subnet
                          minLength: 1
                          maxLength: 64
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        linodes:
                          type: array
                          description: |-
                            __Read-only__ An array of Linode IDs assigned to the VPC Subnet.

                            A Linode is assigned to a VPC Subnet if it has a Configuration Profile with a `vpc` purpose interface with the subnet's `subnet_id`. Even if the Configuration Profile is not active, meaning the Linode does not have access to the Subnet, the Linode still appears in this array.
                          readOnly: true
                          items:
                            type: object
                            additionalProperties: false
                            properties:
                              id:
                                type: integer
                                description: |
                                  ID of a Linode assigned to the VPC Subnet.
                                example: 111
                              interfaces:
                                type: array
                                description: |
                                  An array of Interfaces assigned to the Linode.
                                items:
                                  type: object
                                  additionalProperties: false
                                  properties:
                                    id:
                                      type: integer
                                      description: |
                                        ID of the interface
                                      example: 421
                                    active:
                                      type: boolean
                                      description: |
                                        Returns `true` if the Interface is in use, meaning
                                        that the Linode was powered on using the Configuration
                                        Profile to which the Interface belongs. Otherwise
                                        returns `false`.
                                      example: true
                          x-linode-cli-display: 5
                        updated:
                          type: string
                          description: |
                            __Read-only__ The date-time of the most recent VPC Subnet update.
                          example: '2023-09-11T00:00:00'
                          nullable: true
                          readOnly: true
                          format: date-time
                          x-linode-filterable: true
                  updated:
                    type: string
                    description: |
                      __Read-only__ The date-time of the most recent VPC update.
                    example: '2023-09-11T00:00:00'
                    nullable: true
                    readOnly: true
                    format: date-time
                    x-linode-filterable: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-vpc
      security:
      - personalAccessToken: []
      - oauth:
        - vpc:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl https://api.linode.com/v4/vpcs/$vpcId \
            -H "Authorization: Bearer $TOKEN" \
            -H "Content-Type: application/json" \
            -X PUT -d '{
              "description": "A description of my VPC.",
              "label": "cool-vpc"
            }'
      - lang: CLI
        source: |-
          linode-cli vpcs update $vpcId \
            --description "A description of my VPC."
            --label cool-vpc
      x-linode-cli-action: update
      x-linode-grant: read_write
      x-original-op-id: updateVPC
      x-original-op-title: VPC Update
    delete:
      servers:
      - url: https://api.linode.com/v4
      operationId: delete-vpc
      summary: Delete a VPC
      tags:
      - VPCs
      description: |-
        Delete a single VPC and all of its Subnets.

        - The User accessing this operation must have `read_write` grants to the VPC.
        - A successful request triggers a `vpc_delete` event and `subnet_delete` events for each deleted VPC Subnet.
        - All of the VPC's Subnets must be eligible for deletion. Accordingly, all Configuration Profile Interfaces that each Subnet is assigned to must first be deleted. If those Interfaces are active, the associated Linodes must first be shut down before they can be removed. If any Subnet cannot be deleted, then neither the VPC nor any of its Subnets are deleted.


        ---


        - __CLI__.

            ```
            linode-cli vpcs delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            vpc:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli vpcs delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: vpc:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Delete request successful.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-vpc
      security:
      - personalAccessToken: []
      - oauth:
        - vpc:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl https://api.linode.com/v4/vpcs/$vpcId \
            -H "Authorization: Bearer $TOKEN" \
            -X DELETE
      - lang: CLI
        source: linode-cli vpcs delete $vpcId
      x-linode-cli-action: delete
      x-linode-grant: read_write
      x-original-op-id: deleteVPC
      x-original-op-title: VPC Delete
    x-linode-cli-command: vpcs
  /vpcs/{vpcId}/ips:
    x-akamai:
      file-path: paths/vpc-ips.yaml
      path-info: /vpcs/{vpcId}/ips
    parameters:
    - name: vpcId
      in: path
      description: |
        The `id` of the VPC.
      example: '{{vpcId}}'
      x-akamai:
        file-path: parameters/vpc-id.yaml
      required: true
      schema:
        type: integer
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-vpc-ips
      summary: List a VPC's IP addresses
      tags:
      - IP addresses
      description: |-
        Returns a paginated list of IP addresses for a single VPC.


        ---


        - __OAuth__.

            ```
            ips:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: ips:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The IP addresses for the requested VPC.
          content:
            application/json:
              schema:
                description: |
                  The response data for the VPC IP Addresses List and View operations.
                x-akamai:
                  file-path: schemas/ip-addresses-vpc.yaml
                allOf:
                - type: object
                  description: |
                    An envelope for paginated response. When accessing a collection through a GET endpoint,
                    the results are wrapped in this envelope which includes metadata
                    about those results. Results are presented within a `data` array.
                    See [Pagination](https://techdocs.akamai.com/linode-api/reference/pagination)
                    for more information.
                  x-akamai:
                    file-path: schemas/pagination-envelope.yaml
                  additionalProperties: false
                  properties:
                    page:
                      type: integer
                      description: |
                        __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    pages:
                      type: integer
                      description: |
                        __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    results:
                      type: integer
                      description: |
                        __Read-only__ The total number of results.
                      example: 1
                      readOnly: true
                - type: object
                  description: |
                    A VPC IP address that exists in Linode's system, specific to the response for the [List
                    VPC IP addresses](https://techdocs.akamai.com/linode-api/reference/get-vpcs-ips)
                    operation. Returned as an empty set for Linodes that are not part
                    of a VPC.
                  properties:
                    active:
                      type: boolean
                      description: |
                        __Read-only__ Returns `true` if the VPC interface is in use, meaning that the Linode
                        was powered on using the `config_id` to which the interface
                        belongs. Otherwise returns `false`.
                      example: true
                      readOnly: true
                      x-linode-filterable: true
                    address:
                      type: string
                      description: |
                        __Read-only__ An IPv4 address configured for this VPC interface. These follow
                        the [RFC 1918](https://datatracker.ietf.org/doc/html/rfc1918)
                        private address format. Displayed as `null` if an `address_range`.
                      example: 192.0.2.141
                      nullable: true
                      readOnly: true
                      format: ip
                      x-linode-cli-display: 1
                    address_range:
                      type: string
                      description: |
                        __Read-only__ A range of IPv4 addresses configured for this VPC interface. Displayed
                        as `null` if a single `address`.
                      nullable: true
                      readOnly: true
                    config_id:
                      type: integer
                      description: |
                        __Read-only__ The globally general entity identifier for the Linode configuration
                        profile where the VPC is included.
                      example: 4567
                      readOnly: true
                      x-linode-filterable: true
                    gateway:
                      type: string
                      description: |
                        __Read-only__ The default gateway for the VPC subnet that the IP or IP range
                        belongs to.
                      example: 192.0.2.1
                      nullable: true
                      readOnly: true
                      format: ip
                    interface_id:
                      type: integer
                      description: |
                        __Read-only__ The globally general API entity identifier for the Linode interface.
                      example: 2435
                      readOnly: true
                    linode_id:
                      type: integer
                      description: |
                        __Read-only__ The identifier for the Linode the VPC interface currently belongs
                        to.
                      example: 123
                      readOnly: true
                      x-linode-cli-display: 6
                      x-linode-filterable: true
                    nat_1_1:
                      type: string
                      description: |
                        __Read-only__ The public IP address used for NAT 1:1 with the VPC. This is empty
                        if NAT 1:1 isn't used.
                      example: 192.168.0.42
                      readOnly: true
                      format: ip
                    prefix:
                      type: integer
                      description: |
                        __Read-only__ The number of bits set in the `subnet_mask`.
                      example: 24
                      nullable: true
                      readOnly: true
                    region:
                      type: string
                      description: |
                        __Read-only__ The region of the VPC.
                      example: us-east
                      readOnly: true
                      x-linode-cli-display: 5
                      x-linode-filterable: true
                    subnet_id:
                      type: integer
                      description: |
                        The `id` of the VPC Subnet for this interface.
                      example: 101
                      nullable: false
                    subnet_mask:
                      type: string
                      description: |
                        __Read-only__ The mask that separates host bits from network bits for the `address`
                        or `address_range`.
                      example: 255.255.255.0
                      readOnly: true
                      format: ip
                    vpc_id:
                      type: integer
                      description: |
                        __Read-only__ The unique globally general API entity identifier for the VPC.
                      example: 7654
                      readOnly: true
                      x-linode-filterable: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-vpc-ips
      security:
      - personalAccessToken: []
      - oauth:
        - ips:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/vpcs/123/ips
      - lang: CLI
        source: linode-cli vpcs ip-list 123
      x-linode-cli-action:
      - ip-list
      - ip-ls
      x-original-op-id: getVPCIPs
      x-original-op-title: VPC IP Addresses View
  /vpcs/{vpcId}/subnets:
    x-akamai:
      file-path: paths/subnets.yaml
      path-info: /vpcs/{vpcId}/subnets
    parameters:
    - name: vpcId
      in: path
      description: |
        The `id` of the VPC.
      example: '{{vpcId}}'
      x-akamai:
        file-path: parameters/vpc-id.yaml
      required: true
      schema:
        type: integer
    post:
      servers:
      - url: https://api.linode.com/v4
      operationId: post-vpc-subnet
      summary: Create a VPC subnet
      tags:
      - VPC subnets
      description: |-
        Create a VPC Subnet.

        - The User accessing this operation must have `read_write` grants to the VPC.
        - A successful request triggers a `subnet_create` event.

        Once a VPC Subnet is created, it can be attached to a Linode by assigning the Subnet to one of the Linode's Configuration Profile Interfaces. This step can be accomplished with the following operations:

        - [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
        - [Create a config profile](https://techdocs.akamai.com/linode-api/reference/post-add-linode-config)
        - [Update a config profile](https://techdocs.akamai.com/linode-api/reference/put-linode-config)
        - [Add a configuration profile interface](https://techdocs.akamai.com/linode-api/reference/post-linode-config-interface)


        ---


        - __CLI__.

            ```
            linode-cli vpcs subnet-create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            vpc:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli vpcs subnet-create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: vpc:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          VPC Subnet Create request object.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                VPC Subnet Create request object
              additionalProperties: false
              required:
              - ipv4
              - label
              properties:
                ipv4:
                  type: string
                  description: |-
                    IPv4 range in CIDR canonical form.

                    - The range must belong to a private address space as defined in [RFC1918](https://datatracker.ietf.org/doc/html/rfc1918).
                    - Allowed prefix lengths: 1-29.
                    - The range must not overlap with 192.168.128.0/17.
                    - The range must not overlap with other Subnets on the same VPC.
                  example: '{{ipv4}}'
                  format: ip
                  x-linode-cli-display: 3
                label:
                  type: string
                  description: |-
                    The VPC Subnet's label, for display purposes only.

                    - Must be unique among the VPC's Subnets.
                    - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                  example: '{{label}}'
                  minLength: 1
                  maxLength: 64
                  x-linode-cli-display: 2
                  x-linode-filterable: true
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The new VPC Subnet.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An object describing a VPC Subnet.
                x-akamai:
                  file-path: schemas/vpc-subnet.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of the VPC Subnet.
                    example: 456
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      __Read-only__ The date-time of VPC Subnet creation.
                    example: '2023-07-11T00:00:00'
                    readOnly: true
                    format: date-time
                    x-linode-filterable: true
                  ipv4:
                    type: string
                    description: |-
                      IPv4 range in CIDR canonical form.

                      - The range must belong to a private address space as defined in [RFC1918](https://datatracker.ietf.org/doc/html/rfc1918).
                      - Allowed prefix lengths: 1-29.
                      - The range must not overlap with 192.168.128.0/17.
                      - The range must not overlap with other Subnets on the same VPC.
                    example: 10.0.1.0/24
                    format: ip
                    x-linode-cli-display: 3
                  label:
                    type: string
                    description: |-
                      The VPC Subnet's label, for display purposes only.

                      - Must be unique among the VPC's Subnets.
                      - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                    example: cool-vpc-subnet
                    minLength: 1
                    maxLength: 64
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  linodes:
                    type: array
                    description: |-
                      __Read-only__ An array of Linode IDs assigned to the VPC Subnet.

                      A Linode is assigned to a VPC Subnet if it has a Configuration Profile with a `vpc` purpose interface with the subnet's `subnet_id`. Even if the Configuration Profile is not active, meaning the Linode does not have access to the Subnet, the Linode still appears in this array.
                    readOnly: true
                    items:
                      type: object
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            ID of a Linode assigned to the VPC Subnet.
                          example: 111
                        interfaces:
                          type: array
                          description: |
                            An array of Interfaces assigned to the Linode.
                          items:
                            type: object
                            additionalProperties: false
                            properties:
                              id:
                                type: integer
                                description: |
                                  ID of the interface
                                example: 421
                              active:
                                type: boolean
                                description: |
                                  Returns `true` if the Interface is in use, meaning that
                                  the Linode was powered on using the Configuration
                                  Profile to which the Interface belongs. Otherwise
                                  returns `false`.
                                example: true
                    x-linode-cli-display: 5
                  updated:
                    type: string
                    description: |
                      __Read-only__ The date-time of the most recent VPC Subnet update.
                    example: '2023-09-11T00:00:00'
                    nullable: true
                    readOnly: true
                    format: date-time
                    x-linode-filterable: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-vpc-subnet
      security:
      - personalAccessToken: []
      - oauth:
        - vpc:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl https://api.linode.com/v4/vpcs/$vpcId/subnets \
            -H "Authorization: Bearer $TOKEN" \
            -H "Content-Type: application/json" \
            -X POST -d '{
              "label": "cool-vpc-subnet",
              "ipv4": "10.0.1.0/24",
            }'
      - lang: CLI
        source: |-
          linode-cli vpcs subnet-create $vpcId \
            --label cool-vpc-subnet \
            --ipv4 10.0.1.0/24
      x-linode-cli-action: subnet-create
      x-linode-grant: read_write
      x-original-op-id: createVPCSubnet
      x-original-op-title: VPC Subnet Create
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-vpc-subnets
      summary: List VPC subnets
      tags:
      - VPC subnets
      description: |
        Get information about all VPC Subnets associated with a VPC.
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of VPC Subnet objects.
          content:
            application/json:
              schema:
                allOf:
                - type: object
                  description: |
                    An envelope for paginated response. When accessing a collection through a GET endpoint,
                    the results are wrapped in this envelope which includes metadata
                    about those results. Results are presented within a `data` array.
                    See [Pagination](https://techdocs.akamai.com/linode-api/reference/pagination)
                    for more information.
                  x-akamai:
                    file-path: schemas/pagination-envelope.yaml
                  additionalProperties: false
                  properties:
                    page:
                      type: integer
                      description: |
                        __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    pages:
                      type: integer
                      description: |
                        __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    results:
                      type: integer
                      description: |
                        __Read-only__ The total number of results.
                      example: 1
                      readOnly: true
                - properties:
                    data:
                      type: array
                      items:
                        type: object
                        description: |
                          An object describing a VPC Subnet.
                        x-akamai:
                          file-path: schemas/vpc-subnet.yaml
                        additionalProperties: false
                        properties:
                          id:
                            type: integer
                            description: |
                              __Read-only__ The unique ID of the VPC Subnet.
                            example: 456
                            readOnly: true
                            x-linode-cli-display: 1
                            x-linode-filterable: true
                          created:
                            type: string
                            description: |
                              __Read-only__ The date-time of VPC Subnet creation.
                            example: '2023-07-11T00:00:00'
                            readOnly: true
                            format: date-time
                            x-linode-filterable: true
                          ipv4:
                            type: string
                            description: |-
                              IPv4 range in CIDR canonical form.

                              - The range must belong to a private address space as defined in [RFC1918](https://datatracker.ietf.org/doc/html/rfc1918).
                              - Allowed prefix lengths: 1-29.
                              - The range must not overlap with 192.168.128.0/17.
                              - The range must not overlap with other Subnets on the same VPC.
                            example: 10.0.1.0/24
                            format: ip
                            x-linode-cli-display: 3
                          label:
                            type: string
                            description: |-
                              The VPC Subnet's label, for display purposes only.

                              - Must be unique among the VPC's Subnets.
                              - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                            example: cool-vpc-subnet
                            minLength: 1
                            maxLength: 64
                            x-linode-cli-display: 2
                            x-linode-filterable: true
                          linodes:
                            type: array
                            description: |-
                              __Read-only__ An array of Linode IDs assigned to the VPC Subnet.

                              A Linode is assigned to a VPC Subnet if it has a Configuration Profile with a `vpc` purpose interface with the subnet's `subnet_id`. Even if the Configuration Profile is not active, meaning the Linode does not have access to the Subnet, the Linode still appears in this array.
                            readOnly: true
                            items:
                              type: object
                              additionalProperties: false
                              properties:
                                id:
                                  type: integer
                                  description: |
                                    ID of a Linode assigned to the VPC Subnet.
                                  example: 111
                                interfaces:
                                  type: array
                                  description: |
                                    An array of Interfaces assigned to the Linode.
                                  items:
                                    type: object
                                    additionalProperties: false
                                    properties:
                                      id:
                                        type: integer
                                        description: |
                                          ID of the interface
                                        example: 421
                                      active:
                                        type: boolean
                                        description: |
                                          Returns `true` if the Interface is in use, meaning
                                          that the Linode was powered on using the
                                          Configuration Profile to which the Interface
                                          belongs. Otherwise returns `false`.
                                        example: true
                            x-linode-cli-display: 5
                          updated:
                            type: string
                            description: |
                              __Read-only__ The date-time of the most recent VPC Subnet update.
                            example: '2023-09-11T00:00:00'
                            nullable: true
                            readOnly: true
                            format: date-time
                            x-linode-filterable: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-vpc-subnets
      security:
      - personalAccessToken: []
      - oauth: []
      x-code-samples:
      - lang: Shell
        source: |-
          curl https://api.linode.com/v4/vpcs/$vpcId/subnets \
            -H "Authorization: Bearer $TOKEN"
      - lang: CLI
        source: linode-cli vpcs subnets-list $vpcId
      x-linode-cli-action:
      - subnets-list
      - subnets-ls
      x-original-op-id: getVPCSubnets
      x-original-op-title: VPC Subnets List
    x-linode-cli-command: vpcs
  /vpcs/{vpcId}/subnets/{vpcSubnetId}:
    x-akamai:
      file-path: paths/vpc-subnet.yaml
      path-info: /vpcs/{vpcId}/subnets/{vpcSubnetId}
    parameters:
    - name: vpcId
      in: path
      description: |
        The `id` of the VPC.
      example: '{{vpcId}}'
      x-akamai:
        file-path: parameters/vpc-id.yaml
      required: true
      schema:
        type: integer
    - name: vpcSubnetId
      in: path
      description: |
        The `id` of the VPC Subnet.
      example: '{{vpcSubnetId}}'
      x-akamai:
        file-path: parameters/vpc-subnet-id.yaml
      required: true
      schema:
        type: integer
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-vpc-subnet
      summary: Get a VPC subnet
      tags:
      - VPC subnets
      description: |
        Get information about a single VPC Subnet.
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A VPC Subnet object.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An object describing a VPC Subnet.
                x-akamai:
                  file-path: schemas/vpc-subnet.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of the VPC Subnet.
                    example: 456
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      __Read-only__ The date-time of VPC Subnet creation.
                    example: '2023-07-11T00:00:00'
                    readOnly: true
                    format: date-time
                    x-linode-filterable: true
                  ipv4:
                    type: string
                    description: |-
                      IPv4 range in CIDR canonical form.

                      - The range must belong to a private address space as defined in [RFC1918](https://datatracker.ietf.org/doc/html/rfc1918).
                      - Allowed prefix lengths: 1-29.
                      - The range must not overlap with 192.168.128.0/17.
                      - The range must not overlap with other Subnets on the same VPC.
                    example: 10.0.1.0/24
                    format: ip
                    x-linode-cli-display: 3
                  label:
                    type: string
                    description: |-
                      The VPC Subnet's label, for display purposes only.

                      - Must be unique among the VPC's Subnets.
                      - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                    example: cool-vpc-subnet
                    minLength: 1
                    maxLength: 64
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  linodes:
                    type: array
                    description: |-
                      __Read-only__ An array of Linode IDs assigned to the VPC Subnet.

                      A Linode is assigned to a VPC Subnet if it has a Configuration Profile with a `vpc` purpose interface with the subnet's `subnet_id`. Even if the Configuration Profile is not active, meaning the Linode does not have access to the Subnet, the Linode still appears in this array.
                    readOnly: true
                    items:
                      type: object
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            ID of a Linode assigned to the VPC Subnet.
                          example: 111
                        interfaces:
                          type: array
                          description: |
                            An array of Interfaces assigned to the Linode.
                          items:
                            type: object
                            additionalProperties: false
                            properties:
                              id:
                                type: integer
                                description: |
                                  ID of the interface
                                example: 421
                              active:
                                type: boolean
                                description: |
                                  Returns `true` if the Interface is in use, meaning that
                                  the Linode was powered on using the Configuration
                                  Profile to which the Interface belongs. Otherwise
                                  returns `false`.
                                example: true
                    x-linode-cli-display: 5
                  updated:
                    type: string
                    description: |
                      __Read-only__ The date-time of the most recent VPC Subnet update.
                    example: '2023-09-11T00:00:00'
                    nullable: true
                    readOnly: true
                    format: date-time
                    x-linode-filterable: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-vpc-subnet
      security:
      - personalAccessToken: []
      - oauth: []
      x-code-samples:
      - lang: Shell
        source: |-
          curl https://api.linode.com/v4/vpcs/$vpcId/subnets/$vpcSubnetId \
            -H "Authorization: Bearer $TOKEN"
      - lang: CLI
        source: 'linode-cli vpcs subnet-view $vpcId #vpcSubnetId'
      x-linode-cli-action: subnet-view
      x-original-op-id: getVPCSubnet
      x-original-op-title: VPC Subnet View
    put:
      servers:
      - url: https://api.linode.com/v4
      operationId: put-vpc-subnet
      summary: Update a VPC subnet
      tags:
      - VPC subnets
      description: |-
        Update a VPC Subnet.

        - The User accessing this operation must have `read_write` grants to the VPC.
        - A successful request triggers a `subnet_update` event.


        ---


        - __CLI__.

            ```
            linode-cli vpcs subnet-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            vpc:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli vpcs subnet-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: vpc:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          VPC Update request object.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                A VPC Subnet Update request object.
              additionalProperties: false
              properties:
                label:
                  type: string
                  description: |-
                    The VPC Subnet's label, for display purposes only.

                    - Must be unique among the VPC's Subnets.
                    - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                  example: '{{label}}'
                  minLength: 1
                  maxLength: 64
                  x-linode-cli-display: 2
                  x-linode-filterable: true
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The updated VPC Subnet.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An object describing a VPC Subnet.
                x-akamai:
                  file-path: schemas/vpc-subnet.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of the VPC Subnet.
                    example: 456
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      __Read-only__ The date-time of VPC Subnet creation.
                    example: '2023-07-11T00:00:00'
                    readOnly: true
                    format: date-time
                    x-linode-filterable: true
                  ipv4:
                    type: string
                    description: |-
                      IPv4 range in CIDR canonical form.

                      - The range must belong to a private address space as defined in [RFC1918](https://datatracker.ietf.org/doc/html/rfc1918).
                      - Allowed prefix lengths: 1-29.
                      - The range must not overlap with 192.168.128.0/17.
                      - The range must not overlap with other Subnets on the same VPC.
                    example: 10.0.1.0/24
                    format: ip
                    x-linode-cli-display: 3
                  label:
                    type: string
                    description: |-
                      The VPC Subnet's label, for display purposes only.

                      - Must be unique among the VPC's Subnets.
                      - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                    example: cool-vpc-subnet
                    minLength: 1
                    maxLength: 64
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  linodes:
                    type: array
                    description: |-
                      __Read-only__ An array of Linode IDs assigned to the VPC Subnet.

                      A Linode is assigned to a VPC Subnet if it has a Configuration Profile with a `vpc` purpose interface with the subnet's `subnet_id`. Even if the Configuration Profile is not active, meaning the Linode does not have access to the Subnet, the Linode still appears in this array.
                    readOnly: true
                    items:
                      type: object
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            ID of a Linode assigned to the VPC Subnet.
                          example: 111
                        interfaces:
                          type: array
                          description: |
                            An array of Interfaces assigned to the Linode.
                          items:
                            type: object
                            additionalProperties: false
                            properties:
                              id:
                                type: integer
                                description: |
                                  ID of the interface
                                example: 421
                              active:
                                type: boolean
                                description: |
                                  Returns `true` if the Interface is in use, meaning that
                                  the Linode was powered on using the Configuration
                                  Profile to which the Interface belongs. Otherwise
                                  returns `false`.
                                example: true
                    x-linode-cli-display: 5
                  updated:
                    type: string
                    description: |
                      __Read-only__ The date-time of the most recent VPC Subnet update.
                    example: '2023-09-11T00:00:00'
                    nullable: true
                    readOnly: true
                    format: date-time
                    x-linode-filterable: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-vpc-subnet
      security:
      - personalAccessToken: []
      - oauth:
        - vpc:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl https://api.linode.com/v4/vpcs/$vpcId/subnets/$vpcSubnetId \
            -H "Authorization: Bearer $TOKEN" \
            -H "Content-Type: application/json" \
            -X PUT -d '{
              "label": "cool-vpc-subnet"
            }'
      - lang: CLI
        source: |-
          linode-cli vpcs subnet-update $vpcId \
            --label cool-vpc-subnet
      x-linode-cli-action: subnet-update
      x-linode-grant: read_write
      x-original-op-id: updateVPCSubnet
      x-original-op-title: VPC Subnet Update
    delete:
      servers:
      - url: https://api.linode.com/v4
      operationId: delete-vpc-subnet
      summary: Delete a VPC subnet
      tags:
      - VPC subnets
      description: |-
        Delete a single VPC Subnet.

        The user accessing this operation must have `read_write` grants to the VPC. A successful request triggers a `subnet_delete` event.

        __Note__. You need to delete all the Configuration Profile Interfaces that this Subnet is assigned to before you can delete it. If those Interfaces are active, the associated Linode needs to be shut down before they can be removed.


        ---


        - __CLI__.

            ```
            linode-cli vpcs subnet-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            vpc:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli vpcs subnet-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: vpc:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Delete request successful.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-vpc-subnet
      security:
      - personalAccessToken: []
      - oauth:
        - vpc:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl https://api.linode.com/v4/vpcs/$vpcId/subnets/$vpcSubnetId \
            -H "Authorization: Bearer $TOKEN" \
            -X DELETE
      - lang: CLI
        source: linode-cli vpcs subnet-delete $vpcId $vpcSubnetId
      x-linode-cli-action: subnet-delete
      x-linode-grant: read_write
      x-original-op-id: deleteVPCSubnet
      x-original-op-title: VPC Subnet Delete
    x-linode-cli-command: vpcs
  /account:
    x-akamai:
      file-path: paths/account.yaml
      path-info: /account
    get:
      operationId: get-account
      summary: Get your account
      tags:
      - Account
      description: |-
        Returns the contact and billing information related to your Account.


        ---


        - __CLI__.

            ```
            linode-cli account view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli account view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a single Account object.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Account object
                x-akamai:
                  file-path: schemas/account.yaml
                additionalProperties: false
                properties:
                  active_promotions:
                    type: array
                    readOnly: true
                    items:
                      type: object
                      description: |-
                        __Read-only__ Promotions generally offer a set amount of credit that can be used toward your Linode services, and the promotion expires after a specified date. As well, a monthly cap on the promotional offer is set.

                        Simply put, a promotion offers a certain amount of credit  month, until either the expiration date is passed, or until the total promotional credit is used, whichever comes first.
                      x-akamai:
                        file-path: schemas/promotion.yaml
                      additionalProperties: false
                      readOnly: true
                      properties:
                        summary:
                          type: string
                          description: |
                            Short details of this promotion.
                          example: $10 off your Linode a month!
                          x-linode-cli-display: 10
                        description:
                          type: string
                          description: |
                            A detailed description of this promotion.
                          example: Receive up to $10 off your services every month for 6 months! Unused credits
                            will expire once this promotion period ends.
                        credit_monthly_cap:
                          type: string
                          description: |
                            The amount available to spend per month.
                          example: '10.00'
                          x-linode-cli-display: 5
                        credit_remaining:
                          type: string
                          description: |
                            The total amount of credit left for this promotion.
                          example: '50.00'
                          x-linode-cli-display: 3
                        expire_dt:
                          type: string
                          description: |
                            When this promotion's credits expire.
                          example: '2018-01-31T23:59:59'
                          x-linode-cli-display: 2
                        image_url:
                          type: string
                          description: |
                            The location of an image for this promotion.
                          example: https://linode.com/10_a_month_promotion.svg
                        service_type:
                          type: string
                          description: |
                            The service to which this promotion applies.
                          example: all
                          enum:
                          - all
                          - backup
                          - blockstorage
                          - db_mysql
                          - ip_v4
                          - linode
                          - linode_disk
                          - linode_memory
                          - loadbalancer
                          - longview
                          - managed
                          - nodebalancer
                          - placement_group
                          - objectstorage
                          - transfer_tx
                          x-linode-cli-display: 1
                        this_month_credit_remaining:
                          type: string
                          description: |
                            The amount of credit left for this month for this promotion.
                          example: '10.00'
                          x-linode-cli-display: 4
                  active_since:
                    type: string
                    description: |
                      __Read-only__ The date and time the account was activated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  address_1:
                    type: string
                    description: |
                      First line of this Account's billing address.
                    example: 123 Main Street
                    maxLength: 64
                  address_2:
                    type: string
                    description: |
                      Second line of this Account's billing address.
                    example: Suite A
                    maxLength: 64
                  balance:
                    type: number
                    description: |
                      __Read-only__ This Account's balance, in US dollars.
                    example: 200
                    readOnly: true
                    x-linode-cli-display: 4
                  balance_uninvoiced:
                    type: number
                    description: |
                      __Read-only__ This Account's current estimated invoice in US dollars. This is not
                      your final invoice balance. Transfer charges are not included
                      in the estimate.
                    example: 145
                    readOnly: true
                    x-linode-cli-display: 4
                  billing_source:
                    type: string
                    description: |
                      __Read-only__ The source of service charges for this Account, as determined by its relationship
                      with Akamai. Accounts that are associated with Akamai-specific
                      customers return a value of `akamai`. All other Accounts return
                      a value of `linode`.
                    example: akamai
                    readOnly: true
                    enum:
                    - akamai
                    - linode
                  capabilities:
                    type: array
                    description: |
                      __Read-only__ A list of capabilities your account supports.
                    example:
                    - Linodes
                    - NodeBalancers
                    - Block Storage
                    - Object Storage
                    - Placement Groups
                    readOnly: true
                    items:
                      type: string
                  city:
                    type: string
                    description: |
                      The city for this Account's billing address.
                    example: Philadelphia
                    maxLength: 24
                  company:
                    type: string
                    description: |-
                      The company name associated with this Account.

                      Must not include any of the following characters: `<` `>` `(` `)` `"` `=`
                    example: Linode LLC
                    maxLength: 128
                  country:
                    type: string
                    description: |
                      The two-letter ISO 3166 country code of this Account's billing address.
                    example: US
                  credit_card:
                    type: object
                    description: |
                      __Read-only__ Credit Card information associated with this Account.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      expiry:
                        type: string
                        description: |
                          The expiration month and year of the credit card.
                        example: 11/2022
                      last_four:
                        type: string
                        description: |
                          The last four digits of the credit card associated with this Account.
                        example: 1111
                  email:
                    type: string
                    description: |
                      The email address of the person associated with this Account.
                    example: john.smith@linode.com
                    maxLength: 128
                    x-linode-cli-display: 3
                  euuid:
                    type: string
                    description: |
                      __Read-only__ An external unique identifier for this account.
                    example: E1AF5EEC-526F-487D-B317EBEB34C87D71
                    readOnly: true
                    format: uuid
                  first_name:
                    type: string
                    description: |-
                      The first name of the person associated with this Account.

                      Must not include any of the following characters: `<` `>` `(` `)` `"` `=`
                    example: John
                    maxLength: 50
                    x-linode-cli-display: 1
                  last_name:
                    type: string
                    description: |-
                      The last name of the person associated with this Account.

                      Must not include any of the following characters: `<` `>` `(` `)` `"` `=`
                    example: Smith
                    maxLength: 50
                    x-linode-cli-display: 2
                  phone:
                    type: string
                    description: |
                      The phone number associated with this Account.
                    example: 215-555-1212
                    maxLength: 32
                  state:
                    type: string
                    description: |
                      If billing address is in the United States (US) or Canada (CA), only the two-letter
                      ISO 3166 State or Province code are accepted. If entering a
                      US military address, state abbreviations (AA, AE, AP) should
                      be entered. If the address is outside the US or CA, this is
                      the Province associated with the Account's billing address.
                    example: PA
                    maxLength: 24
                  tax_id:
                    type: string
                    description: |
                      The tax identification number associated with this Account, for tax calculations in
                      some countries. If you do not live in a country that collects
                      tax, this should be an empty string (`""`).
                    example: ATU99999999
                    maxLength: 25
                  zip:
                    type: string
                    description: |-
                      The zip code of this Account's billing address. The following restrictions apply:

                      - Can only contain ASCII letters, numbers, and hyphens (`-`).
                      - Must not contain more than 9 letter or number characters.
                    example: 19102-1234
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-account
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/account
      - lang: CLI
        source: linode-cli account view
      x-linode-cli-action: view
      x-linode-grant: read_only
      x-original-op-id: getAccount
      x-original-op-title: Account View
    put:
      operationId: put-account
      summary: Update your account
      tags:
      - Account
      description: |-
        Updates contact and billing information related to your account. If you exclude any properties from the request, the operation leaves them unchanged.

        __Note__. When updating an account's `country` to "US", you'll get an error if the account's `zip` is not a valid US zip code.

        __Parent and child accounts__

        In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, the following apply:

        - You can't change the `company` for a parent account. Akamai uses this value to set the name for a child account parent user (proxy user) on any child account.

        - Child account users can't run this operation. These users don't have access to billing-related operations.


        ---


        - __CLI__.

            ```
            linode-cli account update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli account update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Updated contact and billing information.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                Account object
              x-akamai:
                file-path: schemas/account.yaml
              additionalProperties: false
              properties:
                active_promotions:
                  type: array
                  readOnly: true
                  items:
                    type: object
                    description: |-
                      __Read-only__ Promotions generally offer a set amount of credit that can be used toward your Linode services, and the promotion expires after a specified date. As well, a monthly cap on the promotional offer is set.

                      Simply put, a promotion offers a certain amount of credit  month, until either the expiration date is passed, or until the total promotional credit is used, whichever comes first.
                    x-akamai:
                      file-path: schemas/promotion.yaml
                    additionalProperties: false
                    readOnly: true
                    properties:
                      summary:
                        type: string
                        description: |
                          Short details of this promotion.
                        example: $10 off your Linode a month!
                        x-linode-cli-display: 10
                      description:
                        type: string
                        description: |
                          A detailed description of this promotion.
                        example: Receive up to $10 off your services every month for 6 months! Unused credits will
                          expire once this promotion period ends.
                      credit_monthly_cap:
                        type: string
                        description: |
                          The amount available to spend per month.
                        example: '10.00'
                        x-linode-cli-display: 5
                      credit_remaining:
                        type: string
                        description: |
                          The total amount of credit left for this promotion.
                        example: '50.00'
                        x-linode-cli-display: 3
                      expire_dt:
                        type: string
                        description: |
                          When this promotion's credits expire.
                        example: '2018-01-31T23:59:59'
                        x-linode-cli-display: 2
                      image_url:
                        type: string
                        description: |
                          The location of an image for this promotion.
                        example: https://linode.com/10_a_month_promotion.svg
                      service_type:
                        type: string
                        description: |
                          The service to which this promotion applies.
                        example: all
                        enum:
                        - all
                        - backup
                        - blockstorage
                        - db_mysql
                        - ip_v4
                        - linode
                        - linode_disk
                        - linode_memory
                        - loadbalancer
                        - longview
                        - managed
                        - nodebalancer
                        - placement_group
                        - objectstorage
                        - transfer_tx
                        x-linode-cli-display: 1
                      this_month_credit_remaining:
                        type: string
                        description: |
                          The amount of credit left for this month for this promotion.
                        example: '10.00'
                        x-linode-cli-display: 4
                active_since:
                  type: string
                  description: |
                    __Read-only__ The date and time the account was activated.
                  example: '{{active_since}}'
                  readOnly: true
                  format: date-time
                address_1:
                  type: string
                  description: |
                    First line of this Account's billing address.
                  example: '{{address_1}}'
                  maxLength: 64
                address_2:
                  type: string
                  description: |
                    Second line of this Account's billing address.
                  example: '{{address_2}}'
                  maxLength: 64
                balance:
                  type: number
                  description: |
                    __Read-only__ This Account's balance, in US dollars.
                  example: '{{balance}}'
                  readOnly: true
                  x-linode-cli-display: 4
                balance_uninvoiced:
                  type: number
                  description: |
                    __Read-only__ This Account's current estimated invoice in US dollars. This is not your final
                    invoice balance. Transfer charges are not included in the estimate.
                  example: '{{balance_uninvoiced}}'
                  readOnly: true
                  x-linode-cli-display: 4
                billing_source:
                  type: string
                  description: |
                    __Read-only__ The source of service charges for this Account, as determined by its relationship
                    with Akamai. Accounts that are associated with Akamai-specific
                    customers return a value of `akamai`. All other Accounts return
                    a value of `linode`.
                  example: '{{billing_source}}'
                  readOnly: true
                  enum:
                  - akamai
                  - linode
                capabilities:
                  type: array
                  description: |
                    __Read-only__ A list of capabilities your account supports.
                  example:
                  - Linodes
                  - NodeBalancers
                  - Block Storage
                  - Object Storage
                  - Placement Groups
                  readOnly: true
                  items:
                    type: string
                city:
                  type: string
                  description: |
                    The city for this Account's billing address.
                  example: '{{city}}'
                  maxLength: 24
                company:
                  type: string
                  description: |-
                    The company name associated with this Account.

                    Must not include any of the following characters: `<` `>` `(` `)` `"` `=`
                  example: '{{company}}'
                  maxLength: 128
                country:
                  type: string
                  description: |
                    The two-letter ISO 3166 country code of this Account's billing address.
                  example: '{{country}}'
                credit_card:
                  type: object
                  description: |
                    __Read-only__ Credit Card information associated with this Account.
                  additionalProperties: false
                  readOnly: true
                  properties:
                    expiry:
                      type: string
                      description: |
                        The expiration month and year of the credit card.
                      example: 11/2022
                    last_four:
                      type: string
                      description: |
                        The last four digits of the credit card associated with this Account.
                      example: 1111
                email:
                  type: string
                  description: |
                    The email address of the person associated with this Account.
                  example: '{{email}}'
                  maxLength: 128
                  x-linode-cli-display: 3
                euuid:
                  type: string
                  description: |
                    __Read-only__ An external unique identifier for this account.
                  example: '{{euuid}}'
                  readOnly: true
                  format: uuid
                first_name:
                  type: string
                  description: |-
                    The first name of the person associated with this Account.

                    Must not include any of the following characters: `<` `>` `(` `)` `"` `=`
                  example: '{{first_name}}'
                  maxLength: 50
                  x-linode-cli-display: 1
                last_name:
                  type: string
                  description: |-
                    The last name of the person associated with this Account.

                    Must not include any of the following characters: `<` `>` `(` `)` `"` `=`
                  example: '{{last_name}}'
                  maxLength: 50
                  x-linode-cli-display: 2
                phone:
                  type: string
                  description: |
                    The phone number associated with this Account.
                  example: '{{phone}}'
                  maxLength: 32
                state:
                  type: string
                  description: |
                    If billing address is in the United States (US) or Canada (CA), only the two-letter
                    ISO 3166 State or Province code are accepted. If entering a US
                    military address, state abbreviations (AA, AE, AP) should be entered.
                    If the address is outside the US or CA, this is the Province associated
                    with the Account's billing address.
                  example: '{{state}}'
                  maxLength: 24
                tax_id:
                  type: string
                  description: |
                    The tax identification number associated with this Account, for tax calculations in some
                    countries. If you do not live in a country that collects tax,
                    this should be an empty string (`""`).
                  example: '{{tax_id}}'
                  maxLength: 25
                zip:
                  type: string
                  description: |-
                    The zip code of this Account's billing address. The following restrictions apply:

                    - Can only contain ASCII letters, numbers, and hyphens (`-`).
                    - Must not contain more than 9 letter or number characters.
                  example: '{{zip}}'
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The updated Account.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Account object
                x-akamai:
                  file-path: schemas/account.yaml
                additionalProperties: false
                properties:
                  active_promotions:
                    type: array
                    readOnly: true
                    items:
                      type: object
                      description: |-
                        __Read-only__ Promotions generally offer a set amount of credit that can be used toward your Linode services, and the promotion expires after a specified date. As well, a monthly cap on the promotional offer is set.

                        Simply put, a promotion offers a certain amount of credit  month, until either the expiration date is passed, or until the total promotional credit is used, whichever comes first.
                      x-akamai:
                        file-path: schemas/promotion.yaml
                      additionalProperties: false
                      readOnly: true
                      properties:
                        summary:
                          type: string
                          description: |
                            Short details of this promotion.
                          example: $10 off your Linode a month!
                          x-linode-cli-display: 10
                        description:
                          type: string
                          description: |
                            A detailed description of this promotion.
                          example: Receive up to $10 off your services every month for 6 months! Unused credits
                            will expire once this promotion period ends.
                        credit_monthly_cap:
                          type: string
                          description: |
                            The amount available to spend per month.
                          example: '10.00'
                          x-linode-cli-display: 5
                        credit_remaining:
                          type: string
                          description: |
                            The total amount of credit left for this promotion.
                          example: '50.00'
                          x-linode-cli-display: 3
                        expire_dt:
                          type: string
                          description: |
                            When this promotion's credits expire.
                          example: '2018-01-31T23:59:59'
                          x-linode-cli-display: 2
                        image_url:
                          type: string
                          description: |
                            The location of an image for this promotion.
                          example: https://linode.com/10_a_month_promotion.svg
                        service_type:
                          type: string
                          description: |
                            The service to which this promotion applies.
                          example: all
                          enum:
                          - all
                          - backup
                          - blockstorage
                          - db_mysql
                          - ip_v4
                          - linode
                          - linode_disk
                          - linode_memory
                          - loadbalancer
                          - longview
                          - managed
                          - nodebalancer
                          - placement_group
                          - objectstorage
                          - transfer_tx
                          x-linode-cli-display: 1
                        this_month_credit_remaining:
                          type: string
                          description: |
                            The amount of credit left for this month for this promotion.
                          example: '10.00'
                          x-linode-cli-display: 4
                  active_since:
                    type: string
                    description: |
                      __Read-only__ The date and time the account was activated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  address_1:
                    type: string
                    description: |
                      First line of this Account's billing address.
                    example: 123 Main Street
                    maxLength: 64
                  address_2:
                    type: string
                    description: |
                      Second line of this Account's billing address.
                    example: Suite A
                    maxLength: 64
                  balance:
                    type: number
                    description: |
                      __Read-only__ This Account's balance, in US dollars.
                    example: 200
                    readOnly: true
                    x-linode-cli-display: 4
                  balance_uninvoiced:
                    type: number
                    description: |
                      __Read-only__ This Account's current estimated invoice in US dollars. This is not
                      your final invoice balance. Transfer charges are not included
                      in the estimate.
                    example: 145
                    readOnly: true
                    x-linode-cli-display: 4
                  billing_source:
                    type: string
                    description: |
                      __Read-only__ The source of service charges for this Account, as determined by its relationship
                      with Akamai. Accounts that are associated with Akamai-specific
                      customers return a value of `akamai`. All other Accounts return
                      a value of `linode`.
                    example: akamai
                    readOnly: true
                    enum:
                    - akamai
                    - linode
                  capabilities:
                    type: array
                    description: |
                      __Read-only__ A list of capabilities your account supports.
                    example:
                    - Linodes
                    - NodeBalancers
                    - Block Storage
                    - Object Storage
                    - Placement Groups
                    readOnly: true
                    items:
                      type: string
                  city:
                    type: string
                    description: |
                      The city for this Account's billing address.
                    example: Philadelphia
                    maxLength: 24
                  company:
                    type: string
                    description: |-
                      The company name associated with this Account.

                      Must not include any of the following characters: `<` `>` `(` `)` `"` `=`
                    example: Linode LLC
                    maxLength: 128
                  country:
                    type: string
                    description: |
                      The two-letter ISO 3166 country code of this Account's billing address.
                    example: US
                  credit_card:
                    type: object
                    description: |
                      __Read-only__ Credit Card information associated with this Account.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      expiry:
                        type: string
                        description: |
                          The expiration month and year of the credit card.
                        example: 11/2022
                      last_four:
                        type: string
                        description: |
                          The last four digits of the credit card associated with this Account.
                        example: 1111
                  email:
                    type: string
                    description: |
                      The email address of the person associated with this Account.
                    example: john.smith@linode.com
                    maxLength: 128
                    x-linode-cli-display: 3
                  euuid:
                    type: string
                    description: |
                      __Read-only__ An external unique identifier for this account.
                    example: E1AF5EEC-526F-487D-B317EBEB34C87D71
                    readOnly: true
                    format: uuid
                  first_name:
                    type: string
                    description: |-
                      The first name of the person associated with this Account.

                      Must not include any of the following characters: `<` `>` `(` `)` `"` `=`
                    example: John
                    maxLength: 50
                    x-linode-cli-display: 1
                  last_name:
                    type: string
                    description: |-
                      The last name of the person associated with this Account.

                      Must not include any of the following characters: `<` `>` `(` `)` `"` `=`
                    example: Smith
                    maxLength: 50
                    x-linode-cli-display: 2
                  phone:
                    type: string
                    description: |
                      The phone number associated with this Account.
                    example: 215-555-1212
                    maxLength: 32
                  state:
                    type: string
                    description: |
                      If billing address is in the United States (US) or Canada (CA), only the two-letter
                      ISO 3166 State or Province code are accepted. If entering a
                      US military address, state abbreviations (AA, AE, AP) should
                      be entered. If the address is outside the US or CA, this is
                      the Province associated with the Account's billing address.
                    example: PA
                    maxLength: 24
                  tax_id:
                    type: string
                    description: |
                      The tax identification number associated with this Account, for tax calculations in
                      some countries. If you do not live in a country that collects
                      tax, this should be an empty string (`""`).
                    example: ATU99999999
                    maxLength: 25
                  zip:
                    type: string
                    description: |-
                      The zip code of this Account's billing address. The following restrictions apply:

                      - Can only contain ASCII letters, numbers, and hyphens (`-`).
                      - Must not contain more than 9 letter or number characters.
                    example: 19102-1234
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-account
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "address_1": "123 Main St.",
                "address_2": "Suite 101",
                "city": "Philadelphia",
                "company": "My Company, LLC",
                "country": "US",
                "email": "jsmith@mycompany.com",
                "first_name": "John",
                "last_name": "Smith",
                "phone": "555-555-1212",
                "state": "PA",
                "tax_id": "ATU99999999",
                "zip": "19102"
              }' \
              https://api.linode.com/v4/account
      - lang: CLI
        source: |-
          linode-cli account update \
            --address_1 "123 Main St." \
            --address_2 "Suite 101" \
            --city Philadelphia \
            --company My Company \ LLC \
            --country US \
            --email jsmith@mycompany.com \
            --first_name John \
            --last_name Smith \
            --phone 555-555-1212 \
            --state PA \
            --tax_id ATU99999999 \
            --zip 19102
      x-linode-cli-action: update
      x-linode-grant: read_write
      x-original-op-id: updateAccount
      x-original-op-title: Account Update
    x-linode-cli-command: account
  /account/availability:
    x-akamai:
      file-path: paths/account-availability.yaml
      path-info: /account/availability
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-availability
      summary: List available services
      tags:
      - Account availability
      description: |-
        Returns a paginated list of the services available to you, for all Linode regions.

        __Note__. Only authorized Users can run this operation.


        ---


        - __CLI__.

            ```
            linode-cli account get-availability
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli account get-availability
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            List of regions and the services available in each.
          content:
            application/json:
              schema:
                allOf:
                - type: object
                  additionalProperties: false
                  properties:
                    data:
                      type: array
                      items:
                        type: object
                        description: |
                          Account Service Availability object
                        x-akamai:
                          file-path: schemas/account-availability.yaml
                        additionalProperties: false
                        properties:
                          available:
                            type: array
                            description: |
                              __Read-only__ A list of services _available_ to your account in the
                              `region`.
                            example:
                            - Linodes
                            - NodeBalancers
                            readOnly: true
                            items:
                              type: string
                          region:
                            type: string
                            description: |
                              __Read-only__ The Akamai cloud computing data center (region), represented
                              by a slug value. You can view a full list of regions
                              and their associated slugs with the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions)
                              operation.
                            example: us-east
                            readOnly: true
                            x-linode-cli-display: 1
                          unavailable:
                            type: array
                            description: |
                              __Read-only__ A list of services _unavailable_ to your account in
                              the `region`.
                            example:
                            - Kubernetes
                            - Block Storage
                            readOnly: true
                            items:
                              type: string
                            x-linode-cli-display: 3
                - type: object
                  description: |
                    An envelope for paginated response. When accessing a collection through a GET endpoint,
                    the results are wrapped in this envelope which includes metadata
                    about those results. Results are presented within a `data` array.
                    See [Pagination](https://techdocs.akamai.com/linode-api/reference/pagination)
                    for more information.
                  x-akamai:
                    file-path: schemas/pagination-envelope.yaml
                  additionalProperties: false
                  properties:
                    page:
                      type: integer
                      description: |
                        __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    pages:
                      type: integer
                      description: |
                        __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    results:
                      type: integer
                      description: |
                        __Read-only__ The total number of results.
                      example: 1
                      readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-availability
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl https://api.linode.com/v4/account/availability \
              -H "Authorization: Bearer $TOKEN"
      - lang: CLI
        source: linode-cli account get-availability
      x-linode-cli-action: get-availability
      x-linode-grant: read_only
      x-original-op-id: getAvailability
      x-original-op-title: Account Availability
    x-linode-cli-command: account
  /account/availability/{id}:
    x-akamai:
      file-path: paths/data-center.yaml
      path-info: /account/availability/{id}
    parameters:
    - name: id
      in: path
      description: |
        The slug for the applicable data center. Run the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions)
        operation to view the slug for each data center.
      example: '{{id}}'
      x-akamai:
        file-path: parameters/id-path.yaml
      required: true
      schema:
        type: string
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-account-availability
      summary: Get a region's service availability
      tags:
      - Account
      description: |-
        View the available services for your account in a specific region.

        __Note__. Only authorized users can access this.


        ---


        - __CLI__.

            ```
            linode-cli account get-account-availability
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli account get-account-availability
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The services available in the specified region.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Account Service Availability object
                x-akamai:
                  file-path: schemas/account-availability.yaml
                additionalProperties: false
                properties:
                  available:
                    type: array
                    description: |
                      __Read-only__ A list of services _available_ to your account in the `region`.
                    example:
                    - Linodes
                    - NodeBalancers
                    readOnly: true
                    items:
                      type: string
                  region:
                    type: string
                    description: |
                      __Read-only__ The Akamai cloud computing data center (region), represented by a slug
                      value. You can view a full list of regions and their associated
                      slugs with the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions)
                      operation.
                    example: us-east
                    readOnly: true
                    x-linode-cli-display: 1
                  unavailable:
                    type: array
                    description: |
                      __Read-only__ A list of services _unavailable_ to your account in the `region`.
                    example:
                    - Kubernetes
                    - Block Storage
                    readOnly: true
                    items:
                      type: string
                    x-linode-cli-display: 3
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-account-availability
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl https://api.linode.com/v4/account/availability/us-east \
              -H "Authorization: Bearer $TOKEN"
      - lang: CLI
        source: linode-cli account get-account-availability us-east
      x-linode-cli-action: get-account-availability
      x-linode-grant: read_only
      x-original-op-id: getAccountAvailability
      x-original-op-title: Region Service Availability
    x-linode-cli-command: account
  /account/betas:
    x-akamai:
      file-path: paths/account-betas.yaml
      path-info: /account/betas
    post:
      servers:
      - url: https://api.linode.com/v4
      operationId: post-beta-program
      summary: Enroll in a Beta program
      tags:
      - Beta programs
      description: |-
        Enroll your Account in an active Beta Program.

        Only unrestricted Users can access this operation.

        To view active Beta Programs, run the [List beta programs](https://techdocs.akamai.com/linode-api/reference/get-beta-programs) operation.

        Active Beta Programs may have a limited number of enrollments. If a Beta Program has reached is maximum number of enrollments, an error is returned even though the request is successful.

        Beta Programs with `"greenlight_only": true` can only be enrolled by Accounts that participate in the [Greenlight](https://www.linode.com/green-light/) program.


        ---


        - __CLI__.

            ```
            linode-cli betas enroll
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli betas enroll
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Updated information for the Managed MySQL Database.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                The Beta Program ID to enroll in for your Account.
              additionalProperties: false
              required:
              - id
              properties:
                id:
                  type: string
                  description: |
                    The unique identifier of the Beta Program.
                  example: '{{id}}'
                  x-linode-cli-display: 1
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Enrollment request successful.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-beta-program
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl https://api.linode.com/v4/account/betas \
              -H "Authorization: Bearer $TOKEN" \
              -H "Content-Type: application/json" \
              -X POST -d '{
                  "id": "example_open"
              }'
      - lang: CLI
        source: linode-cli betas enroll --id example_open
      x-linode-cli-action: enroll
      x-linode-grant: unrestricted only
      x-original-op-id: enrollBetaProgram
      x-original-op-title: Beta Program Enroll
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-enrolled-beta-programs
      summary: List enrolled Beta programs
      tags:
      - Beta programs
      description: |-
        Display all enrolled Beta Programs for your Account. Includes inactive as well as active Beta Programs.

        Only unrestricted Users can access this operation.


        ---


        - __CLI__.

            ```
            linode-cli betas enrolled
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli betas enrolled
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated list of all enrolled Beta Program objects for the Account.
          content:
            application/json:
              schema:
                allOf:
                - type: object
                  description: |
                    An envelope for paginated response. When accessing a collection through a GET endpoint,
                    the results are wrapped in this envelope which includes metadata
                    about those results. Results are presented within a `data` array.
                    See [Pagination](https://techdocs.akamai.com/linode-api/reference/pagination)
                    for more information.
                  x-akamai:
                    file-path: schemas/pagination-envelope.yaml
                  additionalProperties: false
                  properties:
                    page:
                      type: integer
                      description: |
                        __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    pages:
                      type: integer
                      description: |
                        __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    results:
                      type: integer
                      description: |
                        __Read-only__ The total number of results.
                      example: 1
                      readOnly: true
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        type: object
                        description: |
                          An object representing an enrolled Beta Program for the Account.
                        x-akamai:
                          file-path: schemas/beta-program-enrolled.yaml
                        additionalProperties: false
                        properties:
                          id:
                            type: string
                            description: |
                              The unique identifier of the Beta Program.
                            example: example_open
                            x-linode-cli-display: 1
                          description:
                            type: string
                            description: |
                              __Read-only__ Additional details regarding the Beta Program.
                            example: This is an open public beta for an example feature.
                            nullable: true
                            readOnly: true
                            x-linode-cli-display: 3
                          ended:
                            type: string
                            description: |-
                              __Read-only__ The date-time that the Beta Program ended.

                              `null` indicates that the Beta Program is ongoing.
                            example: null
                            nullable: true
                            readOnly: true
                            format: date-time
                            x-linode-cli-display: 5
                            x-linode-filterable: true
                          enrolled:
                            type: string
                            description: |
                              __Read-only__ The date-time of Account enrollment to the Beta Program.
                            example: '2023-09-11T00:00:00'
                            readOnly: true
                            format: date-time
                            x-linode-cli-display: 6
                            x-linode-filterable: true
                          label:
                            type: string
                            description: |
                              __Read-only__ The name of the Beta Program.
                            example: Example Open Beta
                            readOnly: true
                            x-linode-cli-display: 2
                            x-linode-filterable: true
                          started:
                            type: string
                            description: |
                              __Read-only__ The start date-time of the Beta Program.
                            example: '2023-07-11T00:00:00'
                            readOnly: true
                            format: date-time
                            x-linode-cli-display: 4
                            x-linode-filterable: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-enrolled-beta-programs
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl https://api.linode.com/v4/account/betas \
              -H "Authorization: Bearer $TOKEN"
      - lang: CLI
        source: linode-cli betas enrolled
      x-linode-cli-action: enrolled
      x-linode-grant: unrestricted only
      x-original-op-id: getEnrolledBetaPrograms
      x-original-op-title: Enrolled Beta Programs List
    x-linode-cli-command: betas
  /account/betas/{betaId}:
    x-akamai:
      file-path: paths/account-beta.yaml
      path-info: /account/betas/{betaId}
    parameters:
    - name: betaId
      in: path
      description: |
        The ID of the Beta Program.
      example: '{{betaId}}'
      x-akamai:
        file-path: parameters/beta-id.yaml
      required: true
      schema:
        type: string
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-enrolled-beta-program
      summary: Get an enrolled Beta program
      tags:
      - Beta programs
      description: |-
        Display an enrolled Beta Program for your Account. The Beta Program may be inactive.

        Only unrestricted Users can access this operation.


        ---


        - __CLI__.

            ```
            linode-cli betas enrolled-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli betas enrolled-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns an enrolled Beta Program object for the Account.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An object representing an enrolled Beta Program for the Account.
                x-akamai:
                  file-path: schemas/beta-program-enrolled.yaml
                additionalProperties: false
                properties:
                  id:
                    type: string
                    description: |
                      The unique identifier of the Beta Program.
                    example: example_open
                    x-linode-cli-display: 1
                  description:
                    type: string
                    description: |
                      __Read-only__ Additional details regarding the Beta Program.
                    example: This is an open public beta for an example feature.
                    nullable: true
                    readOnly: true
                    x-linode-cli-display: 3
                  ended:
                    type: string
                    description: |-
                      __Read-only__ The date-time that the Beta Program ended.

                      `null` indicates that the Beta Program is ongoing.
                    example: null
                    nullable: true
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 5
                    x-linode-filterable: true
                  enrolled:
                    type: string
                    description: |
                      __Read-only__ The date-time of Account enrollment to the Beta Program.
                    example: '2023-09-11T00:00:00'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 6
                    x-linode-filterable: true
                  label:
                    type: string
                    description: |
                      __Read-only__ The name of the Beta Program.
                    example: Example Open Beta
                    readOnly: true
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  started:
                    type: string
                    description: |
                      __Read-only__ The start date-time of the Beta Program.
                    example: '2023-07-11T00:00:00'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 4
                    x-linode-filterable: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-enrolled-beta-program
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl https://api.linode.com/v4/account/betas/$betaId \
              -H "Authorization: Bearer $TOKEN"
      - lang: CLI
        source: linode-cli betas enrolled-view $betaId
      x-linode-cli-action: enrolled-view
      x-linode-grant: unrestricted only
      x-original-op-id: getEnrolledBetaProgram
      x-original-op-title: Enrolled Beta Program View
    x-linode-cli-command: betas
  /account/cancel:
    x-akamai:
      file-path: paths/account-cancel.yaml
      path-info: /account/cancel
    post:
      operationId: post-cancel-account
      summary: Cancel your account
      tags:
      - Account
      description: |-
        Cancels an active Linode account. Akamai attempts to charge the credit card on file for any remaining balance. An error occurs if this charge fails.

        __Note__. This operation can only be accessed by account users with _unrestricted_ access.

        __Parent and child accounts__ In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, the following apply:

        - A child account user can't cancel a child account.
        - You can't cancel a parent account if it has an active child account.
        - You need to work with your Akamai account team to dissolve any parent-child account relationships before you can fully cancel a child or parent account.


        ---


        - __CLI__.

            ```
            linode-cli account cancel
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli account cancel
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Supply a comment stating the reason that you are cancelling your account.
        required: true
        content:
          application/json:
            schema:
              additionalProperties: false
              properties:
                comments:
                  type: string
                  description: |
                    Any reason for cancelling the account, and any other comments you might have about your
                    Linode service.
                  example: I'm consolidating multiple accounts into one.
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Account canceled
          content:
            application/json:
              schema:
                type: object
                example:
                  survey_link: https://alinktothesurvey.com
                additionalProperties: false
                properties:
                  survey_link:
                    type: string
                    description: |
                      A link to Linode's exit survey.
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
        409:
          description: |
            Could not charge the credit card on file
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      additionalProperties: false
                      properties:
                        reason:
                          type: string
                          description: |
                            A string explaining that the account could not be canceled because there is
                            an outstanding balance on the account that must be paid
                            first.
                          example: We were unable to charge your credit card for services rendered. We cannot
                            cancel this account until the balance has been paid.
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-cancel-account
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "comments": "I am consolidating my accounts."
              }' \
              https://api.linode.com/v4/account/cancel
      - lang: CLI
        source: |-
          linode-cli account cancel \
            --comments "I'm consolidating my accounts"
      x-linode-cli-action: cancel
      x-linode-grant: read_write
      x-original-op-id: cancelAccount
      x-original-op-title: Account Cancel
    x-linode-cli-command: account
  /account/child-accounts:
    x-akamai:
      file-path: paths/child-accounts.yaml
      path-info: /account/child-accounts
    get:
      operationId: get-child-accounts
      summary: List child accounts
      tags:
      - Child accounts
      description: |-
        Returns a paginated list of basic information for the child accounts that exist for your parent account. See [Parent and Child Accounts for Akamai Partners](https://www.linode.com/docs/guides/parent-child-accounts/) for details on these accounts.

        __Note__. This operation can only be accessed by an unrestricted parent user, or restricted parent user with the `child_account_access` grant.


        ---


        - __OAuth__.

            ```
            child_account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: child_account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns child-level accounts.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        Child account object
                      x-akamai:
                        file-path: schemas/child-account.yaml
                      additionalProperties: false
                      properties:
                        active_since:
                          type: string
                          description: |
                            __Read-only__ The activation date and time for the child account.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                        address_1:
                          type: string
                          description: |
                            First line of this child account's billing address.
                          example: 123 Main Street
                          maxLength: 64
                          x-linode-filterable: true
                        address_2:
                          type: string
                          description: |
                            Second line of this child account's billing address, if applicable.
                          example: Suite A
                          maxLength: 64
                          x-linode-filterable: true
                        balance:
                          type: number
                          description: |
                            __Read-only__ This child account's balance, in US dollars.
                          example: 200
                          readOnly: true
                          x-linode-cli-display: 4
                        balance_uninvoiced:
                          type: number
                          description: |
                            __Read-only__ This child account's current estimated invoice in US dollars.
                            This is not your final invoice balance. Transfer charges
                            are not included in the estimate.
                          example: 145
                          readOnly: true
                          x-linode-cli-display: 4
                        billing_source:
                          type: string
                          description: |
                            __Read-only__ The source of service charges for this account, as determined
                            by its relationship with Akamai. The API returns a value
                            of `external` to describe a child account in a parent-child
                            account environment.
                          example: external
                          readOnly: true
                          enum:
                          - external
                        capabilities:
                          type: array
                          description: |
                            __Read-only__ A list of the capabilities the child account supports.
                          example:
                          - Linodes
                          - NodeBalancers
                          - Block Storage
                          - Object Storage
                          readOnly: true
                          items:
                            type: string
                        city:
                          type: string
                          description: |
                            The city for this child account's billing address.
                          example: San Diego
                          maxLength: 24
                          x-linode-filterable: true
                        company:
                          type: string
                          description: |
                            'The company name for the owner of this child account. It can''t include
                            any of these characters: `<` `>` `(` `)` `"` `=`. You
                            can''t change this value yourself. We use it to create
                            the proxy users that a parent account uses to access a
                            child account. Talk to your account team if you need to
                            change this value.'
                          example: Acme
                          maxLength: 128
                          x-linode-filterable: true
                        country:
                          type: string
                          description: |
                            The two-letter ISO 3166 country code for this child account's billing
                            address.
                          example: US
                          x-linode-filterable: true
                        credit_card:
                          type: object
                          description: |
                            __Read-only__ Information for the credit card you've assigned to this
                            child account.
                          additionalProperties: false
                          readOnly: true
                          properties:
                            expiry:
                              type: string
                              description: |
                                The expiration month and year of the credit card.
                              example: 11/2024
                            last_four:
                              type: string
                              description: |
                                The last four digits of the credit card.
                              example: 1111
                        email:
                          type: string
                          description: |
                            The email address of the owner of this child account.
                          example: john.smith@linode.com
                          maxLength: 128
                          x-linode-cli-display: 3
                          x-linode-filterable: true
                        euuid:
                          type: string
                          description: |
                            __Read-only__ An external, unique identifier that Akamai assigned to
                            the child account.
                          example: A1BC2DEF-34GH-567I-J890KLMN12O34P56
                          readOnly: true
                          format: uuid
                        first_name:
                          type: string
                          description: |
                            'The first name of the owner of this child account. It can''t include
                            any of these characters: `<` `>` `(` `)` `"` `=`.'
                          example: John
                          maxLength: 50
                          x-linode-cli-display: 1
                          x-linode-filterable: true
                        last_name:
                          type: string
                          description: |
                            'The last name of the owner of this child account. It can''t include
                            any of these characters: `<` `>` `(` `)` `"` `=`.'
                          example: Smith
                          maxLength: 50
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        phone:
                          type: string
                          description: |
                            The phone number for the owner of this child account.
                          example: 858-555-1212
                          maxLength: 32
                          x-linode-filterable: true
                        state:
                          type: string
                          description: |-
                            The state or province for the billing address (`address_1` and `address_2, if applicable`). If in the United States (US) or Canada (CA), this is the two-letter ISO 3166 State or Province code.

                            __Note:__ If this is a US military address, use state abbreviations (AA, AE, AP).
                          example: CA
                          maxLength: 24
                          x-linode-filterable: true
                        tax_id:
                          type: string
                          description: |
                            The tax identification number for this child account. Use this for tax calculations
                            in some countries. If you live in a country that doesn't
                            collect taxes, ensure this is an empty string (`""`).
                          example: ATU99999999
                          maxLength: 25
                        zip:
                          type: string
                          description: |-
                            The zip code of this Account's billing address. The following restrictions apply:

                            - Can only contain ASCII letters, numbers, and hyphens (`-`).
                            - Can't contain more than 9 letter or number characters.
                          example: 92111-1234
                          x-linode-filterable: true
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-child-accounts
      security:
      - personalAccessToken: []
      - oauth:
        - child_account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/account/child-accounts
      - lang: CLI
        source: linode-cli child-account list
      x-linode-cli-action:
      - list
      - ls
      x-linode-grant: child_account_access
      x-original-op-id: getChildAccounts
      x-original-op-title: Child Account List
    x-linode-cli-command: child-account
  /account/child-accounts/{euuid}:
    x-akamai:
      file-path: paths/child-account.yaml
      path-info: /account/child-accounts/{euuid}
    parameters:
    - name: euuid
      in: path
      description: |
        The child account to look up. You can run the [List child accounts](https://techdocs.akamai.com/linode-api/reference/get-child-accounts)
        operation to find the applicable account and store its `euuid`.
      example: '{{euuid}}'
      x-akamai:
        file-path: parameters/eeuid.yaml
      required: true
      schema:
        type: string
    get:
      operationId: get-child-account
      summary: Get a child account
      tags:
      - Child accounts
      description: |-
        View a specific child account based on its `euuid`. See [Parent and Child Accounts for Akamai Partners](https://www.linode.com/docs/guides/parent-child-accounts/) for details on these accounts.

        __Note__. This operation can only be accessed by an unrestricted user, or restricted user with the `child_account_access` grant.


        ---


        - __OAuth__.

            ```
            child_account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: child_account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns the child-level account for a specified `euuid`.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Child account object
                x-akamai:
                  file-path: schemas/child-account.yaml
                additionalProperties: false
                properties:
                  active_since:
                    type: string
                    description: |
                      __Read-only__ The activation date and time for the child account.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  address_1:
                    type: string
                    description: |
                      First line of this child account's billing address.
                    example: 123 Main Street
                    maxLength: 64
                    x-linode-filterable: true
                  address_2:
                    type: string
                    description: |
                      Second line of this child account's billing address, if applicable.
                    example: Suite A
                    maxLength: 64
                    x-linode-filterable: true
                  balance:
                    type: number
                    description: |
                      __Read-only__ This child account's balance, in US dollars.
                    example: 200
                    readOnly: true
                    x-linode-cli-display: 4
                  balance_uninvoiced:
                    type: number
                    description: |
                      __Read-only__ This child account's current estimated invoice in US dollars. This
                      is not your final invoice balance. Transfer charges are not
                      included in the estimate.
                    example: 145
                    readOnly: true
                    x-linode-cli-display: 4
                  billing_source:
                    type: string
                    description: |
                      __Read-only__ The source of service charges for this account, as determined by its relationship
                      with Akamai. The API returns a value of `external` to describe
                      a child account in a parent-child account environment.
                    example: external
                    readOnly: true
                    enum:
                    - external
                  capabilities:
                    type: array
                    description: |
                      __Read-only__ A list of the capabilities the child account supports.
                    example:
                    - Linodes
                    - NodeBalancers
                    - Block Storage
                    - Object Storage
                    readOnly: true
                    items:
                      type: string
                  city:
                    type: string
                    description: |
                      The city for this child account's billing address.
                    example: San Diego
                    maxLength: 24
                    x-linode-filterable: true
                  company:
                    type: string
                    description: |
                      'The company name for the owner of this child account. It can''t include any of these
                      characters: `<` `>` `(` `)` `"` `=`. You can''t change this
                      value yourself. We use it to create the proxy users that a parent
                      account uses to access a child account. Talk to your account
                      team if you need to change this value.'
                    example: Acme
                    maxLength: 128
                    x-linode-filterable: true
                  country:
                    type: string
                    description: |
                      The two-letter ISO 3166 country code for this child account's billing address.
                    example: US
                    x-linode-filterable: true
                  credit_card:
                    type: object
                    description: |
                      __Read-only__ Information for the credit card you've assigned to this child account.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      expiry:
                        type: string
                        description: |
                          The expiration month and year of the credit card.
                        example: 11/2024
                      last_four:
                        type: string
                        description: |
                          The last four digits of the credit card.
                        example: 1111
                  email:
                    type: string
                    description: |
                      The email address of the owner of this child account.
                    example: john.smith@linode.com
                    maxLength: 128
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  euuid:
                    type: string
                    description: |
                      __Read-only__ An external, unique identifier that Akamai assigned to the child account.
                    example: A1BC2DEF-34GH-567I-J890KLMN12O34P56
                    readOnly: true
                    format: uuid
                  first_name:
                    type: string
                    description: |
                      'The first name of the owner of this child account. It can''t include any of these
                      characters: `<` `>` `(` `)` `"` `=`.'
                    example: John
                    maxLength: 50
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  last_name:
                    type: string
                    description: |
                      'The last name of the owner of this child account. It can''t include any of these
                      characters: `<` `>` `(` `)` `"` `=`.'
                    example: Smith
                    maxLength: 50
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  phone:
                    type: string
                    description: |
                      The phone number for the owner of this child account.
                    example: 858-555-1212
                    maxLength: 32
                    x-linode-filterable: true
                  state:
                    type: string
                    description: |-
                      The state or province for the billing address (`address_1` and `address_2, if applicable`). If in the United States (US) or Canada (CA), this is the two-letter ISO 3166 State or Province code.

                      __Note:__ If this is a US military address, use state abbreviations (AA, AE, AP).
                    example: CA
                    maxLength: 24
                    x-linode-filterable: true
                  tax_id:
                    type: string
                    description: |
                      The tax identification number for this child account. Use this for tax calculations
                      in some countries. If you live in a country that doesn't collect
                      taxes, ensure this is an empty string (`""`).
                    example: ATU99999999
                    maxLength: 25
                  zip:
                    type: string
                    description: |-
                      The zip code of this Account's billing address. The following restrictions apply:

                      - Can only contain ASCII letters, numbers, and hyphens (`-`).
                      - Can't contain more than 9 letter or number characters.
                    example: 92111-1234
                    x-linode-filterable: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-child-account
      security:
      - personalAccessToken: []
      - oauth:
        - child_account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/account/child-accounts/A1BC2DEF-34GH-567I-J890KLMN12O34P56
      - lang: CLI
        source: linode-cli child-account view A1BC2DEF-34GH-567I-J890KLMN12O34P56
      x-linode-cli-action:
      - view
      x-linode-grant: child_account_access
      x-original-op-id: viewChildAccount
      x-original-op-title: Child Account View
    x-linode-cli-command: child-account
  /account/child-accounts/{euuid}/token:
    x-akamai:
      file-path: paths/child-account-token.yaml
      path-info: /account/child-accounts/{euuid}/token
    parameters:
    - name: euuid
      in: path
      description: |
        The child account to look up. You can run the [List child accounts](https://techdocs.akamai.com/linode-api/reference/get-child-accounts)
        operation to find the applicable account and store its `euuid`.
      example: '{{euuid}}'
      x-akamai:
        file-path: parameters/eeuid.yaml
      required: true
      schema:
        type: string
    post:
      operationId: post-child-account-token
      summary: Create a proxy user token
      tags:
      - Child accounts
      description: |-
        Create a short-lived bearer token for a parent user on a child account, using the `euuid` of that child account. In the context of the API, a parent user on a child account is referred to as a "proxy user." When Akamai provisions your parent-child account environment, a proxy user is automatically set in the child account. It follows a specific naming convention:

            <Parent account `company` name>_<SHA256 hash of parent `company` name and child account `euuid`>

        __Note__. The variables above use only the first 15 and 16 characters of these values, respectively.

        The token lets a parent account run API operations through the proxy user, as if they are a child user in the child account.

        These points apply to the use of this operation:

        - To create a token, a parent account user needs the `child_account_access` grant. This lets them use the proxy user on the child account. You can run [List a user's grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants) on a parent account user to check its `child_account_access` setting. To add this access, you can [update](https://techdocs.akamai.com/linode-api/reference/put-user-grants) the parent account user.

        - The created token inherits the permissions of the proxy user. It will never have less.

        - The API returns the raw token in the response. You can't get it again, so be sure to store it.

        Example workflow:

        1. [List child accounts](https://techdocs.akamai.com/linode-api/reference/get-child-accounts) and store the `euuid` for the applicable one.
        2. Run this operation and store the `token` that's created for the proxy user.
        3. As a parent account user with access to the proxy user in the child account, use this `token` to authenticate API operations, as if you were a child user.


        ---


        - __OAuth__.

            ```
            child_account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: child_account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Token created successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  The token generated manually for a child account so its proxy user can access the API and CLI
                  without going through an OAuth login.
                x-akamai:
                  file-path: schemas/proxy-user-token.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The proxy user token's unique ID, which can be used to revoke it.
                    example: 918
                    readOnly: true
                    x-linode-cli-display: 1
                  created:
                    type: string
                    description: |
                      __Read-only__ The date and time this token was created.
                    example: '2024-05-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  expiry:
                    type: string
                    description: |
                      __Read-only__ When this token expires. This is default set to 15 minutes from the
                      time of creation. Proxy user tokens can't be renewed. After
                      this time, Akamai revokes the token and you need to generate
                      a new one.
                    example: '2024-05-01T00:16:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 6
                  label:
                    type: string
                    description: |
                      The name of the token. The API automatically sets this to `<username>_<uid>_<time>`.
                      It's composed of the `username` for your parent account user,
                      the unique `uid` Akamai assigned to identify your user, and
                      the `time` the API generated the token. This is for display
                      purposes only, but you can use it to help track how you're using
                      each proxy user token.
                    example: parent1_1234_2024-05-01T00:01:01
                    minLength: 1
                    maxLength: 100
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  scopes:
                    type: string
                    description: |
                      __Read-only__ The scopes this token was created with. Defaults to `*`. Proxy user tokens
                      automatically inherit all the permissions of the proxy user.
                    example: '*'
                    readOnly: true
                    format: oauth-scopes
                    x-linode-cli-display: 3
                  token:
                    type: string
                    description: |
                      __Read-only__ The proxy user token that can be used to access the API and CLI. After
                      you [create](https://techdocs.akamai.com/linode-api/reference/post-child-account-token)
                      a token, you can see the full token in the response. All other
                      operations that contain this token only show the first 16 characters
                      in their response.
                    example: abcdefghijklmnop
                    readOnly: true
                    x-linode-cli-display: 5
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-child-account-token
      security:
      - personalAccessToken: []
      - oauth:
        - child_account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
            -H "Authorization: Bearer $TOKEN" \
            -X POST \
            https://api.linode.com/v4/account/child-accounts/A1BC2DEF-34GH-567I-J890KLMN12O34P56/token
      - lang: CLI
        source: linode-cli child-account create A1BC2DEF-34GH-567I-J890KLMN12O34P56
      x-linode-cli-action:
      - create
      x-linode-grant: child_account_access
      x-original-op-id: createChildAccountToken
      x-original-op-title: Proxy User Token Create
    x-linode-cli-command: child-account
  /account/credit-card:
    x-akamai:
      file-path: paths/credit-card.yaml
      path-info: /account/credit-card
    post:
      operationId: post-credit-card
      summary: Add or edit a credit card
      tags:
      - Payments
      description: |-
        __Deprecated__ Please run [Add a payment method](https://techdocs.akamai.com/linode-api/reference/post-payment-method).

        Adds a credit card Payment Method to your account and sets it as the default method.


        ---


        - __CLI__.

            ```
            linode-cli account update-card
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: DEPRECATED
        tabs:
        - title: CLI
          syntax: linode-cli account update-card
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Update the credit card information associated with your Account.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                An object representing the credit card information you have on file with Linode to make Payments
                against your Account.
              x-akamai:
                file-path: schemas/credit-card.yaml
              additionalProperties: false
              required:
              - card_number
              - expiry_month
              - expiry_year
              - cvv
              properties:
                card_number:
                  type: string
                  description: |
                    Your credit card number. No spaces or hyphens (`-`) allowed.
                  example: '{{card_number}}'
                  minLength: 14
                  maxLength: 24
                  format: digits
                cvv:
                  type: string
                  description: |
                    CVV (Card Verification Value) of the credit card, typically found on the back of the
                    card.
                  example: '{{cvv}}'
                  minLength: 3
                  maxLength: 4
                  format: digits
                expiry_month:
                  type: integer
                  description: |-
                    A value from 1-12 representing the expiration month of your credit card.

                      - 1 = January
                      - 2 = February
                      - 3 = March
                      - Etc.
                  example: '{{expiry_month}}'
                  minimum: 1
                  maximum: 12
                expiry_year:
                  type: integer
                  description: |-
                    A four-digit integer representing the expiration year of your credit card.

                    The combination of `expiry_month` and `expiry_year` must result in a month/year combination of the current month or in the future. An expiration date set in the past is invalid.
                  example: '{{expiry_year}}'
                  minLength: 4
                  maxLength: 4
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Credit Card updated.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      deprecated: true
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-credit-card
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "card_number": "4111111111111111",
                "expiry_month": 11,
                "expiry_year": 2020,
                "cvv": "111"
              }' \
              https://api.linode.com/v4/account/credit-card
      - lang: CLI
        source: |-
          linode-cli account update-card \
            --card_number 4111111111111111 \
            --expiry_month 11 \
            --expiry_year 2025 \
            --cvv 111
      x-linode-cli-action: update-card
      x-linode-cli-skip: true
      x-linode-grant: read_write
      x-original-op-id: createCreditCard
      x-original-op-title: Credit Card Add/Edit
    x-linode-cli-command: account
  /account/entity-transfers:
    x-akamai:
      file-path: paths/entity-transfers.yaml
      path-info: /account/entity-transfers
    post:
      operationId: post-entity-transfer
      summary: Create an entity transfer
      tags:
      - Entity transfers
      description: |-
        __Deprecated__ Please run [Request a service transfer](https://techdocs.akamai.com/linode-api/reference/post-service-transfer).


        ---


        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: DEPRECATED
        tabs:
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The entities to include in this transfer request.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - entities
              properties:
                entities:
                  type: object
                  description: |
                    A collection of the entities to include in this transfer request, separated by type.
                  additionalProperties: false
                  properties:
                    linodes:
                      type: array
                      description: |
                        An array containing the IDs of each of the Linodes included in this transfer.
                      example:
                      - 111
                      - 222
                      items:
                        type: integer
                      x-linode-cli-display: 5
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns an Entity Transfer object for the request.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An object representing an Entity Transfer.
                x-akamai:
                  file-path: schemas/entity-transfer.yaml
                additionalProperties: false
                properties:
                  created:
                    type: string
                    description: |
                      When this transfer was created.
                    example: '2021-02-11T16:37:03'
                    format: date-time
                  entities:
                    type: object
                    description: |
                      A collection of the entities to include in this transfer request, separated by type.
                    additionalProperties: false
                    properties:
                      linodes:
                        type: array
                        description: |
                          An array containing the IDs of each of the Linodes included in this transfer.
                        example:
                        - 111
                        - 222
                        items:
                          type: integer
                        x-linode-cli-display: 5
                  expiry:
                    type: string
                    description: |
                      When this transfer expires. Transfers will automatically expire 24 hours after
                      creation.
                    example: '2021-02-12T16:37:03'
                    format: date-time
                    x-linode-cli-display: 3
                  is_sender:
                    type: boolean
                    description: |
                      If the requesting account created this transfer.
                    example: true
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  status:
                    type: string
                    description: |-
                      The status of the transfer request:

                      `accepted`: The transfer has been accepted by another user and is currently in progress. Transfers can take up to 3 hours to complete.
                      `canceled`: The transfer has been canceled by the sender.
                      `completed`: The transfer has completed successfully.
                      `failed`: The transfer has failed after initiation.
                      `pending`: The transfer is ready to be accepted.
                      `stale`: The transfer has exceeded its expiration date. It can no longer be accepted or canceled.
                    example: pending
                    enum:
                    - accepted
                    - canceled
                    - completed
                    - failed
                    - pending
                    - stale
                    x-linode-cli-color:
                      accepted: yellow
                      canceled: red
                      completed: green
                      default_: white
                      failed: red
                      pending: yellow
                      stale: red
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  token:
                    type: string
                    description: |
                      The token used to identify and accept or cancel this transfer.
                    example: 123E4567-E89B-12D3-A456-426614174000
                    format: uuid
                    x-linode-cli-display: 1
                  updated:
                    type: string
                    description: |
                      When this transfer was last updated.
                    example: '2021-02-11T16:37:03'
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      deprecated: true
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-entity-transfer
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
            -H "Authorization: Bearer $TOKEN" \
            -X POST -d '{
              "entities": {
                "linodes": [
                  111,
                  222
                ]
              }
            }' \
            https://api.linode.com/v4/account/entity-transfers
      x-linode-cli-skip: true
      x-linode-grant: unrestricted only
      x-original-op-id: createEntityTransfer
      x-original-op-title: Entity Transfer Create
    get:
      operationId: get-entity-transfers
      summary: List entity transfers
      tags:
      - Entity transfers
      description: |-
        __Deprecated__ Please run [List service transfers](https://techdocs.akamai.com/linode-api/reference/get-service-transfers).


        ---


        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: DEPRECATED
        tabs:
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated list of Entity Transfer objects containing the details of all transfers that have been
            created and accepted by this account.
          content:
            application/json:
              schema:
                allOf:
                - type: object
                  description: |
                    An envelope for paginated response. When accessing a collection through a GET endpoint,
                    the results are wrapped in this envelope which includes metadata
                    about those results. Results are presented within a `data` array.
                    See [Pagination](https://techdocs.akamai.com/linode-api/reference/pagination)
                    for more information.
                  x-akamai:
                    file-path: schemas/pagination-envelope.yaml
                  additionalProperties: false
                  properties:
                    page:
                      type: integer
                      description: |
                        __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    pages:
                      type: integer
                      description: |
                        __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    results:
                      type: integer
                      description: |
                        __Read-only__ The total number of results.
                      example: 1
                      readOnly: true
                - properties:
                    data:
                      type: array
                      items:
                        type: object
                        description: |
                          An object representing an Entity Transfer.
                        x-akamai:
                          file-path: schemas/entity-transfer.yaml
                        additionalProperties: false
                        properties:
                          created:
                            type: string
                            description: |
                              When this transfer was created.
                            example: '2021-02-11T16:37:03'
                            format: date-time
                          entities:
                            type: object
                            description: |
                              A collection of the entities to include in this transfer request,
                              separated by type.
                            additionalProperties: false
                            properties:
                              linodes:
                                type: array
                                description: |
                                  An array containing the IDs of each of the Linodes included
                                  in this transfer.
                                example:
                                - 111
                                - 222
                                items:
                                  type: integer
                                x-linode-cli-display: 5
                          expiry:
                            type: string
                            description: |
                              When this transfer expires. Transfers will automatically expire 24 hours
                              after creation.
                            example: '2021-02-12T16:37:03'
                            format: date-time
                            x-linode-cli-display: 3
                          is_sender:
                            type: boolean
                            description: |
                              If the requesting account created this transfer.
                            example: true
                            x-linode-cli-display: 4
                            x-linode-filterable: true
                          status:
                            type: string
                            description: |-
                              The status of the transfer request:

                              `accepted`: The transfer has been accepted by another user and is currently in progress. Transfers can take up to 3 hours to complete.
                              `canceled`: The transfer has been canceled by the sender.
                              `completed`: The transfer has completed successfully.
                              `failed`: The transfer has failed after initiation.
                              `pending`: The transfer is ready to be accepted.
                              `stale`: The transfer has exceeded its expiration date. It can no longer be accepted or canceled.
                            example: pending
                            enum:
                            - accepted
                            - canceled
                            - completed
                            - failed
                            - pending
                            - stale
                            x-linode-cli-color:
                              accepted: yellow
                              canceled: red
                              completed: green
                              default_: white
                              failed: red
                              pending: yellow
                              stale: red
                            x-linode-cli-display: 2
                            x-linode-filterable: true
                          token:
                            type: string
                            description: |
                              The token used to identify and accept or cancel this transfer.
                            example: 123E4567-E89B-12D3-A456-426614174000
                            format: uuid
                            x-linode-cli-display: 1
                          updated:
                            type: string
                            description: |
                              When this transfer was last updated.
                            example: '2021-02-11T16:37:03'
                            format: date-time
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      deprecated: true
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-entity-transfers
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
            https://api.linode.com/v4/account/entity-transfers
      x-linode-cli-skip: true
      x-linode-grant: unrestricted only
      x-original-op-id: getEntityTransfers
      x-original-op-title: Entity Transfers List
  /account/entity-transfers/{token}:
    x-akamai:
      file-path: paths/entity-transfer.yaml
      path-info: /account/entity-transfers/{token}
    parameters:
    - name: token
      in: path
      description: |
        The UUID of the Entity Transfer.
      example: '{{token}}'
      x-akamai:
        file-path: parameters/token-path-faf66b58.yaml
      required: true
      schema:
        type: string
        format: uuid
    get:
      operationId: get-entity-transfer
      summary: Get an entity transfer
      tags:
      - Entity transfers
      description: |-
        __Deprecated__ Please run [Get a service transfer request](https://techdocs.akamai.com/linode-api/reference/get-service-transfer).


        ---


        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: DEPRECATED
        tabs:
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns an Entity Transfer object containing the details of the transfer for the specified token.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An object representing an Entity Transfer.
                x-akamai:
                  file-path: schemas/entity-transfer.yaml
                additionalProperties: false
                properties:
                  created:
                    type: string
                    description: |
                      When this transfer was created.
                    example: '2021-02-11T16:37:03'
                    format: date-time
                  entities:
                    type: object
                    description: |
                      A collection of the entities to include in this transfer request, separated by type.
                    additionalProperties: false
                    properties:
                      linodes:
                        type: array
                        description: |
                          An array containing the IDs of each of the Linodes included in this transfer.
                        example:
                        - 111
                        - 222
                        items:
                          type: integer
                        x-linode-cli-display: 5
                  expiry:
                    type: string
                    description: |
                      When this transfer expires. Transfers will automatically expire 24 hours after
                      creation.
                    example: '2021-02-12T16:37:03'
                    format: date-time
                    x-linode-cli-display: 3
                  is_sender:
                    type: boolean
                    description: |
                      If the requesting account created this transfer.
                    example: true
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  status:
                    type: string
                    description: |-
                      The status of the transfer request:

                      `accepted`: The transfer has been accepted by another user and is currently in progress. Transfers can take up to 3 hours to complete.
                      `canceled`: The transfer has been canceled by the sender.
                      `completed`: The transfer has completed successfully.
                      `failed`: The transfer has failed after initiation.
                      `pending`: The transfer is ready to be accepted.
                      `stale`: The transfer has exceeded its expiration date. It can no longer be accepted or canceled.
                    example: pending
                    enum:
                    - accepted
                    - canceled
                    - completed
                    - failed
                    - pending
                    - stale
                    x-linode-cli-color:
                      accepted: yellow
                      canceled: red
                      completed: green
                      default_: white
                      failed: red
                      pending: yellow
                      stale: red
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  token:
                    type: string
                    description: |
                      The token used to identify and accept or cancel this transfer.
                    example: 123E4567-E89B-12D3-A456-426614174000
                    format: uuid
                    x-linode-cli-display: 1
                  updated:
                    type: string
                    description: |
                      When this transfer was last updated.
                    example: '2021-02-11T16:37:03'
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      deprecated: true
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-entity-transfer
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
            https://api.linode.com/v4/account/entity-transfers/123E4567-E89B-12D3-A456-426614174000
      x-linode-cli-skip: true
      x-linode-grant: unrestricted only
      x-original-op-id: getEntityTransfer
      x-original-op-title: Entity Transfer View
    delete:
      operationId: delete-entity-transfer
      summary: Cancel an entity transfer
      tags:
      - Entity transfers
      description: |-
        __Deprecated__ Please run [Cancel a service transfer](https://techdocs.akamai.com/linode-api/reference/delete-service-transfer).


        ---


        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: DEPRECATED
        tabs:
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Entity Transfer canceled.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      deprecated: true
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-entity-transfer
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
            -X DELETE \
            https://api.linode.com/v4/account/entity-transfers/123E4567-E89B-12D3-A456-426614174000
      x-linode-cli-skip: true
      x-linode-grant: unrestricted only
      x-original-op-id: deleteEntityTransfer
      x-original-op-title: Entity Transfer Cancel
  /account/entity-transfers/{token}/accept:
    x-akamai:
      file-path: paths/entity-transfer-accept.yaml
      path-info: /account/entity-transfers/{token}/accept
    parameters:
    - name: token
      in: path
      description: |
        The UUID of the Entity Transfer.
      example: '{{token}}'
      x-akamai:
        file-path: parameters/token-path-faf66b58.yaml
      required: true
      schema:
        type: string
        format: uuid
    post:
      operationId: post-accept-entity-transfer
      summary: Accept an entity transfer
      tags:
      - Entity transfers
      description: |-
        __Deprecated__ Please run [Accept a service transfer](https://techdocs.akamai.com/linode-api/reference/post-accept-service-transfer).


        ---


        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: DEPRECATED
        tabs:
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Entity Transfer accepted.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      deprecated: true
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-accept-entity-transfer
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
            -X POST \
            https://api.linode.com/v4/account/entity-transfers/123E4567-E89B-12D3-A456-426614174000/accept
      x-linode-cli-skip: true
      x-linode-grant: unrestricted only
      x-original-op-id: acceptEntityTransfer
      x-original-op-title: Entity Transfer Accept
  /account/events:
    x-akamai:
      file-path: paths/events.yaml
      path-info: /account/events
    get:
      operationId: get-events
      summary: List events
      tags:
      - Events
      description: |-
        Returns a collection of Event objects representing actions taken on your Account from the last 90 days. The Events returned depend on your grants.


        ---


        - __OAuth__.

            ```
            events:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: events:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated lists of Event objects from the last 90 days.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        A collection of Event objects. An Event is an action taken against an entity related
                        to your Account. For example, booting a Linode would create
                        an Event. The Events returned depends on your grants.
                      x-akamai:
                        file-path: schemas/event.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ The unique ID of this Event.
                          example: 123
                          readOnly: true
                          x-linode-cli-display: 1
                          x-linode-filterable: true
                        action:
                          type: string
                          description: |
                            __Read-only__ The action that caused this Event. New actions may be
                            added in the future.
                          example: ticket_create
                          readOnly: true
                          enum:
                          - account_update
                          - account_settings_update
                          - aclb_create
                          - aclb_update
                          - aclb_delete
                          - aclb_config_create
                          - aclb_config_update
                          - aclb_config_delete
                          - aclb_route_create
                          - aclb_route_update
                          - aclb_route_delete
                          - aclb_servicetarget_create
                          - aclb_servicetarget_update
                          - aclb_servicetarget_delete
                          - aclb_certificate_create
                          - aclb_certificate_update
                          - aclb_certificate_delete
                          - backups_enable
                          - backups_cancel
                          - backups_restore
                          - community_question_reply
                          - community_like
                          - credit_card_updated
                          - disk_create
                          - disk_delete
                          - disk_update
                          - disk_duplicate
                          - disk_imagize
                          - disk_resize
                          - dns_record_create
                          - dns_record_delete
                          - dns_record_update
                          - dns_zone_create
                          - dns_zone_delete
                          - dns_zone_import
                          - dns_zone_update
                          - entity_transfer_accept
                          - entity_transfer_cancel
                          - entity_transfer_create
                          - entity_transfer_fail
                          - entity_transfer_stale
                          - firewall_create
                          - firewall_delete
                          - firewall_disable
                          - firewall_enable
                          - firewall_update
                          - firewall_device_add
                          - firewall_device_remove
                          - host_reboot
                          - image_delete
                          - image_update
                          - image_upload
                          - ipaddress_update
                          - lassie_reboot
                          - lish_boot
                          - linode_addip
                          - linode_boot
                          - linode_clone
                          - linode_create
                          - linode_delete
                          - linode_update
                          - linode_deleteip
                          - linode_migrate
                          - linode_migrate_datacenter
                          - linode_migrate_datacenter_create
                          - linode_mutate
                          - linode_mutate_create
                          - linode_reboot
                          - linode_rebuild
                          - linode_resize
                          - linode_resize_create
                          - linode_shutdown
                          - linode_snapshot
                          - linode_config_create
                          - linode_config_delete
                          - linode_config_update
                          - lke_node_create
                          - longviewclient_create
                          - longviewclient_delete
                          - longviewclient_update
                          - managed_disabled
                          - managed_enabled
                          - managed_service_create
                          - managed_service_delete
                          - nodebalancer_create
                          - nodebalancer_delete
                          - nodebalancer_update
                          - nodebalancer_config_create
                          - nodebalancer_config_delete
                          - nodebalancer_config_update
                          - nodebalancer_node_create
                          - nodebalancer_node_delete
                          - nodebalancer_node_update
                          - oauth_client_create
                          - oauth_client_delete
                          - oauth_client_secret_reset
                          - oauth_client_update
                          - obj_access_key_create
                          - obj_access_key_delete
                          - obj_access_key_update
                          - password_reset
                          - payment_method_add
                          - payment_submitted
                          - placement_group_assign
                          - placement_group_became_compliant
                          - placement_group_became_non_compliant
                          - placement_group_create
                          - placement_group_delete
                          - placement_group_unassign
                          - placement_group_update
                          - profile_update
                          - stackscript_create
                          - stackscript_delete
                          - stackscript_update
                          - stackscript_publicize
                          - stackscript_revise
                          - subnet_create
                          - subnet_delete
                          - subnet_update
                          - tag_create
                          - tag_delete
                          - tfa_disabled
                          - tfa_enabled
                          - ticket_attachment_upload
                          - ticket_create
                          - ticket_update
                          - token_create
                          - token_delete
                          - token_update
                          - user_create
                          - user_update
                          - user_delete
                          - user_ssh_key_add
                          - user_ssh_key_delete
                          - user_ssh_key_update
                          - vlan_attach
                          - vlan_detach
                          - volume_attach
                          - volume_clone
                          - volume_create
                          - volume_delete
                          - volume_update
                          - volume_detach
                          - volume_resize
                          - vpc_create
                          - vpc_delete
                          - vpc_update
                          x-linode-cli-display: 3
                          x-linode-filterable: true
                        created:
                          type: string
                          description: |
                            __Read-only__ When this Event was created.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                          x-linode-cli-display: 6
                          x-linode-filterable: true
                        duration:
                          type: number
                          description: |
                            __Read-only__ The total duration in seconds that it takes for the Event
                            to complete.
                          example: 300.56
                          readOnly: true
                          x-linode-cli-display: 7
                        entity:
                          type: object
                          description: |
                            __Read-only__ Detailed information about the Event's entity, including
                            ID, type, label, and URL used to access it.
                          additionalProperties: false
                          readOnly: true
                          properties:
                            id:
                              type: integer
                              description: |-
                                The unique ID for an Event's entity.

                                Some Event entities do not have IDs associated with them, so they will not be returned when filtering by ID. These Events include:

                                  - `account`
                                  - `profile`

                                Entities for some Events are assigned the ID of the Linode they correspond to. When filtering by ID for these Events, use the corresponding Linode's ID. These Events include:

                                  - `disks`
                                  - `backups`

                                Tag Events use a tag's name for the entity ID field. When filtering by ID for tag Events, supply the name of the tag.
                              example: 11111
                              x-linode-filterable: true
                            type:
                              type: string
                              description: |
                                __Read-only__ The type of entity that is being referenced by the
                                Event.
                              example: ticket
                              readOnly: true
                              enum:
                              - account
                              - backups
                              - community
                              - disks
                              - domain
                              - entity_transfer
                              - firewall
                              - image
                              - ipaddress
                              - linode
                              - longview
                              - loadbalancer
                              - managed_service
                              - nodebalancer
                              - oauth_client
                              - placement_group
                              - profile
                              - stackscript
                              - tag
                              - ticket
                              - token
                              - user
                              - user_ssh_key
                              - volume
                              x-linode-filterable: true
                            label:
                              type: string
                              description: |
                                The current label of this object. The label may reflect changes
                                that occur with this Event.
                              example: Problem booting my Linode
                              x-linode-cli-display: 5
                            url:
                              type: string
                              description: |
                                The URL where you can access the object this Event is for. If a
                                relative URL, it is relative to the domain you retrieved
                                the Event from.
                              example: /v4/support/tickets/11111
                        message:
                          type: string
                          description: |
                            Provides additional information about the event. Additional information may
                            include, but is not limited to, a more detailed representation
                            of events which can help diagnose non-obvious failures.
                          example: None
                          nullable: true
                          x-linode-cli-display: 9
                        percent_complete:
                          type: integer
                          description: |
                            __Read-only__ A percentage estimating the amount of time remaining for
                            an Event. Returns `null` for notification events.
                          example: null
                          readOnly: true
                        rate:
                          type: string
                          description: |
                            __Read-only__ The rate of completion of the Event. Only some Events
                            will return rate; for example, migration and resize Events.
                          example: null
                          readOnly: true
                        read:
                          type: boolean
                          description: |
                            __Read-only__ If this Event has been read.
                          example: true
                          readOnly: true
                          x-linode-filterable: true
                        secondary_entity:
                          type: object
                          description: |
                            __Read-only__ Detailed information about the Event's secondary entity,
                            which provides additional information for events such
                            as, but not limited to, `linode_boot`, `linode_reboot`,
                            `linode_create`, and `linode_clone` Event actions.
                          additionalProperties: false
                          readOnly: true
                          properties:
                            id:
                              type: string
                              description: |
                                The ID of the object that is the secondary entity.
                              example: linode/debian9
                            type:
                              type: string
                              description: |
                                __Read-only__ The type of entity that is being referenced by the
                                Event.
                              example: linode
                              readOnly: true
                            label:
                              type: string
                              description: |
                                The label of this object.
                              example: linode1234
                            url:
                              type: string
                              description: |
                                The URL where you can access the object this Event is for. If a
                                relative URL, it is relative to the domain you retrieved
                                the Event from.
                              example: /v4/linode/instances/1234
                        seen:
                          type: boolean
                          description: |
                            __Read-only__ If this Event has been seen.
                          example: true
                          readOnly: true
                        status:
                          type: string
                          description: |
                            __Read-only__ The current status of this Event.
                          readOnly: true
                          enum:
                          - failed
                          - finished
                          - notification
                          - scheduled
                          - started
                          x-linode-cli-color:
                            default_: white
                            failed: red
                            finished: green
                            started: yellow
                          x-linode-cli-display: 8
                        time_remaining:
                          type: string
                          description: |
                            __Read-only__ The estimated time remaining until the completion of this
                            Event. This value is only returned for some in-progress
                            migration events. For all other in-progress events, the
                            `percent_complete` attribute will indicate about how much
                            more work is to be done.
                          example: null
                          nullable: true
                          readOnly: true
                        username:
                          type: string
                          description: |
                            __Read-only__ The username of the User who caused the Event.
                          example: exampleUser
                          nullable: true
                          readOnly: true
                          x-linode-cli-display: 2
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-events
      security:
      - personalAccessToken: []
      - oauth:
        - events:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/account/events
      - lang: CLI
        source: linode-cli events list
      x-linode-cli-action:
      - list
      - ls
      x-linode-grant: read_only
      x-original-op-id: getEvents
      x-original-op-title: Events List
    x-linode-cli-command: events
  /account/events/{eventId}:
    x-akamai:
      file-path: paths/event.yaml
      path-info: /account/events/{eventId}
    parameters:
    - name: eventId
      in: path
      description: |
        The ID of the Event.
      example: '{{eventId}}'
      x-akamai:
        file-path: parameters/event-id-path-39255fcf.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-event
      summary: Get an event
      tags:
      - Events
      description: |-
        Returns a single Event object.


        ---


        - __CLI__.

            ```
            linode-cli events view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            events:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli events view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: events:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            An Event object
          content:
            application/json:
              schema:
                type: object
                description: |
                  A collection of Event objects. An Event is an action taken against an entity related to your Account.
                  For example, booting a Linode would create an Event. The Events
                  returned depends on your grants.
                x-akamai:
                  file-path: schemas/event.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of this Event.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  action:
                    type: string
                    description: |
                      __Read-only__ The action that caused this Event. New actions may be added in the future.
                    example: ticket_create
                    readOnly: true
                    enum:
                    - account_update
                    - account_settings_update
                    - aclb_create
                    - aclb_update
                    - aclb_delete
                    - aclb_config_create
                    - aclb_config_update
                    - aclb_config_delete
                    - aclb_route_create
                    - aclb_route_update
                    - aclb_route_delete
                    - aclb_servicetarget_create
                    - aclb_servicetarget_update
                    - aclb_servicetarget_delete
                    - aclb_certificate_create
                    - aclb_certificate_update
                    - aclb_certificate_delete
                    - backups_enable
                    - backups_cancel
                    - backups_restore
                    - community_question_reply
                    - community_like
                    - credit_card_updated
                    - disk_create
                    - disk_delete
                    - disk_update
                    - disk_duplicate
                    - disk_imagize
                    - disk_resize
                    - dns_record_create
                    - dns_record_delete
                    - dns_record_update
                    - dns_zone_create
                    - dns_zone_delete
                    - dns_zone_import
                    - dns_zone_update
                    - entity_transfer_accept
                    - entity_transfer_cancel
                    - entity_transfer_create
                    - entity_transfer_fail
                    - entity_transfer_stale
                    - firewall_create
                    - firewall_delete
                    - firewall_disable
                    - firewall_enable
                    - firewall_update
                    - firewall_device_add
                    - firewall_device_remove
                    - host_reboot
                    - image_delete
                    - image_update
                    - image_upload
                    - ipaddress_update
                    - lassie_reboot
                    - lish_boot
                    - linode_addip
                    - linode_boot
                    - linode_clone
                    - linode_create
                    - linode_delete
                    - linode_update
                    - linode_deleteip
                    - linode_migrate
                    - linode_migrate_datacenter
                    - linode_migrate_datacenter_create
                    - linode_mutate
                    - linode_mutate_create
                    - linode_reboot
                    - linode_rebuild
                    - linode_resize
                    - linode_resize_create
                    - linode_shutdown
                    - linode_snapshot
                    - linode_config_create
                    - linode_config_delete
                    - linode_config_update
                    - lke_node_create
                    - longviewclient_create
                    - longviewclient_delete
                    - longviewclient_update
                    - managed_disabled
                    - managed_enabled
                    - managed_service_create
                    - managed_service_delete
                    - nodebalancer_create
                    - nodebalancer_delete
                    - nodebalancer_update
                    - nodebalancer_config_create
                    - nodebalancer_config_delete
                    - nodebalancer_config_update
                    - nodebalancer_node_create
                    - nodebalancer_node_delete
                    - nodebalancer_node_update
                    - oauth_client_create
                    - oauth_client_delete
                    - oauth_client_secret_reset
                    - oauth_client_update
                    - obj_access_key_create
                    - obj_access_key_delete
                    - obj_access_key_update
                    - password_reset
                    - payment_method_add
                    - payment_submitted
                    - placement_group_assign
                    - placement_group_became_compliant
                    - placement_group_became_non_compliant
                    - placement_group_create
                    - placement_group_delete
                    - placement_group_unassign
                    - placement_group_update
                    - profile_update
                    - stackscript_create
                    - stackscript_delete
                    - stackscript_update
                    - stackscript_publicize
                    - stackscript_revise
                    - subnet_create
                    - subnet_delete
                    - subnet_update
                    - tag_create
                    - tag_delete
                    - tfa_disabled
                    - tfa_enabled
                    - ticket_attachment_upload
                    - ticket_create
                    - ticket_update
                    - token_create
                    - token_delete
                    - token_update
                    - user_create
                    - user_update
                    - user_delete
                    - user_ssh_key_add
                    - user_ssh_key_delete
                    - user_ssh_key_update
                    - vlan_attach
                    - vlan_detach
                    - volume_attach
                    - volume_clone
                    - volume_create
                    - volume_delete
                    - volume_update
                    - volume_detach
                    - volume_resize
                    - vpc_create
                    - vpc_delete
                    - vpc_update
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Event was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 6
                    x-linode-filterable: true
                  duration:
                    type: number
                    description: |
                      __Read-only__ The total duration in seconds that it takes for the Event to complete.
                    example: 300.56
                    readOnly: true
                    x-linode-cli-display: 7
                  entity:
                    type: object
                    description: |
                      __Read-only__ Detailed information about the Event's entity, including ID, type, label,
                      and URL used to access it.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      id:
                        type: integer
                        description: |-
                          The unique ID for an Event's entity.

                          Some Event entities do not have IDs associated with them, so they will not be returned when filtering by ID. These Events include:

                            - `account`
                            - `profile`

                          Entities for some Events are assigned the ID of the Linode they correspond to. When filtering by ID for these Events, use the corresponding Linode's ID. These Events include:

                            - `disks`
                            - `backups`

                          Tag Events use a tag's name for the entity ID field. When filtering by ID for tag Events, supply the name of the tag.
                        example: 11111
                        x-linode-filterable: true
                      type:
                        type: string
                        description: |
                          __Read-only__ The type of entity that is being referenced by the Event.
                        example: ticket
                        readOnly: true
                        enum:
                        - account
                        - backups
                        - community
                        - disks
                        - domain
                        - entity_transfer
                        - firewall
                        - image
                        - ipaddress
                        - linode
                        - longview
                        - loadbalancer
                        - managed_service
                        - nodebalancer
                        - oauth_client
                        - placement_group
                        - profile
                        - stackscript
                        - tag
                        - ticket
                        - token
                        - user
                        - user_ssh_key
                        - volume
                        x-linode-filterable: true
                      label:
                        type: string
                        description: |
                          The current label of this object. The label may reflect changes that occur
                          with this Event.
                        example: Problem booting my Linode
                        x-linode-cli-display: 5
                      url:
                        type: string
                        description: |
                          The URL where you can access the object this Event is for. If a relative URL,
                          it is relative to the domain you retrieved the Event from.
                        example: /v4/support/tickets/11111
                  message:
                    type: string
                    description: |
                      Provides additional information about the event. Additional information may include,
                      but is not limited to, a more detailed representation of events
                      which can help diagnose non-obvious failures.
                    example: None
                    nullable: true
                    x-linode-cli-display: 9
                  percent_complete:
                    type: integer
                    description: |
                      __Read-only__ A percentage estimating the amount of time remaining for an Event. Returns
                      `null` for notification events.
                    example: null
                    readOnly: true
                  rate:
                    type: string
                    description: |
                      __Read-only__ The rate of completion of the Event. Only some Events will return rate;
                      for example, migration and resize Events.
                    example: null
                    readOnly: true
                  read:
                    type: boolean
                    description: |
                      __Read-only__ If this Event has been read.
                    example: true
                    readOnly: true
                    x-linode-filterable: true
                  secondary_entity:
                    type: object
                    description: |
                      __Read-only__ Detailed information about the Event's secondary entity, which provides
                      additional information for events such as, but not limited to,
                      `linode_boot`, `linode_reboot`, `linode_create`, and `linode_clone`
                      Event actions.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      id:
                        type: string
                        description: |
                          The ID of the object that is the secondary entity.
                        example: linode/debian9
                      type:
                        type: string
                        description: |
                          __Read-only__ The type of entity that is being referenced by the Event.
                        example: linode
                        readOnly: true
                      label:
                        type: string
                        description: |
                          The label of this object.
                        example: linode1234
                      url:
                        type: string
                        description: |
                          The URL where you can access the object this Event is for. If a relative URL,
                          it is relative to the domain you retrieved the Event from.
                        example: /v4/linode/instances/1234
                  seen:
                    type: boolean
                    description: |
                      __Read-only__ If this Event has been seen.
                    example: true
                    readOnly: true
                  status:
                    type: string
                    description: |
                      __Read-only__ The current status of this Event.
                    readOnly: true
                    enum:
                    - failed
                    - finished
                    - notification
                    - scheduled
                    - started
                    x-linode-cli-color:
                      default_: white
                      failed: red
                      finished: green
                      started: yellow
                    x-linode-cli-display: 8
                  time_remaining:
                    type: string
                    description: |
                      __Read-only__ The estimated time remaining until the completion of this Event. This value
                      is only returned for some in-progress migration events. For
                      all other in-progress events, the `percent_complete` attribute
                      will indicate about how much more work is to be done.
                    example: null
                    nullable: true
                    readOnly: true
                  username:
                    type: string
                    description: |
                      __Read-only__ The username of the User who caused the Event.
                    example: exampleUser
                    nullable: true
                    readOnly: true
                    x-linode-cli-display: 2
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-event
      security:
      - personalAccessToken: []
      - oauth:
        - events:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/account/events/123
      - lang: CLI
        source: linode-cli events view 123
      x-linode-cli-action: view
      x-linode-grant: read_only
      x-original-op-id: getEvent
      x-original-op-title: Event View
    x-linode-cli-command: events
  /account/events/{eventId}/read:
    x-akamai:
      file-path: paths/read.yaml
      path-info: /account/events/{eventId}/read
    parameters:
    - name: eventId
      in: path
      description: |
        The ID of the Event to designate as read.
      example: '{{eventId}}'
      x-akamai:
        file-path: parameters/event-id-path-625aa248.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-event-read
      summary: Mark an event as read
      tags:
      - Events
      description: |-
        Marks a single Event as read.


        ---


        - __CLI__.

            ```
            linode-cli events mark-read
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            events:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli events mark-read
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: events:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Event read.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-event-read
      security:
      - personalAccessToken: []
      - oauth:
        - events:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST \
              https://api.linode.com/v4/account/events/123/read
      - lang: CLI
        source: linode-cli events mark-read 123
      x-linode-cli-action: mark-read
      x-linode-grant: read_only
      x-original-op-id: eventRead
      x-original-op-title: Event Mark as Read
    x-linode-cli-command: events
  /account/events/{eventId}/seen:
    x-akamai:
      file-path: paths/seen.yaml
      path-info: /account/events/{eventId}/seen
    parameters:
    - name: eventId
      in: path
      description: |
        The ID of the Event to designate as seen.
      example: '{{eventId}}'
      x-akamai:
        file-path: parameters/event-id-path-214cd042.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-event-seen
      summary: Mark an event as seen
      tags:
      - Events
      description: |-
        Marks all Events up to and including this Event by ID as seen.


        ---


        - __CLI__.

            ```
            linode-cli events mark-seen
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            events:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli events mark-seen
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: events:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Events seen.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-event-seen
      security:
      - personalAccessToken: []
      - oauth:
        - events:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST \
              https://api.linode.com/v4/account/events/123/seen
      - lang: CLI
        source: linode-cli events mark-seen 123
      x-linode-cli-action: mark-seen
      x-linode-grant: read_write
      x-original-op-id: eventSeen
      x-original-op-title: Event Mark as Seen
    x-linode-cli-command: events
  /account/invoices:
    x-akamai:
      file-path: paths/invoices.yaml
      path-info: /account/invoices
    get:
      operationId: get-invoices
      summary: List invoices
      tags:
      - Invoices
      description: |-
        Returns a paginated list of Invoices against your Account.


        ---


        - __CLI__.

            ```
            linode-cli account invoices-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli account invoices-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated list of Invoice objects.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        Account Invoice object
                      x-akamai:
                        file-path: schemas/invoice.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ The Invoice's unique ID.
                          example: 123
                          readOnly: true
                          x-linode-cli-display: 1
                        billing_source:
                          type: string
                          description: |-
                            __Read-only__ `akamai`: This Invoice was generated according to the terms of an agreement between the customer and Akamai.

                            `linode`: This Invoice was generated according to the default terms, prices, and discounts.
                          example: linode
                          readOnly: true
                          enum:
                          - akamai
                          - linode
                          x-linode-cli-display: 3.5
                        date:
                          type: string
                          description: |
                            __Read-only__ When this Invoice was generated.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                          x-linode-cli-display: 2
                        label:
                          type: string
                          description: |
                            __Read-only__ The Invoice's display label.
                          example: Invoice
                          readOnly: true
                          x-linode-cli-display: 3
                        subtotal:
                          type: number
                          description: |
                            __Read-only__ The amount of the Invoice before taxes in US Dollars.
                          example: 120.25
                          readOnly: true
                          x-linode-cli-display: 4
                        tax:
                          type: number
                          description: |
                            __Read-only__ The amount of tax levied on the Invoice in US Dollars.
                          example: 12.25
                          readOnly: true
                          x-linode-cli-display: 5
                        tax_summary:
                          type: array
                          description: |
                            __Read-only__ The amount of tax broken down into subtotals by source.
                          readOnly: true
                          items:
                            type: object
                            additionalProperties: false
                            properties:
                              name:
                                type: string
                                description: |
                                  The source of this tax subtotal.
                                example: PA STATE TAX
                              tax:
                                type: number
                                description: |
                                  The amount of tax subtotal attributable to this source.
                                example: 12.25
                        total:
                          type: number
                          description: |
                            __Read-only__ The amount of the Invoice after taxes in US Dollars.
                          example: 132.5
                          readOnly: true
                          x-linode-cli-display: 6
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-invoices
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/account/invoices
      - lang: CLI
        source: linode-cli account invoices-list
      x-linode-cli-action: invoices-list
      x-linode-grant: read_only
      x-original-op-id: getInvoices
      x-original-op-title: Invoices List
    x-linode-cli-command: account
  /account/invoices/{invoiceId}:
    x-akamai:
      file-path: paths/invoice.yaml
      path-info: /account/invoices/{invoiceId}
    parameters:
    - name: invoiceId
      in: path
      description: |
        The ID of the Invoice.
      example: '{{invoiceId}}'
      x-akamai:
        file-path: parameters/invoice-id-path.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-invoice
      summary: Get an invoice
      tags:
      - Invoices
      description: |-
        Returns a single Invoice object.


        ---


        - __CLI__.

            ```
            linode-cli account invoice-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli account invoice-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            An Invoice object
          content:
            application/json:
              schema:
                type: object
                description: |
                  Account Invoice object
                x-akamai:
                  file-path: schemas/invoice.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The Invoice's unique ID.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                  billing_source:
                    type: string
                    description: |-
                      __Read-only__ `akamai`: This Invoice was generated according to the terms of an agreement between the customer and Akamai.

                      `linode`: This Invoice was generated according to the default terms, prices, and discounts.
                    example: linode
                    readOnly: true
                    enum:
                    - akamai
                    - linode
                    x-linode-cli-display: 3.5
                  date:
                    type: string
                    description: |
                      __Read-only__ When this Invoice was generated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 2
                  label:
                    type: string
                    description: |
                      __Read-only__ The Invoice's display label.
                    example: Invoice
                    readOnly: true
                    x-linode-cli-display: 3
                  subtotal:
                    type: number
                    description: |
                      __Read-only__ The amount of the Invoice before taxes in US Dollars.
                    example: 120.25
                    readOnly: true
                    x-linode-cli-display: 4
                  tax:
                    type: number
                    description: |
                      __Read-only__ The amount of tax levied on the Invoice in US Dollars.
                    example: 12.25
                    readOnly: true
                    x-linode-cli-display: 5
                  tax_summary:
                    type: array
                    description: |
                      __Read-only__ The amount of tax broken down into subtotals by source.
                    readOnly: true
                    items:
                      type: object
                      additionalProperties: false
                      properties:
                        name:
                          type: string
                          description: |
                            The source of this tax subtotal.
                          example: PA STATE TAX
                        tax:
                          type: number
                          description: |
                            The amount of tax subtotal attributable to this source.
                          example: 12.25
                  total:
                    type: number
                    description: |
                      __Read-only__ The amount of the Invoice after taxes in US Dollars.
                    example: 132.5
                    readOnly: true
                    x-linode-cli-display: 6
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-invoice
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/account/invoices/123
      - lang: CLI
        source: linode-cli account invoice-view 123
      x-linode-cli-action: invoice-view
      x-linode-grant: read_only
      x-original-op-id: getInvoice
      x-original-op-title: Invoice View
    x-linode-cli-command: account
  /account/invoices/{invoiceId}/items:
    x-akamai:
      file-path: paths/items.yaml
      path-info: /account/invoices/{invoiceId}/items
    parameters:
    - name: invoiceId
      in: path
      description: |
        The ID of the Invoice.
      example: '{{invoiceId}}'
      x-akamai:
        file-path: parameters/invoice-id-path.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-invoice-items
      summary: List invoice items
      tags:
      - Invoices
      description: |-
        Returns a paginated list of Invoice items.


        ---


        - __CLI__.

            ```
            linode-cli account invoice-items
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli account invoice-items
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of InvoiceItem objects
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        An InvoiceItem object.
                      x-akamai:
                        file-path: schemas/invoice-item.yaml
                      additionalProperties: false
                      properties:
                        type:
                          type: string
                          description: |
                            __Read-only__ The type of service, ether `hourly` or `misc`.
                          example: hourly
                          readOnly: true
                          enum:
                          - hourly
                          - misc
                        amount:
                          type: number
                          description: |
                            __Read-only__ The price, in US dollars, of the Invoice Item. Equal to
                            the unit price multiplied by quantity.
                          example: 20.2
                          readOnly: true
                          x-linode-cli-display: 4
                        from:
                          type: string
                          description: |
                            __Read-only__ The date the Invoice Item started, based on month.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                          x-linode-cli-display: 2
                        label:
                          type: string
                          description: |
                            __Read-only__ The Invoice Item's display label.
                          example: Linode 123
                          readOnly: true
                          x-linode-cli-display: 1
                        quantity:
                          type: integer
                          description: |
                            __Read-only__ The quantity of this Item for the specified Invoice.
                          example: 4
                          readOnly: true
                        region:
                          type: string
                          description: |-
                            __Read-only__ The ID of the applicable Region associated with this Invoice Item.

                            `null` if there is no applicable Region.
                          example: us-west
                          nullable: true
                          readOnly: true
                          x-linode-cli-display: 7
                        tax:
                          type: number
                          description: |
                            __Read-only__ The amount of tax levied on this Item in US Dollars.
                          example: 1.25
                          readOnly: true
                          x-linode-cli-display: 5
                        to:
                          type: string
                          description: |
                            __Read-only__ The date the Invoice Item ended, based on month.
                          example: '2018-01-31T11:59:59'
                          readOnly: true
                          format: date-time
                          x-linode-cli-display: 3
                        total:
                          type: number
                          description: |
                            __Read-only__ The price of this Item after taxes in US Dollars.
                          example: 21.45
                          readOnly: true
                          x-linode-cli-display: 6
                        unit_price:
                          type: string
                          description: |
                            __Read-only__ The monthly service fee in US Dollars for this Item.
                          example: 5.05
                          readOnly: true
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-invoice-items
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/account/invoices/123/items
      - lang: CLI
        source: linode-cli account invoice-items 123
      x-linode-cli-action: invoice-items
      x-linode-grant: read_only
      x-original-op-id: getInvoiceItems
      x-original-op-title: Invoice Items List
    x-linode-cli-command: account
  /account/logins:
    x-akamai:
      file-path: paths/account-logins.yaml
      path-info: /account/logins
    get:
      operationId: get-account-logins
      summary: List user logins
      tags:
      - Logins
      description: |-
        Returns a collection of successful logins for all users on the account during the last 90 days. This operation can only be accessed by the unrestricted users of an account.


        ---


        - __CLI__.

            ```
            linode-cli account logins-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli account logins-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A collection of successful logins for all users on the account during the last 90 days.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        An object representing a previous successful login for a User.
                      x-akamai:
                        file-path: schemas/login.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ The unique ID of this login object.
                          example: 1234
                          readOnly: true
                          x-linode-cli-display: 1
                        datetime:
                          type: string
                          description: |
                            __Read-only__ When the login was initiated.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                          x-linode-cli-display: 2
                        ip:
                          type: string
                          description: |
                            __Read-only__ The remote IP address that requested the login.
                          example: 192.0.2.0
                          readOnly: true
                          format: ip
                          x-linode-cli-display: 3
                        restricted:
                          type: boolean
                          description: |
                            __Read-only__ True if the User that attempted the login was a restricted
                            User, false otherwise.
                          example: true
                          readOnly: true
                          x-linode-cli-display: 6
                        status:
                          type: string
                          description: |
                            __Read-only__ Whether the login attempt succeeded or failed.
                          example: successful
                          readOnly: true
                          enum:
                          - successful
                          - failed
                          x-linode-cli-display: 5
                        username:
                          type: string
                          description: |
                            __Read-only__ The username of the User that attempted the login.
                          example: example_user
                          readOnly: true
                          x-linode-cli-display: 4
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-account-logins
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/account/logins
      - lang: CLI
        source: linode-cli account logins-list
      x-linode-cli-action: logins-list
      x-original-op-id: getAccountLogins
      x-original-op-title: User Logins List All
    x-linode-cli-command: account
  /account/logins/{loginId}:
    x-akamai:
      file-path: paths/account-login.yaml
      path-info: /account/logins/{loginId}
    parameters:
    - name: loginId
      in: path
      description: |
        The ID of the login object to access.
      example: '{{loginId}}'
      x-akamai:
        file-path: parameters/login-id-path.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-account-login
      summary: Get an account login
      tags:
      - Logins
      description: |-
        Returns a Login object that displays information about a successful login. The logins that can be viewed can be for any user on the account, and are not limited to only the logins of the user that is accessing this API endpoint. This operation can only be accessed by the unrestricted users of the account.


        ---


        - __CLI__.

            ```
            linode-cli account login-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli account login-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The requested login object.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An object representing a previous successful login for a User.
                x-akamai:
                  file-path: schemas/login.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of this login object.
                    example: 1234
                    readOnly: true
                    x-linode-cli-display: 1
                  datetime:
                    type: string
                    description: |
                      __Read-only__ When the login was initiated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 2
                  ip:
                    type: string
                    description: |
                      __Read-only__ The remote IP address that requested the login.
                    example: 192.0.2.0
                    readOnly: true
                    format: ip
                    x-linode-cli-display: 3
                  restricted:
                    type: boolean
                    description: |
                      __Read-only__ True if the User that attempted the login was a restricted User,
                      false otherwise.
                    example: true
                    readOnly: true
                    x-linode-cli-display: 6
                  status:
                    type: string
                    description: |
                      __Read-only__ Whether the login attempt succeeded or failed.
                    example: successful
                    readOnly: true
                    enum:
                    - successful
                    - failed
                    x-linode-cli-display: 5
                  username:
                    type: string
                    description: |
                      __Read-only__ The username of the User that attempted the login.
                    example: example_user
                    readOnly: true
                    x-linode-cli-display: 4
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-account-login
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/account/logins/1234
      - lang: CLI
        source: linode-cli account login-view 1234
      x-linode-cli-action: login-view
      x-original-op-id: getAccountLogin
      x-original-op-title: Login View
    x-linode-cli-command: account
  /account/maintenance:
    x-akamai:
      file-path: paths/maintenance.yaml
      path-info: /account/maintenance
    get:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: get-maintenance
      summary: List maintenances
      tags:
      - Maintenances
      description: |-
        Returns a collection of Maintenance objects for any entity a user has permissions to view. Canceled Maintenance objects are not returned.

        Currently, Linodes are the only entities available for viewing.
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated list of Maintenance objects.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        Information about maintenance affecting an entity.
                      x-akamai:
                        file-path: schemas/maintenance.yaml
                      additionalProperties: false
                      properties:
                        type:
                          type: string
                          description: |
                            The type of maintenance.
                          example: reboot
                          enum:
                          - reboot
                          - cold_migration
                          - live_migration
                          x-linode-filterable: true
                        entity:
                          type: object
                          description: |
                            The entity being affected by maintenance.
                          additionalProperties: false
                          properties:
                            id:
                              type: number
                              description: |
                                The id of the entity being affected by maintenance.
                              example: 1234
                            type:
                              type: string
                              description: |
                                The type of entity.
                              example: Linode
                            label:
                              type: string
                              description: |
                                The label of the entity being affected by maintenance.
                              example: demo-linode
                            url:
                              type: string
                              description: |
                                The API endpoint prefix to use in combination with the entity id to
                                find specific information about the entity.
                              example: https://api.linode.com/v4/linode/instances/{linodeId}
                        reason:
                          type: string
                          description: |
                            The reason maintenance is being performed.
                          example: This maintenance will allow us to update the BIOS on the host's motherboard.
                        status:
                          type: string
                          description: |-
                            The maintenance status.

                            Maintenance progresses in the following sequence: pending, started, then completed.
                          example: started
                          enum:
                          - completed
                          - pending
                          - started
                          x-linode-filterable: true
                        when:
                          type: string
                          description: |-
                            When the maintenance will begin.

                            [Filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) with the following parameters:

                            - A single value in date-time string format ("%Y-%m-%dT%H:%M:%S"), which returns only matches to that value.

                            - A dictionary containing pairs of inequality operator string keys ("+or", "+gt", "+gte", "+lt", "+lte",
                            or "+neq") and single date-time string format values ("%Y-%m-%dT%H:%M:%S"). "+or" accepts an array of values that
                            may consist of single date-time strings or dictionaries of inequality operator pairs.
                          example: '2020-07-09T00:01:01'
                          format: date-time
                          x-linode-filterable: true
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-maintenance
      security:
      - personalAccessToken: []
      - oauth: []
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/account/maintenance
      - lang: CLI
        source: linode-cli account maintenance-list
      x-linode-cli-action: maintenance-list
      x-linode-grant: read_only
      x-original-op-id: getMaintenance
      x-original-op-title: Maintenance List
    x-linode-cli-command: account
  /account/notifications:
    x-akamai:
      file-path: paths/notifications.yaml
      path-info: /account/notifications
    get:
      operationId: get-notifications
      summary: List notifications
      tags:
      - Notifications
      description: |-
        Returns a collection of Notification objects representing important, often time-sensitive items related to your Account. You cannot interact directly with Notifications, and a Notification will disappear when the circumstances causing it have been resolved. For example, if you have an important Ticket open, you must respond to the Ticket to dismiss the Notification.


        ---


        - __CLI__.

            ```
            linode-cli account notifications-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli account notifications-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated list of Notification objects.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        An important, often time-sensitive item related to your Account.
                      x-akamai:
                        file-path: schemas/notification.yaml
                      additionalProperties: false
                      properties:
                        type:
                          type: string
                          description: |
                            __Read-only__ The type of Notification this is.
                          example: ticket_important
                          readOnly: true
                          enum:
                          - migration_scheduled
                          - migration_imminent
                          - migration_pending
                          - reboot_scheduled
                          - outage
                          - payment_due
                          - ticket_important
                          - ticket_abuse
                          - notice
                          - maintenance
                          - promotion
                        body:
                          type: string
                          description: |
                            __Read-only__ A full description of this Notification, in markdown format.  Not
                            all Notifications include bodies.
                          example: null
                          nullable: true
                          readOnly: true
                        entity:
                          type: object
                          description: |
                            __Read-only__ Detailed information about the Notification.
                          additionalProperties: false
                          readOnly: true
                          properties:
                            id:
                              type: integer
                              description: |-
                                The unique ID of the Notification's entity, based on the entity type.

                                Returns `null` for the following entity types:

                                - `account`
                                - `promotion`
                              example: 3456
                              nullable: true
                            type:
                              type: string
                              description: |
                                The type of entity this is related to.
                              example: ticket
                              enum:
                              - account
                              - entity_transfer
                              - linode
                              - loadbalancers
                              - nodebalancer
                              - promotion
                              - region
                              - ticket
                              - volume
                            label:
                              type: string
                              description: |-
                                The current label for this Notification's entity.

                                Returns `null` for the following entity types:

                                - `entity_transfer`
                                - `promotion`
                                - `region`
                              example: Linode not booting.
                              nullable: true
                            url:
                              type: string
                              description: |-
                                The URL where you can access the object this Notification is for. If a relative URL, it is relative to the domain you retrieved the Notification from.

                                Returns `null` for the `promotion` entity type.
                              example: /support/tickets/3456
                              nullable: true
                        label:
                          type: string
                          description: |
                            __Read-only__ A short description of this Notification.
                          example: You have an important ticket open!
                          readOnly: true
                          x-linode-cli-display: 1
                        message:
                          type: string
                          description: |
                            __Read-only__ A human-readable description of the Notification.
                          example: You have an important ticket open!
                          readOnly: true
                          x-linode-cli-display: 2
                        severity:
                          type: string
                          description: |
                            __Read-only__ The severity of this Notification.  This field can be
                            used to decide how prominently to display the Notification,
                            what color to make the display text, etc.
                          example: major
                          readOnly: true
                          enum:
                          - minor
                          - major
                          - critical
                          x-linode-cli-color:
                            critical: b
                            default_: white
                            minor: blue
                          x-linode-cli-display: 3
                        until:
                          type: string
                          description: |
                            __Read-only__ If this Notification has a duration, this will be the
                            ending time for the Event/action. For example, if there
                            is scheduled maintenance for one of our systems, `until`
                            would be set to the end of the maintenance window.
                          example: null
                          readOnly: true
                          format: date-time
                          x-linode-cli-color:
                            None: black
                            default_: white
                          x-linode-cli-display: 5
                        when:
                          type: string
                          description: |
                            __Read-only__ If this Notification is of an Event that will happen at
                            a fixed, future time, this is when the named action will
                            be taken. For example, if a Linode is to be migrated in
                            response to a Security Advisory, this field will contain
                            the approximate time the Linode will be taken offline
                            for migration.
                          example: null
                          readOnly: true
                          format: date-time
                          x-linode-cli-color:
                            None: black
                            default_: white
                          x-linode-cli-display: 4
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-notifications
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/account/notifications
      - lang: CLI
        source: linode-cli account notifications-list
      x-linode-cli-action: notifications-list
      x-linode-grant: read_only
      x-original-op-id: getNotifications
      x-original-op-title: Notifications List
    x-linode-cli-command: account
  /account/oauth-clients:
    x-akamai:
      file-path: paths/oauth-clients.yaml
      path-info: /account/oauth-clients
    post:
      operationId: post-client
      summary: Create an OAuth client
      tags:
      - OAuth clients
      description: |-
        Creates an OAuth Client, which can be used to allow users (using their Linode account) to log in to your own application, and optionally grant your application some amount of access to their Linodes or other entities.


        ---


        - __CLI__.

            ```
            linode-cli account client-create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli account client-create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the OAuth Client to create.
        content:
          application/json:
            schema:
              required:
              - label
              - redirect_uri
              allOf:
              - type: object
                description: |
                  A third-party application registered to Linode that users may log into with their Linode account
                  through our authentication server at [login.linode.com](https://login.linode.com).  Using
                  an OAuth Client, a third-party developer may be given access to
                  some, or all, of a User's account for the purposes of their application.
                x-akamai:
                  file-path: schemas/oauth-client.yaml
                additionalProperties: false
                properties:
                  id:
                    type: string
                    description: |
                      __Read-only__ The OAuth Client ID.  This is used to identify the client, and is a publicly-known
                      value (it is not a secret).
                    example: 2737bf16b39ab5d7b4a1
                    readOnly: true
                    x-linode-cli-display: 1
                  label:
                    type: string
                    description: |
                      The name of this application.  This will be presented to users when they are asked to grant it access
                      to their Account.
                    example: Test_Client_1
                    minLength: 1
                    maxLength: 512
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  public:
                    type: boolean
                    description: |
                      If this is a public or private OAuth Client.  Public clients have a slightly different
                      authentication workflow than private clients.  See the [OAuth
                      spec](https://oauth.net/2/) for more details.
                    example: false
                    default: false
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  redirect_uri:
                    type: string
                    description: |
                      The location a successful log in from [login.linode.com](https://login.linode.com) should
                      be redirected to for this client.  The receiver of this redirect
                      should be ready to accept an OAuth exchange code and finish
                      the OAuth exchange.
                    example: https://example.org/oauth/callback
                    format: url
                    x-linode-cli-display: 5
                  secret:
                    type: string
                    description: |
                      __Read-only__ The OAuth Client secret, used in the OAuth exchange.  This is returned
                      as `<REDACTED>` except when an OAuth Client is created or its
                      secret is reset.  This is a secret, and should not be shared
                      or disclosed publicly.
                    example: <REDACTED>
                    readOnly: true
                  status:
                    type: string
                    description: |
                      __Read-only__ The status of this application.  `active` by default.
                    example: active
                    readOnly: true
                    enum:
                    - active
                    - disabled
                    - suspended
                    x-linode-cli-color:
                      default_: white
                      suspended: red
                    x-linode-cli-display: 3
                  thumbnail_url:
                    type: string
                    description: |
                      __Read-only__ The URL where this client's thumbnail may be viewed, or `null` if this
                      client does not have a thumbnail set.
                    example: https://api.linode.com/v4/account/clients/2737bf16b39ab5d7b4a1/thumbnail
                    nullable: true
                    readOnly: true
                    format: url
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Client created successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A third-party application registered to Linode that users may log into with their Linode account
                  through our authentication server at [login.linode.com](https://login.linode.com).  Using
                  an OAuth Client, a third-party developer may be given access to
                  some, or all, of a User's account for the purposes of their application.
                x-akamai:
                  file-path: schemas/oauth-client.yaml
                additionalProperties: false
                properties:
                  id:
                    type: string
                    description: |
                      __Read-only__ The OAuth Client ID.  This is used to identify the client, and is a publicly-known
                      value (it is not a secret).
                    example: 2737bf16b39ab5d7b4a1
                    readOnly: true
                    x-linode-cli-display: 1
                  label:
                    type: string
                    description: |
                      The name of this application.  This will be presented to users when they are asked to grant it access
                      to their Account.
                    example: Test_Client_1
                    minLength: 1
                    maxLength: 512
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  public:
                    type: boolean
                    description: |
                      If this is a public or private OAuth Client.  Public clients have a slightly different
                      authentication workflow than private clients.  See the [OAuth
                      spec](https://oauth.net/2/) for more details.
                    example: false
                    default: false
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  redirect_uri:
                    type: string
                    description: |
                      The location a successful log in from [login.linode.com](https://login.linode.com) should
                      be redirected to for this client.  The receiver of this redirect
                      should be ready to accept an OAuth exchange code and finish
                      the OAuth exchange.
                    example: https://example.org/oauth/callback
                    format: url
                    x-linode-cli-display: 5
                  secret:
                    type: string
                    description: |
                      __Read-only__ The OAuth Client secret, used in the OAuth exchange.  This is returned
                      as `<REDACTED>` except when an OAuth Client is created or its
                      secret is reset.  This is a secret, and should not be shared
                      or disclosed publicly.
                    example: <REDACTED>
                    readOnly: true
                  status:
                    type: string
                    description: |
                      __Read-only__ The status of this application.  `active` by default.
                    example: active
                    readOnly: true
                    enum:
                    - active
                    - disabled
                    - suspended
                    x-linode-cli-color:
                      default_: white
                      suspended: red
                    x-linode-cli-display: 3
                  thumbnail_url:
                    type: string
                    description: |
                      __Read-only__ The URL where this client's thumbnail may be viewed, or `null` if this
                      client does not have a thumbnail set.
                    example: https://api.linode.com/v4/account/clients/2737bf16b39ab5d7b4a1/thumbnail
                    nullable: true
                    readOnly: true
                    format: url
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-client
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                  "redirect_uri": "https://example.org/oauth/callback",
                  "label": "Test_Client_1",
                  "public": false
              }' \
              https://api.linode.com/v4/account/oauth-clients
      - lang: CLI
        source: |-
          linode-cli account client-create \
            --label Test_Client_1 \
            --redirect_uri https://example.org/callback
      x-linode-cli-action: client-create
      x-original-op-id: createClient
      x-original-op-title: OAuth Client Create
    get:
      operationId: get-clients
      summary: List OAuth clients
      tags:
      - OAuth clients
      description: |-
        Returns a paginated list of OAuth Clients registered to your Account.  OAuth Clients allow users to log into applications you write or host using their Linode Account, and may allow them to grant some level of access to their Linodes or other entities to your application.


        ---


        - __CLI__.

            ```
            linode-cli account clients-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli account clients-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of OAuth Clients.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        A third-party application registered to Linode that users may log into with their Linode
                        account through our authentication server at [login.linode.com](https://login.linode.com).  Using
                        an OAuth Client, a third-party developer may be given access
                        to some, or all, of a User's account for the purposes of their
                        application.
                      x-akamai:
                        file-path: schemas/oauth-client.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: string
                          description: |
                            __Read-only__ The OAuth Client ID.  This is used to identify the client,
                            and is a publicly-known value (it is not a secret).
                          example: 2737bf16b39ab5d7b4a1
                          readOnly: true
                          x-linode-cli-display: 1
                        label:
                          type: string
                          description: |
                            The name of this application.  This will be presented to users when they
                            are asked to grant it access to their Account.
                          example: Test_Client_1
                          minLength: 1
                          maxLength: 512
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        public:
                          type: boolean
                          description: |
                            If this is a public or private OAuth Client.  Public clients have a
                            slightly different authentication workflow than private
                            clients.  See the [OAuth spec](https://oauth.net/2/) for
                            more details.
                          example: false
                          default: false
                          x-linode-cli-display: 4
                          x-linode-filterable: true
                        redirect_uri:
                          type: string
                          description: |
                            The location a successful log in from [login.linode.com](https://login.linode.com)
                            should be redirected to for this client.  The receiver
                            of this redirect should be ready to accept an OAuth exchange
                            code and finish the OAuth exchange.
                          example: https://example.org/oauth/callback
                          format: url
                          x-linode-cli-display: 5
                        secret:
                          type: string
                          description: |
                            __Read-only__ The OAuth Client secret, used in the OAuth exchange.  This
                            is returned as `<REDACTED>` except when an OAuth Client
                            is created or its secret is reset.  This is a secret,
                            and should not be shared or disclosed publicly.
                          example: <REDACTED>
                          readOnly: true
                        status:
                          type: string
                          description: |
                            __Read-only__ The status of this application.  `active` by default.
                          example: active
                          readOnly: true
                          enum:
                          - active
                          - disabled
                          - suspended
                          x-linode-cli-color:
                            default_: white
                            suspended: red
                          x-linode-cli-display: 3
                        thumbnail_url:
                          type: string
                          description: |
                            __Read-only__ The URL where this client's thumbnail may be viewed, or
                            `null` if this client does not have a thumbnail set.
                          example: https://api.linode.com/v4/account/clients/2737bf16b39ab5d7b4a1/thumbnail
                          nullable: true
                          readOnly: true
                          format: url
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-clients
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/account/oauth-clients
      - lang: CLI
        source: linode-cli account clients-list
      x-linode-cli-action: clients-list
      x-linode-grant: read_only
      x-original-op-id: getClients
      x-original-op-title: OAuth Clients List
    x-linode-cli-command: account
  /account/oauth-clients/{clientId}:
    x-akamai:
      file-path: paths/oauth-client.yaml
      path-info: /account/oauth-clients/{clientId}
    parameters:
    - name: clientId
      in: path
      description: |
        The OAuth Client ID to look up.
      example: '{{clientId}}'
      x-akamai:
        file-path: parameters/client-id-path.yaml
      required: true
      schema:
        type: string
    get:
      operationId: get-client
      summary: Get an OAuth client
      tags:
      - OAuth clients
      description: |-
        Returns information about a single OAuth client.


        ---


        - __CLI__.

            ```
            linode-cli account client-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli account client-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Information about the requested client.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A third-party application registered to Linode that users may log into with their Linode account
                  through our authentication server at [login.linode.com](https://login.linode.com).  Using
                  an OAuth Client, a third-party developer may be given access to
                  some, or all, of a User's account for the purposes of their application.
                x-akamai:
                  file-path: schemas/oauth-client.yaml
                additionalProperties: false
                properties:
                  id:
                    type: string
                    description: |
                      __Read-only__ The OAuth Client ID.  This is used to identify the client, and is a publicly-known
                      value (it is not a secret).
                    example: 2737bf16b39ab5d7b4a1
                    readOnly: true
                    x-linode-cli-display: 1
                  label:
                    type: string
                    description: |
                      The name of this application.  This will be presented to users when they are asked to grant it access
                      to their Account.
                    example: Test_Client_1
                    minLength: 1
                    maxLength: 512
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  public:
                    type: boolean
                    description: |
                      If this is a public or private OAuth Client.  Public clients have a slightly different
                      authentication workflow than private clients.  See the [OAuth
                      spec](https://oauth.net/2/) for more details.
                    example: false
                    default: false
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  redirect_uri:
                    type: string
                    description: |
                      The location a successful log in from [login.linode.com](https://login.linode.com) should
                      be redirected to for this client.  The receiver of this redirect
                      should be ready to accept an OAuth exchange code and finish
                      the OAuth exchange.
                    example: https://example.org/oauth/callback
                    format: url
                    x-linode-cli-display: 5
                  secret:
                    type: string
                    description: |
                      __Read-only__ The OAuth Client secret, used in the OAuth exchange.  This is returned
                      as `<REDACTED>` except when an OAuth Client is created or its
                      secret is reset.  This is a secret, and should not be shared
                      or disclosed publicly.
                    example: <REDACTED>
                    readOnly: true
                  status:
                    type: string
                    description: |
                      __Read-only__ The status of this application.  `active` by default.
                    example: active
                    readOnly: true
                    enum:
                    - active
                    - disabled
                    - suspended
                    x-linode-cli-color:
                      default_: white
                      suspended: red
                    x-linode-cli-display: 3
                  thumbnail_url:
                    type: string
                    description: |
                      __Read-only__ The URL where this client's thumbnail may be viewed, or `null` if this
                      client does not have a thumbnail set.
                    example: https://api.linode.com/v4/account/clients/2737bf16b39ab5d7b4a1/thumbnail
                    nullable: true
                    readOnly: true
                    format: url
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-client
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/account/oauth-clients/edc6790ea9db4d224c5c
      - lang: CLI
        source: |-
          linode-cli account client-view \
            edc6790ea9db4d224c5c
      x-linode-cli-action: client-view
      x-original-op-id: getClient
      x-original-op-title: OAuth Client View
    put:
      operationId: put-client
      summary: Update an OAuth client
      tags:
      - OAuth clients
      description: |-
        Update information about an OAuth Client on your Account. This can be especially useful to update the `redirect_uri` of your client in the event that the callback url changed in your application.


        ---


        - __CLI__.

            ```
            linode-cli account client-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli account client-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The fields to update.
        content:
          application/json:
            schema:
              type: object
              description: |
                A third-party application registered to Linode that users may log into with their Linode account
                through our authentication server at [login.linode.com](https://login.linode.com).  Using
                an OAuth Client, a third-party developer may be given access to some,
                or all, of a User's account for the purposes of their application.
              x-akamai:
                file-path: schemas/oauth-client.yaml
              additionalProperties: false
              properties:
                id:
                  type: string
                  description: |
                    __Read-only__ The OAuth Client ID.  This is used to identify the client, and is a publicly-known
                    value (it is not a secret).
                  example: '{{id}}'
                  readOnly: true
                  x-linode-cli-display: 1
                label:
                  type: string
                  description: |
                    The name of this application.  This will be presented to users when they are asked to grant it access
                    to their Account.
                  example: '{{label}}'
                  minLength: 1
                  maxLength: 512
                  x-linode-cli-display: 2
                  x-linode-filterable: true
                public:
                  type: boolean
                  description: |
                    If this is a public or private OAuth Client.  Public clients have a slightly different
                    authentication workflow than private clients.  See the [OAuth
                    spec](https://oauth.net/2/) for more details.
                  example: '{{public}}'
                  default: false
                  x-linode-cli-display: 4
                  x-linode-filterable: true
                redirect_uri:
                  type: string
                  description: |
                    The location a successful log in from [login.linode.com](https://login.linode.com) should
                    be redirected to for this client.  The receiver of this redirect
                    should be ready to accept an OAuth exchange code and finish the
                    OAuth exchange.
                  example: '{{redirect_uri}}'
                  format: url
                  x-linode-cli-display: 5
                secret:
                  type: string
                  description: |
                    __Read-only__ The OAuth Client secret, used in the OAuth exchange.  This is returned as
                    `<REDACTED>` except when an OAuth Client is created or its secret
                    is reset.  This is a secret, and should not be shared or disclosed
                    publicly.
                  example: '{{secret}}'
                  readOnly: true
                status:
                  type: string
                  description: |
                    __Read-only__ The status of this application.  `active` by default.
                  example: '{{status}}'
                  readOnly: true
                  enum:
                  - active
                  - disabled
                  - suspended
                  x-linode-cli-color:
                    default_: white
                    suspended: red
                  x-linode-cli-display: 3
                thumbnail_url:
                  type: string
                  description: |
                    __Read-only__ The URL where this client's thumbnail may be viewed, or `null` if this
                    client does not have a thumbnail set.
                  example: '{{thumbnail_url}}'
                  nullable: true
                  readOnly: true
                  format: url
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Client updated successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A third-party application registered to Linode that users may log into with their Linode account
                  through our authentication server at [login.linode.com](https://login.linode.com).  Using
                  an OAuth Client, a third-party developer may be given access to
                  some, or all, of a User's account for the purposes of their application.
                x-akamai:
                  file-path: schemas/oauth-client.yaml
                additionalProperties: false
                properties:
                  id:
                    type: string
                    description: |
                      __Read-only__ The OAuth Client ID.  This is used to identify the client, and is a publicly-known
                      value (it is not a secret).
                    example: 2737bf16b39ab5d7b4a1
                    readOnly: true
                    x-linode-cli-display: 1
                  label:
                    type: string
                    description: |
                      The name of this application.  This will be presented to users when they are asked to grant it access
                      to their Account.
                    example: Test_Client_1
                    minLength: 1
                    maxLength: 512
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  public:
                    type: boolean
                    description: |
                      If this is a public or private OAuth Client.  Public clients have a slightly different
                      authentication workflow than private clients.  See the [OAuth
                      spec](https://oauth.net/2/) for more details.
                    example: false
                    default: false
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  redirect_uri:
                    type: string
                    description: |
                      The location a successful log in from [login.linode.com](https://login.linode.com) should
                      be redirected to for this client.  The receiver of this redirect
                      should be ready to accept an OAuth exchange code and finish
                      the OAuth exchange.
                    example: https://example.org/oauth/callback
                    format: url
                    x-linode-cli-display: 5
                  secret:
                    type: string
                    description: |
                      __Read-only__ The OAuth Client secret, used in the OAuth exchange.  This is returned
                      as `<REDACTED>` except when an OAuth Client is created or its
                      secret is reset.  This is a secret, and should not be shared
                      or disclosed publicly.
                    example: <REDACTED>
                    readOnly: true
                  status:
                    type: string
                    description: |
                      __Read-only__ The status of this application.  `active` by default.
                    example: active
                    readOnly: true
                    enum:
                    - active
                    - disabled
                    - suspended
                    x-linode-cli-color:
                      default_: white
                      suspended: red
                    x-linode-cli-display: 3
                  thumbnail_url:
                    type: string
                    description: |
                      __Read-only__ The URL where this client's thumbnail may be viewed, or `null` if this
                      client does not have a thumbnail set.
                    example: https://api.linode.com/v4/account/clients/2737bf16b39ab5d7b4a1/thumbnail
                    nullable: true
                    readOnly: true
                    format: url
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-client
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "redirect_uri": "https://example.org/oauth/callback",
                "label": "Test_Client_1"
                }
              }' \
              https://api.linode.com/v4/account/oauth-clients/edc6790ea9db4d224c5c
      - lang: CLI
        source: |-
          linode-cli account client-update \
            edc6790ea9db4d224c5c \
            --label Test_Client_1
      x-linode-cli-action: client-update
      x-original-op-id: updateClient
      x-original-op-title: OAuth Client Update
    delete:
      operationId: delete-client
      summary: Delete an OAuth client
      tags:
      - OAuth clients
      description: |-
        Deletes an OAuth Client registered with Linode. The Client ID and Client secret will no longer be accepted by [login.linode.com](https://login.linode.com), and all tokens issued to this client will be invalidated (meaning that if your application was using a token, it will no longer work).


        ---


        - __CLI__.

            ```
            linode-cli account client-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli account client-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Client deleted successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-client
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/account/oauth-clients/edc6790ea9db4d224c5c
      - lang: CLI
        source: |-
          linode-cli account client-delete \
            edc6790ea9db4d224c5c
      x-linode-cli-action: client-delete
      x-original-op-id: deleteClient
      x-original-op-title: OAuth Client Delete
    x-linode-cli-command: account
  /account/oauth-clients/{clientId}/reset-secret:
    x-akamai:
      file-path: paths/reset-secret.yaml
      path-info: /account/oauth-clients/{clientId}/reset-secret
    parameters:
    - name: clientId
      in: path
      description: |
        The OAuth Client ID to look up.
      example: '{{clientId}}'
      x-akamai:
        file-path: parameters/client-id-path.yaml
      required: true
      schema:
        type: string
    post:
      operationId: post-reset-client-secret
      summary: Reset an OAuth client secret
      tags:
      - OAuth clients
      description: |-
        Resets the OAuth Client secret for a client you own, and returns the OAuth Client with the plaintext secret. This secret is not supposed to be publicly known or disclosed anywhere. This can be used to generate a new secret in case the one you have has been leaked, or to get a new secret if you lost the original. The old secret is expired immediately, and logins to your client with the old secret will fail.


        ---


        - __CLI__.

            ```
            linode-cli account client-reset-secret
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli account client-reset-secret
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Client secret reset successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A third-party application registered to Linode that users may log into with their Linode account
                  through our authentication server at [login.linode.com](https://login.linode.com).  Using
                  an OAuth Client, a third-party developer may be given access to
                  some, or all, of a User's account for the purposes of their application.
                x-akamai:
                  file-path: schemas/oauth-client.yaml
                additionalProperties: false
                properties:
                  id:
                    type: string
                    description: |
                      __Read-only__ The OAuth Client ID.  This is used to identify the client, and is a publicly-known
                      value (it is not a secret).
                    example: 2737bf16b39ab5d7b4a1
                    readOnly: true
                    x-linode-cli-display: 1
                  label:
                    type: string
                    description: |
                      The name of this application.  This will be presented to users when they are asked to grant it access
                      to their Account.
                    example: Test_Client_1
                    minLength: 1
                    maxLength: 512
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  public:
                    type: boolean
                    description: |
                      If this is a public or private OAuth Client.  Public clients have a slightly different
                      authentication workflow than private clients.  See the [OAuth
                      spec](https://oauth.net/2/) for more details.
                    example: false
                    default: false
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  redirect_uri:
                    type: string
                    description: |
                      The location a successful log in from [login.linode.com](https://login.linode.com) should
                      be redirected to for this client.  The receiver of this redirect
                      should be ready to accept an OAuth exchange code and finish
                      the OAuth exchange.
                    example: https://example.org/oauth/callback
                    format: url
                    x-linode-cli-display: 5
                  secret:
                    type: string
                    description: |
                      __Read-only__ The OAuth Client secret, used in the OAuth exchange.  This is returned
                      as `<REDACTED>` except when an OAuth Client is created or its
                      secret is reset.  This is a secret, and should not be shared
                      or disclosed publicly.
                    example: <REDACTED>
                    readOnly: true
                  status:
                    type: string
                    description: |
                      __Read-only__ The status of this application.  `active` by default.
                    example: active
                    readOnly: true
                    enum:
                    - active
                    - disabled
                    - suspended
                    x-linode-cli-color:
                      default_: white
                      suspended: red
                    x-linode-cli-display: 3
                  thumbnail_url:
                    type: string
                    description: |
                      __Read-only__ The URL where this client's thumbnail may be viewed, or `null` if this
                      client does not have a thumbnail set.
                    example: https://api.linode.com/v4/account/clients/2737bf16b39ab5d7b4a1/thumbnail
                    nullable: true
                    readOnly: true
                    format: url
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-reset-client-secret
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST \
              https://api.linode.com/v4/account/oauth-clients/edc6790ea9db4d224c5c/reset-secret
      - lang: CLI
        source: |-
          linode-cli account client-reset-secret \
            edc6790ea9db4d224c5c
      x-linode-cli-action: client-reset-secret
      x-original-op-id: resetClientSecret
      x-original-op-title: OAuth Client Secret Reset
    x-linode-cli-command: account
  /account/oauth-clients/{clientId}/thumbnail:
    x-akamai:
      file-path: paths/thumbnail.yaml
      path-info: /account/oauth-clients/{clientId}/thumbnail
    parameters:
    - name: clientId
      in: path
      description: |
        The OAuth Client ID to look up.
      example: '{{clientId}}'
      x-akamai:
        file-path: parameters/client-id-path-ecf807fb.yaml
      required: true
      schema:
        type: string
    get:
      operationId: get-client-thumbnail
      summary: Get the OAuth client's thumbnail
      tags:
      - Client thumbnail
      description: |
        Returns the thumbnail for this OAuth Client.  This is a publicly-viewable endpoint, and can be accessed without
        authentication.
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The client's thumbnail.
          content:
            application/json: {}
            image/png:
              schema:
                type: string
                format: binary
                x-ref: ../schemas/added-tbd.yaml
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-client-thumbnail
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/account/oauth-clients/edc6790ea9db4d224c5c/thumbnail > thumbnail.png
      x-linode-cli-action: client-thumbnail
      x-linode-cli-skip: true
      x-original-op-id: getClientThumbnail
      x-original-op-title: OAuth Client Thumbnail View
    put:
      operationId: put-client-thumbnail
      summary: Update the OAuth client's thumbnail
      tags:
      - Client thumbnail
      description: |-
        Upload a thumbnail for a client you own.  You must upload an image file that will be returned when the thumbnail is retrieved.  This image will be publicly-viewable.


        ---


        - __CLI__.

            ```
            linode-cli account update-client-thumbnail
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli account update-client-thumbnail
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The image to set as the thumbnail.
        required: true
        content:
          application/json: {}
          image/png:
            schema:
              type: string
              format: binary
              x-ref: ../schemas/added-tbd.yaml
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Thumbnail updated successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-client-thumbnail
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: image/png" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT \
              --data-binary "@/path/to/image"
              https://api.linode.com/v4/account/oauth-clients/edc6790ea9db4d224c5c/thumbnail
      x-linode-cli-action: update-client-thumbnail
      x-linode-cli-skip: true
      x-original-op-id: setClientThumbnail
      x-original-op-title: OAuth Client Thumbnail Update
    x-linode-cli-command: account
  /account/payment-methods:
    x-akamai:
      file-path: paths/payment-methods.yaml
      path-info: /account/payment-methods
    post:
      servers:
      - url: https://api.linode.com/v4
      operationId: post-payment-method
      summary: Add a payment method
      tags:
      - Payment methods
      description: |-
        Adds a Payment Method to your Account with the option to set it as the default method.

        - Adding a default Payment Method removes the default status from any other Payment Method.

        - An Account can have up to 6 active Payment Methods.

        - Up to 60 Payment Methods can be added each day.

        - Prior to adding a Payment Method, ensure that your billing address information is up-to-date with a valid `zip` by running the [Update your account](https://techdocs.akamai.com/linode-api/reference/put-account) operation.

        - A `payment_method_add` event is generated when a payment is successfully submitted.

        __Parent and child accounts__

        In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, the following apply:

        - Child account users can't run this operation. These users don't have access to billing-related operations.


        ---


        - __CLI__.

            ```
            linode-cli payment-methods add
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli payment-methods add
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The details of the Payment Method to add.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                Payment Method Request Object.
              additionalProperties: false
              required:
              - type
              - data
              - is_default
              properties:
                type:
                  type: string
                  description: |-
                    The type of Payment Method.

                    Alternative Payment Methods including Google Pay and PayPal can be added using the Cloud Manager. See the [Manage Payment Methods](https://www.linode.com/docs/products/platform/billing/guides/payment-methods/) guide
                    for details and instructions.
                  example: '{{type}}'
                  enum:
                  - credit_card
                data:
                  type: object
                  description: |
                    An object representing the credit card information you have on file with Linode to make
                    Payments against your Account.
                  x-akamai:
                    file-path: schemas/credit-card.yaml
                  additionalProperties: false
                  required:
                  - card_number
                  - expiry_month
                  - expiry_year
                  - cvv
                  properties:
                    card_number:
                      type: string
                      description: |
                        Your credit card number. No spaces or hyphens (`-`) allowed.
                      example: 4111111111111111
                      minLength: 14
                      maxLength: 24
                      format: digits
                    cvv:
                      type: string
                      description: |
                        CVV (Card Verification Value) of the credit card, typically found on the back of
                        the card.
                      example: '123'
                      minLength: 3
                      maxLength: 4
                      format: digits
                    expiry_month:
                      type: integer
                      description: |-
                        A value from 1-12 representing the expiration month of your credit card.

                          - 1 = January
                          - 2 = February
                          - 3 = March
                          - Etc.
                      example: 12
                      minimum: 1
                      maximum: 12
                    expiry_year:
                      type: integer
                      description: |-
                        A four-digit integer representing the expiration year of your credit card.

                        The combination of `expiry_month` and `expiry_year` must result in a month/year combination of the current month or in the future. An expiration date set in the past is invalid.
                      example: 2020
                      minLength: 4
                      maxLength: 4
                is_default:
                  type: boolean
                  description: |
                    Whether this Payment Method is the default method for automatically processing service
                    charges.
                  example: '{{is_default}}'
                  x-linode-cli-display: 3
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Payment Method added.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-payment-method
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "type": "credit_card",
                "is_default": true,
                "data": {
                  "card_number": "4111111111111111",
                  "expiry_month": 11,
                  "expiry_year": 2020,
                  "cvv": "111"
                }
              }' \
              https://api.linode.com/v4/account/payment-methods
      - lang: CLI
        source: |-
          linode-cli payment-methods add \
            --type credit_card \
            --is_default true \
            --data.card_number 4111111111111111 \
            --data.expiry_month 11 \
            --data.expiry_year 2020 \
            --data.cvv 111
      x-linode-cli-action: add
      x-linode-grant: read_write
      x-original-op-id: createPaymentMethod
      x-original-op-title: Payment Method Add
    get:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: get-payment-methods
      summary: List payment methods
      tags:
      - Payment methods
      description: |-
        Returns a paginated list of Payment Methods for this Account.


        ---


        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated list of Payment Method objects.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        Payment Method Response Object.
                      x-akamai:
                        file-path: schemas/payment-method.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The unique ID of this Payment Method.
                          example: 123
                          x-linode-cli-display: 1
                        type:
                          type: string
                          description: |
                            The type of Payment Method.
                          example: credit_card
                          enum:
                          - credit_card
                          - google_pay
                          - paypal
                          x-linode-cli-display: 2
                        created:
                          type: string
                          description: |
                            __Read-only__ When the Payment Method was added to the Account.
                          example: '2018-01-15T00:01:01'
                          readOnly: true
                          format: date-time
                        data:
                          oneOf:
                          - type: object
                            description: |
                              Credit card information.
                            x-akamai:
                              file-path: schemas/credit-card-data.yaml
                            title: Credit card
                            additionalProperties: false
                            properties:
                              card_type:
                                type: string
                                description: |
                                  __Read-only__ The type of credit card.
                                example: Discover
                                readOnly: true
                              expiry:
                                type: string
                                description: |
                                  __Read-only__ The expiration month and year of the credit card.
                                example: 06/2022
                                readOnly: true
                                format: MM/YYYY
                              last_four:
                                type: string
                                description: |
                                  __Read-only__ The last four digits of the credit card number.
                                example: '1234'
                                readOnly: true
                            x-linode-ref-name: Credit
                              Card
                          - type: object
                            description: |
                              Google Pay information.
                            x-akamai:
                              file-path: schemas/google-pay-data.yaml
                            title: Google Pay
                            additionalProperties: false
                            properties:
                              card_type:
                                type: string
                                description: |
                                  __Read-only__ The type of credit card.
                                example: Discover
                                readOnly: true
                              expiry:
                                type: string
                                description: |
                                  __Read-only__ The expiration month and year of the credit card.
                                example: 06/2022
                                readOnly: true
                                format: MM/YYYY
                              last_four:
                                type: string
                                description: |
                                  __Read-only__ The last four digits of the credit card number.
                                example: '1234'
                                readOnly: true
                            x-linode-ref-name: Google
                              Pay
                          - type: object
                            description: |
                              PayPal information.
                            x-akamai:
                              file-path: schemas/paypal-data.yaml
                            title: Paypal
                            additionalProperties: false
                            properties:
                              email:
                                type: string
                                description: |
                                  __Read-only__ The email address associated with your PayPal account.
                                example: example@linode.com
                                readOnly: true
                              paypal_id:
                                type: string
                                description: |
                                  __Read-only__ PayPal Merchant ID associated with your PayPal account.
                                example: ABC1234567890
                                readOnly: true
                            x-linode-ref-name: Paypal
                          x-linode-cli-display: 4
                          x-linode-cli-format: json
                        is_default:
                          type: boolean
                          description: |
                            Whether this Payment Method is the default method for automatically
                            processing service charges.
                          example: true
                          x-linode-cli-display: 3
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-payment-methods
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/account/payment-methods
      - lang: CLI
        source: linode-cli payment-methods list
      x-linode-cli-action:
      - list
      - ls
      x-linode-grant: read_only
      x-original-op-id: getPaymentMethods
      x-original-op-title: Payment Methods List
    x-linode-cli-command: payment-methods
  /account/payment-methods/{paymentMethodId}:
    x-akamai:
      file-path: paths/payment-method.yaml
      path-info: /account/payment-methods/{paymentMethodId}
    parameters:
    - name: paymentMethodId
      in: path
      description: |
        The ID of the Payment Method to look up.
      example: '{{paymentMethodId}}'
      x-akamai:
        file-path: parameters/payment-method-id-path-6078bc7b.yaml
      required: true
      schema:
        type: integer
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-payment-method
      summary: Get a payment method
      tags:
      - Payment methods
      description: |-
        View the details of the specified Payment Method.


        ---


        - __CLI__.

            ```
            linode-cli payment-methods view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli payment-methods view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a Payment Method Object.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Payment Method Response Object.
                x-akamai:
                  file-path: schemas/payment-method.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      The unique ID of this Payment Method.
                    example: 123
                    x-linode-cli-display: 1
                  type:
                    type: string
                    description: |
                      The type of Payment Method.
                    example: credit_card
                    enum:
                    - credit_card
                    - google_pay
                    - paypal
                    x-linode-cli-display: 2
                  created:
                    type: string
                    description: |
                      __Read-only__ When the Payment Method was added to the Account.
                    example: '2018-01-15T00:01:01'
                    readOnly: true
                    format: date-time
                  data:
                    oneOf:
                    - type: object
                      description: |
                        Credit card information.
                      x-akamai:
                        file-path: schemas/credit-card-data.yaml
                      title: Credit card
                      additionalProperties: false
                      properties:
                        card_type:
                          type: string
                          description: |
                            __Read-only__ The type of credit card.
                          example: Discover
                          readOnly: true
                        expiry:
                          type: string
                          description: |
                            __Read-only__ The expiration month and year of the credit card.
                          example: 06/2022
                          readOnly: true
                          format: MM/YYYY
                        last_four:
                          type: string
                          description: |
                            __Read-only__ The last four digits of the credit card number.
                          example: '1234'
                          readOnly: true
                      x-linode-ref-name: Credit Card
                    - type: object
                      description: |
                        Google Pay information.
                      x-akamai:
                        file-path: schemas/google-pay-data.yaml
                      title: Google Pay
                      additionalProperties: false
                      properties:
                        card_type:
                          type: string
                          description: |
                            __Read-only__ The type of credit card.
                          example: Discover
                          readOnly: true
                        expiry:
                          type: string
                          description: |
                            __Read-only__ The expiration month and year of the credit card.
                          example: 06/2022
                          readOnly: true
                          format: MM/YYYY
                        last_four:
                          type: string
                          description: |
                            __Read-only__ The last four digits of the credit card number.
                          example: '1234'
                          readOnly: true
                      x-linode-ref-name: Google Pay
                    - type: object
                      description: |
                        PayPal information.
                      x-akamai:
                        file-path: schemas/paypal-data.yaml
                      title: Paypal
                      additionalProperties: false
                      properties:
                        email:
                          type: string
                          description: |
                            __Read-only__ The email address associated with your PayPal account.
                          example: example@linode.com
                          readOnly: true
                        paypal_id:
                          type: string
                          description: |
                            __Read-only__ PayPal Merchant ID associated with your PayPal account.
                          example: ABC1234567890
                          readOnly: true
                      x-linode-ref-name: Paypal
                    x-linode-cli-display: 4
                    x-linode-cli-format: json
                  is_default:
                    type: boolean
                    description: |
                      Whether this Payment Method is the default method for automatically processing service
                      charges.
                    example: true
                    x-linode-cli-display: 3
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-payment-method
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/account/payment-methods/123
      - lang: CLI
        source: linode-cli payment-methods view 123
      x-linode-cli-action: view
      x-linode-grant: read_only
      x-original-op-id: getPaymentMethod
      x-original-op-title: Payment Method View
    delete:
      operationId: delete-payment-method
      summary: Delete a payment method
      tags:
      - Payment methods
      description: |-
        Deactivate the specified Payment Method.

        The default Payment Method can not be deleted. To add a new default Payment Method, run the [Add a payment method](https://techdocs.akamai.com/linode-api/reference/post-payment-method) operation. To designate an existing Payment Method as the default method, run the [Set a default payment method](https://techdocs.akamai.com/linode-api/reference/post-make-payment-method-default) operation.


        ---


        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Payment Method deactivated.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-payment-method
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/account/payment-methods/123
      - lang: CLI
        source: linode-cli payment-methods delete 123
      x-linode-cli-action:
      - delete
      - rm
      x-linode-grant: read_write
      x-original-op-id: deletePaymentMethod
      x-original-op-title: Payment Method Delete
    x-linode-cli-command: payment-methods
  /account/payment-methods/{paymentMethodId}/make-default:
    x-akamai:
      file-path: paths/make-default.yaml
      path-info: /account/payment-methods/{paymentMethodId}/make-default
    parameters:
    - name: paymentMethodId
      in: path
      description: |
        The ID of the Payment Method to make default.
      example: '{{paymentMethodId}}'
      x-akamai:
        file-path: parameters/payment-method-id-path-fb39a844.yaml
      required: true
      schema:
        type: integer
    post:
      servers:
      - url: https://api.linode.com/v4
      operationId: post-make-payment-method-default
      summary: Set a default payment method
      tags:
      - Payments
      description: |-
        Make the specified Payment Method the default method for automatically processing payments. Removes the default status from any other Payment Method.

        __Parent and child accounts__

        In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, the following apply:

        - Child account users can't run this operation. These users don't have access to billing-related operations.


        ---


        - __CLI__.

            ```
            linode-cli payment-methods default
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli payment-methods default
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Payment Method successfully set as the default method.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-make-payment-method-default
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X POST \
              https://api.linode.com/v4/account/payment-methods/123/make-default
      - lang: CLI
        source: linode-cli payment-methods default 123
      x-linode-cli-action: default
      x-linode-grant: read_write
      x-original-op-id: makePaymentMethodDefault
      x-original-op-title: Payment Method Make Default
    x-linode-cli-command: payment-methods
  /account/payments:
    x-akamai:
      file-path: paths/payments.yaml
      path-info: /account/payments
    post:
      operationId: post-payment
      summary: Make a payment
      tags:
      - Payments
      description: |-
        Makes a Payment to your Account.

        - The requested amount is charged to the default Payment Method if no `payment_method_id` is specified.

        - A `payment_submitted` event is generated when a payment is successfully submitted.

        __Parent and child accounts__

        In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, the following apply:

        - Child account users can't run this operation. These users don't have access to billing-related operations.


        ---


        - __CLI__.

            ```
            linode-cli account payment-create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli account payment-create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the Payment you are making.
        required: true
        content:
          application/json:
            schema:
              additionalProperties: false
              properties:
                payment_method_id:
                  type: integer
                  description: |
                    The ID of the Payment Method to apply to the Payment.
                  example: 123
                usd:
                  type: string
                  description: |-
                    The amount in US Dollars of the Payment.

                    - Can begin with or without `$`.
                    - Commas (`,`) are not accepted.
                    - Must end with a decimal expression, such as `.00` or `.99`.
                    - Minimum: `$5.00` or the Account balance, whichever is lower.
                    - Maximum: `$2000.00` or the Account balance up to `$50000.00`, whichever is greater.
                  example: $120.50
                  pattern: ^\$?\d+\.\d{2}$
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Payment submitted successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Payment object response.
                x-akamai:
                  file-path: schemas/payment.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of the Payment.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                  date:
                    type: string
                    description: |
                      __Read-only__ When the Payment was made.
                    example: '2018-01-15T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 2
                  usd:
                    type: integer
                    description: |
                      __Read-only__ The amount, in US dollars, of the Payment.
                    example: '120.50'
                    readOnly: true
                    x-linode-cli-display: 3
              x-example:
                x-ref: ../examples/tbd.json
        202:
          description: |-
            Accepted with warning.

            A warnings array is included with the standard 200 response body.
          content:
            application/json: {}
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-payment
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                  "usd": "120.50",
                  "payment_method_id": 123
              }' \
              https://api.linode.com/v4/account/payments
      - lang: CLI
        source: |-
          linode-cli account payment-create \
            --usd 120.50 \
            --payment_method_id 123
      x-linode-cli-action: payment-create
      x-linode-grant: read_write
      x-original-op-id: createPayment
      x-original-op-title: Payment Make
    get:
      operationId: get-payments
      summary: List payments
      tags:
      - Payments
      description: |-
        Returns a paginated list of Payments made on this Account.


        ---


        - __CLI__.

            ```
            linode-cli account payments-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli account payments-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated list of Payment objects.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        Payment object response.
                      x-akamai:
                        file-path: schemas/payment.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ The unique ID of the Payment.
                          example: 123
                          readOnly: true
                          x-linode-cli-display: 1
                        date:
                          type: string
                          description: |
                            __Read-only__ When the Payment was made.
                          example: '2018-01-15T00:01:01'
                          readOnly: true
                          format: date-time
                          x-linode-cli-display: 2
                        usd:
                          type: integer
                          description: |
                            __Read-only__ The amount, in US dollars, of the Payment.
                          example: '120.50'
                          readOnly: true
                          x-linode-cli-display: 3
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-payments
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/account/payments
      - lang: CLI
        source: linode-cli account payments-list
      x-linode-cli-action: payments-list
      x-linode-grant: read_only
      x-original-op-id: getPayments
      x-original-op-title: Payments List
    x-linode-cli-command: account
  /account/payments/paypal:
    x-akamai:
      file-path: paths/paypal.yaml
      path-info: /account/payments/paypal
    post:
      operationId: post-pay-pal-payment
      summary: Stage a PayPal payment
      tags:
      - Payments
      description: |-
        __Deprecated__ This operation is disabled and no longer accessible. PayPal can be designated as a Payment Method for automated payments using the Cloud Manager. See [Manage Payment Methods](https://www.linode.com/docs/products/platform/billing/guides/payment-methods/).


        ---


        - __CLI__.

            ```
            linode-cli account paypal-start
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: DEPRECATED
        tabs:
        - title: CLI
          syntax: linode-cli account paypal-start
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The amount of the Payment to submit via PayPal.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                An object representing the staging of a Payment via PayPal.
              x-akamai:
                file-path: schemas/paypal.yaml
              additionalProperties: false
              required:
              - cancel_url
              - redirect_url
              - usd
              properties:
                cancel_url:
                  type: string
                  description: |
                    The URL to have PayPal redirect to when Payment is canceled.
                  example: '{{cancel_url}}'
                redirect_url:
                  type: string
                  description: |
                    The URL to have PayPal redirect to when Payment is approved.
                  example: '{{redirect_url}}'
                usd:
                  type: string
                  description: |
                    The payment amount in USD. Minimum accepted value of $5 USD. Maximum accepted value
                    of $500 USD or credit card payment limit; whichever value is highest.
                    PayPal's maximum transaction limit is $10,000 USD.
                  example: '{{usd}}'
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            PayPal Payment staged.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  checkout_token:
                    type: string
                    description: |
                      The checkout token generated for this Payment.
                    example: EC-1A2B3C4D5E6F7G8H9
                    readOnly: true
                  payment_id:
                    type: string
                    description: |
                      The paypal-generated ID for this Payment. Used when authorizing the Payment in PayPal's
                      interface.
                    example: PAY-1234567890ABCDEFGHIJKLMN
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
        299:
          description: |-
            Request successful. This operation is deprecated and may be removed in a future release.

            A warnings array is included with the standard 200 response body.
          content:
            application/json: {}
      deprecated: true
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-pay-pal-payment
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                  "usd": "120.50",
                  "redirect_url": "https://example.org",
                  "cancel_url": "https://example.org"
              }' \
              https://api.linode.com/v4/account/payments/paypal
      - lang: CLI
        source: |-
          linode-cli account paypal-start \
            --cancel_url https://example.org \
            --redirect_url https://example.org \
            --usd 120.50
      x-linode-cli-action: paypal-start
      x-linode-cli-skip: true
      x-linode-grant: read_only
      x-original-op-id: createPayPalPayment
      x-original-op-title: PayPal Payment Stage
    x-linode-cli-command: account
  /account/payments/paypal/execute:
    x-akamai:
      file-path: paths/execute.yaml
      path-info: /account/payments/paypal/execute
    post:
      operationId: post-execute-pay-pal-payment
      summary: Execute a PayPal payment
      tags:
      - Payments
      description: |-
        __Deprecated__ This operation is disabled and no longer accessible. PayPal can be designated as a Payment Method for automated payments using the Cloud Manager. See [Manage Payment Methods](https://www.linode.com/docs/products/platform/billing/guides/payment-methods/).


        ---


        - __CLI__.

            ```
            linode-cli account paypal-execute
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: DEPRECATED
        tabs:
        - title: CLI
          syntax: linode-cli account paypal-execute
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The details of the Payment to execute.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                An object representing an execution of Payment to PayPal to capture the funds and credit your
                Linode Account.
              x-akamai:
                file-path: schemas/paypal-execute.yaml
              additionalProperties: false
              required:
              - payer_id
              - payment_id
              properties:
                payer_id:
                  type: string
                  description: |
                    The PayerID returned by PayPal during the transaction authorization process.
                  example: '{{payer_id}}'
                payment_id:
                  type: string
                  description: |
                    The PaymentID returned from [Stage a PayPal payment](https://techdocs.akamai.com/linode-api/reference/post-pay-pal-payment)
                    that has been approved with PayPal.
                  example: '{{payment_id}}'
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            PayPal Payment executed.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
        202:
          description: |-
            Accepted with warning.

            A warnings array is included with the standard 200 response body.
          content:
            application/json: {}
        299:
          description: |-
            Request successful. This operation is deprecated and may be removed in a future release.

            A warnings array is included with the standard 200 response body.
          content:
            application/json: {}
      deprecated: true
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-execute-pay-pal-payment
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                  "payment_id": "PAY-1234567890ABCDEFGHIJKLMN",
                  "payer_id": "ABCDEFGHIJKLM"
              }' \
              https://api.linode.com/v4/account/payments/paypal
      - lang: CLI
        source: linode-cli account paypal-execute
      x-linode-cli-action: paypal-execute
      x-linode-cli-skip: true
      x-linode-grant: read_write
      x-original-op-id: executePayPalPayment
      x-original-op-title: Staged/Approved PayPal Payment
        Execute
    x-linode-cli-command: account
  /account/payments/{paymentId}:
    x-akamai:
      file-path: paths/payment.yaml
      path-info: /account/payments/{paymentId}
    parameters:
    - name: paymentId
      in: path
      description: |
        The ID of the Payment to look up.
      example: '{{paymentId}}'
      x-akamai:
        file-path: parameters/payment-id-path.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-payment
      summary: Get a payment
      tags:
      - Payments
      description: |-
        Returns information about a specific Payment.


        ---


        - __CLI__.

            ```
            linode-cli account payment-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli account payment-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A Payment object.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Payment object response.
                x-akamai:
                  file-path: schemas/payment.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of the Payment.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                  date:
                    type: string
                    description: |
                      __Read-only__ When the Payment was made.
                    example: '2018-01-15T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 2
                  usd:
                    type: integer
                    description: |
                      __Read-only__ The amount, in US dollars, of the Payment.
                    example: '120.50'
                    readOnly: true
                    x-linode-cli-display: 3
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-payment
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/account/payments/123
      - lang: CLI
        source: linode-cli account payment-view 123
      x-linode-cli-action: payment-view
      x-linode-grant: read_only
      x-original-op-id: getPayment
      x-original-op-title: Payment View
    x-linode-cli-command: account
  /account/promo-codes:
    x-akamai:
      file-path: paths/promo-codes.yaml
      path-info: /account/promo-codes
    post:
      operationId: post-promo-credit
      summary: Add a promo credit
      tags:
      - Promo credits
      description: |-
        Adds an expiring Promo Credit to your account. The following restrictions apply:

        - Your account needs to be less than 90 days old.

        - You can't already have a Promo Credit on your account.

        - The user making the request needs to be unrestricted. You can run the [Update a user](put-user) operation to change a user's restricted status.

        - The `promo_code` needs to be valid and unexpired.

        __Parent and child accounts__

        In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, the following apply:

        - Child account users can't run this operation. These users don't have access to billing-related operations.


        ---


        - __CLI__.

            ```
            linode-cli account promo-add
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli account promo-add
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Enter a Promo Code to add its associated credit to your Account.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - promo_code
              properties:
                promo_code:
                  type: string
                  description: |
                    The Promo Code.
                  example: '{{promo_code}}'
                  minLength: 1
                  maxLength: 32
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Promo Credit successfully added.
          content:
            application/json:
              schema:
                type: object
                description: |-
                  __Read-only__ Promotions generally offer a set amount of credit that can be used toward your Linode services, and the promotion expires after a specified date. As well, a monthly cap on the promotional offer is set.

                  Simply put, a promotion offers a certain amount of credit  month, until either the expiration date is passed, or until the total promotional credit is used, whichever comes first.
                x-akamai:
                  file-path: schemas/promotion.yaml
                additionalProperties: false
                readOnly: true
                properties:
                  summary:
                    type: string
                    description: |
                      Short details of this promotion.
                    example: $10 off your Linode a month!
                    x-linode-cli-display: 10
                  description:
                    type: string
                    description: |
                      A detailed description of this promotion.
                    example: Receive up to $10 off your services every month for 6 months! Unused credits will expire
                      once this promotion period ends.
                  credit_monthly_cap:
                    type: string
                    description: |
                      The amount available to spend per month.
                    example: '10.00'
                    x-linode-cli-display: 5
                  credit_remaining:
                    type: string
                    description: |
                      The total amount of credit left for this promotion.
                    example: '50.00'
                    x-linode-cli-display: 3
                  expire_dt:
                    type: string
                    description: |
                      When this promotion's credits expire.
                    example: '2018-01-31T23:59:59'
                    x-linode-cli-display: 2
                  image_url:
                    type: string
                    description: |
                      The location of an image for this promotion.
                    example: https://linode.com/10_a_month_promotion.svg
                  service_type:
                    type: string
                    description: |
                      The service to which this promotion applies.
                    example: all
                    enum:
                    - all
                    - backup
                    - blockstorage
                    - db_mysql
                    - ip_v4
                    - linode
                    - linode_disk
                    - linode_memory
                    - loadbalancer
                    - longview
                    - managed
                    - nodebalancer
                    - placement_group
                    - objectstorage
                    - transfer_tx
                    x-linode-cli-display: 1
                  this_month_credit_remaining:
                    type: string
                    description: |
                      The amount of credit left for this month for this promotion.
                    example: '10.00'
                    x-linode-cli-display: 4
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-promo-credit
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
            -H "Authorization: Bearer $TOKEN" \
            -X POST -d '{
              "promo_code": "abcdefABCDEF1234567890"
            }' \
            https://api.linode.com/v4/account/promo-codes
      - lang: CLI
        source: |-
          linode-cli account \
            promo-add \
            --promo-code abcdefABCDEF1234567890
      x-linode-cli-action: promo-add
      x-linode-grant: unrestricted only
      x-original-op-id: createPromoCredit
      x-original-op-title: Promo Credit Add
    x-linode-cli-command: account
  /account/service-transfers:
    x-akamai:
      file-path: paths/service-transfers.yaml
      path-info: /account/service-transfers
    post:
      operationId: post-service-transfer
      summary: Request a service transfer
      tags:
      - Service transfers
      description: |-
        Creates a transfer request for the specified services. A request can contain any of the specified service types and any number of each service type. At this time, only Linodes can be transferred.

        When created successfully, a confirmation email is sent to the account that created this transfer containing a transfer token and instructions on completing the transfer.

        When a transfer is [accepted](https://techdocs.akamai.com/linode-api/reference/post-accept-service-transfer), the requested services are moved to the receiving account. Linode services will not experience interruptions due to the transfer process. Backups for Linodes are transferred as well.

        DNS records that are associated with requested services will not be transferred or updated. Please ensure that associated DNS records have been updated or communicated to the recipient prior to the transfer.

        A transfer can take up to three hours to complete once accepted. When a transfer is completed, billing for transferred services ends for the sending account and begins for the receiving account.

        This operation can only be accessed by the unrestricted users of an account.

        There are several conditions that you need to meet to successfully create a transfer request:

        1. The account creating the transfer can't have a past due balance or active Terms of Service violation.

        1. The service needs to be owned by the account that is creating the transfer.

        1. The service can't be assigned to another Service Transfer that is pending or that's been accepted and is incomplete.

        1. Linodes can't:

            - be assigned to a NodeBalancer, Firewall, VLAN, VPC, or Managed Service.

            - have any attached Block Storage Volumes.

            - have any shared IP addresses.

            - have any assigned /56, /64, or /116 IPv6 ranges.


        ---


        - __CLI__.

            ```
            linode-cli service-transfers create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli service-transfers create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The services to include in this transfer request.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - entities
              properties:
                entities:
                  type: object
                  description: |
                    A collection of the services to include in this transfer request, separated by type.
                  additionalProperties: false
                  properties:
                    linodes:
                      type: array
                      description: |
                        An array containing the IDs of each of the Linodes included in this transfer.
                      example:
                      - 111
                      - 222
                      items:
                        type: integer
                      x-linode-cli-display: 5
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a Service Transfer object for the request.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An object representing a Service Transfer.
                x-akamai:
                  file-path: schemas/service-transfer.yaml
                additionalProperties: false
                properties:
                  created:
                    type: string
                    description: |
                      When this transfer was created.
                    example: '2021-02-11T16:37:03'
                    format: date-time
                  entities:
                    type: object
                    description: |
                      A collection of the services to include in this transfer request, separated by type.
                    additionalProperties: false
                    properties:
                      linodes:
                        type: array
                        description: |
                          An array containing the IDs of each of the Linodes included in this transfer.
                        example:
                        - 111
                        - 222
                        items:
                          type: integer
                        x-linode-cli-display: 5
                  expiry:
                    type: string
                    description: |
                      When this transfer expires. Transfers will automatically expire 24 hours after
                      creation.
                    example: '2021-02-12T16:37:03'
                    format: date-time
                    x-linode-cli-display: 3
                  is_sender:
                    type: boolean
                    description: |
                      If the requesting account created this transfer.
                    example: true
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  status:
                    type: string
                    description: |-
                      The status of the transfer request.

                      `accepted`: The transfer has been accepted by another user and is currently in progress.
                      Transfers can take up to 3 hours to complete.

                      `canceled`: The transfer has been canceled by the sender.

                      `completed`: The transfer has completed successfully.

                      `failed`: The transfer has failed after initiation.

                      `pending`: The transfer is ready to be accepted.

                      `stale`: The transfer has exceeded its expiration date. It can no longer be accepted or
                      canceled.
                    example: pending
                    enum:
                    - accepted
                    - canceled
                    - completed
                    - failed
                    - pending
                    - stale
                    x-linode-cli-color:
                      accepted: yellow
                      canceled: red
                      completed: green
                      default_: white
                      failed: red
                      pending: yellow
                      stale: red
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  token:
                    type: string
                    description: |
                      The token used to identify and accept or cancel this transfer.
                    example: 123E4567-E89B-12D3-A456-426614174000
                    format: uuid
                    x-linode-cli-display: 1
                  updated:
                    type: string
                    description: |
                      When this transfer was last updated.
                    example: '2021-02-11T16:37:03'
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-service-transfer
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
            -H "Authorization: Bearer $TOKEN" \
            -X POST -d '{
              "entities": {
                "linodes": [
                  111,
                  222
                ]
              }
            }' \
            https://api.linode.com/v4/account/service-transfers
      - lang: CLI
        source: |-
          linode-cli service-transfers \
            create \
            --entities.linodes 111 \
            --entities.linodes 222
      x-linode-cli-action: create
      x-linode-grant: unrestricted only
      x-original-op-id: createServiceTransfer
      x-original-op-title: Service Transfer Create
    get:
      operationId: get-service-transfers
      summary: List service transfers
      tags:
      - Service transfers
      description: |-
        Returns a collection of all created and accepted Service Transfers for this account, regardless of the user that created or accepted the transfer.

        This operation can only be accessed by the unrestricted users of an account.


        ---


        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated list of Service Transfer objects containing the details of all transfers that have been
            created and accepted by this account.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        An object representing a Service Transfer.
                      x-akamai:
                        file-path: schemas/service-transfer.yaml
                      additionalProperties: false
                      properties:
                        created:
                          type: string
                          description: |
                            When this transfer was created.
                          example: '2021-02-11T16:37:03'
                          format: date-time
                        entities:
                          type: object
                          description: |
                            A collection of the services to include in this transfer request, separated
                            by type.
                          additionalProperties: false
                          properties:
                            linodes:
                              type: array
                              description: |
                                An array containing the IDs of each of the Linodes included
                                in this transfer.
                              example:
                              - 111
                              - 222
                              items:
                                type: integer
                              x-linode-cli-display: 5
                        expiry:
                          type: string
                          description: |
                            When this transfer expires. Transfers will automatically expire 24 hours
                            after creation.
                          example: '2021-02-12T16:37:03'
                          format: date-time
                          x-linode-cli-display: 3
                        is_sender:
                          type: boolean
                          description: |
                            If the requesting account created this transfer.
                          example: true
                          x-linode-cli-display: 4
                          x-linode-filterable: true
                        status:
                          type: string
                          description: |-
                            The status of the transfer request.

                            `accepted`: The transfer has been accepted by another user and is currently in progress.
                            Transfers can take up to 3 hours to complete.

                            `canceled`: The transfer has been canceled by the sender.

                            `completed`: The transfer has completed successfully.

                            `failed`: The transfer has failed after initiation.

                            `pending`: The transfer is ready to be accepted.

                            `stale`: The transfer has exceeded its expiration date. It can no longer be accepted or
                            canceled.
                          example: pending
                          enum:
                          - accepted
                          - canceled
                          - completed
                          - failed
                          - pending
                          - stale
                          x-linode-cli-color:
                            accepted: yellow
                            canceled: red
                            completed: green
                            default_: white
                            failed: red
                            pending: yellow
                            stale: red
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        token:
                          type: string
                          description: |
                            The token used to identify and accept or cancel this transfer.
                          example: 123E4567-E89B-12D3-A456-426614174000
                          format: uuid
                          x-linode-cli-display: 1
                        updated:
                          type: string
                          description: |
                            When this transfer was last updated.
                          example: '2021-02-11T16:37:03'
                          format: date-time
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-service-transfers
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
            https://api.linode.com/v4/account/service-transfers
      - lang: CLI
        source: |-
          linode-cli service-transfers \
            list
      x-linode-cli-action:
      - list
      - ls
      x-linode-grant: unrestricted only
      x-original-op-id: getServiceTransfers
      x-original-op-title: Service Transfers List
    x-linode-cli-command: service-transfers
  /account/service-transfers/{token}:
    x-akamai:
      file-path: paths/service-transfer.yaml
      path-info: /account/service-transfers/{token}
    parameters:
    - name: token
      in: path
      description: |
        The UUID of the Service Transfer.
      example: '{{token}}'
      x-akamai:
        file-path: parameters/token-path-f857f5a2.yaml
      required: true
      schema:
        type: string
        format: uuid
    get:
      operationId: get-service-transfer
      summary: Get a service transfer request
      tags:
      - Service transfers
      description: |-
        Returns the details of the Service Transfer for the provided token.

        While a transfer is pending, any unrestricted user _of any account_ can access this operation. After a transfer has been accepted, it can only be viewed by unrestricted users of the accounts that created and accepted the transfer. If canceled or expired, only unrestricted users of the account that created the transfer can view it.


        ---


        - __CLI__.

            ```
            linode-cli service-transfers view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli service-transfers view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a Service Transfer object containing the details of the transfer for the specified token.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An object representing a Service Transfer.
                x-akamai:
                  file-path: schemas/service-transfer.yaml
                additionalProperties: false
                properties:
                  created:
                    type: string
                    description: |
                      When this transfer was created.
                    example: '2021-02-11T16:37:03'
                    format: date-time
                  entities:
                    type: object
                    description: |
                      A collection of the services to include in this transfer request, separated by type.
                    additionalProperties: false
                    properties:
                      linodes:
                        type: array
                        description: |
                          An array containing the IDs of each of the Linodes included in this transfer.
                        example:
                        - 111
                        - 222
                        items:
                          type: integer
                        x-linode-cli-display: 5
                  expiry:
                    type: string
                    description: |
                      When this transfer expires. Transfers will automatically expire 24 hours after
                      creation.
                    example: '2021-02-12T16:37:03'
                    format: date-time
                    x-linode-cli-display: 3
                  is_sender:
                    type: boolean
                    description: |
                      If the requesting account created this transfer.
                    example: true
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  status:
                    type: string
                    description: |-
                      The status of the transfer request.

                      `accepted`: The transfer has been accepted by another user and is currently in progress.
                      Transfers can take up to 3 hours to complete.

                      `canceled`: The transfer has been canceled by the sender.

                      `completed`: The transfer has completed successfully.

                      `failed`: The transfer has failed after initiation.

                      `pending`: The transfer is ready to be accepted.

                      `stale`: The transfer has exceeded its expiration date. It can no longer be accepted or
                      canceled.
                    example: pending
                    enum:
                    - accepted
                    - canceled
                    - completed
                    - failed
                    - pending
                    - stale
                    x-linode-cli-color:
                      accepted: yellow
                      canceled: red
                      completed: green
                      default_: white
                      failed: red
                      pending: yellow
                      stale: red
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  token:
                    type: string
                    description: |
                      The token used to identify and accept or cancel this transfer.
                    example: 123E4567-E89B-12D3-A456-426614174000
                    format: uuid
                    x-linode-cli-display: 1
                  updated:
                    type: string
                    description: |
                      When this transfer was last updated.
                    example: '2021-02-11T16:37:03'
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-service-transfer
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
            https://api.linode.com/v4/account/service-transfers/123E4567-E89B-12D3-A456-426614174000
      - lang: CLI
        source: |-
          linode-cli service-transfers \
            view 123E4567-E89B-12D3-A456-426614174000
      x-linode-cli-action: view
      x-linode-grant: unrestricted only
      x-original-op-id: getServiceTransfer
      x-original-op-title: Service Transfer View
    delete:
      operationId: delete-service-transfer
      summary: Cancel a service transfer
      tags:
      - Service transfers
      description: |-
        Cancels the Service Transfer for the provided token. Once canceled, a transfer cannot be accepted or otherwise acted on in any way. If canceled in error, the transfer must be [created](https://techdocs.akamai.com/linode-api/reference/post-service-transfer) again.

        When canceled, an email notification for the cancellation is sent to the account that created this transfer. Transfers can not be canceled if they are expired or have been accepted.

        This operation can only be accessed by the unrestricted users of the account that created this transfer.


        ---


        - __CLI__.

            ```
            linode-cli service-transfers cancel
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli service-transfers cancel
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Service Transfer canceled.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-service-transfer
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
            -X DELETE \
            https://api.linode.com/v4/account/service-transfers/123E4567-E89B-12D3-A456-426614174000
      - lang: CLI
        source: |-
          linode-cli service-transfers \
            cancel 123E4567-E89B-12D3-A456-426614174000
      x-linode-cli-action: cancel
      x-linode-grant: unrestricted only
      x-original-op-id: deleteServiceTransfer
      x-original-op-title: Service Transfer Cancel
    x-linode-cli-command: service-transfers
  /account/service-transfers/{token}/accept:
    x-akamai:
      file-path: paths/service-transfer-accept.yaml
      path-info: /account/service-transfers/{token}/accept
    parameters:
    - name: token
      in: path
      description: |
        The UUID of the Service Transfer.
      example: '{{token}}'
      x-akamai:
        file-path: parameters/token-path-f857f5a2.yaml
      required: true
      schema:
        type: string
        format: uuid
    post:
      operationId: post-accept-service-transfer
      summary: Accept a service transfer
      tags:
      - Service transfers
      description: |-
        Accept a Service Transfer for the provided token to receive the services included in the transfer to your account. At this time, only Linodes can be transferred.

        When accepted, email confirmations are sent to the accounts that created and accepted the transfer. A transfer can take up to three hours to complete once accepted. Once a transfer is completed, billing for transferred services ends for the sending account and begins for the receiving account.

        This operation can only be accessed by the unrestricted users of the account that receives the transfer. Users of the same account that created a transfer cannot accept the transfer.

        There are several conditions that must be met in order to accept a transfer request:

        1. Only transfers with a `pending` status can be accepted.

        1. The account accepting the transfer must have a registered payment method and must not have a past due balance or other account limitations for the services to be transferred.

        1. Both the account that created the transfer and the account that is accepting the transfer must not have any active Terms of Service violations.

        1. The service must still be owned by the account that created the transfer.

        1. Linodes must not:

            - be assigned to a NodeBalancer, Firewall, VLAN, or Managed Service.

            - have any attached Block Storage Volumes.

            - have any shared IP addresses.

            - have any assigned /56, /64, or /116 IPv6 ranges.

        Any and all of the above conditions must be cured and maintained by the relevant account prior to the transfer's expiration to allow the transfer to be accepted by the receiving account.


        ---


        - __CLI__.

            ```
            linode-cli service-transfers accept
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli service-transfers accept
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Service Transfer accepted.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-accept-service-transfer
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
            -X POST \
            https://api.linode.com/v4/account/service-transfers/123E4567-E89B-12D3-A456-426614174000/accept
      - lang: CLI
        source: |-
          linode-cli service-transfers \
            accept 123E4567-E89B-12D3-A456-426614174000
      x-linode-cli-action: accept
      x-linode-grant: unrestricted only
      x-original-op-id: acceptServiceTransfer
      x-original-op-title: Service Transfer Accept
    x-linode-cli-command: service-transfers
  /account/settings:
    x-akamai:
      file-path: paths/settings.yaml
      path-info: /account/settings
    get:
      operationId: get-account-settings
      summary: Get account settings
      tags:
      - Account settings
      description: |-
        Returns information related to your Account settings: Managed service subscription, Longview subscription, and network helper.


        ---


        - __CLI__.

            ```
            linode-cli account settings
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli account settings
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a single Account settings object.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Account Settings object
                x-akamai:
                  file-path: schemas/account-settings.yaml
                additionalProperties: false
                properties:
                  backups_enabled:
                    type: boolean
                    description: |
                      Account-wide backups default.  If `true`, all Linodes created will automatically
                      be enrolled in the Backups service.  If `false`, Linodes will
                      not be enrolled by default, but may still be enrolled on creation
                      or later.
                    example: true
                    x-linode-cli-display: 4
                  longview_subscription:
                    type: string
                    description: |
                      __Read-only__ The Longview Pro tier you are currently subscribed to. The value must be
                      a [Longview subscription](https://techdocs.akamai.com/linode-api/reference/get-longview-subscriptions)
                      ID or `null` for Longview Free.
                    example: longview-3
                    readOnly: true
                    x-linode-cli-display: 2
                  managed:
                    type: boolean
                    description: |
                      __Read-only__ Our 24/7 incident response service. This robust, multi-homed monitoring
                      system distributes monitoring checks to ensure that your servers
                      remain online and available at all times. Linode Managed can
                      monitor any service or software stack reachable over TCP or
                      HTTP. Once you add a service to Linode Managed, we'll monitor
                      it for connectivity, response, and total request time.
                    example: true
                    readOnly: true
                    x-linode-cli-display: 3
                  network_helper:
                    type: boolean
                    description: |
                      Enables network helper across all users by default for new Linodes and Linode Configs.
                    example: false
                    x-linode-cli-display: 1
                  object_storage:
                    type: string
                    description: |
                      __Read-only__ A string describing the status of this account's Object Storage service
                      enrollment.
                    example: active
                    default: disabled
                    readOnly: true
                    enum:
                    - disabled
                    - suspended
                    - active
                    x-linode-cli-display: 5
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-account-settings
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/account/settings
      - lang: CLI
        source: linode-cli account settings
      x-linode-cli-action: settings
      x-linode-grant: read_only
      x-original-op-id: getAccountSettings
      x-original-op-title: Account Settings View
    put:
      operationId: put-account-settings
      summary: Update account settings
      tags:
      - Account settings
      description: |-
        Updates your account settings. For a Longview subscription plan, see [Update a Longview plan](https://techdocs.akamai.com/linode-api/reference/put-longview-plan).


        ---


        - __CLI__.

            ```
            linode-cli account settings-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli account settings-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Update Account settings information.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                Account Settings object
              x-akamai:
                file-path: schemas/account-settings.yaml
              additionalProperties: false
              properties:
                backups_enabled:
                  type: boolean
                  description: |
                    Account-wide backups default.  If `true`, all Linodes created will automatically be
                    enrolled in the Backups service.  If `false`, Linodes will not
                    be enrolled by default, but may still be enrolled on creation
                    or later.
                  example: '{{backups_enabled}}'
                  x-linode-cli-display: 4
                longview_subscription:
                  type: string
                  description: |
                    __Read-only__ The Longview Pro tier you are currently subscribed to. The value must be a
                    [Longview subscription](https://techdocs.akamai.com/linode-api/reference/get-longview-subscriptions)
                    ID or `null` for Longview Free.
                  example: '{{longview_subscription}}'
                  readOnly: true
                  x-linode-cli-display: 2
                managed:
                  type: boolean
                  description: |
                    __Read-only__ Our 24/7 incident response service. This robust, multi-homed monitoring
                    system distributes monitoring checks to ensure that your servers
                    remain online and available at all times. Linode Managed can monitor
                    any service or software stack reachable over TCP or HTTP. Once
                    you add a service to Linode Managed, we'll monitor it for connectivity,
                    response, and total request time.
                  example: '{{managed}}'
                  readOnly: true
                  x-linode-cli-display: 3
                network_helper:
                  type: boolean
                  description: |
                    Enables network helper across all users by default for new Linodes and Linode Configs.
                  example: '{{network_helper}}'
                  x-linode-cli-display: 1
                object_storage:
                  type: string
                  description: |
                    __Read-only__ A string describing the status of this account's Object Storage service
                    enrollment.
                  example: '{{object_storage}}'
                  default: disabled
                  readOnly: true
                  enum:
                  - disabled
                  - suspended
                  - active
                  x-linode-cli-display: 5
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The updated Account settings.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Account Settings object
                x-akamai:
                  file-path: schemas/account-settings.yaml
                additionalProperties: false
                properties:
                  backups_enabled:
                    type: boolean
                    description: |
                      Account-wide backups default.  If `true`, all Linodes created will automatically
                      be enrolled in the Backups service.  If `false`, Linodes will
                      not be enrolled by default, but may still be enrolled on creation
                      or later.
                    example: true
                    x-linode-cli-display: 4
                  longview_subscription:
                    type: string
                    description: |
                      __Read-only__ The Longview Pro tier you are currently subscribed to. The value must be
                      a [Longview subscription](https://techdocs.akamai.com/linode-api/reference/get-longview-subscriptions)
                      ID or `null` for Longview Free.
                    example: longview-3
                    readOnly: true
                    x-linode-cli-display: 2
                  managed:
                    type: boolean
                    description: |
                      __Read-only__ Our 24/7 incident response service. This robust, multi-homed monitoring
                      system distributes monitoring checks to ensure that your servers
                      remain online and available at all times. Linode Managed can
                      monitor any service or software stack reachable over TCP or
                      HTTP. Once you add a service to Linode Managed, we'll monitor
                      it for connectivity, response, and total request time.
                    example: true
                    readOnly: true
                    x-linode-cli-display: 3
                  network_helper:
                    type: boolean
                    description: |
                      Enables network helper across all users by default for new Linodes and Linode Configs.
                    example: false
                    x-linode-cli-display: 1
                  object_storage:
                    type: string
                    description: |
                      __Read-only__ A string describing the status of this account's Object Storage service
                      enrollment.
                    example: active
                    default: disabled
                    readOnly: true
                    enum:
                    - disabled
                    - suspended
                    - active
                    x-linode-cli-display: 5
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-account-settings
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                  "network_helper": true
              }' \
              https://api.linode.com/v4/account/settings
      - lang: CLI
        source: |-
          linode-cli account settings-update \
            --network_helper false
      x-linode-cli-action: settings-update
      x-linode-grant: read_write
      x-original-op-id: updateAccountSettings
      x-original-op-title: Account Settings Update
    x-linode-cli-command: account
  /account/settings/managed-enable:
    x-akamai:
      file-path: paths/managed-enable.yaml
      path-info: /account/settings/managed-enable
    post:
      operationId: post-enable-account-managed
      summary: Enable Linode Managed
      tags:
      - Settings
      description: |-
        Enables Linode Managed for the entire account and sends a welcome email to the account's associated email address. Linode Managed can monitor any service or software stack reachable over TCP or HTTP. See our [Linode Managed guide](https://www.linode.com/docs/guides/linode-managed/) to learn more.


        ---


        - __CLI__.

            ```
            linode-cli account enable-managed
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli account enable-managed
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Managed services enabled for account.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-enable-account-managed
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X POST \
              https://api.linode.com/v4/account/settings/managed-enable
      - lang: CLI
        source: linode-cli account enable-managed
      x-linode-cli-action: enable-managed
      x-linode-grant: read_write
      x-original-op-id: enableAccountManaged
      x-original-op-title: Linode Managed Enable
    x-linode-cli-command: account
  /account/transfer:
    x-akamai:
      file-path: paths/account-transfer.yaml
      path-info: /account/transfer
    get:
      operationId: get-transfer
      summary: Get network usage
      tags:
      - Account transfer
      description: |-
        Returns a Transfer object showing your network utilization, in GB, for the current month.


        ---


        - __CLI__.

            ```
            linode-cli account transfer
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli account transfer
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a single Transfer object.
          content:
            application/json:
              schema:
                type: object
                description: |-
                  An object representing your network utilization for the current month, in Gigabytes.

                  Certain Regions have separate utilization quotas and rates. For Region-specific network utilization data, see `region_transfers`.
                x-akamai:
                  file-path: schemas/transfer.yaml
                additionalProperties: false
                properties:
                  billable:
                    type: integer
                    description: |
                      __Read-only__ The amount of your transfer pool that is billable this billing cycle.
                    example: 0
                    readOnly: true
                    x-linode-cli-display: 3
                  quota:
                    type: integer
                    description: |
                      __Read-only__ The amount of network usage allowed this billing cycle.
                    example: 9141
                    readOnly: true
                    x-linode-cli-display: 1
                  region_transfers:
                    type: array
                    items:
                      type: object
                      additionalProperties: false
                      properties:
                        id:
                          type: string
                          description: |
                            The Region ID for this network utilization data.
                          example: us-east
                        billable:
                          type: integer
                          description: |
                            __Read-only__ The amount of your transfer pool that is billable this
                            billing cycle for this Region.
                          example: 0
                          readOnly: true
                        quota:
                          type: integer
                          description: |
                            __Read-only__ The amount of network usage allowed this billing cycle
                            for this Region.
                          example: 5010
                          readOnly: true
                        used:
                          type: integer
                          description: |
                            __Read-only__ The amount of network usage you have used this billing
                            cycle for this Region.
                          example: 1
                          readOnly: true
                  used:
                    type: integer
                    description: |
                      __Read-only__ The amount of network usage you have used this billing cycle.
                    example: 2
                    readOnly: true
                    x-linode-cli-display: 2
              x-example:
                x-ref: ../examples/tbd.json
              x-linode-cli-subtables:
              - region_transfers
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-transfer
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/account/transfer
      - lang: CLI
        source: linode-cli account transfer
      x-linode-cli-action: transfer
      x-linode-grant: read_only
      x-original-op-id: getTransfer
      x-original-op-title: Network Utilization View
    x-linode-cli-command: account
  /account/users:
    x-akamai:
      file-path: paths/users.yaml
      path-info: /account/users
    post:
      operationId: post-user
      summary: Create a user
      tags:
      - Users
      description: |-
        Creates a user on your account. You determine the new user's account access by setting it to restricted or unrestricted and by defining its grants. After completion, the API sends a confirmation message containing password creation and login instructions to the user's `email` address.

        __Note__. This operation can only be accessed by account users with _unrestricted_ access.

        __Parent and child accounts__

        In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, the following apply:

        - A parent account user can create new parent account users.

        - A child account can [update](https://techdocs.akamai.com/linode-api/reference/put-user) the child account parent user (proxy user) to `unrestricted`. This gives the proxy user access to create new child account users.

        - A child account user can create new child account users.

        - You can't create a proxy user. The proxy user in a child account is predefined when you initially provision the parent-child relationship.


        ---


        - __CLI__.

            ```
            linode-cli users create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli users create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the User to create.
        content:
          application/json:
            schema:
              required:
              - username
              - email
              allOf:
              - type: object
                description: |
                  A User on your Account. Unrestricted users can log in and access information about your
                  Account, while restricted users may only access entities or perform
                  actions they've been granted access to.
                x-akamai:
                  file-path: schemas/user.yaml
                additionalProperties: false
                properties:
                  email:
                    type: string
                    description: |
                      The email address for the User. Linode sends emails to this address for account management
                      communications. May be used for other communications as configured.
                    example: example_user@linode.com
                    format: email
                    x-linode-cli-display: 2
                  last_login:
                    type: object
                    description: |-
                      __Read-only__ Information for the most recent login attempt for this User.

                      `null` if no login attempts have been made since creation of this User.

                      Run the [List user logins](https://techdocs.akamai.com/linode-api/reference/get-account-logins) operation for additional login information.
                    additionalProperties: false
                    nullable: true
                    readOnly: true
                    properties:
                      login_datetime:
                        type: string
                        description: |
                          __Read-only__ The date and time of this User's most recent login attempt.
                        example: '2018-01-01T01:01:01'
                        readOnly: true
                        format: date-time
                      status:
                        type: string
                        description: |
                          __Read-only__ The result of the most recent login attempt for this User.
                        example: successful
                        readOnly: true
                        enum:
                        - successful
                        - failed
                  password_created:
                    type: string
                    description: |-
                      __Read-only__ The date and time when this User's current password was created.

                      User passwords are first created during the Account sign-up process, and updated using the [Reset Password](https://login.linode.com/forgot/password) webpage.

                      `null` if this User has not created a password yet.
                    example: '2018-01-01T01:01:01'
                    nullable: true
                    readOnly: true
                    format: date-time
                  restricted:
                    type: boolean
                    description: |
                      If true, the User must be granted access to perform actions or access entities on this
                      Account. Run [List a user's grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants)
                      for details on how to configure grants for a restricted User.
                    example: true
                    x-linode-cli-display: 3
                  ssh_keys:
                    type: array
                    description: |-
                      __Read-only__ A list of SSH Key labels added by this User.

                      Users can add keys with the [Add an SSH key](https://techdocs.akamai.com/linode-api/reference/post-add-ssh-key) operation.

                      These keys are deployed when this User is included in the `authorized_users` field of the following requests:

                      - [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
                      - [Rebuild a Linode](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance)
                      - [Create a disk](https://techdocs.akamai.com/linode-api/reference/post-add-linode-disk)
                    example:
                    - home-pc
                    - laptop
                    readOnly: true
                    items:
                      type: string
                  tfa_enabled:
                    type: boolean
                    description: |
                      __Read-only__ A boolean value indicating if the User has Two Factor Authentication (TFA)
                      enabled. Run the [Create a two factor secret](https://techdocs.akamai.com/linode-api/reference/post-tfa-enable)
                      operation to enable TFA.
                    example: true
                    readOnly: true
                  username:
                    type: string
                    description: |
                      The User's username. This is used for logging in, and may also be displayed alongside
                      actions the User performs (for example, in Events or public
                      StackScripts).
                    example: example_user
                    minLength: 3
                    maxLength: 32
                    pattern: ^[a-zA-Z0-9]((?![_-]{2,})[a-zA-Z0-9-_])+[a-zA-Z0-9]$
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  verified_phone_number:
                    type: string
                    description: |-
                      __Read-only__ The phone number verified for this User Profile with the [Verify a phone number](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number-verify) operation.

                      `null` if this User Profile has no verified phone number.
                    example: '+5555555555'
                    nullable: true
                    readOnly: true
                    format: phone
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            New User created successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A User on your Account. Unrestricted users can log in and access information about your
                  Account, while restricted users may only access entities or perform
                  actions they've been granted access to.
                x-akamai:
                  file-path: schemas/user.yaml
                additionalProperties: false
                properties:
                  email:
                    type: string
                    description: |
                      The email address for the User. Linode sends emails to this address for account management
                      communications. May be used for other communications as configured.
                    example: example_user@linode.com
                    format: email
                    x-linode-cli-display: 2
                  last_login:
                    type: object
                    description: |-
                      __Read-only__ Information for the most recent login attempt for this User.

                      `null` if no login attempts have been made since creation of this User.

                      Run the [List user logins](https://techdocs.akamai.com/linode-api/reference/get-account-logins) operation for additional login information.
                    additionalProperties: false
                    nullable: true
                    readOnly: true
                    properties:
                      login_datetime:
                        type: string
                        description: |
                          __Read-only__ The date and time of this User's most recent login attempt.
                        example: '2018-01-01T01:01:01'
                        readOnly: true
                        format: date-time
                      status:
                        type: string
                        description: |
                          __Read-only__ The result of the most recent login attempt for this User.
                        example: successful
                        readOnly: true
                        enum:
                        - successful
                        - failed
                  password_created:
                    type: string
                    description: |-
                      __Read-only__ The date and time when this User's current password was created.

                      User passwords are first created during the Account sign-up process, and updated using the [Reset Password](https://login.linode.com/forgot/password) webpage.

                      `null` if this User has not created a password yet.
                    example: '2018-01-01T01:01:01'
                    nullable: true
                    readOnly: true
                    format: date-time
                  restricted:
                    type: boolean
                    description: |
                      If true, the User must be granted access to perform actions or access entities on this
                      Account. Run [List a user's grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants)
                      for details on how to configure grants for a restricted User.
                    example: true
                    x-linode-cli-display: 3
                  ssh_keys:
                    type: array
                    description: |-
                      __Read-only__ A list of SSH Key labels added by this User.

                      Users can add keys with the [Add an SSH key](https://techdocs.akamai.com/linode-api/reference/post-add-ssh-key) operation.

                      These keys are deployed when this User is included in the `authorized_users` field of the following requests:

                      - [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
                      - [Rebuild a Linode](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance)
                      - [Create a disk](https://techdocs.akamai.com/linode-api/reference/post-add-linode-disk)
                    example:
                    - home-pc
                    - laptop
                    readOnly: true
                    items:
                      type: string
                  tfa_enabled:
                    type: boolean
                    description: |
                      __Read-only__ A boolean value indicating if the User has Two Factor Authentication (TFA)
                      enabled. Run the [Create a two factor secret](https://techdocs.akamai.com/linode-api/reference/post-tfa-enable)
                      operation to enable TFA.
                    example: true
                    readOnly: true
                  username:
                    type: string
                    description: |
                      The User's username. This is used for logging in, and may also be displayed alongside
                      actions the User performs (for example, in Events or public
                      StackScripts).
                    example: example_user
                    minLength: 3
                    maxLength: 32
                    pattern: ^[a-zA-Z0-9]((?![_-]{2,})[a-zA-Z0-9-_])+[a-zA-Z0-9]$
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  verified_phone_number:
                    type: string
                    description: |-
                      __Read-only__ The phone number verified for this User Profile with the [Verify a phone number](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number-verify) operation.

                      `null` if this User Profile has no verified phone number.
                    example: '+5555555555'
                    nullable: true
                    readOnly: true
                    format: phone
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-user
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                  "username": "example_user",
                  "email": "person@place.com",
                  "restricted": true
              }' \
              https://api.linode.com/v4/account/users
      - lang: CLI
        source: |-
          linode-cli users create \
            --username example_user \
            --email example_user@linode.com \
            --restricted true
      x-linode-cli-action: create
      x-linode-grant: unrestricted only
      x-original-op-id: createUser
      x-original-op-title: User Create
    get:
      operationId: get-uses
      summary: List users
      tags:
      - Users
      description: |-
        Returns a paginated list of all users on your account.

        __Note__. This operation can only be accessed by account users with _unrestricted_ access.

        A user can access all or part of an account based on their access status and grants:

        - __Unrestricted access__. These users can access everything on an account.

        - __Restricted access__. These users can only access entities or perform actions they've been given specific grants to.


        ---


        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of users.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      allOf:
                      - type: object
                        description: |
                          A User on your Account. Unrestricted users can log in and access information
                          about your Account, while restricted users may only access
                          entities or perform actions they've been granted access
                          to.
                        x-akamai:
                          file-path: schemas/user.yaml
                        additionalProperties: false
                        properties:
                          email:
                            type: string
                            description: |
                              The email address for the User. Linode sends emails to this address for
                              account management communications. May be used for other
                              communications as configured.
                            example: example_user@linode.com
                            format: email
                            x-linode-cli-display: 2
                          last_login:
                            type: object
                            description: |-
                              __Read-only__ Information for the most recent login attempt for this User.

                              `null` if no login attempts have been made since creation of this User.

                              Run the [List user logins](https://techdocs.akamai.com/linode-api/reference/get-account-logins) operation for additional login information.
                            additionalProperties: false
                            nullable: true
                            readOnly: true
                            properties:
                              login_datetime:
                                type: string
                                description: |
                                  __Read-only__ The date and time of this User's most recent login
                                  attempt.
                                example: '2018-01-01T01:01:01'
                                readOnly: true
                                format: date-time
                              status:
                                type: string
                                description: |
                                  __Read-only__ The result of the most recent login attempt for
                                  this User.
                                example: successful
                                readOnly: true
                                enum:
                                - successful
                                - failed
                          password_created:
                            type: string
                            description: |-
                              __Read-only__ The date and time when this User's current password was created.

                              User passwords are first created during the Account sign-up process, and updated using the [Reset Password](https://login.linode.com/forgot/password) webpage.

                              `null` if this User has not created a password yet.
                            example: '2018-01-01T01:01:01'
                            nullable: true
                            readOnly: true
                            format: date-time
                          restricted:
                            type: boolean
                            description: |
                              If true, the User must be granted access to perform actions or access
                              entities on this Account. Run [List a user's grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants)
                              for details on how to configure grants for a restricted
                              User.
                            example: true
                            x-linode-cli-display: 3
                          ssh_keys:
                            type: array
                            description: |-
                              __Read-only__ A list of SSH Key labels added by this User.

                              Users can add keys with the [Add an SSH key](https://techdocs.akamai.com/linode-api/reference/post-add-ssh-key) operation.

                              These keys are deployed when this User is included in the `authorized_users` field of the following requests:

                              - [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
                              - [Rebuild a Linode](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance)
                              - [Create a disk](https://techdocs.akamai.com/linode-api/reference/post-add-linode-disk)
                            example:
                            - home-pc
                            - laptop
                            readOnly: true
                            items:
                              type: string
                          tfa_enabled:
                            type: boolean
                            description: |
                              __Read-only__ A boolean value indicating if the User has Two Factor
                              Authentication (TFA) enabled. Run the [Create a two
                              factor secret](https://techdocs.akamai.com/linode-api/reference/post-tfa-enable)
                              operation to enable TFA.
                            example: true
                            readOnly: true
                          username:
                            type: string
                            description: |
                              The User's username. This is used for logging in, and may also
                              be displayed alongside actions the User performs (for
                              example, in Events or public StackScripts).
                            example: example_user
                            minLength: 3
                            maxLength: 32
                            pattern: ^[a-zA-Z0-9]((?![_-]{2,})[a-zA-Z0-9-_])+[a-zA-Z0-9]$
                            x-linode-cli-display: 1
                            x-linode-filterable: true
                          verified_phone_number:
                            type: string
                            description: |-
                              __Read-only__ The phone number verified for this User Profile with the [Verify a phone number](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number-verify) operation.

                              `null` if this User Profile has no verified phone number.
                            example: '+5555555555'
                            nullable: true
                            readOnly: true
                            format: phone
                      - type: object
                        description: |
                          The type of user on an account. Mostly applies to the use of the parent and
                          child accounts for Akamai partners functionality.
                        x-akamai:
                          file-path: schemas/user-type.yaml
                        additionalProperties: false
                        properties:
                          user_type:
                            type: string
                            description: |-
                              __Read-only__ If the user belongs to a [parent or child account](https://www.linode.com/docs/guides/parent-child-accounts/) relationship, this defines the user type in the respective account. Possible values include:

                              - `parent`. This is a user in an Akamai partner account. Akamai partners have a contractural relationship with their end customers, to sell Akamai services. This user can either have full access (a parent account admin user) or limited access. Limited users don't have access to manage child accounts, but they can be granted this access by an admin user.

                              - `child`. This is an Akamai partner's end customer user, in a child account. A child user can have either full or limited access. Full access lets the user manage other child users and the proxy user in a child account.

                              - `proxy`. This is a user on a child account that gives parent account users access to that child account. A parent account user with the `child_account_access` grant can [Create a proxy user token](https://techdocs.akamai.com/linode-api/reference/post-child-account-token) from the parent account. The parent user can use this token to run API operations from the child account, as if they were a child user.

                              - `default`. This applies to all regular, non-parent-child account users.
                            example: parent
                            readOnly: true
                            enum:
                            - parent
                            - child
                            - proxy
                            - default
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-uses
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/account/users
      - lang: CLI
        source: linode-cli users list
      x-linode-cli-action:
      - list
      - ls
      x-linode-grant: unrestricted only
      x-original-op-id: getUsers
      x-original-op-title: Users List
    x-linode-cli-command: users
  /account/users/{username}:
    x-akamai:
      file-path: paths/username.yaml
      path-info: /account/users/{username}
    parameters:
    - name: username
      in: path
      description: |
        The username to look up.
      example: '{{username}}'
      x-akamai:
        file-path: parameters/username-path.yaml
      required: true
      schema:
        type: string
    get:
      operationId: get-user
      summary: Get a user
      tags:
      - Users
      description: |-
        Returns information about a single user on your account.

        __Note__. This operation can only be accessed by account users with _unrestricted_ access.


        ---


        - __CLI__.

            ```
            linode-cli users view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli users view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The requested User object
          content:
            application/json:
              schema:
                items:
                  allOf:
                  - type: object
                    description: |
                      A User on your Account. Unrestricted users can log in and access information about your
                      Account, while restricted users may only access entities or
                      perform actions they've been granted access to.
                    x-akamai:
                      file-path: schemas/user.yaml
                    additionalProperties: false
                    properties:
                      email:
                        type: string
                        description: |
                          The email address for the User. Linode sends emails to this address for account
                          management communications. May be used for other communications
                          as configured.
                        example: example_user@linode.com
                        format: email
                        x-linode-cli-display: 2
                      last_login:
                        type: object
                        description: |-
                          __Read-only__ Information for the most recent login attempt for this User.

                          `null` if no login attempts have been made since creation of this User.

                          Run the [List user logins](https://techdocs.akamai.com/linode-api/reference/get-account-logins) operation for additional login information.
                        additionalProperties: false
                        nullable: true
                        readOnly: true
                        properties:
                          login_datetime:
                            type: string
                            description: |
                              __Read-only__ The date and time of this User's most recent login attempt.
                            example: '2018-01-01T01:01:01'
                            readOnly: true
                            format: date-time
                          status:
                            type: string
                            description: |
                              __Read-only__ The result of the most recent login attempt for this
                              User.
                            example: successful
                            readOnly: true
                            enum:
                            - successful
                            - failed
                      password_created:
                        type: string
                        description: |-
                          __Read-only__ The date and time when this User's current password was created.

                          User passwords are first created during the Account sign-up process, and updated using the [Reset Password](https://login.linode.com/forgot/password) webpage.

                          `null` if this User has not created a password yet.
                        example: '2018-01-01T01:01:01'
                        nullable: true
                        readOnly: true
                        format: date-time
                      restricted:
                        type: boolean
                        description: |
                          If true, the User must be granted access to perform actions or access entities
                          on this Account. Run [List a user's grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants)
                          for details on how to configure grants for a restricted
                          User.
                        example: true
                        x-linode-cli-display: 3
                      ssh_keys:
                        type: array
                        description: |-
                          __Read-only__ A list of SSH Key labels added by this User.

                          Users can add keys with the [Add an SSH key](https://techdocs.akamai.com/linode-api/reference/post-add-ssh-key) operation.

                          These keys are deployed when this User is included in the `authorized_users` field of the following requests:

                          - [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
                          - [Rebuild a Linode](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance)
                          - [Create a disk](https://techdocs.akamai.com/linode-api/reference/post-add-linode-disk)
                        example:
                        - home-pc
                        - laptop
                        readOnly: true
                        items:
                          type: string
                      tfa_enabled:
                        type: boolean
                        description: |
                          __Read-only__ A boolean value indicating if the User has Two Factor Authentication
                          (TFA) enabled. Run the [Create a two factor secret](https://techdocs.akamai.com/linode-api/reference/post-tfa-enable)
                          operation to enable TFA.
                        example: true
                        readOnly: true
                      username:
                        type: string
                        description: |
                          The User's username. This is used for logging in, and may also be displayed alongside
                          actions the User performs (for example, in Events or public
                          StackScripts).
                        example: example_user
                        minLength: 3
                        maxLength: 32
                        pattern: ^[a-zA-Z0-9]((?![_-]{2,})[a-zA-Z0-9-_])+[a-zA-Z0-9]$
                        x-linode-cli-display: 1
                        x-linode-filterable: true
                      verified_phone_number:
                        type: string
                        description: |-
                          __Read-only__ The phone number verified for this User Profile with the [Verify a phone number](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number-verify) operation.

                          `null` if this User Profile has no verified phone number.
                        example: '+5555555555'
                        nullable: true
                        readOnly: true
                        format: phone
                  - type: object
                    description: |
                      The type of user on an account. Mostly applies to the use of the parent and child
                      accounts for Akamai partners functionality.
                    x-akamai:
                      file-path: schemas/user-type.yaml
                    additionalProperties: false
                    properties:
                      user_type:
                        type: string
                        description: |-
                          __Read-only__ If the user belongs to a [parent or child account](https://www.linode.com/docs/guides/parent-child-accounts/) relationship, this defines the user type in the respective account. Possible values include:

                          - `parent`. This is a user in an Akamai partner account. Akamai partners have a contractural relationship with their end customers, to sell Akamai services. This user can either have full access (a parent account admin user) or limited access. Limited users don't have access to manage child accounts, but they can be granted this access by an admin user.

                          - `child`. This is an Akamai partner's end customer user, in a child account. A child user can have either full or limited access. Full access lets the user manage other child users and the proxy user in a child account.

                          - `proxy`. This is a user on a child account that gives parent account users access to that child account. A parent account user with the `child_account_access` grant can [Create a proxy user token](https://techdocs.akamai.com/linode-api/reference/post-child-account-token) from the parent account. The parent user can use this token to run API operations from the child account, as if they were a child user.

                          - `default`. This applies to all regular, non-parent-child account users.
                        example: parent
                        readOnly: true
                        enum:
                        - parent
                        - child
                        - proxy
                        - default
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-user
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/account/users/example_user
      - lang: CLI
        source: linode-cli users view example_user
      x-linode-cli-action: view
      x-linode-grant: unrestricted only
      x-original-op-id: getUser
      x-original-op-title: User View
    put:
      operationId: put-user
      summary: Update a user
      tags:
      - Users
      description: |-
        Update information about a user on your account, including its restricted status. When setting a user to `restricted`, the API sets no grants for it. You need to set grants so that user can access things on the account.

        __Note__. This operation can only be accessed by account users with _unrestricted_ access.

        __Parent and child accounts__

        In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, the following apply:

        - You can't edit the `username` or `email` values for the child account parent user (proxy user). These are predefined for the proxy user when you initially provision the parent-child relationship. Only a proxy user's `restricted` status can be modified. This can only be done by an unrestricted child account user.

        - A parent account using an unrestricted proxy user in a child account can modify the `username`, `email`, and `restricted` status for an existing child account user.

        - A restricted account user--parent or child--can't change their user to `unrestricted`.


        ---


        - __CLI__.

            ```
            linode-cli users update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli users update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The information to update.
        content:
          application/json:
            schema:
              type: object
              description: |
                A User on your Account. Unrestricted users can log in and access information about your Account, while
                restricted users may only access entities or perform actions they've
                been granted access to.
              x-akamai:
                file-path: schemas/user.yaml
              additionalProperties: false
              properties:
                email:
                  type: string
                  description: |
                    The email address for the User. Linode sends emails to this address for account management
                    communications. May be used for other communications as configured.
                  example: '{{email}}'
                  format: email
                  x-linode-cli-display: 2
                last_login:
                  type: object
                  description: |-
                    __Read-only__ Information for the most recent login attempt for this User.

                    `null` if no login attempts have been made since creation of this User.

                    Run the [List user logins](https://techdocs.akamai.com/linode-api/reference/get-account-logins) operation for additional login information.
                  additionalProperties: false
                  nullable: true
                  readOnly: true
                  properties:
                    login_datetime:
                      type: string
                      description: |
                        __Read-only__ The date and time of this User's most recent login attempt.
                      example: '2018-01-01T01:01:01'
                      readOnly: true
                      format: date-time
                    status:
                      type: string
                      description: |
                        __Read-only__ The result of the most recent login attempt for this User.
                      example: successful
                      readOnly: true
                      enum:
                      - successful
                      - failed
                password_created:
                  type: string
                  description: |-
                    __Read-only__ The date and time when this User's current password was created.

                    User passwords are first created during the Account sign-up process, and updated using the [Reset Password](https://login.linode.com/forgot/password) webpage.

                    `null` if this User has not created a password yet.
                  example: '{{password_created}}'
                  nullable: true
                  readOnly: true
                  format: date-time
                restricted:
                  type: boolean
                  description: |
                    If true, the User must be granted access to perform actions or access entities on this
                    Account. Run [List a user's grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants)
                    for details on how to configure grants for a restricted User.
                  example: '{{restricted}}'
                  x-linode-cli-display: 3
                ssh_keys:
                  type: array
                  description: |-
                    __Read-only__ A list of SSH Key labels added by this User.

                    Users can add keys with the [Add an SSH key](https://techdocs.akamai.com/linode-api/reference/post-add-ssh-key) operation.

                    These keys are deployed when this User is included in the `authorized_users` field of the following requests:

                    - [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
                    - [Rebuild a Linode](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance)
                    - [Create a disk](https://techdocs.akamai.com/linode-api/reference/post-add-linode-disk)
                  example:
                  - home-pc
                  - laptop
                  readOnly: true
                  items:
                    type: string
                tfa_enabled:
                  type: boolean
                  description: |
                    __Read-only__ A boolean value indicating if the User has Two Factor Authentication (TFA)
                    enabled. Run the [Create a two factor secret](https://techdocs.akamai.com/linode-api/reference/post-tfa-enable)
                    operation to enable TFA.
                  example: '{{tfa_enabled}}'
                  readOnly: true
                username:
                  type: string
                  description: |
                    The User's username. This is used for logging in, and may also be displayed alongside
                    actions the User performs (for example, in Events or public StackScripts).
                  example: '{{username}}'
                  minLength: 3
                  maxLength: 32
                  pattern: ^[a-zA-Z0-9]((?![_-]{2,})[a-zA-Z0-9-_])+[a-zA-Z0-9]$
                  x-linode-cli-display: 1
                  x-linode-filterable: true
                verified_phone_number:
                  type: string
                  description: |-
                    __Read-only__ The phone number verified for this User Profile with the [Verify a phone number](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number-verify) operation.

                    `null` if this User Profile has no verified phone number.
                  example: '{{verified_phone_number}}'
                  nullable: true
                  readOnly: true
                  format: phone
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            User updated successfully.
          content:
            application/json:
              schema:
                allOf:
                - type: object
                  description: |
                    A User on your Account. Unrestricted users can log in and access information about your
                    Account, while restricted users may only access entities or perform
                    actions they've been granted access to.
                  x-akamai:
                    file-path: schemas/user.yaml
                  additionalProperties: false
                  properties:
                    email:
                      type: string
                      description: |
                        The email address for the User. Linode sends emails to this address for account
                        management communications. May be used for other communications
                        as configured.
                      example: example_user@linode.com
                      format: email
                      x-linode-cli-display: 2
                    last_login:
                      type: object
                      description: |-
                        __Read-only__ Information for the most recent login attempt for this User.

                        `null` if no login attempts have been made since creation of this User.

                        Run the [List user logins](https://techdocs.akamai.com/linode-api/reference/get-account-logins) operation for additional login information.
                      additionalProperties: false
                      nullable: true
                      readOnly: true
                      properties:
                        login_datetime:
                          type: string
                          description: |
                            __Read-only__ The date and time of this User's most recent login attempt.
                          example: '2018-01-01T01:01:01'
                          readOnly: true
                          format: date-time
                        status:
                          type: string
                          description: |
                            __Read-only__ The result of the most recent login attempt for this User.
                          example: successful
                          readOnly: true
                          enum:
                          - successful
                          - failed
                    password_created:
                      type: string
                      description: |-
                        __Read-only__ The date and time when this User's current password was created.

                        User passwords are first created during the Account sign-up process, and updated using the [Reset Password](https://login.linode.com/forgot/password) webpage.

                        `null` if this User has not created a password yet.
                      example: '2018-01-01T01:01:01'
                      nullable: true
                      readOnly: true
                      format: date-time
                    restricted:
                      type: boolean
                      description: |
                        If true, the User must be granted access to perform actions or access entities
                        on this Account. Run [List a user's grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants)
                        for details on how to configure grants for a restricted User.
                      example: true
                      x-linode-cli-display: 3
                    ssh_keys:
                      type: array
                      description: |-
                        __Read-only__ A list of SSH Key labels added by this User.

                        Users can add keys with the [Add an SSH key](https://techdocs.akamai.com/linode-api/reference/post-add-ssh-key) operation.

                        These keys are deployed when this User is included in the `authorized_users` field of the following requests:

                        - [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
                        - [Rebuild a Linode](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance)
                        - [Create a disk](https://techdocs.akamai.com/linode-api/reference/post-add-linode-disk)
                      example:
                      - home-pc
                      - laptop
                      readOnly: true
                      items:
                        type: string
                    tfa_enabled:
                      type: boolean
                      description: |
                        __Read-only__ A boolean value indicating if the User has Two Factor Authentication
                        (TFA) enabled. Run the [Create a two factor secret](https://techdocs.akamai.com/linode-api/reference/post-tfa-enable)
                        operation to enable TFA.
                      example: true
                      readOnly: true
                    username:
                      type: string
                      description: |
                        The User's username. This is used for logging in, and may also be displayed alongside
                        actions the User performs (for example, in Events or public
                        StackScripts).
                      example: example_user
                      minLength: 3
                      maxLength: 32
                      pattern: ^[a-zA-Z0-9]((?![_-]{2,})[a-zA-Z0-9-_])+[a-zA-Z0-9]$
                      x-linode-cli-display: 1
                      x-linode-filterable: true
                    verified_phone_number:
                      type: string
                      description: |-
                        __Read-only__ The phone number verified for this User Profile with the [Verify a phone number](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number-verify) operation.

                        `null` if this User Profile has no verified phone number.
                      example: '+5555555555'
                      nullable: true
                      readOnly: true
                      format: phone
                - type: object
                  description: |
                    The type of user on an account. Mostly applies to the use of the parent and child accounts
                    for Akamai partners functionality.
                  x-akamai:
                    file-path: schemas/user-type.yaml
                  additionalProperties: false
                  properties:
                    user_type:
                      type: string
                      description: |-
                        __Read-only__ If the user belongs to a [parent or child account](https://www.linode.com/docs/guides/parent-child-accounts/) relationship, this defines the user type in the respective account. Possible values include:

                        - `parent`. This is a user in an Akamai partner account. Akamai partners have a contractural relationship with their end customers, to sell Akamai services. This user can either have full access (a parent account admin user) or limited access. Limited users don't have access to manage child accounts, but they can be granted this access by an admin user.

                        - `child`. This is an Akamai partner's end customer user, in a child account. A child user can have either full or limited access. Full access lets the user manage other child users and the proxy user in a child account.

                        - `proxy`. This is a user on a child account that gives parent account users access to that child account. A parent account user with the `child_account_access` grant can [Create a proxy user token](https://techdocs.akamai.com/linode-api/reference/post-child-account-token) from the parent account. The parent user can use this token to run API operations from the child account, as if they were a child user.

                        - `default`. This applies to all regular, non-parent-child account users.
                      example: parent
                      readOnly: true
                      enum:
                      - parent
                      - child
                      - proxy
                      - default
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-user
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                  "username": example_user,
                  "email": example@linode.com,
                  "restricted": true
              }' \
              https://api.linode.com/v4/account/users/example_user
      - lang: CLI
        source: |-
          linode-cli users update example_user \
            --username example_user \
            --email example@linode.com \
            --restricted true
      x-linode-cli-action: update
      x-linode-grant: unrestricted only
      x-original-op-id: updateUser
      x-original-op-title: User Update
    delete:
      operationId: delete-user
      summary: Delete a user
      tags:
      - Users
      description: |-
        Deletes a user. The API immediately logs the user out and removes all of its `grants`.

        __Note__. This operation can only be accessed by account users with _unrestricted_ access.

        __Parent and child accounts__

        In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, the following apply:

        - You can't delete a child account parent user (proxy user). The API returns a 403 error if you target a proxy user with this operation.

        - A parent account using an unrestricted proxy user can use this operation to delete a child account user.


        ---


        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            User deleted successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-user
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/account/users/example_user
      - lang: CLI
        source: linode-cli users delete example_user
      x-linode-cli-action:
      - delete
      - rm
      x-linode-grant: unrestricted only
      x-original-op-id: deleteUser
      x-original-op-title: User Delete
    x-linode-cli-command: users
  /account/users/{username}/grants:
    x-akamai:
      file-path: paths/account-grants.yaml
      path-info: /account/users/{username}/grants
    parameters:
    - name: username
      in: path
      description: |
        The username to look up.
      example: '{{username}}'
      x-akamai:
        file-path: parameters/username-path.yaml
      required: true
      schema:
        type: string
    get:
      operationId: get-user-grants
      summary: List a user's grants
      tags:
      - Users
      description: |-
        Returns the full grants structure for an account username you specify. This includes all entities on the account, and the level of access this user has to each of them.

        This doesn't apply to the account owner or the current authenticated user. You can run the [List grants](https://techdocs.akamai.com/linode-api/reference/get-profile-grants) operation to view those grants. However, this doesn't show the entities that they _don't_ have access to.

        __Note__. This operation can only be accessed by account users with _unrestricted_ access.


        ---


        - __CLI__.

            ```
            linode-cli users grants
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli users grants
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The User's grants.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A structure representing all grants a restricted User has on the Account. Not available for
                  unrestricted users, as they have access to everything without grants.
                  If retrieved from the `/profile/grants` endpoint, entities to which
                  a User has no access will be omitted.
                x-akamai:
                  file-path: schemas/grants-response.yaml
                additionalProperties: false
                properties:
                  database:
                    type: array
                    description: |
                      The grants this User has for each Database that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  domain:
                    type: array
                    description: |
                      The grants this User has for each Domain that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  global:
                    type: object
                    description: |
                      A structure containing the Account-level grants a User has.
                    additionalProperties: false
                    properties:
                      account_access:
                        type: string
                        description: |-
                          The level of access this User has to Account-level actions, like billing information. A restricted User will never be able to manage users.

                          __Parent and child accounts__

                          In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, this grant can be added to a child account user, to give the user `read-write` access. This gives the child user unrestricted access to expected management operations, such as creating other child users. However, child users don't have write access to billing operations. The API issues a specific error message if a write operation is attempted by a child user.
                        example: read_only
                        nullable: true
                        enum:
                        - read_only
                        - read_write
                      add_databases:
                        type: boolean
                        description: |
                          If true, this User may add Managed Databases.
                        example: true
                      add_domains:
                        type: boolean
                        description: |
                          If true, this User may add Domains.
                        example: true
                      add_firewalls:
                        type: boolean
                        description: |
                          If true, this User may add Firewalls.
                        example: true
                      add_images:
                        type: boolean
                        description: |
                          If true, this User may add Images.
                        example: true
                      add_linodes:
                        type: boolean
                        description: |
                          If true, this User may create Linodes.
                        example: true
                      add_loadbalancers:
                        type: boolean
                        description: |
                          If true, this User may add Cloud Load Balancers.
                        example: true
                      add_longview:
                        type: boolean
                        description: |
                          If true, this User may create Longview clients and view the current plan.
                        example: true
                      add_nodebalancers:
                        type: boolean
                        description: |
                          If true, this User may add NodeBalancers.
                        example: true
                      add_stackscripts:
                        type: boolean
                        description: |
                          If true, this User may add StackScripts.
                        example: true
                      add_volumes:
                        type: boolean
                        description: |
                          If true, this User may add Volumes.
                        example: true
                      add_vpcs:
                        type: boolean
                        description: |
                          If true, this User may add VPCs.
                        example: true
                      cancel_account:
                        type: boolean
                        description: |
                          If true, this User may cancel the entire Account.
                        example: false
                      child_account_access:
                        type: boolean
                        description: |
                          In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/)
                          environment, this gives a parent account access to endpoints
                          that can be used to manage child accounts. Unrestricted
                          parent account users have access to this grant, while restricted
                          parent users don't. An unrestricted parent user can set
                          this to `true` to add this grant to a restricted parent
                          user. Displayed as `null` for all non-parent accounts.
                        example: true
                        nullable: true
                      longview_subscription:
                        type: boolean
                        description: |
                          If true, this User may manage the Account's Longview subscription.
                        example: true
                  image:
                    type: array
                    description: |
                      The grants this User has for each Image that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  linode:
                    type: array
                    description: |
                      The grants this User has for each Linode that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  loadbalancer:
                    type: array
                    description: |
                      The grants this User has for each Cloud Load Balancer that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  longview:
                    type: array
                    description: |
                      The grants this User has for each Longview Client that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  nodebalancer:
                    type: array
                    description: |
                      The grants this User has for each NodeBalancer that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  stackscript:
                    type: array
                    description: |
                      The grants this User has for each StackScript that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  volume:
                    type: array
                    description: |
                      The grants this User has for each Block Storage Volume that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  vpc:
                    type: array
                    description: |
                      The grants this User has for each VPC that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
              x-example:
                x-ref: ../examples/tbd.json
        204:
          description: |
            This is an unrestricted User, and therefore has no grants to return. This User may access everything
            on the Account and perform all actions.
          content:
            application/json: {}
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-user-grants
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/account/users/example_user/grants
      x-linode-cli-action: grants
      x-linode-cli-skip: true
      x-linode-grant: unrestricted only
      x-original-op-id: getUserGrants
      x-original-op-title: User's Grants View
    put:
      operationId: put-user-grants
      summary: Update a user's grants
      tags:
      - Users
      description: |-
        Update the grants a user has. This can be used to give a user access to new entities or actions, or take access away.  You don't need to include the grant for every entity on the account in this request. Any that are not included remain unchanged.

        __Note__. This operation can only be accessed by account users with _unrestricted_ access.

        __Parent and child accounts__

        In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, the following apply:

        - No child account user can modify the `account_access` grant for the child account parent user (proxy user).

        - An unrestricted child account user can configure all other grants for the proxy user, via `global` object.

        - An unrestricted child account user can enable the `account_access` grant for other child account users. However, enabled child users are still subject to child user restrictions--they can't perform write operations for any billing or account information.


        ---


        - __CLI__.

            ```
            linode-cli users update-grants
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli users update-grants
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The grants to update. Omitted grants will be left unchanged.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                A structure representing all grants a restricted User has on the Account. Not available for
                unrestricted users, as they have access to everything without grants.
                If retrieved from the `/profile/grants` endpoint, entities to which
                a User has no access will be omitted.
              x-akamai:
                file-path: schemas/grants-response.yaml
              additionalProperties: false
              properties:
                database:
                  type: array
                  description: |
                    The grants this User has for each Database that is owned by this Account.
                  items:
                    type: object
                    description: |
                      Represents the level of access a restricted User has to a specific resource on the
                      Account.
                    x-akamai:
                      file-path: schemas/grant.yaml
                    additionalProperties: false
                    properties:
                      id:
                        type: integer
                        description: |
                          The ID of the entity this grant applies to.
                        example: 123
                      label:
                        type: string
                        description: |
                          __Read-only__ The current label of the entity this grant applies to, for display
                          purposes.
                        example: example-entity
                        readOnly: true
                      permissions:
                        type: string
                        description: |
                          The level of access this User has to this entity.  If null, this User has no
                          access.
                        example: read_only
                        nullable: true
                        enum:
                        - read_only
                        - read_write
                domain:
                  type: array
                  description: |
                    The grants this User has for each Domain that is owned by this Account.
                  items:
                    type: object
                    description: |
                      Represents the level of access a restricted User has to a specific resource on the
                      Account.
                    x-akamai:
                      file-path: schemas/grant.yaml
                    additionalProperties: false
                    properties:
                      id:
                        type: integer
                        description: |
                          The ID of the entity this grant applies to.
                        example: 123
                      label:
                        type: string
                        description: |
                          __Read-only__ The current label of the entity this grant applies to, for display
                          purposes.
                        example: example-entity
                        readOnly: true
                      permissions:
                        type: string
                        description: |
                          The level of access this User has to this entity.  If null, this User has no
                          access.
                        example: read_only
                        nullable: true
                        enum:
                        - read_only
                        - read_write
                global:
                  type: object
                  description: |
                    A structure containing the Account-level grants a User has.
                  additionalProperties: false
                  properties:
                    account_access:
                      type: string
                      description: |-
                        The level of access this User has to Account-level actions, like billing information. A restricted User will never be able to manage users.

                        __Parent and child accounts__

                        In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, this grant can be added to a child account user, to give the user `read-write` access. This gives the child user unrestricted access to expected management operations, such as creating other child users. However, child users don't have write access to billing operations. The API issues a specific error message if a write operation is attempted by a child user.
                      example: read_only
                      nullable: true
                      enum:
                      - read_only
                      - read_write
                    add_databases:
                      type: boolean
                      description: |
                        If true, this User may add Managed Databases.
                      example: true
                    add_domains:
                      type: boolean
                      description: |
                        If true, this User may add Domains.
                      example: true
                    add_firewalls:
                      type: boolean
                      description: |
                        If true, this User may add Firewalls.
                      example: true
                    add_images:
                      type: boolean
                      description: |
                        If true, this User may add Images.
                      example: true
                    add_linodes:
                      type: boolean
                      description: |
                        If true, this User may create Linodes.
                      example: true
                    add_loadbalancers:
                      type: boolean
                      description: |
                        If true, this User may add Cloud Load Balancers.
                      example: true
                    add_longview:
                      type: boolean
                      description: |
                        If true, this User may create Longview clients and view the current plan.
                      example: true
                    add_nodebalancers:
                      type: boolean
                      description: |
                        If true, this User may add NodeBalancers.
                      example: true
                    add_stackscripts:
                      type: boolean
                      description: |
                        If true, this User may add StackScripts.
                      example: true
                    add_volumes:
                      type: boolean
                      description: |
                        If true, this User may add Volumes.
                      example: true
                    add_vpcs:
                      type: boolean
                      description: |
                        If true, this User may add VPCs.
                      example: true
                    cancel_account:
                      type: boolean
                      description: |
                        If true, this User may cancel the entire Account.
                      example: false
                    child_account_access:
                      type: boolean
                      description: |
                        In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/)
                        environment, this gives a parent account access to endpoints
                        that can be used to manage child accounts. Unrestricted parent
                        account users have access to this grant, while restricted
                        parent users don't. An unrestricted parent user can set this
                        to `true` to add this grant to a restricted parent user. Displayed
                        as `null` for all non-parent accounts.
                      example: true
                      nullable: true
                    longview_subscription:
                      type: boolean
                      description: |
                        If true, this User may manage the Account's Longview subscription.
                      example: true
                image:
                  type: array
                  description: |
                    The grants this User has for each Image that is owned by this Account.
                  items:
                    type: object
                    description: |
                      Represents the level of access a restricted User has to a specific resource on the
                      Account.
                    x-akamai:
                      file-path: schemas/grant.yaml
                    additionalProperties: false
                    properties:
                      id:
                        type: integer
                        description: |
                          The ID of the entity this grant applies to.
                        example: 123
                      label:
                        type: string
                        description: |
                          __Read-only__ The current label of the entity this grant applies to, for display
                          purposes.
                        example: example-entity
                        readOnly: true
                      permissions:
                        type: string
                        description: |
                          The level of access this User has to this entity.  If null, this User has no
                          access.
                        example: read_only
                        nullable: true
                        enum:
                        - read_only
                        - read_write
                linode:
                  type: array
                  description: |
                    The grants this User has for each Linode that is owned by this Account.
                  items:
                    type: object
                    description: |
                      Represents the level of access a restricted User has to a specific resource on the
                      Account.
                    x-akamai:
                      file-path: schemas/grant.yaml
                    additionalProperties: false
                    properties:
                      id:
                        type: integer
                        description: |
                          The ID of the entity this grant applies to.
                        example: 123
                      label:
                        type: string
                        description: |
                          __Read-only__ The current label of the entity this grant applies to, for display
                          purposes.
                        example: example-entity
                        readOnly: true
                      permissions:
                        type: string
                        description: |
                          The level of access this User has to this entity.  If null, this User has no
                          access.
                        example: read_only
                        nullable: true
                        enum:
                        - read_only
                        - read_write
                loadbalancer:
                  type: array
                  description: |
                    The grants this User has for each Cloud Load Balancer that is owned by this Account.
                  items:
                    type: object
                    description: |
                      Represents the level of access a restricted User has to a specific resource on the
                      Account.
                    x-akamai:
                      file-path: schemas/grant.yaml
                    additionalProperties: false
                    properties:
                      id:
                        type: integer
                        description: |
                          The ID of the entity this grant applies to.
                        example: 123
                      label:
                        type: string
                        description: |
                          __Read-only__ The current label of the entity this grant applies to, for display
                          purposes.
                        example: example-entity
                        readOnly: true
                      permissions:
                        type: string
                        description: |
                          The level of access this User has to this entity.  If null, this User has no
                          access.
                        example: read_only
                        nullable: true
                        enum:
                        - read_only
                        - read_write
                longview:
                  type: array
                  description: |
                    The grants this User has for each Longview Client that is owned by this Account.
                  items:
                    type: object
                    description: |
                      Represents the level of access a restricted User has to a specific resource on the
                      Account.
                    x-akamai:
                      file-path: schemas/grant.yaml
                    additionalProperties: false
                    properties:
                      id:
                        type: integer
                        description: |
                          The ID of the entity this grant applies to.
                        example: 123
                      label:
                        type: string
                        description: |
                          __Read-only__ The current label of the entity this grant applies to, for display
                          purposes.
                        example: example-entity
                        readOnly: true
                      permissions:
                        type: string
                        description: |
                          The level of access this User has to this entity.  If null, this User has no
                          access.
                        example: read_only
                        nullable: true
                        enum:
                        - read_only
                        - read_write
                nodebalancer:
                  type: array
                  description: |
                    The grants this User has for each NodeBalancer that is owned by this Account.
                  items:
                    type: object
                    description: |
                      Represents the level of access a restricted User has to a specific resource on the
                      Account.
                    x-akamai:
                      file-path: schemas/grant.yaml
                    additionalProperties: false
                    properties:
                      id:
                        type: integer
                        description: |
                          The ID of the entity this grant applies to.
                        example: 123
                      label:
                        type: string
                        description: |
                          __Read-only__ The current label of the entity this grant applies to, for display
                          purposes.
                        example: example-entity
                        readOnly: true
                      permissions:
                        type: string
                        description: |
                          The level of access this User has to this entity.  If null, this User has no
                          access.
                        example: read_only
                        nullable: true
                        enum:
                        - read_only
                        - read_write
                stackscript:
                  type: array
                  description: |
                    The grants this User has for each StackScript that is owned by this Account.
                  items:
                    type: object
                    description: |
                      Represents the level of access a restricted User has to a specific resource on the
                      Account.
                    x-akamai:
                      file-path: schemas/grant.yaml
                    additionalProperties: false
                    properties:
                      id:
                        type: integer
                        description: |
                          The ID of the entity this grant applies to.
                        example: 123
                      label:
                        type: string
                        description: |
                          __Read-only__ The current label of the entity this grant applies to, for display
                          purposes.
                        example: example-entity
                        readOnly: true
                      permissions:
                        type: string
                        description: |
                          The level of access this User has to this entity.  If null, this User has no
                          access.
                        example: read_only
                        nullable: true
                        enum:
                        - read_only
                        - read_write
                volume:
                  type: array
                  description: |
                    The grants this User has for each Block Storage Volume that is owned by this Account.
                  items:
                    type: object
                    description: |
                      Represents the level of access a restricted User has to a specific resource on the
                      Account.
                    x-akamai:
                      file-path: schemas/grant.yaml
                    additionalProperties: false
                    properties:
                      id:
                        type: integer
                        description: |
                          The ID of the entity this grant applies to.
                        example: 123
                      label:
                        type: string
                        description: |
                          __Read-only__ The current label of the entity this grant applies to, for display
                          purposes.
                        example: example-entity
                        readOnly: true
                      permissions:
                        type: string
                        description: |
                          The level of access this User has to this entity.  If null, this User has no
                          access.
                        example: read_only
                        nullable: true
                        enum:
                        - read_only
                        - read_write
                vpc:
                  type: array
                  description: |
                    The grants this User has for each VPC that is owned by this Account.
                  items:
                    type: object
                    description: |
                      Represents the level of access a restricted User has to a specific resource on the
                      Account.
                    x-akamai:
                      file-path: schemas/grant.yaml
                    additionalProperties: false
                    properties:
                      id:
                        type: integer
                        description: |
                          The ID of the entity this grant applies to.
                        example: 123
                      label:
                        type: string
                        description: |
                          __Read-only__ The current label of the entity this grant applies to, for display
                          purposes.
                        example: example-entity
                        readOnly: true
                      permissions:
                        type: string
                        description: |
                          The level of access this User has to this entity.  If null, this User has no
                          access.
                        example: read_only
                        nullable: true
                        enum:
                        - read_only
                        - read_write
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Grants updated successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A structure representing all grants a restricted User has on the Account. Not available for
                  unrestricted users, as they have access to everything without grants.
                  If retrieved from the `/profile/grants` endpoint, entities to which
                  a User has no access will be omitted.
                x-akamai:
                  file-path: schemas/grants-response.yaml
                additionalProperties: false
                properties:
                  database:
                    type: array
                    description: |
                      The grants this User has for each Database that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  domain:
                    type: array
                    description: |
                      The grants this User has for each Domain that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  global:
                    type: object
                    description: |
                      A structure containing the Account-level grants a User has.
                    additionalProperties: false
                    properties:
                      account_access:
                        type: string
                        description: |-
                          The level of access this User has to Account-level actions, like billing information. A restricted User will never be able to manage users.

                          __Parent and child accounts__

                          In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, this grant can be added to a child account user, to give the user `read-write` access. This gives the child user unrestricted access to expected management operations, such as creating other child users. However, child users don't have write access to billing operations. The API issues a specific error message if a write operation is attempted by a child user.
                        example: read_only
                        nullable: true
                        enum:
                        - read_only
                        - read_write
                      add_databases:
                        type: boolean
                        description: |
                          If true, this User may add Managed Databases.
                        example: true
                      add_domains:
                        type: boolean
                        description: |
                          If true, this User may add Domains.
                        example: true
                      add_firewalls:
                        type: boolean
                        description: |
                          If true, this User may add Firewalls.
                        example: true
                      add_images:
                        type: boolean
                        description: |
                          If true, this User may add Images.
                        example: true
                      add_linodes:
                        type: boolean
                        description: |
                          If true, this User may create Linodes.
                        example: true
                      add_loadbalancers:
                        type: boolean
                        description: |
                          If true, this User may add Cloud Load Balancers.
                        example: true
                      add_longview:
                        type: boolean
                        description: |
                          If true, this User may create Longview clients and view the current plan.
                        example: true
                      add_nodebalancers:
                        type: boolean
                        description: |
                          If true, this User may add NodeBalancers.
                        example: true
                      add_stackscripts:
                        type: boolean
                        description: |
                          If true, this User may add StackScripts.
                        example: true
                      add_volumes:
                        type: boolean
                        description: |
                          If true, this User may add Volumes.
                        example: true
                      add_vpcs:
                        type: boolean
                        description: |
                          If true, this User may add VPCs.
                        example: true
                      cancel_account:
                        type: boolean
                        description: |
                          If true, this User may cancel the entire Account.
                        example: false
                      child_account_access:
                        type: boolean
                        description: |
                          In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/)
                          environment, this gives a parent account access to endpoints
                          that can be used to manage child accounts. Unrestricted
                          parent account users have access to this grant, while restricted
                          parent users don't. An unrestricted parent user can set
                          this to `true` to add this grant to a restricted parent
                          user. Displayed as `null` for all non-parent accounts.
                        example: true
                        nullable: true
                      longview_subscription:
                        type: boolean
                        description: |
                          If true, this User may manage the Account's Longview subscription.
                        example: true
                  image:
                    type: array
                    description: |
                      The grants this User has for each Image that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  linode:
                    type: array
                    description: |
                      The grants this User has for each Linode that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  loadbalancer:
                    type: array
                    description: |
                      The grants this User has for each Cloud Load Balancer that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  longview:
                    type: array
                    description: |
                      The grants this User has for each Longview Client that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  nodebalancer:
                    type: array
                    description: |
                      The grants this User has for each NodeBalancer that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  stackscript:
                    type: array
                    description: |
                      The grants this User has for each StackScript that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  volume:
                    type: array
                    description: |
                      The grants this User has for each Block Storage Volume that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
                  vpc:
                    type: array
                    description: |
                      The grants this User has for each VPC that is owned by this Account.
                    items:
                      type: object
                      description: |
                        Represents the level of access a restricted User has to a specific resource on
                        the Account.
                      x-akamai:
                        file-path: schemas/grant.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The ID of the entity this grant applies to.
                          example: 123
                        label:
                          type: string
                          description: |
                            __Read-only__ The current label of the entity this grant applies to,
                            for display purposes.
                          example: example-entity
                          readOnly: true
                        permissions:
                          type: string
                          description: |
                            The level of access this User has to this entity.  If null, this User
                            has no access.
                          example: read_only
                          nullable: true
                          enum:
                          - read_only
                          - read_write
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-user-grants
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                  "global": {
                    "add_linodes": true,
                    "add_loadbalancers": true,
                    "add_nodebalancers": false,
                    "add_databases": true,
                    "add_domains": true,
                    "add_longview": false,
                    "add_stackscripts": true,
                    "longview_subscription": true,
                    "add_images": true,
                    "add_volumes": true,
                    "add_firewalls": true,
                    "account_access": "read_only",
                    "cancel_account": false,
                    "add_vpcs": true
                  },
                  "domain": [
                    {
                      "id": 123,
                      "permissions": "read_only"
                    }
                  ],
                  "image": [
                    {
                      "id": 123,
                      "permissions": "read_only"
                    }
                  ],
                  "linode": [
                    {
                      "id": 123,
                      "permissions": "read_only"
                    },
                    {
                      "id": 234,
                      "permissions": "read_write"
                    },
                    {
                      "id": 345,
                      "permissions": "read_only"
                    }
                  ],
                  "longview": [
                    {
                      "id": 123,
                      "permissions": "read_only"
                    },
                    {
                      "id": 234,
                      "permissions": "read_write"
                    }
                  ],
                  "nodebalancer": [
                    {
                      "id": 123,
                      "permissions": "read_write"
                    }
                  ],
                  "stackscript": [
                    {
                      "id": 123,
                      "permissions": "read_only"
                    },
                    {
                      "id": 124,
                      "permissions": "read_write"
                    }
                  ],
                  "volume": [
                    {
                      "id": 123,
                      "permissions": "read_only"
                    }
                  ],
                  "vpc": [
                    {
                      "id": 123,
                      "permissions": "read_write"
                    }
                  ]
              }' \
              https://api.linode.com/v4/account/users/example_user/grants
      x-linode-cli-action: update-grants
      x-linode-cli-skip: true
      x-linode-grant: unrestricted only
      x-original-op-id: updateUserGrants
      x-original-op-title: User's Grants Update
    x-linode-cli-command: users
  /aclb:
    x-akamai:
      file-path: paths/aclb.yaml
      path-info: /aclb
    post:
      servers:
      - url: https://api.linode.com/v4beta
      operationId: post-load-balancer
      summary: Create a cloud load balancer
      tags:
      - Cloud load balancers
      description: |-
        __Beta__ For BETA, this operation only creates the Cloud Load Balancer `label`. After you create a label, the Cloud Load Balancer ID is generated.


        ---


        - __CLI__.

            ```
            linode-cli aclb create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            aclb:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: BETA
        tabs:
        - title: CLI
          syntax: linode-cli aclb create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: aclb:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Cloud Load Balancer settings.
        required: true
        content:
          application/json:
            schema:
              additionalProperties: false
              required:
              - label
              - regions
              properties:
                label:
                  type: string
                  description: |
                    This Cloud Load Balancer's label. Labels must be unique on your Account.
                  example: balancer12345
                  x-linode-cli-display: 3
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
        x-linode-cli-allowed-defaults:
        - region
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Cloud Load Balancer created successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Akamai Cloud Load Balancer offers global, configurable, scalable, distributed compute traffic
                  management across physical, virtual, and cloud-hosted applications.
                  It can automatically detect load conditions and levels and route
                  traffic to the optimal target while maintaining custom routing policies
                  and consistent visitor session behavior.
                x-akamai:
                  file-path: schemas/aclb-load-balancer.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Cloud Load Balancer's unique ID.
                    example: 12345
                    readOnly: true
                    x-linode-cli-display: 1
                  tags:
                    type: object
                    description: |
                      Optional tags to help organize and group your load balancers.
                    example:
                    - test-aclb
                    - production-aclb
                    items:
                      type: string
                    x-linode-cli-display: 2
                  configurations:
                    type: array
                    description: |
                      Cloud Load Balancer configurations.
                    items:
                      type: object
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The configurations assigned id.
                          example: 125
                          x-linode-cli-display: 1
                        label:
                          type: string
                          description: |
                            The configuration label.
                          example: config-12345
                          x-linode-cli-display: 2
                  hostname:
                    type: string
                    description: |
                      __Read-only__ This Cloud Load Balancer's hostname.
                    example: example.mesh.akadns.net
                    readOnly: true
                    x-linode-cli-display: 4
                  label:
                    type: string
                    description: |
                      This Cloud Load Balancer's label. Labels must be unique on your Account.
                    example: balancer12345
                    x-linode-cli-display: 3
                  regions:
                    type: object
                    description: |
                      Regions this Cloud Load Balancer covers. To get the regions available for Cloud
                      Load Balancer and other services, refer to the Regions List
                      API.
                    example:
                    - us-iad
                    - ap-southeast
                    items:
                      type: string
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-load-balancer
      security:
      - personalAccessToken: []
      - oauth:
        - aclb:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "label": "balancer12345",
          }' \
              https://api.linode.com/v4beta/aclb
      - lang: CLI
        source: |-
          linode-cli loadbalancers create \
            --region us-east \
            --label balancer12345 \
            --rate_capacity 10000
      x-linode-cli-action: create
      x-linode-grant: add_loadbalancers
      x-original-op-id: createLoadBalancer
      x-original-op-title: Cloud Load Balancer Create
    get:
      servers:
      - url: https://api.linode.com/v4beta
      operationId: get-load-balancers
      summary: List cloud load balancers
      tags:
      - Cloud load balancers
      description: |-
        __Beta__ Returns a paginated list of Cloud Load Balancers you have access to.


        ---


        - __OAuth__.

            ```
            aclb:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: BETA
        tabs:
        - title: OAuth
          syntax: aclb:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated lists of Cloud Load Balancers.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        Akamai Cloud Load Balancer offers global, configurable, scalable, distributed
                        compute traffic management across physical, virtual, and cloud-hosted
                        applications. It can automatically detect load conditions
                        and levels and route traffic to the optimal target while maintaining
                        custom routing policies and consistent visitor session behavior.
                      x-akamai:
                        file-path: schemas/aclb-load-balancer.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ This Cloud Load Balancer's unique ID.
                          example: 12345
                          readOnly: true
                          x-linode-cli-display: 1
                        tags:
                          type: object
                          description: |
                            Optional tags to help organize and group your load balancers.
                          example:
                          - test-aclb
                          - production-aclb
                          items:
                            type: string
                          x-linode-cli-display: 2
                        configurations:
                          type: array
                          description: |
                            Cloud Load Balancer configurations.
                          items:
                            type: object
                            additionalProperties: false
                            properties:
                              id:
                                type: integer
                                description: |
                                  The configurations assigned id.
                                example: 125
                                x-linode-cli-display: 1
                              label:
                                type: string
                                description: |
                                  The configuration label.
                                example: config-12345
                                x-linode-cli-display: 2
                        hostname:
                          type: string
                          description: |
                            __Read-only__ This Cloud Load Balancer's hostname.
                          example: example.mesh.akadns.net
                          readOnly: true
                          x-linode-cli-display: 4
                        label:
                          type: string
                          description: |
                            This Cloud Load Balancer's label. Labels must be unique on your Account.
                          example: balancer12345
                          x-linode-cli-display: 3
                        regions:
                          type: object
                          description: |
                            Regions this Cloud Load Balancer covers. To get the regions available
                            for Cloud Load Balancer and other services, refer to the
                            Regions List API.
                          example:
                          - us-iad
                          - ap-southeast
                          items:
                            type: string
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-load-balancers
      security:
      - personalAccessToken: []
      - oauth:
        - aclb:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4beta/aclb
      - lang: CLI
        source: linode-cli loadbalancers list
      x-linode-cli-action:
      - list
      - ls
      x-linode-grant: read_only
      x-original-op-id: getLoadBalancers
      x-original-op-title: Cloud Load Balancers List
    x-linode-cli-command: aclb
  /aclb/{cloudLoadBalancerId}:
    x-akamai:
      file-path: paths/cloud-load-balancer.yaml
      path-info: /aclb/{cloudLoadBalancerId}
    parameters:
    - name: cloudLoadBalancerId
      in: path
      description: |
        The ID of the Cloud Load Balancer to access.
      example: '{{cloudLoadBalancerId}}'
      x-akamai:
        file-path: parameters/cloud-load-balancer-id.yaml
      required: true
      schema:
        type: integer
    get:
      servers:
      - url: https://api.linode.com/v4beta
      operationId: get-load-balancer
      summary: Get a cloud load balancer
      tags:
      - Cloud load balancers
      description: |-
        __Beta__ Returns a single Cloud Load Balancer you can access.


        ---


        - __CLI__.

            ```
            linode-cli aclb view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            aclb:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: BETA
        tabs:
        - title: CLI
          syntax: linode-cli aclb view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: aclb:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The requested Cloud Load Balancer object.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Akamai Cloud Load Balancer offers global, configurable, scalable, distributed compute traffic
                  management across physical, virtual, and cloud-hosted applications.
                  It can automatically detect load conditions and levels and route
                  traffic to the optimal target while maintaining custom routing policies
                  and consistent visitor session behavior.
                x-akamai:
                  file-path: schemas/aclb-load-balancer.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Cloud Load Balancer's unique ID.
                    example: 12345
                    readOnly: true
                    x-linode-cli-display: 1
                  tags:
                    type: object
                    description: |
                      Optional tags to help organize and group your load balancers.
                    example:
                    - test-aclb
                    - production-aclb
                    items:
                      type: string
                    x-linode-cli-display: 2
                  configurations:
                    type: array
                    description: |
                      Cloud Load Balancer configurations.
                    items:
                      type: object
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The configurations assigned id.
                          example: 125
                          x-linode-cli-display: 1
                        label:
                          type: string
                          description: |
                            The configuration label.
                          example: config-12345
                          x-linode-cli-display: 2
                  hostname:
                    type: string
                    description: |
                      __Read-only__ This Cloud Load Balancer's hostname.
                    example: example.mesh.akadns.net
                    readOnly: true
                    x-linode-cli-display: 4
                  label:
                    type: string
                    description: |
                      This Cloud Load Balancer's label. Labels must be unique on your Account.
                    example: balancer12345
                    x-linode-cli-display: 3
                  regions:
                    type: object
                    description: |
                      Regions this Cloud Load Balancer covers. To get the regions available for Cloud
                      Load Balancer and other services, refer to the Regions List
                      API.
                    example:
                    - us-iad
                    - ap-southeast
                    items:
                      type: string
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-load-balancer
      security:
      - personalAccessToken: []
      - oauth:
        - aclb:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4beta/aclb/12345
      - lang: CLI
        source: linode-cli loadbalancers view 12345
      x-linode-cli-action: view
      x-linode-grant: read_only
      x-original-op-id: getLoadBalancer
      x-original-op-title: Cloud Load Balancer View
    put:
      servers:
      - url: https://api.linode.com/v4beta
      operationId: put-load-balancer
      summary: Update a cloud load balancer
      tags:
      - Cloud load balancers
      description: |-
        __Beta__ Updates settings for a Cloud Load Balancer you can access.


        ---


        - __CLI__.

            ```
            linode-cli aclb update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            aclb:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: BETA
        tabs:
        - title: CLI
          syntax: linode-cli aclb update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: aclb:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The settings to update.
        required: true
        content:
          application/json:
            schema:
              additionalProperties: false
              required:
              - region
              properties:
                tags:
                  type: object
                  description: |
                    Optional tags to help organize and group your load balancers.
                  example:
                  - test-aclb
                  - production-aclb
                  items:
                    type: string
                  x-linode-cli-display: 2
                label:
                  type: string
                  description: |
                    This Cloud Load Balancer's label. Labels must be unique on your Account.
                  example: balancer12345
                  x-linode-cli-display: 3
                regions:
                  type: object
                  description: |
                    Regions this Cloud Load Balancer covers. To get the regions available for Cloud Load Balancer
                    and other services, refer to the Regions List API.
                  example:
                  - us-iad
                  - ap-southeast
                  items:
                    type: string
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Cloud Load Balancer settings updated successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Akamai Cloud Load Balancer offers global, configurable, scalable, distributed compute traffic
                  management across physical, virtual, and cloud-hosted applications.
                  It can automatically detect load conditions and levels and route
                  traffic to the optimal target while maintaining custom routing policies
                  and consistent visitor session behavior.
                x-akamai:
                  file-path: schemas/aclb-load-balancer.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Cloud Load Balancer's unique ID.
                    example: 12345
                    readOnly: true
                    x-linode-cli-display: 1
                  tags:
                    type: object
                    description: |
                      Optional tags to help organize and group your load balancers.
                    example:
                    - test-aclb
                    - production-aclb
                    items:
                      type: string
                    x-linode-cli-display: 2
                  configurations:
                    type: array
                    description: |
                      Cloud Load Balancer configurations.
                    items:
                      type: object
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The configurations assigned id.
                          example: 125
                          x-linode-cli-display: 1
                        label:
                          type: string
                          description: |
                            The configuration label.
                          example: config-12345
                          x-linode-cli-display: 2
                  hostname:
                    type: string
                    description: |
                      __Read-only__ This Cloud Load Balancer's hostname.
                    example: example.mesh.akadns.net
                    readOnly: true
                    x-linode-cli-display: 4
                  label:
                    type: string
                    description: |
                      This Cloud Load Balancer's label. Labels must be unique on your Account.
                    example: balancer12345
                    x-linode-cli-display: 3
                  regions:
                    type: object
                    description: |
                      Regions this Cloud Load Balancer covers. To get the regions available for Cloud
                      Load Balancer and other services, refer to the Regions List
                      API.
                    example:
                    - us-iad
                    - ap-southeast
                    items:
                      type: string
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-load-balancer
      security:
      - personalAccessToken: []
      - oauth:
        - aclb:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "label": "balancer12345",
                "tags": [
                  "test-aclb",
                  "production-aclb",
                ],
                "regions": [
                  "us-iad",
                  "ap-southeast",
              }' \
              https://api.linode.com/v4beta/aclb/12345
      - lang: CLI
        source: |-
          linode-cli loadbalancers update 12345 \
            --label balancer12345 \
            --tags test-aclb, production-aclb \
            --regions us-iad, ap-southeast
      x-linode-cli-action: update
      x-linode-grant: read_write
      x-original-op-id: updateLoadBalancer
      x-original-op-title: Cloud Load Balancer Update
    delete:
      servers:
      - url: https://api.linode.com/v4beta
      operationId: delete-load-balancer
      summary: Delete a cloud load balancer
      tags:
      - Cloud load balancers
      description: |-
        __Beta__ Deletes a Cloud Load Balancer.

        __This is a destructive action and cannot be undone.__

        Deleting a Cloud Load Balancer deletes all associated configurations.

        Routes and service targets are not deleted and available to use on other Cloud Load Balancers.


        ---


        - __OAuth__.

            ```
            aclb:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: BETA
        tabs:
        - title: OAuth
          syntax: aclb:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The Cloud Load Balancer deleted successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-load-balancer
      security:
      - personalAccessToken: []
      - oauth:
        - aclb:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4beta/aclb/12345
      - lang: CLI
        source: linode-cli loadbalancers delete 12345
      x-linode-cli-action:
      - delete
      - rm
      x-linode-grant: read_write
      x-original-op-id: deleteLoadBalancer
      x-original-op-title: Cloud Load Balancer Delete
    x-linode-cli-command: aclb
  /aclb/{cloudLoadBalancerId}/certificates:
    x-akamai:
      file-path: paths/certificates.yaml
      path-info: /aclb/{cloudLoadBalancerId}/certificates
    parameters:
    - name: cloudLoadBalancerId
      in: path
      description: |
        The ID of the Cloud Load Balancer to access.
      example: '{{cloudLoadBalancerId}}'
      x-akamai:
        file-path: parameters/cloud-load-balancer-id.yaml
      required: true
      schema:
        type: integer
    post:
      servers:
      - url: https://api.linode.com/v4beta
      operationId: post-load-balancer-certificate
      summary: Create a certificate
      tags:
      - Certificates
      description: |-
        __Beta__ Creates a certificate.

        Service target certificates, `ca`, are used by the load balancer to accept responses from your service target endpoints. These certificate(s) are applied to service target endpoints using the generated numeric identifier, `id`.

        TLS termination certificates, `downstream`, are required for HTTPS Cloud Load Balancers. They are used by the load balancer to terminate the connection and decrypt requests from clients. These certificate(s) are applied to configurations using the generated numeric identifier, `id`.


        ---


        - __CLI__.

            ```
            linode-cli loadbalancers certificate-create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            aclb:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: BETA
        tabs:
        - title: CLI
          syntax: linode-cli loadbalancers certificate-create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: aclb:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Create an aclb certificate and return a numeric identifier to be used in the Configuration creation endpoint.
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                type:
                  type: string
                  description: |
                    For TLS Certificates use `downstream`. For Service Target Certificates use `ca`.
                  example: '{{type}}'
                  enum:
                  - ca
                  - downstream
                certificate:
                  type: string
                  description: |
                    The PEM-formatted contents of the tls certificate.
                  example: '{{certificate}}'
                key:
                  type: integer
                  description: |-
                    The PEM-formatted contents of your private key. Your private key must not have a passphrase.

                    For `ca` certificates, the key must be blank.

                    For `downstream` certificates, the key must be populated.
                  example: '{{key}}'
                label:
                  type: string
                  description: |
                    The certificate name.
                  example: '{{label}}'
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Certificate created successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  TLS termination certificates create an encrypted link between your clients and Cloud Load Balancer
                  and terminate incoming traffic on the load balancer. Once the load
                  balancing policy is applied, traffic is forwarded to your service
                  targets over encrypted TLS connections.
                x-akamai:
                  file-path: schemas/aclb-certificate.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      This certificate's generated ID.
                    example: 10
                  type:
                    type: string
                    description: |
                      Service target certificates, `ca`, are used by the load balancer to accept responses from
                      your service target endpoints. TLS termination certificates,
                      `downstream`, are required for HTTPS Cloud Load Balancers.
                    example: downstream
                    enum:
                    - ca
                    - downstream
                  certificate:
                    type: string
                    description: |-
                      The PEM-formatted contents of your certificate.

                      __Note:__ `certificate` is not returned for BETA.
                    example: |-
                      -----BEGIN CERTIFICATE-----
                      CERTIFICATE_INFORMATION
                      -----END CERTIFICATE-----
                  label:
                    type: string
                    description: |
                      Certificate name.
                    example: cert-12345
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-load-balancer-certificate
      security:
      - personalAccessToken: []
      - oauth:
        - aclb:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "key": "1111111111",
                "certificate": "-----BEGIN CERTIFICATE-----\nCERTIFICATE_INFORMATION\n-----END CERTIFICATE-----",
                "label": "cert-12345",
                "type": "downstream"
               }' \
              https://api.linode.com/v4beta/aclb/12345/certificates
      - lang: CLI
        source: |-
          linode-cli loadbalancers certificates-create \
            12345 \
            --key 1111111111 \
            --certificate "-----BEGIN CERTIFICATE-----\nCERTIFICATE_INFORMATION\n-----END CERTIFICATE-----" \
            --label cert-12345 \
            --type downstream
      x-linode-cli-action: certificate-create
      x-linode-grant: read_write
      x-original-op-id: createLoadBalancerCertificate
      x-original-op-title: Certificate Create
    get:
      operationId: get-load-balancer-certificates
      summary: List certificates
      tags:
      - Certificates
      description: |-
        __Beta__ View certificates assigned to this Cloud Load Balancer.


        ---


        - __CLI__.

            ```
            linode-cli loadbalancers certificates
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            aclb:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: BETA
        tabs:
        - title: CLI
          syntax: linode-cli loadbalancers certificates
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: aclb:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated list of certificates assigned to this Cloud Load Balancer.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        TLS termination certificates create an encrypted link between your clients and Cloud Load
                        Balancer and terminate incoming traffic on the load balancer.
                        Once the load balancing policy is applied, traffic is forwarded
                        to your service targets over encrypted TLS connections.
                      x-akamai:
                        file-path: schemas/aclb-certificate.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            This certificate's generated ID.
                          example: 10
                        type:
                          type: string
                          description: |
                            Service target certificates, `ca`, are used by the load balancer to accept
                            responses from your service target endpoints. TLS termination
                            certificates, `downstream`, are required for HTTPS Cloud
                            Load Balancers.
                          example: downstream
                          enum:
                          - ca
                          - downstream
                        certificate:
                          type: string
                          description: |-
                            The PEM-formatted contents of your certificate.

                            __Note:__ `certificate` is not returned for BETA.
                          example: |-
                            -----BEGIN CERTIFICATE-----
                            CERTIFICATE_INFORMATION
                            -----END CERTIFICATE-----
                        label:
                          type: string
                          description: |
                            Certificate name.
                          example: cert-12345
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-load-balancer-certificates
      security:
      - personalAccessToken: []
      - oauth:
        - aclb:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl https://api.linode.com/v4beta/aclb/12345/certificates \
            -H "Authorization: Bearer $TOKEN"
      - lang: CLI
        source: linode-cli loadbalancers certificates-list 12345
      x-linode-cli-action: certificates
      x-linode-grant: read_only
      x-original-op-id: getLoadBalancerCertificates
      x-original-op-title: Certificates List
    x-linode-cli-command: loadbalancers
  /aclb/{cloudLoadBalancerId}/certificates/{certificateId}:
    x-akamai:
      file-path: paths/certificate.yaml
      path-info: /aclb/{cloudLoadBalancerId}/certificates/{certificateId}
    parameters:
    - name: cloudLoadBalancerId
      in: path
      description: |
        The ID of the Cloud Load Balancer to access.
      example: '{{cloudLoadBalancerId}}'
      x-akamai:
        file-path: parameters/cloud-load-balancer-id.yaml
      required: true
      schema:
        type: integer
    - name: certificateId
      in: path
      description: |
        The ID of the certificate to access
      example: '{{certificateId}}'
      x-akamai:
        file-path: parameters/certificate-id.yaml
      required: true
      schema:
        type: integer
    get:
      servers:
      - url: https://api.linode.com/v4beta
      operationId: get-load-balancer-certificate
      summary: Get a certificate
      tags:
      - Certificates
      description: |-
        __Beta__ Returns information about a certificate.


        ---


        - __CLI__.

            ```
            linode-cli loadbalancers service-target-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            aclb:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: BETA
        tabs:
        - title: CLI
          syntax: linode-cli loadbalancers service-target-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: aclb:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns information about a single certificate.
          content:
            application/json:
              schema:
                type: object
                description: |
                  TLS termination certificates create an encrypted link between your clients and Cloud Load Balancer
                  and terminate incoming traffic on the load balancer. Once the load
                  balancing policy is applied, traffic is forwarded to your service
                  targets over encrypted TLS connections.
                x-akamai:
                  file-path: schemas/aclb-certificate.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      This certificate's generated ID.
                    example: 10
                  type:
                    type: string
                    description: |
                      Service target certificates, `ca`, are used by the load balancer to accept responses from
                      your service target endpoints. TLS termination certificates,
                      `downstream`, are required for HTTPS Cloud Load Balancers.
                    example: downstream
                    enum:
                    - ca
                    - downstream
                  certificate:
                    type: string
                    description: |-
                      The PEM-formatted contents of your certificate.

                      __Note:__ `certificate` is not returned for BETA.
                    example: |-
                      -----BEGIN CERTIFICATE-----
                      CERTIFICATE_INFORMATION
                      -----END CERTIFICATE-----
                  label:
                    type: string
                    description: |
                      Certificate name.
                    example: cert-12345
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-load-balancer-certificate
      security:
      - personalAccessToken: []
      - oauth:
        - aclb:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4beta/aclb/12345/certificate/10
      - lang: CLI
        source: linode-cli loadbalancers certificate-view 12345 10
      x-linode-cli-action: service-target-view
      x-linode-grant: read_only
      x-original-op-id: getLoadBalancerCertificate
      x-original-op-title: Certificate View
    put:
      servers:
      - url: https://api.linode.com/v4beta
      operationId: put-load-balancer-certificate
      summary: Update a certificate
      tags:
      - Certificates
      description: |-
        __Beta__ Updates a certificate.


        ---


        - __CLI__.

            ```
            linode-cli loadbalancers certificate-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            aclb:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: BETA
        tabs:
        - title: CLI
          syntax: linode-cli loadbalancers certificate-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: aclb:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The fields to update.
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                type:
                  type: string
                  description: |
                    For TLS Certificates use `downstream`. For Service Target Certificates use `ca`.
                  example: '{{type}}'
                  enum:
                  - ca
                  - downstream
                certificate:
                  type: string
                  description: |
                    The PEM-formatted contents of the tls certificate.
                  example: '{{certificate}}'
                key:
                  type: integer
                  description: |-
                    The PEM-formatted contents of your private key. Your private key must not have a passphrase.

                    For `ca` certificates, the key must be blank.

                    For `downstream` certificates, the key must be populated.
                  example: '{{key}}'
                label:
                  type: string
                  description: |
                    The certificate name.
                  example: '{{label}}'
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Certificate updated successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  TLS termination certificates create an encrypted link between your clients and Cloud Load Balancer
                  and terminate incoming traffic on the load balancer. Once the load
                  balancing policy is applied, traffic is forwarded to your service
                  targets over encrypted TLS connections.
                x-akamai:
                  file-path: schemas/aclb-certificate.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      This certificate's generated ID.
                    example: 10
                  type:
                    type: string
                    description: |
                      Service target certificates, `ca`, are used by the load balancer to accept responses from
                      your service target endpoints. TLS termination certificates,
                      `downstream`, are required for HTTPS Cloud Load Balancers.
                    example: downstream
                    enum:
                    - ca
                    - downstream
                  certificate:
                    type: string
                    description: |-
                      The PEM-formatted contents of your certificate.

                      __Note:__ `certificate` is not returned for BETA.
                    example: |-
                      -----BEGIN CERTIFICATE-----
                      CERTIFICATE_INFORMATION
                      -----END CERTIFICATE-----
                  label:
                    type: string
                    description: |
                      Certificate name.
                    example: cert-12345
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-load-balancer-certificate
      security:
      - personalAccessToken: []
      - oauth:
        - aclb:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "key": "1111111111",
                "certificate": "-----BEGIN CERTIFICATE-----\nCERTIFICATE_INFORMATION\n-----END CERTIFICATE-----",
                "label": "cert-12345",
                "type": "downstream"
              }' \
              https://api.linode.com/v4beta/aclb/12345/certificates
      - lang: CLI
        source: |-
          linode-cli loadbalancers certificates-create \
            12345 \
            --key 1111111111 \
            --certificate "-----BEGIN CERTIFICATE-----\nCERTIFICATE_INFORMATION\n-----END CERTIFICATE-----" \
            --label cert-12345 \
            --type downstream
      x-linode-cli-action: certificate-update
      x-linode-grant: read_write
      x-original-op-id: updateLoadBalancerCertificate
      x-original-op-title: Certificate Update
    delete:
      servers:
      - url: https://api.linode.com/v4beta
      operationId: delete-load-balancer-certificate
      summary: Delete a certificate
      tags:
      - Certificates
      description: |-
        __Beta__ Deletes a certificate.

        This does not remove the Cloud Load Balancer, but all associations to the deleted certificate are removed.


        ---


        - __CLI__.

            ```
            linode-cli loadbalancers certificate-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            aclb:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: BETA
        tabs:
        - title: CLI
          syntax: linode-cli loadbalancers certificate-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: aclb:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Certificate deleted successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-load-balancer-certificate
      security:
      - personalAccessToken: []
      - oauth:
        - aclb:read_write
      x-linode-cli-action: certificate-delete
      x-linode-grant: read_write
      x-original-op-id: deleteLoadBalancerCertificate
      x-original-op-title: Certificate Delete
    x-linode-cli-command: loadbalancers
  /aclb/{cloudLoadBalancerId}/configurations:
    x-akamai:
      file-path: paths/configs.yaml
      path-info: /aclb/{cloudLoadBalancerId}/configurations
    parameters:
    - name: cloudLoadBalancerId
      in: path
      description: |
        The ID of the Cloud Load Balancer to access.
      example: '{{cloudLoadBalancerId}}'
      x-akamai:
        file-path: parameters/cloud-load-balancer-id.yaml
      required: true
      schema:
        type: integer
    post:
      servers:
      - url: https://api.linode.com/v4beta
      operationId: post-load-balancer-configuration
      summary: Create a configuration
      tags:
      - Configurations
      description: |-
        __Beta__ Creates a Cloud Load Balancer Configuration which allows the load balancer to accept traffic on a new port.

        For example, if you wanted to accept standard HTTP traffic, you could create a configuration listening on port 80.

        Configurations use `routes` to direct requests to service targets. To add routes to a configuration, you can use existing `route_ids`or create new `routes`. Route rules can not be created or updated using Configuration Create. Run [Create a route](https://techdocs.akamai.com/linode-api/reference/post-load-balancer-route) and [Update a route](https://techdocs.akamai.com/linode-api/reference/put-load-balancer-route) to manage rules.

        The route protocol and the configuration protocol must match, or the operation will be rejected.

        If the protocol is http or tcp, `certificates` must be left blank.

        You will need to add service targets to the new configuration before it can actually serve requests.


        ---


        - __CLI__.

            ```
            linode-cli loadbalancers configuration-create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            aclb:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: BETA
        tabs:
        - title: CLI
          syntax: linode-cli loadbalancers configuration-create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: aclb:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the port to configure.
        required: true
        content:
          application/json:
            schema:
              allOf:
              - type: object
                description: |
                  Akamai Cloud Load Balancer offers global, configurable, scalable, distributed compute traffic
                  management across physical, virtual, and cloud-hosted applications.
                  It can automatically detect load conditions and levels and route
                  traffic to the optimal target while maintaining custom routing policies
                  and consistent visitor session behavior.
                x-akamai:
                  file-path: schemas/aclb-configuration-configuration.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The configuration's assigned id.
                    example: 125
                    readOnly: true
                    x-linode-cli-display: 1
                  certificates:
                    type: array
                    description: |
                      Used by the load balancer to terminate the connection and decrypt request from clients.
                    x-akamai:
                      file-path: schemas/aclb-configuration-certificate.yaml
                    items:
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            This certificate's ID.
                          example: 10
                        hostname:
                          type: string
                          description: |
                            Server Name Indication (SNI) Hostname. The request host header sent by the client.
                            This host header tells the load balancer which certificate
                            to use when responding back to the client.
                          example: www.example.org
                  label:
                    type: string
                    description: |
                      This configuration's label.
                    example: balancer12345
                    x-linode-cli-display: 2
                  port:
                    type: integer
                    description: |
                      The port this Cloud Load Balancer configuration listens on.
                    example: 443
                    x-linode-cli-display: 4
                  protocol:
                    type: string
                    description: |-
                      The protocol this port is configured to serve.

                      - The `http` and `tcp` protocols do not support `certificates`.
                      - The `https` protocol is mutually required with `certificates`.

                      Review our guide on [Available Protocols](https://deploy-preview-14--roaring-gelato-12dc9e.netlify.app/docs/products/networking/global-loadbalancer/guides/protocols/) for information on protocol features.
                    example: https
                    default: https
                    enum:
                    - http
                    - https
                    - tcp
                  routes:
                    type: array
                    description: |
                      Cloud Load Balancer Routes. Routes assigned to a load balancer’s configuration define
                      how incoming requests are directed to service targets.
                    x-akamai:
                      file-path: schemas/aclb-configuration-route.yaml
                    items:
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            This routes unique ID.
                          example: 12345
                        label:
                          type: string
                          description: |
                            The route name.
                          example: route-a-b
              - properties:
                  route_ids:
                    type: integer
                    description: |
                      The ids of existing routes.
                    example: 1
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Configuration created successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Akamai Cloud Load Balancer offers global, configurable, scalable, distributed compute traffic
                  management across physical, virtual, and cloud-hosted applications.
                  It can automatically detect load conditions and levels and route
                  traffic to the optimal target while maintaining custom routing policies
                  and consistent visitor session behavior.
                x-akamai:
                  file-path: schemas/aclb-configuration-configuration.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The configuration's assigned id.
                    example: 125
                    readOnly: true
                    x-linode-cli-display: 1
                  certificates:
                    type: array
                    description: |
                      Used by the load balancer to terminate the connection and decrypt request from clients.
                    x-akamai:
                      file-path: schemas/aclb-configuration-certificate.yaml
                    items:
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            This certificate's ID.
                          example: 10
                        hostname:
                          type: string
                          description: |
                            Server Name Indication (SNI) Hostname. The request host header sent by the client.
                            This host header tells the load balancer which certificate
                            to use when responding back to the client.
                          example: www.example.org
                  label:
                    type: string
                    description: |
                      This configuration's label.
                    example: balancer12345
                    x-linode-cli-display: 2
                  port:
                    type: integer
                    description: |
                      The port this Cloud Load Balancer configuration listens on.
                    example: 443
                    x-linode-cli-display: 4
                  protocol:
                    type: string
                    description: |-
                      The protocol this port is configured to serve.

                      - The `http` and `tcp` protocols do not support `certificates`.
                      - The `https` protocol is mutually required with `certificates`.

                      Review our guide on [Available Protocols](https://deploy-preview-14--roaring-gelato-12dc9e.netlify.app/docs/products/networking/global-loadbalancer/guides/protocols/) for information on protocol features.
                    example: https
                    default: https
                    enum:
                    - http
                    - https
                    - tcp
                  routes:
                    type: array
                    description: |
                      Cloud Load Balancer Routes. Routes assigned to a load balancer’s configuration define
                      how incoming requests are directed to service targets.
                    x-akamai:
                      file-path: schemas/aclb-configuration-route.yaml
                    items:
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            This routes unique ID.
                          example: 12345
                        label:
                          type: string
                          description: |
                            The route name.
                          example: route-a-b
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-load-balancer-configuration
      security:
      - personalAccessToken: []
      - oauth:
        - aclb:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "label": aclb-configuration,
                "port": 443,
                "protocol": "https",
                "certificates": [
                      {
                        "hostname": "example.net",
                        "id": 456789
                      }
                ],
                "route_ids": [
                  1,
                  2
                ],
                "routes": [
                      {
                        "label": "route-d-e",
                        "protocol": "https"
                      }
                ]
              }' \
              https://api.linode.com/v4beta/aclb/12345/configurations
      - lang: CLI
        source: |-
          linode-cli nodebalancers configuration-create 12345 \
            --label aclb-configuration \
            --port 443 \
            --protocol https \
            certificates \
            --hostname example.net \
            --id 456789 \
            --route_ids 150 \
            routes \
            --label glb-route-1 \
            --protocol http
      x-linode-cli-action: configuration-create
      x-linode-grant: read_write
      x-original-op-id: createLoadBalancerConfiguration
      x-original-op-title: Configuration Create
    get:
      servers:
      - url: https://api.linode.com/v4beta
      operationId: get-load-balancer-configurations
      summary: List configurations
      tags:
      - Configurations
      description: |-
        __Beta__ List of configurations for a specific Cloud Load Balancer. Cloud Load Balancer configurations represent individual ports that this load balancer accepts traffic on. Each configuration accepts traffic on one port.


        ---


        - __CLI__.

            ```
            linode-cli loadbalancers configurations-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            aclb:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: BETA
        tabs:
        - title: CLI
          syntax: linode-cli loadbalancers configurations-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: aclb:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated lists of configurations for the Cloud Load Balancer.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        Akamai Cloud Load Balancer offers global, configurable, scalable, distributed
                        compute traffic management across physical, virtual, and cloud-hosted
                        applications. It can automatically detect load conditions
                        and levels and route traffic to the optimal target while maintaining
                        custom routing policies and consistent visitor session behavior.
                      x-akamai:
                        file-path: schemas/aclb-configuration-configuration.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ The configuration's assigned id.
                          example: 125
                          readOnly: true
                          x-linode-cli-display: 1
                        certificates:
                          type: array
                          description: |
                            Used by the load balancer to terminate the connection and decrypt request
                            from clients.
                          x-akamai:
                            file-path: schemas/aclb-configuration-certificate.yaml
                          items:
                            additionalProperties: false
                            properties:
                              id:
                                type: integer
                                description: |
                                  This certificate's ID.
                                example: 10
                              hostname:
                                type: string
                                description: |
                                  Server Name Indication (SNI) Hostname. The request host
                                  header sent by the client. This host header tells
                                  the load balancer which certificate to use when
                                  responding back to the client.
                                example: www.example.org
                        label:
                          type: string
                          description: |
                            This configuration's label.
                          example: balancer12345
                          x-linode-cli-display: 2
                        port:
                          type: integer
                          description: |
                            The port this Cloud Load Balancer configuration listens on.
                          example: 443
                          x-linode-cli-display: 4
                        protocol:
                          type: string
                          description: |-
                            The protocol this port is configured to serve.

                            - The `http` and `tcp` protocols do not support `certificates`.
                            - The `https` protocol is mutually required with `certificates`.

                            Review our guide on [Available Protocols](https://deploy-preview-14--roaring-gelato-12dc9e.netlify.app/docs/products/networking/global-loadbalancer/guides/protocols/) for information on protocol features.
                          example: https
                          default: https
                          enum:
                          - http
                          - https
                          - tcp
                        routes:
                          type: array
                          description: |
                            Cloud Load Balancer Routes. Routes assigned to a load balancer’s configuration
                            define how incoming requests are directed to service targets.
                          x-akamai:
                            file-path: schemas/aclb-configuration-route.yaml
                          items:
                            additionalProperties: false
                            properties:
                              id:
                                type: integer
                                description: |
                                  This routes unique ID.
                                example: 12345
                              label:
                                type: string
                                description: |
                                  The route name.
                                example: route-a-b
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-load-balancer-configurations
      security:
      - personalAccessToken: []
      - oauth:
        - aclb:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4beta/aclb/12345/configurations
      - lang: CLI
        source: linode-cli nodebalancers configs-list 12345
      x-linode-cli-action: configurations-list
      x-linode-grant: read_only
      x-original-op-id: getLoadBalancerConfigurations
      x-original-op-title: Configurations List
    x-linode-cli-command: loadbalancers
  /aclb/{cloudLoadBalancerId}/configurations/{configurationId}:
    x-akamai:
      file-path: paths/config.yaml
      path-info: /aclb/{cloudLoadBalancerId}/configurations/{configurationId}
    parameters:
    - name: cloudLoadBalancerId
      in: path
      description: |
        The ID of the Cloud Load Balancer to access.
      example: '{{cloudLoadBalancerId}}'
      x-akamai:
        file-path: parameters/cloud-load-balancer-id.yaml
      required: true
      schema:
        type: integer
    - name: configurationId
      in: path
      description: |
        The ID of the configuration to access.
      example: '{{configurationId}}'
      x-akamai:
        file-path: parameters/configuration-id-path.yaml
      required: true
      schema:
        type: integer
    get:
      servers:
      - url: https://api.linode.com/v4beta
      operationId: get-load-balancer-configuration
      summary: Get a configuration
      tags:
      - Configurations
      description: |-
        __Beta__ Returns configuration information for a single port of this Cloud Load Balancer.


        ---


        - __CLI__.

            ```
            linode-cli loadbalancers configurations-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            aclb:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: BETA
        tabs:
        - title: CLI
          syntax: linode-cli loadbalancers configurations-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: aclb:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The requested Cloud Load Balancer configuration.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Akamai Cloud Load Balancer offers global, configurable, scalable, distributed compute traffic
                  management across physical, virtual, and cloud-hosted applications.
                  It can automatically detect load conditions and levels and route
                  traffic to the optimal target while maintaining custom routing policies
                  and consistent visitor session behavior.
                x-akamai:
                  file-path: schemas/aclb-configuration-configuration.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The configuration's assigned id.
                    example: 125
                    readOnly: true
                    x-linode-cli-display: 1
                  certificates:
                    type: array
                    description: |
                      Used by the load balancer to terminate the connection and decrypt request from clients.
                    x-akamai:
                      file-path: schemas/aclb-configuration-certificate.yaml
                    items:
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            This certificate's ID.
                          example: 10
                        hostname:
                          type: string
                          description: |
                            Server Name Indication (SNI) Hostname. The request host header sent by the client.
                            This host header tells the load balancer which certificate
                            to use when responding back to the client.
                          example: www.example.org
                  label:
                    type: string
                    description: |
                      This configuration's label.
                    example: balancer12345
                    x-linode-cli-display: 2
                  port:
                    type: integer
                    description: |
                      The port this Cloud Load Balancer configuration listens on.
                    example: 443
                    x-linode-cli-display: 4
                  protocol:
                    type: string
                    description: |-
                      The protocol this port is configured to serve.

                      - The `http` and `tcp` protocols do not support `certificates`.
                      - The `https` protocol is mutually required with `certificates`.

                      Review our guide on [Available Protocols](https://deploy-preview-14--roaring-gelato-12dc9e.netlify.app/docs/products/networking/global-loadbalancer/guides/protocols/) for information on protocol features.
                    example: https
                    default: https
                    enum:
                    - http
                    - https
                    - tcp
                  routes:
                    type: array
                    description: |
                      Cloud Load Balancer Routes. Routes assigned to a load balancer’s configuration define
                      how incoming requests are directed to service targets.
                    x-akamai:
                      file-path: schemas/aclb-configuration-route.yaml
                    items:
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            This routes unique ID.
                          example: 12345
                        label:
                          type: string
                          description: |
                            The route name.
                          example: route-a-b
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-load-balancer-configuration
      security:
      - personalAccessToken: []
      - oauth:
        - aclb:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4beta/aclb/12345/configurations/4567
      - lang: CLI
        source: |-
          linode-cli loadbalancers configurations-view \
            12345 4567
      x-linode-cli-action: configurations-view
      x-linode-grant: read_only
      x-original-op-id: getLoadBalancerConfiguration
      x-original-op-title: Configuration View
    put:
      operationId: put-load-balancer-configuration
      summary: Update a configuration
      tags:
      - Configurations
      description: |-
        __Beta__ Updates the configuration for a single port on a Cloud Load Balancer.

        You can update the protocol only if the configuration doesn't have any assigned routes. The protocols for routes assigned to a configuration must match the configuration protocol.

        Certificates are not applicable when the protocol is http/tcp.


        ---


        - __CLI__.

            ```
            linode-cli loadbalancers configuration-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            aclb:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: BETA
        tabs:
        - title: CLI
          syntax: linode-cli loadbalancers configuration-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: aclb:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The Configuration fields to update. You can update the protocol only if the configuration doesn't have any assigned
          routes.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - LoadBalancerId
              - RouteId
              properties:
                certificates:
                  type: array
                  description: |
                    Used by the load balancer to terminate the connection and decrypt request from clients.
                  x-akamai:
                    file-path: schemas/aclb-configuration-certificate.yaml
                  items:
                    additionalProperties: false
                    properties:
                      id:
                        type: integer
                        description: |
                          This certificate's ID.
                        example: 10
                      hostname:
                        type: string
                        description: |
                          Server Name Indication (SNI) Hostname. The request host header sent by the client.
                          This host header tells the load balancer which certificate
                          to use when responding back to the client.
                        example: www.example.org
                label:
                  type: string
                  description: |
                    This configuration's label.
                  example: '{{label}}'
                  x-linode-cli-display: 2
                port:
                  type: integer
                  description: |
                    The port this Cloud Load Balancer configuration listens on.
                  example: '{{port}}'
                  x-linode-cli-display: 4
                protocol:
                  type: string
                  description: |-
                    The protocol this port is configured to serve.

                    - The `http` and `tcp` protocols do not support `certificates`.
                    - The `https` protocol is mutually required with `certificates`.

                    Review our guide on [Available Protocols](https://deploy-preview-14--roaring-gelato-12dc9e.netlify.app/docs/products/networking/global-loadbalancer/guides/protocols/) for information on protocol features.
                  example: '{{protocol}}'
                  default: https
                  enum:
                  - http
                  - https
                  - tcp
                route_ids:
                  type: integer
                  description: |
                    The route ids assigned to this configuration.
                  example: '{{route_ids}}'
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Configuration updated successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Akamai Cloud Load Balancer offers global, configurable, scalable, distributed compute traffic
                  management across physical, virtual, and cloud-hosted applications.
                  It can automatically detect load conditions and levels and route
                  traffic to the optimal target while maintaining custom routing policies
                  and consistent visitor session behavior.
                x-akamai:
                  file-path: schemas/aclb-configuration-configuration.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The configuration's assigned id.
                    example: 125
                    readOnly: true
                    x-linode-cli-display: 1
                  certificates:
                    type: array
                    description: |
                      Used by the load balancer to terminate the connection and decrypt request from clients.
                    x-akamai:
                      file-path: schemas/aclb-configuration-certificate.yaml
                    items:
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            This certificate's ID.
                          example: 10
                        hostname:
                          type: string
                          description: |
                            Server Name Indication (SNI) Hostname. The request host header sent by the client.
                            This host header tells the load balancer which certificate
                            to use when responding back to the client.
                          example: www.example.org
                  label:
                    type: string
                    description: |
                      This configuration's label.
                    example: balancer12345
                    x-linode-cli-display: 2
                  port:
                    type: integer
                    description: |
                      The port this Cloud Load Balancer configuration listens on.
                    example: 443
                    x-linode-cli-display: 4
                  protocol:
                    type: string
                    description: |-
                      The protocol this port is configured to serve.

                      - The `http` and `tcp` protocols do not support `certificates`.
                      - The `https` protocol is mutually required with `certificates`.

                      Review our guide on [Available Protocols](https://deploy-preview-14--roaring-gelato-12dc9e.netlify.app/docs/products/networking/global-loadbalancer/guides/protocols/) for information on protocol features.
                    example: https
                    default: https
                    enum:
                    - http
                    - https
                    - tcp
                  routes:
                    type: array
                    description: |
                      Cloud Load Balancer Routes. Routes assigned to a load balancer’s configuration define
                      how incoming requests are directed to service targets.
                    x-akamai:
                      file-path: schemas/aclb-configuration-route.yaml
                    items:
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            This routes unique ID.
                          example: 12345
                        label:
                          type: string
                          description: |
                            The route name.
                          example: route-a-b
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-load-balancer-configuration
      security:
      - personalAccessToken: []
      - oauth:
        - aclb:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "label": "config_name",
                "port": 443,
                "protocol": "https",
                "certificates": [
                  {
                    "hostname": "example.net",
                    "id": 456789,
                  }
                ],
                "route_ids": [
                  "1",
                  "2",
                  "3"
                ]
              }' \
              https://api.linode.com/v4beta/aclb/12345/configurations/4567
      - lang: CLI
        source: |-
          linode-cli loadbalancers configurations-update \
            12345 4567 \
            --port 443 \
            --protocol https \
            certificates \
            --hostname example.net \
            --id 456789 \
            --route_ids 150
      x-linode-cli-action: configuration-update
      x-linode-grant: read_write
      x-original-op-id: updateLoadBalancerConfiguration
      x-original-op-title: Configuration Update
    delete:
      operationId: delete-load-balancer-configuration
      summary: Delete a configuration
      tags:
      - Configurations
      description: |-
        __Beta__ Deletes the Configuration for a port of this Cloud Load Balancer.

        All route associations with this Configuration are removed. The routes are not deleted.

        __This cannot be undone.__

        Once completed, this load balancer will no longer respond to requests on the given port.


        ---


        - __CLI__.

            ```
            linode-cli loadbalancers configuration-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            aclb:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: BETA
        tabs:
        - title: CLI
          syntax: linode-cli loadbalancers configuration-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: aclb:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Configuration deleted successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-load-balancer-configuration
      security:
      - personalAccessToken: []
      - oauth:
        - aclb:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4beta/aclb/12345/configurations/4567
      - lang: CLI
        source: |-
          linode-cli loadbalancers configuration-delete \
            12345 4567
      x-linode-cli-action: configuration-delete
      x-linode-grant: read_write
      x-original-op-id: deleteLoadBalancerConfiguration
      x-original-op-title: Configuration Delete
    x-linode-cli-command: loadbalancers
  /aclb/{cloudLoadBalancerId}/routes:
    x-akamai:
      file-path: paths/routes.yaml
      path-info: /aclb/{cloudLoadBalancerId}/routes
    parameters:
    - name: cloudLoadBalancerId
      in: path
      description: |
        The ID of the Cloud Load Balancer to access.
      example: '{{cloudLoadBalancerId}}'
      x-akamai:
        file-path: parameters/cloud-load-balancer-id.yaml
      required: true
      schema:
        type: integer
    post:
      servers:
      - url: https://api.linode.com/v4beta
      operationId: post-load-balancer-route
      summary: Create a route
      tags:
      - Routes
      description: |-
        __Beta__ Creates a route. Routes contain rules that define how the load balancer directs incoming requests to service targets. A routing rule may have multiple service-targets. You can re-use routes on multiple load balancer configurations. If the `service_target` already exists, and you want to use it, use the `service_target` `id` and `percentage` instead of full `service_target` configuration.


        ---


        - __CLI__.

            ```
            linode-cli loadbalancers route-create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            aclb:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: BETA
        tabs:
        - title: CLI
          syntax: linode-cli loadbalancers route-create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: aclb:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the route to create.
        required: true
        content:
          application/json:
            schema:
              additionalProperties: false
              required:
              - label
              - protocol
              properties:
                label:
                  type: string
                  description: |
                    The route name.
                  example: route-a-b
                protocol:
                  type: string
                  description: |
                    The protocol to use for this route. The port protocol, route protocol, and service target
                    protocol for a load balancer configuration must all be the same.
                  example: http
                  default: http
                  enum:
                  - http
                  - tcp
                rules:
                  type: array
                  items:
                    additionalProperties: false
                    properties:
                      match_condition:
                        type: object
                        description: |
                          Match conditions are for HTTP and HTTPS load balancers only, in addition
                          to setting the percentage of incoming requests to a target,
                          match conditions such as `path_prefix`, `path_regex`, `query`,
                          `header`, and `method` can be added to the route rules.
                          A rule consists of a `match_field`, and a pattern to match
                          on called a `match_value`. Each rule can specify only one
                          field or pattern pair. If match match conditions are added
                          for a TCP load balancer, the conditions are ignored.
                        x-akamai:
                          file-path: schemas/aclb-match-condition.yaml
                        additionalProperties: false
                        properties:
                          hostname:
                            type: string
                            description: |
                              Additional hostname match that is used in the routing decision.
                              Wildcards (*) are supported with the Path Regex Match
                              Type.
                            example: www.example.com
                          match_field:
                            type: string
                            example: path_prefix
                            enum:
                            - path_prefix
                            - path_regex
                            - query
                            - header
                            - method
                          match_value:
                            type: string
                            description: |-
                              The `match_value` format:

                                - `path_prefix`: The format of the path rule is: /path1/path2. The initial slash is required, but the trailing slash is not.
                                - `path_regex`: The format for path regex is a regular expression in RE2 syntax. An example for any jpg file in the /path/ directory would be /path/.*[.]jpg. The initial slash is required.
                                - `query`: The format for query string is: ?name=value. The query string name must be preceded by a question mark (?).
                                - `header`: The format for http header is: x-nameheader:value.
                                - `method`: The request methods include: DELETE, GET, HEAD, POST, and PUT.
                            example: /my-path
                          session_stickiness_cookie:
                            type: string
                            description: |
                              The cookie name used to obtain cookie value from downstream HTTP
                              request or the cookie name used by the load balancer
                              to generate new cookie for downstream. If you want the
                              load balancer to generate a cookie, enter a value for
                              `session_stickiness_ttl`.
                            example: cookie-name
                          session_stickiness_ttl:
                            type: integer
                            description: |
                              Expiry time in seconds for the HTTP/S load balancer generated cookie.
                              If the TTL is set to zero, the generated cookie is a
                              session cookie only. If a `session_stickiness_ttl` is
                              absent, the origin's cookie is used.
                      service_targets:
                        type: array
                        description: |
                          The service targets for this configuration. Service targets consist of a group
                          of endpoints. Requests are directed to service targets using
                          configured routes. The sum of traffic across all service
                          targets must total 100%.
                        x-akamai:
                          file-path: schemas/aclb-rule-service-target.yaml
                        items:
                          additionalProperties: false
                          properties:
                            certificate_id:
                              type: integer
                              description: |
                                Service Target CA Certificate identification number. The certificate
                                installed on your endpoints that are used by the load
                                balancer to accept responses from this Service Target.
                            healthcheck:
                              type: object
                              description: |
                                Health checks query the service targets by performing TCP connections
                                or by making HTTP requests. For TCP, a service target
                                is considered healthy and able to accept incoming
                                requests when there is a successful TCP handshake
                                with the service target. When HTTP/S is used to validate
                                health status, the service target is considered healthy
                                when requests to its path or host return a 2xx or
                                3xx status code response.
                              x-akamai:
                                file-path: schemas/aclb-health-check.yaml
                              additionalProperties: false
                              properties:
                                healthy_threshold:
                                  type: integer
                                  description: |
                                    The number of consecutive health checks that must be successful
                                    before marking a service target as healthy.
                                  example: 2
                                  default: 3
                                  minimum: 1
                                host:
                                  type: string
                                  description: |
                                    When the `healthcheck` protocol is set to `http`, this
                                    is the optional host header sent in the health
                                    check request. When the health check `protocol`
                                    is set to TCP, `host` is not applicable.
                                  example: example.com
                                interval:
                                  type: integer
                                  description: |
                                    The number of seconds between health checks for this service
                                    target.
                                  example: 5
                                  default: 10
                                  minimum: 1
                                path:
                                  type: string
                                  description: |
                                    When `protocol` is set to HTTP/S, enter an optional
                                    request url path for the health check. When the
                                    health check `protocol` is set to TCP, `path`
                                    is not applicable.
                                  example: /path/to/endpoint
                                protocol:
                                  type: string
                                  description: |
                                    For HTTP service targets, the health check protocol can
                                    be HTTP or TCP. For TCP service targets, the health
                                    check protocol must be TCP. When set to TCP, `path`
                                    and `host` are not applicable.
                                  example: http
                                  default: http
                                  enum:
                                  - tcp
                                  - http
                                timeout:
                                  type: integer
                                  description: |
                                    How long to wait (in seconds) before canceling a health
                                    check because a connection could not be established
                                    with the service target.
                                  example: 10
                                  default: 5
                                  minimum: 1
                                unhealthy_threshold:
                                  type: integer
                                  description: |
                                    The number of consecutive health checks that must fail before
                                    marking a service target as unhealthy.
                                  example: 3
                                  default: 3
                                  minimum: 1
                            label:
                              type: string
                              description: |
                                Service target's unique label.
                            load_balancing_policy:
                              type: string
                              description: |
                                The algorithm this Cloud Load Balancer uses for routing new requests
                                to service targets. Cloud Load Balancer also has a
                                geo-proximity feature for selecting the optimum endpoint.
                              example: roundrobin
                              default: roundrobin
                              enum:
                              - round_robin
                              - least_request
                              - ring_hash
                              - random
                              - maglev
                            percentage:
                              type: integer
                              description: |
                                The percentage of incoming requests that should be directed to each
                                target.
                              example: 50
                              minimum: 0
                              maximum: 100
                            protocol:
                              type: string
                              description: |
                                The protocol this target is configured to serve. The port protocol,
                                route protocol, and service target protocol for a
                                load balancer configuration must all be the same.
                              example: http
                              default: http
                              enum:
                              - http
                              - https
                              - tcp
                      service_targets`:
                        type: object
                        description: |
                          If the `service_target` already exists, and you want to use it, use the `service_target`
                          `id` and `percentage` instead of full `service_target` configuration.
                        additionalProperties: false
                        properties:
                          id:
                            type: integer
                            description: |
                              Existing Service Target id.
                            example: 200
                          percentage:
                            type: integer
                            description: |
                              The percentage of incoming requests routed to this service target.
                            example: 75
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Route created successfully.
          content:
            application/json:
              schema:
                type: array
                description: |
                  Routes define how the load balancer directs incoming requests to service targets. After a route
                  is created, you add rules to set match conditions that are used
                  for target selection.
                x-akamai:
                  file-path: schemas/aclb-routes.yaml
                items:
                  additionalProperties: false
                  properties:
                    id:
                      type: integer
                      description: |
                        __Read-only__ This routes unique ID.
                      example: 12345
                      readOnly: true
                    label:
                      type: string
                      description: |
                        The route name.
                      example: route-a-b
                    protocol:
                      type: string
                      description: |
                        The route protocol. The port protocol, route protocol, and service target protocol
                        for a load balancer configuration must all be the same.
                      example: http
                      default: http
                      enum:
                      - http
                      - tcp
                    rules:
                      type: array
                      description: |-
                        Rules set the conditions that are used for target selection.

                         - HTTP/S rules include match conditions, session stickiness, and the percentage of incoming requests that should be directed to each target.

                         - TCP rules include the percentage of incoming requests that should be directed to each target.
                      items:
                        additionalProperties: false
                        properties:
                          match_condition:
                            type: object
                            description: |
                              Match conditions are for HTTP and HTTPS load balancers only, in addition
                              to setting the percentage of incoming requests to a
                              target, match conditions such as `path_prefix`, `path_regex`,
                              `query`, `header`, and `method` can be added to the
                              route rules. A rule consists of a `match_field`, and
                              a pattern to match on called a `match_value`. Each rule
                              can specify only one field or pattern pair. If match
                              match conditions are added for a TCP load balancer,
                              the conditions are ignored.
                            x-akamai:
                              file-path: schemas/aclb-match-condition.yaml
                            additionalProperties: false
                            properties:
                              hostname:
                                type: string
                                description: |
                                  Additional hostname match that is used in the routing decision.
                                  Wildcards (*) are supported with the Path Regex
                                  Match Type.
                                example: www.example.com
                              match_field:
                                type: string
                                example: path_prefix
                                enum:
                                - path_prefix
                                - path_regex
                                - query
                                - header
                                - method
                              match_value:
                                type: string
                                description: |-
                                  The `match_value` format:

                                    - `path_prefix`: The format of the path rule is: /path1/path2. The initial slash is required, but the trailing slash is not.
                                    - `path_regex`: The format for path regex is a regular expression in RE2 syntax. An example for any jpg file in the /path/ directory would be /path/.*[.]jpg. The initial slash is required.
                                    - `query`: The format for query string is: ?name=value. The query string name must be preceded by a question mark (?).
                                    - `header`: The format for http header is: x-nameheader:value.
                                    - `method`: The request methods include: DELETE, GET, HEAD, POST, and PUT.
                                example: /my-path
                              session_stickiness_cookie:
                                type: string
                                description: |
                                  The cookie name used to obtain cookie value from downstream
                                  HTTP request or the cookie name used by the load
                                  balancer to generate new cookie for downstream.
                                  If you want the load balancer to generate a cookie,
                                  enter a value for `session_stickiness_ttl`.
                                example: cookie-name
                              session_stickiness_ttl:
                                type: integer
                                description: |
                                  Expiry time in seconds for the HTTP/S load balancer generated
                                  cookie. If the TTL is set to zero, the generated
                                  cookie is a session cookie only. If a `session_stickiness_ttl`
                                  is absent, the origin's cookie is used.
                          service_targets:
                            type: array
                            description: |
                              A service target is a cluster of endpoints that the load balancer directs
                              incoming requests to.
                            items:
                              additionalProperties: false
                              properties:
                                id:
                                  type: integer
                                  description: |
                                    Assigned Service Target id.
                                  example: 12
                                label:
                                  type: string
                                  description: |
                                    Service Target label.
                                  example: service-target-abc
                                percentage:
                                  type: integer
                                  description: |
                                    The percentage of incoming requests routed to this service target.
                                  example: 75
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-load-balancer-route
      security:
      - personalAccessToken: []
      - oauth:
        - aclb:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "label": "glb-route-1",
                "protocol": "http",
                "rules": [
                {
                  "match_condition":
                    {
                      "hostname": "example.com",
                      "match_field": "query",
                      "match_value": "?svc=images",
                      "session_stickiness_cookie": "cookie-name",
                      "session_stickiness_ttl":
                  },
                  "service_targets":[
                    {
                    "percentage": 50,
                    "label": "service-target-1",
                    "certificate_id": 456789,
                    "protocol": "http",
                    "load_balancing_policy": "round_robin",
                    "healthcheck": [
                      {
                        "protocol": "http",
                        "interval": 20,
                        "timeout": 15,
                        "unhealthy_threshold": 2,
                        "healthy_threshold": 4,
                        "path": "/path1",
                        "host": "example.com"
                    },
                    {
                    "id": "12",
                    "percentage": 50
                    }
                  ]
                }]
                }]
                }' \
              https://api.linode.com/v4beta/aclb/12345/route
      - lang: CLI
        source: |-
          linode-cli loadbalancers route-create \
            12345 \
            --label glb-route-1 \
            --protocol http \
            rules [{hostname example.com, match_field query, match_value ?svc=images, session_stickiness_cookie cookie-name, session_stickiness_ttl }] \
            rules
            service_targets [{percentage 50, label service-target-1, certificate_id 456789, protocol http, load_balancing_policy round_robin}] \
            rules
            healthcheck \
            --interval 20 \
            --timeout 15 \
            --unhealthy_threshold 2 \
            --healthy_threshold 4 \
            --path /path1 \
            --host example.com \
            [{--id  1, percentage 50}]
      x-linode-cli-action: route-create
      x-linode-grant: read_write
      x-original-op-id: createLoadBalancerRoute
      x-original-op-title: Route Create
    get:
      servers:
      - url: https://api.linode.com/v4beta
      operationId: get-load-balancer-routes
      summary: List routes
      tags:
      - Routes
      description: |-
        __Beta__ Returns a paginated list of Cloud Load Balancer routes. Routes define how the load balancer directs incoming requests to service targets.


        ---


        - __CLI__.

            ```
            linode-cli loadbalancers routes-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            aclb:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: BETA
        tabs:
        - title: CLI
          syntax: linode-cli loadbalancers routes-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: aclb:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of Cloud Load Balancer routes.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    description: |
                      Routes define how the load balancer directs incoming requests to service targets.
                      After a route is created, you add rules to set match conditions
                      that are used for target selection.
                    x-akamai:
                      file-path: schemas/aclb-routes.yaml
                    items:
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ This routes unique ID.
                          example: 12345
                          readOnly: true
                        label:
                          type: string
                          description: |
                            The route name.
                          example: route-a-b
                        protocol:
                          type: string
                          description: |
                            The route protocol. The port protocol, route protocol, and service target
                            protocol for a load balancer configuration must all be
                            the same.
                          example: http
                          default: http
                          enum:
                          - http
                          - tcp
                        rules:
                          type: array
                          description: |-
                            Rules set the conditions that are used for target selection.

                             - HTTP/S rules include match conditions, session stickiness, and the percentage of incoming requests that should be directed to each target.

                             - TCP rules include the percentage of incoming requests that should be directed to each target.
                          items:
                            additionalProperties: false
                            properties:
                              match_condition:
                                type: object
                                description: |
                                  Match conditions are for HTTP and HTTPS load balancers only, in addition
                                  to setting the percentage of incoming requests to
                                  a target, match conditions such as `path_prefix`,
                                  `path_regex`, `query`, `header`, and `method` can
                                  be added to the route rules. A rule consists of
                                  a `match_field`, and a pattern to match on called
                                  a `match_value`. Each rule can specify only one
                                  field or pattern pair. If match match conditions
                                  are added for a TCP load balancer, the conditions
                                  are ignored.
                                x-akamai:
                                  file-path: schemas/aclb-match-condition.yaml
                                additionalProperties: false
                                properties:
                                  hostname:
                                    type: string
                                    description: |
                                      Additional hostname match that is used in the routing decision.
                                      Wildcards (*) are supported with the Path Regex
                                      Match Type.
                                    example: www.example.com
                                  match_field:
                                    type: string
                                    example: path_prefix
                                    enum:
                                    - path_prefix
                                    - path_regex
                                    - query
                                    - header
                                    - method
                                  match_value:
                                    type: string
                                    description: |-
                                      The `match_value` format:

                                        - `path_prefix`: The format of the path rule is: /path1/path2. The initial slash is required, but the trailing slash is not.
                                        - `path_regex`: The format for path regex is a regular expression in RE2 syntax. An example for any jpg file in the /path/ directory would be /path/.*[.]jpg. The initial slash is required.
                                        - `query`: The format for query string is: ?name=value. The query string name must be preceded by a question mark (?).
                                        - `header`: The format for http header is: x-nameheader:value.
                                        - `method`: The request methods include: DELETE, GET, HEAD, POST, and PUT.
                                    example: /my-path
                                  session_stickiness_cookie:
                                    type: string
                                    description: |
                                      The cookie name used to obtain cookie value from
                                      downstream HTTP request or the cookie name used
                                      by the load balancer to generate new cookie
                                      for downstream. If you want the load balancer
                                      to generate a cookie, enter a value for `session_stickiness_ttl`.
                                    example: cookie-name
                                  session_stickiness_ttl:
                                    type: integer
                                    description: |
                                      Expiry time in seconds for the HTTP/S load balancer
                                      generated cookie. If the TTL is set to zero,
                                      the generated cookie is a session cookie only.
                                      If a `session_stickiness_ttl` is absent, the
                                      origin's cookie is used.
                              service_targets:
                                type: array
                                description: |
                                  A service target is a cluster of endpoints that the load balancer
                                  directs incoming requests to.
                                items:
                                  additionalProperties: false
                                  properties:
                                    id:
                                      type: integer
                                      description: |
                                        Assigned Service Target id.
                                      example: 12
                                    label:
                                      type: string
                                      description: |
                                        Service Target label.
                                      example: service-target-abc
                                    percentage:
                                      type: integer
                                      description: |
                                        The percentage of incoming requests routed to
                                        this service target.
                                      example: 75
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-load-balancer-routes
      security:
      - personalAccessToken: []
      - oauth:
        - aclb:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4beta/aclb/12345/routes
      - lang: CLI
        source: linode-cli loadbalancers routes-list 12345
      x-linode-cli-action: routes-list
      x-linode-grant: read_only
      x-original-op-id: getLoadBalancerRoutes
      x-original-op-title: Routes List
    x-linode-cli-command: loadbalancers
  /aclb/{cloudLoadBalancerId}/routes/{routeId}:
    x-akamai:
      file-path: paths/routes-route.yaml
      path-info: /aclb/{cloudLoadBalancerId}/routes/{routeId}
    parameters:
    - name: cloudLoadBalancerId
      in: path
      description: |
        The ID of the Cloud Load Balancer to access.
      example: '{{cloudLoadBalancerId}}'
      x-akamai:
        file-path: parameters/cloud-load-balancer-id.yaml
      required: true
      schema:
        type: integer
    - name: routeId
      in: path
      description: |
        The ID of the Route to access
      example: '{{routeId}}'
      x-akamai:
        file-path: parameters/route-id-path.yaml
      required: true
      schema:
        type: integer
    get:
      servers:
      - url: https://api.linode.com/v4beta
      operationId: get-load-balancer-route
      summary: Get a route
      tags:
      - Routes
      description: |-
        __Beta__ Returns information about a single route, its rules and service targets.


        ---


        - __CLI__.

            ```
            linode-cli loadbalancers route-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            aclb:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: BETA
        tabs:
        - title: CLI
          syntax: linode-cli loadbalancers route-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: aclb:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Route created successfully.
          content:
            application/json:
              schema:
                type: array
                description: |
                  Routes define how the load balancer directs incoming requests to service targets. After a route
                  is created, you add rules to set match conditions that are used
                  for target selection.
                x-akamai:
                  file-path: schemas/aclb-routes.yaml
                items:
                  additionalProperties: false
                  properties:
                    id:
                      type: integer
                      description: |
                        __Read-only__ This routes unique ID.
                      example: 12345
                      readOnly: true
                    label:
                      type: string
                      description: |
                        The route name.
                      example: route-a-b
                    protocol:
                      type: string
                      description: |
                        The route protocol. The port protocol, route protocol, and service target protocol
                        for a load balancer configuration must all be the same.
                      example: http
                      default: http
                      enum:
                      - http
                      - tcp
                    rules:
                      type: array
                      description: |-
                        Rules set the conditions that are used for target selection.

                         - HTTP/S rules include match conditions, session stickiness, and the percentage of incoming requests that should be directed to each target.

                         - TCP rules include the percentage of incoming requests that should be directed to each target.
                      items:
                        additionalProperties: false
                        properties:
                          match_condition:
                            type: object
                            description: |
                              Match conditions are for HTTP and HTTPS load balancers only, in addition
                              to setting the percentage of incoming requests to a
                              target, match conditions such as `path_prefix`, `path_regex`,
                              `query`, `header`, and `method` can be added to the
                              route rules. A rule consists of a `match_field`, and
                              a pattern to match on called a `match_value`. Each rule
                              can specify only one field or pattern pair. If match
                              match conditions are added for a TCP load balancer,
                              the conditions are ignored.
                            x-akamai:
                              file-path: schemas/aclb-match-condition.yaml
                            additionalProperties: false
                            properties:
                              hostname:
                                type: string
                                description: |
                                  Additional hostname match that is used in the routing decision.
                                  Wildcards (*) are supported with the Path Regex
                                  Match Type.
                                example: www.example.com
                              match_field:
                                type: string
                                example: path_prefix
                                enum:
                                - path_prefix
                                - path_regex
                                - query
                                - header
                                - method
                              match_value:
                                type: string
                                description: |-
                                  The `match_value` format:

                                    - `path_prefix`: The format of the path rule is: /path1/path2. The initial slash is required, but the trailing slash is not.
                                    - `path_regex`: The format for path regex is a regular expression in RE2 syntax. An example for any jpg file in the /path/ directory would be /path/.*[.]jpg. The initial slash is required.
                                    - `query`: The format for query string is: ?name=value. The query string name must be preceded by a question mark (?).
                                    - `header`: The format for http header is: x-nameheader:value.
                                    - `method`: The request methods include: DELETE, GET, HEAD, POST, and PUT.
                                example: /my-path
                              session_stickiness_cookie:
                                type: string
                                description: |
                                  The cookie name used to obtain cookie value from downstream
                                  HTTP request or the cookie name used by the load
                                  balancer to generate new cookie for downstream.
                                  If you want the load balancer to generate a cookie,
                                  enter a value for `session_stickiness_ttl`.
                                example: cookie-name
                              session_stickiness_ttl:
                                type: integer
                                description: |
                                  Expiry time in seconds for the HTTP/S load balancer generated
                                  cookie. If the TTL is set to zero, the generated
                                  cookie is a session cookie only. If a `session_stickiness_ttl`
                                  is absent, the origin's cookie is used.
                          service_targets:
                            type: array
                            description: |
                              A service target is a cluster of endpoints that the load balancer directs
                              incoming requests to.
                            items:
                              additionalProperties: false
                              properties:
                                id:
                                  type: integer
                                  description: |
                                    Assigned Service Target id.
                                  example: 12
                                label:
                                  type: string
                                  description: |
                                    Service Target label.
                                  example: service-target-abc
                                percentage:
                                  type: integer
                                  description: |
                                    The percentage of incoming requests routed to this service target.
                                  example: 75
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-load-balancer-route
      security:
      - personalAccessToken: []
      - oauth:
        - aclb:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4beta/aclb/12345/routes/54321
      - lang: CLI
        source: linode-cli loadbalancers route-view 12345 54321
      x-linode-cli-action: route-view
      x-linode-grant: read_only
      x-original-op-id: getLoadBalancerRoute
      x-original-op-title: Route View
    put:
      servers:
      - url: https://api.linode.com/v4beta
      operationId: put-load-balancer-route
      summary: Update a route
      tags:
      - Routes
      description: |-
        __Beta__ Updates information about a Route including its service targets or match conditions.


        ---


        - __CLI__.

            ```
            linode-cli loadbalancers route-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            aclb:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: BETA
        tabs:
        - title: CLI
          syntax: linode-cli loadbalancers route-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: aclb:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The fields to update.
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                label:
                  type: string
                  description: |
                    The route name.
                  example: '{{label}}'
                protocol:
                  type: string
                  example: '{{protocol}}'
                  default: http
                  enum:
                  - http
                  - tcp
                rules:
                  type: array
                  items:
                    additionalProperties: false
                    properties:
                      match_condition:
                        type: object
                        description: |
                          Match conditions are for HTTP and HTTPS load balancers only, in addition
                          to setting the percentage of incoming requests to a target,
                          match conditions such as `path_prefix`, `path_regex`, `query`,
                          `header`, and `method` can be added to the route rules.
                          A rule consists of a `match_field`, and a pattern to match
                          on called a `match_value`. Each rule can specify only one
                          field or pattern pair. If match match conditions are added
                          for a TCP load balancer, the conditions are ignored.
                        x-akamai:
                          file-path: schemas/aclb-match-condition.yaml
                        additionalProperties: false
                        properties:
                          hostname:
                            type: string
                            description: |
                              Additional hostname match that is used in the routing decision.
                              Wildcards (*) are supported with the Path Regex Match
                              Type.
                            example: www.example.com
                          match_field:
                            type: string
                            example: path_prefix
                            enum:
                            - path_prefix
                            - path_regex
                            - query
                            - header
                            - method
                          match_value:
                            type: string
                            description: |-
                              The `match_value` format:

                                - `path_prefix`: The format of the path rule is: /path1/path2. The initial slash is required, but the trailing slash is not.
                                - `path_regex`: The format for path regex is a regular expression in RE2 syntax. An example for any jpg file in the /path/ directory would be /path/.*[.]jpg. The initial slash is required.
                                - `query`: The format for query string is: ?name=value. The query string name must be preceded by a question mark (?).
                                - `header`: The format for http header is: x-nameheader:value.
                                - `method`: The request methods include: DELETE, GET, HEAD, POST, and PUT.
                            example: /my-path
                          session_stickiness_cookie:
                            type: string
                            description: |
                              The cookie name used to obtain cookie value from downstream HTTP
                              request or the cookie name used by the load balancer
                              to generate new cookie for downstream. If you want the
                              load balancer to generate a cookie, enter a value for
                              `session_stickiness_ttl`.
                            example: cookie-name
                          session_stickiness_ttl:
                            type: integer
                            description: |
                              Expiry time in seconds for the HTTP/S load balancer generated cookie.
                              If the TTL is set to zero, the generated cookie is a
                              session cookie only. If a `session_stickiness_ttl` is
                              absent, the origin's cookie is used.
                      service_targets`:
                        type: object
                        description: |
                          If the `service_target` already exists, and you want to use it, use the `service_target`
                          `id` and `percentage` instead of full `service_target` configuration.
                        additionalProperties: false
                        properties:
                          id:
                            type: integer
                            description: |
                              Existing Service Target id.
                            example: 200
                          percentage:
                            type: integer
                            description: |
                              The percentage of incoming requests routed to this service target.
                            example: 75
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Route updated successfully.
          content:
            application/json:
              schema:
                type: array
                description: |
                  Routes define how the load balancer directs incoming requests to service targets. After a route
                  is created, you add rules to set match conditions that are used
                  for target selection.
                x-akamai:
                  file-path: schemas/aclb-routes.yaml
                items:
                  additionalProperties: false
                  properties:
                    id:
                      type: integer
                      description: |
                        __Read-only__ This routes unique ID.
                      example: 12345
                      readOnly: true
                    label:
                      type: string
                      description: |
                        The route name.
                      example: route-a-b
                    protocol:
                      type: string
                      description: |
                        The route protocol. The port protocol, route protocol, and service target protocol
                        for a load balancer configuration must all be the same.
                      example: http
                      default: http
                      enum:
                      - http
                      - tcp
                    rules:
                      type: array
                      description: |-
                        Rules set the conditions that are used for target selection.

                         - HTTP/S rules include match conditions, session stickiness, and the percentage of incoming requests that should be directed to each target.

                         - TCP rules include the percentage of incoming requests that should be directed to each target.
                      items:
                        additionalProperties: false
                        properties:
                          match_condition:
                            type: object
                            description: |
                              Match conditions are for HTTP and HTTPS load balancers only, in addition
                              to setting the percentage of incoming requests to a
                              target, match conditions such as `path_prefix`, `path_regex`,
                              `query`, `header`, and `method` can be added to the
                              route rules. A rule consists of a `match_field`, and
                              a pattern to match on called a `match_value`. Each rule
                              can specify only one field or pattern pair. If match
                              match conditions are added for a TCP load balancer,
                              the conditions are ignored.
                            x-akamai:
                              file-path: schemas/aclb-match-condition.yaml
                            additionalProperties: false
                            properties:
                              hostname:
                                type: string
                                description: |
                                  Additional hostname match that is used in the routing decision.
                                  Wildcards (*) are supported with the Path Regex
                                  Match Type.
                                example: www.example.com
                              match_field:
                                type: string
                                example: path_prefix
                                enum:
                                - path_prefix
                                - path_regex
                                - query
                                - header
                                - method
                              match_value:
                                type: string
                                description: |-
                                  The `match_value` format:

                                    - `path_prefix`: The format of the path rule is: /path1/path2. The initial slash is required, but the trailing slash is not.
                                    - `path_regex`: The format for path regex is a regular expression in RE2 syntax. An example for any jpg file in the /path/ directory would be /path/.*[.]jpg. The initial slash is required.
                                    - `query`: The format for query string is: ?name=value. The query string name must be preceded by a question mark (?).
                                    - `header`: The format for http header is: x-nameheader:value.
                                    - `method`: The request methods include: DELETE, GET, HEAD, POST, and PUT.
                                example: /my-path
                              session_stickiness_cookie:
                                type: string
                                description: |
                                  The cookie name used to obtain cookie value from downstream
                                  HTTP request or the cookie name used by the load
                                  balancer to generate new cookie for downstream.
                                  If you want the load balancer to generate a cookie,
                                  enter a value for `session_stickiness_ttl`.
                                example: cookie-name
                              session_stickiness_ttl:
                                type: integer
                                description: |
                                  Expiry time in seconds for the HTTP/S load balancer generated
                                  cookie. If the TTL is set to zero, the generated
                                  cookie is a session cookie only. If a `session_stickiness_ttl`
                                  is absent, the origin's cookie is used.
                          service_targets:
                            type: array
                            description: |
                              A service target is a cluster of endpoints that the load balancer directs
                              incoming requests to.
                            items:
                              additionalProperties: false
                              properties:
                                id:
                                  type: integer
                                  description: |
                                    Assigned Service Target id.
                                  example: 12
                                label:
                                  type: string
                                  description: |
                                    Service Target label.
                                  example: service-target-abc
                                percentage:
                                  type: integer
                                  description: |
                                    The percentage of incoming requests routed to this service target.
                                  example: 75
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-load-balancer-route
      security:
      - personalAccessToken: []
      - oauth:
        - aclb:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "label": "glb-route-1",
                "protocol": "http",
                "rules": [
                {
                  "match_condition":
                    {
                      "hostname": "example.com",
                      "match_field": "query",
                      "match_value": "?svc=images",
                      "session_stickiness_cookie": "cookie-name",
                      "session_stickiness_ttl":
                  },
                  "service_targets":[
                    {
                    "id": "12",
                    "percentage": 50
                    }
                  ]
                }]
                }' \
              https://api.linode.com/v4beta/aclb/12345/route/54321
      - lang: CLI
        source: |-
          linode-cli loadbalancers route-update \
            12345 54321 \
            --label glb-route-1 \
            --protocol http \
            rules [{hostname example.com, match_field query, match_value ?svc=images, session_stickiness_cookie cookie-name, session_stickiness_ttl }] \
            service_targets [{percentage 50, label service-target-1, certificate_id 456789, protocol http, load_balancing_policy round_robin}] \
            healthcheck \
            --interval 20 \
            --timeout 15 \
            --unhealthy_threshold 2 \
            --healthy_threshold 4 \
            --path /path1 \
            --host example.com \
            [{--id  1, percentage 100}]
      x-linode-cli-action: route-update
      x-linode-grant: read_write
      x-original-op-id: updateLoadBalancerRoute
      x-original-op-title: Route Update
    delete:
      servers:
      - url: https://api.linode.com/v4beta
      operationId: delete-load-balancer-route
      summary: Delete a route
      tags:
      - Routes
      description: |-
        __Beta__ Deletes a route. The route is deleted from all configurations that referenced it.

        This does not delete the Cloud Load Balancer or configurations that used the route.


        ---


        - __CLI__.

            ```
            linode-cli loadbalancers route-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            aclb:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: BETA
        tabs:
        - title: CLI
          syntax: linode-cli loadbalancers route-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: aclb:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Route deleted successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-load-balancer-route
      security:
      - personalAccessToken: []
      - oauth:
        - aclb:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4beta/aclb/12345/routes/54321
      - lang: CLI
        source: |-
          linode-cli loadbalancers route-delete \
            12345 54321
      x-linode-cli-action: route-delete
      x-linode-grant: read_write
      x-original-op-id: deleteLoadBalancerRoute
      x-original-op-title: Route Delete
    x-linode-cli-command: loadbalancers
  /aclb/{cloudLoadBalancerId}/service-targets:
    x-akamai:
      file-path: paths/service-targets.yaml
      path-info: /aclb/{cloudLoadBalancerId}/service-targets
    parameters:
    - name: cloudLoadBalancerId
      in: path
      description: |
        The ID of the Cloud Load Balancer to access.
      example: '{{cloudLoadBalancerId}}'
      x-akamai:
        file-path: parameters/cloud-load-balancer-id.yaml
      required: true
      schema:
        type: integer
    post:
      servers:
      - url: https://api.linode.com/v4beta
      operationId: post-load-balancer-service-target
      summary: Create a service target
      tags:
      - Service targets
      description: |-
        __Beta__ Creates a Service Target. The load balancer directs requests to service target endpoints using configured routes.

        __Note__. For BETA,  when creating a new service target, add at least one endpoint. If at service target was created without an endpoint, delete the service target and re-add it with an endpoint.


        ---


        - __CLI__.

            ```
            linode-cli loadbalancers servicetarget-create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            aclb:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: BETA
        tabs:
        - title: CLI
          syntax: linode-cli loadbalancers servicetarget-create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: aclb:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the service target to create.
        required: true
        content:
          application/json:
            schema:
              additionalProperties: false
              required:
              - label
              - endpoints
              properties:
                certificate_id:
                  type: integer
                  description: |
                    Certificate identification number.
                endpoints:
                  type: array
                  description: |
                    A service target is a cluster of endpoints that the load balancer directs incoming requests
                    to. At least one endpoint must be added when adding a new service
                    target. You can add different service targets and endpoints for
                    different types of requests. Health checks are configured at the
                    service target level and apply to all of the service targets endpoints.
                  x-akamai:
                    file-path: schemas/aclb-endpoint.yaml
                  required:
                  - ip
                  - port
                  - rate_capacity
                  items:
                    additionalProperties: false
                    properties:
                      host:
                        type: string
                        description: |
                          The host header for HTTP/S requests to the endpoint.
                      ip:
                        type: string
                        description: |
                          The public IPv4 or IPv6 address of the endpoint.
                      port:
                        type: integer
                        description: |
                          The service target port that the load balancer directs incoming requests to.
                          This is the port that the application is listening on.
                      rate_capacity:
                        type: integer
                        description: |
                          The maximum number of requests/second that can be directed to this endpoint.
                          If the actual number of requests/second exceeds the configured
                          capacity value, requests are distributed to the other endpoints.
                        example: 5000
                        default: 10000
                healthcheck:
                  type: object
                  description: |
                    Health checks query the service targets by performing TCP connections or by making HTTP
                    requests. For TCP, a service target is considered healthy and
                    able to accept incoming requests when there is a successful TCP
                    handshake with the service target. When HTTP/S is used to validate
                    health status, the service target is considered healthy when requests
                    to its path or host return a 2xx or 3xx status code response.
                  x-akamai:
                    file-path: schemas/aclb-health-check.yaml
                  additionalProperties: false
                  properties:
                    healthy_threshold:
                      type: integer
                      description: |
                        The number of consecutive health checks that must be successful before marking a service
                        target as healthy.
                      example: 2
                      default: 3
                      minimum: 1
                    host:
                      type: string
                      description: |
                        When the `healthcheck` protocol is set to `http`, this is the optional host header
                        sent in the health check request. When the health check `protocol`
                        is set to TCP, `host` is not applicable.
                      example: example.com
                    interval:
                      type: integer
                      description: |
                        The number of seconds between health checks for this service target.
                      example: 5
                      default: 10
                      minimum: 1
                    path:
                      type: string
                      description: |
                        When `protocol` is set to HTTP/S, enter an optional request url path for the
                        health check. When the health check `protocol` is set to TCP,
                        `path` is not applicable.
                      example: /path/to/endpoint
                    protocol:
                      type: string
                      description: |
                        For HTTP service targets, the health check protocol can be HTTP or TCP. For
                        TCP service targets, the health check protocol must be TCP.
                        When set to TCP, `path` and `host` are not applicable.
                      example: http
                      default: http
                      enum:
                      - tcp
                      - http
                    timeout:
                      type: integer
                      description: |
                        How long to wait (in seconds) before canceling a health check because a connection
                        could not be established with the service target.
                      example: 10
                      default: 5
                      minimum: 1
                    unhealthy_threshold:
                      type: integer
                      description: |
                        The number of consecutive health checks that must fail before marking a service target
                        as unhealthy.
                      example: 3
                      default: 3
                      minimum: 1
                label:
                  type: string
                  description: |
                    The service target label.
                  example: service-target-1
                load_balancing_policy:
                  type: string
                  description: |
                    The algorithm this Cloud Load Balancer uses for routing new requests to service targets.
                    Cloud Load Balancer also has a geo-proximity feature for selecting
                    the optimum endpoint.
                  example: roundrobin
                  default: roundrobin
                  enum:
                  - round_robin
                  - least_request
                  - ring_hash
                  - random
                  - maglev
                protocol:
                  type: string
                  description: |
                    The protocol this service target is configured to serve. The port protocol, route protocol,
                    and service target protocol for a load balancer configuration
                    must all be the same.
                  default: http
                  enum:
                  - http
                  - https
                  - tcp
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Service Target created successfully.
          content:
            application/json:
              schema:
                type: array
                description: |
                  A service target is a cluster of endpoints that the load balancer directs incoming requests to.
                x-akamai:
                  file-path: schemas/aclb-service-target-endpoint.yaml
                required:
                - endpoints
                items:
                  additionalProperties: false
                  properties:
                    id:
                      type: integer
                      description: |
                        Service Target id.
                      example: 200
                    certificate_id:
                      type: integer
                      description: |
                        Certificate identification number.
                      example: 50
                    endpoints:
                      type: array
                      description: |
                        A service target is a cluster of endpoints that the load balancer directs incoming
                        requests to. At least one endpoint must be added when adding
                        a new service target.
                      items:
                        additionalProperties: false
                        properties:
                          health_status:
                            type: string
                            description: |-
                              Health status is `up` for healthy service target endpoints and `down` for unhealthy endpoints.

                              For TCP, a service target is considered healthy and able to accept incoming requests when there is a successful TCP handshake with the service target.

                              For HTTP/S, the service target endpoint is considered healthy, when requests to its path or host return a 2xx or 3xx status code response.
                            example: up
                            enum:
                            - up
                            - down
                          host:
                            type: string
                            description: |
                              The host header for HTTP/S requests to the endpoint.
                            example: example.org
                          ip:
                            type: string
                            description: |
                              The public IPv4 or IPv6 address of the endpoint.
                            example: 192.0.2.10
                          port:
                            type: integer
                            description: |
                              The service target port that the load balancer directs incoming
                              requests to. This is the port that the application is
                              listening on.
                            example: 443
                          rate_capacity:
                            type: integer
                            description: |
                              The maximum number of requests/second that can be directed to this
                              endpoint. If the actual number of requests/second exceeds
                              the configured capacity value, requests are distributed
                              to the other endpoints.
                            example: 5000
                            default: 10000
                    healthcheck:
                      type: object
                      description: |
                        Health checks query the service targets by performing TCP connections or by making
                        HTTP requests. For TCP, a service target is considered healthy
                        and able to accept incoming requests when there is a successful
                        TCP handshake with the service target. When HTTP/S is used
                        to validate health status, the service target is considered
                        healthy when requests to its path or host return a 2xx or
                        3xx status code response.
                      x-akamai:
                        file-path: schemas/aclb-health-check.yaml
                      additionalProperties: false
                      properties:
                        healthy_threshold:
                          type: integer
                          description: |
                            The number of consecutive health checks that must be successful before
                            marking a service target as healthy.
                          example: 2
                          default: 3
                          minimum: 1
                        host:
                          type: string
                          description: |
                            When the `healthcheck` protocol is set to `http`, this is the optional host header
                            sent in the health check request. When the health check
                            `protocol` is set to TCP, `host` is not applicable.
                          example: example.com
                        interval:
                          type: integer
                          description: |
                            The number of seconds between health checks for this service target.
                          example: 5
                          default: 10
                          minimum: 1
                        path:
                          type: string
                          description: |
                            When `protocol` is set to HTTP/S, enter an optional request url path for
                            the health check. When the health check `protocol` is
                            set to TCP, `path` is not applicable.
                          example: /path/to/endpoint
                        protocol:
                          type: string
                          description: |
                            For HTTP service targets, the health check protocol can be HTTP or TCP.
                            For TCP service targets, the health check protocol must
                            be TCP. When set to TCP, `path` and `host` are not applicable.
                          example: http
                          default: http
                          enum:
                          - tcp
                          - http
                        timeout:
                          type: integer
                          description: |
                            How long to wait (in seconds) before canceling a health check because a
                            connection could not be established with the service target.
                          example: 10
                          default: 5
                          minimum: 1
                        unhealthy_threshold:
                          type: integer
                          description: |
                            The number of consecutive health checks that must fail before marking
                            a service target as unhealthy.
                          example: 3
                          default: 3
                          minimum: 1
                    label:
                      type: string
                      description: |
                        Service Target label.
                      example: service-target-abc
                    load_balancing_policy:
                      type: string
                      description: |
                        The algorithm this Cloud Load Balancer uses for routing new requests to service
                        targets. Cloud Load Balancer also has a geo-proximity feature
                        for selecting the optimum endpoint.
                      example: roundrobin
                      default: roundrobin
                      enum:
                      - round_robin
                      - least_request
                      - ring_hash
                      - random
                      - maglev
                    protocol:
                      type: string
                      description: |
                        The protocol this target is configured to serve. The port protocol, route protocols,
                        and service target protocols for a load balancer configuration
                        must all be the same.
                      example: http
                      default: http
                      enum:
                      - http
                      - tcp
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-load-balancer-service-target
      security:
      - personalAccessToken: []
      - oauth:
        - aclb:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "label": "service-target-1",
                "endpoints": [
                  {
                  "ip": "192.0.2.0",
                  "host": "example.org",
                  "port": 443,
                  "rate_capacity": 10000
                  }
                  ],
                "certificate_id": "456789",
                "protocol": "https",
                "load_balancing_policy": "round_robin",
                "healthcheck": {
                  "protocol": "http",
                  "interval": 20,
                  "timeout": 15,
                  "unhealthy_threshold": 2,
                  "healthy_threshold": 4,
                  "path": "/path1",
                  "host": "example.com"
                }
              }' \
              https://api.linode.com/v4beta/aclb/12345/service-targets
      - lang: CLI
        source: |-
          linode-cli loadbalancers servicetarget-create \
            12345 \
            --label service-target-1 \
            --endpoints [{ip 192.0.2.0, host example.com, port 443, rate_capacity 5000}] \
            --certificate_id 456789 \
            --protocol https \
            --load_balancing_policy round_robin \
            healthcheck \
            --protocol https \
            --interval 20 \
            --timeout 15 \
            --unhealthy_threshold 2 \
            --healthy_threshold 4 \
            --path /path1 \
            --host example.com
      x-linode-cli-action: servicetarget-create
      x-linode-grant: read_write
      x-original-op-id: createLoadBalancerServiceTarget
      x-original-op-title: Service Target Create
    get:
      operationId: get-load-balancer-service-targets
      summary: List service targets
      tags:
      - Service targets
      description: |-
        __Beta__ View information for the service targets assigned to this Cloud Load Balancer.

        A service target is a group of endpoints that a route directs requests to.

        A service target may be associated with one or many routing rules, as long as the routing rules belong to the same Cloud Load Balancer instance.


        ---


        - __CLI__.

            ```
            linode-cli loadbalancers service-targets
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            aclb:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: BETA
        tabs:
        - title: CLI
          syntax: linode-cli loadbalancers service-targets
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: aclb:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of Cloud Load Balancer service targets.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    description: |
                      A service target is a cluster of endpoints that the load balancer directs incoming
                      requests to.
                    x-akamai:
                      file-path: schemas/aclb-service-target-endpoint.yaml
                    required:
                    - endpoints
                    items:
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            Service Target id.
                          example: 200
                        certificate_id:
                          type: integer
                          description: |
                            Certificate identification number.
                          example: 50
                        endpoints:
                          type: array
                          description: |
                            A service target is a cluster of endpoints that the load balancer directs
                            incoming requests to. At least one endpoint must be added
                            when adding a new service target.
                          items:
                            additionalProperties: false
                            properties:
                              health_status:
                                type: string
                                description: |-
                                  Health status is `up` for healthy service target endpoints and `down` for unhealthy endpoints.

                                  For TCP, a service target is considered healthy and able to accept incoming requests when there is a successful TCP handshake with the service target.

                                  For HTTP/S, the service target endpoint is considered healthy, when requests to its path or host return a 2xx or 3xx status code response.
                                example: up
                                enum:
                                - up
                                - down
                              host:
                                type: string
                                description: |
                                  The host header for HTTP/S requests to the endpoint.
                                example: example.org
                              ip:
                                type: string
                                description: |
                                  The public IPv4 or IPv6 address of the endpoint.
                                example: 192.0.2.10
                              port:
                                type: integer
                                description: |
                                  The service target port that the load balancer directs incoming
                                  requests to. This is the port that the application
                                  is listening on.
                                example: 443
                              rate_capacity:
                                type: integer
                                description: |
                                  The maximum number of requests/second that can be directed to
                                  this endpoint. If the actual number of requests/second
                                  exceeds the configured capacity value, requests
                                  are distributed to the other endpoints.
                                example: 5000
                                default: 10000
                        healthcheck:
                          type: object
                          description: |
                            Health checks query the service targets by performing TCP connections
                            or by making HTTP requests. For TCP, a service target
                            is considered healthy and able to accept incoming requests
                            when there is a successful TCP handshake with the service
                            target. When HTTP/S is used to validate health status,
                            the service target is considered healthy when requests
                            to its path or host return a 2xx or 3xx status code response.
                          x-akamai:
                            file-path: schemas/aclb-health-check.yaml
                          additionalProperties: false
                          properties:
                            healthy_threshold:
                              type: integer
                              description: |
                                The number of consecutive health checks that must be successful
                                before marking a service target as healthy.
                              example: 2
                              default: 3
                              minimum: 1
                            host:
                              type: string
                              description: |
                                When the `healthcheck` protocol is set to `http`, this is the
                                optional host header sent in the health check request.
                                When the health check `protocol` is set to TCP, `host`
                                is not applicable.
                              example: example.com
                            interval:
                              type: integer
                              description: |
                                The number of seconds between health checks for this service
                                target.
                              example: 5
                              default: 10
                              minimum: 1
                            path:
                              type: string
                              description: |
                                When `protocol` is set to HTTP/S, enter an optional request url path
                                for the health check. When the health check `protocol`
                                is set to TCP, `path` is not applicable.
                              example: /path/to/endpoint
                            protocol:
                              type: string
                              description: |
                                For HTTP service targets, the health check protocol can be
                                HTTP or TCP. For TCP service targets, the health check
                                protocol must be TCP. When set to TCP, `path` and
                                `host` are not applicable.
                              example: http
                              default: http
                              enum:
                              - tcp
                              - http
                            timeout:
                              type: integer
                              description: |
                                How long to wait (in seconds) before canceling a health check
                                because a connection could not be established with
                                the service target.
                              example: 10
                              default: 5
                              minimum: 1
                            unhealthy_threshold:
                              type: integer
                              description: |
                                The number of consecutive health checks that must fail before
                                marking a service target as unhealthy.
                              example: 3
                              default: 3
                              minimum: 1
                        label:
                          type: string
                          description: |
                            Service Target label.
                          example: service-target-abc
                        load_balancing_policy:
                          type: string
                          description: |
                            The algorithm this Cloud Load Balancer uses for routing new requests
                            to service targets. Cloud Load Balancer also has a geo-proximity
                            feature for selecting the optimum endpoint.
                          example: roundrobin
                          default: roundrobin
                          enum:
                          - round_robin
                          - least_request
                          - ring_hash
                          - random
                          - maglev
                        protocol:
                          type: string
                          description: |
                            The protocol this target is configured to serve. The port protocol,
                            route protocols, and service target protocols for a load
                            balancer configuration must all be the same.
                          example: http
                          default: http
                          enum:
                          - http
                          - tcp
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-load-balancer-service-targets
      security:
      - personalAccessToken: []
      - oauth:
        - aclb:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl https://api.linode.com/v4beta/aclb/12345/service-targets \
            -H "Authorization: Bearer $TOKEN"
      - lang: CLI
        source: linode-cli loadbalancers servicetargets-list 12345
      x-linode-cli-action: service-targets
      x-linode-grant: read_only
      x-original-op-id: getLoadBalancerService-Targets
      x-original-op-title: Service Targets List
    x-linode-cli-command: loadbalancers
  /aclb/{cloudLoadBalancerId}/service-targets/{serviceTargetId}:
    x-akamai:
      file-path: paths/service-target.yaml
      path-info: /aclb/{cloudLoadBalancerId}/service-targets/{serviceTargetId}
    parameters:
    - name: cloudLoadBalancerId
      in: path
      description: |
        The ID of the Cloud Load Balancer to access.
      example: '{{cloudLoadBalancerId}}'
      x-akamai:
        file-path: parameters/cloud-load-balancer-id.yaml
      required: true
      schema:
        type: integer
    - name: serviceTargetId
      in: path
      description: |
        The ID of the service target to access
      example: '{{serviceTargetId}}'
      x-akamai:
        file-path: parameters/service-target-id-path.yaml
      required: true
      schema:
        type: integer
    get:
      servers:
      - url: https://api.linode.com/v4beta
      operationId: get-load-balancer-service-target
      summary: Get a service target
      tags:
      - Service targets
      description: |-
        __Beta__ Returns information about a service target.


        ---


        - __CLI__.

            ```
            linode-cli loadbalancers service-target-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            aclb:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: BETA
        tabs:
        - title: CLI
          syntax: linode-cli loadbalancers service-target-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: aclb:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns information about a single service target.
          content:
            application/json:
              schema:
                type: array
                description: |
                  A service target is a cluster of endpoints that the load balancer directs incoming requests to.
                x-akamai:
                  file-path: schemas/aclb-service-target-endpoint.yaml
                required:
                - endpoints
                items:
                  additionalProperties: false
                  properties:
                    id:
                      type: integer
                      description: |
                        Service Target id.
                      example: 200
                    certificate_id:
                      type: integer
                      description: |
                        Certificate identification number.
                      example: 50
                    endpoints:
                      type: array
                      description: |
                        A service target is a cluster of endpoints that the load balancer directs incoming
                        requests to. At least one endpoint must be added when adding
                        a new service target.
                      items:
                        additionalProperties: false
                        properties:
                          health_status:
                            type: string
                            description: |-
                              Health status is `up` for healthy service target endpoints and `down` for unhealthy endpoints.

                              For TCP, a service target is considered healthy and able to accept incoming requests when there is a successful TCP handshake with the service target.

                              For HTTP/S, the service target endpoint is considered healthy, when requests to its path or host return a 2xx or 3xx status code response.
                            example: up
                            enum:
                            - up
                            - down
                          host:
                            type: string
                            description: |
                              The host header for HTTP/S requests to the endpoint.
                            example: example.org
                          ip:
                            type: string
                            description: |
                              The public IPv4 or IPv6 address of the endpoint.
                            example: 192.0.2.10
                          port:
                            type: integer
                            description: |
                              The service target port that the load balancer directs incoming
                              requests to. This is the port that the application is
                              listening on.
                            example: 443
                          rate_capacity:
                            type: integer
                            description: |
                              The maximum number of requests/second that can be directed to this
                              endpoint. If the actual number of requests/second exceeds
                              the configured capacity value, requests are distributed
                              to the other endpoints.
                            example: 5000
                            default: 10000
                    healthcheck:
                      type: object
                      description: |
                        Health checks query the service targets by performing TCP connections or by making
                        HTTP requests. For TCP, a service target is considered healthy
                        and able to accept incoming requests when there is a successful
                        TCP handshake with the service target. When HTTP/S is used
                        to validate health status, the service target is considered
                        healthy when requests to its path or host return a 2xx or
                        3xx status code response.
                      x-akamai:
                        file-path: schemas/aclb-health-check.yaml
                      additionalProperties: false
                      properties:
                        healthy_threshold:
                          type: integer
                          description: |
                            The number of consecutive health checks that must be successful before
                            marking a service target as healthy.
                          example: 2
                          default: 3
                          minimum: 1
                        host:
                          type: string
                          description: |
                            When the `healthcheck` protocol is set to `http`, this is the optional host header
                            sent in the health check request. When the health check
                            `protocol` is set to TCP, `host` is not applicable.
                          example: example.com
                        interval:
                          type: integer
                          description: |
                            The number of seconds between health checks for this service target.
                          example: 5
                          default: 10
                          minimum: 1
                        path:
                          type: string
                          description: |
                            When `protocol` is set to HTTP/S, enter an optional request url path for
                            the health check. When the health check `protocol` is
                            set to TCP, `path` is not applicable.
                          example: /path/to/endpoint
                        protocol:
                          type: string
                          description: |
                            For HTTP service targets, the health check protocol can be HTTP or TCP.
                            For TCP service targets, the health check protocol must
                            be TCP. When set to TCP, `path` and `host` are not applicable.
                          example: http
                          default: http
                          enum:
                          - tcp
                          - http
                        timeout:
                          type: integer
                          description: |
                            How long to wait (in seconds) before canceling a health check because a
                            connection could not be established with the service target.
                          example: 10
                          default: 5
                          minimum: 1
                        unhealthy_threshold:
                          type: integer
                          description: |
                            The number of consecutive health checks that must fail before marking
                            a service target as unhealthy.
                          example: 3
                          default: 3
                          minimum: 1
                    label:
                      type: string
                      description: |
                        Service Target label.
                      example: service-target-abc
                    load_balancing_policy:
                      type: string
                      description: |
                        The algorithm this Cloud Load Balancer uses for routing new requests to service
                        targets. Cloud Load Balancer also has a geo-proximity feature
                        for selecting the optimum endpoint.
                      example: roundrobin
                      default: roundrobin
                      enum:
                      - round_robin
                      - least_request
                      - ring_hash
                      - random
                      - maglev
                    protocol:
                      type: string
                      description: |
                        The protocol this target is configured to serve. The port protocol, route protocols,
                        and service target protocols for a load balancer configuration
                        must all be the same.
                      example: http
                      default: http
                      enum:
                      - http
                      - tcp
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-load-balancer-service-target
      security:
      - personalAccessToken: []
      - oauth:
        - aclb:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4beta/aclb/12345/service-targets/54321
      - lang: CLI
        source: linode-cli loadbalancers servicetarget-view 12345 54321
      x-linode-cli-action: service-target-view
      x-linode-grant: read_only
      x-original-op-id: getLoadBalancerService-Target
      x-original-op-title: Service Target View
    put:
      servers:
      - url: https://api.linode.com/v4beta
      operationId: put-load-balancer-service-target
      summary: Update a service target
      tags:
      - Service targets
      description: |-
        __Beta__ Updates information for a service target.

        __Note__. Editing the service target endpoint `host` value is not supported for Beta. If you need to change the service target endpoint `host`, delete the service target, and create a new service target with the new `host` value.


        ---


        - __CLI__.

            ```
            linode-cli loadbalancers servicetarget-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            aclb:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: BETA
        tabs:
        - title: CLI
          syntax: linode-cli loadbalancers servicetarget-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: aclb:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The fields to update.
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                certificate_id:
                  type: integer
                  description: |
                    Certificate identification number.
                  example: '{{certificate_id}}'
                endpoints:
                  type: array
                  description: |
                    A service target is a cluster of endpoints that the load balancer directs incoming requests
                    to. At least one endpoint must be added when adding a new service
                    target. You can add different service targets and endpoints for
                    different types of requests. Health checks are configured at the
                    service target level and apply to all of the service targets endpoints.
                  x-akamai:
                    file-path: schemas/aclb-endpoint.yaml
                  required:
                  - ip
                  - port
                  - rate_capacity
                  items:
                    additionalProperties: false
                    properties:
                      host:
                        type: string
                        description: |
                          The host header for HTTP/S requests to the endpoint.
                      ip:
                        type: string
                        description: |
                          The public IPv4 or IPv6 address of the endpoint.
                      port:
                        type: integer
                        description: |
                          The service target port that the load balancer directs incoming requests to.
                          This is the port that the application is listening on.
                      rate_capacity:
                        type: integer
                        description: |
                          The maximum number of requests/second that can be directed to this endpoint.
                          If the actual number of requests/second exceeds the configured
                          capacity value, requests are distributed to the other endpoints.
                        example: 5000
                        default: 10000
                healthcheck:
                  type: object
                  description: |
                    Health checks query the service targets by performing TCP connections or by making HTTP
                    requests. For TCP, a service target is considered healthy and
                    able to accept incoming requests when there is a successful TCP
                    handshake with the service target. When HTTP/S is used to validate
                    health status, the service target is considered healthy when requests
                    to its path or host return a 2xx or 3xx status code response.
                  x-akamai:
                    file-path: schemas/aclb-health-check.yaml
                  additionalProperties: false
                  properties:
                    healthy_threshold:
                      type: integer
                      description: |
                        The number of consecutive health checks that must be successful before marking a service
                        target as healthy.
                      example: 2
                      default: 3
                      minimum: 1
                    host:
                      type: string
                      description: |
                        When the `healthcheck` protocol is set to `http`, this is the optional host header
                        sent in the health check request. When the health check `protocol`
                        is set to TCP, `host` is not applicable.
                      example: example.com
                    interval:
                      type: integer
                      description: |
                        The number of seconds between health checks for this service target.
                      example: 5
                      default: 10
                      minimum: 1
                    path:
                      type: string
                      description: |
                        When `protocol` is set to HTTP/S, enter an optional request url path for the
                        health check. When the health check `protocol` is set to TCP,
                        `path` is not applicable.
                      example: /path/to/endpoint
                    protocol:
                      type: string
                      description: |
                        For HTTP service targets, the health check protocol can be HTTP or TCP. For
                        TCP service targets, the health check protocol must be TCP.
                        When set to TCP, `path` and `host` are not applicable.
                      example: http
                      default: http
                      enum:
                      - tcp
                      - http
                    timeout:
                      type: integer
                      description: |
                        How long to wait (in seconds) before canceling a health check because a connection
                        could not be established with the service target.
                      example: 10
                      default: 5
                      minimum: 1
                    unhealthy_threshold:
                      type: integer
                      description: |
                        The number of consecutive health checks that must fail before marking a service target
                        as unhealthy.
                      example: 3
                      default: 3
                      minimum: 1
                label:
                  type: string
                  description: |
                    The service target name.
                  example: '{{label}}'
                load_balancing_policy:
                  type: string
                  description: |
                    The algorithm this Cloud Load Balancer uses for routing new requests to service targets.
                    Cloud Load Balancer also has a geo-proximity feature for selecting
                    the optimum endpoint.
                  example: '{{load_balancing_policy}}'
                  default: roundrobin
                  enum:
                  - round_robin
                  - least_request
                  - ring_hash
                  - random
                  - maglev
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Service target updated successfully.
          content:
            application/json:
              schema:
                type: array
                description: |
                  A service target is a cluster of endpoints that the load balancer directs incoming requests to.
                x-akamai:
                  file-path: schemas/aclb-service-target-endpoint.yaml
                required:
                - endpoints
                items:
                  additionalProperties: false
                  properties:
                    id:
                      type: integer
                      description: |
                        Service Target id.
                      example: 200
                    certificate_id:
                      type: integer
                      description: |
                        Certificate identification number.
                      example: 50
                    endpoints:
                      type: array
                      description: |
                        A service target is a cluster of endpoints that the load balancer directs incoming
                        requests to. At least one endpoint must be added when adding
                        a new service target.
                      items:
                        additionalProperties: false
                        properties:
                          health_status:
                            type: string
                            description: |-
                              Health status is `up` for healthy service target endpoints and `down` for unhealthy endpoints.

                              For TCP, a service target is considered healthy and able to accept incoming requests when there is a successful TCP handshake with the service target.

                              For HTTP/S, the service target endpoint is considered healthy, when requests to its path or host return a 2xx or 3xx status code response.
                            example: up
                            enum:
                            - up
                            - down
                          host:
                            type: string
                            description: |
                              The host header for HTTP/S requests to the endpoint.
                            example: example.org
                          ip:
                            type: string
                            description: |
                              The public IPv4 or IPv6 address of the endpoint.
                            example: 192.0.2.10
                          port:
                            type: integer
                            description: |
                              The service target port that the load balancer directs incoming
                              requests to. This is the port that the application is
                              listening on.
                            example: 443
                          rate_capacity:
                            type: integer
                            description: |
                              The maximum number of requests/second that can be directed to this
                              endpoint. If the actual number of requests/second exceeds
                              the configured capacity value, requests are distributed
                              to the other endpoints.
                            example: 5000
                            default: 10000
                    healthcheck:
                      type: object
                      description: |
                        Health checks query the service targets by performing TCP connections or by making
                        HTTP requests. For TCP, a service target is considered healthy
                        and able to accept incoming requests when there is a successful
                        TCP handshake with the service target. When HTTP/S is used
                        to validate health status, the service target is considered
                        healthy when requests to its path or host return a 2xx or
                        3xx status code response.
                      x-akamai:
                        file-path: schemas/aclb-health-check.yaml
                      additionalProperties: false
                      properties:
                        healthy_threshold:
                          type: integer
                          description: |
                            The number of consecutive health checks that must be successful before
                            marking a service target as healthy.
                          example: 2
                          default: 3
                          minimum: 1
                        host:
                          type: string
                          description: |
                            When the `healthcheck` protocol is set to `http`, this is the optional host header
                            sent in the health check request. When the health check
                            `protocol` is set to TCP, `host` is not applicable.
                          example: example.com
                        interval:
                          type: integer
                          description: |
                            The number of seconds between health checks for this service target.
                          example: 5
                          default: 10
                          minimum: 1
                        path:
                          type: string
                          description: |
                            When `protocol` is set to HTTP/S, enter an optional request url path for
                            the health check. When the health check `protocol` is
                            set to TCP, `path` is not applicable.
                          example: /path/to/endpoint
                        protocol:
                          type: string
                          description: |
                            For HTTP service targets, the health check protocol can be HTTP or TCP.
                            For TCP service targets, the health check protocol must
                            be TCP. When set to TCP, `path` and `host` are not applicable.
                          example: http
                          default: http
                          enum:
                          - tcp
                          - http
                        timeout:
                          type: integer
                          description: |
                            How long to wait (in seconds) before canceling a health check because a
                            connection could not be established with the service target.
                          example: 10
                          default: 5
                          minimum: 1
                        unhealthy_threshold:
                          type: integer
                          description: |
                            The number of consecutive health checks that must fail before marking
                            a service target as unhealthy.
                          example: 3
                          default: 3
                          minimum: 1
                    label:
                      type: string
                      description: |
                        Service Target label.
                      example: service-target-abc
                    load_balancing_policy:
                      type: string
                      description: |
                        The algorithm this Cloud Load Balancer uses for routing new requests to service
                        targets. Cloud Load Balancer also has a geo-proximity feature
                        for selecting the optimum endpoint.
                      example: roundrobin
                      default: roundrobin
                      enum:
                      - round_robin
                      - least_request
                      - ring_hash
                      - random
                      - maglev
                    protocol:
                      type: string
                      description: |
                        The protocol this target is configured to serve. The port protocol, route protocols,
                        and service target protocols for a load balancer configuration
                        must all be the same.
                      example: http
                      default: http
                      enum:
                      - http
                      - tcp
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-load-balancer-service-target
      security:
      - personalAccessToken: []
      - oauth:
        - aclb:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "label": "service-target-a",
                "endpoints": [
                  {
                  "ip": "192.0.2.0",
                  "host": "example.org",
                  "port": 443,
                  "rate_capacity": 10000
                  }
                ],
                "certificate_id": "456789",
                "load_balancing_policy": "round_robin",
                "healthcheck":
                  {
                  "protocol": "http",
                  "interval": 20,
                  "timeout": 15,
                  "unhealthy_threshold": 2,
                  "healthy_threshold": 4,
                  "path": "/path1",
                  "host": "example.com"
                  }
            }' \
              https://api.linode.com/v4beta/aclb/12345/service-targets/54321
      - lang: CLI
        source: |-
          linode-cli loadbalancers servicetarget-update \
            12345 54321 \
            --label service-target-a \
            --endpoints [{ip 192.0.2.0, host example.com, port 443, rate_capacity 5000}] \
            --certificate_id 456789 \
            --protocol https \
            --load_balancing_policy round_robin \
            healthcheck \
            --protocol https \
            --interval 20 \
            --timeout 15 \
            --unhealthy_threshold 2 \
            --healthy_threshold 4 \
            --path /path1 \
            --host example.com
      x-linode-cli-action: servicetarget-update
      x-linode-grant: read_write
      x-original-op-id: updateLoadBalancerServiceTarget
      x-original-op-title: Service Target Update
    delete:
      servers:
      - url: https://api.linode.com/v4beta
      operationId: delete-load-balancer-service-target
      summary: Delete a service target
      tags:
      - Service targets
      description: |-
        __Beta__ Deletes a service target.

        Before deleting a service target, make sure that no requests are being directed to it.

        This does not change or remove the Cloud Load Balancer.


        ---


        - __CLI__.

            ```
            linode-cli loadbalancers servicetarget-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            aclb:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        status: BETA
        tabs:
        - title: CLI
          syntax: linode-cli loadbalancers servicetarget-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: aclb:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Service target deleted successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-load-balancer-service-target
      security:
      - personalAccessToken: []
      - oauth:
        - aclb:read_write
      x-linode-cli-action: servicetarget-delete
      x-linode-grant: read_write
      x-original-op-id: deleteLoadBalancerServiceTarget
      x-original-op-title: Service Target Delete
    x-linode-cli-command: loadbalancers
  /betas:
    x-akamai:
      file-path: paths/betas.yaml
      path-info: /betas
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-beta-programs
      summary: List Beta programs
      tags:
      - Beta programs
      description: |-
        Display all active Beta Programs.

        Only unrestricted Users can access this operation.
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated list of all available Beta Program objects.
          content:
            application/json:
              schema:
                allOf:
                - type: object
                  description: |
                    An envelope for paginated response. When accessing a collection through a GET endpoint,
                    the results are wrapped in this envelope which includes metadata
                    about those results. Results are presented within a `data` array.
                    See [Pagination](https://techdocs.akamai.com/linode-api/reference/pagination)
                    for more information.
                  x-akamai:
                    file-path: schemas/pagination-envelope.yaml
                  additionalProperties: false
                  properties:
                    page:
                      type: integer
                      description: |
                        __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    pages:
                      type: integer
                      description: |
                        __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    results:
                      type: integer
                      description: |
                        __Read-only__ The total number of results.
                      example: 1
                      readOnly: true
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        type: object
                        description: |
                          An object representing Beta Program information.
                        x-akamai:
                          file-path: schemas/beta-program.yaml
                        additionalProperties: false
                        properties:
                          id:
                            type: string
                            description: |
                              The unique identifier of the Beta Program.
                            example: example_open
                            x-linode-cli-display: 1
                          description:
                            type: string
                            description: |
                              __Read-only__ Additional details regarding the Beta Program.
                            example: This is an open public beta for an example feature.
                            nullable: true
                            readOnly: true
                            x-linode-cli-display: 3
                          ended:
                            type: string
                            description: |-
                              __Read-only__ The date-time that the Beta Program ended.

                              `null` indicates that the Beta Program is ongoing.
                            example: null
                            nullable: true
                            readOnly: true
                            format: date-time
                            x-linode-cli-display: 6
                            x-linode-filterable: true
                          greenlight_only:
                            type: boolean
                            description: |
                              __Read-only__ Whether the Beta Program requires [Green Light](https://www.linode.com/green-light/)
                              participation for enrollment.
                            example: true
                            readOnly: true
                            x-linode-cli-display: 4
                            x-linode-filterable: true
                          label:
                            type: string
                            description: |
                              __Read-only__ The name of the Beta Program.
                            example: Example Open Beta
                            readOnly: true
                            x-linode-cli-display: 2
                            x-linode-filterable: true
                          more_info:
                            type: string
                            description: |
                              __Read-only__ Additional source of information for the Beta Program.
                            example: https://www.linode.com/green-light/
                            nullable: true
                            readOnly: true
                            x-linode-cli-display: 7
                          started:
                            type: string
                            description: |
                              __Read-only__ The start date-time of the Beta Program.
                            example: '2023-07-11T00:00:00'
                            readOnly: true
                            format: date-time
                            x-linode-cli-display: 5
                            x-linode-filterable: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-beta-programs
      security:
      - personalAccessToken: []
      - oauth: []
      x-code-samples:
      - lang: Shell
        source: |-
          curl https://api.linode.com/v4/betas \
              -H "Authorization: Bearer $TOKEN"
      - lang: CLI
        source: linode-cli betas list
      x-linode-cli-action:
      - list
      - ls
      x-linode-grant: unrestricted only
      x-original-op-id: getBetaPrograms
      x-original-op-title: Beta Programs List
    x-linode-cli-command: betas
  /betas/{betaId}:
    x-akamai:
      file-path: paths/beta.yaml
      path-info: /betas/{betaId}
    parameters:
    - name: betaId
      in: path
      description: |
        The ID of the Beta Program.
      example: '{{betaId}}'
      x-akamai:
        file-path: parameters/beta-id.yaml
      required: true
      schema:
        type: string
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-beta-program
      summary: Get a Beta program
      tags:
      - Beta programs
      description: |-
        Display information about a Beta Program. This operation can be used to access inactive as well as active Beta Programs.

        Only unrestricted Users can access this operation.
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated list of all available Beta Program objects.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An object representing Beta Program information.
                x-akamai:
                  file-path: schemas/beta-program.yaml
                additionalProperties: false
                properties:
                  id:
                    type: string
                    description: |
                      The unique identifier of the Beta Program.
                    example: example_open
                    x-linode-cli-display: 1
                  description:
                    type: string
                    description: |
                      __Read-only__ Additional details regarding the Beta Program.
                    example: This is an open public beta for an example feature.
                    nullable: true
                    readOnly: true
                    x-linode-cli-display: 3
                  ended:
                    type: string
                    description: |-
                      __Read-only__ The date-time that the Beta Program ended.

                      `null` indicates that the Beta Program is ongoing.
                    example: null
                    nullable: true
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 6
                    x-linode-filterable: true
                  greenlight_only:
                    type: boolean
                    description: |
                      __Read-only__ Whether the Beta Program requires [Green Light](https://www.linode.com/green-light/)
                      participation for enrollment.
                    example: true
                    readOnly: true
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  label:
                    type: string
                    description: |
                      __Read-only__ The name of the Beta Program.
                    example: Example Open Beta
                    readOnly: true
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  more_info:
                    type: string
                    description: |
                      __Read-only__ Additional source of information for the Beta Program.
                    example: https://www.linode.com/green-light/
                    nullable: true
                    readOnly: true
                    x-linode-cli-display: 7
                  started:
                    type: string
                    description: |
                      __Read-only__ The start date-time of the Beta Program.
                    example: '2023-07-11T00:00:00'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 5
                    x-linode-filterable: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-beta-program
      security:
      - personalAccessToken: []
      - oauth: []
      x-code-samples:
      - lang: Shell
        source: |-
          curl https://api.linode.com/v4/betas/$betaId \
              -H "Authorization: Bearer $TOKEN"
      - lang: CLI
        source: linode-cli beta view $betaId
      x-linode-cli-action: view
      x-linode-grant: unrestricted only
      x-original-op-id: getBetaProgram
      x-original-op-title: Beta Program View
    x-linode-cli-command: betas
  /databases/engines:
    x-akamai:
      file-path: paths/engines.yaml
      path-info: /databases/engines
    get:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: get-databases-engines
      summary: List managed database engines
      tags:
      - Engines
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Display all available Managed Database engine types and versions. Engine IDs are used when creating new Managed Databases.
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated list of all available Managed Database engines and versions.
          content:
            application/json:
              schema:
                allOf:
                - type: object
                  description: |
                    An envelope for paginated response. When accessing a collection through a GET endpoint,
                    the results are wrapped in this envelope which includes metadata
                    about those results. Results are presented within a `data` array.
                    See [Pagination](https://techdocs.akamai.com/linode-api/reference/pagination)
                    for more information.
                  x-akamai:
                    file-path: schemas/pagination-envelope.yaml
                  additionalProperties: false
                  properties:
                    page:
                      type: integer
                      description: |
                        __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    pages:
                      type: integer
                      description: |
                        __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    results:
                      type: integer
                      description: |
                        __Read-only__ The total number of results.
                      example: 1
                      readOnly: true
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        type: object
                        description: |
                          Managed Database engine object.
                        x-akamai:
                          file-path: schemas/database-engine.yaml
                        additionalProperties: false
                        properties:
                          id:
                            type: string
                            description: |
                              The Managed Database engine ID in engine/version format.
                            example: mysql/8.0.26
                            x-linode-cli-display: 1
                          version:
                            type: string
                            description: |
                              The Managed Database engine version.
                            example: 8.0.26
                            x-linode-cli-display: 3
                            x-linode-filterable: true
                          engine:
                            type: string
                            description: |
                              The Managed Database engine type.
                            example: mysql
                            x-linode-cli-display: 2
                            x-linode-filterable: true
                          total_disk_size_gb:
                            type: integer
                            description: |
                              The total disk size of the database in GB.
                            example: 15
                            x-linode-cli-display: 4
                          used_disk_size_gb:
                            type: integer
                            description: |
                              The used space of the database in GB.
                            example: 2
                            x-linode-cli-display: 5
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-databases-engines
      x-code-samples:
      - lang: Shell
        source: curl https://api.linode.com/v4/databases/engines/
      - lang: CLI
        source: linode-cli databases engines
      x-linode-cli-action: engines
      x-original-op-id: getDatabasesEngines
      x-original-op-title: Managed Database Engines
        List
    x-linode-cli-command: databases
  /databases/engines/{engineId}:
    x-akamai:
      file-path: paths/engine.yaml
      path-info: /databases/engines/{engineId}
    parameters:
    - name: engineId
      in: path
      description: |
        The ID of the Managed Database engine.
      example: '{{engineId}}'
      x-akamai:
        file-path: parameters/engine-id-path.yaml
      required: true
      schema:
        type: string
    get:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: get-databases-engine
      summary: Get a managed database engine
      tags:
      - Engines
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Display information for a single Managed Database engine type and version.
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns information for a single Managed Database engine type and version.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Managed Database engine object.
                x-akamai:
                  file-path: schemas/database-engine.yaml
                additionalProperties: false
                properties:
                  id:
                    type: string
                    description: |
                      The Managed Database engine ID in engine/version format.
                    example: mysql/8.0.26
                    x-linode-cli-display: 1
                  version:
                    type: string
                    description: |
                      The Managed Database engine version.
                    example: 8.0.26
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  engine:
                    type: string
                    description: |
                      The Managed Database engine type.
                    example: mysql
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  total_disk_size_gb:
                    type: integer
                    description: |
                      The total disk size of the database in GB.
                    example: 15
                    x-linode-cli-display: 4
                  used_disk_size_gb:
                    type: integer
                    description: |
                      The used space of the database in GB.
                    example: 2
                    x-linode-cli-display: 5
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-databases-engine
      x-code-samples:
      - lang: Shell
        source: curl https://api.linode.com/v4/databases/engines/mysql/5.7.30
      - lang: CLI
        source: linode-cli databases engine-view mysql/5.7.30
      x-linode-cli-action: engine-view
      x-original-op-id: getDatabasesEngine
      x-original-op-title: Managed Database Engine View
    x-linode-cli-command: databases
  /databases/instances:
    x-akamai:
      file-path: paths/database-instances.yaml
      path-info: /databases/instances
    get:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: get-databases-instances
      summary: List managed databases
      tags:
      - Databases
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Display all Managed Databases that are accessible by your User, regardless of engine type.

        For more detailed information on a particular Database instance, make a request to its `instance_uri`.


        ---


        - __OAuth__.

            ```
            databases:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: databases:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated list of all accessible Managed Databases on your Account.
          content:
            application/json:
              schema:
                allOf:
                - type: object
                  description: |
                    An envelope for paginated response. When accessing a collection through a GET endpoint,
                    the results are wrapped in this envelope which includes metadata
                    about those results. Results are presented within a `data` array.
                    See [Pagination](https://techdocs.akamai.com/linode-api/reference/pagination)
                    for more information.
                  x-akamai:
                    file-path: schemas/pagination-envelope.yaml
                  additionalProperties: false
                  properties:
                    page:
                      type: integer
                      description: |
                        __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    pages:
                      type: integer
                      description: |
                        __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    results:
                      type: integer
                      description: |
                        __Read-only__ The total number of results.
                      example: 1
                      readOnly: true
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        type: object
                        description: |
                          A general Managed Database instance object containing properties that are
                          identical for all database types.
                        x-akamai:
                          file-path: schemas/database.yaml
                        additionalProperties: false
                        properties:
                          id:
                            type: integer
                            description: |
                              __Read-only__ A unique ID that can be used to identify and reference
                              the Managed Database.
                            example: 123
                            readOnly: true
                            x-linode-cli-display: 1
                          type:
                            type: string
                            description: |
                              The Linode Instance type used by the Managed Database for its nodes.
                            example: g6-dedicated-2
                            x-linode-cli-display: 4
                            x-linode-filterable: true
                          version:
                            type: string
                            description: |
                              __Read-only__ The Managed Database engine version.
                            example: 8.0.26
                            readOnly: true
                            x-linode-cli-display: 7
                            x-linode-filterable: true
                          allow_list:
                            type: array
                            description: |-
                              A list of IP addresses that can access the Managed Database. Each item can be a single IP address or a range in CIDR format.

                              By default, this is an empty array (`[]`), which blocks all connections (both public and private) to the Managed Database.

                              If `0.0.0.0/0` is a value in this list, then all IP addresses can access the Managed Database.
                            example:
                            - 203.0.113.1/32
                            - 192.0.1.0/24
                            items:
                              type: string
                              pattern: ^([0-9]{1,3}\.){3}[0-9]{1,3}(\/([0-9]|[1-2][0-9]|3[0-2]))?$
                              format: ipv4/prefix_length
                          cluster_size:
                            type: integer
                            description: |-
                              The number of Linode Instance nodes deployed to the Managed Database.

                              Choosing 3 nodes creates a high availability cluster consisting of 1 primary node and 2 replica nodes.
                            example: 3
                            default: 1
                            enum:
                            - 1
                            - 3
                            x-linode-cli-display: 5
                          created:
                            type: string
                            description: |
                              __Read-only__ When this Managed Database was created.
                            example: '2022-01-01T00:01:01'
                            readOnly: true
                            format: date-time
                          encrypted:
                            type: boolean
                            description: |
                              Whether the Managed Databases is encrypted.
                            example: false
                            default: false
                          engine:
                            type: string
                            description: |
                              __Read-only__ The Managed Database engine type.
                            example: mysql
                            readOnly: true
                            enum:
                            - mysql
                            - postgresql
                            x-linode-cli-display: 6
                            x-linode-filterable: true
                          hosts:
                            type: object
                            description: |
                              __Read-only__ The primary and secondary hosts for the Managed Database.
                              These are assigned after provisioning is complete.
                            additionalProperties: false
                            readOnly: true
                            properties:
                              primary:
                                type: string
                                description: |
                                  The primary host for the Managed Database.
                                example: lin-123-456-mysql-mysql-primary.servers.linodedb.net
                                nullable: true
                              secondary:
                                type: string
                                description: |-
                                  The secondary/private network host for the Managed Database.

                                  A private network host and a private IP can only be used to access a Database Cluster from Linodes in the same data center and will not incur transfer costs.

                                  __Note__: The secondary hostname is publicly viewable and accessible.
                                example: lin-123-456-mysql-primary-private.servers.linodedb.net
                                nullable: true
                          instance_uri:
                            type: string
                            description: |
                              __Read-only__ Append this to `https://api.linode.com` to run commands
                              for the Managed Database.
                            example: /v4/databases/mysql/instances/123
                            readOnly: true
                          label:
                            type: string
                            description: |
                              A unique, user-defined string referring to the Managed Database.
                            example: example-db
                            minLength: 3
                            maxLength: 32
                            x-linode-cli-display: 2
                            x-linode-filterable: true
                          region:
                            type: string
                            description: |
                              The [Region](https://techdocs.akamai.com/linode-api/reference/get-regions) ID for the Managed Database.
                            example: us-east
                            x-linode-cli-display: 3
                            x-linode-filterable: true
                          status:
                            type: string
                            description: |
                              __Read-only__ The operating status of the Managed Database.
                            example: active
                            readOnly: true
                            enum:
                            - provisioning
                            - active
                            - suspending
                            - suspended
                            - resuming
                            - restoring
                            - failed
                            - degraded
                            - updating
                            - backing_up
                            x-linode-cli-color:
                              active: green
                              default_: white
                              degraded: red
                              failed: red
                              provisioning: yellow
                              restoring: yellow
                              resuming: yellow
                              updating: yellow
                            x-linode-cli-display: 100
                            x-linode-filterable: true
                          updated:
                            type: string
                            description: |
                              __Read-only__ When this Managed Database was last updated.
                            example: '2022-01-01T00:01:01'
                            readOnly: true
                            format: date-time
                          updates:
                            type: object
                            description: |
                              Configuration settings for automated patch update maintenance for
                              the Managed Database.
                            additionalProperties: false
                            properties:
                              day_of_week:
                                type: integer
                                description: |
                                  The day to perform maintenance. 1=Monday, 2=Tuesday, etc.
                                example: 1
                                minimum: 1
                                maximum: 7
                              duration:
                                type: integer
                                description: |
                                  The maximum maintenance window time in hours.
                                example: 3
                                minimum: 1
                                maximum: 3
                              frequency:
                                type: string
                                description: |
                                  Whether maintenance occurs on a weekly or monthly basis.
                                example: weekly
                                default: weekly
                                enum:
                                - weekly
                                - monthly
                              hour_of_day:
                                type: integer
                                description: |
                                  The hour to begin maintenance based in UTC time.
                                example: 0
                                minimum: 0
                                maximum: 23
                              week_of_month:
                                type: integer
                                description: |-
                                  The week of the month to perform `monthly` frequency updates. Defaults to `null`.

                                  - Required for `monthly` frequency updates.

                                  - Must be `null` for `weekly` frequency updates.
                                example: null
                                nullable: true
                                minimum: 1
                                maximum: 4
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-databases-instances
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/databases/instances
      - lang: CLI
        source: linode-cli databases list
      x-linode-cli-action:
      - list
      - ls
      x-linode-grant: read_only
      x-original-op-id: getDatabasesInstances
      x-original-op-title: Managed Databases List All
    x-linode-cli-command: databases
  /databases/mysql/instances:
    x-akamai:
      file-path: paths/mysql-instances.yaml
      path-info: /databases/mysql/instances
    post:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: post-databases-mysql-instances
      summary: Create a managed MySQL database
      tags:
      - Databases
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Provision a Managed MySQL Database.

        Restricted Users must have the `add_databases` grant to use this operation.

        New instances can take approximately 15 to 30 minutes to provision.

        The `allow_list` is used to control access to the Managed Database.

        - IP addresses and ranges in this list can access the Managed Database. All other sources are blocked.

        - If `0.0.0.0/0` is a value in this list, then all IP addresses can access the Managed Database.

        - Entering an empty array (`[]`) blocks all connections (both public and private) to the Managed Database.

        All Managed Databases include automatic, daily backups. Up to seven backups are automatically stored for each Managed Database, providing restore points for each day of the past week.

        All Managed Databases include automatic patch updates, which apply security patches and updates to the underlying operating system of the Managed MySQL Database during configurable maintenance windows.

        - If your database cluster is configured with a single node, you will experience downtime during this maintenance window when any updates occur. It's recommended that you adjust this window to match a time that will be the least disruptive for your application and users. You may also want to consider upgrading to a high availability plan to avoid any downtime due to maintenance.

        - __The database software is not updated automatically.__ To upgrade to a new database engine version, consider deploying a new Managed Database with your preferred version. You can then [migrate your databases](https://www.linode.com/docs/products/databases/managed-databases/guides/migrate-mysql/) from the original Managed Database cluster to the new one.

        - To modify update the maintenance window for a Database, run the [Update a managed MySQL database](https://techdocs.akamai.com/linode-api/reference/put-databases-mysql-instance) operation.


        ---


        - __CLI__.

            ```
            linode-cli databases mysql-create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases mysql-create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the Managed MySQL Database you are creating.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                Managed MySQL Database request object.
              x-akamai:
                file-path: schemas/database-mysql-request.yaml
              additionalProperties: false
              required:
              - label
              - type
              - engine
              - region
              properties:
                type:
                  type: string
                  description: |
                    The Linode Instance type used by the Managed Database for its nodes.
                  example: '{{type}}'
                  x-linode-cli-display: 4
                  x-linode-filterable: true
                allow_list:
                  type: array
                  description: |-
                    A list of IP addresses that can access the Managed Database. Each item can be a single IP address or a range in CIDR format.

                    By default, this is an empty array (`[]`), which blocks all connections (both public and private) to the Managed Database.

                    If `0.0.0.0/0` is a value in this list, then all IP addresses can access the Managed Database.
                  example:
                  - 203.0.113.1/32
                  - 192.0.1.0/24
                  items:
                    type: string
                    pattern: ^([0-9]{1,3}\.){3}[0-9]{1,3}(\/([0-9]|[1-2][0-9]|3[0-2]))?$
                    format: ipv4/prefix_length
                cluster_size:
                  type: integer
                  description: |-
                    The number of Linode Instance nodes deployed to the Managed Database.

                    Choosing 3 nodes creates a high availability cluster consisting of 1 primary node and 2 replica nodes.
                  example: '{{cluster_size}}'
                  default: 1
                  enum:
                  - 1
                  - 3
                  x-linode-cli-display: 7
                encrypted:
                  type: boolean
                  description: |
                    Whether the Managed Databases is encrypted.
                  example: '{{encrypted}}'
                  default: false
                engine:
                  type: string
                  description: |
                    The Managed Database engine in engine/version format.
                  example: '{{engine}}'
                label:
                  type: string
                  description: |
                    A unique, user-defined string referring to the Managed Database.
                  example: '{{label}}'
                  minLength: 3
                  maxLength: 32
                  x-linode-cli-display: 2
                  x-linode-filterable: true
                region:
                  type: string
                  description: |
                    The [Region](https://techdocs.akamai.com/linode-api/reference/get-regions) ID for the Managed Database.
                  example: '{{region}}'
                  x-linode-cli-display: 3
                  x-linode-filterable: true
                replication_type:
                  type: string
                  description: |-
                    The replication method used for the Managed Database.

                    Defaults to `none` for a single cluster and `semi_synch` for a high availability cluster.

                    Must be `none` for a single node cluster.

                    Must be `asynch` or `semi_synch` for a high availability cluster.
                  example: '{{replication_type}}'
                  enum:
                  - none
                  - asynch
                  - semi_synch
                  x-linode-cli-display: 99
                ssl_connection:
                  type: boolean
                  description: |-
                    Whether to require SSL credentials to establish a connection to the Managed Database.

                    Run the [Get managed MySQL database credentials](https://techdocs.akamai.com/linode-api/reference/get-databases-mysql-instance-credentials) operation for access information.
                  example: '{{ssl_connection}}'
                  default: true
            x-example:
              x-ref: ../examples/tbd.json
        x-linode-cli-allowed-defaults:
        - engine
        - region
        - type
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A new Managed MySQL Database is provisioning.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Managed MySQL Databases object.
                x-akamai:
                  file-path: schemas/database-mysql.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ A unique ID that can be used to identify and reference the Managed Database.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                  type:
                    type: string
                    description: |
                      The Linode Instance type used by the Managed Database for its nodes.
                    example: g6-dedicated-2
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  version:
                    type: string
                    description: |
                      __Read-only__ The Managed Database engine version.
                    example: 8.0.26
                    readOnly: true
                    x-linode-cli-display: 9
                    x-linode-filterable: true
                  allow_list:
                    type: array
                    description: |-
                      A list of IP addresses that can access the Managed Database. Each item can be a single IP address or a range in CIDR format.

                      By default, this is an empty array (`[]`), which blocks all connections (both public and private) to the Managed Database.

                      If `0.0.0.0/0` is a value in this list, then all IP addresses can access the Managed Database.
                    example:
                    - 203.0.113.1/32
                    - 192.0.1.0/24
                    items:
                      type: string
                      pattern: ^([0-9]{1,3}\.){3}[0-9]{1,3}(\/([0-9]|[1-2][0-9]|3[0-2]))?$
                      format: ipv4/prefix_length
                  cluster_size:
                    type: integer
                    description: |-
                      The number of Linode Instance nodes deployed to the Managed Database.

                      Choosing 3 nodes creates a high availability cluster consisting of 1 primary node and 2 replica nodes.
                    example: 3
                    default: 1
                    enum:
                    - 1
                    - 3
                    x-linode-cli-display: 7
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Managed Database was created.
                    example: '2022-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  encrypted:
                    type: boolean
                    description: |
                      Whether the Managed Databases is encrypted.
                    example: false
                    default: false
                  engine:
                    type: string
                    description: |
                      __Read-only__ The Managed Database engine type.
                    example: mysql
                    readOnly: true
                    x-linode-cli-display: 8
                    x-linode-filterable: true
                  hosts:
                    type: object
                    description: |
                      __Read-only__ The primary and secondary hosts for the Managed Database. These are assigned
                      after provisioning is complete.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      primary:
                        type: string
                        description: |
                          The primary host for the Managed Database.
                        example: lin-123-456-mysql-mysql-primary.servers.linodedb.net
                        nullable: true
                      secondary:
                        type: string
                        description: |-
                          The secondary/private network host for the Managed Database.

                          A private network host and a private IP can only be used to access a Database Cluster from Linodes in the same data center and will not incur transfer costs.

                          __Note__: The secondary hostname is publicly viewable and accessible.
                        example: lin-123-456-mysql-primary-private.servers.linodedb.net
                        nullable: true
                  label:
                    type: string
                    description: |
                      A unique, user-defined string referring to the Managed Database.
                    example: example-db
                    minLength: 3
                    maxLength: 32
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  port:
                    type: integer
                    description: |
                      The access port for this Managed Database.
                    example: 3306
                    x-linode-cli-display: 98
                  region:
                    type: string
                    description: |
                      The [Region](https://techdocs.akamai.com/linode-api/reference/get-regions) ID for the Managed Database.
                    example: us-east
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  replication_type:
                    type: string
                    description: |-
                      The replication method used for the Managed Database.

                      Defaults to `none` for a single cluster and `semi_synch` for a high availability cluster.

                      Must be `none` for a single node cluster.

                      Must be `asynch` or `semi_synch` for a high availability cluster.
                    example: semi_synch
                    enum:
                    - none
                    - asynch
                    - semi_synch
                    x-linode-cli-display: 99
                  ssl_connection:
                    type: boolean
                    description: |-
                      Whether to require SSL credentials to establish a connection to the Managed Database.

                      Run the [Get managed MySQL database credentials](https://techdocs.akamai.com/linode-api/reference/get-databases-mysql-instance-credentials) operation for access information.
                    example: true
                    default: true
                  status:
                    type: string
                    description: |
                      __Read-only__ The operating status of the Managed Database.
                    example: active
                    readOnly: true
                    enum:
                    - provisioning
                    - active
                    - suspending
                    - suspended
                    - resuming
                    - restoring
                    - failed
                    - degraded
                    - updating
                    - backing_up
                    x-linode-cli-color:
                      active: green
                      default_: white
                      degraded: red
                      failed: red
                      provisioning: yellow
                      restoring: yellow
                      resuming: yellow
                    x-linode-cli-display: 100
                    x-linode-filterable: true
                  total_disk_size_gb:
                    type: integer
                    description: |
                      The total disk size of the database in GB.
                    example: 15
                    x-linode-cli-display: 5
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Managed Database was last updated.
                    example: '2022-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  updates:
                    type: object
                    description: |
                      Configuration settings for automated patch update maintenance for the Managed Database.
                    additionalProperties: false
                    properties:
                      day_of_week:
                        type: integer
                        description: |
                          The day to perform maintenance. 1=Monday, 2=Tuesday, etc.
                        example: 1
                        minimum: 1
                        maximum: 7
                      duration:
                        type: integer
                        description: |
                          The maximum maintenance window time in hours.
                        example: 3
                        minimum: 1
                        maximum: 3
                      frequency:
                        type: string
                        description: |
                          Whether maintenance occurs on a weekly or monthly basis.
                        example: weekly
                        default: weekly
                        enum:
                        - weekly
                        - monthly
                      hour_of_day:
                        type: integer
                        description: |
                          The hour to begin maintenance based in UTC time.
                        example: 0
                        minimum: 0
                        maximum: 23
                      week_of_month:
                        type: integer
                        description: |-
                          The week of the month to perform `monthly` frequency updates. Defaults to `null`.

                          - Required for `monthly` frequency updates.

                          - Must be `null` for `weekly` frequency updates.
                        example: null
                        nullable: true
                        minimum: 1
                        maximum: 4
                  used_disk_size_gb:
                    type: integer
                    description: |
                      The used space of the database in GB.
                    example: 2
                    x-linode-cli-display: 6
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-databases-mysql-instances
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                  "label": "example-db",
                  "region": "us-east",
                  "type": "g6-dedicated-2",
                  "cluster_size": 3,
                  "engine": "mysql/8.0.26",
                  "encrypted": false,
                  "ssl_connection": true,
                  "replication_type": "semi_synch",
                  "allow_list": [
                    "203.0.113.1",
                    "192.0.1.0/24"
                  ]
              }' \
              https://api.linode.com/v4/databases/mysql/instances
      - lang: CLI
        source: |-
          linode-cli databases mysql-create \
            --label example-db1 \
            --region us-east \
            --type g6-dedicated-2 \
            --cluster_size 3 \
            --engine mysql/8.0.26 \
            --encrypted false \
            --ssl_connection false \
            --replication_type semi_synch \
            --allow_list 203.0.113.1 \
            --allow_list 192.0.1.0/24
      x-linode-cli-action: mysql-create
      x-linode-grant: add_databases
      x-original-op-id: postDatabasesMySQLInstances
      x-original-op-title: Managed MySQL Database Create
    get:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: get-databases-mysql-instances
      summary: List managed MySQL databases
      tags:
      - Databases
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Display all accessible Managed MySQL Databases.


        ---


        - __CLI__.

            ```
            linode-cli databases mysql-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases mysql-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated list of all accessible Managed MySQL Databases on your Account.
          content:
            application/json:
              schema:
                allOf:
                - type: object
                  description: |
                    An envelope for paginated response. When accessing a collection through a GET endpoint,
                    the results are wrapped in this envelope which includes metadata
                    about those results. Results are presented within a `data` array.
                    See [Pagination](https://techdocs.akamai.com/linode-api/reference/pagination)
                    for more information.
                  x-akamai:
                    file-path: schemas/pagination-envelope.yaml
                  additionalProperties: false
                  properties:
                    page:
                      type: integer
                      description: |
                        __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    pages:
                      type: integer
                      description: |
                        __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    results:
                      type: integer
                      description: |
                        __Read-only__ The total number of results.
                      example: 1
                      readOnly: true
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        type: object
                        description: |
                          Managed MySQL Databases object.
                        x-akamai:
                          file-path: schemas/database-mysql.yaml
                        additionalProperties: false
                        properties:
                          id:
                            type: integer
                            description: |
                              __Read-only__ A unique ID that can be used to identify and reference
                              the Managed Database.
                            example: 123
                            readOnly: true
                            x-linode-cli-display: 1
                          type:
                            type: string
                            description: |
                              The Linode Instance type used by the Managed Database for its nodes.
                            example: g6-dedicated-2
                            x-linode-cli-display: 4
                            x-linode-filterable: true
                          version:
                            type: string
                            description: |
                              __Read-only__ The Managed Database engine version.
                            example: 8.0.26
                            readOnly: true
                            x-linode-cli-display: 9
                            x-linode-filterable: true
                          allow_list:
                            type: array
                            description: |-
                              A list of IP addresses that can access the Managed Database. Each item can be a single IP address or a range in CIDR format.

                              By default, this is an empty array (`[]`), which blocks all connections (both public and private) to the Managed Database.

                              If `0.0.0.0/0` is a value in this list, then all IP addresses can access the Managed Database.
                            example:
                            - 203.0.113.1/32
                            - 192.0.1.0/24
                            items:
                              type: string
                              pattern: ^([0-9]{1,3}\.){3}[0-9]{1,3}(\/([0-9]|[1-2][0-9]|3[0-2]))?$
                              format: ipv4/prefix_length
                          cluster_size:
                            type: integer
                            description: |-
                              The number of Linode Instance nodes deployed to the Managed Database.

                              Choosing 3 nodes creates a high availability cluster consisting of 1 primary node and 2 replica nodes.
                            example: 3
                            default: 1
                            enum:
                            - 1
                            - 3
                            x-linode-cli-display: 7
                          created:
                            type: string
                            description: |
                              __Read-only__ When this Managed Database was created.
                            example: '2022-01-01T00:01:01'
                            readOnly: true
                            format: date-time
                          encrypted:
                            type: boolean
                            description: |
                              Whether the Managed Databases is encrypted.
                            example: false
                            default: false
                          engine:
                            type: string
                            description: |
                              __Read-only__ The Managed Database engine type.
                            example: mysql
                            readOnly: true
                            x-linode-cli-display: 8
                            x-linode-filterable: true
                          hosts:
                            type: object
                            description: |
                              __Read-only__ The primary and secondary hosts for the Managed Database.
                              These are assigned after provisioning is complete.
                            additionalProperties: false
                            readOnly: true
                            properties:
                              primary:
                                type: string
                                description: |
                                  The primary host for the Managed Database.
                                example: lin-123-456-mysql-mysql-primary.servers.linodedb.net
                                nullable: true
                              secondary:
                                type: string
                                description: |-
                                  The secondary/private network host for the Managed Database.

                                  A private network host and a private IP can only be used to access a Database Cluster from Linodes in the same data center and will not incur transfer costs.

                                  __Note__: The secondary hostname is publicly viewable and accessible.
                                example: lin-123-456-mysql-primary-private.servers.linodedb.net
                                nullable: true
                          label:
                            type: string
                            description: |
                              A unique, user-defined string referring to the Managed Database.
                            example: example-db
                            minLength: 3
                            maxLength: 32
                            x-linode-cli-display: 2
                            x-linode-filterable: true
                          port:
                            type: integer
                            description: |
                              The access port for this Managed Database.
                            example: 3306
                            x-linode-cli-display: 98
                          region:
                            type: string
                            description: |
                              The [Region](https://techdocs.akamai.com/linode-api/reference/get-regions) ID for the Managed Database.
                            example: us-east
                            x-linode-cli-display: 3
                            x-linode-filterable: true
                          replication_type:
                            type: string
                            description: |-
                              The replication method used for the Managed Database.

                              Defaults to `none` for a single cluster and `semi_synch` for a high availability cluster.

                              Must be `none` for a single node cluster.

                              Must be `asynch` or `semi_synch` for a high availability cluster.
                            example: semi_synch
                            enum:
                            - none
                            - asynch
                            - semi_synch
                            x-linode-cli-display: 99
                          ssl_connection:
                            type: boolean
                            description: |-
                              Whether to require SSL credentials to establish a connection to the Managed Database.

                              Run the [Get managed MySQL database credentials](https://techdocs.akamai.com/linode-api/reference/get-databases-mysql-instance-credentials) operation for access information.
                            example: true
                            default: true
                          status:
                            type: string
                            description: |
                              __Read-only__ The operating status of the Managed Database.
                            example: active
                            readOnly: true
                            enum:
                            - provisioning
                            - active
                            - suspending
                            - suspended
                            - resuming
                            - restoring
                            - failed
                            - degraded
                            - updating
                            - backing_up
                            x-linode-cli-color:
                              active: green
                              default_: white
                              degraded: red
                              failed: red
                              provisioning: yellow
                              restoring: yellow
                              resuming: yellow
                            x-linode-cli-display: 100
                            x-linode-filterable: true
                          total_disk_size_gb:
                            type: integer
                            description: |
                              The total disk size of the database in GB.
                            example: 15
                            x-linode-cli-display: 5
                          updated:
                            type: string
                            description: |
                              __Read-only__ When this Managed Database was last updated.
                            example: '2022-01-01T00:01:01'
                            readOnly: true
                            format: date-time
                          updates:
                            type: object
                            description: |
                              Configuration settings for automated patch update maintenance for
                              the Managed Database.
                            additionalProperties: false
                            properties:
                              day_of_week:
                                type: integer
                                description: |
                                  The day to perform maintenance. 1=Monday, 2=Tuesday, etc.
                                example: 1
                                minimum: 1
                                maximum: 7
                              duration:
                                type: integer
                                description: |
                                  The maximum maintenance window time in hours.
                                example: 3
                                minimum: 1
                                maximum: 3
                              frequency:
                                type: string
                                description: |
                                  Whether maintenance occurs on a weekly or monthly basis.
                                example: weekly
                                default: weekly
                                enum:
                                - weekly
                                - monthly
                              hour_of_day:
                                type: integer
                                description: |
                                  The hour to begin maintenance based in UTC time.
                                example: 0
                                minimum: 0
                                maximum: 23
                              week_of_month:
                                type: integer
                                description: |-
                                  The week of the month to perform `monthly` frequency updates. Defaults to `null`.

                                  - Required for `monthly` frequency updates.

                                  - Must be `null` for `weekly` frequency updates.
                                example: null
                                nullable: true
                                minimum: 1
                                maximum: 4
                          used_disk_size_gb:
                            type: integer
                            description: |
                              The used space of the database in GB.
                            example: 2
                            x-linode-cli-display: 6
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-databases-mysql-instances
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/databases/mysql/instances/
      - lang: CLI
        source: linode-cli databases mysql-list
      x-linode-cli-action: mysql-list
      x-linode-grant: read_only
      x-original-op-id: getDatabasesMySQLInstances
      x-original-op-title: Managed MySQL Databases List
    x-linode-cli-command: databases
  /databases/mysql/instances/{instanceId}:
    x-akamai:
      file-path: paths/mysql-instance.yaml
      path-info: /databases/mysql/instances/{instanceId}
    parameters:
    - name: instanceId
      in: path
      description: |
        The ID of the Managed PostgreSQL Database.
      example: '{{instanceId}}'
      x-akamai:
        file-path: parameters/instance-id-path.yaml
      required: true
      schema:
        type: integer
    get:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: get-databases-mysql-instance
      summary: Get a managed MySQL database
      tags:
      - Databases
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Display information for a single, accessible Managed MySQL Database.


        ---


        - __CLI__.

            ```
            linode-cli databases mysql-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases mysql-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns information for a single Managed MySQL Database.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Managed MySQL Databases object.
                x-akamai:
                  file-path: schemas/database-mysql.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ A unique ID that can be used to identify and reference the Managed Database.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                  type:
                    type: string
                    description: |
                      The Linode Instance type used by the Managed Database for its nodes.
                    example: g6-dedicated-2
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  version:
                    type: string
                    description: |
                      __Read-only__ The Managed Database engine version.
                    example: 8.0.26
                    readOnly: true
                    x-linode-cli-display: 9
                    x-linode-filterable: true
                  allow_list:
                    type: array
                    description: |-
                      A list of IP addresses that can access the Managed Database. Each item can be a single IP address or a range in CIDR format.

                      By default, this is an empty array (`[]`), which blocks all connections (both public and private) to the Managed Database.

                      If `0.0.0.0/0` is a value in this list, then all IP addresses can access the Managed Database.
                    example:
                    - 203.0.113.1/32
                    - 192.0.1.0/24
                    items:
                      type: string
                      pattern: ^([0-9]{1,3}\.){3}[0-9]{1,3}(\/([0-9]|[1-2][0-9]|3[0-2]))?$
                      format: ipv4/prefix_length
                  cluster_size:
                    type: integer
                    description: |-
                      The number of Linode Instance nodes deployed to the Managed Database.

                      Choosing 3 nodes creates a high availability cluster consisting of 1 primary node and 2 replica nodes.
                    example: 3
                    default: 1
                    enum:
                    - 1
                    - 3
                    x-linode-cli-display: 7
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Managed Database was created.
                    example: '2022-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  encrypted:
                    type: boolean
                    description: |
                      Whether the Managed Databases is encrypted.
                    example: false
                    default: false
                  engine:
                    type: string
                    description: |
                      __Read-only__ The Managed Database engine type.
                    example: mysql
                    readOnly: true
                    x-linode-cli-display: 8
                    x-linode-filterable: true
                  hosts:
                    type: object
                    description: |
                      __Read-only__ The primary and secondary hosts for the Managed Database. These are assigned
                      after provisioning is complete.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      primary:
                        type: string
                        description: |
                          The primary host for the Managed Database.
                        example: lin-123-456-mysql-mysql-primary.servers.linodedb.net
                        nullable: true
                      secondary:
                        type: string
                        description: |-
                          The secondary/private network host for the Managed Database.

                          A private network host and a private IP can only be used to access a Database Cluster from Linodes in the same data center and will not incur transfer costs.

                          __Note__: The secondary hostname is publicly viewable and accessible.
                        example: lin-123-456-mysql-primary-private.servers.linodedb.net
                        nullable: true
                  label:
                    type: string
                    description: |
                      A unique, user-defined string referring to the Managed Database.
                    example: example-db
                    minLength: 3
                    maxLength: 32
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  port:
                    type: integer
                    description: |
                      The access port for this Managed Database.
                    example: 3306
                    x-linode-cli-display: 98
                  region:
                    type: string
                    description: |
                      The [Region](https://techdocs.akamai.com/linode-api/reference/get-regions) ID for the Managed Database.
                    example: us-east
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  replication_type:
                    type: string
                    description: |-
                      The replication method used for the Managed Database.

                      Defaults to `none` for a single cluster and `semi_synch` for a high availability cluster.

                      Must be `none` for a single node cluster.

                      Must be `asynch` or `semi_synch` for a high availability cluster.
                    example: semi_synch
                    enum:
                    - none
                    - asynch
                    - semi_synch
                    x-linode-cli-display: 99
                  ssl_connection:
                    type: boolean
                    description: |-
                      Whether to require SSL credentials to establish a connection to the Managed Database.

                      Run the [Get managed MySQL database credentials](https://techdocs.akamai.com/linode-api/reference/get-databases-mysql-instance-credentials) operation for access information.
                    example: true
                    default: true
                  status:
                    type: string
                    description: |
                      __Read-only__ The operating status of the Managed Database.
                    example: active
                    readOnly: true
                    enum:
                    - provisioning
                    - active
                    - suspending
                    - suspended
                    - resuming
                    - restoring
                    - failed
                    - degraded
                    - updating
                    - backing_up
                    x-linode-cli-color:
                      active: green
                      default_: white
                      degraded: red
                      failed: red
                      provisioning: yellow
                      restoring: yellow
                      resuming: yellow
                    x-linode-cli-display: 100
                    x-linode-filterable: true
                  total_disk_size_gb:
                    type: integer
                    description: |
                      The total disk size of the database in GB.
                    example: 15
                    x-linode-cli-display: 5
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Managed Database was last updated.
                    example: '2022-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  updates:
                    type: object
                    description: |
                      Configuration settings for automated patch update maintenance for the Managed Database.
                    additionalProperties: false
                    properties:
                      day_of_week:
                        type: integer
                        description: |
                          The day to perform maintenance. 1=Monday, 2=Tuesday, etc.
                        example: 1
                        minimum: 1
                        maximum: 7
                      duration:
                        type: integer
                        description: |
                          The maximum maintenance window time in hours.
                        example: 3
                        minimum: 1
                        maximum: 3
                      frequency:
                        type: string
                        description: |
                          Whether maintenance occurs on a weekly or monthly basis.
                        example: weekly
                        default: weekly
                        enum:
                        - weekly
                        - monthly
                      hour_of_day:
                        type: integer
                        description: |
                          The hour to begin maintenance based in UTC time.
                        example: 0
                        minimum: 0
                        maximum: 23
                      week_of_month:
                        type: integer
                        description: |-
                          The week of the month to perform `monthly` frequency updates. Defaults to `null`.

                          - Required for `monthly` frequency updates.

                          - Must be `null` for `weekly` frequency updates.
                        example: null
                        nullable: true
                        minimum: 1
                        maximum: 4
                  used_disk_size_gb:
                    type: integer
                    description: |
                      The used space of the database in GB.
                    example: 2
                    x-linode-cli-display: 6
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-databases-mysql-instance
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/databases/mysql/instances/123
      - lang: CLI
        source: linode-cli databases mysql-view 123
      x-linode-cli-action: mysql-view
      x-linode-grant: read_only
      x-original-op-id: getDatabasesMySQLInstance
      x-original-op-title: Managed MySQL Database View
    put:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: put-databases-mysql-instance
      summary: Update a managed MySQL database
      tags:
      - Databases
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Update a Managed MySQL Database.

        Requires `read_write` access to the Database.

        The Database must have an `active` status to perform this operation.

        Updating addresses in the `allow_list` overwrites any existing addresses.

        - IP addresses and ranges in this list can access the Managed Database. All other sources are blocked.

        - If `0.0.0.0/0` is a value in this list, then all IP addresses can access the Managed Database.

        - Entering an empty array (`[]`) blocks all connections (both public and private) to the Managed Database.

        - __Note__. Updates to the `allow_list` may take a short period of time to complete, making this operation inappropriate for rapid successive updates to this property.

        All Managed Databases include automatic patch updates, which apply security patches and updates to the underlying operating system of the Managed MySQL Database. The maintenance window for these updates is configured with the Managed Database's `updates` property.

        - If your database cluster is configured with a single node, you will experience downtime during this maintenance window when any updates occur. It's recommended that you adjust this window to match a time that will be the least disruptive for your application and users. You may also want to consider upgrading to a high availability plan to avoid any downtime due to maintenance.

        - __The database software is not updated automatically.__ To upgrade to a new database engine version, consider deploying a new Managed Database with your preferred version. You can then [migrate your databases](https://www.linode.com/docs/products/databases/managed-databases/guides/migrate-mysql/) from the original Managed Database cluster to the new one.


        ---


        - __CLI__.

            ```
            linode-cli databases mysql-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases mysql-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Updated information for the Managed MySQL Database.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                Updated information for the Managed MySQL Database.
              additionalProperties: false
              properties:
                type:
                  type: string
                  description: |-
                    Request re-sizing of your cluster to a Linode Type with more disk space. For example, you could request a Linode Type that uses a higher plan.

                    - Needs to be a Linode Type with more disk space than your current Linode.

                    - Resizing to a larger Linode Type can accrue additional cost. Review the `price` output in the [List types](https://techdocs.akamai.com/linode-api/reference/get-linode-types) operation for more information.

                    - You can't update the `allow_list` and set a new `type` in the same request.

                    - Any active updates to your cluster need to complete before you can request a resize. The reverse is also true: An active resizing needs to complete before you can perform any other update.
                  example: '{{type}}'
                allow_list:
                  type: array
                  description: |-
                    A list of IP addresses that can access the Managed Database. Each item can be a single IP address or a range in CIDR format.

                    By default, this is an empty array (`[]`), which blocks all connections (both public and private) to the Managed Database.

                    If `0.0.0.0/0` is a value in this list, then all IP addresses can access the Managed Database.
                  example:
                  - 203.0.113.1/32
                  - 192.0.1.0/24
                  items:
                    type: string
                    pattern: ^([0-9]{1,3}\.){3}[0-9]{1,3}(\/([0-9]|[1-2][0-9]|3[0-2]))?$
                    format: ipv4/prefix_length
                label:
                  type: string
                  description: |
                    A unique, user-defined string referring to the Managed Database.
                  example: '{{label}}'
                  minLength: 3
                  maxLength: 32
                  x-linode-cli-display: 2
                  x-linode-filterable: true
                updates:
                  type: object
                  description: |
                    Configuration settings for automated patch update maintenance for the Managed Database.
                  additionalProperties: false
                  properties:
                    day_of_week:
                      type: integer
                      description: |
                        The day to perform maintenance. 1=Monday, 2=Tuesday, etc.
                      example: 1
                      minimum: 1
                      maximum: 7
                    duration:
                      type: integer
                      description: |
                        The maximum maintenance window time in hours.
                      example: 3
                      minimum: 1
                      maximum: 3
                    frequency:
                      type: string
                      description: |
                        Whether maintenance occurs on a weekly or monthly basis.
                      example: weekly
                      default: weekly
                      enum:
                      - weekly
                      - monthly
                    hour_of_day:
                      type: integer
                      description: |
                        The hour to begin maintenance based in UTC time.
                      example: 0
                      minimum: 0
                      maximum: 23
                    week_of_month:
                      type: integer
                      description: |-
                        The week of the month to perform `monthly` frequency updates. Defaults to `null`.

                        - Required for `monthly` frequency updates.

                        - Must be `null` for `weekly` frequency updates.
                      example: null
                      nullable: true
                      minimum: 1
                      maximum: 4
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Managed Database updated successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Managed MySQL Databases object.
                x-akamai:
                  file-path: schemas/database-mysql.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ A unique ID that can be used to identify and reference the Managed Database.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                  type:
                    type: string
                    description: |
                      The Linode Instance type used by the Managed Database for its nodes.
                    example: g6-dedicated-2
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  version:
                    type: string
                    description: |
                      __Read-only__ The Managed Database engine version.
                    example: 8.0.26
                    readOnly: true
                    x-linode-cli-display: 9
                    x-linode-filterable: true
                  allow_list:
                    type: array
                    description: |-
                      A list of IP addresses that can access the Managed Database. Each item can be a single IP address or a range in CIDR format.

                      By default, this is an empty array (`[]`), which blocks all connections (both public and private) to the Managed Database.

                      If `0.0.0.0/0` is a value in this list, then all IP addresses can access the Managed Database.
                    example:
                    - 203.0.113.1/32
                    - 192.0.1.0/24
                    items:
                      type: string
                      pattern: ^([0-9]{1,3}\.){3}[0-9]{1,3}(\/([0-9]|[1-2][0-9]|3[0-2]))?$
                      format: ipv4/prefix_length
                  cluster_size:
                    type: integer
                    description: |-
                      The number of Linode Instance nodes deployed to the Managed Database.

                      Choosing 3 nodes creates a high availability cluster consisting of 1 primary node and 2 replica nodes.
                    example: 3
                    default: 1
                    enum:
                    - 1
                    - 3
                    x-linode-cli-display: 7
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Managed Database was created.
                    example: '2022-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  encrypted:
                    type: boolean
                    description: |
                      Whether the Managed Databases is encrypted.
                    example: false
                    default: false
                  engine:
                    type: string
                    description: |
                      __Read-only__ The Managed Database engine type.
                    example: mysql
                    readOnly: true
                    x-linode-cli-display: 8
                    x-linode-filterable: true
                  hosts:
                    type: object
                    description: |
                      __Read-only__ The primary and secondary hosts for the Managed Database. These are assigned
                      after provisioning is complete.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      primary:
                        type: string
                        description: |
                          The primary host for the Managed Database.
                        example: lin-123-456-mysql-mysql-primary.servers.linodedb.net
                        nullable: true
                      secondary:
                        type: string
                        description: |-
                          The secondary/private network host for the Managed Database.

                          A private network host and a private IP can only be used to access a Database Cluster from Linodes in the same data center and will not incur transfer costs.

                          __Note__: The secondary hostname is publicly viewable and accessible.
                        example: lin-123-456-mysql-primary-private.servers.linodedb.net
                        nullable: true
                  label:
                    type: string
                    description: |
                      A unique, user-defined string referring to the Managed Database.
                    example: example-db
                    minLength: 3
                    maxLength: 32
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  port:
                    type: integer
                    description: |
                      The access port for this Managed Database.
                    example: 3306
                    x-linode-cli-display: 98
                  region:
                    type: string
                    description: |
                      The [Region](https://techdocs.akamai.com/linode-api/reference/get-regions) ID for the Managed Database.
                    example: us-east
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  replication_type:
                    type: string
                    description: |-
                      The replication method used for the Managed Database.

                      Defaults to `none` for a single cluster and `semi_synch` for a high availability cluster.

                      Must be `none` for a single node cluster.

                      Must be `asynch` or `semi_synch` for a high availability cluster.
                    example: semi_synch
                    enum:
                    - none
                    - asynch
                    - semi_synch
                    x-linode-cli-display: 99
                  ssl_connection:
                    type: boolean
                    description: |-
                      Whether to require SSL credentials to establish a connection to the Managed Database.

                      Run the [Get managed MySQL database credentials](https://techdocs.akamai.com/linode-api/reference/get-databases-mysql-instance-credentials) operation for access information.
                    example: true
                    default: true
                  status:
                    type: string
                    description: |
                      __Read-only__ The operating status of the Managed Database.
                    example: active
                    readOnly: true
                    enum:
                    - provisioning
                    - active
                    - suspending
                    - suspended
                    - resuming
                    - restoring
                    - failed
                    - degraded
                    - updating
                    - backing_up
                    x-linode-cli-color:
                      active: green
                      default_: white
                      degraded: red
                      failed: red
                      provisioning: yellow
                      restoring: yellow
                      resuming: yellow
                    x-linode-cli-display: 100
                    x-linode-filterable: true
                  total_disk_size_gb:
                    type: integer
                    description: |
                      The total disk size of the database in GB.
                    example: 15
                    x-linode-cli-display: 5
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Managed Database was last updated.
                    example: '2022-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  updates:
                    type: object
                    description: |
                      Configuration settings for automated patch update maintenance for the Managed Database.
                    additionalProperties: false
                    properties:
                      day_of_week:
                        type: integer
                        description: |
                          The day to perform maintenance. 1=Monday, 2=Tuesday, etc.
                        example: 1
                        minimum: 1
                        maximum: 7
                      duration:
                        type: integer
                        description: |
                          The maximum maintenance window time in hours.
                        example: 3
                        minimum: 1
                        maximum: 3
                      frequency:
                        type: string
                        description: |
                          Whether maintenance occurs on a weekly or monthly basis.
                        example: weekly
                        default: weekly
                        enum:
                        - weekly
                        - monthly
                      hour_of_day:
                        type: integer
                        description: |
                          The hour to begin maintenance based in UTC time.
                        example: 0
                        minimum: 0
                        maximum: 23
                      week_of_month:
                        type: integer
                        description: |-
                          The week of the month to perform `monthly` frequency updates. Defaults to `null`.

                          - Required for `monthly` frequency updates.

                          - Must be `null` for `weekly` frequency updates.
                        example: null
                        nullable: true
                        minimum: 1
                        maximum: 4
                  used_disk_size_gb:
                    type: integer
                    description: |
                      The used space of the database in GB.
                    example: 2
                    x-linode-cli-display: 6
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-databases-mysql-instance
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                  "label": "example-db",
                  "allow_list": [
                    "203.0.113.1",
                    "192.0.1.0/24"
                  ],
                  "type": "g6-standard-1",
                  "updates": {
                    "frequency": "monthly",
                    "duration": 3,
                    "hour_of_day": 12,
                    "day_of_week": 4,
                    "week_of_month": 3
                  }
              }' \
              https://api.linode.com/v4/databases/mysql/instances/123
      - lang: CLI
        source: |-
          linode-cli databases mysql-update 123 \
            --label example-db \
            --allow_list 203.0.113.1 \
            --allow_list 192.0.1.0/24 \
            --type g6-standard-1 \
            --updates.frequency monthly \
            --updates.duration 3 \
            --updates.hour_of_day 12 \
            --updates.day_of_week 4 \
            --updates.week_of_month 3
      x-linode-cli-action: mysql-update
      x-linode-grant: read_write
      x-original-op-id: putDatabasesMySQLInstance
      x-original-op-title: Managed MySQL Database Update
    delete:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: delete-databases-mysql-instance
      summary: Delete a managed MySQL database
      tags:
      - Databases
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Remove a Managed MySQL Database from your Account.

        Requires `read_write` access to the Database.

        The Database must have an `active`, `failed`, or `degraded` status to perform this operation.

        Only unrestricted Users can access this operation, and have access regardless of the acting token's OAuth scopes.


        ---


        - __CLI__.

            ```
            linode-cli databases mysql-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases mysql-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Managed MySQL Database successfully deleted.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-databases-mysql-instance
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/databases/mysql/instances/123
      - lang: CLI
        source: linode-cli databases mysql-delete 123
      x-linode-cli-action: mysql-delete
      x-linode-grant: read_write
      x-original-op-id: deleteDatabasesMySQLInstance
      x-original-op-title: Managed MySQL Database Delete
    x-linode-cli-command: databases
  /databases/mysql/instances/{instanceId}/backups:
    x-akamai:
      file-path: paths/mysql-backups.yaml
      path-info: /databases/mysql/instances/{instanceId}/backups
    parameters:
    - name: instanceId
      in: path
      description: |
        The ID of the Managed PostgreSQL Database.
      example: '{{instanceId}}'
      x-akamai:
        file-path: parameters/instance-id-path.yaml
      required: true
      schema:
        type: integer
    post:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: post-databases-mysql-instance-backup
      summary: Create a managed MySQL database backup snapshot
      tags:
      - Backups
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Creates a snapshot backup of a Managed MySQL Database.

        Requires `read_write` access to the Database.

        Up to 3 snapshot backups for each Database can be stored at a time. If 3 snapshots have been created for a Database, one must be deleted before another can be made.

        Backups generated by this operation have the type `snapshot`. Snapshot backups may take several minutes to complete, after which they will be accessible to view or restore.

        The Database must have an `active` status to perform this operation. If another backup is in progress, it must complete before a new backup can be initiated.


        ---


        - __CLI__.

            ```
            linode-cli databases mysql-backup-snapshot
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases mysql-backup-snapshot
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the snapshot backup to create.
        content:
          application/json:
            schema:
              type: object
              description: |
                Managed Database request object for snapshot backup.
              x-akamai:
                file-path: schemas/database-backup-snapshot.yaml
              additionalProperties: false
              required:
              - label
              properties:
                label:
                  type: string
                  description: |-
                    The label for the Database snapshot backup.

                    - Can only contain ASCII letters, numbers, and underscores (`_`).
                    - Must be unique among other backup labels for this Database.
                  example: '{{label}}'
                  minLength: 1
                  maxLength: 30
                target:
                  type: string
                  description: |
                    The Database cluster target. If the Database is a high availability cluster, choosing
                    `secondary` creates a snapshot backup of a replica node.
                  example: '{{target}}'
                  default: primary
                  enum:
                  - primary
                  - secondary
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Database snapshot backup request successful.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-databases-mysql-instance-backup
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -H "Content-Type: application/json" \
              -X POST -d '{
                  "label": "snapshot1",
                  "target": "primary"
              }' \
              https://api.linode.com/v4/databases/mysql/instances/123/backups/
      - lang: CLI
        source: |-
          linode-cli databases mysql-backup-snapshot 123 \
            --label snapshot1 \
            --target primary
      x-linode-cli-action: mysql-backup-snapshot
      x-linode-grant: read_write
      x-original-op-id: postDatabasesMySQLInstanceBackup
      x-original-op-title: Managed MySQL Database Backup
        Snapshot Create
    get:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: get-databases-mysql-instance-backups
      summary: List managed MySQL database backups
      tags:
      - Backups
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Display all backups for an accessible Managed MySQL Database.

        The Database must not be provisioning to perform this operation.

        Database `auto` type backups are created every 24 hours at 0:00 UTC. Each `auto` backup is retained for 7 days.

        Database `snapshot` type backups are created by accessing the [Create a managed MySQL database backup snapshot](https://techdocs.akamai.com/linode-api/reference/post-databases-mysql-instance-backup) operation.


        ---


        - __CLI__.

            ```
            linode-cli databases mysql-backups-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases mysql-backups-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated list of backups for the Managed MySQL Database.
          content:
            application/json:
              schema:
                allOf:
                - type: object
                  description: |
                    An envelope for paginated response. When accessing a collection through a GET endpoint,
                    the results are wrapped in this envelope which includes metadata
                    about those results. Results are presented within a `data` array.
                    See [Pagination](https://techdocs.akamai.com/linode-api/reference/pagination)
                    for more information.
                  x-akamai:
                    file-path: schemas/pagination-envelope.yaml
                  additionalProperties: false
                  properties:
                    page:
                      type: integer
                      description: |
                        __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    pages:
                      type: integer
                      description: |
                        __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    results:
                      type: integer
                      description: |
                        __Read-only__ The total number of results.
                      example: 1
                      readOnly: true
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        type: object
                        description: |
                          A database backup object.
                        x-akamai:
                          file-path: schemas/database-backup.yaml
                        additionalProperties: false
                        properties:
                          id:
                            type: integer
                            description: |
                              The ID of the database backup object.
                            example: 123
                            x-linode-cli-display: 1
                          type:
                            type: string
                            description: |
                              The type of database backup, determined by how the backup was created.
                            example: auto
                            enum:
                            - snapshot
                            - auto
                            x-linode-cli-display: 3
                            x-linode-filterable: true
                          created:
                            type: string
                            description: |
                              A time value given in a combined date and time format that represents
                              when the database backup was created.
                            example: '2022-01-01T00:01:01'
                            format: date-time
                            x-linode-cli-display: 4
                            x-linode-filterable: true
                          label:
                            type: string
                            description: |-
                              The database backup's label, for display purposes only.

                              Must include only ASCII letters or numbers.
                            example: Scheduled - 02/04/22 11:11 UTC-XcCRmI
                            maxLength: 30
                            x-linode-cli-display: 2
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-databases-mysql-instance-backups
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/databases/mysql/instances/123/backups
      - lang: CLI
        source: linode-cli databases mysql-backups-list 123
      x-linode-cli-action: mysql-backups-list
      x-linode-grant: read_only
      x-original-op-id: getDatabasesMySQLInstanceBackups
      x-original-op-title: Managed MySQL Database Backups
        List
    x-linode-cli-command: databases
  /databases/mysql/instances/{instanceId}/backups/{backupId}:
    x-akamai:
      file-path: paths/mysql-backup.yaml
      path-info: /databases/mysql/instances/{instanceId}/backups/{backupId}
    parameters:
    - name: instanceId
      in: path
      description: |
        The ID of the Managed PostgreSQL Database.
      example: '{{instanceId}}'
      x-akamai:
        file-path: parameters/instance-id-path.yaml
      required: true
      schema:
        type: integer
    - name: backupId
      in: path
      description: |
        The ID of the Managed MySQL Database backup.
      example: '{{backupId}}'
      x-akamai:
        file-path: parameters/backup-id-path-591248be.yaml
      required: true
      schema:
        type: integer
    get:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: get-databases-mysql-instance-backup
      summary: Get a managed MySQL database backup
      tags:
      - Backups
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Display information for a single backup for an accessible Managed MySQL Database.

        The Database must not be provisioning to perform this operation.


        ---


        - __CLI__.

            ```
            linode-cli databases mysql-backup-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases mysql-backup-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a single backup for the Managed MySQL Database.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A database backup object.
                x-akamai:
                  file-path: schemas/database-backup.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      The ID of the database backup object.
                    example: 123
                    x-linode-cli-display: 1
                  type:
                    type: string
                    description: |
                      The type of database backup, determined by how the backup was created.
                    example: auto
                    enum:
                    - snapshot
                    - auto
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      A time value given in a combined date and time format that represents when the
                      database backup was created.
                    example: '2022-01-01T00:01:01'
                    format: date-time
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  label:
                    type: string
                    description: |-
                      The database backup's label, for display purposes only.

                      Must include only ASCII letters or numbers.
                    example: Scheduled - 02/04/22 11:11 UTC-XcCRmI
                    maxLength: 30
                    x-linode-cli-display: 2
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-databases-mysql-instance-backup
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/databases/mysql/instances/123/backups/456
      - lang: CLI
        source: linode-cli databases mysql-backup-view 123 456
      x-linode-cli-action: mysql-backup-view
      x-linode-grant: read_only
      x-original-op-id: getDatabasesMySQLInstanceBackup
      x-original-op-title: Managed MySQL Database Backup
        View
    delete:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: delete-database-mysql-instance-backup
      summary: Delete a managed MySQL database backup
      tags:
      - Backups
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Delete a single backup for an accessible Managed MySQL Database.

        Requires `read_write` access to the Database.

        The Database must not be provisioning to perform this operation.


        ---


        - __CLI__.

            ```
            linode-cli databases mysql-backup-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases mysql-backup-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Request to delete Database backup successful.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-database-mysql-instance-backup
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/databases/mysql/instances/123/backups/456
      - lang: CLI
        source: linode-cli databases mysql-backup-delete 123 456
      x-linode-cli-action: mysql-backup-delete
      x-original-op-id: deleteDatabaseMySQLInstanceBackup
      x-original-op-title: Managed MySQL Database Backup
        Delete
    x-linode-cli-command: databases
  /databases/mysql/instances/{instanceId}/backups/{backupId}/restore:
    x-akamai:
      file-path: paths/mysql-restore.yaml
      path-info: /databases/mysql/instances/{instanceId}/backups/{backupId}/restore
    parameters:
    - name: instanceId
      in: path
      description: |
        The ID of the Managed MySQL Database.
      example: '{{instanceId}}'
      x-akamai:
        file-path: parameters/instance-id-path-c09a6713.yaml
      required: true
      schema:
        type: integer
    - name: backupId
      in: path
      description: |
        The ID of the Managed MySQL Database backup.
      example: '{{backupId}}'
      x-akamai:
        file-path: parameters/backup-id-path-591248be.yaml
      required: true
      schema:
        type: integer
    post:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: post-databases-mysql-instance-backup-restore
      summary: Restore a managed MySQL database backup
      tags:
      - Backups
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Restore a backup to a Managed MySQL Database on your Account.

        Requires `read_write` access to the Database.

        The Database must have an `active`, `degraded`, or `failed` status to perform this operation.

        __Note__. Restoring from a backup will erase all existing data on the database instance and replace it with backup data.

        __Note__. Currently, restoring a backup after resetting Managed Database credentials results in a failed cluster. Please contact Customer Support if this occurs.


        ---


        - __CLI__.

            ```
            linode-cli databases mysql-backup-restore
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases mysql-backup-restore
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Request to restore backup successful.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-databases-mysql-instance-backup-restore
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X POST https://api.linode.com/v4/databases/mysql/instances/123/backups/456/restore
      - lang: CLI
        source: linode-cli databases mysql-backup-restore 123 456
      x-linode-cli-action: mysql-backup-restore
      x-linode-grant: read_write
      x-original-op-id: postDatabasesMySQLInstanceBackupRestore
      x-original-op-title: Managed MySQL Database Backup
        Restore
    x-linode-cli-command: databases
  /databases/mysql/instances/{instanceId}/credentials:
    x-akamai:
      file-path: paths/mysql-credentials.yaml
      path-info: /databases/mysql/instances/{instanceId}/credentials
    parameters:
    - name: instanceId
      in: path
      description: |
        The ID of the Managed PostgreSQL Database.
      example: '{{instanceId}}'
      x-akamai:
        file-path: parameters/instance-id-path.yaml
      required: true
      schema:
        type: integer
    get:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: get-databases-mysql-instance-credentials
      summary: Get managed MySQL database credentials
      tags:
      - Credentials
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Display the root username and password for an accessible Managed MySQL Database.

        The Database must have an `active` status to perform this operation.


        ---


        - __CLI__.

            ```
            linode-cli databases mysql-creds-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases mysql-creds-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Managed Database root username and password.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Managed Database object for database credentials.
                x-akamai:
                  file-path: schemas/database-credentials.yaml
                additionalProperties: false
                properties:
                  password:
                    type: string
                    description: |
                      __Read-only__ The randomly-generated root password for the Managed Database instance.
                    example: s3cur3P@ssw0rd
                    readOnly: true
                    x-linode-cli-display: 2
                  username:
                    type: string
                    description: |
                      __Read-only__ The root username for the Managed Database instance.
                    example: linroot
                    readOnly: true
                    x-linode-cli-display: 1
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-databases-mysql-instance-credentials
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/databases/mysql/instances/123/credentials/
      - lang: CLI
        source: linode-cli databases mysql-creds-view 123
      x-linode-cli-action: mysql-creds-view
      x-linode-grant: read_only
      x-original-op-id: getDatabasesMySQLInstanceCredentials
      x-original-op-title: Managed MySQL Database Credentials
        View
    x-linode-cli-command: databases
  /databases/mysql/instances/{instanceId}/credentials/reset:
    x-akamai:
      file-path: paths/mysql-reset.yaml
      path-info: /databases/mysql/instances/{instanceId}/credentials/reset
    parameters:
    - name: instanceId
      in: path
      description: |
        The ID of the Managed MySQL Database.
      example: '{{instanceId}}'
      x-akamai:
        file-path: parameters/instance-id-path-c09a6713.yaml
      required: true
      schema:
        type: integer
    post:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: post-databases-mysql-instance-credentials-reset
      summary: Reset managed MySQL database credentials
      tags:
      - Credentials
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Reset the root password for a Managed MySQL Database.

        Requires `read_write` access to the Database.

        A new root password is randomly generated and accessible with the [Get managed MySQL database credentials](https://techdocs.akamai.com/linode-api/reference/get-databases-mysql-instance-credentials) operation.

        Only unrestricted Users can access this operation, and have access regardless of the acting token's OAuth scopes.

        __Note__. Note that it may take several seconds for credentials to reset.


        ---


        - __CLI__.

            ```
            linode-cli databases mysql-creds-reset
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases mysql-creds-reset
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Managed Database instance credentials successfully reset.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-databases-mysql-instance-credentials-reset
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X POST https://api.linode.com/v4/databases/mysql/instances/123/credentials/reset
      - lang: CLI
        source: linode-cli databases mysql-creds-reset 123
      x-linode-cli-action: mysql-creds-reset
      x-linode-grant: read_write
      x-original-op-id: postDatabasesMySQLInstanceCredentialsReset
      x-original-op-title: Managed MySQL Database Credentials
        Reset
    x-linode-cli-command: databases
  /databases/mysql/instances/{instanceId}/patch:
    x-akamai:
      file-path: paths/mysql-patch.yaml
      path-info: /databases/mysql/instances/{instanceId}/patch
    parameters:
    - name: instanceId
      in: path
      description: |
        The ID of the Managed PostgreSQL Database.
      example: '{{instanceId}}'
      x-akamai:
        file-path: parameters/instance-id-path.yaml
      required: true
      schema:
        type: integer
    post:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: post-databases-mysql-instance-patch
      summary: Patch a managed MySQL database
      tags:
      - Databases
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Apply security patches and updates to the underlying operating system of the Managed MySQL Database. This function runs during regular maintenance windows, which are configurable with the [Update a managed MySQL database](https://techdocs.akamai.com/linode-api/reference/put-databases-mysql-instance) operation.

        Requires `read_write` access to the Database.

        The Database must have an `active` status to perform this operation.

        __NOTE__

        - If your database cluster is configured with a single node, you will experience downtime during this maintenance. Consider upgrading to a high availability plan to avoid any downtime due to maintenance.

        - __The database software is not updated automatically.__ To upgrade to a new database engine version, consider deploying a new Managed Database with your preferred version. You can then [migrate your databases](https://www.linode.com/docs/products/databases/managed-databases/guides/migrate-mysql/) from the original Managed Database cluster to the new one.


        ---


        - __CLI__.

            ```
            linode-cli databases mysql-patch
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases mysql-patch
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Managed Database instance patch request successful.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-databases-mysql-instance-patch
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X POST https://api.linode.com/v4/databases/mysql/instances/123/patch
      - lang: CLI
        source: linode-cli databases mysql-patch 123
      x-linode-cli-action: mysql-patch
      x-linode-grant: read_write
      x-original-op-id: postDatabasesMySQLInstancePatch
      x-original-op-title: Managed MySQL Database Patch
    x-linode-cli-command: databases
  /databases/mysql/instances/{instanceId}/ssl:
    x-akamai:
      file-path: paths/mysql-ssl.yaml
      path-info: /databases/mysql/instances/{instanceId}/ssl
    parameters:
    - name: instanceId
      in: path
      description: |
        The ID of the Managed MySQL Database.
      example: '{{instanceId}}'
      x-akamai:
        file-path: parameters/instance-id-path-c09a6713.yaml
      required: true
      schema:
        type: integer
    get:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: get-databases-mysql-instance-ssl
      summary: Get a managed MySQL database SSL certificate
      tags:
      - SSL certificates
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Display the SSL CA certificate for an accessible Managed MySQL Database.

        The Database must have an `active` status to perform this operation.


        ---


        - __CLI__.

            ```
            linode-cli databases mysql-ssl-cert
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases mysql-ssl-cert
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns the SSL CA certificate of a single Managed MySQL Database.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Managed Database SSL object.
                x-akamai:
                  file-path: schemas/database-ssl.yaml
                additionalProperties: false
                properties:
                  ca_certificate:
                    type: string
                    description: |
                      The base64-encoded SSL CA certificate for the Managed Database instance.
                    example: LS0tLS1CRUdJ...==
                    format: base64
                    x-linode-cli-display: 1
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-databases-mysql-instance-ssl
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/databases/mysql/instances/123/ssl
      - lang: CLI
        source: linode-cli databases mysql-ssl-cert 123
      x-linode-cli-action: mysql-ssl-cert
      x-linode-grant: read_only
      x-original-op-id: getDatabasesMySQLInstanceSSL
      x-original-op-title: Managed MySQL Database SSL
        Certificate View
    x-linode-cli-command: databases
  /databases/postgresql/instances:
    x-akamai:
      file-path: paths/postgresql-instances.yaml
      path-info: /databases/postgresql/instances
    post:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: post-databases-postgre-sql-instances
      summary: Create a managed PostgreSQL database
      tags:
      - Instances
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Provision a Managed PostgreSQL Database.

        Restricted Users must have the `add_databases` grant to use this operation.

        New instances can take approximately 15 to 30 minutes to provision.

        The `allow_list` is used to control access to the Managed Database.

        - IP addresses and ranges in this list can access the Managed Database. All other sources are blocked.

        - If `0.0.0.0/0` is a value in this list, then all IP addresses can access the Managed Database.

        - Entering an empty array (`[]`) blocks all connections (both public and private) to the Managed Database.

        All Managed Databases include automatic, daily backups. Up to seven backups are automatically stored for each Managed Database, providing restore points for each day of the past week.

        All Managed Databases include automatic patch updates, which apply security patches and updates to the underlying operating system of the Managed PostgreSQL Database during configurable maintenance windows.

        - If your database cluster is configured with a single node, you will experience downtime during this maintenance window when any updates occur. It's recommended that you adjust this window to match a time that will be the least disruptive for your application and users. You may also want to consider upgrading to a high availability plan to avoid any downtime due to maintenance.

        - __The database software is not updated automatically.__ To upgrade to a new database engine version, consider deploying a new Managed Database with your preferred version. You can then migrate your databases from the original Managed Database cluster to the new one.

        - To modify update the maintenance window for a Database, run the [Update a managed PostgreSQL database](https://techdocs.akamai.com/linode-api/reference/put-databases-postgre-sql-instance) operation.


        ---


        - __CLI__.

            ```
            linode-cli databases postgresql-create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases postgresql-create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the Managed PostgreSQL Database you are creating.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                Managed PostgreSQL Database request object.
              x-akamai:
                file-path: schemas/database-postgresql-request.yaml
              additionalProperties: false
              required:
              - label
              - type
              - engine
              - region
              properties:
                type:
                  type: string
                  description: |
                    The Linode Instance type used by the Managed Database for its nodes.
                  example: '{{type}}'
                  x-linode-cli-display: 4
                  x-linode-filterable: true
                allow_list:
                  type: array
                  description: |-
                    A list of IP addresses that can access the Managed Database. Each item can be a single IP address or a range in CIDR format.

                    By default, this is an empty array (`[]`), which blocks all connections (both public and private) to the Managed Database.

                    If `0.0.0.0/0` is a value in this list, then all IP addresses can access the Managed Database.
                  example:
                  - 203.0.113.1/32
                  - 192.0.1.0/24
                  items:
                    type: string
                    pattern: ^([0-9]{1,3}\.){3}[0-9]{1,3}(\/([0-9]|[1-2][0-9]|3[0-2]))?$
                    format: ipv4/prefix_length
                cluster_size:
                  type: integer
                  description: |-
                    The number of Linode Instance nodes deployed to the Managed Database.

                    Choosing 3 nodes creates a high availability cluster consisting of 1 primary node and 2 replica nodes.
                  example: '{{cluster_size}}'
                  default: 1
                  enum:
                  - 1
                  - 3
                  x-linode-cli-display: 5
                encrypted:
                  type: boolean
                  description: |
                    Whether the Managed Databases is encrypted.
                  example: '{{encrypted}}'
                  default: false
                engine:
                  type: string
                  description: |
                    The Managed Database engine in engine/version format.
                  example: '{{engine}}'
                label:
                  type: string
                  description: |
                    A unique, user-defined string referring to the Managed Database.
                  example: '{{label}}'
                  minLength: 3
                  maxLength: 32
                  x-linode-cli-display: 2
                  x-linode-filterable: true
                region:
                  type: string
                  description: |
                    The [Region](https://techdocs.akamai.com/linode-api/reference/get-regions) ID for the Managed Database.
                  example: '{{region}}'
                  x-linode-cli-display: 3
                  x-linode-filterable: true
                replication_commit_type:
                  type: string
                  description: |-
                    The synchronization level of the replicating server.

                    Must be `local` or `off` for the `asynch` replication type.
                  example: '{{replication_commit_type}}'
                  default: local
                  enum:
                  - true
                  - local
                  - remote_write
                  - remote_apply
                  - false
                  x-linode-cli-display: 100
                replication_type:
                  type: string
                  description: |-
                    The replication method used for the Managed Database.

                    Defaults to `none` for a single cluster and `asynch` for a high availability cluster.

                    Must be `none` for a single node cluster.

                    Must be `asynch` for a high availability cluster.
                  example: '{{replication_type}}'
                  enum:
                  - none
                  - asynch
                  x-linode-cli-display: 99
                ssl_connection:
                  type: boolean
                  description: |-
                    Whether to require SSL credentials to establish a connection to the Managed Database.

                    Run the [Get managed PostgreSQL database credentials](https://techdocs.akamai.com/linode-api/reference/get-databases-postgre-sql-instance-credentials) operation for access information.
                  example: '{{ssl_connection}}'
                  default: true
            x-example:
              x-ref: ../examples/tbd.json
        x-linode-cli-allowed-defaults:
        - engine
        - region
        - type
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A new Managed PostgreSQL Database is provisioning.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Managed PostgreSQL Databases object.
                x-akamai:
                  file-path: schemas/database-postgresql.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ A unique ID that can be used to identify and reference the Managed Database.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                  type:
                    type: string
                    description: |
                      The Linode Instance type used by the Managed Database for its nodes.
                    example: g6-dedicated-2
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  version:
                    type: string
                    description: |
                      __Read-only__ The Managed Database engine version.
                    example: '13.2'
                    readOnly: true
                    x-linode-cli-display: 7
                    x-linode-filterable: true
                  allow_list:
                    type: array
                    description: |-
                      A list of IP addresses that can access the Managed Database. Each item can be a single IP address or a range in CIDR format.

                      By default, this is an empty array (`[]`), which blocks all connections (both public and private) to the Managed Database.

                      If `0.0.0.0/0` is a value in this list, then all IP addresses can access the Managed Database.
                    example:
                    - 203.0.113.1/32
                    - 192.0.1.0/24
                    items:
                      type: string
                      pattern: ^([0-9]{1,3}\.){3}[0-9]{1,3}(\/([0-9]|[1-2][0-9]|3[0-2]))?$
                      format: ipv4/prefix_length
                  cluster_size:
                    type: integer
                    description: |-
                      The number of Linode Instance nodes deployed to the Managed Database.

                      Choosing 3 nodes creates a high availability cluster consisting of 1 primary node and 2 replica nodes.
                    example: 3
                    default: 1
                    enum:
                    - 1
                    - 3
                    x-linode-cli-display: 5
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Managed Database was created.
                    example: '2022-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  encrypted:
                    type: boolean
                    description: |
                      Whether the Managed Databases is encrypted.
                    example: false
                    default: false
                  engine:
                    type: string
                    description: |
                      __Read-only__ The Managed Database engine type.
                    example: postgresql
                    readOnly: true
                    x-linode-cli-display: 6
                    x-linode-filterable: true
                  hosts:
                    type: object
                    description: |
                      __Read-only__ The primary and secondary hosts for the Managed Database. These are assigned
                      after provisioning is complete.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      primary:
                        type: string
                        description: |
                          The primary host for the Managed Database.
                        example: lin-0000-000-pgsql-primary.servers.linodedb.net
                        nullable: true
                      secondary:
                        type: string
                        description: |-
                          The secondary/private network host for the Managed Database.

                          A private network host and a private IP can only be used to access a Database Cluster from Linodes in the same data center and will not incur transfer costs.

                          __Note__: The secondary hostname is publicly viewable and accessible.
                        example: lin-0000-000-pgsql-primary-private.servers.linodedb.net
                        nullable: true
                  label:
                    type: string
                    description: |
                      A unique, user-defined string referring to the Managed Database.
                    example: example-db
                    minLength: 3
                    maxLength: 32
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  port:
                    type: integer
                    description: |
                      The access port for this Managed Database.
                    example: 3306
                    x-linode-cli-display: 98
                  region:
                    type: string
                    description: |
                      The [Region](https://techdocs.akamai.com/linode-api/reference/get-regions) ID for the Managed Database.
                    example: us-east
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  replication_commit_type:
                    type: string
                    description: |-
                      The synchronization level of the replicating server.

                      Must be `local` or `off` for the `asynch` replication type.
                    example: local
                    default: local
                    enum:
                    - true
                    - local
                    - remote_write
                    - remote_apply
                    - false
                    x-linode-cli-display: 100
                  replication_type:
                    type: string
                    description: |-
                      The replication method used for the Managed Database.

                      Defaults to `none` for a single cluster and `asynch` for a high availability cluster.

                      Must be `none` for a single node cluster.

                      Must be `asynch` for a high availability cluster.
                    example: async
                    enum:
                    - none
                    - asynch
                    x-linode-cli-display: 99
                  ssl_connection:
                    type: boolean
                    description: |-
                      Whether to require SSL credentials to establish a connection to the Managed Database.

                      Run the [Get managed PostgreSQL database credentials](https://techdocs.akamai.com/linode-api/reference/get-databases-postgre-sql-instance-credentials) operation for access information.
                    example: true
                    default: true
                  status:
                    type: string
                    description: |
                      __Read-only__ The operating status of the Managed Database.
                    example: active
                    readOnly: true
                    enum:
                    - provisioning
                    - active
                    - suspending
                    - suspended
                    - resuming
                    - restoring
                    - failed
                    - degraded
                    - updating
                    - backing_up
                    x-linode-cli-color:
                      active: green
                      default_: white
                      degraded: red
                      failed: red
                      provisioning: yellow
                      restoring: yellow
                      resuming: yellow
                    x-linode-cli-display: 100
                    x-linode-filterable: true
                  total_disk_size_gb:
                    type: integer
                    description: |
                      The total disk size of the database in GB.
                    example: 15
                    x-linode-cli-display: 8
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Managed Database was last updated.
                    example: '2022-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  updates:
                    type: object
                    description: |
                      Configuration settings for automated patch update maintenance for the Managed Database.
                    additionalProperties: false
                    properties:
                      day_of_week:
                        type: integer
                        description: |
                          The day to perform maintenance. 1=Monday, 2=Tuesday, etc.
                        example: 1
                        minimum: 1
                        maximum: 7
                      duration:
                        type: integer
                        description: |
                          The maximum maintenance window time in hours.
                        example: 3
                        minimum: 1
                        maximum: 3
                      frequency:
                        type: string
                        description: |
                          Whether maintenance occurs on a weekly or monthly basis.
                        example: weekly
                        default: weekly
                        enum:
                        - weekly
                        - monthly
                      hour_of_day:
                        type: integer
                        description: |
                          The hour to begin maintenance based in UTC time.
                        example: 0
                        minimum: 0
                        maximum: 23
                      week_of_month:
                        type: integer
                        description: |-
                          The week of the month to perform `monthly` frequency updates. Defaults to `null`.

                          - Required for `monthly` frequency updates.

                          - Must be `null` for `weekly` frequency updates.
                        example: null
                        nullable: true
                        minimum: 1
                        maximum: 4
                  used_disk_size_gb:
                    type: integer
                    description: |
                      The used space of the database in GB.
                    example: 2
                    x-linode-cli-display: 9
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-databases-postgre-sql-instances
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \ -H "Authorization: Bearer $TOKEN" \ -X POST -d '{
              "label": "example-db",
              "region": "us-east",
              "type": "g6-dedicated-2",
              "cluster_size": 3,
              "engine": "postgresql/13.2",
              "encrypted": false,
              "ssl_connection": false,
              "replication_type": "asynch",
              "replication_commit_type": "local",
              "allow_list": [
                "203.0.113.1",
                "192.0.1.0/24"
              ]
          }' \ https://api.linode.com/v4/databases/postgresql/instances
      - lang: CLI
        source: |-
          linode-cli databases postgresql-create \
            --label example-db \
            --region us-east \
            --type g6-dedicated-2 \
            --cluster_size 3 \
            --engine postgresql/13.2 \
            --encrypted false \
            --ssl_connection false \
            --replication_type asynch \
            --replication_commit_type local \
            --allow_list 203.0.113.1 \
            --allow_list 192.0.1.0/24
      x-linode-cli-action: postgresql-create
      x-linode-grant: add_databases
      x-original-op-id: postDatabasesPostgreSQLInstances
      x-original-op-title: Managed PostgreSQL Database
        Create
    get:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: get-databases-postgre-sql-instances
      summary: List managed PostgreSQL databases
      tags:
      - Instances
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Display all accessible Managed PostgreSQL Databases.


        ---


        - __CLI__.

            ```
            linode-cli databases postgresql-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases postgresql-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated list of all accessible Managed PostgreSQL Databases on your Account.
          content:
            application/json:
              schema:
                allOf:
                - type: object
                  description: |
                    An envelope for paginated response. When accessing a collection through a GET endpoint,
                    the results are wrapped in this envelope which includes metadata
                    about those results. Results are presented within a `data` array.
                    See [Pagination](https://techdocs.akamai.com/linode-api/reference/pagination)
                    for more information.
                  x-akamai:
                    file-path: schemas/pagination-envelope.yaml
                  additionalProperties: false
                  properties:
                    page:
                      type: integer
                      description: |
                        __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    pages:
                      type: integer
                      description: |
                        __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    results:
                      type: integer
                      description: |
                        __Read-only__ The total number of results.
                      example: 1
                      readOnly: true
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        type: object
                        description: |
                          Managed PostgreSQL Databases object.
                        x-akamai:
                          file-path: schemas/database-postgresql.yaml
                        additionalProperties: false
                        properties:
                          id:
                            type: integer
                            description: |
                              __Read-only__ A unique ID that can be used to identify and reference
                              the Managed Database.
                            example: 123
                            readOnly: true
                            x-linode-cli-display: 1
                          type:
                            type: string
                            description: |
                              The Linode Instance type used by the Managed Database for its nodes.
                            example: g6-dedicated-2
                            x-linode-cli-display: 4
                            x-linode-filterable: true
                          version:
                            type: string
                            description: |
                              __Read-only__ The Managed Database engine version.
                            example: '13.2'
                            readOnly: true
                            x-linode-cli-display: 7
                            x-linode-filterable: true
                          allow_list:
                            type: array
                            description: |-
                              A list of IP addresses that can access the Managed Database. Each item can be a single IP address or a range in CIDR format.

                              By default, this is an empty array (`[]`), which blocks all connections (both public and private) to the Managed Database.

                              If `0.0.0.0/0` is a value in this list, then all IP addresses can access the Managed Database.
                            example:
                            - 203.0.113.1/32
                            - 192.0.1.0/24
                            items:
                              type: string
                              pattern: ^([0-9]{1,3}\.){3}[0-9]{1,3}(\/([0-9]|[1-2][0-9]|3[0-2]))?$
                              format: ipv4/prefix_length
                          cluster_size:
                            type: integer
                            description: |-
                              The number of Linode Instance nodes deployed to the Managed Database.

                              Choosing 3 nodes creates a high availability cluster consisting of 1 primary node and 2 replica nodes.
                            example: 3
                            default: 1
                            enum:
                            - 1
                            - 3
                            x-linode-cli-display: 5
                          created:
                            type: string
                            description: |
                              __Read-only__ When this Managed Database was created.
                            example: '2022-01-01T00:01:01'
                            readOnly: true
                            format: date-time
                          encrypted:
                            type: boolean
                            description: |
                              Whether the Managed Databases is encrypted.
                            example: false
                            default: false
                          engine:
                            type: string
                            description: |
                              __Read-only__ The Managed Database engine type.
                            example: postgresql
                            readOnly: true
                            x-linode-cli-display: 6
                            x-linode-filterable: true
                          hosts:
                            type: object
                            description: |
                              __Read-only__ The primary and secondary hosts for the Managed Database.
                              These are assigned after provisioning is complete.
                            additionalProperties: false
                            readOnly: true
                            properties:
                              primary:
                                type: string
                                description: |
                                  The primary host for the Managed Database.
                                example: lin-0000-000-pgsql-primary.servers.linodedb.net
                                nullable: true
                              secondary:
                                type: string
                                description: |-
                                  The secondary/private network host for the Managed Database.

                                  A private network host and a private IP can only be used to access a Database Cluster from Linodes in the same data center and will not incur transfer costs.

                                  __Note__: The secondary hostname is publicly viewable and accessible.
                                example: lin-0000-000-pgsql-primary-private.servers.linodedb.net
                                nullable: true
                          label:
                            type: string
                            description: |
                              A unique, user-defined string referring to the Managed Database.
                            example: example-db
                            minLength: 3
                            maxLength: 32
                            x-linode-cli-display: 2
                            x-linode-filterable: true
                          port:
                            type: integer
                            description: |
                              The access port for this Managed Database.
                            example: 3306
                            x-linode-cli-display: 98
                          region:
                            type: string
                            description: |
                              The [Region](https://techdocs.akamai.com/linode-api/reference/get-regions) ID for the Managed Database.
                            example: us-east
                            x-linode-cli-display: 3
                            x-linode-filterable: true
                          replication_commit_type:
                            type: string
                            description: |-
                              The synchronization level of the replicating server.

                              Must be `local` or `off` for the `asynch` replication type.
                            example: local
                            default: local
                            enum:
                            - true
                            - local
                            - remote_write
                            - remote_apply
                            - false
                            x-linode-cli-display: 100
                          replication_type:
                            type: string
                            description: |-
                              The replication method used for the Managed Database.

                              Defaults to `none` for a single cluster and `asynch` for a high availability cluster.

                              Must be `none` for a single node cluster.

                              Must be `asynch` for a high availability cluster.
                            example: async
                            enum:
                            - none
                            - asynch
                            x-linode-cli-display: 99
                          ssl_connection:
                            type: boolean
                            description: |-
                              Whether to require SSL credentials to establish a connection to the Managed Database.

                              Run the [Get managed PostgreSQL database credentials](https://techdocs.akamai.com/linode-api/reference/get-databases-postgre-sql-instance-credentials) operation for access information.
                            example: true
                            default: true
                          status:
                            type: string
                            description: |
                              __Read-only__ The operating status of the Managed Database.
                            example: active
                            readOnly: true
                            enum:
                            - provisioning
                            - active
                            - suspending
                            - suspended
                            - resuming
                            - restoring
                            - failed
                            - degraded
                            - updating
                            - backing_up
                            x-linode-cli-color:
                              active: green
                              default_: white
                              degraded: red
                              failed: red
                              provisioning: yellow
                              restoring: yellow
                              resuming: yellow
                            x-linode-cli-display: 100
                            x-linode-filterable: true
                          total_disk_size_gb:
                            type: integer
                            description: |
                              The total disk size of the database in GB.
                            example: 15
                            x-linode-cli-display: 8
                          updated:
                            type: string
                            description: |
                              __Read-only__ When this Managed Database was last updated.
                            example: '2022-01-01T00:01:01'
                            readOnly: true
                            format: date-time
                          updates:
                            type: object
                            description: |
                              Configuration settings for automated patch update maintenance for
                              the Managed Database.
                            additionalProperties: false
                            properties:
                              day_of_week:
                                type: integer
                                description: |
                                  The day to perform maintenance. 1=Monday, 2=Tuesday, etc.
                                example: 1
                                minimum: 1
                                maximum: 7
                              duration:
                                type: integer
                                description: |
                                  The maximum maintenance window time in hours.
                                example: 3
                                minimum: 1
                                maximum: 3
                              frequency:
                                type: string
                                description: |
                                  Whether maintenance occurs on a weekly or monthly basis.
                                example: weekly
                                default: weekly
                                enum:
                                - weekly
                                - monthly
                              hour_of_day:
                                type: integer
                                description: |
                                  The hour to begin maintenance based in UTC time.
                                example: 0
                                minimum: 0
                                maximum: 23
                              week_of_month:
                                type: integer
                                description: |-
                                  The week of the month to perform `monthly` frequency updates. Defaults to `null`.

                                  - Required for `monthly` frequency updates.

                                  - Must be `null` for `weekly` frequency updates.
                                example: null
                                nullable: true
                                minimum: 1
                                maximum: 4
                          used_disk_size_gb:
                            type: integer
                            description: |
                              The used space of the database in GB.
                            example: 2
                            x-linode-cli-display: 9
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-databases-postgre-sql-instances
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/databases/postgresql/instances/
      - lang: CLI
        source: linode-cli databases postgresql-list
      x-linode-cli-action: postgresql-list
      x-linode-grant: read_only
      x-original-op-id: getDatabasesPostgreSQLInstances
      x-original-op-title: Managed PostgreSQL Databases
        List
    x-linode-cli-command: databases
  /databases/postgresql/instances/{instanceId}:
    x-akamai:
      file-path: paths/postgresql-instance.yaml
      path-info: /databases/postgresql/instances/{instanceId}
    parameters:
    - name: instanceId
      in: path
      description: |
        The ID of the Managed PostgreSQL Database.
      example: '{{instanceId}}'
      x-akamai:
        file-path: parameters/instance-id-path.yaml
      required: true
      schema:
        type: integer
    get:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: get-databases-postgre-sql-instance
      summary: Get a managed PostgreSQL database
      tags:
      - Instances
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Display information for a single, accessible Managed PostgreSQL Database.


        ---


        - __CLI__.

            ```
            linode-cli databases postgresql-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases postgresql-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns information for a single Managed PostgreSQL Database.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Managed PostgreSQL Databases object.
                x-akamai:
                  file-path: schemas/database-postgresql.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ A unique ID that can be used to identify and reference the Managed Database.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                  type:
                    type: string
                    description: |
                      The Linode Instance type used by the Managed Database for its nodes.
                    example: g6-dedicated-2
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  version:
                    type: string
                    description: |
                      __Read-only__ The Managed Database engine version.
                    example: '13.2'
                    readOnly: true
                    x-linode-cli-display: 7
                    x-linode-filterable: true
                  allow_list:
                    type: array
                    description: |-
                      A list of IP addresses that can access the Managed Database. Each item can be a single IP address or a range in CIDR format.

                      By default, this is an empty array (`[]`), which blocks all connections (both public and private) to the Managed Database.

                      If `0.0.0.0/0` is a value in this list, then all IP addresses can access the Managed Database.
                    example:
                    - 203.0.113.1/32
                    - 192.0.1.0/24
                    items:
                      type: string
                      pattern: ^([0-9]{1,3}\.){3}[0-9]{1,3}(\/([0-9]|[1-2][0-9]|3[0-2]))?$
                      format: ipv4/prefix_length
                  cluster_size:
                    type: integer
                    description: |-
                      The number of Linode Instance nodes deployed to the Managed Database.

                      Choosing 3 nodes creates a high availability cluster consisting of 1 primary node and 2 replica nodes.
                    example: 3
                    default: 1
                    enum:
                    - 1
                    - 3
                    x-linode-cli-display: 5
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Managed Database was created.
                    example: '2022-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  encrypted:
                    type: boolean
                    description: |
                      Whether the Managed Databases is encrypted.
                    example: false
                    default: false
                  engine:
                    type: string
                    description: |
                      __Read-only__ The Managed Database engine type.
                    example: postgresql
                    readOnly: true
                    x-linode-cli-display: 6
                    x-linode-filterable: true
                  hosts:
                    type: object
                    description: |
                      __Read-only__ The primary and secondary hosts for the Managed Database. These are assigned
                      after provisioning is complete.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      primary:
                        type: string
                        description: |
                          The primary host for the Managed Database.
                        example: lin-0000-000-pgsql-primary.servers.linodedb.net
                        nullable: true
                      secondary:
                        type: string
                        description: |-
                          The secondary/private network host for the Managed Database.

                          A private network host and a private IP can only be used to access a Database Cluster from Linodes in the same data center and will not incur transfer costs.

                          __Note__: The secondary hostname is publicly viewable and accessible.
                        example: lin-0000-000-pgsql-primary-private.servers.linodedb.net
                        nullable: true
                  label:
                    type: string
                    description: |
                      A unique, user-defined string referring to the Managed Database.
                    example: example-db
                    minLength: 3
                    maxLength: 32
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  port:
                    type: integer
                    description: |
                      The access port for this Managed Database.
                    example: 3306
                    x-linode-cli-display: 98
                  region:
                    type: string
                    description: |
                      The [Region](https://techdocs.akamai.com/linode-api/reference/get-regions) ID for the Managed Database.
                    example: us-east
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  replication_commit_type:
                    type: string
                    description: |-
                      The synchronization level of the replicating server.

                      Must be `local` or `off` for the `asynch` replication type.
                    example: local
                    default: local
                    enum:
                    - true
                    - local
                    - remote_write
                    - remote_apply
                    - false
                    x-linode-cli-display: 100
                  replication_type:
                    type: string
                    description: |-
                      The replication method used for the Managed Database.

                      Defaults to `none` for a single cluster and `asynch` for a high availability cluster.

                      Must be `none` for a single node cluster.

                      Must be `asynch` for a high availability cluster.
                    example: async
                    enum:
                    - none
                    - asynch
                    x-linode-cli-display: 99
                  ssl_connection:
                    type: boolean
                    description: |-
                      Whether to require SSL credentials to establish a connection to the Managed Database.

                      Run the [Get managed PostgreSQL database credentials](https://techdocs.akamai.com/linode-api/reference/get-databases-postgre-sql-instance-credentials) operation for access information.
                    example: true
                    default: true
                  status:
                    type: string
                    description: |
                      __Read-only__ The operating status of the Managed Database.
                    example: active
                    readOnly: true
                    enum:
                    - provisioning
                    - active
                    - suspending
                    - suspended
                    - resuming
                    - restoring
                    - failed
                    - degraded
                    - updating
                    - backing_up
                    x-linode-cli-color:
                      active: green
                      default_: white
                      degraded: red
                      failed: red
                      provisioning: yellow
                      restoring: yellow
                      resuming: yellow
                    x-linode-cli-display: 100
                    x-linode-filterable: true
                  total_disk_size_gb:
                    type: integer
                    description: |
                      The total disk size of the database in GB.
                    example: 15
                    x-linode-cli-display: 8
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Managed Database was last updated.
                    example: '2022-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  updates:
                    type: object
                    description: |
                      Configuration settings for automated patch update maintenance for the Managed Database.
                    additionalProperties: false
                    properties:
                      day_of_week:
                        type: integer
                        description: |
                          The day to perform maintenance. 1=Monday, 2=Tuesday, etc.
                        example: 1
                        minimum: 1
                        maximum: 7
                      duration:
                        type: integer
                        description: |
                          The maximum maintenance window time in hours.
                        example: 3
                        minimum: 1
                        maximum: 3
                      frequency:
                        type: string
                        description: |
                          Whether maintenance occurs on a weekly or monthly basis.
                        example: weekly
                        default: weekly
                        enum:
                        - weekly
                        - monthly
                      hour_of_day:
                        type: integer
                        description: |
                          The hour to begin maintenance based in UTC time.
                        example: 0
                        minimum: 0
                        maximum: 23
                      week_of_month:
                        type: integer
                        description: |-
                          The week of the month to perform `monthly` frequency updates. Defaults to `null`.

                          - Required for `monthly` frequency updates.

                          - Must be `null` for `weekly` frequency updates.
                        example: null
                        nullable: true
                        minimum: 1
                        maximum: 4
                  used_disk_size_gb:
                    type: integer
                    description: |
                      The used space of the database in GB.
                    example: 2
                    x-linode-cli-display: 9
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-databases-postgre-sql-instance
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/databases/postgresql/instances/123
      - lang: CLI
        source: linode-cli databases postgresql-view 123
      x-linode-cli-action: postgresql-view
      x-linode-grant: read_only
      x-original-op-id: getDatabasesPostgreSQLInstance
      x-original-op-title: Managed PostgreSQL Database
        View
    put:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: put-databases-postgre-sql-instance
      summary: Update a managed PostgreSQL database
      tags:
      - Instances
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Update a Managed PostgreSQL Database.

        Requires `read_write` access to the Database.

        The Database must have an `active` status to perform this operation.

        Updating addresses in the `allow_list` overwrites any existing addresses.

        - IP addresses and ranges in this list can access the Managed Database. All other sources are blocked.

        - If `0.0.0.0/0` is a value in this list, then all IP addresses can access the Managed Database.

        - Entering an empty array (`[]`) blocks all connections (both public and private) to the Managed Database.

        - __Note__. Updates to the `allow_list` may take a short period of time to complete, making this operation inappropriate for rapid successive updates to this property.

        All Managed Databases include automatic patch updates, which apply security patches and updates to the underlying operating system of the Managed PostgreSQL Database. The maintenance window for these updates is configured with the Managed Database's `updates` property.

        - If your database cluster is configured with a single node, you will experience downtime during this maintenance window when any updates occur. It's recommended that you adjust this window to match a time that will be the least disruptive for your application and users. You may also want to consider upgrading to a high availability plan to avoid any downtime due to maintenance.

        - __The database software is not updated automatically.__ To upgrade to a new database engine version, consider deploying a new Managed Database with your preferred version. You can then migrate your databases from the original Managed Database cluster to the new one.


        ---


        - __CLI__.

            ```
            linode-cli databases postgresql-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases postgresql-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Updated information for the Managed PostgreSQL Database.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                Updated information for the Managed PostgreSQL Database.
              additionalProperties: false
              properties:
                type:
                  type: string
                  description: |-
                    Request re-sizing of your cluster to a Linode Type with more disk space. For example, you could request a Linode Type that uses a higher plan.

                    - Needs to be a Linode Type with more disk space than your current Linode.

                    - Resizing to a larger Linode Type can accrue additional cost. Review the `price` output from the [List types](https://techdocs.akamai.com/linode-api/reference/get-linode-types) operation for more information.

                    - You can't update the `allow_list` and set a new `type` in the same request.

                    - Any active updates to your cluster need to complete before you can request a resize. The reverse is also true: An active resizing needs to complete before you can perform any other update.
                  example: '{{type}}'
                allow_list:
                  type: array
                  description: |-
                    A list of IP addresses that can access the Managed Database. Each item can be a single IP address or a range in CIDR format.

                    By default, this is an empty array (`[]`), which blocks all connections (both public and private) to the Managed Database.

                    If `0.0.0.0/0` is a value in this list, then all IP addresses can access the Managed Database.
                  example:
                  - 203.0.113.1/32
                  - 192.0.1.0/24
                  items:
                    type: string
                    pattern: ^([0-9]{1,3}\.){3}[0-9]{1,3}(\/([0-9]|[1-2][0-9]|3[0-2]))?$
                    format: ipv4/prefix_length
                label:
                  type: string
                  description: |
                    A unique, user-defined string referring to the Managed Database.
                  example: '{{label}}'
                  minLength: 3
                  maxLength: 32
                  x-linode-cli-display: 2
                  x-linode-filterable: true
                updates:
                  type: object
                  description: |
                    Configuration settings for automated patch update maintenance for the Managed Database.
                  additionalProperties: false
                  properties:
                    day_of_week:
                      type: integer
                      description: |
                        The day to perform maintenance. 1=Monday, 2=Tuesday, etc.
                      example: 1
                      minimum: 1
                      maximum: 7
                    duration:
                      type: integer
                      description: |
                        The maximum maintenance window time in hours.
                      example: 3
                      minimum: 1
                      maximum: 3
                    frequency:
                      type: string
                      description: |
                        Whether maintenance occurs on a weekly or monthly basis.
                      example: weekly
                      default: weekly
                      enum:
                      - weekly
                      - monthly
                    hour_of_day:
                      type: integer
                      description: |
                        The hour to begin maintenance based in UTC time.
                      example: 0
                      minimum: 0
                      maximum: 23
                    week_of_month:
                      type: integer
                      description: |-
                        The week of the month to perform `monthly` frequency updates. Defaults to `null`.

                        - Required for `monthly` frequency updates.

                        - Must be `null` for `weekly` frequency updates.
                      example: null
                      nullable: true
                      minimum: 1
                      maximum: 4
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Managed Database updated successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Managed PostgreSQL Databases object.
                x-akamai:
                  file-path: schemas/database-postgresql.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ A unique ID that can be used to identify and reference the Managed Database.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                  type:
                    type: string
                    description: |
                      The Linode Instance type used by the Managed Database for its nodes.
                    example: g6-dedicated-2
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  version:
                    type: string
                    description: |
                      __Read-only__ The Managed Database engine version.
                    example: '13.2'
                    readOnly: true
                    x-linode-cli-display: 7
                    x-linode-filterable: true
                  allow_list:
                    type: array
                    description: |-
                      A list of IP addresses that can access the Managed Database. Each item can be a single IP address or a range in CIDR format.

                      By default, this is an empty array (`[]`), which blocks all connections (both public and private) to the Managed Database.

                      If `0.0.0.0/0` is a value in this list, then all IP addresses can access the Managed Database.
                    example:
                    - 203.0.113.1/32
                    - 192.0.1.0/24
                    items:
                      type: string
                      pattern: ^([0-9]{1,3}\.){3}[0-9]{1,3}(\/([0-9]|[1-2][0-9]|3[0-2]))?$
                      format: ipv4/prefix_length
                  cluster_size:
                    type: integer
                    description: |-
                      The number of Linode Instance nodes deployed to the Managed Database.

                      Choosing 3 nodes creates a high availability cluster consisting of 1 primary node and 2 replica nodes.
                    example: 3
                    default: 1
                    enum:
                    - 1
                    - 3
                    x-linode-cli-display: 5
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Managed Database was created.
                    example: '2022-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  encrypted:
                    type: boolean
                    description: |
                      Whether the Managed Databases is encrypted.
                    example: false
                    default: false
                  engine:
                    type: string
                    description: |
                      __Read-only__ The Managed Database engine type.
                    example: postgresql
                    readOnly: true
                    x-linode-cli-display: 6
                    x-linode-filterable: true
                  hosts:
                    type: object
                    description: |
                      __Read-only__ The primary and secondary hosts for the Managed Database. These are assigned
                      after provisioning is complete.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      primary:
                        type: string
                        description: |
                          The primary host for the Managed Database.
                        example: lin-0000-000-pgsql-primary.servers.linodedb.net
                        nullable: true
                      secondary:
                        type: string
                        description: |-
                          The secondary/private network host for the Managed Database.

                          A private network host and a private IP can only be used to access a Database Cluster from Linodes in the same data center and will not incur transfer costs.

                          __Note__: The secondary hostname is publicly viewable and accessible.
                        example: lin-0000-000-pgsql-primary-private.servers.linodedb.net
                        nullable: true
                  label:
                    type: string
                    description: |
                      A unique, user-defined string referring to the Managed Database.
                    example: example-db
                    minLength: 3
                    maxLength: 32
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  port:
                    type: integer
                    description: |
                      The access port for this Managed Database.
                    example: 3306
                    x-linode-cli-display: 98
                  region:
                    type: string
                    description: |
                      The [Region](https://techdocs.akamai.com/linode-api/reference/get-regions) ID for the Managed Database.
                    example: us-east
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  replication_commit_type:
                    type: string
                    description: |-
                      The synchronization level of the replicating server.

                      Must be `local` or `off` for the `asynch` replication type.
                    example: local
                    default: local
                    enum:
                    - true
                    - local
                    - remote_write
                    - remote_apply
                    - false
                    x-linode-cli-display: 100
                  replication_type:
                    type: string
                    description: |-
                      The replication method used for the Managed Database.

                      Defaults to `none` for a single cluster and `asynch` for a high availability cluster.

                      Must be `none` for a single node cluster.

                      Must be `asynch` for a high availability cluster.
                    example: async
                    enum:
                    - none
                    - asynch
                    x-linode-cli-display: 99
                  ssl_connection:
                    type: boolean
                    description: |-
                      Whether to require SSL credentials to establish a connection to the Managed Database.

                      Run the [Get managed PostgreSQL database credentials](https://techdocs.akamai.com/linode-api/reference/get-databases-postgre-sql-instance-credentials) operation for access information.
                    example: true
                    default: true
                  status:
                    type: string
                    description: |
                      __Read-only__ The operating status of the Managed Database.
                    example: active
                    readOnly: true
                    enum:
                    - provisioning
                    - active
                    - suspending
                    - suspended
                    - resuming
                    - restoring
                    - failed
                    - degraded
                    - updating
                    - backing_up
                    x-linode-cli-color:
                      active: green
                      default_: white
                      degraded: red
                      failed: red
                      provisioning: yellow
                      restoring: yellow
                      resuming: yellow
                    x-linode-cli-display: 100
                    x-linode-filterable: true
                  total_disk_size_gb:
                    type: integer
                    description: |
                      The total disk size of the database in GB.
                    example: 15
                    x-linode-cli-display: 8
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Managed Database was last updated.
                    example: '2022-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  updates:
                    type: object
                    description: |
                      Configuration settings for automated patch update maintenance for the Managed Database.
                    additionalProperties: false
                    properties:
                      day_of_week:
                        type: integer
                        description: |
                          The day to perform maintenance. 1=Monday, 2=Tuesday, etc.
                        example: 1
                        minimum: 1
                        maximum: 7
                      duration:
                        type: integer
                        description: |
                          The maximum maintenance window time in hours.
                        example: 3
                        minimum: 1
                        maximum: 3
                      frequency:
                        type: string
                        description: |
                          Whether maintenance occurs on a weekly or monthly basis.
                        example: weekly
                        default: weekly
                        enum:
                        - weekly
                        - monthly
                      hour_of_day:
                        type: integer
                        description: |
                          The hour to begin maintenance based in UTC time.
                        example: 0
                        minimum: 0
                        maximum: 23
                      week_of_month:
                        type: integer
                        description: |-
                          The week of the month to perform `monthly` frequency updates. Defaults to `null`.

                          - Required for `monthly` frequency updates.

                          - Must be `null` for `weekly` frequency updates.
                        example: null
                        nullable: true
                        minimum: 1
                        maximum: 4
                  used_disk_size_gb:
                    type: integer
                    description: |
                      The used space of the database in GB.
                    example: 2
                    x-linode-cli-display: 9
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-databases-postgre-sql-instance
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                  "label": "example-db",
                  "allow_list": [
                    "203.0.113.1",
                    "192.0.1.0/24"
                  ],
                  "type": "g6-standard-1",
                  "updates": {
                    "frequency": "monthly",
                    "duration": 3,
                    "hour_of_day": 12,
                    "day_of_week": 4,
                    "week_of_month": 3
                  }
              }' \
              https://api.linode.com/v4/databases/postgresql/instances/123
      - lang: CLI
        source: |-
          linode-cli databases postgresql-update 123 \
            --label example-db \
            --allow_list 203.0.113.1 \
            --allow_list 192.0.1.0/24 \
            --type g6-standard-1 \
            --updates.frequency monthly \
            --updates.duration 3 \
            --updates.hour_of_day 12 \
            --updates.day_of_week 4 \
            --updates.week_of_month 3
      x-linode-cli-action: postgresql-update
      x-linode-grant: read_write
      x-original-op-id: putDatabasesPostgreSQLInstance
      x-original-op-title: Managed PostgreSQL Database
        Update
    delete:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: delete-databases-postgre-sql-instance
      summary: Delete a managed PostgreSQL database
      tags:
      - Instances
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Remove a Managed PostgreSQL Database from your Account.

        Requires `read_write` access to the Database.

        The Database must have an `active`, `failed`, or `degraded` status to perform this operation.

        Only unrestricted Users can access this operation, and have access regardless of the acting token's OAuth scopes.


        ---


        - __CLI__.

            ```
            linode-cli databases postgresql-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases postgresql-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Managed PostgreSQL Database successfully deleted.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-databases-postgre-sql-instance
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/databases/postgresql/instances/123
      - lang: CLI
        source: linode-cli databases postgresql-delete 123
      x-linode-cli-action: postgresql-delete
      x-linode-grant: read_write
      x-original-op-id: deleteDatabasesPostgreSQLInstance
      x-original-op-title: Managed PostgreSQL Database
        Delete
    x-linode-cli-command: databases
  /databases/postgresql/instances/{instanceId}/backups:
    x-akamai:
      file-path: paths/postgresql-backups.yaml
      path-info: /databases/postgresql/instances/{instanceId}/backups
    parameters:
    - name: instanceId
      in: path
      description: |
        The ID of the Managed PostgreSQL Database.
      example: '{{instanceId}}'
      x-akamai:
        file-path: parameters/instance-id-path.yaml
      required: true
      schema:
        type: integer
    post:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: post-databases-postgre-sql-instance-backup
      summary: Create a managed PostgreSQL database backup snapshot
      tags:
      - Backups
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Creates a snapshot backup of a Managed PostgreSQL Database.

        Requires `read_write` access to the Database.

        Up to 3 snapshot backups for each Database can be stored at a time. If 3 snapshots have been created for a Database, one must be deleted before another can be made.

        Backups generated by this operation have the type `snapshot`. Snapshot backups may take several minutes to complete, after which they will be accessible to view or restore.

        The Database must have an `active` status to perform this operation. If another backup is in progress, it must complete before a new backup can be initiated.


        ---


        - __CLI__.

            ```
            linode-cli databases postgresql-backup-snapshot
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases postgresql-backup-snapshot
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the snapshot backup to create.
        content:
          application/json:
            schema:
              type: object
              description: |
                Managed Database request object for snapshot backup.
              x-akamai:
                file-path: schemas/database-backup-snapshot.yaml
              additionalProperties: false
              required:
              - label
              properties:
                label:
                  type: string
                  description: |-
                    The label for the Database snapshot backup.

                    - Can only contain ASCII letters, numbers, and underscores (`_`).
                    - Must be unique among other backup labels for this Database.
                  example: '{{label}}'
                  minLength: 1
                  maxLength: 30
                target:
                  type: string
                  description: |
                    The Database cluster target. If the Database is a high availability cluster, choosing
                    `secondary` creates a snapshot backup of a replica node.
                  example: '{{target}}'
                  default: primary
                  enum:
                  - primary
                  - secondary
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Database snapshot backup request successful.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-databases-postgre-sql-instance-backup
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -H "Content-Type: application/json" \
              -X POST -d '{
                  "label": "snapshot1",
                  "target": "primary"
              }' \
              https://api.linode.com/v4/databases/postgresql/instances/123/backups/
      - lang: CLI
        source: |-
          linode-cli databases postgresql-backup-snapshot 123 \
            --label snapshot1 \
            --target primary
      x-linode-cli-action: postgresql-backup-snapshot
      x-linode-grant: read_write
      x-original-op-id: postDatabasesPostgreSQLInstanceBackup
      x-original-op-title: Managed PostgreSQL Database
        Backup Snapshot Create
    get:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: get-databases-postgre-sql-instance-backups
      summary: List managed PostgreSQL database backups
      tags:
      - Backups
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Display all backups for an accessible Managed PostgreSQL Database.

        The Database must not be provisioning to perform this operation.

        Database `auto` type backups are created every 24 hours at 0:00 UTC. Each `auto` backup is retained for 7 days.

        Database `snapshot` type backups are created by accessing the [Create a managed PostgreSQL database backup snapshot](https://techdocs.akamai.com/linode-api/reference/post-databases-postgre-sql-instance-backup) operation.


        ---


        - __CLI__.

            ```
            linode-cli databases postgresql-backups-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases postgresql-backups-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated list of backups for the Managed PostgreSQL Database.
          content:
            application/json:
              schema:
                allOf:
                - type: object
                  description: |
                    An envelope for paginated response. When accessing a collection through a GET endpoint,
                    the results are wrapped in this envelope which includes metadata
                    about those results. Results are presented within a `data` array.
                    See [Pagination](https://techdocs.akamai.com/linode-api/reference/pagination)
                    for more information.
                  x-akamai:
                    file-path: schemas/pagination-envelope.yaml
                  additionalProperties: false
                  properties:
                    page:
                      type: integer
                      description: |
                        __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    pages:
                      type: integer
                      description: |
                        __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    results:
                      type: integer
                      description: |
                        __Read-only__ The total number of results.
                      example: 1
                      readOnly: true
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        type: object
                        description: |
                          A database backup object.
                        x-akamai:
                          file-path: schemas/database-backup.yaml
                        additionalProperties: false
                        properties:
                          id:
                            type: integer
                            description: |
                              The ID of the database backup object.
                            example: 123
                            x-linode-cli-display: 1
                          type:
                            type: string
                            description: |
                              The type of database backup, determined by how the backup was created.
                            example: auto
                            enum:
                            - snapshot
                            - auto
                            x-linode-cli-display: 3
                            x-linode-filterable: true
                          created:
                            type: string
                            description: |
                              A time value given in a combined date and time format that represents
                              when the database backup was created.
                            example: '2022-01-01T00:01:01'
                            format: date-time
                            x-linode-cli-display: 4
                            x-linode-filterable: true
                          label:
                            type: string
                            description: |-
                              The database backup's label, for display purposes only.

                              Must include only ASCII letters or numbers.
                            example: Scheduled - 02/04/22 11:11 UTC-XcCRmI
                            maxLength: 30
                            x-linode-cli-display: 2
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-databases-postgre-sql-instance-backups
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/databases/postgresql/instances/123/backups
      - lang: CLI
        source: linode-cli databases postgresql-backups-list 123
      x-linode-cli-action: postgresql-backups-list
      x-linode-grant: read_only
      x-original-op-id: getDatabasesPostgreSQLInstanceBackups
      x-original-op-title: Managed PostgreSQL Database
        Backups List
    x-linode-cli-command: databases
  /databases/postgresql/instances/{instanceId}/backups/{backupId}:
    x-akamai:
      file-path: paths/postgresql-backup.yaml
      path-info: /databases/postgresql/instances/{instanceId}/backups/{backupId}
    parameters:
    - name: instanceId
      in: path
      description: |
        The ID of the Managed PostgreSQL Database.
      example: '{{instanceId}}'
      x-akamai:
        file-path: parameters/instance-id-path.yaml
      required: true
      schema:
        type: integer
    - name: backupId
      in: path
      description: |
        The ID of the Managed PostgreSQL Database backup.
      example: '{{backupId}}'
      x-akamai:
        file-path: parameters/backup-id-path-85b1bbbf.yaml
      required: true
      schema:
        type: integer
    get:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: get-databases-postgresql-instance-backup
      summary: Get a managed PostgreSQL database backup
      tags:
      - Backups
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Display information for a single backup for an accessible Managed PostgreSQL Database.

        The Database must not be provisioning to perform this operation.


        ---


        - __CLI__.

            ```
            linode-cli databases postgresql-backup-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases postgresql-backup-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a single backup for the Managed PostgreSQL Database.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A database backup object.
                x-akamai:
                  file-path: schemas/database-backup.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      The ID of the database backup object.
                    example: 123
                    x-linode-cli-display: 1
                  type:
                    type: string
                    description: |
                      The type of database backup, determined by how the backup was created.
                    example: auto
                    enum:
                    - snapshot
                    - auto
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      A time value given in a combined date and time format that represents when the
                      database backup was created.
                    example: '2022-01-01T00:01:01'
                    format: date-time
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  label:
                    type: string
                    description: |-
                      The database backup's label, for display purposes only.

                      Must include only ASCII letters or numbers.
                    example: Scheduled - 02/04/22 11:11 UTC-XcCRmI
                    maxLength: 30
                    x-linode-cli-display: 2
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-databases-postgresql-instance-backup
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/databases/postgresql/instances/123/backups/456
      - lang: CLI
        source: linode-cli databases postgresql-backup-view 123 456
      x-linode-cli-action: postgresql-backup-view
      x-linode-grant: read_only
      x-original-op-id: getDatabasesPostgreSQLInstanceBackup
      x-original-op-title: Managed PostgreSQL Database
        Backup View
    delete:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: delete-database-postgre-sql-instance-backup
      summary: Delete a managed PostgreSQL database backup
      tags:
      - Backups
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Delete a single backup for an accessible Managed PostgreSQL Database.

        Requires `read_write` access to the Database.

        The Database must not be provisioning to perform this operation.


        ---


        - __CLI__.

            ```
            linode-cli databases postgresql-backup-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases postgresql-backup-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Request to delete Database backup successful.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-database-postgre-sql-instance-backup
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/databases/postgresql/instances/123/backups/456
      - lang: CLI
        source: linode-cli databases postgresql-backup-delete 123 456
      x-linode-cli-action: postgresql-backup-delete
      x-original-op-id: deleteDatabasePostgreSQLInstanceBackup
      x-original-op-title: Managed PostgreSQL Database
        Backup Delete
    x-linode-cli-command: databases
  /databases/postgresql/instances/{instanceId}/backups/{backupId}/restore:
    x-akamai:
      file-path: paths/postgresql-restore.yaml
      path-info: /databases/postgresql/instances/{instanceId}/backups/{backupId}/restore
    parameters:
    - name: instanceId
      in: path
      description: |
        The ID of the Managed PostgreSQL Database.
      example: '{{instanceId}}'
      x-akamai:
        file-path: parameters/instance-id-path-9f3ead4a.yaml
      required: true
      schema:
        type: integer
    - name: backupId
      in: path
      description: |
        The ID of the Managed PostgreSQL Database backup.
      example: '{{backupId}}'
      x-akamai:
        file-path: parameters/backup-id-path-85b1bbbf.yaml
      required: true
      schema:
        type: integer
    post:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: post-databases-postgre-sql-instance-backup-restore
      summary: Restore a managed PostgreSQL database backup
      tags:
      - Backups
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Restore a backup to a Managed PostgreSQL Database on your Account.

        Requires `read_write` access to the Database.

        The Database must have an `active`, `degraded`, or `failed` status to perform this operation.

        __Note__. Restoring from a backup will erase all existing data on the database instance and replace it with backup data.

        __Note__. Currently, restoring a backup after resetting Managed Database credentials results in a failed cluster. Please contact Customer Support if this occurs.


        ---


        - __CLI__.

            ```
            linode-cli databases postgresql-backup-restore
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases postgresql-backup-restore
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Request to restore backup successful.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-databases-postgre-sql-instance-backup-restore
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X POST https://api.linode.com/v4/databases/postgresql/instances/123/backups/456/restore
      - lang: CLI
        source: linode-cli databases postgresql-backup-restore 123 456
      x-linode-cli-action: postgresql-backup-restore
      x-linode-grant: read_write
      x-original-op-id: postDatabasesPostgreSQLInstanceBackupRestore
      x-original-op-title: Managed PostgreSQL Database
        Backup Restore
    x-linode-cli-command: databases
  /databases/postgresql/instances/{instanceId}/credentials:
    x-akamai:
      file-path: paths/postgresql-credentials.yaml
      path-info: /databases/postgresql/instances/{instanceId}/credentials
    parameters:
    - name: instanceId
      in: path
      description: |
        The ID of the Managed PostgreSQL Database.
      example: '{{instanceId}}'
      x-akamai:
        file-path: parameters/instance-id-path.yaml
      required: true
      schema:
        type: integer
    get:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: get-databases-postgre-sql-instance-credentials
      summary: Get managed PostgreSQL database credentials
      tags:
      - Instances
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Display the root username and password for an accessible Managed PostgreSQL Database.

        The Database must have an `active` status to perform this operation.


        ---


        - __CLI__.

            ```
            linode-cli databases postgresql-creds-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases postgresql-creds-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Managed Database root username and password.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Managed Database object for database credentials.
                x-akamai:
                  file-path: schemas/database-credentials.yaml
                additionalProperties: false
                properties:
                  password:
                    type: string
                    description: |
                      __Read-only__ The randomly-generated root password for the Managed Database instance.
                    example: s3cur3P@ssw0rd
                    readOnly: true
                    x-linode-cli-display: 2
                  username:
                    type: string
                    description: |
                      __Read-only__ The root username for the Managed Database instance.
                    example: linroot
                    readOnly: true
                    x-linode-cli-display: 1
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-databases-postgre-sql-instance-credentials
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/databases/postgresql/instances/123/credentials/
      - lang: CLI
        source: linode-cli databases postgresql-creds-view 123
      x-linode-cli-action: postgresql-creds-view
      x-linode-grant: read_only
      x-original-op-id: getDatabasesPostgreSQLInstanceCredentials
      x-original-op-title: Managed PostgreSQL Database
        Credentials View
    x-linode-cli-command: databases
  /databases/postgresql/instances/{instanceId}/credentials/reset:
    x-akamai:
      file-path: paths/postgresql-reset.yaml
      path-info: /databases/postgresql/instances/{instanceId}/credentials/reset
    parameters:
    - name: instanceId
      in: path
      description: |
        The ID of the Managed PostgreSQL Database.
      example: '{{instanceId}}'
      x-akamai:
        file-path: parameters/instance-id-path-9f3ead4a.yaml
      required: true
      schema:
        type: integer
    post:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: post-databases-postgre-sql-instance-credentials-reset
      summary: Reset managed PostgreSQL database credentials
      tags:
      - Credentials
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Reset the root password for a Managed PostgreSQL Database.

        Requires `read_write` access to the Database.

        A new root password is randomly generated and accessible with the [Get managed PostgreSQL database credentials](https://techdocs.akamai.com/linode-api/reference/get-databases-postgre-sql-instance-credentials) operation.

        Only unrestricted Users can access this operation, and have access regardless of the acting token's OAuth scopes.

        __Note__. Note that it may take several seconds for credentials to reset.


        ---


        - __CLI__.

            ```
            linode-cli databases postgresql-creds-reset
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases postgresql-creds-reset
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Managed Database instance credentials successfully reset.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-databases-postgre-sql-instance-credentials-reset
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X POST https://api.linode.com/v4/databases/postgresql/instances/123/credentials/reset
      - lang: CLI
        source: linode-cli databases postgresql-creds-reset 123
      x-linode-cli-action: postgresql-creds-reset
      x-linode-grant: read_write
      x-original-op-id: postDatabasesPostgreSQLInstanceCredentialsReset
      x-original-op-title: Managed PostgreSQL Database
        Credentials Reset
    x-linode-cli-command: databases
  /databases/postgresql/instances/{instanceId}/patch:
    x-akamai:
      file-path: paths/postgresql-patch.yaml
      path-info: /databases/postgresql/instances/{instanceId}/patch
    parameters:
    - name: instanceId
      in: path
      description: |
        The ID of the Managed PostgreSQL Database.
      example: '{{instanceId}}'
      x-akamai:
        file-path: parameters/instance-id-path.yaml
      required: true
      schema:
        type: integer
    post:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: post-databases-postgre-sql-instance-patch
      summary: Patch a managed PostgreSQL database
      tags:
      - Instances
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Apply security patches and updates to the underlying operating system of the Managed PostgreSQL Database. This function runs during regular maintenance windows, which are configurable with the [Update a managed PostgreSQL database](https://techdocs.akamai.com/linode-api/reference/put-databases-postgre-sql-instance) operation.

        Requires `read_write` access to the Database.

        The Database must have an `active` status to perform this operation.

        __NOTE__

        - If your database cluster is configured with a single node, you will experience downtime during this maintenance. Consider upgrading to a high availability plan to avoid any downtime due to maintenance.

        - __The database software is not updated automatically.__ To upgrade to a new database engine version, consider deploying a new Managed Database with your preferred version. You can then migrate your databases from the original Managed Database cluster to the new one.


        ---


        - __CLI__.

            ```
            linode-cli databases postgresql-patch
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases postgresql-patch
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Managed Database instance patch request successful.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-databases-postgre-sql-instance-patch
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X POST https://api.linode.com/v4/databases/postgresql/instances/123/patch
      - lang: CLI
        source: linode-cli databases postgresql-patch 123
      x-linode-cli-action: postgresql-patch
      x-linode-grant: read_write
      x-original-op-id: postDatabasesPostgreSQLInstancePatch
      x-original-op-title: Managed PostgreSQL Database
        Patch
    x-linode-cli-command: databases
  /databases/postgresql/instances/{instanceId}/ssl:
    x-akamai:
      file-path: paths/postgresql-ssl.yaml
      path-info: /databases/postgresql/instances/{instanceId}/ssl
    parameters:
    - name: instanceId
      in: path
      description: |
        The ID of the Managed PostgreSQL Database.
      example: '{{instanceId}}'
      x-akamai:
        file-path: parameters/instance-id-path-9f3ead4a.yaml
      required: true
      schema:
        type: integer
    get:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: get-databases-postgresql-instance-ssl
      summary: Get a managed PostgreSQL database SSL certificate
      tags:
      - SSL certificates
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Display the SSL CA certificate for an accessible Managed PostgreSQL Database.

        The Database must have an `active` status to perform this operation.


        ---


        - __CLI__.

            ```
            linode-cli databases postgresql-ssl-cert
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            databases:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli databases postgresql-ssl-cert
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: databases:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns the SSL CA certificate of a single Managed PostgreSQL Database.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Managed Database SSL object.
                x-akamai:
                  file-path: schemas/database-ssl.yaml
                additionalProperties: false
                properties:
                  ca_certificate:
                    type: string
                    description: |
                      The base64-encoded SSL CA certificate for the Managed Database instance.
                    example: LS0tLS1CRUdJ...==
                    format: base64
                    x-linode-cli-display: 1
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-databases-postgresql-instance-ssl
      security:
      - personalAccessToken: []
      - oauth:
        - databases:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/databases/postgresql/instances/123/ssl
      - lang: CLI
        source: linode-cli databases postgresql-ssl-cert 123
      x-linode-cli-action: postgresql-ssl-cert
      x-linode-grant: read_only
      x-original-op-id: getDatabasesPostgreSQLInstanceSSL
      x-original-op-title: Managed PostgreSQL Database
        SSL Certificate View
    x-linode-cli-command: databases
  /databases/types:
    x-akamai:
      file-path: paths/database-types.yaml
      path-info: /databases/types
    get:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: get-databases-types
      summary: List managed database types
      tags:
      - Types
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Display all Managed Database node types. The type and number of nodes determine the resources and price of a Managed Database instance.

        Each Managed Database can have one node type. In the case of a high availability Database, all nodes are provisioned according to the chosen type.
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated list of all Managed Database types.
          content:
            application/json:
              schema:
                allOf:
                - type: object
                  description: |
                    An envelope for paginated response. When accessing a collection through a GET endpoint,
                    the results are wrapped in this envelope which includes metadata
                    about those results. Results are presented within a `data` array.
                    See [Pagination](https://techdocs.akamai.com/linode-api/reference/pagination)
                    for more information.
                  x-akamai:
                    file-path: schemas/pagination-envelope.yaml
                  additionalProperties: false
                  properties:
                    page:
                      type: integer
                      description: |
                        __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    pages:
                      type: integer
                      description: |
                        __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    results:
                      type: integer
                      description: |
                        __Read-only__ The total number of results.
                      example: 1
                      readOnly: true
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        type: object
                        description: |
                          Managed Database plan type object.
                        x-akamai:
                          file-path: schemas/database-type.yaml
                        additionalProperties: false
                        properties:
                          id:
                            type: string
                            description: |
                              __Read-only__ The ID representing the Managed Database node plan type.
                            example: g6-nanode-1
                            readOnly: true
                            x-linode-cli-display: 1
                          class:
                            type: string
                            description: |
                              The compute class category.
                            example: nanode
                          deprecated:
                            type: boolean
                            description: |
                              Whether this Database plan type has been deprecated and is no longer
                              available.
                            example: false
                            x-linode-cli-display: 6
                            x-linode-filterable: true
                          disk:
                            type: integer
                            description: |
                              The amount of disk space set aside for Databases of this plan type.
                              The value is represented in megabytes.
                            example: 25600
                            x-linode-cli-display: 4
                          engines:
                            type: object
                            additionalProperties: false
                            properties:
                              mysql:
                                type: array
                                description: |
                                  Pricing details for MySQL Managed Databases.
                                items:
                                  type: object
                                  x-akamai:
                                    file-path: schemas/database-type-engine.yaml
                                  additionalProperties: false
                                  properties:
                                    price:
                                      type: object
                                      description: |
                                        Cost in US dollars, broken down into hourly and
                                        monthly charges.
                                      additionalProperties: false
                                      properties:
                                        hourly:
                                          type: number
                                          description: |
                                            Cost (in US dollars) per hour for this subscription
                                            tier.
                                          example: 0.03
                                        monthly:
                                          type: number
                                          description: |
                                            Maximum cost (in US dollars) per month for this
                                            subscription tier.
                                          example: 20
                                    quantity:
                                      type: integer
                                      description: |
                                        The number of nodes for the Managed Database cluster
                                        for this subscription tier.
                                      example: 1
                                      enum:
                                      - 1
                                      - 2
                                      - 3
                              postgresql:
                                type: array
                                description: |
                                  Pricing details for PostgreSQL Managed Databases.
                                items:
                                  type: object
                                  x-akamai:
                                    file-path: schemas/database-type-engine.yaml
                                  additionalProperties: false
                                  properties:
                                    price:
                                      type: object
                                      description: |
                                        Cost in US dollars, broken down into hourly and
                                        monthly charges.
                                      additionalProperties: false
                                      properties:
                                        hourly:
                                          type: number
                                          description: |
                                            Cost (in US dollars) per hour for this subscription
                                            tier.
                                          example: 0.03
                                        monthly:
                                          type: number
                                          description: |
                                            Maximum cost (in US dollars) per month for this
                                            subscription tier.
                                          example: 20
                                    quantity:
                                      type: integer
                                      description: |
                                        The number of nodes for the Managed Database cluster
                                        for this subscription tier.
                                      example: 1
                                      enum:
                                      - 1
                                      - 2
                                      - 3
                          label:
                            type: string
                            description: |
                              __Read-only__ A human-readable string that describes each plan type.
                              For display purposes only.
                            example: DBaaS - Nanode 1GB
                            readOnly: true
                            x-linode-cli-display: 2
                          memory:
                            type: integer
                            description: |
                              The amount of RAM allocated to Database created of this plan type.
                              The value is represented in megabytes.
                            example: 1024
                            x-linode-cli-display: 3
                          vcpus:
                            type: integer
                            description: |
                              The integer of number CPUs allocated to databases of this plan type.
                            example: 1
                            x-linode-cli-display: 5
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
              x-linode-cli-nested-list: engines.mysql,
                engines.postgresql
              x-linode-cli-use-schema:
                type: object
                additionalProperties: false
                properties:
                  id:
                    x-linode-cli-display: 1
                  _split:
                    x-linode-cli-display: 2.5
                  engines:
                    type: object
                    additionalProperties: false
                    properties:
                      price:
                        type: object
                        additionalProperties: false
                        properties:
                          hourly:
                            x-linode-cli-display: 4
                          monthly:
                            x-linode-cli-display: 5
                      quantity:
                        x-linode-cli-display: 3
                  label:
                    x-linode-cli-display: 2
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-databases-types
      x-code-samples:
      - lang: Shell
        source: curl https://api.linode.com/v4/databases/types
      - lang: CLI
        source: linode-cli databases types
      x-linode-cli-action: types
      x-original-op-id: getDatabasesTypes
      x-original-op-title: Managed Database Types List
    x-linode-cli-command: databases
  /databases/types/{typeId}:
    x-akamai:
      file-path: paths/database-type.yaml
      path-info: /databases/types/{typeId}
    parameters:
    - name: typeId
      in: path
      description: |
        The ID of the Managed Database type.
      example: '{{typeId}}'
      x-akamai:
        file-path: parameters/type-id-path-024e328f.yaml
      required: true
      schema:
        type: string
    get:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: get-databases-type
      summary: Get a managed database type
      tags:
      - Types
      description: |-
        __This operation is currently only available for customers who already have an active Managed Database.__

        Display the details of a single Managed Database type. The type and number of nodes determine the resources and price of a Managed Database instance.
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a single Managed Database type.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Managed Database plan type object.
                x-akamai:
                  file-path: schemas/database-type.yaml
                additionalProperties: false
                properties:
                  id:
                    type: string
                    description: |
                      __Read-only__ The ID representing the Managed Database node plan type.
                    example: g6-nanode-1
                    readOnly: true
                    x-linode-cli-display: 1
                  class:
                    type: string
                    description: |
                      The compute class category.
                    example: nanode
                  deprecated:
                    type: boolean
                    description: |
                      Whether this Database plan type has been deprecated and is no longer available.
                    example: false
                    x-linode-cli-display: 6
                    x-linode-filterable: true
                  disk:
                    type: integer
                    description: |
                      The amount of disk space set aside for Databases of this plan type. The value is
                      represented in megabytes.
                    example: 25600
                    x-linode-cli-display: 4
                  engines:
                    type: object
                    additionalProperties: false
                    properties:
                      mysql:
                        type: array
                        description: |
                          Pricing details for MySQL Managed Databases.
                        items:
                          type: object
                          x-akamai:
                            file-path: schemas/database-type-engine.yaml
                          additionalProperties: false
                          properties:
                            price:
                              type: object
                              description: |
                                Cost in US dollars, broken down into hourly and monthly charges.
                              additionalProperties: false
                              properties:
                                hourly:
                                  type: number
                                  description: |
                                    Cost (in US dollars) per hour for this subscription
                                    tier.
                                  example: 0.03
                                monthly:
                                  type: number
                                  description: |
                                    Maximum cost (in US dollars) per month for this subscription
                                    tier.
                                  example: 20
                            quantity:
                              type: integer
                              description: |
                                The number of nodes for the Managed Database cluster for this
                                subscription tier.
                              example: 1
                              enum:
                              - 1
                              - 2
                              - 3
                      postgresql:
                        type: array
                        description: |
                          Pricing details for PostgreSQL Managed Databases.
                        items:
                          type: object
                          x-akamai:
                            file-path: schemas/database-type-engine.yaml
                          additionalProperties: false
                          properties:
                            price:
                              type: object
                              description: |
                                Cost in US dollars, broken down into hourly and monthly charges.
                              additionalProperties: false
                              properties:
                                hourly:
                                  type: number
                                  description: |
                                    Cost (in US dollars) per hour for this subscription
                                    tier.
                                  example: 0.03
                                monthly:
                                  type: number
                                  description: |
                                    Maximum cost (in US dollars) per month for this subscription
                                    tier.
                                  example: 20
                            quantity:
                              type: integer
                              description: |
                                The number of nodes for the Managed Database cluster for this
                                subscription tier.
                              example: 1
                              enum:
                              - 1
                              - 2
                              - 3
                  label:
                    type: string
                    description: |
                      __Read-only__ A human-readable string that describes each plan type. For display purposes only.
                    example: DBaaS - Nanode 1GB
                    readOnly: true
                    x-linode-cli-display: 2
                  memory:
                    type: integer
                    description: |
                      The amount of RAM allocated to Database created of this plan type. The value is represented
                      in megabytes.
                    example: 1024
                    x-linode-cli-display: 3
                  vcpus:
                    type: integer
                    description: |
                      The integer of number CPUs allocated to databases of this plan type.
                    example: 1
                    x-linode-cli-display: 5
              x-example:
                x-ref: ../examples/tbd.json
              x-linode-cli-nested-list: engines.mysql,
                engines.postgresql
              x-linode-cli-use-schema:
                type: object
                additionalProperties: false
                properties:
                  id:
                    x-linode-cli-display: 1
                  _split:
                    x-linode-cli-display: 2.5
                  engines:
                    type: object
                    additionalProperties: false
                    properties:
                      price:
                        type: object
                        additionalProperties: false
                        properties:
                          hourly:
                            x-linode-cli-display: 4
                          monthly:
                            x-linode-cli-display: 5
                      quantity:
                        x-linode-cli-display: 3
                  label:
                    x-linode-cli-display: 2
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-databases-type
      x-code-samples:
      - lang: Shell
        source: curl https://api.linode.com/v4/databases/types/g6-nanode-1
      - lang: CLI
        source: linode-cli databases type-view g6-nanode-1
      x-linode-cli-action: type-view
      x-original-op-id: getDatabasesType
      x-original-op-title: Managed Database Type View
    x-linode-cli-command: databases
  /domains:
    x-akamai:
      file-path: paths/domains.yaml
      path-info: /domains
    post:
      operationId: post-domain
      summary: Create a domain
      tags:
      - Domains
      description: |-
        Adds a new Domain to Linode's DNS Manager. Linode is not a registrar, and you must own the domain before adding it here. Be sure to point your registrar to Linode's nameservers so that the records hosted here are used.


        ---


        - __CLI__.

            ```
            linode-cli domains create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            domains:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli domains create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: domains:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the domain you are registering.
        required: true
        content:
          application/json:
            schema:
              required:
              - domain
              - type
              allOf:
              - type: object
                description: |
                  A domain zonefile in our DNS system.  You must own the domain name and tell your registrar
                  to use Linode's nameservers in order for a domain in our system
                  to be treated as authoritative.
                x-akamai:
                  file-path: schemas/domain.yaml
                title: Domain
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Domain's unique ID
                    example: 1234
                    readOnly: true
                    x-linode-cli-display: 1
                  tags:
                    type: array
                    description: |
                      An array of tags applied to this object.  Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  type:
                    type: string
                    description: |
                      Whether this Domain represents the authoritative source of information for the domain
                      it describes ("master"), or whether it is a read-only copy of
                      a master ("slave").
                    example: master
                    enum:
                    - master
                    - slave
                    x-linode-cli-display: 3
                  description:
                    type: string
                    description: |
                      A description for this Domain. This is for display purposes only.
                    example: null
                    minLength: 1
                    maxLength: 253
                  axfr_ips:
                    type: array
                    description: |-
                      The list of IPs that may perform a zone transfer for this Domain. The total combined length of all data within this array cannot exceed 1000 characters.

                      __Note__: This is potentially dangerous, and should be set to an empty list unless you intend to use it.
                    example: []
                    items:
                      type: string
                      format: ip
                  domain:
                    type: string
                    description: |
                      The domain this Domain represents. Domain labels cannot be longer than 63 characters
                      and must conform to [RFC1035](https://tools.ietf.org/html/rfc1035).
                      Domains must be unique on Linode's platform, including across
                      different Linode accounts; there cannot be two Domains representing
                      the same domain.
                    example: example.org
                    minLength: 1
                    maxLength: 253
                    pattern: ^(\*\.)?([a-zA-Z0-9-_]{1,63}\.)+([a-zA-Z]{2,3}\.)?([a-zA-Z]{2,16}|xn--[a-zA-Z0-9]+)$
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  expire_sec:
                    type: integer
                    description: |-
                      The amount of time in seconds that may pass before this Domain is no longer authoritative.

                      - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                      - Any other value is rounded up to the nearest valid value.

                      - A value of 0 is equivalent to the default value of 1209600.
                    example: 300
                    default: 0
                  group:
                    type: string
                    description: |
                      The group this Domain belongs to.  This is for display purposes only.
                    example: null
                    minLength: 1
                    maxLength: 50
                    deprecated: true
                    x-linode-filterable: true
                  master_ips:
                    type: array
                    description: |
                      The IP addresses representing the master DNS for this Domain. At least one value is required
                      for `type` slave Domains. The total combined length of all data
                      within this array cannot exceed 1000 characters.
                    example: []
                    items:
                      type: string
                      format: ip
                  refresh_sec:
                    type: integer
                    description: |-
                      The amount of time in seconds before this Domain should be refreshed.

                      - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                      - Any other value is rounded up to the nearest valid value.

                      - A value of 0 is equivalent to the default value of 14400.
                    example: 300
                    default: 0
                  retry_sec:
                    type: integer
                    description: |-
                      The interval, in seconds, at which a failed refresh should be retried.

                      - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                      - Any other value is rounded up to the nearest valid value.

                      - A value of 0 is equivalent to the default value of 14400.
                    example: 300
                    default: 0
                  soa_email:
                    type: string
                    description: |
                      Start of Authority email address. This is required for `type` master Domains.
                    example: admin@example.org
                    format: email
                    x-linode-cli-display: 5
                  status:
                    type: string
                    description: |
                      Used to control whether this Domain is currently being rendered.
                    example: active
                    default: active
                    enum:
                    - disabled
                    - active
                    x-linode-cli-color:
                      active: green
                      default_: red
                      disabled: yellow
                      edit_mode: yellow
                    x-linode-cli-display: 4
                  ttl_sec:
                    type: integer
                    description: |-
                      "Time to Live" - the amount of time in seconds that this Domain's records may be cached by resolvers or other domain servers.

                      - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
                      - Any other value is rounded up to the nearest valid value.
                      - A value of 0 is equivalent to the default value of 86400.
                    example: 300
                    default: 0
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Domain added successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A domain zonefile in our DNS system.  You must own the domain name and tell your registrar
                  to use Linode's nameservers in order for a domain in our system
                  to be treated as authoritative.
                x-akamai:
                  file-path: schemas/domain.yaml
                title: Domain
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Domain's unique ID
                    example: 1234
                    readOnly: true
                    x-linode-cli-display: 1
                  tags:
                    type: array
                    description: |
                      An array of tags applied to this object.  Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  type:
                    type: string
                    description: |
                      Whether this Domain represents the authoritative source of information for the domain
                      it describes ("master"), or whether it is a read-only copy of
                      a master ("slave").
                    example: master
                    enum:
                    - master
                    - slave
                    x-linode-cli-display: 3
                  description:
                    type: string
                    description: |
                      A description for this Domain. This is for display purposes only.
                    example: null
                    minLength: 1
                    maxLength: 253
                  axfr_ips:
                    type: array
                    description: |-
                      The list of IPs that may perform a zone transfer for this Domain. The total combined length of all data within this array cannot exceed 1000 characters.

                      __Note__: This is potentially dangerous, and should be set to an empty list unless you intend to use it.
                    example: []
                    items:
                      type: string
                      format: ip
                  domain:
                    type: string
                    description: |
                      The domain this Domain represents. Domain labels cannot be longer than 63 characters
                      and must conform to [RFC1035](https://tools.ietf.org/html/rfc1035).
                      Domains must be unique on Linode's platform, including across
                      different Linode accounts; there cannot be two Domains representing
                      the same domain.
                    example: example.org
                    minLength: 1
                    maxLength: 253
                    pattern: ^(\*\.)?([a-zA-Z0-9-_]{1,63}\.)+([a-zA-Z]{2,3}\.)?([a-zA-Z]{2,16}|xn--[a-zA-Z0-9]+)$
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  expire_sec:
                    type: integer
                    description: |-
                      The amount of time in seconds that may pass before this Domain is no longer authoritative.

                      - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                      - Any other value is rounded up to the nearest valid value.

                      - A value of 0 is equivalent to the default value of 1209600.
                    example: 300
                    default: 0
                  group:
                    type: string
                    description: |
                      The group this Domain belongs to.  This is for display purposes only.
                    example: null
                    minLength: 1
                    maxLength: 50
                    deprecated: true
                    x-linode-filterable: true
                  master_ips:
                    type: array
                    description: |
                      The IP addresses representing the master DNS for this Domain. At least one value is required
                      for `type` slave Domains. The total combined length of all data
                      within this array cannot exceed 1000 characters.
                    example: []
                    items:
                      type: string
                      format: ip
                  refresh_sec:
                    type: integer
                    description: |-
                      The amount of time in seconds before this Domain should be refreshed.

                      - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                      - Any other value is rounded up to the nearest valid value.

                      - A value of 0 is equivalent to the default value of 14400.
                    example: 300
                    default: 0
                  retry_sec:
                    type: integer
                    description: |-
                      The interval, in seconds, at which a failed refresh should be retried.

                      - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                      - Any other value is rounded up to the nearest valid value.

                      - A value of 0 is equivalent to the default value of 14400.
                    example: 300
                    default: 0
                  soa_email:
                    type: string
                    description: |
                      Start of Authority email address. This is required for `type` master Domains.
                    example: admin@example.org
                    format: email
                    x-linode-cli-display: 5
                  status:
                    type: string
                    description: |
                      Used to control whether this Domain is currently being rendered.
                    example: active
                    default: active
                    enum:
                    - disabled
                    - active
                    x-linode-cli-color:
                      active: green
                      default_: red
                      disabled: yellow
                      edit_mode: yellow
                    x-linode-cli-display: 4
                  ttl_sec:
                    type: integer
                    description: |-
                      "Time to Live" - the amount of time in seconds that this Domain's records may be cached by resolvers or other domain servers.

                      - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
                      - Any other value is rounded up to the nearest valid value.
                      - A value of 0 is equivalent to the default value of 86400.
                    example: 300
                    default: 0
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-domain
      security:
      - personalAccessToken: []
      - oauth:
        - domains:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                  "domain": "example.com",
                  "type": "master",
                  "soa_email": "admin@example.com",
                  "description": "Example Description",
                  "refresh_sec": 14400,
                  "retry_sec": 3600,
                  "expire_sec": 604800,
                  "ttl_sec": 3600,
                  "status": "active",
                  "master_ips": ["127.0.0.1","255.255.255.1","123.123.123.7"],
                  "axfr_ips": ["44.55.66.77"],
                  "group": "Example Display Group",
                  "tags": ["tag1","tag2"]
              }' \
              https://api.linode.com/v4/domains
      - lang: CLI
        source: |-
          linode-cli domains create \
            --type master \
            --domain example.org \
            --soa_email admin@example.org
      x-linode-cli-action: create
      x-linode-grant: add_domains
      x-original-op-id: createDomain
      x-original-op-title: Domain Create
    get:
      operationId: get-domains
      summary: List domains
      tags:
      - Domains
      description: |-
        This is a collection of Domains that you have registered in Linode's DNS Manager.  Linode is not a registrar, and in order for these to work you must own the domains and point your registrar at Linode's nameservers.


        ---


        - __OAuth__.

            ```
            domains:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: domains:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of Domains you have registered.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        A domain zonefile in our DNS system.  You must own the domain name and tell
                        your registrar to use Linode's nameservers in order for a
                        domain in our system to be treated as authoritative.
                      x-akamai:
                        file-path: schemas/domain.yaml
                      title: Domain
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ This Domain's unique ID
                          example: 1234
                          readOnly: true
                          x-linode-cli-display: 1
                        tags:
                          type: array
                          description: |
                            An array of tags applied to this object.  Tags are for organizational purposes
                            only.
                          example:
                          - example tag
                          - another example
                          items:
                            type: string
                          x-linode-filterable: true
                        type:
                          type: string
                          description: |
                            Whether this Domain represents the authoritative source of information
                            for the domain it describes ("master"), or whether it
                            is a read-only copy of a master ("slave").
                          example: master
                          enum:
                          - master
                          - slave
                          x-linode-cli-display: 3
                        description:
                          type: string
                          description: |
                            A description for this Domain. This is for display purposes only.
                          example: null
                          minLength: 1
                          maxLength: 253
                        axfr_ips:
                          type: array
                          description: |-
                            The list of IPs that may perform a zone transfer for this Domain. The total combined length of all data within this array cannot exceed 1000 characters.

                            __Note__: This is potentially dangerous, and should be set to an empty list unless you intend to use it.
                          example: []
                          items:
                            type: string
                            format: ip
                        domain:
                          type: string
                          description: |
                            The domain this Domain represents. Domain labels cannot be longer than
                            63 characters and must conform to [RFC1035](https://tools.ietf.org/html/rfc1035).
                            Domains must be unique on Linode's platform, including
                            across different Linode accounts; there cannot be two
                            Domains representing the same domain.
                          example: example.org
                          minLength: 1
                          maxLength: 253
                          pattern: ^(\*\.)?([a-zA-Z0-9-_]{1,63}\.)+([a-zA-Z]{2,3}\.)?([a-zA-Z]{2,16}|xn--[a-zA-Z0-9]+)$
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        expire_sec:
                          type: integer
                          description: |-
                            The amount of time in seconds that may pass before this Domain is no longer authoritative.

                            - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                            - Any other value is rounded up to the nearest valid value.

                            - A value of 0 is equivalent to the default value of 1209600.
                          example: 300
                          default: 0
                        group:
                          type: string
                          description: |
                            The group this Domain belongs to.  This is for display purposes only.
                          example: null
                          minLength: 1
                          maxLength: 50
                          deprecated: true
                          x-linode-filterable: true
                        master_ips:
                          type: array
                          description: |
                            The IP addresses representing the master DNS for this Domain. At least
                            one value is required for `type` slave Domains. The total
                            combined length of all data within this array cannot exceed
                            1000 characters.
                          example: []
                          items:
                            type: string
                            format: ip
                        refresh_sec:
                          type: integer
                          description: |-
                            The amount of time in seconds before this Domain should be refreshed.

                            - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                            - Any other value is rounded up to the nearest valid value.

                            - A value of 0 is equivalent to the default value of 14400.
                          example: 300
                          default: 0
                        retry_sec:
                          type: integer
                          description: |-
                            The interval, in seconds, at which a failed refresh should be retried.

                            - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                            - Any other value is rounded up to the nearest valid value.

                            - A value of 0 is equivalent to the default value of 14400.
                          example: 300
                          default: 0
                        soa_email:
                          type: string
                          description: |
                            Start of Authority email address. This is required for `type` master Domains.
                          example: admin@example.org
                          format: email
                          x-linode-cli-display: 5
                        status:
                          type: string
                          description: |
                            Used to control whether this Domain is currently being rendered.
                          example: active
                          default: active
                          enum:
                          - disabled
                          - active
                          x-linode-cli-color:
                            active: green
                            default_: red
                            disabled: yellow
                            edit_mode: yellow
                          x-linode-cli-display: 4
                        ttl_sec:
                          type: integer
                          description: |-
                            "Time to Live" - the amount of time in seconds that this Domain's records may be cached by resolvers or other domain servers.

                            - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
                            - Any other value is rounded up to the nearest valid value.
                            - A value of 0 is equivalent to the default value of 86400.
                          example: 300
                          default: 0
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-domains
      security:
      - personalAccessToken: []
      - oauth:
        - domains:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/domains
      - lang: CLI
        source: linode-cli domains list
      x-linode-cli-action:
      - list
      - ls
      x-linode-grant: read_only
      x-original-op-id: getDomains
      x-original-op-title: Domains List
    x-linode-cli-command: domains
  /domains/import:
    x-akamai:
      file-path: paths/import.yaml
      path-info: /domains/import
    post:
      operationId: post-import-domain
      summary: Import a domain
      tags:
      - Domains
      description: |-
        Imports a domain zone from a remote nameserver. Your nameserver must allow zone transfers (AXFR) from the following IPs:

        - 96.126.114.97
        - 96.126.114.98
        - 2600:3c00::5e
        - 2600:3c00::5f


        ---


        - __CLI__.

            ```
            linode-cli domains import
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            domains:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli domains import
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: domains:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the Domain to import.
        content:
          application/json:
            schema:
              additionalProperties: false
              required:
              - domain
              - remote_nameserver
              properties:
                domain:
                  type: string
                  description: |
                    The domain to import.
                  example: example.com
                remote_nameserver:
                  type: string
                  description: |
                    The remote nameserver that allows zone transfers (AXFR).
                  example: examplenameserver.com
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A single Domain in Linode's DNS Manager.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A domain zonefile in our DNS system.  You must own the domain name and tell your registrar
                  to use Linode's nameservers in order for a domain in our system
                  to be treated as authoritative.
                x-akamai:
                  file-path: schemas/domain.yaml
                title: Domain
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Domain's unique ID
                    example: 1234
                    readOnly: true
                    x-linode-cli-display: 1
                  tags:
                    type: array
                    description: |
                      An array of tags applied to this object.  Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  type:
                    type: string
                    description: |
                      Whether this Domain represents the authoritative source of information for the domain
                      it describes ("master"), or whether it is a read-only copy of
                      a master ("slave").
                    example: master
                    enum:
                    - master
                    - slave
                    x-linode-cli-display: 3
                  description:
                    type: string
                    description: |
                      A description for this Domain. This is for display purposes only.
                    example: null
                    minLength: 1
                    maxLength: 253
                  axfr_ips:
                    type: array
                    description: |-
                      The list of IPs that may perform a zone transfer for this Domain. The total combined length of all data within this array cannot exceed 1000 characters.

                      __Note__: This is potentially dangerous, and should be set to an empty list unless you intend to use it.
                    example: []
                    items:
                      type: string
                      format: ip
                  domain:
                    type: string
                    description: |
                      The domain this Domain represents. Domain labels cannot be longer than 63 characters
                      and must conform to [RFC1035](https://tools.ietf.org/html/rfc1035).
                      Domains must be unique on Linode's platform, including across
                      different Linode accounts; there cannot be two Domains representing
                      the same domain.
                    example: example.org
                    minLength: 1
                    maxLength: 253
                    pattern: ^(\*\.)?([a-zA-Z0-9-_]{1,63}\.)+([a-zA-Z]{2,3}\.)?([a-zA-Z]{2,16}|xn--[a-zA-Z0-9]+)$
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  expire_sec:
                    type: integer
                    description: |-
                      The amount of time in seconds that may pass before this Domain is no longer authoritative.

                      - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                      - Any other value is rounded up to the nearest valid value.

                      - A value of 0 is equivalent to the default value of 1209600.
                    example: 300
                    default: 0
                  group:
                    type: string
                    description: |
                      The group this Domain belongs to.  This is for display purposes only.
                    example: null
                    minLength: 1
                    maxLength: 50
                    deprecated: true
                    x-linode-filterable: true
                  master_ips:
                    type: array
                    description: |
                      The IP addresses representing the master DNS for this Domain. At least one value is required
                      for `type` slave Domains. The total combined length of all data
                      within this array cannot exceed 1000 characters.
                    example: []
                    items:
                      type: string
                      format: ip
                  refresh_sec:
                    type: integer
                    description: |-
                      The amount of time in seconds before this Domain should be refreshed.

                      - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                      - Any other value is rounded up to the nearest valid value.

                      - A value of 0 is equivalent to the default value of 14400.
                    example: 300
                    default: 0
                  retry_sec:
                    type: integer
                    description: |-
                      The interval, in seconds, at which a failed refresh should be retried.

                      - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                      - Any other value is rounded up to the nearest valid value.

                      - A value of 0 is equivalent to the default value of 14400.
                    example: 300
                    default: 0
                  soa_email:
                    type: string
                    description: |
                      Start of Authority email address. This is required for `type` master Domains.
                    example: admin@example.org
                    format: email
                    x-linode-cli-display: 5
                  status:
                    type: string
                    description: |
                      Used to control whether this Domain is currently being rendered.
                    example: active
                    default: active
                    enum:
                    - disabled
                    - active
                    x-linode-cli-color:
                      active: green
                      default_: red
                      disabled: yellow
                      edit_mode: yellow
                    x-linode-cli-display: 4
                  ttl_sec:
                    type: integer
                    description: |-
                      "Time to Live" - the amount of time in seconds that this Domain's records may be cached by resolvers or other domain servers.

                      - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
                      - Any other value is rounded up to the nearest valid value.
                      - A value of 0 is equivalent to the default value of 86400.
                    example: 300
                    default: 0
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-import-domain
      security:
      - personalAccessToken: []
      - oauth:
        - domains:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "domain": "example.com",
                "remote_nameserver": "examplenameserver.com"
              }' \
              https://api.linode.com/v4/domains/import
      - lang: CLI
        source: linode-cli domains import --domain example.com --remote_nameserver examplenameserver.com
      x-linode-cli-action: import
      x-linode-cli-command: domains
      x-linode-grant: read_write
      x-original-op-id: importDomain
      x-original-op-title: Domain Import
    x-linode-cli-command: domains
  /domains/{domainId}:
    x-akamai:
      file-path: paths/domain.yaml
      path-info: /domains/{domainId}
    parameters:
    - name: domainId
      in: path
      description: |
        The ID of the Domain to access.
      example: '{{domainId}}'
      x-akamai:
        file-path: parameters/domain-id-path-6aab9d6d.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-domain
      summary: Get a domain
      tags:
      - Domains
      description: |-
        This is a single Domain that you have registered in Linode's DNS Manager. Linode is not a registrar, and in order for this Domain record to work you must own the domain and point your registrar at Linode's nameservers.


        ---


        - __CLI__.

            ```
            linode-cli domains view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            domains:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli domains view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: domains:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A single Domain in Linode's DNS Manager.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A domain zonefile in our DNS system.  You must own the domain name and tell your registrar
                  to use Linode's nameservers in order for a domain in our system
                  to be treated as authoritative.
                x-akamai:
                  file-path: schemas/domain.yaml
                title: Domain
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Domain's unique ID
                    example: 1234
                    readOnly: true
                    x-linode-cli-display: 1
                  tags:
                    type: array
                    description: |
                      An array of tags applied to this object.  Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  type:
                    type: string
                    description: |
                      Whether this Domain represents the authoritative source of information for the domain
                      it describes ("master"), or whether it is a read-only copy of
                      a master ("slave").
                    example: master
                    enum:
                    - master
                    - slave
                    x-linode-cli-display: 3
                  description:
                    type: string
                    description: |
                      A description for this Domain. This is for display purposes only.
                    example: null
                    minLength: 1
                    maxLength: 253
                  axfr_ips:
                    type: array
                    description: |-
                      The list of IPs that may perform a zone transfer for this Domain. The total combined length of all data within this array cannot exceed 1000 characters.

                      __Note__: This is potentially dangerous, and should be set to an empty list unless you intend to use it.
                    example: []
                    items:
                      type: string
                      format: ip
                  domain:
                    type: string
                    description: |
                      The domain this Domain represents. Domain labels cannot be longer than 63 characters
                      and must conform to [RFC1035](https://tools.ietf.org/html/rfc1035).
                      Domains must be unique on Linode's platform, including across
                      different Linode accounts; there cannot be two Domains representing
                      the same domain.
                    example: example.org
                    minLength: 1
                    maxLength: 253
                    pattern: ^(\*\.)?([a-zA-Z0-9-_]{1,63}\.)+([a-zA-Z]{2,3}\.)?([a-zA-Z]{2,16}|xn--[a-zA-Z0-9]+)$
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  expire_sec:
                    type: integer
                    description: |-
                      The amount of time in seconds that may pass before this Domain is no longer authoritative.

                      - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                      - Any other value is rounded up to the nearest valid value.

                      - A value of 0 is equivalent to the default value of 1209600.
                    example: 300
                    default: 0
                  group:
                    type: string
                    description: |
                      The group this Domain belongs to.  This is for display purposes only.
                    example: null
                    minLength: 1
                    maxLength: 50
                    deprecated: true
                    x-linode-filterable: true
                  master_ips:
                    type: array
                    description: |
                      The IP addresses representing the master DNS for this Domain. At least one value is required
                      for `type` slave Domains. The total combined length of all data
                      within this array cannot exceed 1000 characters.
                    example: []
                    items:
                      type: string
                      format: ip
                  refresh_sec:
                    type: integer
                    description: |-
                      The amount of time in seconds before this Domain should be refreshed.

                      - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                      - Any other value is rounded up to the nearest valid value.

                      - A value of 0 is equivalent to the default value of 14400.
                    example: 300
                    default: 0
                  retry_sec:
                    type: integer
                    description: |-
                      The interval, in seconds, at which a failed refresh should be retried.

                      - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                      - Any other value is rounded up to the nearest valid value.

                      - A value of 0 is equivalent to the default value of 14400.
                    example: 300
                    default: 0
                  soa_email:
                    type: string
                    description: |
                      Start of Authority email address. This is required for `type` master Domains.
                    example: admin@example.org
                    format: email
                    x-linode-cli-display: 5
                  status:
                    type: string
                    description: |
                      Used to control whether this Domain is currently being rendered.
                    example: active
                    default: active
                    enum:
                    - disabled
                    - active
                    x-linode-cli-color:
                      active: green
                      default_: red
                      disabled: yellow
                      edit_mode: yellow
                    x-linode-cli-display: 4
                  ttl_sec:
                    type: integer
                    description: |-
                      "Time to Live" - the amount of time in seconds that this Domain's records may be cached by resolvers or other domain servers.

                      - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
                      - Any other value is rounded up to the nearest valid value.
                      - A value of 0 is equivalent to the default value of 86400.
                    example: 300
                    default: 0
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-domain
      security:
      - personalAccessToken: []
      - oauth:
        - domains:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/domains/123
      - lang: CLI
        source: linode-cli domains view 123
      x-linode-cli-action: view
      x-linode-grant: read_only
      x-original-op-id: getDomain
      x-original-op-title: Domain View
    put:
      operationId: put-domain
      summary: Update a domain
      tags:
      - Domains
      description: |-
        Update information about a Domain in Linode's DNS Manager.


        ---


        - __CLI__.

            ```
            linode-cli domains update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            domains:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli domains update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: domains:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The Domain information to update.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                A domain zonefile in our DNS system.  You must own the domain name and tell your registrar
                to use Linode's nameservers in order for a domain in our system to
                be treated as authoritative.
              x-akamai:
                file-path: schemas/domain.yaml
              title: Domain
              additionalProperties: false
              properties:
                id:
                  type: integer
                  description: |
                    __Read-only__ This Domain's unique ID
                  example: '{{id}}'
                  readOnly: true
                  x-linode-cli-display: 1
                tags:
                  type: array
                  description: |
                    An array of tags applied to this object.  Tags are for organizational purposes only.
                  example:
                  - example tag
                  - another example
                  items:
                    type: string
                  x-linode-filterable: true
                type:
                  type: string
                  description: |
                    Whether this Domain represents the authoritative source of information for the domain it describes
                    ("master"), or whether it is a read-only copy of a master ("slave").
                  example: '{{type}}'
                  enum:
                  - master
                  - slave
                  x-linode-cli-display: 3
                description:
                  type: string
                  description: |
                    A description for this Domain. This is for display purposes only.
                  example: '{{description}}'
                  minLength: 1
                  maxLength: 253
                axfr_ips:
                  type: array
                  description: |-
                    The list of IPs that may perform a zone transfer for this Domain. The total combined length of all data within this array cannot exceed 1000 characters.

                    __Note__: This is potentially dangerous, and should be set to an empty list unless you intend to use it.
                  example: []
                  items:
                    type: string
                    format: ip
                domain:
                  type: string
                  description: |
                    The domain this Domain represents. Domain labels cannot be longer than 63 characters
                    and must conform to [RFC1035](https://tools.ietf.org/html/rfc1035).
                    Domains must be unique on Linode's platform, including across
                    different Linode accounts; there cannot be two Domains representing
                    the same domain.
                  example: '{{domain}}'
                  minLength: 1
                  maxLength: 253
                  pattern: ^(\*\.)?([a-zA-Z0-9-_]{1,63}\.)+([a-zA-Z]{2,3}\.)?([a-zA-Z]{2,16}|xn--[a-zA-Z0-9]+)$
                  x-linode-cli-display: 2
                  x-linode-filterable: true
                expire_sec:
                  type: integer
                  description: |-
                    The amount of time in seconds that may pass before this Domain is no longer authoritative.

                    - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                    - Any other value is rounded up to the nearest valid value.

                    - A value of 0 is equivalent to the default value of 1209600.
                  example: '{{expire_sec}}'
                  default: 0
                group:
                  type: string
                  description: |
                    The group this Domain belongs to.  This is for display purposes only.
                  example: '{{group}}'
                  minLength: 1
                  maxLength: 50
                  deprecated: true
                  x-linode-filterable: true
                master_ips:
                  type: array
                  description: |
                    The IP addresses representing the master DNS for this Domain. At least one value is required
                    for `type` slave Domains. The total combined length of all data
                    within this array cannot exceed 1000 characters.
                  example: []
                  items:
                    type: string
                    format: ip
                refresh_sec:
                  type: integer
                  description: |-
                    The amount of time in seconds before this Domain should be refreshed.

                    - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                    - Any other value is rounded up to the nearest valid value.

                    - A value of 0 is equivalent to the default value of 14400.
                  example: '{{refresh_sec}}'
                  default: 0
                retry_sec:
                  type: integer
                  description: |-
                    The interval, in seconds, at which a failed refresh should be retried.

                    - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                    - Any other value is rounded up to the nearest valid value.

                    - A value of 0 is equivalent to the default value of 14400.
                  example: '{{retry_sec}}'
                  default: 0
                soa_email:
                  type: string
                  description: |
                    Start of Authority email address. This is required for `type` master Domains.
                  example: '{{soa_email}}'
                  format: email
                  x-linode-cli-display: 5
                status:
                  type: string
                  description: |
                    Used to control whether this Domain is currently being rendered.
                  example: '{{status}}'
                  default: active
                  enum:
                  - disabled
                  - active
                  x-linode-cli-color:
                    active: green
                    default_: red
                    disabled: yellow
                    edit_mode: yellow
                  x-linode-cli-display: 4
                ttl_sec:
                  type: integer
                  description: |-
                    "Time to Live" - the amount of time in seconds that this Domain's records may be cached by resolvers or other domain servers.

                    - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
                    - Any other value is rounded up to the nearest valid value.
                    - A value of 0 is equivalent to the default value of 86400.
                  example: '{{ttl_sec}}'
                  default: 0
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Domain update successful.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A domain zonefile in our DNS system.  You must own the domain name and tell your registrar
                  to use Linode's nameservers in order for a domain in our system
                  to be treated as authoritative.
                x-akamai:
                  file-path: schemas/domain.yaml
                title: Domain
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Domain's unique ID
                    example: 1234
                    readOnly: true
                    x-linode-cli-display: 1
                  tags:
                    type: array
                    description: |
                      An array of tags applied to this object.  Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  type:
                    type: string
                    description: |
                      Whether this Domain represents the authoritative source of information for the domain
                      it describes ("master"), or whether it is a read-only copy of
                      a master ("slave").
                    example: master
                    enum:
                    - master
                    - slave
                    x-linode-cli-display: 3
                  description:
                    type: string
                    description: |
                      A description for this Domain. This is for display purposes only.
                    example: null
                    minLength: 1
                    maxLength: 253
                  axfr_ips:
                    type: array
                    description: |-
                      The list of IPs that may perform a zone transfer for this Domain. The total combined length of all data within this array cannot exceed 1000 characters.

                      __Note__: This is potentially dangerous, and should be set to an empty list unless you intend to use it.
                    example: []
                    items:
                      type: string
                      format: ip
                  domain:
                    type: string
                    description: |
                      The domain this Domain represents. Domain labels cannot be longer than 63 characters
                      and must conform to [RFC1035](https://tools.ietf.org/html/rfc1035).
                      Domains must be unique on Linode's platform, including across
                      different Linode accounts; there cannot be two Domains representing
                      the same domain.
                    example: example.org
                    minLength: 1
                    maxLength: 253
                    pattern: ^(\*\.)?([a-zA-Z0-9-_]{1,63}\.)+([a-zA-Z]{2,3}\.)?([a-zA-Z]{2,16}|xn--[a-zA-Z0-9]+)$
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  expire_sec:
                    type: integer
                    description: |-
                      The amount of time in seconds that may pass before this Domain is no longer authoritative.

                      - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                      - Any other value is rounded up to the nearest valid value.

                      - A value of 0 is equivalent to the default value of 1209600.
                    example: 300
                    default: 0
                  group:
                    type: string
                    description: |
                      The group this Domain belongs to.  This is for display purposes only.
                    example: null
                    minLength: 1
                    maxLength: 50
                    deprecated: true
                    x-linode-filterable: true
                  master_ips:
                    type: array
                    description: |
                      The IP addresses representing the master DNS for this Domain. At least one value is required
                      for `type` slave Domains. The total combined length of all data
                      within this array cannot exceed 1000 characters.
                    example: []
                    items:
                      type: string
                      format: ip
                  refresh_sec:
                    type: integer
                    description: |-
                      The amount of time in seconds before this Domain should be refreshed.

                      - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                      - Any other value is rounded up to the nearest valid value.

                      - A value of 0 is equivalent to the default value of 14400.
                    example: 300
                    default: 0
                  retry_sec:
                    type: integer
                    description: |-
                      The interval, in seconds, at which a failed refresh should be retried.

                      - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                      - Any other value is rounded up to the nearest valid value.

                      - A value of 0 is equivalent to the default value of 14400.
                    example: 300
                    default: 0
                  soa_email:
                    type: string
                    description: |
                      Start of Authority email address. This is required for `type` master Domains.
                    example: admin@example.org
                    format: email
                    x-linode-cli-display: 5
                  status:
                    type: string
                    description: |
                      Used to control whether this Domain is currently being rendered.
                    example: active
                    default: active
                    enum:
                    - disabled
                    - active
                    x-linode-cli-color:
                      active: green
                      default_: red
                      disabled: yellow
                      edit_mode: yellow
                    x-linode-cli-display: 4
                  ttl_sec:
                    type: integer
                    description: |-
                      "Time to Live" - the amount of time in seconds that this Domain's records may be cached by resolvers or other domain servers.

                      - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
                      - Any other value is rounded up to the nearest valid value.
                      - A value of 0 is equivalent to the default value of 86400.
                    example: 300
                    default: 0
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-domain
      security:
      - personalAccessToken: []
      - oauth:
        - domains:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                  "domain": "example.com",
                  "type": "master",
                  "soa_email": "admin@example.com",
                  "description": "Example Description",
                  "refresh_sec": 14400,
                  "retry_sec": 3600,
                  "expire_sec": 604800,
                  "ttl_sec": 3600,
                  "status": "active",
                  "master_ips": ["127.0.0.1","255.255.255.1","123.123.123.7"],
                  "axfr_ips": ["44.55.66.77"],
                  "group": "Example Display Group",
                  "tags": ["tag1","tag2"]
              }' \
              https://api.linode.com/v4/domains/123
      - lang: CLI
        source: |-
          linode-cli domains update 1234 \
            --retry_sec 7200 \
            --ttl_sec 300
      x-linode-cli-action: update
      x-linode-grant: read_write
      x-original-op-id: updateDomain
      x-original-op-title: Domain Update
    delete:
      operationId: delete-domain
      summary: Delete a domain
      tags:
      - Domains
      description: |-
        Deletes a Domain from Linode's DNS Manager. The Domain will be removed from Linode's nameservers shortly after this operation completes. This also deletes all associated Domain Records.


        ---


        - __OAuth__.

            ```
            domains:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: domains:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Domain deleted successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-domain
      security:
      - personalAccessToken: []
      - oauth:
        - domains:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/domains/1234
      - lang: CLI
        source: linode-cli domains delete 1234
      x-linode-cli-action:
      - delete
      - rm
      x-linode-grant: read_write
      x-original-op-id: deleteDomain
      x-original-op-title: Domain Delete
    x-linode-cli-command: domains
  /domains/{domainId}/clone:
    x-akamai:
      file-path: paths/domain-clone.yaml
      path-info: /domains/{domainId}/clone
    parameters:
    - name: domainId
      in: path
      description: |
        ID of the Domain to clone.
      example: '{{domainId}}'
      x-akamai:
        file-path: parameters/domain-id-path-f5ed829a.yaml
      required: true
      schema:
        type: string
    post:
      operationId: post-clone-domain
      summary: Clone a domain
      tags:
      - Domains
      description: |-
        Clones a Domain and all associated DNS records from a Domain that is registered in Linode's DNS manager.


        ---


        - __CLI__.

            ```
            linode-cli domains clone
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            domains:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli domains clone
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: domains:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the Domain to clone.
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - domain
              properties:
                domain:
                  type: string
                  description: |
                    The new domain for the clone. Domain labels cannot be longer than 63 characters and must
                    conform to [RFC1035](https://tools.ietf.org/html/rfc1035). Domains
                    must be unique on Linode's platform, including across different
                    Linode accounts; there cannot be two Domains representing the
                    same domain.
                  example: '{{domain}}'
                  minLength: 1
                  maxLength: 253
                  pattern: ^(\*\.)?([a-zA-Z0-9-_]{1,63}\.)+([a-zA-Z]{2,3}\.)?([a-zA-Z]{2,16}|xn--[a-zA-Z0-9]+)$
                  x-linode-filterable: true
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A new Domain in Linode's DNS Manager, based on a cloned Domain.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A domain zonefile in our DNS system.  You must own the domain name and tell your registrar
                  to use Linode's nameservers in order for a domain in our system
                  to be treated as authoritative.
                x-akamai:
                  file-path: schemas/domain.yaml
                title: Domain
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Domain's unique ID
                    example: 1234
                    readOnly: true
                    x-linode-cli-display: 1
                  tags:
                    type: array
                    description: |
                      An array of tags applied to this object.  Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  type:
                    type: string
                    description: |
                      Whether this Domain represents the authoritative source of information for the domain
                      it describes ("master"), or whether it is a read-only copy of
                      a master ("slave").
                    example: master
                    enum:
                    - master
                    - slave
                    x-linode-cli-display: 3
                  description:
                    type: string
                    description: |
                      A description for this Domain. This is for display purposes only.
                    example: null
                    minLength: 1
                    maxLength: 253
                  axfr_ips:
                    type: array
                    description: |-
                      The list of IPs that may perform a zone transfer for this Domain. The total combined length of all data within this array cannot exceed 1000 characters.

                      __Note__: This is potentially dangerous, and should be set to an empty list unless you intend to use it.
                    example: []
                    items:
                      type: string
                      format: ip
                  domain:
                    type: string
                    description: |
                      The domain this Domain represents. Domain labels cannot be longer than 63 characters
                      and must conform to [RFC1035](https://tools.ietf.org/html/rfc1035).
                      Domains must be unique on Linode's platform, including across
                      different Linode accounts; there cannot be two Domains representing
                      the same domain.
                    example: example.org
                    minLength: 1
                    maxLength: 253
                    pattern: ^(\*\.)?([a-zA-Z0-9-_]{1,63}\.)+([a-zA-Z]{2,3}\.)?([a-zA-Z]{2,16}|xn--[a-zA-Z0-9]+)$
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  expire_sec:
                    type: integer
                    description: |-
                      The amount of time in seconds that may pass before this Domain is no longer authoritative.

                      - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                      - Any other value is rounded up to the nearest valid value.

                      - A value of 0 is equivalent to the default value of 1209600.
                    example: 300
                    default: 0
                  group:
                    type: string
                    description: |
                      The group this Domain belongs to.  This is for display purposes only.
                    example: null
                    minLength: 1
                    maxLength: 50
                    deprecated: true
                    x-linode-filterable: true
                  master_ips:
                    type: array
                    description: |
                      The IP addresses representing the master DNS for this Domain. At least one value is required
                      for `type` slave Domains. The total combined length of all data
                      within this array cannot exceed 1000 characters.
                    example: []
                    items:
                      type: string
                      format: ip
                  refresh_sec:
                    type: integer
                    description: |-
                      The amount of time in seconds before this Domain should be refreshed.

                      - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                      - Any other value is rounded up to the nearest valid value.

                      - A value of 0 is equivalent to the default value of 14400.
                    example: 300
                    default: 0
                  retry_sec:
                    type: integer
                    description: |-
                      The interval, in seconds, at which a failed refresh should be retried.

                      - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.

                      - Any other value is rounded up to the nearest valid value.

                      - A value of 0 is equivalent to the default value of 14400.
                    example: 300
                    default: 0
                  soa_email:
                    type: string
                    description: |
                      Start of Authority email address. This is required for `type` master Domains.
                    example: admin@example.org
                    format: email
                    x-linode-cli-display: 5
                  status:
                    type: string
                    description: |
                      Used to control whether this Domain is currently being rendered.
                    example: active
                    default: active
                    enum:
                    - disabled
                    - active
                    x-linode-cli-color:
                      active: green
                      default_: red
                      disabled: yellow
                      edit_mode: yellow
                    x-linode-cli-display: 4
                  ttl_sec:
                    type: integer
                    description: |-
                      "Time to Live" - the amount of time in seconds that this Domain's records may be cached by resolvers or other domain servers.

                      - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
                      - Any other value is rounded up to the nearest valid value.
                      - A value of 0 is equivalent to the default value of 86400.
                    example: 300
                    default: 0
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-clone-domain
      security:
      - personalAccessToken: []
      - oauth:
        - domains:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
            -H "Authorization: Bearer $TOKEN" \
            -X POST -d '{
            "domain": "example.com"
          }' \ https://api.linode.com/v4/domains/123/clone
      - lang: CLI
        source: linode-cli domains clone 123 --domain example.com
      x-linode-cli-action: clone
      x-linode-grant: read_write
      x-original-op-id: cloneDomain
      x-original-op-title: Domain Clone
    x-linode-cli-command: domains
  /domains/{domainId}/records:
    x-akamai:
      file-path: paths/records.yaml
      path-info: /domains/{domainId}/records
    parameters:
    - name: domainId
      in: path
      description: |
        The ID of the Domain we are accessing Records for.
      example: '{{domainId}}'
      x-akamai:
        file-path: parameters/domain-id-path-eb9ccd41.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-domain-record
      summary: Create a domain record
      tags:
      - Records
      description: |-
        Adds a new Domain Record to the zonefile this Domain represents.

        Each domain can have up to 12,000 active records.


        ---


        - __CLI__.

            ```
            linode-cli domains records-create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            domains:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli domains records-create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: domains:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the new Record to add.
        required: true
        content:
          application/json:
            schema:
              required:
              - type
              allOf:
              - type: object
                description: |
                  A single record on a Domain.
                x-akamai:
                  file-path: schemas/domain-record.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Record's unique ID.
                    example: 123456
                    readOnly: true
                    x-linode-cli-display: 1
                  name:
                    type: string
                    description: |-
                      The name of this Record. For requests, this property's actual usage and whether it is required depends on the type of record this represents:

                      `A` and `AAAA`: The hostname or FQDN of the Record.

                      `NS`: The subdomain, if any, to use with the Domain of the Record. Wildcard NS records (`*`) are not supported.

                      `MX`: The mail subdomain. For example, `sub` for the address `user@sub.example.com` under the `example.com` Domain.

                      - The left-most subdomain component may be an asterisk (`*`) to designate a wildcard subdomain.
                      - Other subdomain components must only contain letters, digits, and hyphens, start with a letter, end with a letter or digit, and contain less than 64 characters.
                      - Must be an empty string (`""`) for a Null MX Record.

                      `CNAME`: The hostname. Must be unique. Required.

                      `TXT`: The hostname.

                      `SRV`: Unused. Use the `service` property to set the service name for this record.

                      `CAA`: The subdomain. Omit or enter an empty string (`""`) to apply to the entire Domain.

                      `PTR`: See our guide on how to [Configure Your Linode for Reverse DNS
                      (rDNS)](https://www.linode.com/docs/guides/configure-rdns/).
                    example: test
                    minLength: 1
                    maxLength: 100
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  type:
                    type: string
                    description: |
                      The type of Record this is in the DNS system. For example, A records associate
                      a domain name with an IPv4 address, and AAAA records associate
                      a domain name with an IPv6 address. For more information, see
                      the guides on [DNS Record Types](https://www.linode.com/docs/products/networking/dns-manager/guides/#dns-record-types).
                    example: A
                    enum:
                    - A
                    - AAAA
                    - NS
                    - MX
                    - CNAME
                    - TXT
                    - SRV
                    - PTR
                    - CAA
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Domain Record was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  port:
                    type: integer
                    description: |
                      The port this Record points to. Only valid and required for SRV record requests.
                    example: 80
                    minimum: 0
                    maximum: 65535
                  priority:
                    type: integer
                    description: |-
                      The priority of the target host for this Record. Lower values are preferred. Only valid for MX and SRV record requests. Required for SRV record requests.

                      Defaults to `0` for MX record requests. Must be `0` for Null MX records.
                    example: 50
                    minimum: 0
                    maximum: 255
                    x-linode-cli-display: 6
                  protocol:
                    type: string
                    description: |
                      The protocol this Record's service communicates with. An underscore (`_`) is prepended
                      automatically to the submitted value for this property. Only
                      valid for SRV record requests.
                    example: null
                    nullable: true
                  service:
                    type: string
                    description: |
                      The name of the service. An underscore (`_`) is prepended and a period (`.`) is appended
                      automatically to the submitted value for this property. Only
                      valid and required for SRV record requests.
                    example: null
                    nullable: true
                  tag:
                    type: string
                    description: |
                      The tag portion of a CAA record. Only valid and required for CAA record requests.
                    example: null
                    nullable: true
                    enum:
                    - issue
                    - issuewild
                    - iodef
                    x-linode-filterable: true
                  target:
                    type: string
                    description: |-
                      The target for this Record. For requests, this property's actual usage and whether it is required depends on the type of record this represents:

                      `A` and `AAAA`: The IP address. Use `[remote_addr]` to submit the IPv4 address of the request. Required.

                      `NS`: The name server. Must be a valid domain. Required.

                      `MX`: The mail server. Must be a valid domain unless creating a Null MX Record. Required.

                      - Must have less than 254 total characters.
                      - The left-most domain component may be an asterisk (`*`) to designate a wildcard domain.
                      - Other domain components must only contain letters, digits, and hyphens, start with a letter, end with a letter or digit, and contain less than 64 characters.
                      - To create a [Null MX Record](https://datatracker.ietf.org/doc/html/rfc7505), first [remove](https://techdocs.akamai.com/linode-api/reference/delete-domain-record) any additional MX records, then create an MX record with empty strings (`""`) for the `target` and `name`. If a Domain has a Null MX record, new MX records cannot be created.

                      `CNAME`: The alias. Must be a valid domain. Required.

                      `TXT`: The value. Required.

                      `SRV`: The target domain or subdomain. If a subdomain is entered, it is automatically used with the Domain.
                      To configure for a different domain, enter a valid FQDN. For example, the value `www` with a Domain for
                      `example.com` results in a target set to `www.example.com`, whereas the value `sample.com` results in a
                      target set to `sample.com`. Required.

                      `CAA`: The value. For `issue` or `issuewild` tags, the domain of your certificate issuer. For the `iodef`
                      tag, a contact or submission URL (domain, http, https, or mailto). Requirements depend on the tag for this record:

                        - `issue`: The domain of your certificate issuer. Must include a valid domain. May include additional parameters separated with semicolons (`;`), for example: `www.example.com; foo=bar`
                        - `issuewild`: The domain of your wildcard certificate issuer. Must be a valid domain and must not start with an asterisk (`*`).
                        - `iodef`: Must be either (1) a valid domain, (2) a valid domain prepended with `http://` or `https://`, or (3) a valid email address prepended with `mailto:`.

                      `PTR`: Required. See our guide on how to [Configure Your Linode for Reverse DNS
                      (rDNS)](https://www.linode.com/docs/guides/configure-rdns/).

                      With the exception of A, AAAA, and CAA records, this field accepts a trailing period.
                    example: 192.0.2.0
                    maxLength: 65535
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  ttl_sec:
                    type: integer
                    description: |
                      '"Time to Live" - the amount of time in seconds that this Domain''s records may be
                      cached by resolvers or other domain servers. Valid values are
                      300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600,
                      604800, 1209600, and 2419200 - any other value will be rounded
                      to the nearest valid value.'
                    example: 604800
                    x-linode-cli-display: 5
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Domain Record was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  weight:
                    type: integer
                    description: |
                      The relative weight of this Record used in the case of identical priority. Higher
                      values are preferred. Only valid and required for SRV record
                      requests.
                    example: 50
                    minimum: 0
                    maximum: 65535
                    x-linode-cli-display: 7
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Domain Record created successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A single record on a Domain.
                x-akamai:
                  file-path: schemas/domain-record.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Record's unique ID.
                    example: 123456
                    readOnly: true
                    x-linode-cli-display: 1
                  name:
                    type: string
                    description: |-
                      The name of this Record. For requests, this property's actual usage and whether it is required depends on the type of record this represents:

                      `A` and `AAAA`: The hostname or FQDN of the Record.

                      `NS`: The subdomain, if any, to use with the Domain of the Record. Wildcard NS records (`*`) are not supported.

                      `MX`: The mail subdomain. For example, `sub` for the address `user@sub.example.com` under the `example.com` Domain.

                      - The left-most subdomain component may be an asterisk (`*`) to designate a wildcard subdomain.
                      - Other subdomain components must only contain letters, digits, and hyphens, start with a letter, end with a letter or digit, and contain less than 64 characters.
                      - Must be an empty string (`""`) for a Null MX Record.

                      `CNAME`: The hostname. Must be unique. Required.

                      `TXT`: The hostname.

                      `SRV`: Unused. Use the `service` property to set the service name for this record.

                      `CAA`: The subdomain. Omit or enter an empty string (`""`) to apply to the entire Domain.

                      `PTR`: See our guide on how to [Configure Your Linode for Reverse DNS
                      (rDNS)](https://www.linode.com/docs/guides/configure-rdns/).
                    example: test
                    minLength: 1
                    maxLength: 100
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  type:
                    type: string
                    description: |
                      The type of Record this is in the DNS system. For example, A records associate
                      a domain name with an IPv4 address, and AAAA records associate
                      a domain name with an IPv6 address. For more information, see
                      the guides on [DNS Record Types](https://www.linode.com/docs/products/networking/dns-manager/guides/#dns-record-types).
                    example: A
                    enum:
                    - A
                    - AAAA
                    - NS
                    - MX
                    - CNAME
                    - TXT
                    - SRV
                    - PTR
                    - CAA
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Domain Record was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  port:
                    type: integer
                    description: |
                      The port this Record points to. Only valid and required for SRV record requests.
                    example: 80
                    minimum: 0
                    maximum: 65535
                  priority:
                    type: integer
                    description: |-
                      The priority of the target host for this Record. Lower values are preferred. Only valid for MX and SRV record requests. Required for SRV record requests.

                      Defaults to `0` for MX record requests. Must be `0` for Null MX records.
                    example: 50
                    minimum: 0
                    maximum: 255
                    x-linode-cli-display: 6
                  protocol:
                    type: string
                    description: |
                      The protocol this Record's service communicates with. An underscore (`_`) is prepended
                      automatically to the submitted value for this property. Only
                      valid for SRV record requests.
                    example: null
                    nullable: true
                  service:
                    type: string
                    description: |
                      The name of the service. An underscore (`_`) is prepended and a period (`.`) is appended
                      automatically to the submitted value for this property. Only
                      valid and required for SRV record requests.
                    example: null
                    nullable: true
                  tag:
                    type: string
                    description: |
                      The tag portion of a CAA record. Only valid and required for CAA record requests.
                    example: null
                    nullable: true
                    enum:
                    - issue
                    - issuewild
                    - iodef
                    x-linode-filterable: true
                  target:
                    type: string
                    description: |-
                      The target for this Record. For requests, this property's actual usage and whether it is required depends on the type of record this represents:

                      `A` and `AAAA`: The IP address. Use `[remote_addr]` to submit the IPv4 address of the request. Required.

                      `NS`: The name server. Must be a valid domain. Required.

                      `MX`: The mail server. Must be a valid domain unless creating a Null MX Record. Required.

                      - Must have less than 254 total characters.
                      - The left-most domain component may be an asterisk (`*`) to designate a wildcard domain.
                      - Other domain components must only contain letters, digits, and hyphens, start with a letter, end with a letter or digit, and contain less than 64 characters.
                      - To create a [Null MX Record](https://datatracker.ietf.org/doc/html/rfc7505), first [remove](https://techdocs.akamai.com/linode-api/reference/delete-domain-record) any additional MX records, then create an MX record with empty strings (`""`) for the `target` and `name`. If a Domain has a Null MX record, new MX records cannot be created.

                      `CNAME`: The alias. Must be a valid domain. Required.

                      `TXT`: The value. Required.

                      `SRV`: The target domain or subdomain. If a subdomain is entered, it is automatically used with the Domain.
                      To configure for a different domain, enter a valid FQDN. For example, the value `www` with a Domain for
                      `example.com` results in a target set to `www.example.com`, whereas the value `sample.com` results in a
                      target set to `sample.com`. Required.

                      `CAA`: The value. For `issue` or `issuewild` tags, the domain of your certificate issuer. For the `iodef`
                      tag, a contact or submission URL (domain, http, https, or mailto). Requirements depend on the tag for this record:

                        - `issue`: The domain of your certificate issuer. Must include a valid domain. May include additional parameters separated with semicolons (`;`), for example: `www.example.com; foo=bar`
                        - `issuewild`: The domain of your wildcard certificate issuer. Must be a valid domain and must not start with an asterisk (`*`).
                        - `iodef`: Must be either (1) a valid domain, (2) a valid domain prepended with `http://` or `https://`, or (3) a valid email address prepended with `mailto:`.

                      `PTR`: Required. See our guide on how to [Configure Your Linode for Reverse DNS
                      (rDNS)](https://www.linode.com/docs/guides/configure-rdns/).

                      With the exception of A, AAAA, and CAA records, this field accepts a trailing period.
                    example: 192.0.2.0
                    maxLength: 65535
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  ttl_sec:
                    type: integer
                    description: |
                      '"Time to Live" - the amount of time in seconds that this Domain''s records may be
                      cached by resolvers or other domain servers. Valid values are
                      300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600,
                      604800, 1209600, and 2419200 - any other value will be rounded
                      to the nearest valid value.'
                    example: 604800
                    x-linode-cli-display: 5
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Domain Record was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  weight:
                    type: integer
                    description: |
                      The relative weight of this Record used in the case of identical priority. Higher
                      values are preferred. Only valid and required for SRV record
                      requests.
                    example: 50
                    minimum: 0
                    maximum: 65535
                    x-linode-cli-display: 7
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-domain-record
      security:
      - personalAccessToken: []
      - oauth:
        - domains:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "type": "A",
                "name": "test",
                "target": "203.0.113.1",
                "priority": 50,
                "weight": 50,
                "port": 80,
                "service": null,
                "protocol": null,
                "ttl_sec": 604800
              }' \
              https://api.linode.com/v4/domains/123/records
      - lang: CLI
        source: |-
          linode-cli domains records-create 123 \
            --type A \
            --name test \
            --target 203.0.113.1 \
            --priority 50 \
            --weight 50 \
            --port 80 \
            --ttl_sec 604800
      x-linode-cli-action: records-create
      x-linode-grant: read_write
      x-original-op-id: createDomainRecord
      x-original-op-title: Domain Record Create
    get:
      operationId: get-domain-records
      summary: List domain records
      tags:
      - Records
      description: |-
        Returns a paginated list of Records configured on a Domain in Linode's DNS Manager.


        ---


        - __CLI__.

            ```
            linode-cli domains records-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            domains:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli domains records-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: domains:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        200:
          description: |
            A list of Domain Records.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        A single record on a Domain.
                      x-akamai:
                        file-path: schemas/domain-record.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ This Record's unique ID.
                          example: 123456
                          readOnly: true
                          x-linode-cli-display: 1
                        name:
                          type: string
                          description: |-
                            The name of this Record. For requests, this property's actual usage and whether it is required depends on the type of record this represents:

                            `A` and `AAAA`: The hostname or FQDN of the Record.

                            `NS`: The subdomain, if any, to use with the Domain of the Record. Wildcard NS records (`*`) are not supported.

                            `MX`: The mail subdomain. For example, `sub` for the address `user@sub.example.com` under the `example.com` Domain.

                            - The left-most subdomain component may be an asterisk (`*`) to designate a wildcard subdomain.
                            - Other subdomain components must only contain letters, digits, and hyphens, start with a letter, end with a letter or digit, and contain less than 64 characters.
                            - Must be an empty string (`""`) for a Null MX Record.

                            `CNAME`: The hostname. Must be unique. Required.

                            `TXT`: The hostname.

                            `SRV`: Unused. Use the `service` property to set the service name for this record.

                            `CAA`: The subdomain. Omit or enter an empty string (`""`) to apply to the entire Domain.

                            `PTR`: See our guide on how to [Configure Your Linode for Reverse DNS
                            (rDNS)](https://www.linode.com/docs/guides/configure-rdns/).
                          example: test
                          minLength: 1
                          maxLength: 100
                          x-linode-cli-display: 3
                          x-linode-filterable: true
                        type:
                          type: string
                          description: |
                            The type of Record this is in the DNS system. For example, A records associate
                            a domain name with an IPv4 address, and AAAA records associate
                            a domain name with an IPv6 address. For more information,
                            see the guides on [DNS Record Types](https://www.linode.com/docs/products/networking/dns-manager/guides/#dns-record-types).
                          example: A
                          enum:
                          - A
                          - AAAA
                          - NS
                          - MX
                          - CNAME
                          - TXT
                          - SRV
                          - PTR
                          - CAA
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        created:
                          type: string
                          description: |
                            __Read-only__ When this Domain Record was created.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                        port:
                          type: integer
                          description: |
                            The port this Record points to. Only valid and required for SRV record
                            requests.
                          example: 80
                          minimum: 0
                          maximum: 65535
                        priority:
                          type: integer
                          description: |-
                            The priority of the target host for this Record. Lower values are preferred. Only valid for MX and SRV record requests. Required for SRV record requests.

                            Defaults to `0` for MX record requests. Must be `0` for Null MX records.
                          example: 50
                          minimum: 0
                          maximum: 255
                          x-linode-cli-display: 6
                        protocol:
                          type: string
                          description: |
                            The protocol this Record's service communicates with. An underscore
                            (`_`) is prepended automatically to the submitted value
                            for this property. Only valid for SRV record requests.
                          example: null
                          nullable: true
                        service:
                          type: string
                          description: |
                            The name of the service. An underscore (`_`) is prepended and a period
                            (`.`) is appended automatically to the submitted value
                            for this property. Only valid and required for SRV record
                            requests.
                          example: null
                          nullable: true
                        tag:
                          type: string
                          description: |
                            The tag portion of a CAA record. Only valid and required for CAA record
                            requests.
                          example: null
                          nullable: true
                          enum:
                          - issue
                          - issuewild
                          - iodef
                          x-linode-filterable: true
                        target:
                          type: string
                          description: |-
                            The target for this Record. For requests, this property's actual usage and whether it is required depends on the type of record this represents:

                            `A` and `AAAA`: The IP address. Use `[remote_addr]` to submit the IPv4 address of the request. Required.

                            `NS`: The name server. Must be a valid domain. Required.

                            `MX`: The mail server. Must be a valid domain unless creating a Null MX Record. Required.

                            - Must have less than 254 total characters.
                            - The left-most domain component may be an asterisk (`*`) to designate a wildcard domain.
                            - Other domain components must only contain letters, digits, and hyphens, start with a letter, end with a letter or digit, and contain less than 64 characters.
                            - To create a [Null MX Record](https://datatracker.ietf.org/doc/html/rfc7505), first [remove](https://techdocs.akamai.com/linode-api/reference/delete-domain-record) any additional MX records, then create an MX record with empty strings (`""`) for the `target` and `name`. If a Domain has a Null MX record, new MX records cannot be created.

                            `CNAME`: The alias. Must be a valid domain. Required.

                            `TXT`: The value. Required.

                            `SRV`: The target domain or subdomain. If a subdomain is entered, it is automatically used with the Domain.
                            To configure for a different domain, enter a valid FQDN. For example, the value `www` with a Domain for
                            `example.com` results in a target set to `www.example.com`, whereas the value `sample.com` results in a
                            target set to `sample.com`. Required.

                            `CAA`: The value. For `issue` or `issuewild` tags, the domain of your certificate issuer. For the `iodef`
                            tag, a contact or submission URL (domain, http, https, or mailto). Requirements depend on the tag for this record:

                              - `issue`: The domain of your certificate issuer. Must include a valid domain. May include additional parameters separated with semicolons (`;`), for example: `www.example.com; foo=bar`
                              - `issuewild`: The domain of your wildcard certificate issuer. Must be a valid domain and must not start with an asterisk (`*`).
                              - `iodef`: Must be either (1) a valid domain, (2) a valid domain prepended with `http://` or `https://`, or (3) a valid email address prepended with `mailto:`.

                            `PTR`: Required. See our guide on how to [Configure Your Linode for Reverse DNS
                            (rDNS)](https://www.linode.com/docs/guides/configure-rdns/).

                            With the exception of A, AAAA, and CAA records, this field accepts a trailing period.
                          example: 192.0.2.0
                          maxLength: 65535
                          x-linode-cli-display: 4
                          x-linode-filterable: true
                        ttl_sec:
                          type: integer
                          description: |
                            '"Time to Live" - the amount of time in seconds that this Domain''s records
                            may be cached by resolvers or other domain servers. Valid
                            values are 300, 3600, 7200, 14400, 28800, 57600, 86400,
                            172800, 345600, 604800, 1209600, and 2419200 - any other
                            value will be rounded to the nearest valid value.'
                          example: 604800
                          x-linode-cli-display: 5
                        updated:
                          type: string
                          description: |
                            __Read-only__ When this Domain Record was last updated.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                        weight:
                          type: integer
                          description: |
                            The relative weight of this Record used in the case of identical priority.
                            Higher values are preferred. Only valid and required for
                            SRV record requests.
                          example: 50
                          minimum: 0
                          maximum: 65535
                          x-linode-cli-display: 7
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-domain-records
      security:
      - personalAccessToken: []
      - oauth:
        - domains:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/domains/1234/records
      - lang: CLI
        source: linode-cli domains records-list 1234
      x-linode-cli-action: records-list
      x-linode-grant: read_only
      x-original-op-id: getDomainRecords
      x-original-op-title: Domain Records List
    x-linode-cli-command: domains
  /domains/{domainId}/records/{recordId}:
    x-akamai:
      file-path: paths/record.yaml
      path-info: /domains/{domainId}/records/{recordId}
    parameters:
    - name: domainId
      in: path
      description: |
        The ID of the Domain whose Record you are accessing.
      example: '{{domainId}}'
      x-akamai:
        file-path: parameters/domain-id-path-8876155d.yaml
      required: true
      schema:
        type: integer
    - name: recordId
      in: path
      description: |
        The ID of the Record you are accessing.
      example: '{{recordId}}'
      x-akamai:
        file-path: parameters/record-id-path.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-domain-record
      summary: Get a domain record
      tags:
      - Records
      description: |-
        View a single Record on this Domain.


        ---


        - __CLI__.

            ```
            linode-cli domains records-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            domains:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli domains records-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: domains:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A Domain Record object.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A single record on a Domain.
                x-akamai:
                  file-path: schemas/domain-record.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Record's unique ID.
                    example: 123456
                    readOnly: true
                    x-linode-cli-display: 1
                  name:
                    type: string
                    description: |-
                      The name of this Record. For requests, this property's actual usage and whether it is required depends on the type of record this represents:

                      `A` and `AAAA`: The hostname or FQDN of the Record.

                      `NS`: The subdomain, if any, to use with the Domain of the Record. Wildcard NS records (`*`) are not supported.

                      `MX`: The mail subdomain. For example, `sub` for the address `user@sub.example.com` under the `example.com` Domain.

                      - The left-most subdomain component may be an asterisk (`*`) to designate a wildcard subdomain.
                      - Other subdomain components must only contain letters, digits, and hyphens, start with a letter, end with a letter or digit, and contain less than 64 characters.
                      - Must be an empty string (`""`) for a Null MX Record.

                      `CNAME`: The hostname. Must be unique. Required.

                      `TXT`: The hostname.

                      `SRV`: Unused. Use the `service` property to set the service name for this record.

                      `CAA`: The subdomain. Omit or enter an empty string (`""`) to apply to the entire Domain.

                      `PTR`: See our guide on how to [Configure Your Linode for Reverse DNS
                      (rDNS)](https://www.linode.com/docs/guides/configure-rdns/).
                    example: test
                    minLength: 1
                    maxLength: 100
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  type:
                    type: string
                    description: |
                      The type of Record this is in the DNS system. For example, A records associate
                      a domain name with an IPv4 address, and AAAA records associate
                      a domain name with an IPv6 address. For more information, see
                      the guides on [DNS Record Types](https://www.linode.com/docs/products/networking/dns-manager/guides/#dns-record-types).
                    example: A
                    enum:
                    - A
                    - AAAA
                    - NS
                    - MX
                    - CNAME
                    - TXT
                    - SRV
                    - PTR
                    - CAA
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Domain Record was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  port:
                    type: integer
                    description: |
                      The port this Record points to. Only valid and required for SRV record requests.
                    example: 80
                    minimum: 0
                    maximum: 65535
                  priority:
                    type: integer
                    description: |-
                      The priority of the target host for this Record. Lower values are preferred. Only valid for MX and SRV record requests. Required for SRV record requests.

                      Defaults to `0` for MX record requests. Must be `0` for Null MX records.
                    example: 50
                    minimum: 0
                    maximum: 255
                    x-linode-cli-display: 6
                  protocol:
                    type: string
                    description: |
                      The protocol this Record's service communicates with. An underscore (`_`) is prepended
                      automatically to the submitted value for this property. Only
                      valid for SRV record requests.
                    example: null
                    nullable: true
                  service:
                    type: string
                    description: |
                      The name of the service. An underscore (`_`) is prepended and a period (`.`) is appended
                      automatically to the submitted value for this property. Only
                      valid and required for SRV record requests.
                    example: null
                    nullable: true
                  tag:
                    type: string
                    description: |
                      The tag portion of a CAA record. Only valid and required for CAA record requests.
                    example: null
                    nullable: true
                    enum:
                    - issue
                    - issuewild
                    - iodef
                    x-linode-filterable: true
                  target:
                    type: string
                    description: |-
                      The target for this Record. For requests, this property's actual usage and whether it is required depends on the type of record this represents:

                      `A` and `AAAA`: The IP address. Use `[remote_addr]` to submit the IPv4 address of the request. Required.

                      `NS`: The name server. Must be a valid domain. Required.

                      `MX`: The mail server. Must be a valid domain unless creating a Null MX Record. Required.

                      - Must have less than 254 total characters.
                      - The left-most domain component may be an asterisk (`*`) to designate a wildcard domain.
                      - Other domain components must only contain letters, digits, and hyphens, start with a letter, end with a letter or digit, and contain less than 64 characters.
                      - To create a [Null MX Record](https://datatracker.ietf.org/doc/html/rfc7505), first [remove](https://techdocs.akamai.com/linode-api/reference/delete-domain-record) any additional MX records, then create an MX record with empty strings (`""`) for the `target` and `name`. If a Domain has a Null MX record, new MX records cannot be created.

                      `CNAME`: The alias. Must be a valid domain. Required.

                      `TXT`: The value. Required.

                      `SRV`: The target domain or subdomain. If a subdomain is entered, it is automatically used with the Domain.
                      To configure for a different domain, enter a valid FQDN. For example, the value `www` with a Domain for
                      `example.com` results in a target set to `www.example.com`, whereas the value `sample.com` results in a
                      target set to `sample.com`. Required.

                      `CAA`: The value. For `issue` or `issuewild` tags, the domain of your certificate issuer. For the `iodef`
                      tag, a contact or submission URL (domain, http, https, or mailto). Requirements depend on the tag for this record:

                        - `issue`: The domain of your certificate issuer. Must include a valid domain. May include additional parameters separated with semicolons (`;`), for example: `www.example.com; foo=bar`
                        - `issuewild`: The domain of your wildcard certificate issuer. Must be a valid domain and must not start with an asterisk (`*`).
                        - `iodef`: Must be either (1) a valid domain, (2) a valid domain prepended with `http://` or `https://`, or (3) a valid email address prepended with `mailto:`.

                      `PTR`: Required. See our guide on how to [Configure Your Linode for Reverse DNS
                      (rDNS)](https://www.linode.com/docs/guides/configure-rdns/).

                      With the exception of A, AAAA, and CAA records, this field accepts a trailing period.
                    example: 192.0.2.0
                    maxLength: 65535
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  ttl_sec:
                    type: integer
                    description: |
                      '"Time to Live" - the amount of time in seconds that this Domain''s records may be
                      cached by resolvers or other domain servers. Valid values are
                      300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600,
                      604800, 1209600, and 2419200 - any other value will be rounded
                      to the nearest valid value.'
                    example: 604800
                    x-linode-cli-display: 5
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Domain Record was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  weight:
                    type: integer
                    description: |
                      The relative weight of this Record used in the case of identical priority. Higher
                      values are preferred. Only valid and required for SRV record
                      requests.
                    example: 50
                    minimum: 0
                    maximum: 65535
                    x-linode-cli-display: 7
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-domain-record
      security:
      - personalAccessToken: []
      - oauth:
        - domains:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/domains/123/records/234
      - lang: CLI
        source: linode-cli domains records-view 123 234
      x-linode-cli-action: records-view
      x-linode-grant: read_only
      x-original-op-id: getDomainRecord
      x-original-op-title: Domain Record View
    put:
      operationId: put-domain-record
      summary: Update a domain record
      tags:
      - Records
      description: |-
        Updates a single Record on this Domain.


        ---


        - __CLI__.

            ```
            linode-cli domains records-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            domains:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli domains records-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: domains:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The values to change.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                A Domain Record Update request object.
              additionalProperties: false
              properties:
                name:
                  type: string
                  description: |-
                    The name of this Record. For requests, this property's actual usage and whether it is required depends on the type of record this represents:

                    `A` and `AAAA`: The hostname or FQDN of the Record.

                    `NS`: The subdomain, if any, to use with the Domain of the Record. Wildcard NS records (`*`) are not supported.

                    `MX`: The mail subdomain. For example, `sub` for the address `user@sub.example.com` under the `example.com` Domain.

                    - The left-most subdomain component may be an asterisk (`*`) to designate a wildcard subdomain.
                    - Other subdomain components must only contain letters, digits, and hyphens, start with a letter, end with a letter or digit, and contain less than 64 characters.
                    - Must be an empty string (`""`) for a Null MX Record.

                    `CNAME`: The hostname. Must be unique. Required.

                    `TXT`: The hostname.

                    `SRV`: Unused. Use the `service` property to set the service name for this record.

                    `CAA`: The subdomain. Omit or enter an empty string (`""`) to apply to the entire Domain.

                    `PTR`: See our guide on how to [Configure Your Linode for Reverse DNS
                    (rDNS)](https://www.linode.com/docs/guides/configure-rdns/).
                  example: '{{name}}'
                  minLength: 1
                  maxLength: 100
                  x-linode-cli-display: 3
                  x-linode-filterable: true
                port:
                  type: integer
                  description: |
                    The port this Record points to. Only valid and required for SRV record requests.
                  example: '{{port}}'
                  minimum: 0
                  maximum: 65535
                priority:
                  type: integer
                  description: |-
                    The priority of the target host for this Record. Lower values are preferred. Only valid for MX and SRV record requests. Required for SRV record requests.

                    Defaults to `0` for MX record requests. Must be `0` for Null MX records.
                  example: '{{priority}}'
                  minimum: 0
                  maximum: 255
                  x-linode-cli-display: 6
                protocol:
                  type: string
                  description: |
                    The protocol this Record's service communicates with. An underscore (`_`) is prepended automatically
                    to the submitted value for this property. Only valid for SRV record
                    requests.
                  example: '{{protocol}}'
                  nullable: true
                service:
                  type: string
                  description: |
                    The name of the service. An underscore (`_`) is prepended and a period (`.`) is appended
                    automatically to the submitted value for this property. Only valid
                    and required for SRV record requests.
                  example: '{{service}}'
                  nullable: true
                tag:
                  type: string
                  description: |
                    The tag portion of a CAA record. Only valid and required for CAA record requests.
                  example: '{{tag}}'
                  nullable: true
                  enum:
                  - issue
                  - issuewild
                  - iodef
                  x-linode-filterable: true
                target:
                  type: string
                  description: |-
                    The target for this Record. For requests, this property's actual usage and whether it is required depends on the type of record this represents:

                    `A` and `AAAA`: The IP address. Use `[remote_addr]` to submit the IPv4 address of the request. Required.

                    `NS`: The name server. Must be a valid domain. Required.

                    `MX`: The mail server. Must be a valid domain unless creating a Null MX Record. Required.

                    - Must have less than 254 total characters.
                    - The left-most domain component may be an asterisk (`*`) to designate a wildcard domain.
                    - Other domain components must only contain letters, digits, and hyphens, start with a letter, end with a letter or digit, and contain less than 64 characters.
                    - To create a [Null MX Record](https://datatracker.ietf.org/doc/html/rfc7505), first [remove](https://techdocs.akamai.com/linode-api/reference/delete-domain-record) any additional MX records, then create an MX record with empty strings (`""`) for the `target` and `name`. If a Domain has a Null MX record, new MX records cannot be created.

                    `CNAME`: The alias. Must be a valid domain. Required.

                    `TXT`: The value. Required.

                    `SRV`: The target domain or subdomain. If a subdomain is entered, it is automatically used with the Domain.
                    To configure for a different domain, enter a valid FQDN. For example, the value `www` with a Domain for
                    `example.com` results in a target set to `www.example.com`, whereas the value `sample.com` results in a
                    target set to `sample.com`. Required.

                    `CAA`: The value. For `issue` or `issuewild` tags, the domain of your certificate issuer. For the `iodef`
                    tag, a contact or submission URL (domain, http, https, or mailto). Requirements depend on the tag for this record:

                      - `issue`: The domain of your certificate issuer. Must include a valid domain. May include additional parameters separated with semicolons (`;`), for example: `www.example.com; foo=bar`
                      - `issuewild`: The domain of your wildcard certificate issuer. Must be a valid domain and must not start with an asterisk (`*`).
                      - `iodef`: Must be either (1) a valid domain, (2) a valid domain prepended with `http://` or `https://`, or (3) a valid email address prepended with `mailto:`.

                    `PTR`: Required. See our guide on how to [Configure Your Linode for Reverse DNS
                    (rDNS)](https://www.linode.com/docs/guides/configure-rdns/).

                    With the exception of A, AAAA, and CAA records, this field accepts a trailing period.
                  example: '{{target}}'
                  maxLength: 65535
                  x-linode-cli-display: 4
                  x-linode-filterable: true
                ttl_sec:
                  type: integer
                  description: |
                    '"Time to Live" - the amount of time in seconds that this Domain''s records may be cached
                    by resolvers or other domain servers. Valid values are 300, 3600,
                    7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600,
                    and 2419200 - any other value will be rounded to the nearest valid
                    value.'
                  example: '{{ttl_sec}}'
                  x-linode-cli-display: 5
                weight:
                  type: integer
                  description: |
                    The relative weight of this Record used in the case of identical priority. Higher values
                    are preferred. Only valid and required for SRV record requests.
                  example: '{{weight}}'
                  minimum: 0
                  maximum: 65535
                  x-linode-cli-display: 7
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Domain Record updated.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A single record on a Domain.
                x-akamai:
                  file-path: schemas/domain-record.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Record's unique ID.
                    example: 123456
                    readOnly: true
                    x-linode-cli-display: 1
                  name:
                    type: string
                    description: |-
                      The name of this Record. For requests, this property's actual usage and whether it is required depends on the type of record this represents:

                      `A` and `AAAA`: The hostname or FQDN of the Record.

                      `NS`: The subdomain, if any, to use with the Domain of the Record. Wildcard NS records (`*`) are not supported.

                      `MX`: The mail subdomain. For example, `sub` for the address `user@sub.example.com` under the `example.com` Domain.

                      - The left-most subdomain component may be an asterisk (`*`) to designate a wildcard subdomain.
                      - Other subdomain components must only contain letters, digits, and hyphens, start with a letter, end with a letter or digit, and contain less than 64 characters.
                      - Must be an empty string (`""`) for a Null MX Record.

                      `CNAME`: The hostname. Must be unique. Required.

                      `TXT`: The hostname.

                      `SRV`: Unused. Use the `service` property to set the service name for this record.

                      `CAA`: The subdomain. Omit or enter an empty string (`""`) to apply to the entire Domain.

                      `PTR`: See our guide on how to [Configure Your Linode for Reverse DNS
                      (rDNS)](https://www.linode.com/docs/guides/configure-rdns/).
                    example: test
                    minLength: 1
                    maxLength: 100
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  type:
                    type: string
                    description: |
                      The type of Record this is in the DNS system. For example, A records associate
                      a domain name with an IPv4 address, and AAAA records associate
                      a domain name with an IPv6 address. For more information, see
                      the guides on [DNS Record Types](https://www.linode.com/docs/products/networking/dns-manager/guides/#dns-record-types).
                    example: A
                    enum:
                    - A
                    - AAAA
                    - NS
                    - MX
                    - CNAME
                    - TXT
                    - SRV
                    - PTR
                    - CAA
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Domain Record was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  port:
                    type: integer
                    description: |
                      The port this Record points to. Only valid and required for SRV record requests.
                    example: 80
                    minimum: 0
                    maximum: 65535
                  priority:
                    type: integer
                    description: |-
                      The priority of the target host for this Record. Lower values are preferred. Only valid for MX and SRV record requests. Required for SRV record requests.

                      Defaults to `0` for MX record requests. Must be `0` for Null MX records.
                    example: 50
                    minimum: 0
                    maximum: 255
                    x-linode-cli-display: 6
                  protocol:
                    type: string
                    description: |
                      The protocol this Record's service communicates with. An underscore (`_`) is prepended
                      automatically to the submitted value for this property. Only
                      valid for SRV record requests.
                    example: null
                    nullable: true
                  service:
                    type: string
                    description: |
                      The name of the service. An underscore (`_`) is prepended and a period (`.`) is appended
                      automatically to the submitted value for this property. Only
                      valid and required for SRV record requests.
                    example: null
                    nullable: true
                  tag:
                    type: string
                    description: |
                      The tag portion of a CAA record. Only valid and required for CAA record requests.
                    example: null
                    nullable: true
                    enum:
                    - issue
                    - issuewild
                    - iodef
                    x-linode-filterable: true
                  target:
                    type: string
                    description: |-
                      The target for this Record. For requests, this property's actual usage and whether it is required depends on the type of record this represents:

                      `A` and `AAAA`: The IP address. Use `[remote_addr]` to submit the IPv4 address of the request. Required.

                      `NS`: The name server. Must be a valid domain. Required.

                      `MX`: The mail server. Must be a valid domain unless creating a Null MX Record. Required.

                      - Must have less than 254 total characters.
                      - The left-most domain component may be an asterisk (`*`) to designate a wildcard domain.
                      - Other domain components must only contain letters, digits, and hyphens, start with a letter, end with a letter or digit, and contain less than 64 characters.
                      - To create a [Null MX Record](https://datatracker.ietf.org/doc/html/rfc7505), first [remove](https://techdocs.akamai.com/linode-api/reference/delete-domain-record) any additional MX records, then create an MX record with empty strings (`""`) for the `target` and `name`. If a Domain has a Null MX record, new MX records cannot be created.

                      `CNAME`: The alias. Must be a valid domain. Required.

                      `TXT`: The value. Required.

                      `SRV`: The target domain or subdomain. If a subdomain is entered, it is automatically used with the Domain.
                      To configure for a different domain, enter a valid FQDN. For example, the value `www` with a Domain for
                      `example.com` results in a target set to `www.example.com`, whereas the value `sample.com` results in a
                      target set to `sample.com`. Required.

                      `CAA`: The value. For `issue` or `issuewild` tags, the domain of your certificate issuer. For the `iodef`
                      tag, a contact or submission URL (domain, http, https, or mailto). Requirements depend on the tag for this record:

                        - `issue`: The domain of your certificate issuer. Must include a valid domain. May include additional parameters separated with semicolons (`;`), for example: `www.example.com; foo=bar`
                        - `issuewild`: The domain of your wildcard certificate issuer. Must be a valid domain and must not start with an asterisk (`*`).
                        - `iodef`: Must be either (1) a valid domain, (2) a valid domain prepended with `http://` or `https://`, or (3) a valid email address prepended with `mailto:`.

                      `PTR`: Required. See our guide on how to [Configure Your Linode for Reverse DNS
                      (rDNS)](https://www.linode.com/docs/guides/configure-rdns/).

                      With the exception of A, AAAA, and CAA records, this field accepts a trailing period.
                    example: 192.0.2.0
                    maxLength: 65535
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  ttl_sec:
                    type: integer
                    description: |
                      '"Time to Live" - the amount of time in seconds that this Domain''s records may be
                      cached by resolvers or other domain servers. Valid values are
                      300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600,
                      604800, 1209600, and 2419200 - any other value will be rounded
                      to the nearest valid value.'
                    example: 604800
                    x-linode-cli-display: 5
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Domain Record was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  weight:
                    type: integer
                    description: |
                      The relative weight of this Record used in the case of identical priority. Higher
                      values are preferred. Only valid and required for SRV record
                      requests.
                    example: 50
                    minimum: 0
                    maximum: 65535
                    x-linode-cli-display: 7
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-domain-record
      security:
      - personalAccessToken: []
      - oauth:
        - domains:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "name": "test",
                "target": "203.0.113.1",
                "priority": 50,
                "weight": 50,
                "port": 80,
                "service": null,
                "protocol": null,
                "ttl_sec": 604800,
                "tag": null
              }' \
              https://api.linode.com/v4/domains/123/records/234
      - lang: CLI
        source: |-
          linode-cli domains records-update 123 234 \
            --name test \
            --target 203.0.113.1 \
            --priority 50 \
            --weight 50 \
            --port 80 \
            --ttl_sec 604800
      x-linode-cli-action: records-update
      x-linode-grant: read_write
      x-original-op-id: updateDomainRecord
      x-original-op-title: Domain Record Update
    delete:
      operationId: delete-domain-record
      summary: Delete a domain record
      tags:
      - Records
      description: |-
        Deletes a Record on this Domain.


        ---


        - __CLI__.

            ```
            linode-cli domains records-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            domains:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli domains records-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: domains:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Record deleted successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-domain-record
      security:
      - personalAccessToken: []
      - oauth:
        - domains:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/domains/123/records/234
      - lang: CLI
        source: linode-cli domains records-delete 123 234
      x-linode-cli-action: records-delete
      x-linode-grant: read_write
      x-original-op-id: deleteDomainRecord
      x-original-op-title: Domain Record Delete
    x-linode-cli-command: domains
  /domains/{domainId}/zone-file:
    x-akamai:
      file-path: paths/zone-file.yaml
      path-info: /domains/{domainId}/zone-file
    parameters:
    - name: domainId
      in: path
      description: |
        ID of the Domain.
      example: '{{domainId}}'
      x-akamai:
        file-path: parameters/domain-id-path-32edd7bc.yaml
      required: true
      schema:
        type: string
    get:
      operationId: get-domain-zone
      summary: Get a domain zone file
      tags:
      - Domain zone file
      description: |-
        Returns the zone file for the last rendered zone for the specified domain.


        ---


        - __CLI__.

            ```
            linode-cli domains zone-file
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            domains:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli domains zone-file
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: domains:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            An array containing the lines of the domain zone file.
          content:
            application/json:
              schema:
                additionalProperties: false
                properties:
                  zone_file:
                    type: array
                    description: |
                      The lines of the zone file for the last rendered zone for this domain.
                    example:
                    - ; example.com [123]
                    - $TTL 864000
                    - '@  IN  SOA  ns1.linode.com. user.example.com. 2021000066 14400
                      14400 1209600 86400'
                    - '@    NS  ns1.linode.com.'
                    - '@    NS  ns2.linode.com.'
                    - '@    NS  ns3.linode.com.'
                    - '@    NS  ns4.linode.com.'
                    - '@    NS  ns5.linode.com.'
                    items:
                      type: string
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-domain-zone
      security:
      - personalAccessToken: []
      - oauth:
        - domains:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
            https://api.linode.com/v4/domains/123/zone-file
      - lang: CLI
        source: linode-cli domains zone-file 123
      x-linode-cli-action: zone-file
      x-linode-grant: read_only
      x-original-op-id: getDomainZone
      x-original-op-title: Domain Zone File View
    x-linode-cli-command: domains
  /images:
    x-akamai:
      file-path: paths/images.yaml
      path-info: /images
    post:
      operationId: post-image
      summary: Create an image
      tags:
      - Images
      description: |-
        Captures a private gold-master Image from a Linode Disk.

        __Note__. Images are not encrypted even when they are taken from an encrypted disk. When a compute instance is rebuilt from an image, and if the instance has disk encryption enabled, the disk is automatically encrypted.


        ---


        - __CLI__.

            ```
            linode-cli images create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            images:read_write
        linodes:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli images create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: |-
            images:read_write
            linodes:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the Image to create.
        content:
          application/json:
            schema:
              additionalProperties: false
              required:
              - disk_id
              properties:
                description:
                  type: string
                  description: |
                    A detailed description of this Image.
                cloud_init:
                  type: boolean
                  description: |
                    Whether this Image supports cloud-init.
                  example: true
                disk_id:
                  type: integer
                  description: |
                    The ID of the Linode Disk that this Image will be created from.
                  example: 42
                label:
                  type: string
                  description: |
                    A short title of this Image. Defaults to the label of the Disk it is being created from
                    if not provided.
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            New private Image created successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Image object
                x-akamai:
                  file-path: schemas/image.yaml
                additionalProperties: false
                properties:
                  id:
                    type: string
                    description: |
                      __Read-only__ The unique ID of this Image.
                    example: linode/debian11
                    readOnly: true
                    x-linode-cli-display: 1
                  type:
                    type: string
                    description: |-
                      __Read-only__ How the Image was created.

                      "Manual" Images can be created at any time.

                      "Automatic" Images are created automatically from a deleted Linode.
                    example: manual
                    readOnly: true
                    enum:
                    - manual
                    - automatic
                    x-linode-filterable: true
                  description:
                    type: string
                    description: |
                      A detailed description of this Image.
                    example: Example Image description.
                    nullable: true
                    minLength: 1
                    maxLength: 65000
                    x-linode-cli-color:
                      None: black
                      default_: white
                    x-linode-cli-display: 4
                  capabilities:
                    type: array
                    description: |-
                      __Read-only__ A list containing the following possible capabilities of this Image:

                      `cloud-init`: This Image supports cloud-init with [Metadata](https://www.linode.com/docs/products/compute/compute-instances/guides/metadata/). Only applies to public Images.
                    example:
                    - cloud-init
                    readOnly: true
                    items:
                      type: string
                    x-linode-cli-display: 8
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Image was created.
                    example: '2021-08-14T22:44:02'
                    readOnly: true
                    format: date-time
                  created_by:
                    type: string
                    description: |
                      __Read-only__ The name of the User who created this Image, or "linode" for public Images.
                    example: linode
                    readOnly: true
                  deprecated:
                    type: boolean
                    description: |
                      __Read-only__ Whether or not this Image is deprecated. Will only be true for deprecated
                      public Images.
                    example: false
                    readOnly: true
                    x-linode-filterable: true
                  eol:
                    type: string
                    description: |
                      __Read-only__ The date of the public Image's planned end of life. `null` for private
                      Images.
                    example: '2026-07-01T04:00:00'
                    nullable: true
                    readOnly: true
                    format: date-time
                  expiry:
                    type: string
                    description: |
                      __Read-only__ Only Images created automatically from a deleted Linode (type=automatic)
                      will expire. `null` for private Images.
                    example: null
                    nullable: true
                    readOnly: true
                    format: date-time
                  is_public:
                    type: boolean
                    description: |
                      __Read-only__ True if the Image is a public distribution image. False if Image
                      is private Account-specific Image.
                    example: true
                    readOnly: true
                    x-linode-cli-display: 5
                    x-linode-filterable: true
                  label:
                    type: string
                    description: |
                      A short description of the Image.
                    example: Debian 11
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  size:
                    type: integer
                    description: |
                      __Read-only__ The minimum size this Image needs to deploy. Size is in MB.
                    example: 2500
                    readOnly: true
                    x-linode-cli-display: 6
                    x-linode-filterable: true
                  status:
                    type: string
                    description: |-
                      __Read-only__ The current status of this Image.

                      Only Images in an "available" status can be deployed. Images in a "creating" status are being created from a Linode Disk, and will become "available" shortly. Images in a "pending_upload" status are waiting for data to be [uploaded](https://techdocs.akamai.com/linode-api/reference/post-upload-image), and become "available" after the upload and processing are complete.

                      The "+order_by" and "+order" operators are not available for [filtering](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) on this key.
                    example: available
                    readOnly: true
                    enum:
                    - creating
                    - pending_upload
                    - available
                    x-linode-cli-display: 7
                    x-linode-filterable: true
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Image was last updated.
                    example: '2021-08-14T22:44:02'
                    readOnly: true
                    format: date-time
                  vendor:
                    type: string
                    description: |
                      __Read-only__ The upstream distribution vendor. `null` for private Images.
                    example: Debian
                    nullable: true
                    readOnly: true
                    x-linode-cli-color:
                      None: black
                      default_: white
                    x-linode-cli-display: 3
                    x-linode-filterable: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-image
      security:
      - personalAccessToken: []
      - oauth:
        - images:read_write
        - linodes:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "disk_id": 123,
                "label": "this_is_a_label",
                "description": "A longer description of the image"
              }' \
              https://api.linode.com/v4/images
      - lang: CLI
        source: |-
          linode-cli images create \
            --label this_is_a_label \
            --description "A longer description \
              of the image" \
            --disk_id 123
      x-linode-cli-action: create
      x-linode-grant: add_images
      x-original-op-id: createImage
      x-original-op-title: Image Create
    get:
      operationId: get-images
      summary: List images
      tags:
      - Images
      description: |-
        Returns a paginated list of Images.

        - __Public__ Images have IDs that begin with "linode/". These distribution images are generally available to all users.

        - __Private__ Images have IDs that begin with "private/". These Images are Account-specific and only accessible to Users with appropriate [Grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants).

        - To view only public Images, call this operation with or without authentication. To view private Images as well, call this operation with authentication.


        ---


        - __OAuth__.

            ```
            images:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: images:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of Images.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        Image object
                      x-akamai:
                        file-path: schemas/image.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: string
                          description: |
                            __Read-only__ The unique ID of this Image.
                          example: linode/debian11
                          readOnly: true
                          x-linode-cli-display: 1
                        type:
                          type: string
                          description: |-
                            __Read-only__ How the Image was created.

                            "Manual" Images can be created at any time.

                            "Automatic" Images are created automatically from a deleted Linode.
                          example: manual
                          readOnly: true
                          enum:
                          - manual
                          - automatic
                          x-linode-filterable: true
                        description:
                          type: string
                          description: |
                            A detailed description of this Image.
                          example: Example Image description.
                          nullable: true
                          minLength: 1
                          maxLength: 65000
                          x-linode-cli-color:
                            None: black
                            default_: white
                          x-linode-cli-display: 4
                        capabilities:
                          type: array
                          description: |-
                            __Read-only__ A list containing the following possible capabilities of this Image:

                            `cloud-init`: This Image supports cloud-init with [Metadata](https://www.linode.com/docs/products/compute/compute-instances/guides/metadata/). Only applies to public Images.
                          example:
                          - cloud-init
                          readOnly: true
                          items:
                            type: string
                          x-linode-cli-display: 8
                        created:
                          type: string
                          description: |
                            __Read-only__ When this Image was created.
                          example: '2021-08-14T22:44:02'
                          readOnly: true
                          format: date-time
                        created_by:
                          type: string
                          description: |
                            __Read-only__ The name of the User who created this Image, or "linode"
                            for public Images.
                          example: linode
                          readOnly: true
                        deprecated:
                          type: boolean
                          description: |
                            __Read-only__ Whether or not this Image is deprecated. Will only be
                            true for deprecated public Images.
                          example: false
                          readOnly: true
                          x-linode-filterable: true
                        eol:
                          type: string
                          description: |
                            __Read-only__ The date of the public Image's planned end of life. `null`
                            for private Images.
                          example: '2026-07-01T04:00:00'
                          nullable: true
                          readOnly: true
                          format: date-time
                        expiry:
                          type: string
                          description: |
                            __Read-only__ Only Images created automatically from a deleted Linode
                            (type=automatic) will expire. `null` for private Images.
                          example: null
                          nullable: true
                          readOnly: true
                          format: date-time
                        is_public:
                          type: boolean
                          description: |
                            __Read-only__ True if the Image is a public distribution image. False
                            if Image is private Account-specific Image.
                          example: true
                          readOnly: true
                          x-linode-cli-display: 5
                          x-linode-filterable: true
                        label:
                          type: string
                          description: |
                            A short description of the Image.
                          example: Debian 11
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        size:
                          type: integer
                          description: |
                            __Read-only__ The minimum size this Image needs to deploy. Size is in
                            MB.
                          example: 2500
                          readOnly: true
                          x-linode-cli-display: 6
                          x-linode-filterable: true
                        status:
                          type: string
                          description: |-
                            __Read-only__ The current status of this Image.

                            Only Images in an "available" status can be deployed. Images in a "creating" status are being created from a Linode Disk, and will become "available" shortly. Images in a "pending_upload" status are waiting for data to be [uploaded](https://techdocs.akamai.com/linode-api/reference/post-upload-image), and become "available" after the upload and processing are complete.

                            The "+order_by" and "+order" operators are not available for [filtering](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) on this key.
                          example: available
                          readOnly: true
                          enum:
                          - creating
                          - pending_upload
                          - available
                          x-linode-cli-display: 7
                          x-linode-filterable: true
                        updated:
                          type: string
                          description: |
                            __Read-only__ When this Image was last updated.
                          example: '2021-08-14T22:44:02'
                          readOnly: true
                          format: date-time
                        vendor:
                          type: string
                          description: |
                            __Read-only__ The upstream distribution vendor. `null` for private Images.
                          example: Debian
                          nullable: true
                          readOnly: true
                          x-linode-cli-color:
                            None: black
                            default_: white
                          x-linode-cli-display: 3
                          x-linode-filterable: true
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-images
      security:
      - personalAccessToken: []
      - oauth:
        - images:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          # Returns public Images only
          curl https://api.linode.com/v4/images

          # Returns private and public Images
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/images
      - lang: CLI
        source: linode-cli images list
      x-linode-cli-action:
      - list
      - ls
      x-linode-redoc-load-ids: true
      x-original-op-id: getImages
      x-original-op-title: Images List
    x-linode-cli-command: images
  /images/upload:
    x-akamai:
      file-path: paths/upload.yaml
      path-info: /images/upload
    post:
      servers:
      - url: https://api.linode.com/v4
      - url: https://api.linode.com/v4beta
      operationId: post-upload-image
      summary: Upload an image
      tags:
      - Images
      description: |-
        Initiates an Image upload.

        This operation creates a new private Image object and returns it along with the URL to which image data can be uploaded.

        - Image data must be uploaded within 24 hours of creation or the upload will be canceled and the image deleted.

        - Image uploads should be made as an HTTP PUT request to the URL returned in the `upload_to` response parameter, with a `Content-type: application/octet-stream` header included in the request. For example:

              curl -v \
                -H "Content-Type: application/octet-stream" \
                --upload-file example.img.gz \
                $UPLOAD_URL \
                --progress-bar \
                --output /dev/null

        - Uploaded image data should be compressed in gzip (`.gz`) format. The uncompressed disk should be in raw disk image (`.img`) format. A maximum compressed file size of 5GB is supported for upload at this time.

        To initiate and complete an Image upload in a single step, see our guide on how to [Upload an Image](https://www.linode.com/docs/products/tools/images/guides/upload-an-image/) using Cloud Manager or the Linode CLI `image-upload` plugin.

        __Note__. Images are not encrypted even when they are taken from an encrypted disk. When a compute instance is rebuilt from an image, and if the instance has disk encryption enabled, the disk is automatically encrypted.


        ---


        - __CLI__.

            ```
            linode-cli images upload
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            images:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli images upload
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: images:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The uploaded Image details.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - label
              - region
              properties:
                description:
                  type: string
                  description: |
                    Description for the uploaded Image.
                  example: '{{description}}'
                cloud_init:
                  type: boolean
                  description: |
                    Whether the uploaded Image supports cloud-init.
                  example: '{{cloud_init}}'
                label:
                  type: string
                  description: |
                    Label for the uploaded Image.
                  example: '{{label}}'
                region:
                  type: string
                  description: |
                    The region to upload to. Once uploaded, the Image can be used in any region.
                  example: '{{region}}'
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
        x-linode-cli-allowed-defaults:
        - region
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Image Upload object including the upload URL and Image object.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  image:
                    type: object
                    description: |
                      Image object
                    x-akamai:
                      file-path: schemas/image.yaml
                    additionalProperties: false
                    properties:
                      id:
                        type: string
                        description: |
                          __Read-only__ The unique ID of this Image.
                        example: linode/debian11
                        readOnly: true
                        x-linode-cli-display: 1
                      type:
                        type: string
                        description: |-
                          __Read-only__ How the Image was created.

                          "Manual" Images can be created at any time.

                          "Automatic" Images are created automatically from a deleted Linode.
                        example: manual
                        readOnly: true
                        enum:
                        - manual
                        - automatic
                        x-linode-filterable: true
                      description:
                        type: string
                        description: |
                          A detailed description of this Image.
                        example: Example Image description.
                        nullable: true
                        minLength: 1
                        maxLength: 65000
                        x-linode-cli-color:
                          None: black
                          default_: white
                        x-linode-cli-display: 4
                      capabilities:
                        type: array
                        description: |-
                          __Read-only__ A list containing the following possible capabilities of this Image:

                          `cloud-init`: This Image supports cloud-init with [Metadata](https://www.linode.com/docs/products/compute/compute-instances/guides/metadata/). Only applies to public Images.
                        example:
                        - cloud-init
                        readOnly: true
                        items:
                          type: string
                        x-linode-cli-display: 8
                      created:
                        type: string
                        description: |
                          __Read-only__ When this Image was created.
                        example: '2021-08-14T22:44:02'
                        readOnly: true
                        format: date-time
                      created_by:
                        type: string
                        description: |
                          __Read-only__ The name of the User who created this Image, or "linode" for
                          public Images.
                        example: linode
                        readOnly: true
                      deprecated:
                        type: boolean
                        description: |
                          __Read-only__ Whether or not this Image is deprecated. Will only be true for deprecated
                          public Images.
                        example: false
                        readOnly: true
                        x-linode-filterable: true
                      eol:
                        type: string
                        description: |
                          __Read-only__ The date of the public Image's planned end of life. `null` for
                          private Images.
                        example: '2026-07-01T04:00:00'
                        nullable: true
                        readOnly: true
                        format: date-time
                      expiry:
                        type: string
                        description: |
                          __Read-only__ Only Images created automatically from a deleted Linode (type=automatic)
                          will expire. `null` for private Images.
                        example: null
                        nullable: true
                        readOnly: true
                        format: date-time
                      is_public:
                        type: boolean
                        description: |
                          __Read-only__ True if the Image is a public distribution image. False if Image
                          is private Account-specific Image.
                        example: true
                        readOnly: true
                        x-linode-cli-display: 5
                        x-linode-filterable: true
                      label:
                        type: string
                        description: |
                          A short description of the Image.
                        example: Debian 11
                        x-linode-cli-display: 2
                        x-linode-filterable: true
                      size:
                        type: integer
                        description: |
                          __Read-only__ The minimum size this Image needs to deploy. Size is in MB.
                        example: 2500
                        readOnly: true
                        x-linode-cli-display: 6
                        x-linode-filterable: true
                      status:
                        type: string
                        description: |-
                          __Read-only__ The current status of this Image.

                          Only Images in an "available" status can be deployed. Images in a "creating" status are being created from a Linode Disk, and will become "available" shortly. Images in a "pending_upload" status are waiting for data to be [uploaded](https://techdocs.akamai.com/linode-api/reference/post-upload-image), and become "available" after the upload and processing are complete.

                          The "+order_by" and "+order" operators are not available for [filtering](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) on this key.
                        example: available
                        readOnly: true
                        enum:
                        - creating
                        - pending_upload
                        - available
                        x-linode-cli-display: 7
                        x-linode-filterable: true
                      updated:
                        type: string
                        description: |
                          __Read-only__ When this Image was last updated.
                        example: '2021-08-14T22:44:02'
                        readOnly: true
                        format: date-time
                      vendor:
                        type: string
                        description: |
                          __Read-only__ The upstream distribution vendor. `null` for private Images.
                        example: Debian
                        nullable: true
                        readOnly: true
                        x-linode-cli-color:
                          None: black
                          default_: white
                        x-linode-cli-display: 3
                        x-linode-filterable: true
                  upload_to:
                    type: string
                    description: |
                      The URL to upload the Image to.
                    x-linode-cli-display: 1
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-upload-image
      security:
      - personalAccessToken: []
      - oauth:
        - images:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "description": "Optional details about the Image",
                "label": "Example Image",
                "region": "us-east"
              }' \
              https://api.linode.com/v4/images/upload
      - lang: CLI
        source: |-
          # Upload the Image file in a single step
          linode-cli image-upload \
            --description "Optional details about the Image" \
            --label "Example Image" \
            --region us-east \
            /path/to/image-file.img.gz

          # Returns the upload_to URL
          linode-cli images upload \
            --description "Optional details about the Image" \
            --label "Example Image" \
            --region us-east
      x-linode-cli-action: upload
      x-linode-grant: add_images
      x-original-op-id: post-images-upload
      x-original-op-title: Image Upload
    x-linode-cli-command: images
  /images/{imageId}:
    x-akamai:
      file-path: paths/image.yaml
      path-info: /images/{imageId}
    parameters:
    - name: imageId
      in: path
      description: |
        ID of the Image to look up.
      example: '{{imageId}}'
      x-akamai:
        file-path: parameters/image-id-path.yaml
      required: true
      schema:
        type: string
    get:
      operationId: get-image
      summary: Get an image
      tags:
      - Images
      description: |-
        Get information about a single Image.

        - __Public__ Images have IDs that begin with "linode/". These distribution images are generally available to all users.

        - __Private__ Images have IDs that begin with "private/". These Images are Account-specific and only accessible to Users with appropriate [Grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants).

        - To view a public Image, call this operation with or without authentication. To view a private Image, call this operation with authentication.


        ---


        - __CLI__.

            ```
            linode-cli images view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            images:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli images view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: images:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A single Image object.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Image object
                x-akamai:
                  file-path: schemas/image.yaml
                additionalProperties: false
                properties:
                  id:
                    type: string
                    description: |
                      __Read-only__ The unique ID of this Image.
                    example: linode/debian11
                    readOnly: true
                    x-linode-cli-display: 1
                  type:
                    type: string
                    description: |-
                      __Read-only__ How the Image was created.

                      "Manual" Images can be created at any time.

                      "Automatic" Images are created automatically from a deleted Linode.
                    example: manual
                    readOnly: true
                    enum:
                    - manual
                    - automatic
                    x-linode-filterable: true
                  description:
                    type: string
                    description: |
                      A detailed description of this Image.
                    example: Example Image description.
                    nullable: true
                    minLength: 1
                    maxLength: 65000
                    x-linode-cli-color:
                      None: black
                      default_: white
                    x-linode-cli-display: 4
                  capabilities:
                    type: array
                    description: |-
                      __Read-only__ A list containing the following possible capabilities of this Image:

                      `cloud-init`: This Image supports cloud-init with [Metadata](https://www.linode.com/docs/products/compute/compute-instances/guides/metadata/). Only applies to public Images.
                    example:
                    - cloud-init
                    readOnly: true
                    items:
                      type: string
                    x-linode-cli-display: 8
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Image was created.
                    example: '2021-08-14T22:44:02'
                    readOnly: true
                    format: date-time
                  created_by:
                    type: string
                    description: |
                      __Read-only__ The name of the User who created this Image, or "linode" for public Images.
                    example: linode
                    readOnly: true
                  deprecated:
                    type: boolean
                    description: |
                      __Read-only__ Whether or not this Image is deprecated. Will only be true for deprecated
                      public Images.
                    example: false
                    readOnly: true
                    x-linode-filterable: true
                  eol:
                    type: string
                    description: |
                      __Read-only__ The date of the public Image's planned end of life. `null` for private
                      Images.
                    example: '2026-07-01T04:00:00'
                    nullable: true
                    readOnly: true
                    format: date-time
                  expiry:
                    type: string
                    description: |
                      __Read-only__ Only Images created automatically from a deleted Linode (type=automatic)
                      will expire. `null` for private Images.
                    example: null
                    nullable: true
                    readOnly: true
                    format: date-time
                  is_public:
                    type: boolean
                    description: |
                      __Read-only__ True if the Image is a public distribution image. False if Image
                      is private Account-specific Image.
                    example: true
                    readOnly: true
                    x-linode-cli-display: 5
                    x-linode-filterable: true
                  label:
                    type: string
                    description: |
                      A short description of the Image.
                    example: Debian 11
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  size:
                    type: integer
                    description: |
                      __Read-only__ The minimum size this Image needs to deploy. Size is in MB.
                    example: 2500
                    readOnly: true
                    x-linode-cli-display: 6
                    x-linode-filterable: true
                  status:
                    type: string
                    description: |-
                      __Read-only__ The current status of this Image.

                      Only Images in an "available" status can be deployed. Images in a "creating" status are being created from a Linode Disk, and will become "available" shortly. Images in a "pending_upload" status are waiting for data to be [uploaded](https://techdocs.akamai.com/linode-api/reference/post-upload-image), and become "available" after the upload and processing are complete.

                      The "+order_by" and "+order" operators are not available for [filtering](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) on this key.
                    example: available
                    readOnly: true
                    enum:
                    - creating
                    - pending_upload
                    - available
                    x-linode-cli-display: 7
                    x-linode-filterable: true
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Image was last updated.
                    example: '2021-08-14T22:44:02'
                    readOnly: true
                    format: date-time
                  vendor:
                    type: string
                    description: |
                      __Read-only__ The upstream distribution vendor. `null` for private Images.
                    example: Debian
                    nullable: true
                    readOnly: true
                    x-linode-cli-color:
                      None: black
                      default_: white
                    x-linode-cli-display: 3
                    x-linode-filterable: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-image
      security:
      - personalAccessToken: []
      - oauth:
        - images:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          # Public Image
          curl https://api.linode.com/v4/images/linode/debian11

          # Private Image
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/images/private/12345
      - lang: CLI
        source: linode-cli images view linode/debian9
      x-linode-cli-action: view
      x-original-op-id: getImage
      x-original-op-title: Image View
    put:
      operationId: put-image
      summary: Update an image
      tags:
      - Images
      description: |-
        Updates a private Image that you have permission to `read_write`.


        ---


        - __CLI__.

            ```
            linode-cli images update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            images:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli images update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: images:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The fields to update.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                Image object
              x-akamai:
                file-path: schemas/image.yaml
              additionalProperties: false
              properties:
                id:
                  type: string
                  description: |
                    __Read-only__ The unique ID of this Image.
                  example: '{{id}}'
                  readOnly: true
                  x-linode-cli-display: 1
                type:
                  type: string
                  description: |-
                    __Read-only__ How the Image was created.

                    "Manual" Images can be created at any time.

                    "Automatic" Images are created automatically from a deleted Linode.
                  example: '{{type}}'
                  readOnly: true
                  enum:
                  - manual
                  - automatic
                  x-linode-filterable: true
                description:
                  type: string
                  description: |
                    A detailed description of this Image.
                  example: '{{description}}'
                  nullable: true
                  minLength: 1
                  maxLength: 65000
                  x-linode-cli-color:
                    None: black
                    default_: white
                  x-linode-cli-display: 4
                capabilities:
                  type: array
                  description: |-
                    __Read-only__ A list containing the following possible capabilities of this Image:

                    `cloud-init`: This Image supports cloud-init with [Metadata](https://www.linode.com/docs/products/compute/compute-instances/guides/metadata/). Only applies to public Images.
                  example:
                  - cloud-init
                  readOnly: true
                  items:
                    type: string
                  x-linode-cli-display: 8
                created:
                  type: string
                  description: |
                    __Read-only__ When this Image was created.
                  example: '{{created}}'
                  readOnly: true
                  format: date-time
                created_by:
                  type: string
                  description: |
                    __Read-only__ The name of the User who created this Image, or "linode" for public Images.
                  example: '{{created_by}}'
                  readOnly: true
                deprecated:
                  type: boolean
                  description: |
                    __Read-only__ Whether or not this Image is deprecated. Will only be true for deprecated
                    public Images.
                  example: '{{deprecated}}'
                  readOnly: true
                  x-linode-filterable: true
                eol:
                  type: string
                  description: |
                    __Read-only__ The date of the public Image's planned end of life. `null` for private
                    Images.
                  example: '{{eol}}'
                  nullable: true
                  readOnly: true
                  format: date-time
                expiry:
                  type: string
                  description: |
                    __Read-only__ Only Images created automatically from a deleted Linode (type=automatic) will
                    expire. `null` for private Images.
                  example: '{{expiry}}'
                  nullable: true
                  readOnly: true
                  format: date-time
                is_public:
                  type: boolean
                  description: |
                    __Read-only__ True if the Image is a public distribution image. False if Image is private
                    Account-specific Image.
                  example: '{{is_public}}'
                  readOnly: true
                  x-linode-cli-display: 5
                  x-linode-filterable: true
                label:
                  type: string
                  description: |
                    A short description of the Image.
                  example: '{{label}}'
                  x-linode-cli-display: 2
                  x-linode-filterable: true
                size:
                  type: integer
                  description: |
                    __Read-only__ The minimum size this Image needs to deploy. Size is in MB.
                  example: '{{size}}'
                  readOnly: true
                  x-linode-cli-display: 6
                  x-linode-filterable: true
                status:
                  type: string
                  description: |-
                    __Read-only__ The current status of this Image.

                    Only Images in an "available" status can be deployed. Images in a "creating" status are being created from a Linode Disk, and will become "available" shortly. Images in a "pending_upload" status are waiting for data to be [uploaded](https://techdocs.akamai.com/linode-api/reference/post-upload-image), and become "available" after the upload and processing are complete.

                    The "+order_by" and "+order" operators are not available for [filtering](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) on this key.
                  example: '{{status}}'
                  readOnly: true
                  enum:
                  - creating
                  - pending_upload
                  - available
                  x-linode-cli-display: 7
                  x-linode-filterable: true
                updated:
                  type: string
                  description: |
                    __Read-only__ When this Image was last updated.
                  example: '{{updated}}'
                  readOnly: true
                  format: date-time
                vendor:
                  type: string
                  description: |
                    __Read-only__ The upstream distribution vendor. `null` for private Images.
                  example: '{{vendor}}'
                  nullable: true
                  readOnly: true
                  x-linode-cli-color:
                    None: black
                    default_: white
                  x-linode-cli-display: 3
                  x-linode-filterable: true
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The updated image.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Image object
                x-akamai:
                  file-path: schemas/image.yaml
                additionalProperties: false
                properties:
                  id:
                    type: string
                    description: |
                      __Read-only__ The unique ID of this Image.
                    example: linode/debian11
                    readOnly: true
                    x-linode-cli-display: 1
                  type:
                    type: string
                    description: |-
                      __Read-only__ How the Image was created.

                      "Manual" Images can be created at any time.

                      "Automatic" Images are created automatically from a deleted Linode.
                    example: manual
                    readOnly: true
                    enum:
                    - manual
                    - automatic
                    x-linode-filterable: true
                  description:
                    type: string
                    description: |
                      A detailed description of this Image.
                    example: Example Image description.
                    nullable: true
                    minLength: 1
                    maxLength: 65000
                    x-linode-cli-color:
                      None: black
                      default_: white
                    x-linode-cli-display: 4
                  capabilities:
                    type: array
                    description: |-
                      __Read-only__ A list containing the following possible capabilities of this Image:

                      `cloud-init`: This Image supports cloud-init with [Metadata](https://www.linode.com/docs/products/compute/compute-instances/guides/metadata/). Only applies to public Images.
                    example:
                    - cloud-init
                    readOnly: true
                    items:
                      type: string
                    x-linode-cli-display: 8
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Image was created.
                    example: '2021-08-14T22:44:02'
                    readOnly: true
                    format: date-time
                  created_by:
                    type: string
                    description: |
                      __Read-only__ The name of the User who created this Image, or "linode" for public Images.
                    example: linode
                    readOnly: true
                  deprecated:
                    type: boolean
                    description: |
                      __Read-only__ Whether or not this Image is deprecated. Will only be true for deprecated
                      public Images.
                    example: false
                    readOnly: true
                    x-linode-filterable: true
                  eol:
                    type: string
                    description: |
                      __Read-only__ The date of the public Image's planned end of life. `null` for private
                      Images.
                    example: '2026-07-01T04:00:00'
                    nullable: true
                    readOnly: true
                    format: date-time
                  expiry:
                    type: string
                    description: |
                      __Read-only__ Only Images created automatically from a deleted Linode (type=automatic)
                      will expire. `null` for private Images.
                    example: null
                    nullable: true
                    readOnly: true
                    format: date-time
                  is_public:
                    type: boolean
                    description: |
                      __Read-only__ True if the Image is a public distribution image. False if Image
                      is private Account-specific Image.
                    example: true
                    readOnly: true
                    x-linode-cli-display: 5
                    x-linode-filterable: true
                  label:
                    type: string
                    description: |
                      A short description of the Image.
                    example: Debian 11
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  size:
                    type: integer
                    description: |
                      __Read-only__ The minimum size this Image needs to deploy. Size is in MB.
                    example: 2500
                    readOnly: true
                    x-linode-cli-display: 6
                    x-linode-filterable: true
                  status:
                    type: string
                    description: |-
                      __Read-only__ The current status of this Image.

                      Only Images in an "available" status can be deployed. Images in a "creating" status are being created from a Linode Disk, and will become "available" shortly. Images in a "pending_upload" status are waiting for data to be [uploaded](https://techdocs.akamai.com/linode-api/reference/post-upload-image), and become "available" after the upload and processing are complete.

                      The "+order_by" and "+order" operators are not available for [filtering](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) on this key.
                    example: available
                    readOnly: true
                    enum:
                    - creating
                    - pending_upload
                    - available
                    x-linode-cli-display: 7
                    x-linode-filterable: true
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Image was last updated.
                    example: '2021-08-14T22:44:02'
                    readOnly: true
                    format: date-time
                  vendor:
                    type: string
                    description: |
                      __Read-only__ The upstream distribution vendor. `null` for private Images.
                    example: Debian
                    nullable: true
                    readOnly: true
                    x-linode-cli-color:
                      None: black
                      default_: white
                    x-linode-cli-display: 3
                    x-linode-filterable: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-image
      security:
      - personalAccessToken: []
      - oauth:
        - images:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "label": "My gold-master image",
                "description": "The detailed description of my Image."
              }' \
              https://api.linode.com/v4/images/private/12345
      - lang: CLI
        source: |-
          linode-cli images update private/12345 \
            --label "My gold-master image" \
            --description "The detailed description \
              of my Image."
      x-linode-cli-action: update
      x-linode-grant: read_write
      x-original-op-id: updateImage
      x-original-op-title: Image Update
    delete:
      operationId: delete-image
      summary: Delete an image
      tags:
      - Images
      description: |-
        Deletes a private Image you have permission to `read_write`.

        __Deleting an Image is a destructive action and cannot be undone.__


        ---


        - __OAuth__.

            ```
            images:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: images:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Delete successful
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-image
      security:
      - personalAccessToken: []
      - oauth:
        - images:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/images/private/12345
      - lang: CLI
        source: linode-cli images delete 12345
      x-linode-cli-action:
      - delete
      - rm
      x-linode-grant: read_write
      x-original-op-id: deleteImage
      x-original-op-title: Image Delete
    x-linode-cli-command: images
  /linode/instances:
    x-akamai:
      file-path: paths/linode-instances.yaml
      path-info: /linode/instances
    post:
      operationId: post-linode-instance
      summary: Create a Linode
      tags:
      - Linode instances
      description: |-
        Creates a Linode Instance on your Account. In order for this request to complete successfully, your User must have the `add_linodes` grant. Creating a new Linode will incur a charge on your Account.

        Linodes can be created using one of the available Types. Run [List Linode types](https://techdocs.akamai.com/linode-api/reference/get-linode-types) to get more information about each Type's specs and cost.

        Linodes can be created in any one of our available Regions, which are accessible from the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation.

        In an effort to fight spam, Linode restricts outbound connections on ports 25, 465, and 587 on all Linodes for new accounts created after November 5th, 2019. For more information, see our guide on [Running a Mail Server](https://www.linode.com/docs/guides/running-a-mail-server/).

        __Important__: You must be an unrestricted User in order to add or modify tags on Linodes.

        Linodes can be created in a number of ways:

        - Using a Linode Public Image distribution or a Private Image you created based on another Linode.

          - Run the [List images](https://techdocs.akamai.com/linode-api/reference/get-images) operation with authentication to view all available Images.

          - The Linode will be `running` after it completes `provisioning`.
          - A default config with two Disks, one being a 512 swap disk, is created.
            - `swap_size` can be used to customize the swap disk size.
          - Requires a `root_pass` be supplied to use for the root User's Account.
          - It is recommended to supply SSH keys for the root User using the `authorized_keys` field.
          - You may also supply a list of usernames via the `authorized_users` field.
            - These users must have an SSH Key associated with your Profile first. See the [Add an SSH key](https://techdocs.akamai.com/linode-api/reference/post-add-ssh-key)) operation for more information.

        - Using cloud-init with [Metadata](https://www.linode.com/docs/products/compute/compute-instances/guides/metadata/).
          - Automate system configuration and software installation by providing a base-64 encoded [cloud-config](https://www.linode.com/docs/products/compute/compute-instances/guides/metadata-cloud-config/) file.
          - Requires a compatible Image. You can determine compatible Images by checking for `cloud-init` under `capabilities` when running [List images](https://techdocs.akamai.com/linode-api/reference/get-images).
          - Requires a compatible Region.  You can determine compatible Regions by checking for `Metadata` under `capabilities` when running [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions).

        - Using a StackScript.

          - Run [List StackScripts](https://techdocs.akamai.com/linode-api/reference/get-stack-scripts) for a list of available StackScripts.
          - The Linode will be `running` after it completes `provisioning`.
          - Requires a compatible Image to be supplied.
            - Run [Get a StackScript](https://techdocs.akamai.com/linode-api/reference/get-stack-script) for compatible Images.
          - Requires a `root_pass` be supplied to use for the root User's Account.
          - It is recommended to supply SSH keys for the root User using the `authorized_keys` field.
          - You may also supply a list of usernames via the `authorized_users` field.
            - These users must have an SSH Key associated with your Profile first. See [Add an SSH key](https://techdocs.akamai.com/linode-api/reference/post-add-ssh-key) for more information.

        - Using one of your other Linode's backups.
          - You must create a Linode large enough to accommodate the Backup's size.
          - The Disks and Config will match that of the Linode that was backed up.
          - The `root_pass` will match that of the Linode that was backed up.

        - Attached to a private VLAN.
          - Review the `interfaces` property of the [Request Body Schema](https://techdocs.akamai.com/linode-api/reference/post-linode-instance) for details.
          - For more information, see our guide on [Getting Started with VLANs](https://www.linode.com/docs/products/networking/vlans/get-started/).

        - Create an empty Linode.
          - The Linode will remain `offline` and must be manually started.
            - Run [Boot a Linode](https://techdocs.akamai.com/linode-api/reference/post-boot-linode-instance).
          - Disks and Configs must be created manually.
          - This is only recommended for advanced use cases.


        ---


        - __CLI__.

            ```
            linode-cli linodes create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The requested initial state of a new Linode.
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - type
              - region
              allOf:
              - type: object
                description: |
                  Common properties for creating and rebuilding Linodes.
                x-akamai:
                  file-path: schemas/linode-request.yaml
                additionalProperties: false
                properties:
                  authorized_keys:
                    type: array
                    description: |
                      __Write-only__ A list of public SSH keys that will be automatically appended to the
                      root user's `~/.ssh/authorized_keys` file when deploying from
                      an Image.
                    example:
                    - ssh-rsa AAAA_valid_public_ssh_key_123456785== user@their-computer
                    items:
                      type: string
                    writeOnly: true
                  authorized_users:
                    type: array
                    description: |
                      __Write-only__ A list of usernames. If the usernames have associated SSH keys, the
                      keys will be appended to the root users `~/.ssh/authorized_keys`
                      file automatically when deploying from an Image.
                    example:
                    - myUser
                    - secondaryUser
                    items:
                      type: string
                    writeOnly: true
                  booted:
                    type: boolean
                    description: |
                      __Write-only__ This field defaults to `true` if the Linode is created with an Image
                      or from a Backup. If it is deployed from an Image or a Backup
                      and you wish it to remain `offline` after deployment, set this
                      to `false`.
                    default: true
                    writeOnly: true
                  disk_encryption:
                    type: string
                    description: |-
                      Local disk encryption ensures that your data stored on compute instances is encrypted. Encryption converts the data on the compute instance into unreadable code. Decryption of the the disk requires other systems within the datacenter. This requirement protects against data leakage if the disk is removed from a datacenter, lost, stolen, recycled or disposed.

                      By default, encryption is `enabled` on all compute instances but you can opt-out of implementing this feature. If you opted-out of encryption or if the compute instance was created prior to the introduction of the local disk encryption support, you can encrypt your data using Rebuild.
                    enum:
                    - enabled
                    - disabled
                  image:
                    type: string
                    description: |-
                      An Image ID to deploy the Linode Disk from.

                      Run the [List images](https://techdocs.akamai.com/linode-api/reference/get-images) operation with authentication to view all available Images. Official Linode Images start with `linode/`, while your Account's Images start with `private/`. Creating a disk from a Private Image requires `read_only` or `read_write` permissions for that Image. Run the [Update a user's grants](https://techdocs.akamai.com/linode-api/reference/put-user-grants) operation to adjust permissions for an Account Image.
                    example: linode/debian9
                  metadata:
                    type: object
                    description: |
                      __Write-only__ An object containing user-defined data relevant to the creation of Linodes.
                    additionalProperties: false
                    properties:
                      user_data:
                        type: string
                        description: |-
                          Base64-encoded [cloud-config](https://www.linode.com/docs/products/compute/compute-instances/guides/metadata-cloud-config/) data.

                          Cannot be modified after provisioning. To update, use either the [Clone a Linode](https://techdocs.akamai.com/linode-api/reference/post-clone-linode-instance) or [Rebuild a Linode](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance) operations.

                          Must not be included when cloning to an existing Linode.

                          Unencoded data must not exceed 65535 bytes, or about 16kb encoded.
                        example: I2Nsb3VkLWNvbmZpZwpwYWNrYWdlX3VwZGF0ZTogdHJ1ZQpwYWNrYWdlX3VwZ3JhZGU6IHRydWU=
                        format: byte
                    writeOnly: true
                  root_pass:
                    type: string
                    description: |-
                      __Write-only__ This sets the root user's password on a newly-created Linode Disk when deploying from an Image.

                      - __Required__ when creating a Linode Disk from an Image, including when using a StackScript.

                      - Must meet a password strength score requirement that is calculated internally by the API. If the strength requirement is not met, you will receive a `Password does not meet strength requirement` error.
                    example: aComplexP@ssword
                    minLength: 7
                    maxLength: 128
                    format: password
                    writeOnly: true
                  stackscript_data:
                    type: object
                    description: |-
                      This field is required only if the StackScript being deployed requires input data from the User for successful completion. See [User Defined Fields (UDFs)](https://www.linode.com/docs/products/tools/stackscripts/guides/write-a-custom-script/#declare-user-defined-fields-udfs) for more details.

                      This field is required to be valid JSON.

                      Total length cannot exceed 65,535 characters.
                    example:
                      gh_username: linode
                    maxLength: 65535
                  stackscript_id:
                    type: integer
                    description: |
                      A StackScript ID that will cause the referenced StackScript to be run during deployment
                      of this Linode. A compatible `image` is required to use a StackScript.
                      To get a list of available StackScript and their permitted Images,
                      run [List StackScripts](https://techdocs.akamai.com/linode-api/reference/get-stack-scripts).
                      This field cannot be used when deploying from a Backup or a
                      Private Image.
                    example: 10079
              - properties:
                  tags:
                    type: array
                    description: |
                      An array of tags applied to this object.  Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  type:
                    type: string
                    description: |
                      The [Linode type](https://techdocs.akamai.com/linode-api/reference/get-linode-types) of the Linode you are creating.
                    example: g6-standard-2
                  backup_id:
                    type: integer
                    description: |-
                      A Backup ID from another Linode's available backups. Your User must have `read_write` access to that Linode, the Backup must have a `status` of `successful`, and the Linode must be deployed to the same `region` as the Backup. Run [List backups](https://techdocs.akamai.com/linode-api/reference/get-backups) for a Linode's available backups.

                      This field and the `image` field are mutually exclusive.
                    example: 1234
                  backups_enabled:
                    type: boolean
                    description: |-
                      If this field is set to `true`, the created Linode will automatically be enrolled in the Linode Backup service. This will incur an additional charge. The cost for the Backup service is dependent on the Type of Linode deployed.

                      This option is always treated as `true` if the account-wide `backups_enabled` setting is `true`.  See [Get account settings](https://techdocs.akamai.com/linode-api/reference/get-account-settings) for more information.

                      Backup pricing is included in the response from [List types](https://techdocs.akamai.com/linode-api/reference/get-linode-types)
                  firewall_id:
                    type: integer
                    description: |
                      The `id` of the Firewall to attach this Linode to upon creation.
                  group:
                    type: string
                    description: |
                      A deprecated property denoting a group label for this Linode.
                    example: Linode-Group
                    deprecated: true
                    x-linode-filterable: true
                  interfaces:
                    type: array
                    description: |-
                      An array of Network Interfaces to add to this Linode's Configuration Profile. At least one and up to three Interface objects can exist in this array. The position in the array determines which of the Linode's network Interfaces is configured:

                      - First [0]:  eth0
                      - Second [1]: eth1
                      - Third [2]:  eth2

                      When updating a Linode's Interfaces, _each Interface must be redefined_. An empty `interfaces` array results in a default `public` type Interface configuration only.

                      If no public Interface is configured, public IP addresses are still assigned to the Linode but will not be usable without manual configuration.

                      __Note:__ Changes to Linode Interface configurations can be enabled by rebooting the Linode.

                      `vpc` details

                      See the [VPC documentation](https://www.linode.com/docs/products/networking/vpc/#technical-specifications) guide for its specifications and limitations.

                      `vlan` details

                      - Only Next Generation Network (NGN) data centers support VLANs. Run the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation to view the capabilities of data center regions. If a VLAN is attached to your Linode and you attempt to migrate or clone it to a non-NGN data center, the migration or cloning will not initiate. If a Linode cannot be migrated or cloned because of an incompatibility, you will be prompted to select a different data center or contact support.
                      - See the [VLANs Overview](https://www.linode.com/docs/products/networking/vlans/#technical-specifications) guide to view additional specifications and limitations.
                    example:
                    - id: 101
                      ipam_address: null
                      ipv4: null
                      label: null
                      primary: false
                      purpose: public
                      subnet_id: null
                      vpc_id: null
                    - id: 102
                      ipam_address: 10.0.0.1/24
                      ipv4:
                        nat_1_1: null
                        vpc: 10.0.0.2
                      label: vlan-1
                      primary: false
                      purpose: vlan
                      subnet_id: null
                      vpc_id: null
                    - id: 103
                      ipam_address: null
                      ipv4:
                        nat_1_1: 203.0.113.2
                        vpc: 10.0.1.2
                      label: null
                      primary: true
                      purpose: vpc
                      subnet_id: 101
                      vpc_id: 111
                    x-akamai:
                      file-path: schemas/linode-config-interfaces.yaml
                    required:
                    - purpose
                    minItems: 1
                    maxItems: 3
                    uniqueItems: true
                    items:
                      type: object
                      description: |
                        The Network Interface to apply to this Linode's configuration profile.
                      x-akamai:
                        file-path: schemas/linode-config-interface.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ The unique ID representing this Interface.
                          example: 101
                          readOnly: true
                          x-linode-cli-display: 1
                        active:
                          type: boolean
                          description: |
                            Returns `true` if the Interface is in use, meaning that Compute Instance
                            has been booted using the Configuration Profile to which
                            the Interface belongs. Otherwise returns `false`.
                          example: true
                        ip_ranges:
                          type: array
                          description: |-
                            An array of IPv4 CIDR VPC Subnet ranges that are routed to this Interface.

                            - Array items are only allowed for `vpc` type Interfaces.
                            - This must be empty for non-`vpc` type Interfaces.

                            For requests:

                            - Addresses in submitted ranges must not already be actively assigned.
                            - Submitting values replaces any existing values.
                            - Submitting an empty array removes any existing values.
                            - Omitting this property results in no change to existing values.
                          example:
                          - 10.0.0.64/26
                          - fd04:495a:691c:971c::1:0/112
                          nullable: true
                          items:
                            type: string
                            format: ip
                        ipam_address:
                          type: string
                          description: |-
                            This Network Interface's private IP address in Classless Inter-Domain Routing (CIDR) notation.

                            For `vlan` purpose Interfaces:

                            - Must be unique among the Linode's Interfaces to avoid conflicting addresses.
                            - Should be unique among devices attached to the VLAN to avoid conflict.
                            - The Linode is configured to use this address for the associated Interface upon reboot if Network Helper is enabled. If Network Helper is disabled, the address can be enabled with [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).

                            For `public` purpose Interfaces:

                            - In requests, must be an empty string (`""`) or `null` if included.
                            - In responses, always returns `null`.

                            For `vpc` purpose Interfaces:

                            - In requests, must be an empty string (`""`) or `null` if included.
                            - In responses, always returns `null`.
                          example: 10.0.0.1/24
                          nullable: true
                          format: ip/netmask
                          x-linode-cli-display: 4
                        ipv4:
                          type: object
                          description: |
                            IPv4 addresses configured for this Interface. Only allowed for `vpc`
                            type Interfaces. Returns `null` if no `vpc` Interface
                            is assigned.
                          additionalProperties: false
                          properties:
                            nat_1_1:
                              type: string
                              description: |-
                                The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the VPC Subnet IPv4 address assigned to this Interface.

                                - Only allowed for `vpc` type Interfaces.
                                - Returns `null` if no 1:1 NAT is set for a `vpc` type Interface.
                                - Returns an empty string (`""`) for non-`vpc` type Interfaces.

                                For requests:

                                - Setting this value to `any` enables the Linode's assigned public IPv4 address on this Interface and establishes a 1:1 NAT between the public IPv4 and VPC Subnet IPv4 addresses.
                                - Setting the value to a specific public IPv4 address that is assigned to the Linode enables a 1:1 NAT between that address and the VPC Subnet IPv4 address.
                                - The public IPv4 address can't be shared with another Linode.
                                - If omitted, set to `null`, or set to an empty string (`""`), no 1:1 NAT is established.

                                __Note__: When creating a new compute-instance, you can't set this to a specific IPv4 address. When a new compute instance is created, the network establishes a public IPv4 address for it. Since this address doesn't exist yet, you can't include a custom IPv4 address to change it. Once your compute instances is created, you can [update your configuration profile interface](https://www.linode.com/docs/api/linode-instances/#configuration-profile-interface-update) to change the `nat_1_1` address.
                              example: 203.0.113.2
                              nullable: true
                              format: ip
                            vpc:
                              type: string
                              description: |-
                                The VPC Subnet IPv4 address for this Interface.

                                - Only allowed for `vpc` type Interfaces.
                                - Returns an empty string (`""`) for non-`vpc` type Interfaces.

                                For requests:

                                - Must not already be actively assigned as an address or within a range to any Linodes.
                                - Must not be the first two or last two addresses in the Subnet IPv4 Range.
                                - If omitted, a valid address within the Subnet IPv4 range is automatically assigned.
                              example: 10.0.0.2
                              nullable: true
                              format: ip
                        label:
                          type: string
                          description: |-
                            The name of this Interface.

                            For `vlan` purpose Interfaces:

                            - Required.
                            - Must be unique among the Linode's Interfaces (a Linode cannot be attached to the same VLAN multiple times).
                            - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                            - If the VLAN label is new, a VLAN is created. Up to 10 VLANs can be created in each data center region. To view your active VLANs, run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans) operation.

                            For `public` purpose Interfaces:

                            - In requests, must be an empty string (`""`) or `null` if included.
                            - In responses, always returns `null`.

                            For `vpc` purpose Interfaces:

                            - In requests, must be an empty string (`""`) or `null` if included.
                            - In responses, always returns `null`.
                          example: example-interface
                          nullable: true
                          minLength: 1
                          maxLength: 64
                          pattern: '[a-zA-Z0-9-]+'
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        primary:
                          type: boolean
                          description: |-
                            The primary Interface is configured as the default route to the Linode.

                            Each Configuration Profile can have up to one `"primary": true` Interface at a time.

                            Must be `false` for `vlan` type Interfaces.

                            If no Interface is configured as the primary, the first non-`vlan` type Interface in the `interfaces` array is automatically treated as the primary Interface.
                          example: true
                        purpose:
                          type: string
                          description: |-
                            The type of Interface.

                            - `public`
                              - Only one `public` Interface per Linode can be defined.
                              - The Linode's default public IPv4 address is assigned to the `public` Interface.
                              - A Linode must have a public Interface in the first/eth0 position to be reachable via the public internet upon boot without additional system configuration. If no `public` Interface is configured, the Linode is not directly reachable via the public internet. In this case, access can only be established via [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/) or other Linodes connected to the same VLAN or VPC.

                            - `vlan`
                              - Configuring a `vlan` purpose Interface attaches this Linode to the VLAN with the specified `label`.
                              - The Linode is configured to use the specified `ipam_address`, if any.

                            - `vpc`
                              - Configuring a `vpc` purpose Interface attaches this Linode to the existing VPC Subnet with the specified `subnet_id`.
                              - When the Interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC Subnet. See `ipv4.vpc` for more information.
                          example: vlan
                          enum:
                          - public
                          - vlan
                          - vpc
                          x-linode-cli-display: 3
                        subnet_id:
                          type: integer
                          description: |-
                            The `id` of the VPC Subnet for this Interface.

                            In requests, this value is used to assign a Linode to a VPC Subnet.

                            - Required for `vpc` type Interfaces.
                            - Returns `null` for non-`vpc` type Interfaces.
                            - Once a VPC Subnet is assigned to an Interface, it cannot be updated.
                            - The Linode must be rebooted with the Interface's Configuration Profile to complete assignment to a VPC Subnet.
                          example: 101
                          nullable: true
                        vpc_id:
                          type: integer
                          description: |
                            __Read-only__ The `id` of the VPC configured for this Interface. Returns
                            `null` for non-`vpc` type Interfaces.
                          example: 111
                          nullable: true
                          readOnly: true
                  label:
                    type: string
                    description: |-
                      The Linode's label is for display purposes only. If no label is provided for a Linode, a default will be assigned.

                      Linode labels have the following constraints:

                      - Must begin and end with an alphanumeric character.
                      - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                      - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
                    example: linode123
                    minLength: 3
                    maxLength: 64
                    pattern: ^[a-zA-Z]((?!--|__|..)[a-zA-Z0-9-_.])+$
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  placement_group:
                    type: object
                    description: |-
                      Include this to assign this Linode to an existing [placement group](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/). These constraints apply:

                      - The target placement group needs to be in the same `region` set for this Linode.
                      - The placement group needs to have capacity. Run the [Get a region](https://techdocs.akamai.com/linode-api/reference/get-region) operation and store the `maximum_linodes_per_pg` value to know the Linode limit per placement group. You can then run the [Get a placement group](https://techdocs.akamai.com/linode-api/reference/get-placement-group) operation to review the Linodes in that group.
                    additionalProperties: false
                    properties:
                      id:
                        type: integer
                        description: |
                          The placement group's ID. You need to provide it for all operations impacting
                          it.
                        example: 528
                        nullable: false
                        x-linode-cli-display: 1
                  private_ip:
                    type: boolean
                    description: |
                      If true, the created Linode will have private networking enabled and assigned a private
                      IPv4 address.
                    example: true
                  region:
                    type: string
                    description: |
                      The [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the Linode will be located.
                    example: us-east
                  swap_size:
                    type: integer
                    description: |
                      When deploying from an Image, this field is optional, otherwise it is ignored. This
                      is used to set the swap disk size for the newly-created Linode.
                    example: 512
                    default: 512
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
        x-linode-cli-allowed-defaults:
        - authorized_users
        - region
        - image
        - type
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A new Linode is being created.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/linode.yaml
                title: Linode
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Linode's ID which must be provided for all operations impacting
                      this Linode.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  tags:
                    type: array
                    description: |
                      An array of tags applied to this object.  Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  type:
                    type: string
                    description: |
                      __Read-only__ This is the [Linode type](https://techdocs.akamai.com/linode-api/reference/get-linode-types)
                      that this Linode was deployed with. To change a Linode's Type,
                      use [Resize a Linode](https://techdocs.akamai.com/linode-api/reference/post-resize-linode-instance).
                    example: g6-standard-1
                    readOnly: true
                    x-linode-cli-display: 4
                  alerts:
                    type: object
                    additionalProperties: false
                    properties:
                      cpu:
                        type: integer
                        description: |-
                          The percentage of CPU usage required to trigger an alert. If the average CPU usage over two hours exceeds this value, we'll send you an alert. Your Linode's total CPU capacity is represented as 100%, multiplied by its number of cores.

                          For example, a two core Linode's CPU capacity is represented as 200%. If you want to be alerted at 90% of a two core Linode's CPU capacity, set the alert value to `180`.

                          The default value is 90% multiplied by the number of cores.

                          If the value is set to `0` (zero), the alert is disabled.
                        example: 180
                      io:
                        type: integer
                        description: |
                          The amount of disk IO operation per second required to trigger an alert. If
                          the average disk IO over two hours exceeds this value, we'll
                          send you an alert. If set to `0` (zero), this alert is disabled.
                        example: 10000
                      network_in:
                        type: integer
                        description: |
                          The amount of incoming traffic, in Mbit/s, required to trigger an alert. If the
                          average incoming traffic over two hours exceeds this value,
                          we'll send you an alert. If this is set to `0` (zero), the
                          alert is disabled.
                        example: 10
                      network_out:
                        type: integer
                        description: |
                          The amount of outbound traffic, in Mbit/s, required to trigger an alert. If the
                          average outbound traffic over two hours exceeds this value,
                          we'll send you an alert. If this is set to `0` (zero), the
                          alert is disabled.
                        example: 10
                      transfer_quota:
                        type: integer
                        description: |
                          The percentage of network transfer that may be used before an alert is
                          triggered. When this value is exceeded, we'll alert you.
                          If this is set to `0` (zero), the alert is disabled.
                        example: 80
                  backups:
                    type: object
                    description: |
                      Information about this Linode's backups status. For information about available backups,
                      run [List backups](https://techdocs.akamai.com/linode-api/reference/get-backups).
                    additionalProperties: false
                    properties:
                      available:
                        type: boolean
                        description: |-
                          __Read-only__ Whether Backups for this Linode are available for restoration.

                          Backups undergoing maintenance are not available for restoration.
                        example: true
                        readOnly: true
                      enabled:
                        type: boolean
                        description: |
                          __Read-only__ If this Linode has the Backup service enabled. To enable backups,
                          run [Enable backups](https://techdocs.akamai.com/linode-api/reference/post-enable-backups).
                        example: true
                        readOnly: true
                      last_successful:
                        type: string
                        description: |
                          __Read-only__ The last successful backup date. 'null' if there was no previous
                          backup.
                        example: '2018-01-01T00:01:01'
                        readOnly: true
                        format: date-time
                      schedule:
                        type: object
                        additionalProperties: false
                        properties:
                          day:
                            type: string
                            description: |-
                              The day of the week that your Linode's weekly Backup is taken. If not set manually, a day will be chosen for you. Backups are taken every day, but backups taken on this day are preferred when selecting backups to retain for a longer period.

                              If not set manually, then when backups are initially enabled, this may come back as `Scheduling` until the `day` is automatically selected.
                            example: Saturday
                            nullable: true
                            enum:
                            - Scheduling
                            - Sunday
                            - Monday
                            - Tuesday
                            - Wednesday
                            - Thursday
                            - Friday
                            - Saturday
                          window:
                            type: string
                            description: |-
                              The window in which your backups will be taken, in UTC. A backups window is a two-hour span of time in which the backup may occur.

                              For example, `W10` indicates that your backups should be taken between 10:00 and 12:00. If you do not choose a backup window, one will be selected for you automatically.

                              If not set manually, when backups are initially enabled this may come back as `Scheduling` until the `window` is automatically selected.
                            example: W22
                            nullable: true
                            enum:
                            - Scheduling
                            - W0
                            - W2
                            - W4
                            - W6
                            - W8
                            - W10
                            - W12
                            - W14
                            - W16
                            - W18
                            - W20
                            - W22
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Linode was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  disk_encryption:
                    type: string
                    description: |
                      Local disk encryption ensures that your data stored on compute instances is encrypted.
                      Encryption converts the data on the compute instance into unreadable
                      code. Decryption of the the disk requires other systems within
                      the datacenter. This requirement protects against data leakage
                      if the disk is removed from a datacenter, lost, stolen, recycled
                      or disposed.
                    example: enabled
                    default: enabled
                    enum:
                    - enabled
                    - disabled
                  group:
                    type: string
                    description: |
                      A deprecated property denoting a group label for this Linode.
                    example: Linode-Group
                    deprecated: true
                    x-linode-filterable: true
                  has_user_data:
                    type: boolean
                    description: |
                      __Read-only__ Whether this compute instance was provisioned utilizing `user_data`
                      provided via the Metadata service. See the [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
                      description for more information on Metadata.
                    example: true
                    readOnly: true
                  host_uuid:
                    type: string
                    description: |
                      __Read-only__ The Linode's host machine, as a UUID.
                    example: 3a3ddd59d9a78bb8de041391075df44de62bfec8
                    readOnly: true
                    format: uuid
                  hypervisor:
                    type: string
                    description: |
                      __Read-only__ The virtualization software powering this Linode.
                    example: kvm
                    readOnly: true
                    enum:
                    - kvm
                  image:
                    example: linode/debian10
                    nullable: true
                    readOnly: true
                    allOf:
                    - type: string
                      description: |-
                        An Image ID to deploy the Linode Disk from.

                        Run the [List images](https://techdocs.akamai.com/linode-api/reference/get-images) operation with authentication to view all available Images. Official Linode Images start with `linode/`, while your Account's Images start with `private/`. Creating a disk from a Private Image requires `read_only` or `read_write` permissions for that Image. Run the [Update a user's grants](https://techdocs.akamai.com/linode-api/reference/put-user-grants) operation to adjust permissions for an Account Image.
                      example: linode/debian9
                    x-linode-cli-display: 5
                    x-linode-filterable: true
                  ipv4:
                    type: array
                    description: |-
                      __Read-only__ This Linode's IPv4 Addresses. Each Linode is assigned a single public IPv4 address upon creation, and may get a single private IPv4 address if needed. You may need to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) to get additional IPv4 addresses.

                      IPv4 addresses may be reassigned between your Linodes, or shared with other Linodes. See the [networking](https://techdocs.akamai.com/linode-api/reference/post-firewalls) operations for details.
                    example:
                    - 203.0.113.1
                    - 192.0.2.1
                    readOnly: true
                    format: ipv4
                    items:
                      type: string
                    x-linode-cli-display: 7
                    x-linode-filterable: true
                  ipv6:
                    type: string
                    description: |
                      __Read-only__ This Linode's IPv6 SLAAC address. This address is specific to a Linode,
                      and may not be shared. If the Linode has not been assigned an
                      IPv6 address, the return value will be `null`.
                    example: c001:d00d::1337/128
                    nullable: true
                    readOnly: true
                    format: ipv6/128
                  label:
                    type: string
                    description: |-
                      The Linode's label is for display purposes only. If no label is provided for a Linode, a default will be assigned.

                      Linode labels have the following constraints:

                      - Must begin and end with an alphanumeric character.
                      - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                      - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
                    example: linode123
                    minLength: 3
                    maxLength: 64
                    pattern: ^[a-zA-Z]((?!--|__|..)[a-zA-Z0-9-_.])+$
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  placement_group:
                    type: object
                    description: |
                      __Read-only__ Details on the [placement group](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/)
                      that this Linode belongs to. Empty if the Linode isn't in a
                      placement group.
                    additionalProperties: false
                    nullable: true
                    readOnly: true
                    properties:
                      id:
                        type: integer
                        description: |
                          The placement group's ID. You need to provide it for all operations impacting
                          it.
                        example: 528
                        nullable: false
                        x-linode-cli-display: 1
                      affinity_type:
                        type: string
                        description: |-
                          __Read-only__ How compute instances are distributed in your placement group. `anti-affinity:local` places compute instances in separate fault domains, but still in the same region.

                          __Note__: With the limited availability release, only `anti_affinity:local` is available for `affinity_type`.
                        example: anti-affinity:local
                        readOnly: true
                        x-linode-cli-display: 1
                        x-linode-filterable: true
                      is_strict:
                        type: boolean
                        description: |-
                          How the API enforces your `affinity_type`:

                          - Set to `true`, your group is strict. You can't add more compute instances to your placement group if your preferred container lacks capacity or is unavailable.
                          - Set to `false`, your group is flexible. You can add more compute instances to it even if they violate the `affinity_type`. If you violate the `affinity_type` your placement group becomes non-compliant and you need to wait for our assistance.
                        example: true
                        nullable: false
                      label:
                        type: string
                        description: |-
                          The unique name set for the placement group. A label has these constraints:

                          - It needs to begin and end with an alphanumeric character.
                          - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                        example: PG_Miami_failover
                        minLength: 1
                        x-linode-cli-display: 1
                        x-linode-filterable: true
                  region:
                    type: string
                    description: |
                      __Read-only__ This is the [region](https://techdocs.akamai.com/linode-api/reference/get-regions)
                      where the Linode was deployed. A Linode's region can only be
                      changed by initiating a [cross data center migration](https://techdocs.akamai.com/linode-api/reference/post-migrate-linode-instance).
                    example: us-east
                    readOnly: true
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  specs:
                    type: object
                    description: |
                      __Read-only__ Information about the resources available to this Linode.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      disk:
                        type: integer
                        description: |
                          __Read-only__ The amount of storage space, in MB, this Linode has access to.
                          A typical Linode will divide this space between a primary
                          disk with an `image` deployed to it, and a swap disk, usually
                          512 MB. This is the default configuration created when deploying
                          a Linode with an `image` through [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance).
                          While this configuration is suitable for 99% of use cases,
                          if you need finer control over your Linode's disks, see
                          the [List disks](https://techdocs.akamai.com/linode-api/reference/get-linode-disks)
                          operations.
                        example: 81920
                        readOnly: true
                      gpus:
                        type: integer
                        description: |
                          __Read-only__ The number of gpus this Linode has access to.
                        example: 0
                        readOnly: true
                      memory:
                        type: integer
                        description: |-
                          __Read-only__ The amount of RAM, in MB, this Linode has access to.

                          Typically, a Linode boots with all of its available RAM, but this can be configured in a Config profile. See the [List config profiles](https://techdocs.akamai.com/linode-api/reference/get-linode-configs) operations and the LinodeConfig object for more information.
                        example: 4096
                        readOnly: true
                      transfer:
                        type: integer
                        description: |
                          __Read-only__ The amount of network transfer this Linode is allotted each
                          month.
                        example: 4000
                        readOnly: true
                      vcpus:
                        type: integer
                        description: |
                          __Read-only__ The number of vcpus this Linode has access to.
                        example: 2
                        readOnly: true
                  status:
                    type: string
                    description: |
                      __Read-only__ A brief description of this Linode's current state. This field may change
                      without direct action from you. For example, when a Linode goes
                      into maintenance mode its status will display "stopped".
                    example: running
                    readOnly: true
                    enum:
                    - running
                    - offline
                    - booting
                    - rebooting
                    - shutting_down
                    - provisioning
                    - deleting
                    - migrating
                    - rebuilding
                    - cloning
                    - restoring
                    - stopped
                    - billing_suspension
                    x-linode-cli-color:
                      billing_suspension: red
                      default_: yellow
                      offline: red
                      running: green
                    x-linode-cli-display: 6
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Linode was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  watchdog_enabled:
                    type: boolean
                    description: |
                      The watchdog, named Lassie, is a Shutdown Watchdog that monitors your Linode and
                      will reboot it if it powers off unexpectedly. It works by issuing
                      a boot job when your Linode powers off without a shutdown job
                      being responsible. To prevent a loop, Lassie will give up if
                      there have been more than 5 boot jobs issued within 15 minutes.
                    example: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-linode-instance
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "backup_id": 1234,
                "backups_enabled": true,
                "swap_size": 512,
                "image": "linode/ubuntu22.04",
                "root_pass": "aComplexP@ssword",
                "stackscript_id": 10079,
                "stackscript_data": {
                  "gh_username": "linode"
                },
                "interfaces": [
                  {
                    "purpose": "public",
                    "label": "",
                    "ipam_address": ""
                  },
                  {
                    "purpose": "vlan",
                    "label": "vlan-1",
                    "ipam_address": "10.0.0.1/24"
                  },
                  {
                    "purpose": "vpc",
                    "primary": false,
                    "label": "",
                    "ipam_address": "",
                    "subnet_id": 101,
                    "ipv4": {
                      "vpc": "10.0.1.2",
                      "nat_1_1": "any"
                    }
                  }
                ],
                "authorized_keys": [
                  "ssh-rsa AAAA_valid_public_ssh_key_123456785== user@their-computer"
                ],
                "authorized_users": [
                  "myUser",
                  "secondaryUser"
                ],
                "booted": true,
                "label": "linode123",
                "type": "g6-standard-2",
                "region": "us-east",
                "disk_encryption": "enabled",
                "group": "Linode-Group",
                "placement_group": {
                  "id": 528
                },
                "metadata": {
                  "user_data": "I2Nsb3VkLWNvbmZpZw=="
                },
                "firewall_id": 9000
              }' \
              https://api.linode.com/v4/linode/instances
      - lang: CLI
        source: |-
          linode-cli linodes create \
            --label linode123 \
            --root_pass aComplex@Password \
            --booted true \
            --stackscript_id 10079 \
            --stackscript_data '{"gh_username": "linode"}' \
            --region us-east \
            --disk_encryption enabled\
            --placement_group.id 528 \
            --type g6-standard-2 \
            --authorized_keys "ssh-rsa AAAA_valid_public_ssh_key_123456785== user@their-computer" \
            --authorized_users "myUser" \
            --authorized_users "secondaryUser" \
            --metadata.user_data "I2Nsb3VkLWNvbmZpZw==" \
            --firewall_id 9000
      x-linode-charge: true
      x-linode-cli-action: create
      x-linode-grant: add_linodes
      x-original-op-id: createLinodeInstance
      x-original-op-title: Linode Create
    get:
      operationId: get-linode-instances
      summary: List Linodes
      tags:
      - Linode instances
      description: |-
        Returns a paginated list of Linodes you have permission to view.


        ---


        - __OAuth__.

            ```
            linodes:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: linodes:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns an array of all Linodes on your Account.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      x-akamai:
                        file-path: schemas/linode.yaml
                      title: Linode
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ This Linode's ID which must be provided for all operations
                            impacting this Linode.
                          example: 123
                          readOnly: true
                          x-linode-cli-display: 1
                          x-linode-filterable: true
                        tags:
                          type: array
                          description: |
                            An array of tags applied to this object.  Tags are for organizational purposes
                            only.
                          example:
                          - example tag
                          - another example
                          items:
                            type: string
                          x-linode-filterable: true
                        type:
                          type: string
                          description: |
                            __Read-only__ This is the [Linode type](https://techdocs.akamai.com/linode-api/reference/get-linode-types)
                            that this Linode was deployed with. To change a Linode's
                            Type, use [Resize a Linode](https://techdocs.akamai.com/linode-api/reference/post-resize-linode-instance).
                          example: g6-standard-1
                          readOnly: true
                          x-linode-cli-display: 4
                        alerts:
                          type: object
                          additionalProperties: false
                          properties:
                            cpu:
                              type: integer
                              description: |-
                                The percentage of CPU usage required to trigger an alert. If the average CPU usage over two hours exceeds this value, we'll send you an alert. Your Linode's total CPU capacity is represented as 100%, multiplied by its number of cores.

                                For example, a two core Linode's CPU capacity is represented as 200%. If you want to be alerted at 90% of a two core Linode's CPU capacity, set the alert value to `180`.

                                The default value is 90% multiplied by the number of cores.

                                If the value is set to `0` (zero), the alert is disabled.
                              example: 180
                            io:
                              type: integer
                              description: |
                                The amount of disk IO operation per second required to trigger
                                an alert. If the average disk IO over two hours exceeds
                                this value, we'll send you an alert. If set to `0`
                                (zero), this alert is disabled.
                              example: 10000
                            network_in:
                              type: integer
                              description: |
                                The amount of incoming traffic, in Mbit/s, required to trigger
                                an alert. If the average incoming traffic over two
                                hours exceeds this value, we'll send you an alert.
                                If this is set to `0` (zero), the alert is disabled.
                              example: 10
                            network_out:
                              type: integer
                              description: |
                                The amount of outbound traffic, in Mbit/s, required to trigger
                                an alert. If the average outbound traffic over two
                                hours exceeds this value, we'll send you an alert.
                                If this is set to `0` (zero), the alert is disabled.
                              example: 10
                            transfer_quota:
                              type: integer
                              description: |
                                The percentage of network transfer that may be used before an alert
                                is triggered. When this value is exceeded, we'll alert
                                you. If this is set to `0` (zero), the alert is disabled.
                              example: 80
                        backups:
                          type: object
                          description: |
                            Information about this Linode's backups status. For information about available
                            backups, run [List backups](https://techdocs.akamai.com/linode-api/reference/get-backups).
                          additionalProperties: false
                          properties:
                            available:
                              type: boolean
                              description: |-
                                __Read-only__ Whether Backups for this Linode are available for restoration.

                                Backups undergoing maintenance are not available for restoration.
                              example: true
                              readOnly: true
                            enabled:
                              type: boolean
                              description: |
                                __Read-only__ If this Linode has the Backup service enabled. To
                                enable backups, run [Enable backups](https://techdocs.akamai.com/linode-api/reference/post-enable-backups).
                              example: true
                              readOnly: true
                            last_successful:
                              type: string
                              description: |
                                __Read-only__ The last successful backup date. 'null' if there was
                                no previous backup.
                              example: '2018-01-01T00:01:01'
                              readOnly: true
                              format: date-time
                            schedule:
                              type: object
                              additionalProperties: false
                              properties:
                                day:
                                  type: string
                                  description: |-
                                    The day of the week that your Linode's weekly Backup is taken. If not set manually, a day will be chosen for you. Backups are taken every day, but backups taken on this day are preferred when selecting backups to retain for a longer period.

                                    If not set manually, then when backups are initially enabled, this may come back as `Scheduling` until the `day` is automatically selected.
                                  example: Saturday
                                  nullable: true
                                  enum:
                                  - Scheduling
                                  - Sunday
                                  - Monday
                                  - Tuesday
                                  - Wednesday
                                  - Thursday
                                  - Friday
                                  - Saturday
                                window:
                                  type: string
                                  description: |-
                                    The window in which your backups will be taken, in UTC. A backups window is a two-hour span of time in which the backup may occur.

                                    For example, `W10` indicates that your backups should be taken between 10:00 and 12:00. If you do not choose a backup window, one will be selected for you automatically.

                                    If not set manually, when backups are initially enabled this may come back as `Scheduling` until the `window` is automatically selected.
                                  example: W22
                                  nullable: true
                                  enum:
                                  - Scheduling
                                  - W0
                                  - W2
                                  - W4
                                  - W6
                                  - W8
                                  - W10
                                  - W12
                                  - W14
                                  - W16
                                  - W18
                                  - W20
                                  - W22
                        created:
                          type: string
                          description: |
                            __Read-only__ When this Linode was created.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                        disk_encryption:
                          type: string
                          description: |
                            Local disk encryption ensures that your data stored on compute instances is
                            encrypted. Encryption converts the data on the compute
                            instance into unreadable code. Decryption of the the disk
                            requires other systems within the datacenter. This requirement
                            protects against data leakage if the disk is removed from
                            a datacenter, lost, stolen, recycled or disposed.
                          example: enabled
                          default: enabled
                          enum:
                          - enabled
                          - disabled
                        group:
                          type: string
                          description: |
                            A deprecated property denoting a group label for this Linode.
                          example: Linode-Group
                          deprecated: true
                          x-linode-filterable: true
                        has_user_data:
                          type: boolean
                          description: |
                            __Read-only__ Whether this compute instance was provisioned utilizing
                            `user_data` provided via the Metadata service. See the
                            [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
                            description for more information on Metadata.
                          example: true
                          readOnly: true
                        host_uuid:
                          type: string
                          description: |
                            __Read-only__ The Linode's host machine, as a UUID.
                          example: 3a3ddd59d9a78bb8de041391075df44de62bfec8
                          readOnly: true
                          format: uuid
                        hypervisor:
                          type: string
                          description: |
                            __Read-only__ The virtualization software powering this Linode.
                          example: kvm
                          readOnly: true
                          enum:
                          - kvm
                        image:
                          example: linode/debian10
                          nullable: true
                          readOnly: true
                          allOf:
                          - type: string
                            description: |-
                              An Image ID to deploy the Linode Disk from.

                              Run the [List images](https://techdocs.akamai.com/linode-api/reference/get-images) operation with authentication to view all available Images. Official Linode Images start with `linode/`, while your Account's Images start with `private/`. Creating a disk from a Private Image requires `read_only` or `read_write` permissions for that Image. Run the [Update a user's grants](https://techdocs.akamai.com/linode-api/reference/put-user-grants) operation to adjust permissions for an Account Image.
                            example: linode/debian9
                          x-linode-cli-display: 5
                          x-linode-filterable: true
                        ipv4:
                          type: array
                          description: |-
                            __Read-only__ This Linode's IPv4 Addresses. Each Linode is assigned a single public IPv4 address upon creation, and may get a single private IPv4 address if needed. You may need to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) to get additional IPv4 addresses.

                            IPv4 addresses may be reassigned between your Linodes, or shared with other Linodes. See the [networking](https://techdocs.akamai.com/linode-api/reference/post-firewalls) operations for details.
                          example:
                          - 203.0.113.1
                          - 192.0.2.1
                          readOnly: true
                          format: ipv4
                          items:
                            type: string
                          x-linode-cli-display: 7
                          x-linode-filterable: true
                        ipv6:
                          type: string
                          description: |
                            __Read-only__ This Linode's IPv6 SLAAC address. This address is specific
                            to a Linode, and may not be shared. If the Linode has
                            not been assigned an IPv6 address, the return value will
                            be `null`.
                          example: c001:d00d::1337/128
                          nullable: true
                          readOnly: true
                          format: ipv6/128
                        label:
                          type: string
                          description: |-
                            The Linode's label is for display purposes only. If no label is provided for a Linode, a default will be assigned.

                            Linode labels have the following constraints:

                            - Must begin and end with an alphanumeric character.
                            - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                            - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
                          example: linode123
                          minLength: 3
                          maxLength: 64
                          pattern: ^[a-zA-Z]((?!--|__|..)[a-zA-Z0-9-_.])+$
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        placement_group:
                          type: object
                          description: |
                            __Read-only__ Details on the [placement group](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/)
                            that this Linode belongs to. Empty if the Linode isn't
                            in a placement group.
                          additionalProperties: false
                          nullable: true
                          readOnly: true
                          properties:
                            id:
                              type: integer
                              description: |
                                The placement group's ID. You need to provide it for all operations
                                impacting it.
                              example: 528
                              nullable: false
                              x-linode-cli-display: 1
                            affinity_type:
                              type: string
                              description: |-
                                __Read-only__ How compute instances are distributed in your placement group. `anti-affinity:local` places compute instances in separate fault domains, but still in the same region.

                                __Note__: With the limited availability release, only `anti_affinity:local` is available for `affinity_type`.
                              example: anti-affinity:local
                              readOnly: true
                              x-linode-cli-display: 1
                              x-linode-filterable: true
                            is_strict:
                              type: boolean
                              description: |-
                                How the API enforces your `affinity_type`:

                                - Set to `true`, your group is strict. You can't add more compute instances to your placement group if your preferred container lacks capacity or is unavailable.
                                - Set to `false`, your group is flexible. You can add more compute instances to it even if they violate the `affinity_type`. If you violate the `affinity_type` your placement group becomes non-compliant and you need to wait for our assistance.
                              example: true
                              nullable: false
                            label:
                              type: string
                              description: |-
                                The unique name set for the placement group. A label has these constraints:

                                - It needs to begin and end with an alphanumeric character.
                                - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                              example: PG_Miami_failover
                              minLength: 1
                              x-linode-cli-display: 1
                              x-linode-filterable: true
                        region:
                          type: string
                          description: |
                            __Read-only__ This is the [region](https://techdocs.akamai.com/linode-api/reference/get-regions)
                            where the Linode was deployed. A Linode's region can only
                            be changed by initiating a [cross data center migration](https://techdocs.akamai.com/linode-api/reference/post-migrate-linode-instance).
                          example: us-east
                          readOnly: true
                          x-linode-cli-display: 3
                          x-linode-filterable: true
                        specs:
                          type: object
                          description: |
                            __Read-only__ Information about the resources available to this Linode.
                          additionalProperties: false
                          readOnly: true
                          properties:
                            disk:
                              type: integer
                              description: |
                                __Read-only__ The amount of storage space, in MB, this Linode has
                                access to. A typical Linode will divide this space
                                between a primary disk with an `image` deployed to
                                it, and a swap disk, usually 512 MB. This is the default
                                configuration created when deploying a Linode with
                                an `image` through [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance).
                                While this configuration is suitable for 99% of use
                                cases, if you need finer control over your Linode's
                                disks, see the [List disks](https://techdocs.akamai.com/linode-api/reference/get-linode-disks)
                                operations.
                              example: 81920
                              readOnly: true
                            gpus:
                              type: integer
                              description: |
                                __Read-only__ The number of gpus this Linode has access to.
                              example: 0
                              readOnly: true
                            memory:
                              type: integer
                              description: |-
                                __Read-only__ The amount of RAM, in MB, this Linode has access to.

                                Typically, a Linode boots with all of its available RAM, but this can be configured in a Config profile. See the [List config profiles](https://techdocs.akamai.com/linode-api/reference/get-linode-configs) operations and the LinodeConfig object for more information.
                              example: 4096
                              readOnly: true
                            transfer:
                              type: integer
                              description: |
                                __Read-only__ The amount of network transfer this Linode is allotted
                                each month.
                              example: 4000
                              readOnly: true
                            vcpus:
                              type: integer
                              description: |
                                __Read-only__ The number of vcpus this Linode has access to.
                              example: 2
                              readOnly: true
                        status:
                          type: string
                          description: |
                            __Read-only__ A brief description of this Linode's current state. This
                            field may change without direct action from you. For example,
                            when a Linode goes into maintenance mode its status will
                            display "stopped".
                          example: running
                          readOnly: true
                          enum:
                          - running
                          - offline
                          - booting
                          - rebooting
                          - shutting_down
                          - provisioning
                          - deleting
                          - migrating
                          - rebuilding
                          - cloning
                          - restoring
                          - stopped
                          - billing_suspension
                          x-linode-cli-color:
                            billing_suspension: red
                            default_: yellow
                            offline: red
                            running: green
                          x-linode-cli-display: 6
                        updated:
                          type: string
                          description: |
                            __Read-only__ When this Linode was last updated.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                        watchdog_enabled:
                          type: boolean
                          description: |
                            The watchdog, named Lassie, is a Shutdown Watchdog that monitors your
                            Linode and will reboot it if it powers off unexpectedly.
                            It works by issuing a boot job when your Linode powers
                            off without a shutdown job being responsible. To prevent
                            a loop, Lassie will give up if there have been more than
                            5 boot jobs issued within 15 minutes.
                          example: true
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-linode-instances
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/linode/instances
      - lang: CLI
        source: linode-cli linodes list
      x-linode-cli-action:
      - list
      - ls
      x-linode-grant: read_only
      x-original-op-id: getLinodeInstances
      x-original-op-title: Linodes List
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}:
    x-akamai:
      file-path: paths/linode.yaml
      path-info: /linode/instances/{linodeId}
    parameters:
    - name: linodeId
      in: path
      description: |
        ID of the Linode to look up
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path-32b5e821.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-linode-instance
      summary: Get a Linode
      tags:
      - Linode instances
      description: |-
        Get a specific Linode by ID.


        ---


        - __CLI__.

            ```
            linode-cli linodes view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a single Linode object.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/linode.yaml
                title: Linode
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Linode's ID which must be provided for all operations impacting
                      this Linode.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  tags:
                    type: array
                    description: |
                      An array of tags applied to this object.  Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  type:
                    type: string
                    description: |
                      __Read-only__ This is the [Linode type](https://techdocs.akamai.com/linode-api/reference/get-linode-types)
                      that this Linode was deployed with. To change a Linode's Type,
                      use [Resize a Linode](https://techdocs.akamai.com/linode-api/reference/post-resize-linode-instance).
                    example: g6-standard-1
                    readOnly: true
                    x-linode-cli-display: 4
                  alerts:
                    type: object
                    additionalProperties: false
                    properties:
                      cpu:
                        type: integer
                        description: |-
                          The percentage of CPU usage required to trigger an alert. If the average CPU usage over two hours exceeds this value, we'll send you an alert. Your Linode's total CPU capacity is represented as 100%, multiplied by its number of cores.

                          For example, a two core Linode's CPU capacity is represented as 200%. If you want to be alerted at 90% of a two core Linode's CPU capacity, set the alert value to `180`.

                          The default value is 90% multiplied by the number of cores.

                          If the value is set to `0` (zero), the alert is disabled.
                        example: 180
                      io:
                        type: integer
                        description: |
                          The amount of disk IO operation per second required to trigger an alert. If
                          the average disk IO over two hours exceeds this value, we'll
                          send you an alert. If set to `0` (zero), this alert is disabled.
                        example: 10000
                      network_in:
                        type: integer
                        description: |
                          The amount of incoming traffic, in Mbit/s, required to trigger an alert. If the
                          average incoming traffic over two hours exceeds this value,
                          we'll send you an alert. If this is set to `0` (zero), the
                          alert is disabled.
                        example: 10
                      network_out:
                        type: integer
                        description: |
                          The amount of outbound traffic, in Mbit/s, required to trigger an alert. If the
                          average outbound traffic over two hours exceeds this value,
                          we'll send you an alert. If this is set to `0` (zero), the
                          alert is disabled.
                        example: 10
                      transfer_quota:
                        type: integer
                        description: |
                          The percentage of network transfer that may be used before an alert is
                          triggered. When this value is exceeded, we'll alert you.
                          If this is set to `0` (zero), the alert is disabled.
                        example: 80
                  backups:
                    type: object
                    description: |
                      Information about this Linode's backups status. For information about available backups,
                      run [List backups](https://techdocs.akamai.com/linode-api/reference/get-backups).
                    additionalProperties: false
                    properties:
                      available:
                        type: boolean
                        description: |-
                          __Read-only__ Whether Backups for this Linode are available for restoration.

                          Backups undergoing maintenance are not available for restoration.
                        example: true
                        readOnly: true
                      enabled:
                        type: boolean
                        description: |
                          __Read-only__ If this Linode has the Backup service enabled. To enable backups,
                          run [Enable backups](https://techdocs.akamai.com/linode-api/reference/post-enable-backups).
                        example: true
                        readOnly: true
                      last_successful:
                        type: string
                        description: |
                          __Read-only__ The last successful backup date. 'null' if there was no previous
                          backup.
                        example: '2018-01-01T00:01:01'
                        readOnly: true
                        format: date-time
                      schedule:
                        type: object
                        additionalProperties: false
                        properties:
                          day:
                            type: string
                            description: |-
                              The day of the week that your Linode's weekly Backup is taken. If not set manually, a day will be chosen for you. Backups are taken every day, but backups taken on this day are preferred when selecting backups to retain for a longer period.

                              If not set manually, then when backups are initially enabled, this may come back as `Scheduling` until the `day` is automatically selected.
                            example: Saturday
                            nullable: true
                            enum:
                            - Scheduling
                            - Sunday
                            - Monday
                            - Tuesday
                            - Wednesday
                            - Thursday
                            - Friday
                            - Saturday
                          window:
                            type: string
                            description: |-
                              The window in which your backups will be taken, in UTC. A backups window is a two-hour span of time in which the backup may occur.

                              For example, `W10` indicates that your backups should be taken between 10:00 and 12:00. If you do not choose a backup window, one will be selected for you automatically.

                              If not set manually, when backups are initially enabled this may come back as `Scheduling` until the `window` is automatically selected.
                            example: W22
                            nullable: true
                            enum:
                            - Scheduling
                            - W0
                            - W2
                            - W4
                            - W6
                            - W8
                            - W10
                            - W12
                            - W14
                            - W16
                            - W18
                            - W20
                            - W22
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Linode was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  disk_encryption:
                    type: string
                    description: |
                      Local disk encryption ensures that your data stored on compute instances is encrypted.
                      Encryption converts the data on the compute instance into unreadable
                      code. Decryption of the the disk requires other systems within
                      the datacenter. This requirement protects against data leakage
                      if the disk is removed from a datacenter, lost, stolen, recycled
                      or disposed.
                    example: enabled
                    default: enabled
                    enum:
                    - enabled
                    - disabled
                  group:
                    type: string
                    description: |
                      A deprecated property denoting a group label for this Linode.
                    example: Linode-Group
                    deprecated: true
                    x-linode-filterable: true
                  has_user_data:
                    type: boolean
                    description: |
                      __Read-only__ Whether this compute instance was provisioned utilizing `user_data`
                      provided via the Metadata service. See the [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
                      description for more information on Metadata.
                    example: true
                    readOnly: true
                  host_uuid:
                    type: string
                    description: |
                      __Read-only__ The Linode's host machine, as a UUID.
                    example: 3a3ddd59d9a78bb8de041391075df44de62bfec8
                    readOnly: true
                    format: uuid
                  hypervisor:
                    type: string
                    description: |
                      __Read-only__ The virtualization software powering this Linode.
                    example: kvm
                    readOnly: true
                    enum:
                    - kvm
                  image:
                    example: linode/debian10
                    nullable: true
                    readOnly: true
                    allOf:
                    - type: string
                      description: |-
                        An Image ID to deploy the Linode Disk from.

                        Run the [List images](https://techdocs.akamai.com/linode-api/reference/get-images) operation with authentication to view all available Images. Official Linode Images start with `linode/`, while your Account's Images start with `private/`. Creating a disk from a Private Image requires `read_only` or `read_write` permissions for that Image. Run the [Update a user's grants](https://techdocs.akamai.com/linode-api/reference/put-user-grants) operation to adjust permissions for an Account Image.
                      example: linode/debian9
                    x-linode-cli-display: 5
                    x-linode-filterable: true
                  ipv4:
                    type: array
                    description: |-
                      __Read-only__ This Linode's IPv4 Addresses. Each Linode is assigned a single public IPv4 address upon creation, and may get a single private IPv4 address if needed. You may need to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) to get additional IPv4 addresses.

                      IPv4 addresses may be reassigned between your Linodes, or shared with other Linodes. See the [networking](https://techdocs.akamai.com/linode-api/reference/post-firewalls) operations for details.
                    example:
                    - 203.0.113.1
                    - 192.0.2.1
                    readOnly: true
                    format: ipv4
                    items:
                      type: string
                    x-linode-cli-display: 7
                    x-linode-filterable: true
                  ipv6:
                    type: string
                    description: |
                      __Read-only__ This Linode's IPv6 SLAAC address. This address is specific to a Linode,
                      and may not be shared. If the Linode has not been assigned an
                      IPv6 address, the return value will be `null`.
                    example: c001:d00d::1337/128
                    nullable: true
                    readOnly: true
                    format: ipv6/128
                  label:
                    type: string
                    description: |-
                      The Linode's label is for display purposes only. If no label is provided for a Linode, a default will be assigned.

                      Linode labels have the following constraints:

                      - Must begin and end with an alphanumeric character.
                      - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                      - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
                    example: linode123
                    minLength: 3
                    maxLength: 64
                    pattern: ^[a-zA-Z]((?!--|__|..)[a-zA-Z0-9-_.])+$
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  placement_group:
                    type: object
                    description: |
                      __Read-only__ Details on the [placement group](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/)
                      that this Linode belongs to. Empty if the Linode isn't in a
                      placement group.
                    additionalProperties: false
                    nullable: true
                    readOnly: true
                    properties:
                      id:
                        type: integer
                        description: |
                          The placement group's ID. You need to provide it for all operations impacting
                          it.
                        example: 528
                        nullable: false
                        x-linode-cli-display: 1
                      affinity_type:
                        type: string
                        description: |-
                          __Read-only__ How compute instances are distributed in your placement group. `anti-affinity:local` places compute instances in separate fault domains, but still in the same region.

                          __Note__: With the limited availability release, only `anti_affinity:local` is available for `affinity_type`.
                        example: anti-affinity:local
                        readOnly: true
                        x-linode-cli-display: 1
                        x-linode-filterable: true
                      is_strict:
                        type: boolean
                        description: |-
                          How the API enforces your `affinity_type`:

                          - Set to `true`, your group is strict. You can't add more compute instances to your placement group if your preferred container lacks capacity or is unavailable.
                          - Set to `false`, your group is flexible. You can add more compute instances to it even if they violate the `affinity_type`. If you violate the `affinity_type` your placement group becomes non-compliant and you need to wait for our assistance.
                        example: true
                        nullable: false
                      label:
                        type: string
                        description: |-
                          The unique name set for the placement group. A label has these constraints:

                          - It needs to begin and end with an alphanumeric character.
                          - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                        example: PG_Miami_failover
                        minLength: 1
                        x-linode-cli-display: 1
                        x-linode-filterable: true
                  region:
                    type: string
                    description: |
                      __Read-only__ This is the [region](https://techdocs.akamai.com/linode-api/reference/get-regions)
                      where the Linode was deployed. A Linode's region can only be
                      changed by initiating a [cross data center migration](https://techdocs.akamai.com/linode-api/reference/post-migrate-linode-instance).
                    example: us-east
                    readOnly: true
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  specs:
                    type: object
                    description: |
                      __Read-only__ Information about the resources available to this Linode.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      disk:
                        type: integer
                        description: |
                          __Read-only__ The amount of storage space, in MB, this Linode has access to.
                          A typical Linode will divide this space between a primary
                          disk with an `image` deployed to it, and a swap disk, usually
                          512 MB. This is the default configuration created when deploying
                          a Linode with an `image` through [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance).
                          While this configuration is suitable for 99% of use cases,
                          if you need finer control over your Linode's disks, see
                          the [List disks](https://techdocs.akamai.com/linode-api/reference/get-linode-disks)
                          operations.
                        example: 81920
                        readOnly: true
                      gpus:
                        type: integer
                        description: |
                          __Read-only__ The number of gpus this Linode has access to.
                        example: 0
                        readOnly: true
                      memory:
                        type: integer
                        description: |-
                          __Read-only__ The amount of RAM, in MB, this Linode has access to.

                          Typically, a Linode boots with all of its available RAM, but this can be configured in a Config profile. See the [List config profiles](https://techdocs.akamai.com/linode-api/reference/get-linode-configs) operations and the LinodeConfig object for more information.
                        example: 4096
                        readOnly: true
                      transfer:
                        type: integer
                        description: |
                          __Read-only__ The amount of network transfer this Linode is allotted each
                          month.
                        example: 4000
                        readOnly: true
                      vcpus:
                        type: integer
                        description: |
                          __Read-only__ The number of vcpus this Linode has access to.
                        example: 2
                        readOnly: true
                  status:
                    type: string
                    description: |
                      __Read-only__ A brief description of this Linode's current state. This field may change
                      without direct action from you. For example, when a Linode goes
                      into maintenance mode its status will display "stopped".
                    example: running
                    readOnly: true
                    enum:
                    - running
                    - offline
                    - booting
                    - rebooting
                    - shutting_down
                    - provisioning
                    - deleting
                    - migrating
                    - rebuilding
                    - cloning
                    - restoring
                    - stopped
                    - billing_suspension
                    x-linode-cli-color:
                      billing_suspension: red
                      default_: yellow
                      offline: red
                      running: green
                    x-linode-cli-display: 6
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Linode was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  watchdog_enabled:
                    type: boolean
                    description: |
                      The watchdog, named Lassie, is a Shutdown Watchdog that monitors your Linode and
                      will reboot it if it powers off unexpectedly. It works by issuing
                      a boot job when your Linode powers off without a shutdown job
                      being responsible. To prevent a loop, Lassie will give up if
                      there have been more than 5 boot jobs issued within 15 minutes.
                    example: true
              x-example:
                x-ref: ../examples/tbd.json
          links:
            delete:
              operationId: delete-linode-instance
              parameters:
                linodeId: $request.body#/id
            boot:
              operationId: post-boot-linode-instance
              parameters:
                linodeId: $request.body#/id
            clone:
              operationId: post-clone-linode-instance
              parameters:
                linodeId: $request.body#/id
            mutate:
              operationId: post-mutate-linode-instance
              parameters:
                linodeId: $request.body#/id
            reboot:
              operationId: post-reboot-linode-instance
              parameters:
                linodeId: $request.body#/id
            rebuild:
              operationId: post-rebuild-linode-instance
              parameters:
                linodeId: $request.body#/id
            rescue:
              operationId: post-rescue-linode-instance
              parameters:
                linodeId: $request.body#/id
            resize:
              operationId: post-resize-linode-instance
              parameters:
                linodeId: $request.body#/id
            shutdown:
              operationId: post-shutdown-linode-instance
              parameters:
                linodeId: $request.body#/id
            update:
              operationId: put-linode-instance
              parameters:
                linodeId: $request.body#/id
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-linode-instance
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/linode/instances/123
      - lang: CLI
        source: linode-cli linodes view 123
      x-linode-cli-action: view
      x-linode-grant: read_only
      x-original-op-id: getLinodeInstance
      x-original-op-title: Linode View
    put:
      operationId: put-linode-instance
      summary: Update a Linode
      tags:
      - Linode instances
      description: |-
        Updates a Linode that you have permission to `read_write`.

        __Important__: You must be an unrestricted User in order to add or modify tags on Linodes.


        ---


        - __CLI__.

            ```
            linode-cli linodes update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Any field that is not marked as `readOnly` may be updated. Fields that are marked `readOnly` will be ignored.
          If any updated field fails to pass validation, the Linode will not be updated.
        required: true
        content:
          application/json:
            schema:
              type: object
              x-akamai:
                file-path: schemas/linode.yaml
              title: Linode
              additionalProperties: false
              properties:
                id:
                  type: integer
                  description: |
                    __Read-only__ This Linode's ID which must be provided for all operations impacting this Linode.
                  example: '{{id}}'
                  readOnly: true
                  x-linode-cli-display: 1
                  x-linode-filterable: true
                tags:
                  type: array
                  description: |
                    An array of tags applied to this object.  Tags are for organizational purposes only.
                  example:
                  - example tag
                  - another example
                  items:
                    type: string
                  x-linode-filterable: true
                type:
                  type: string
                  description: |
                    __Read-only__ This is the [Linode type](https://techdocs.akamai.com/linode-api/reference/get-linode-types)
                    that this Linode was deployed with. To change a Linode's Type,
                    use [Resize a Linode](https://techdocs.akamai.com/linode-api/reference/post-resize-linode-instance).
                  example: '{{type}}'
                  readOnly: true
                  x-linode-cli-display: 4
                alerts:
                  type: object
                  additionalProperties: false
                  properties:
                    cpu:
                      type: integer
                      description: |-
                        The percentage of CPU usage required to trigger an alert. If the average CPU usage over two hours exceeds this value, we'll send you an alert. Your Linode's total CPU capacity is represented as 100%, multiplied by its number of cores.

                        For example, a two core Linode's CPU capacity is represented as 200%. If you want to be alerted at 90% of a two core Linode's CPU capacity, set the alert value to `180`.

                        The default value is 90% multiplied by the number of cores.

                        If the value is set to `0` (zero), the alert is disabled.
                      example: 180
                    io:
                      type: integer
                      description: |
                        The amount of disk IO operation per second required to trigger an alert. If the
                        average disk IO over two hours exceeds this value, we'll send
                        you an alert. If set to `0` (zero), this alert is disabled.
                      example: 10000
                    network_in:
                      type: integer
                      description: |
                        The amount of incoming traffic, in Mbit/s, required to trigger an alert. If the average
                        incoming traffic over two hours exceeds this value, we'll
                        send you an alert. If this is set to `0` (zero), the alert
                        is disabled.
                      example: 10
                    network_out:
                      type: integer
                      description: |
                        The amount of outbound traffic, in Mbit/s, required to trigger an alert. If the average
                        outbound traffic over two hours exceeds this value, we'll
                        send you an alert. If this is set to `0` (zero), the alert
                        is disabled.
                      example: 10
                    transfer_quota:
                      type: integer
                      description: |
                        The percentage of network transfer that may be used before an alert is triggered.
                        When this value is exceeded, we'll alert you. If this is set
                        to `0` (zero), the alert is disabled.
                      example: 80
                backups:
                  type: object
                  description: |
                    Information about this Linode's backups status. For information about available backups,
                    run [List backups](https://techdocs.akamai.com/linode-api/reference/get-backups).
                  additionalProperties: false
                  properties:
                    available:
                      type: boolean
                      description: |-
                        __Read-only__ Whether Backups for this Linode are available for restoration.

                        Backups undergoing maintenance are not available for restoration.
                      example: true
                      readOnly: true
                    enabled:
                      type: boolean
                      description: |
                        __Read-only__ If this Linode has the Backup service enabled. To enable backups,
                        run [Enable backups](https://techdocs.akamai.com/linode-api/reference/post-enable-backups).
                      example: true
                      readOnly: true
                    last_successful:
                      type: string
                      description: |
                        __Read-only__ The last successful backup date. 'null' if there was no previous
                        backup.
                      example: '2018-01-01T00:01:01'
                      readOnly: true
                      format: date-time
                    schedule:
                      type: object
                      additionalProperties: false
                      properties:
                        day:
                          type: string
                          description: |-
                            The day of the week that your Linode's weekly Backup is taken. If not set manually, a day will be chosen for you. Backups are taken every day, but backups taken on this day are preferred when selecting backups to retain for a longer period.

                            If not set manually, then when backups are initially enabled, this may come back as `Scheduling` until the `day` is automatically selected.
                          example: Saturday
                          nullable: true
                          enum:
                          - Scheduling
                          - Sunday
                          - Monday
                          - Tuesday
                          - Wednesday
                          - Thursday
                          - Friday
                          - Saturday
                        window:
                          type: string
                          description: |-
                            The window in which your backups will be taken, in UTC. A backups window is a two-hour span of time in which the backup may occur.

                            For example, `W10` indicates that your backups should be taken between 10:00 and 12:00. If you do not choose a backup window, one will be selected for you automatically.

                            If not set manually, when backups are initially enabled this may come back as `Scheduling` until the `window` is automatically selected.
                          example: W22
                          nullable: true
                          enum:
                          - Scheduling
                          - W0
                          - W2
                          - W4
                          - W6
                          - W8
                          - W10
                          - W12
                          - W14
                          - W16
                          - W18
                          - W20
                          - W22
                created:
                  type: string
                  description: |
                    __Read-only__ When this Linode was created.
                  example: '{{created}}'
                  readOnly: true
                  format: date-time
                disk_encryption:
                  type: string
                  description: |
                    Local disk encryption ensures that your data stored on compute instances is encrypted.
                    Encryption converts the data on the compute instance into unreadable
                    code. Decryption of the the disk requires other systems within
                    the datacenter. This requirement protects against data leakage
                    if the disk is removed from a datacenter, lost, stolen, recycled
                    or disposed.
                  example: '{{disk_encryption}}'
                  default: enabled
                  enum:
                  - enabled
                  - disabled
                group:
                  type: string
                  description: |
                    A deprecated property denoting a group label for this Linode.
                  example: '{{group}}'
                  deprecated: true
                  x-linode-filterable: true
                has_user_data:
                  type: boolean
                  description: |
                    __Read-only__ Whether this compute instance was provisioned utilizing `user_data` provided
                    via the Metadata service. See the [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
                    description for more information on Metadata.
                  example: '{{has_user_data}}'
                  readOnly: true
                host_uuid:
                  type: string
                  description: |
                    __Read-only__ The Linode's host machine, as a UUID.
                  example: '{{host_uuid}}'
                  readOnly: true
                  format: uuid
                hypervisor:
                  type: string
                  description: |
                    __Read-only__ The virtualization software powering this Linode.
                  example: '{{hypervisor}}'
                  readOnly: true
                  enum:
                  - kvm
                image:
                  example: '{{image}}'
                  nullable: true
                  readOnly: true
                  allOf:
                  - type: string
                    description: |-
                      An Image ID to deploy the Linode Disk from.

                      Run the [List images](https://techdocs.akamai.com/linode-api/reference/get-images) operation with authentication to view all available Images. Official Linode Images start with `linode/`, while your Account's Images start with `private/`. Creating a disk from a Private Image requires `read_only` or `read_write` permissions for that Image. Run the [Update a user's grants](https://techdocs.akamai.com/linode-api/reference/put-user-grants) operation to adjust permissions for an Account Image.
                    example: linode/debian9
                  x-linode-cli-display: 5
                  x-linode-filterable: true
                ipv4:
                  type: array
                  description: |-
                    __Read-only__ This Linode's IPv4 Addresses. Each Linode is assigned a single public IPv4 address upon creation, and may get a single private IPv4 address if needed. You may need to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) to get additional IPv4 addresses.

                    IPv4 addresses may be reassigned between your Linodes, or shared with other Linodes. See the [networking](https://techdocs.akamai.com/linode-api/reference/post-firewalls) operations for details.
                  example:
                  - 203.0.113.1
                  - 192.0.2.1
                  readOnly: true
                  format: ipv4
                  items:
                    type: string
                  x-linode-cli-display: 7
                  x-linode-filterable: true
                ipv6:
                  type: string
                  description: |
                    __Read-only__ This Linode's IPv6 SLAAC address. This address is specific to a Linode, and
                    may not be shared. If the Linode has not been assigned an IPv6
                    address, the return value will be `null`.
                  example: '{{ipv6}}'
                  nullable: true
                  readOnly: true
                  format: ipv6/128
                label:
                  type: string
                  description: |-
                    The Linode's label is for display purposes only. If no label is provided for a Linode, a default will be assigned.

                    Linode labels have the following constraints:

                    - Must begin and end with an alphanumeric character.
                    - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                    - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
                  example: '{{label}}'
                  minLength: 3
                  maxLength: 64
                  pattern: ^[a-zA-Z]((?!--|__|..)[a-zA-Z0-9-_.])+$
                  x-linode-cli-display: 2
                  x-linode-filterable: true
                placement_group:
                  type: object
                  description: |
                    __Read-only__ Details on the [placement group](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/)
                    that this Linode belongs to. Empty if the Linode isn't in a placement
                    group.
                  additionalProperties: false
                  nullable: true
                  readOnly: true
                  properties:
                    id:
                      type: integer
                      description: |
                        The placement group's ID. You need to provide it for all operations impacting it.
                      example: 528
                      nullable: false
                      x-linode-cli-display: 1
                    affinity_type:
                      type: string
                      description: |-
                        __Read-only__ How compute instances are distributed in your placement group. `anti-affinity:local` places compute instances in separate fault domains, but still in the same region.

                        __Note__: With the limited availability release, only `anti_affinity:local` is available for `affinity_type`.
                      example: anti-affinity:local
                      readOnly: true
                      x-linode-cli-display: 1
                      x-linode-filterable: true
                    is_strict:
                      type: boolean
                      description: |-
                        How the API enforces your `affinity_type`:

                        - Set to `true`, your group is strict. You can't add more compute instances to your placement group if your preferred container lacks capacity or is unavailable.
                        - Set to `false`, your group is flexible. You can add more compute instances to it even if they violate the `affinity_type`. If you violate the `affinity_type` your placement group becomes non-compliant and you need to wait for our assistance.
                      example: true
                      nullable: false
                    label:
                      type: string
                      description: |-
                        The unique name set for the placement group. A label has these constraints:

                        - It needs to begin and end with an alphanumeric character.
                        - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                      example: PG_Miami_failover
                      minLength: 1
                      x-linode-cli-display: 1
                      x-linode-filterable: true
                region:
                  type: string
                  description: |
                    __Read-only__ This is the [region](https://techdocs.akamai.com/linode-api/reference/get-regions)
                    where the Linode was deployed. A Linode's region can only be changed
                    by initiating a [cross data center migration](https://techdocs.akamai.com/linode-api/reference/post-migrate-linode-instance).
                  example: '{{region}}'
                  readOnly: true
                  x-linode-cli-display: 3
                  x-linode-filterable: true
                specs:
                  type: object
                  description: |
                    __Read-only__ Information about the resources available to this Linode.
                  additionalProperties: false
                  readOnly: true
                  properties:
                    disk:
                      type: integer
                      description: |
                        __Read-only__ The amount of storage space, in MB, this Linode has access to.
                        A typical Linode will divide this space between a primary
                        disk with an `image` deployed to it, and a swap disk, usually
                        512 MB. This is the default configuration created when deploying
                        a Linode with an `image` through [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance).
                        While this configuration is suitable for 99% of use cases,
                        if you need finer control over your Linode's disks, see the
                        [List disks](https://techdocs.akamai.com/linode-api/reference/get-linode-disks)
                        operations.
                      example: 81920
                      readOnly: true
                    gpus:
                      type: integer
                      description: |
                        __Read-only__ The number of gpus this Linode has access to.
                      example: 0
                      readOnly: true
                    memory:
                      type: integer
                      description: |-
                        __Read-only__ The amount of RAM, in MB, this Linode has access to.

                        Typically, a Linode boots with all of its available RAM, but this can be configured in a Config profile. See the [List config profiles](https://techdocs.akamai.com/linode-api/reference/get-linode-configs) operations and the LinodeConfig object for more information.
                      example: 4096
                      readOnly: true
                    transfer:
                      type: integer
                      description: |
                        __Read-only__ The amount of network transfer this Linode is allotted each month.
                      example: 4000
                      readOnly: true
                    vcpus:
                      type: integer
                      description: |
                        __Read-only__ The number of vcpus this Linode has access to.
                      example: 2
                      readOnly: true
                status:
                  type: string
                  description: |
                    __Read-only__ A brief description of this Linode's current state. This field may change
                    without direct action from you. For example, when a Linode goes
                    into maintenance mode its status will display "stopped".
                  example: '{{status}}'
                  readOnly: true
                  enum:
                  - running
                  - offline
                  - booting
                  - rebooting
                  - shutting_down
                  - provisioning
                  - deleting
                  - migrating
                  - rebuilding
                  - cloning
                  - restoring
                  - stopped
                  - billing_suspension
                  x-linode-cli-color:
                    billing_suspension: red
                    default_: yellow
                    offline: red
                    running: green
                  x-linode-cli-display: 6
                updated:
                  type: string
                  description: |
                    __Read-only__ When this Linode was last updated.
                  example: '{{updated}}'
                  readOnly: true
                  format: date-time
                watchdog_enabled:
                  type: boolean
                  description: |
                    The watchdog, named Lassie, is a Shutdown Watchdog that monitors your Linode and will reboot
                    it if it powers off unexpectedly. It works by issuing a boot job
                    when your Linode powers off without a shutdown job being responsible.
                    To prevent a loop, Lassie will give up if there have been more
                    than 5 boot jobs issued within 15 minutes.
                  example: '{{watchdog_enabled}}'
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The updated Linode.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/linode.yaml
                title: Linode
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Linode's ID which must be provided for all operations impacting
                      this Linode.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  tags:
                    type: array
                    description: |
                      An array of tags applied to this object.  Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  type:
                    type: string
                    description: |
                      __Read-only__ This is the [Linode type](https://techdocs.akamai.com/linode-api/reference/get-linode-types)
                      that this Linode was deployed with. To change a Linode's Type,
                      use [Resize a Linode](https://techdocs.akamai.com/linode-api/reference/post-resize-linode-instance).
                    example: g6-standard-1
                    readOnly: true
                    x-linode-cli-display: 4
                  alerts:
                    type: object
                    additionalProperties: false
                    properties:
                      cpu:
                        type: integer
                        description: |-
                          The percentage of CPU usage required to trigger an alert. If the average CPU usage over two hours exceeds this value, we'll send you an alert. Your Linode's total CPU capacity is represented as 100%, multiplied by its number of cores.

                          For example, a two core Linode's CPU capacity is represented as 200%. If you want to be alerted at 90% of a two core Linode's CPU capacity, set the alert value to `180`.

                          The default value is 90% multiplied by the number of cores.

                          If the value is set to `0` (zero), the alert is disabled.
                        example: 180
                      io:
                        type: integer
                        description: |
                          The amount of disk IO operation per second required to trigger an alert. If
                          the average disk IO over two hours exceeds this value, we'll
                          send you an alert. If set to `0` (zero), this alert is disabled.
                        example: 10000
                      network_in:
                        type: integer
                        description: |
                          The amount of incoming traffic, in Mbit/s, required to trigger an alert. If the
                          average incoming traffic over two hours exceeds this value,
                          we'll send you an alert. If this is set to `0` (zero), the
                          alert is disabled.
                        example: 10
                      network_out:
                        type: integer
                        description: |
                          The amount of outbound traffic, in Mbit/s, required to trigger an alert. If the
                          average outbound traffic over two hours exceeds this value,
                          we'll send you an alert. If this is set to `0` (zero), the
                          alert is disabled.
                        example: 10
                      transfer_quota:
                        type: integer
                        description: |
                          The percentage of network transfer that may be used before an alert is
                          triggered. When this value is exceeded, we'll alert you.
                          If this is set to `0` (zero), the alert is disabled.
                        example: 80
                  backups:
                    type: object
                    description: |
                      Information about this Linode's backups status. For information about available backups,
                      run [List backups](https://techdocs.akamai.com/linode-api/reference/get-backups).
                    additionalProperties: false
                    properties:
                      available:
                        type: boolean
                        description: |-
                          __Read-only__ Whether Backups for this Linode are available for restoration.

                          Backups undergoing maintenance are not available for restoration.
                        example: true
                        readOnly: true
                      enabled:
                        type: boolean
                        description: |
                          __Read-only__ If this Linode has the Backup service enabled. To enable backups,
                          run [Enable backups](https://techdocs.akamai.com/linode-api/reference/post-enable-backups).
                        example: true
                        readOnly: true
                      last_successful:
                        type: string
                        description: |
                          __Read-only__ The last successful backup date. 'null' if there was no previous
                          backup.
                        example: '2018-01-01T00:01:01'
                        readOnly: true
                        format: date-time
                      schedule:
                        type: object
                        additionalProperties: false
                        properties:
                          day:
                            type: string
                            description: |-
                              The day of the week that your Linode's weekly Backup is taken. If not set manually, a day will be chosen for you. Backups are taken every day, but backups taken on this day are preferred when selecting backups to retain for a longer period.

                              If not set manually, then when backups are initially enabled, this may come back as `Scheduling` until the `day` is automatically selected.
                            example: Saturday
                            nullable: true
                            enum:
                            - Scheduling
                            - Sunday
                            - Monday
                            - Tuesday
                            - Wednesday
                            - Thursday
                            - Friday
                            - Saturday
                          window:
                            type: string
                            description: |-
                              The window in which your backups will be taken, in UTC. A backups window is a two-hour span of time in which the backup may occur.

                              For example, `W10` indicates that your backups should be taken between 10:00 and 12:00. If you do not choose a backup window, one will be selected for you automatically.

                              If not set manually, when backups are initially enabled this may come back as `Scheduling` until the `window` is automatically selected.
                            example: W22
                            nullable: true
                            enum:
                            - Scheduling
                            - W0
                            - W2
                            - W4
                            - W6
                            - W8
                            - W10
                            - W12
                            - W14
                            - W16
                            - W18
                            - W20
                            - W22
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Linode was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  disk_encryption:
                    type: string
                    description: |
                      Local disk encryption ensures that your data stored on compute instances is encrypted.
                      Encryption converts the data on the compute instance into unreadable
                      code. Decryption of the the disk requires other systems within
                      the datacenter. This requirement protects against data leakage
                      if the disk is removed from a datacenter, lost, stolen, recycled
                      or disposed.
                    example: enabled
                    default: enabled
                    enum:
                    - enabled
                    - disabled
                  group:
                    type: string
                    description: |
                      A deprecated property denoting a group label for this Linode.
                    example: Linode-Group
                    deprecated: true
                    x-linode-filterable: true
                  has_user_data:
                    type: boolean
                    description: |
                      __Read-only__ Whether this compute instance was provisioned utilizing `user_data`
                      provided via the Metadata service. See the [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
                      description for more information on Metadata.
                    example: true
                    readOnly: true
                  host_uuid:
                    type: string
                    description: |
                      __Read-only__ The Linode's host machine, as a UUID.
                    example: 3a3ddd59d9a78bb8de041391075df44de62bfec8
                    readOnly: true
                    format: uuid
                  hypervisor:
                    type: string
                    description: |
                      __Read-only__ The virtualization software powering this Linode.
                    example: kvm
                    readOnly: true
                    enum:
                    - kvm
                  image:
                    example: linode/debian10
                    nullable: true
                    readOnly: true
                    allOf:
                    - type: string
                      description: |-
                        An Image ID to deploy the Linode Disk from.

                        Run the [List images](https://techdocs.akamai.com/linode-api/reference/get-images) operation with authentication to view all available Images. Official Linode Images start with `linode/`, while your Account's Images start with `private/`. Creating a disk from a Private Image requires `read_only` or `read_write` permissions for that Image. Run the [Update a user's grants](https://techdocs.akamai.com/linode-api/reference/put-user-grants) operation to adjust permissions for an Account Image.
                      example: linode/debian9
                    x-linode-cli-display: 5
                    x-linode-filterable: true
                  ipv4:
                    type: array
                    description: |-
                      __Read-only__ This Linode's IPv4 Addresses. Each Linode is assigned a single public IPv4 address upon creation, and may get a single private IPv4 address if needed. You may need to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) to get additional IPv4 addresses.

                      IPv4 addresses may be reassigned between your Linodes, or shared with other Linodes. See the [networking](https://techdocs.akamai.com/linode-api/reference/post-firewalls) operations for details.
                    example:
                    - 203.0.113.1
                    - 192.0.2.1
                    readOnly: true
                    format: ipv4
                    items:
                      type: string
                    x-linode-cli-display: 7
                    x-linode-filterable: true
                  ipv6:
                    type: string
                    description: |
                      __Read-only__ This Linode's IPv6 SLAAC address. This address is specific to a Linode,
                      and may not be shared. If the Linode has not been assigned an
                      IPv6 address, the return value will be `null`.
                    example: c001:d00d::1337/128
                    nullable: true
                    readOnly: true
                    format: ipv6/128
                  label:
                    type: string
                    description: |-
                      The Linode's label is for display purposes only. If no label is provided for a Linode, a default will be assigned.

                      Linode labels have the following constraints:

                      - Must begin and end with an alphanumeric character.
                      - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                      - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
                    example: linode123
                    minLength: 3
                    maxLength: 64
                    pattern: ^[a-zA-Z]((?!--|__|..)[a-zA-Z0-9-_.])+$
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  placement_group:
                    type: object
                    description: |
                      __Read-only__ Details on the [placement group](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/)
                      that this Linode belongs to. Empty if the Linode isn't in a
                      placement group.
                    additionalProperties: false
                    nullable: true
                    readOnly: true
                    properties:
                      id:
                        type: integer
                        description: |
                          The placement group's ID. You need to provide it for all operations impacting
                          it.
                        example: 528
                        nullable: false
                        x-linode-cli-display: 1
                      affinity_type:
                        type: string
                        description: |-
                          __Read-only__ How compute instances are distributed in your placement group. `anti-affinity:local` places compute instances in separate fault domains, but still in the same region.

                          __Note__: With the limited availability release, only `anti_affinity:local` is available for `affinity_type`.
                        example: anti-affinity:local
                        readOnly: true
                        x-linode-cli-display: 1
                        x-linode-filterable: true
                      is_strict:
                        type: boolean
                        description: |-
                          How the API enforces your `affinity_type`:

                          - Set to `true`, your group is strict. You can't add more compute instances to your placement group if your preferred container lacks capacity or is unavailable.
                          - Set to `false`, your group is flexible. You can add more compute instances to it even if they violate the `affinity_type`. If you violate the `affinity_type` your placement group becomes non-compliant and you need to wait for our assistance.
                        example: true
                        nullable: false
                      label:
                        type: string
                        description: |-
                          The unique name set for the placement group. A label has these constraints:

                          - It needs to begin and end with an alphanumeric character.
                          - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                        example: PG_Miami_failover
                        minLength: 1
                        x-linode-cli-display: 1
                        x-linode-filterable: true
                  region:
                    type: string
                    description: |
                      __Read-only__ This is the [region](https://techdocs.akamai.com/linode-api/reference/get-regions)
                      where the Linode was deployed. A Linode's region can only be
                      changed by initiating a [cross data center migration](https://techdocs.akamai.com/linode-api/reference/post-migrate-linode-instance).
                    example: us-east
                    readOnly: true
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  specs:
                    type: object
                    description: |
                      __Read-only__ Information about the resources available to this Linode.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      disk:
                        type: integer
                        description: |
                          __Read-only__ The amount of storage space, in MB, this Linode has access to.
                          A typical Linode will divide this space between a primary
                          disk with an `image` deployed to it, and a swap disk, usually
                          512 MB. This is the default configuration created when deploying
                          a Linode with an `image` through [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance).
                          While this configuration is suitable for 99% of use cases,
                          if you need finer control over your Linode's disks, see
                          the [List disks](https://techdocs.akamai.com/linode-api/reference/get-linode-disks)
                          operations.
                        example: 81920
                        readOnly: true
                      gpus:
                        type: integer
                        description: |
                          __Read-only__ The number of gpus this Linode has access to.
                        example: 0
                        readOnly: true
                      memory:
                        type: integer
                        description: |-
                          __Read-only__ The amount of RAM, in MB, this Linode has access to.

                          Typically, a Linode boots with all of its available RAM, but this can be configured in a Config profile. See the [List config profiles](https://techdocs.akamai.com/linode-api/reference/get-linode-configs) operations and the LinodeConfig object for more information.
                        example: 4096
                        readOnly: true
                      transfer:
                        type: integer
                        description: |
                          __Read-only__ The amount of network transfer this Linode is allotted each
                          month.
                        example: 4000
                        readOnly: true
                      vcpus:
                        type: integer
                        description: |
                          __Read-only__ The number of vcpus this Linode has access to.
                        example: 2
                        readOnly: true
                  status:
                    type: string
                    description: |
                      __Read-only__ A brief description of this Linode's current state. This field may change
                      without direct action from you. For example, when a Linode goes
                      into maintenance mode its status will display "stopped".
                    example: running
                    readOnly: true
                    enum:
                    - running
                    - offline
                    - booting
                    - rebooting
                    - shutting_down
                    - provisioning
                    - deleting
                    - migrating
                    - rebuilding
                    - cloning
                    - restoring
                    - stopped
                    - billing_suspension
                    x-linode-cli-color:
                      billing_suspension: red
                      default_: yellow
                      offline: red
                      running: green
                    x-linode-cli-display: 6
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Linode was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  watchdog_enabled:
                    type: boolean
                    description: |
                      The watchdog, named Lassie, is a Shutdown Watchdog that monitors your Linode and
                      will reboot it if it powers off unexpectedly. It works by issuing
                      a boot job when your Linode powers off without a shutdown job
                      being responsible. To prevent a loop, Lassie will give up if
                      there have been more than 5 boot jobs issued within 15 minutes.
                    example: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-linode-instance
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "label": "linode123",
                "group": "Linode-Group",
                "alerts": {
                  "cpu": 180,
                  "network_in": 10,
                  "network_out": 10,
                  "transfer_quota": 80,
                  "io": 10000
                },
                "backups": {
                  "schedule": {
                    "day": "Saturday",
                    "window": "W22"
                  }
                }
              }' \
              https://api.linode.com/v4/linode/instances/123
      - lang: CLI
        source: |-
          linode-cli linodes update 7833080 \
            --label linode123 \
            --backups.schedule.day "Saturday" \
            --backups.schedule.window "W22" \
            --alerts.cpu 180 \
            --alerts.network_in 10 \
            --alerts.network_out 10 \
            --alerts.transfer_quota 80 \
            --alerts.io 10000
      x-linode-cli-action: update
      x-linode-grant: read_write
      x-original-op-id: updateLinodeInstance
      x-original-op-title: Linode Update
    delete:
      operationId: delete-linode-instance
      summary: Delete a Linode
      tags:
      - Linode instances
      description: |-
        Deletes a Linode you have permission to `read_write`.

        __Deleting a Linode is a destructive action and cannot be undone.__

        Additionally, deleting a Linode:

          - Gives up any IP addresses the Linode was assigned.
          - Deletes all Disks, Backups, Configs, etc.
          - Detaches any Volumes associated with the Linode.
          - Stops billing for the Linode and its associated services. You will be billed for time used within the billing period the Linode was active.

        Linodes that are in the process of [cloning](https://techdocs.akamai.com/linode-api/reference/post-clone-linode-instance) or [backup restoration](https://techdocs.akamai.com/linode-api/reference/post-restore-backup) cannot be deleted.


        ---


        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Delete successful
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-linode-instance
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/linode/instances/123
      - lang: CLI
        source: linode-cli linodes delete 123
      x-linode-cli-action:
      - delete
      - rm
      x-linode-grant: read_write
      x-original-op-id: deleteLinodeInstance
      x-original-op-title: Linode Delete
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/backups:
    x-akamai:
      file-path: paths/linode-backups.yaml
      path-info: /linode/instances/{linodeId}/backups
    parameters:
    - name: linodeId
      in: path
      description: |
        The ID of the Linode the backups belong to.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path-c264fcc4.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-snapshot
      summary: Create a snapshot
      tags:
      - Backups
      description: |-
        Creates a snapshot backup of a Linode.

        __Note__. Backups are not encrypted even when they are taken from an encrypted disk. When a backup is restored, and if encryption is enabled, the data stored on the disk is encrypted again.

        __Important:__ If you already have a snapshot of this Linode, this is a destructive action. The previous snapshot will be deleted.


        ---


        - __CLI__.

            ```
            linode-cli linodes snapshot
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes snapshot
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - label
              properties:
                label:
                  type: string
                  description: |
                    The label for the new snapshot.
                  example: '{{label}}'
                  minLength: 1
                  maxLength: 255
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Snapshot request successful.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An object representing a Backup or snapshot for a Linode with Backup service enabled.
                x-akamai:
                  file-path: schemas/backup.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of this Backup.
                    example: 123456
                    readOnly: true
                    x-linode-cli-display: 1
                  type:
                    type: string
                    description: |
                      __Read-only__ This indicates whether the Backup is an automatic Backup or manual
                      snapshot taken by the User at a specific point in time.
                    example: snapshot
                    readOnly: true
                    enum:
                    - auto
                    - snapshot
                    x-linode-cli-display: 3
                  available:
                    type: boolean
                    description: |-
                      __Read-only__ Whether this Backup is available for restoration.

                      Backups undergoing maintenance are not available for restoration.
                    example: true
                    readOnly: true
                  configs:
                    type: array
                    description: |
                      __Read-only__ A list of the labels of the Configuration profiles that are part of
                      the Backup.
                    readOnly: true
                    items:
                      type: string
                      example: My Debian 9 Config
                  created:
                    type: string
                    description: |
                      __Read-only__ The date the Backup was taken.
                    example: '2018-01-15T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 4
                  disks:
                    type: array
                    description: |
                      __Read-only__ A list of the disks that are part of the Backup.
                    readOnly: true
                    items:
                      type: object
                      additionalProperties: false
                      properties:
                        filesystem:
                          type: string
                          description: |-
                            The Disk filesystem can be one of:

                              - raw - No filesystem, just a raw binary stream.
                              - swap - Linux swap area.
                              - ext3 - The ext3 journaling filesystem for Linux.
                              - ext4 - The ext4 journaling filesystem for Linux.
                              - initrd - initrd (uncompressed initrd, ext2, max 32 MB).
                          example: ext4
                          enum:
                          - raw
                          - swap
                          - ext3
                          - ext4
                          - initrd
                          x-linode-cli-display: 5
                        label:
                          type: string
                          example: My Debian 9 Disk
                        size:
                          type: integer
                          example: 9001
                  finished:
                    type: string
                    description: |
                      __Read-only__ The date the Backup completed.
                    example: '2018-01-15T00:01:01'
                    readOnly: true
                    format: date-time
                  label:
                    type: string
                    description: |
                      A label for Backups that are of type `snapshot`.
                    example: Webserver-Backup-2018
                    nullable: true
                    x-linode-cli-display: 5
                  status:
                    type: string
                    description: |
                      __Read-only__ The current state of a specific Backup.
                    example: successful
                    readOnly: true
                    enum:
                    - paused
                    - pending
                    - running
                    - needsPostProcessing
                    - successful
                    - failed
                    - userAborted
                    x-linode-cli-color:
                      default_: yellow
                      failed: red
                      successful: green
                      userAborted: f
                    x-linode-cli-display: 2
                  updated:
                    type: string
                    description: |
                      __Read-only__ The date the Backup was most recently updated.
                    example: '2018-01-15T00:01:01'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-snapshot
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "label": "MyNewSnapshot"
              }' \
              https://api.linode.com/v4/linode/instances/123/backups
      - lang: CLI
        source: linode-cli linodes snapshot 123
      x-linode-cli-action: snapshot
      x-linode-grant: read_write
      x-original-op-id: createSnapshot
      x-original-op-title: Snapshot Create
    get:
      operationId: get-backups
      summary: List backups
      tags:
      - Backups
      description: |-
        Returns information about this Linode's available backups.


        ---


        - __CLI__.

            ```
            linode-cli linodes backups-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes backups-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A collection of the specified Linode's available backups.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  automatic:
                    type: array
                    items:
                      allOf:
                      - type: object
                        description: |
                          An object representing a Backup or snapshot for a Linode with Backup service enabled.
                        x-akamai:
                          file-path: schemas/backup.yaml
                        additionalProperties: false
                        properties:
                          id:
                            type: integer
                            description: |
                              __Read-only__ The unique ID of this Backup.
                            example: 123456
                            readOnly: true
                            x-linode-cli-display: 1
                          type:
                            type: string
                            description: |
                              __Read-only__ This indicates whether the Backup is an automatic Backup
                              or manual snapshot taken by the User at a specific point
                              in time.
                            example: snapshot
                            readOnly: true
                            enum:
                            - auto
                            - snapshot
                            x-linode-cli-display: 3
                          available:
                            type: boolean
                            description: |-
                              __Read-only__ Whether this Backup is available for restoration.

                              Backups undergoing maintenance are not available for restoration.
                            example: true
                            readOnly: true
                          configs:
                            type: array
                            description: |
                              __Read-only__ A list of the labels of the Configuration profiles that
                              are part of the Backup.
                            readOnly: true
                            items:
                              type: string
                              example: My Debian 9 Config
                          created:
                            type: string
                            description: |
                              __Read-only__ The date the Backup was taken.
                            example: '2018-01-15T00:01:01'
                            readOnly: true
                            format: date-time
                            x-linode-cli-display: 4
                          disks:
                            type: array
                            description: |
                              __Read-only__ A list of the disks that are part of the Backup.
                            readOnly: true
                            items:
                              type: object
                              additionalProperties: false
                              properties:
                                filesystem:
                                  type: string
                                  description: |-
                                    The Disk filesystem can be one of:

                                      - raw - No filesystem, just a raw binary stream.
                                      - swap - Linux swap area.
                                      - ext3 - The ext3 journaling filesystem for Linux.
                                      - ext4 - The ext4 journaling filesystem for Linux.
                                      - initrd - initrd (uncompressed initrd, ext2, max 32 MB).
                                  example: ext4
                                  enum:
                                  - raw
                                  - swap
                                  - ext3
                                  - ext4
                                  - initrd
                                  x-linode-cli-display: 5
                                label:
                                  type: string
                                  example: My Debian 9 Disk
                                size:
                                  type: integer
                                  example: 9001
                          finished:
                            type: string
                            description: |
                              __Read-only__ The date the Backup completed.
                            example: '2018-01-15T00:01:01'
                            readOnly: true
                            format: date-time
                          label:
                            type: string
                            description: |
                              A label for Backups that are of type `snapshot`.
                            example: Webserver-Backup-2018
                            nullable: true
                            x-linode-cli-display: 5
                          status:
                            type: string
                            description: |
                              __Read-only__ The current state of a specific Backup.
                            example: successful
                            readOnly: true
                            enum:
                            - paused
                            - pending
                            - running
                            - needsPostProcessing
                            - successful
                            - failed
                            - userAborted
                            x-linode-cli-color:
                              default_: yellow
                              failed: red
                              successful: green
                              userAborted: f
                            x-linode-cli-display: 2
                          updated:
                            type: string
                            description: |
                              __Read-only__ The date the Backup was most recently updated.
                            example: '2018-01-15T00:01:01'
                            readOnly: true
                            format: date-time
                      - properties:
                          type:
                            type: string
                            example: automatic
                  snapshot:
                    type: object
                    additionalProperties: false
                    properties:
                      current:
                        type: object
                        description: |
                          An object representing a Backup or snapshot for a Linode with Backup service enabled.
                        x-akamai:
                          file-path: schemas/backup.yaml
                        additionalProperties: false
                        properties:
                          id:
                            type: integer
                            description: |
                              __Read-only__ The unique ID of this Backup.
                            example: 123456
                            readOnly: true
                            x-linode-cli-display: 1
                          type:
                            type: string
                            description: |
                              __Read-only__ This indicates whether the Backup is an automatic Backup
                              or manual snapshot taken by the User at a specific point
                              in time.
                            example: snapshot
                            readOnly: true
                            enum:
                            - auto
                            - snapshot
                            x-linode-cli-display: 3
                          available:
                            type: boolean
                            description: |-
                              __Read-only__ Whether this Backup is available for restoration.

                              Backups undergoing maintenance are not available for restoration.
                            example: true
                            readOnly: true
                          configs:
                            type: array
                            description: |
                              __Read-only__ A list of the labels of the Configuration profiles that
                              are part of the Backup.
                            readOnly: true
                            items:
                              type: string
                              example: My Debian 9 Config
                          created:
                            type: string
                            description: |
                              __Read-only__ The date the Backup was taken.
                            example: '2018-01-15T00:01:01'
                            readOnly: true
                            format: date-time
                            x-linode-cli-display: 4
                          disks:
                            type: array
                            description: |
                              __Read-only__ A list of the disks that are part of the Backup.
                            readOnly: true
                            items:
                              type: object
                              additionalProperties: false
                              properties:
                                filesystem:
                                  type: string
                                  description: |-
                                    The Disk filesystem can be one of:

                                      - raw - No filesystem, just a raw binary stream.
                                      - swap - Linux swap area.
                                      - ext3 - The ext3 journaling filesystem for Linux.
                                      - ext4 - The ext4 journaling filesystem for Linux.
                                      - initrd - initrd (uncompressed initrd, ext2, max 32 MB).
                                  example: ext4
                                  enum:
                                  - raw
                                  - swap
                                  - ext3
                                  - ext4
                                  - initrd
                                  x-linode-cli-display: 5
                                label:
                                  type: string
                                  example: My Debian 9 Disk
                                size:
                                  type: integer
                                  example: 9001
                          finished:
                            type: string
                            description: |
                              __Read-only__ The date the Backup completed.
                            example: '2018-01-15T00:01:01'
                            readOnly: true
                            format: date-time
                          label:
                            type: string
                            description: |
                              A label for Backups that are of type `snapshot`.
                            example: Webserver-Backup-2018
                            nullable: true
                            x-linode-cli-display: 5
                          status:
                            type: string
                            description: |
                              __Read-only__ The current state of a specific Backup.
                            example: successful
                            readOnly: true
                            enum:
                            - paused
                            - pending
                            - running
                            - needsPostProcessing
                            - successful
                            - failed
                            - userAborted
                            x-linode-cli-color:
                              default_: yellow
                              failed: red
                              successful: green
                              userAborted: f
                            x-linode-cli-display: 2
                          updated:
                            type: string
                            description: |
                              __Read-only__ The date the Backup was most recently updated.
                            example: '2018-01-15T00:01:01'
                            readOnly: true
                            format: date-time
                      in_progress:
                        type: object
                        description: |
                          An object representing a Backup or snapshot for a Linode with Backup service enabled.
                        x-akamai:
                          file-path: schemas/backup.yaml
                        additionalProperties: false
                        properties:
                          id:
                            type: integer
                            description: |
                              __Read-only__ The unique ID of this Backup.
                            example: 123456
                            readOnly: true
                            x-linode-cli-display: 1
                          type:
                            type: string
                            description: |
                              __Read-only__ This indicates whether the Backup is an automatic Backup
                              or manual snapshot taken by the User at a specific point
                              in time.
                            example: snapshot
                            readOnly: true
                            enum:
                            - auto
                            - snapshot
                            x-linode-cli-display: 3
                          available:
                            type: boolean
                            description: |-
                              __Read-only__ Whether this Backup is available for restoration.

                              Backups undergoing maintenance are not available for restoration.
                            example: true
                            readOnly: true
                          configs:
                            type: array
                            description: |
                              __Read-only__ A list of the labels of the Configuration profiles that
                              are part of the Backup.
                            readOnly: true
                            items:
                              type: string
                              example: My Debian 9 Config
                          created:
                            type: string
                            description: |
                              __Read-only__ The date the Backup was taken.
                            example: '2018-01-15T00:01:01'
                            readOnly: true
                            format: date-time
                            x-linode-cli-display: 4
                          disks:
                            type: array
                            description: |
                              __Read-only__ A list of the disks that are part of the Backup.
                            readOnly: true
                            items:
                              type: object
                              additionalProperties: false
                              properties:
                                filesystem:
                                  type: string
                                  description: |-
                                    The Disk filesystem can be one of:

                                      - raw - No filesystem, just a raw binary stream.
                                      - swap - Linux swap area.
                                      - ext3 - The ext3 journaling filesystem for Linux.
                                      - ext4 - The ext4 journaling filesystem for Linux.
                                      - initrd - initrd (uncompressed initrd, ext2, max 32 MB).
                                  example: ext4
                                  enum:
                                  - raw
                                  - swap
                                  - ext3
                                  - ext4
                                  - initrd
                                  x-linode-cli-display: 5
                                label:
                                  type: string
                                  example: My Debian 9 Disk
                                size:
                                  type: integer
                                  example: 9001
                          finished:
                            type: string
                            description: |
                              __Read-only__ The date the Backup completed.
                            example: '2018-01-15T00:01:01'
                            readOnly: true
                            format: date-time
                          label:
                            type: string
                            description: |
                              A label for Backups that are of type `snapshot`.
                            example: Webserver-Backup-2018
                            nullable: true
                            x-linode-cli-display: 5
                          status:
                            type: string
                            description: |
                              __Read-only__ The current state of a specific Backup.
                            example: successful
                            readOnly: true
                            enum:
                            - paused
                            - pending
                            - running
                            - needsPostProcessing
                            - successful
                            - failed
                            - userAborted
                            x-linode-cli-color:
                              default_: yellow
                              failed: red
                              successful: green
                              userAborted: f
                            x-linode-cli-display: 2
                          updated:
                            type: string
                            description: |
                              __Read-only__ The date the Backup was most recently updated.
                            example: '2018-01-15T00:01:01'
                            readOnly: true
                            format: date-time
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
              x-linode-cli-rows:
              - automatic
              - snapshot.current
              - snapshot.in_progress
              x-linode-cli-use-schema:
                type: object
                description: |
                  An object representing a Backup or snapshot for a Linode with Backup service enabled.
                x-akamai:
                  file-path: schemas/backup.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of this Backup.
                    example: 123456
                    readOnly: true
                    x-linode-cli-display: 1
                  type:
                    type: string
                    description: |
                      __Read-only__ This indicates whether the Backup is an automatic Backup or manual
                      snapshot taken by the User at a specific point in time.
                    example: snapshot
                    readOnly: true
                    enum:
                    - auto
                    - snapshot
                    x-linode-cli-display: 3
                  available:
                    type: boolean
                    description: |-
                      __Read-only__ Whether this Backup is available for restoration.

                      Backups undergoing maintenance are not available for restoration.
                    example: true
                    readOnly: true
                  configs:
                    type: array
                    description: |
                      __Read-only__ A list of the labels of the Configuration profiles that are part of
                      the Backup.
                    readOnly: true
                    items:
                      type: string
                      example: My Debian 9 Config
                  created:
                    type: string
                    description: |
                      __Read-only__ The date the Backup was taken.
                    example: '2018-01-15T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 4
                  disks:
                    type: array
                    description: |
                      __Read-only__ A list of the disks that are part of the Backup.
                    readOnly: true
                    items:
                      type: object
                      additionalProperties: false
                      properties:
                        filesystem:
                          type: string
                          description: |-
                            The Disk filesystem can be one of:

                              - raw - No filesystem, just a raw binary stream.
                              - swap - Linux swap area.
                              - ext3 - The ext3 journaling filesystem for Linux.
                              - ext4 - The ext4 journaling filesystem for Linux.
                              - initrd - initrd (uncompressed initrd, ext2, max 32 MB).
                          example: ext4
                          enum:
                          - raw
                          - swap
                          - ext3
                          - ext4
                          - initrd
                          x-linode-cli-display: 5
                        label:
                          type: string
                          example: My Debian 9 Disk
                        size:
                          type: integer
                          example: 9001
                  finished:
                    type: string
                    description: |
                      __Read-only__ The date the Backup completed.
                    example: '2018-01-15T00:01:01'
                    readOnly: true
                    format: date-time
                  label:
                    type: string
                    description: |
                      A label for Backups that are of type `snapshot`.
                    example: Webserver-Backup-2018
                    nullable: true
                    x-linode-cli-display: 5
                  status:
                    type: string
                    description: |
                      __Read-only__ The current state of a specific Backup.
                    example: successful
                    readOnly: true
                    enum:
                    - paused
                    - pending
                    - running
                    - needsPostProcessing
                    - successful
                    - failed
                    - userAborted
                    x-linode-cli-color:
                      default_: yellow
                      failed: red
                      successful: green
                      userAborted: f
                    x-linode-cli-display: 2
                  updated:
                    type: string
                    description: |
                      __Read-only__ The date the Backup was most recently updated.
                    example: '2018-01-15T00:01:01'
                    readOnly: true
                    format: date-time
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-backups
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/linode/instances/123/backups
      - lang: CLI
        source: linode-cli linodes backups-list 123
      x-linode-cli-action: backups-list
      x-linode-grant: read_only
      x-original-op-id: getBackups
      x-original-op-title: Backups List
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/backups/cancel:
    x-akamai:
      file-path: paths/linode-backups-cancel.yaml
      path-info: /linode/instances/{linodeId}/backups/cancel
    parameters:
    - name: linodeId
      in: path
      description: |
        The ID of the Linode to cancel backup service for.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path-8c2bdc46.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-cancel-backups
      summary: Cancel backups
      tags:
      - Backups
      description: |-
        Cancels the Backup service on the given Linode. Deletes all of this Linode's existing backups forever.


        ---


        - __CLI__.

            ```
            linode-cli linodes backups-cancel
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes backups-cancel
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Backup service was canceled for the specified Linode.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-cancel-backups
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST \
              https://api.linode.com/v4/linode/instances/123/backups/cancel
      - lang: CLI
        source: linode-cli linodes backups-cancel 123
      x-linode-cli-action: backups-cancel
      x-linode-grant: read_write
      x-original-op-id: cancelBackups
      x-original-op-title: Backups Cancel
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/backups/enable:
    x-akamai:
      file-path: paths/backup-enable.yaml
      path-info: /linode/instances/{linodeId}/backups/enable
    parameters:
    - name: linodeId
      in: path
      description: |
        The ID of the Linode to enable backup service for.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path-b606dac5.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-enable-backups
      summary: Enable backups
      tags:
      - Backups
      description: |-
        Enables backups for the specified Linode.

        __Note__. Backups are not encrypted even when they are taken from an encrypted disk. When a backup is restored, and if encryption is enabled, the data stored on the disk is encrypted again.


        ---


        - __CLI__.

            ```
            linode-cli linodes backups-enable
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes backups-enable
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Backup service was enabled.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-enable-backups
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST \
              https://api.linode.com/v4/linode/instances/123/backups/enable
      - lang: CLI
        source: linode-cli linodes backups-enable 123
      x-linode-cli-action: backups-enable
      x-linode-grant: read_write
      x-original-op-id: enableBackups
      x-original-op-title: Backups Enable
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/backups/{backupId}:
    x-akamai:
      file-path: paths/linode-backup.yaml
      path-info: /linode/instances/{linodeId}/backups/{backupId}
    parameters:
    - name: linodeId
      in: path
      description: |
        The ID of the Linode the Backup belongs to.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path-2f961b69.yaml
      required: true
      schema:
        type: integer
    - name: backupId
      in: path
      description: |
        The ID of the Backup to look up.
      example: '{{backupId}}'
      x-akamai:
        file-path: parameters/backup-id-path-4ea00e5a.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-backup
      summary: Get a backup
      tags:
      - Backups
      description: |-
        Returns information about a Backup.


        ---


        - __CLI__.

            ```
            linode-cli linodes backup-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes backup-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A single Backup.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An object representing a Backup or snapshot for a Linode with Backup service enabled.
                x-akamai:
                  file-path: schemas/backup.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of this Backup.
                    example: 123456
                    readOnly: true
                    x-linode-cli-display: 1
                  type:
                    type: string
                    description: |
                      __Read-only__ This indicates whether the Backup is an automatic Backup or manual
                      snapshot taken by the User at a specific point in time.
                    example: snapshot
                    readOnly: true
                    enum:
                    - auto
                    - snapshot
                    x-linode-cli-display: 3
                  available:
                    type: boolean
                    description: |-
                      __Read-only__ Whether this Backup is available for restoration.

                      Backups undergoing maintenance are not available for restoration.
                    example: true
                    readOnly: true
                  configs:
                    type: array
                    description: |
                      __Read-only__ A list of the labels of the Configuration profiles that are part of
                      the Backup.
                    readOnly: true
                    items:
                      type: string
                      example: My Debian 9 Config
                  created:
                    type: string
                    description: |
                      __Read-only__ The date the Backup was taken.
                    example: '2018-01-15T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 4
                  disks:
                    type: array
                    description: |
                      __Read-only__ A list of the disks that are part of the Backup.
                    readOnly: true
                    items:
                      type: object
                      additionalProperties: false
                      properties:
                        filesystem:
                          type: string
                          description: |-
                            The Disk filesystem can be one of:

                              - raw - No filesystem, just a raw binary stream.
                              - swap - Linux swap area.
                              - ext3 - The ext3 journaling filesystem for Linux.
                              - ext4 - The ext4 journaling filesystem for Linux.
                              - initrd - initrd (uncompressed initrd, ext2, max 32 MB).
                          example: ext4
                          enum:
                          - raw
                          - swap
                          - ext3
                          - ext4
                          - initrd
                          x-linode-cli-display: 5
                        label:
                          type: string
                          example: My Debian 9 Disk
                        size:
                          type: integer
                          example: 9001
                  finished:
                    type: string
                    description: |
                      __Read-only__ The date the Backup completed.
                    example: '2018-01-15T00:01:01'
                    readOnly: true
                    format: date-time
                  label:
                    type: string
                    description: |
                      A label for Backups that are of type `snapshot`.
                    example: Webserver-Backup-2018
                    nullable: true
                    x-linode-cli-display: 5
                  status:
                    type: string
                    description: |
                      __Read-only__ The current state of a specific Backup.
                    example: successful
                    readOnly: true
                    enum:
                    - paused
                    - pending
                    - running
                    - needsPostProcessing
                    - successful
                    - failed
                    - userAborted
                    x-linode-cli-color:
                      default_: yellow
                      failed: red
                      successful: green
                      userAborted: f
                    x-linode-cli-display: 2
                  updated:
                    type: string
                    description: |
                      __Read-only__ The date the Backup was most recently updated.
                    example: '2018-01-15T00:01:01'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-backup
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/linode/instances/123/backups/123456
      - lang: CLI
        source: linode-cli linodes backup-view 123 123456
      x-linode-cli-action: backup-view
      x-linode-grant: read_only
      x-original-op-id: getBackup
      x-original-op-title: Backup View
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/backups/{backupId}/restore:
    x-akamai:
      file-path: paths/restore.yaml
      path-info: /linode/instances/{linodeId}/backups/{backupId}/restore
    parameters:
    - name: linodeId
      in: path
      description: |
        The ID of the Linode that the Backup belongs to.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path-b7f66cf7.yaml
      required: true
      schema:
        type: integer
    - name: backupId
      in: path
      description: |
        The ID of the Backup to restore.
      example: '{{backupId}}'
      x-akamai:
        file-path: parameters/backup-id-path-d7402da7.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-restore-backup
      summary: Restore a backup
      tags:
      - Backups
      description: |-
        Restores a Linode's Backup to the specified Linode.

        The following conditions apply:

          - Backups may not be restored across Regions.
          - Only successfully completed Backups that are not undergoing maintenance can be restored.
          - The Linode that the Backup is being restored to must not itself be in the process of creating a Backup.

        __Note__. When you restore a backup, the restored disk is assigned the same [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) as the original disk. In most cases, this is acceptable and does not cause issues. However, if you attempt to mount both the original disk and the corresponding restore disk at the same time (by assigning them both to devices in your Configuration Profile's __Block Device Assignment__), you will encounter a UUID "collision".

        When this happens, the system selects, and mounts, only one of the disks at random. This is due to both disks sharing the same UUID, and your instance _may fail to boot_ since it will not be clear which disk is root. If your system does boot, you will not see any immediate indication if you are booted into the restored disk or the original disk, and you will be unable to access both disks at the same time.

        To avoid this, we recommend only restoring a backup to the same Compute Instance if you do not intend on mounting them at the same time or are comfortable modifying UUIDs. If you need access to files on both the original disk and the restored disk simultaneously (such as needing to copy files between them), we suggest either restoring the backup to a separate Compute Instance or [creating](https://techdocs.akamai.com/linode-api/reference/post-linode-instance) a new Compute Instance with the desired `backup_id`.

        To learn more about block device assignments and viewing your disks' UUIDs, see our guide on [Configuration Profiles](https://www.linode.com/docs/products/compute/compute-instances/guides/configuration-profiles/#block-device-assignment).

        __Note__. Backups are not encrypted even when they are taken from an encrypted disk. When a backup is restored, and if encryption is enabled, the data stored on the disk is encrypted again.


        ---


        - __CLI__.

            ```
            linode-cli linodes backup-restore
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes backup-restore
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Parameters to provide when restoring the Backup.
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - linode_id
              properties:
                linode_id:
                  type: integer
                  description: |
                    The ID of the Linode to restore a Backup to.
                  example: '{{linode_id}}'
                overwrite:
                  type: boolean
                  description: |-
                    If True, deletes all Disks and Configs on the target Linode before restoring.

                    If False, and the Disk image size is larger than the available
                    space on the Linode, an error message indicating insufficient
                    space is returned.
                  example: '{{overwrite}}'
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Restore from Backup was initiated.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-restore-backup
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "linode_id": 234,
                "overwrite": true
              }' \
              https://api.linode.com/v4/linode/instances/123/backups/123456/restore
      - lang: CLI
        source: |-
          linode-cli linodes backup-restore 123 123456 \
            --linode_id 234 \
            --overwrite true
      x-linode-cli-action: backup-restore
      x-linode-grant: read_write
      x-original-op-id: restoreBackup
      x-original-op-title: Backup Restore
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/boot:
    x-akamai:
      file-path: paths/boot.yaml
      path-info: /linode/instances/{linodeId}/boot
    parameters:
    - name: linodeId
      in: path
      description: |
        The ID of the Linode to boot.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path-3880d973.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-boot-linode-instance
      summary: Boot a Linode
      tags:
      - Linode instances
      description: |-
        Boots a Linode you have permission to modify. If no parameters are given, a Config profile will be chosen for this boot based on the following criteria:

        - If there is only one Config profile for this Linode, it will be used.
        - If there is more than one Config profile, the last booted config will be used.
        - If there is more than one Config profile and none were the last to be booted (because the Linode was never booted or the last booted config was deleted) an error will be returned.


        ---


        - __CLI__.

            ```
            linode-cli linodes boot
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes boot
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Optional configuration to boot into (see above).
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                config_id:
                  type: integer
                  description: |
                    The Linode Config ID to boot into.
                  example: '{{config_id}}'
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Boot started.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-boot-linode-instance
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST \
              https://api.linode.com/v4/linode/instances/123/boot
      - lang: CLI
        source: linode-cli linodes boot 123
      x-linode-cli-action: boot
      x-linode-grant: read_write
      x-original-op-id: bootLinodeInstance
      x-original-op-title: Linode Boot
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/clone:
    x-akamai:
      file-path: paths/linode-clone.yaml
      path-info: /linode/instances/{linodeId}/clone
    parameters:
    - name: linodeId
      in: path
      description: |
        ID of the Linode to clone.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path-8a1bf096.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-clone-linode-instance
      summary: Clone a Linode
      tags:
      - Linode instances
      description: |-
        You can clone your Linode's existing Disks or Configuration profiles to another Linode on your Account. In order for this request to complete successfully, your User must have the `add_linodes` grant. Cloning to a new Linode will incur a charge on your Account.

        If cloning to an existing Linode, any actions currently running or queued must be completed first before you can clone to it.

        Up to five clone operations from any given source Linode can be run concurrently. If more concurrent clones are attempted, an HTTP 400 error will be returned by this operation.

        Any [tags](https://techdocs.akamai.com/linode-api/reference/get-tags) existing on the source Linode will be cloned to the target Linode.

        Linodes utilizing Metadata (`"has_user_data": true`) must be cloned to a new Linode with `metadata.user_data` included with the clone request.

        `vpc` details

        - If the Linode you are cloning has a `vpc` purpose Interface on its active Configuration Profile that includes a 1:1 NAT, the resulting clone is configured with an `any` 1:1 NAT.
        - See the [VPC documentation](https://www.linode.com/docs/products/networking/vpc/#technical-specifications) guide for its specifications and limitations.

        `vlan` details

        - Only Next Generation Network (NGN) data centers support VLANs. If a VLAN is attached to your Linode and you attempt clone it to a non-NGN data center, the cloning will not initiate. If a Linode cannot be cloned because of an incompatibility, you will be prompted to select a different data center or contact support.
        - See the [VLANs Overview](https://www.linode.com/docs/products/networking/vlans/#technical-specifications) guide to view additional specifications and limitations.


        ---


        - __CLI__.

            ```
            linode-cli linodes clone
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes clone
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The requested state your Linode will be cloned into.
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                type:
                  type: string
                  description: |-
                    A Linode's Type determines what resources are available to it, including disk space, memory, and virtual cpus. The amounts available to a specific Linode are returned as `specs` on the Linode object.

                    To view all available Linode Types you can deploy with, run [List types](https://techdocs.akamai.com/linode-api/reference/get-types).

                    - Type can only be provided and is required when cloning to a new Linode.
                  example: '{{type}}'
                backups_enabled:
                  type: boolean
                  description: |-
                    If this field is set to `true`, the created Linode will automatically be enrolled in the Linode Backup service. This will incur an additional charge. Pricing is included in the response from [List types](https://techdocs.akamai.com/linode-api/reference/get-linode-types).

                    - Can only be included when cloning to a new Linode.
                  example: '{{backups_enabled}}'
                configs:
                  type: array
                  description: |-
                    An array of configuration profile IDs.

                    - If the `configs` parameter __is not provided__, then __all configuration profiles and their associated disks will be cloned__ from the source Linode. Any disks specified by the `disks` parameter will also be cloned.
                    - __If an empty array is provided__ for the `configs` parameter, then __no configuration profiles (nor their associated disks) will be cloned__ from the source Linode. Any disks specified by the `disks` parameter will still be cloned.
                    - __If a non-empty array is provided__ for the `configs` parameter, then __the configuration profiles specified in the array (and their associated disks) will be cloned__ from the source Linode. Any disks specified by the `disks` parameter will also be cloned.
                  items:
                    type: integer
                    example: 23456
                disks:
                  type: array
                  description: |-
                    An array of disk IDs.

                    - If the `disks` parameter __is not provided__, then __no extra disks will be cloned__ from the source Linode. All disks associated with the configuration profiles specified by the `configs` parameter will still be cloned.
                    - __If an empty array is provided__ for the `disks` parameter, then __no extra disks will be cloned__ from the source Linode. All disks associated with the configuration profiles specified by the `configs` parameter will still be cloned.
                    - __If a non-empty array is provided__ for the `disks` parameter, then __the disks specified in the array will be cloned__ from the source Linode, in addition to any disks associated with the configuration profiles specified by the `configs` parameter.
                  items:
                    type: integer
                    example: 25674
                group:
                  type: string
                  description: |
                    A label used to group Linodes for display. Linodes are not required to have a group.
                  example: '{{group}}'
                  deprecated: true
                label:
                  type: string
                  description: |-
                    The label to assign this Linode when cloning to a new Linode.

                    - Can only be provided when cloning to a new Linode.
                    - Defaults to "linode".
                  example: '{{label}}'
                  minLength: 3
                  maxLength: 64
                linode_id:
                  type: integer
                  description: |
                    If an existing Linode is the target for the clone, the ID of that Linode. The existing
                    Linode must have enough resources to accept the clone.
                  example: '{{linode_id}}'
                metadata:
                  type: object
                  description: |
                    __Write-only__ An object containing user-defined data relevant to the creation of Linodes.
                  additionalProperties: false
                  properties:
                    user_data:
                      type: string
                      description: |-
                        Base64-encoded [cloud-config](https://www.linode.com/docs/products/compute/compute-instances/guides/metadata-cloud-config/) data.

                        Cannot be modified after provisioning. To update, use either the [Clone a Linode](https://techdocs.akamai.com/linode-api/reference/post-clone-linode-instance) or [Rebuild a Linode](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance) operations.

                        Must not be included when cloning to an existing Linode.

                        Unencoded data must not exceed 65535 bytes, or about 16kb encoded.
                      example: I2Nsb3VkLWNvbmZpZwpwYWNrYWdlX3VwZGF0ZTogdHJ1ZQpwYWNrYWdlX3VwZ3JhZGU6IHRydWU=
                      format: byte
                  writeOnly: true
                placement_group:
                  type: object
                  description: |-
                    Include this to assign this Linode to an existing [placement group](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/). Consider these points when cloning:

                    - If the Linode you're cloning exists in a placement group, the API won't automatically add the cloned instance to the same placement group. You need to specify a placement group to add the clone to.
                    - The target placement group needs to be in the same `region` set for this Linode.
                    - The placement group needs to have capacity. Run the [Get a region](https://techdocs.akamai.com/linode-api/reference/get-region) operation and store the `maximum_linodes_per_pg` value to know the Linode limit per placement group. You can then run the [Get a placement group](https://techdocs.akamai.com/linode-api/reference/get-placement-group) operation to review the Linodes in that group.
                  additionalProperties: false
                  required:
                  - id
                  properties:
                    id:
                      type: integer
                      description: |
                        The placement group's ID. You need to provide it for all operations impacting it.
                      example: 528
                      nullable: false
                      x-linode-cli-display: 1
                private_ip:
                  type: boolean
                  description: |-
                    If true, the created Linode will have private networking enabled and assigned a private IPv4 address.

                    - Can only be provided when cloning to a new Linode.
                  example: '{{private_ip}}'
                region:
                  type: string
                  description: |-
                    This is the Region where the Linode will be deployed. To view all available Regions you can deploy to, run [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions).

                    - Region can only be provided and is required when cloning to a new Linode.
                  example: '{{region}}'
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
        x-linode-cli-allowed-defaults:
        - region
        - type
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Clone started.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/linode.yaml
                title: Linode
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Linode's ID which must be provided for all operations impacting
                      this Linode.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  tags:
                    type: array
                    description: |
                      An array of tags applied to this object.  Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  type:
                    type: string
                    description: |
                      __Read-only__ This is the [Linode type](https://techdocs.akamai.com/linode-api/reference/get-linode-types)
                      that this Linode was deployed with. To change a Linode's Type,
                      use [Resize a Linode](https://techdocs.akamai.com/linode-api/reference/post-resize-linode-instance).
                    example: g6-standard-1
                    readOnly: true
                    x-linode-cli-display: 4
                  alerts:
                    type: object
                    additionalProperties: false
                    properties:
                      cpu:
                        type: integer
                        description: |-
                          The percentage of CPU usage required to trigger an alert. If the average CPU usage over two hours exceeds this value, we'll send you an alert. Your Linode's total CPU capacity is represented as 100%, multiplied by its number of cores.

                          For example, a two core Linode's CPU capacity is represented as 200%. If you want to be alerted at 90% of a two core Linode's CPU capacity, set the alert value to `180`.

                          The default value is 90% multiplied by the number of cores.

                          If the value is set to `0` (zero), the alert is disabled.
                        example: 180
                      io:
                        type: integer
                        description: |
                          The amount of disk IO operation per second required to trigger an alert. If
                          the average disk IO over two hours exceeds this value, we'll
                          send you an alert. If set to `0` (zero), this alert is disabled.
                        example: 10000
                      network_in:
                        type: integer
                        description: |
                          The amount of incoming traffic, in Mbit/s, required to trigger an alert. If the
                          average incoming traffic over two hours exceeds this value,
                          we'll send you an alert. If this is set to `0` (zero), the
                          alert is disabled.
                        example: 10
                      network_out:
                        type: integer
                        description: |
                          The amount of outbound traffic, in Mbit/s, required to trigger an alert. If the
                          average outbound traffic over two hours exceeds this value,
                          we'll send you an alert. If this is set to `0` (zero), the
                          alert is disabled.
                        example: 10
                      transfer_quota:
                        type: integer
                        description: |
                          The percentage of network transfer that may be used before an alert is
                          triggered. When this value is exceeded, we'll alert you.
                          If this is set to `0` (zero), the alert is disabled.
                        example: 80
                  backups:
                    type: object
                    description: |
                      Information about this Linode's backups status. For information about available backups,
                      run [List backups](https://techdocs.akamai.com/linode-api/reference/get-backups).
                    additionalProperties: false
                    properties:
                      available:
                        type: boolean
                        description: |-
                          __Read-only__ Whether Backups for this Linode are available for restoration.

                          Backups undergoing maintenance are not available for restoration.
                        example: true
                        readOnly: true
                      enabled:
                        type: boolean
                        description: |
                          __Read-only__ If this Linode has the Backup service enabled. To enable backups,
                          run [Enable backups](https://techdocs.akamai.com/linode-api/reference/post-enable-backups).
                        example: true
                        readOnly: true
                      last_successful:
                        type: string
                        description: |
                          __Read-only__ The last successful backup date. 'null' if there was no previous
                          backup.
                        example: '2018-01-01T00:01:01'
                        readOnly: true
                        format: date-time
                      schedule:
                        type: object
                        additionalProperties: false
                        properties:
                          day:
                            type: string
                            description: |-
                              The day of the week that your Linode's weekly Backup is taken. If not set manually, a day will be chosen for you. Backups are taken every day, but backups taken on this day are preferred when selecting backups to retain for a longer period.

                              If not set manually, then when backups are initially enabled, this may come back as `Scheduling` until the `day` is automatically selected.
                            example: Saturday
                            nullable: true
                            enum:
                            - Scheduling
                            - Sunday
                            - Monday
                            - Tuesday
                            - Wednesday
                            - Thursday
                            - Friday
                            - Saturday
                          window:
                            type: string
                            description: |-
                              The window in which your backups will be taken, in UTC. A backups window is a two-hour span of time in which the backup may occur.

                              For example, `W10` indicates that your backups should be taken between 10:00 and 12:00. If you do not choose a backup window, one will be selected for you automatically.

                              If not set manually, when backups are initially enabled this may come back as `Scheduling` until the `window` is automatically selected.
                            example: W22
                            nullable: true
                            enum:
                            - Scheduling
                            - W0
                            - W2
                            - W4
                            - W6
                            - W8
                            - W10
                            - W12
                            - W14
                            - W16
                            - W18
                            - W20
                            - W22
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Linode was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  disk_encryption:
                    type: string
                    description: |
                      Local disk encryption ensures that your data stored on compute instances is encrypted.
                      Encryption converts the data on the compute instance into unreadable
                      code. Decryption of the the disk requires other systems within
                      the datacenter. This requirement protects against data leakage
                      if the disk is removed from a datacenter, lost, stolen, recycled
                      or disposed.
                    example: enabled
                    default: enabled
                    enum:
                    - enabled
                    - disabled
                  group:
                    type: string
                    description: |
                      A deprecated property denoting a group label for this Linode.
                    example: Linode-Group
                    deprecated: true
                    x-linode-filterable: true
                  has_user_data:
                    type: boolean
                    description: |
                      __Read-only__ Whether this compute instance was provisioned utilizing `user_data`
                      provided via the Metadata service. See the [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
                      description for more information on Metadata.
                    example: true
                    readOnly: true
                  host_uuid:
                    type: string
                    description: |
                      __Read-only__ The Linode's host machine, as a UUID.
                    example: 3a3ddd59d9a78bb8de041391075df44de62bfec8
                    readOnly: true
                    format: uuid
                  hypervisor:
                    type: string
                    description: |
                      __Read-only__ The virtualization software powering this Linode.
                    example: kvm
                    readOnly: true
                    enum:
                    - kvm
                  image:
                    example: linode/debian10
                    nullable: true
                    readOnly: true
                    allOf:
                    - type: string
                      description: |-
                        An Image ID to deploy the Linode Disk from.

                        Run the [List images](https://techdocs.akamai.com/linode-api/reference/get-images) operation with authentication to view all available Images. Official Linode Images start with `linode/`, while your Account's Images start with `private/`. Creating a disk from a Private Image requires `read_only` or `read_write` permissions for that Image. Run the [Update a user's grants](https://techdocs.akamai.com/linode-api/reference/put-user-grants) operation to adjust permissions for an Account Image.
                      example: linode/debian9
                    x-linode-cli-display: 5
                    x-linode-filterable: true
                  ipv4:
                    type: array
                    description: |-
                      __Read-only__ This Linode's IPv4 Addresses. Each Linode is assigned a single public IPv4 address upon creation, and may get a single private IPv4 address if needed. You may need to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) to get additional IPv4 addresses.

                      IPv4 addresses may be reassigned between your Linodes, or shared with other Linodes. See the [networking](https://techdocs.akamai.com/linode-api/reference/post-firewalls) operations for details.
                    example:
                    - 203.0.113.1
                    - 192.0.2.1
                    readOnly: true
                    format: ipv4
                    items:
                      type: string
                    x-linode-cli-display: 7
                    x-linode-filterable: true
                  ipv6:
                    type: string
                    description: |
                      __Read-only__ This Linode's IPv6 SLAAC address. This address is specific to a Linode,
                      and may not be shared. If the Linode has not been assigned an
                      IPv6 address, the return value will be `null`.
                    example: c001:d00d::1337/128
                    nullable: true
                    readOnly: true
                    format: ipv6/128
                  label:
                    type: string
                    description: |-
                      The Linode's label is for display purposes only. If no label is provided for a Linode, a default will be assigned.

                      Linode labels have the following constraints:

                      - Must begin and end with an alphanumeric character.
                      - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                      - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
                    example: linode123
                    minLength: 3
                    maxLength: 64
                    pattern: ^[a-zA-Z]((?!--|__|..)[a-zA-Z0-9-_.])+$
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  placement_group:
                    type: object
                    description: |
                      __Read-only__ Details on the [placement group](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/)
                      that this Linode belongs to. Empty if the Linode isn't in a
                      placement group.
                    additionalProperties: false
                    nullable: true
                    readOnly: true
                    properties:
                      id:
                        type: integer
                        description: |
                          The placement group's ID. You need to provide it for all operations impacting
                          it.
                        example: 528
                        nullable: false
                        x-linode-cli-display: 1
                      affinity_type:
                        type: string
                        description: |-
                          __Read-only__ How compute instances are distributed in your placement group. `anti-affinity:local` places compute instances in separate fault domains, but still in the same region.

                          __Note__: With the limited availability release, only `anti_affinity:local` is available for `affinity_type`.
                        example: anti-affinity:local
                        readOnly: true
                        x-linode-cli-display: 1
                        x-linode-filterable: true
                      is_strict:
                        type: boolean
                        description: |-
                          How the API enforces your `affinity_type`:

                          - Set to `true`, your group is strict. You can't add more compute instances to your placement group if your preferred container lacks capacity or is unavailable.
                          - Set to `false`, your group is flexible. You can add more compute instances to it even if they violate the `affinity_type`. If you violate the `affinity_type` your placement group becomes non-compliant and you need to wait for our assistance.
                        example: true
                        nullable: false
                      label:
                        type: string
                        description: |-
                          The unique name set for the placement group. A label has these constraints:

                          - It needs to begin and end with an alphanumeric character.
                          - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                        example: PG_Miami_failover
                        minLength: 1
                        x-linode-cli-display: 1
                        x-linode-filterable: true
                  region:
                    type: string
                    description: |
                      __Read-only__ This is the [region](https://techdocs.akamai.com/linode-api/reference/get-regions)
                      where the Linode was deployed. A Linode's region can only be
                      changed by initiating a [cross data center migration](https://techdocs.akamai.com/linode-api/reference/post-migrate-linode-instance).
                    example: us-east
                    readOnly: true
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  specs:
                    type: object
                    description: |
                      __Read-only__ Information about the resources available to this Linode.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      disk:
                        type: integer
                        description: |
                          __Read-only__ The amount of storage space, in MB, this Linode has access to.
                          A typical Linode will divide this space between a primary
                          disk with an `image` deployed to it, and a swap disk, usually
                          512 MB. This is the default configuration created when deploying
                          a Linode with an `image` through [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance).
                          While this configuration is suitable for 99% of use cases,
                          if you need finer control over your Linode's disks, see
                          the [List disks](https://techdocs.akamai.com/linode-api/reference/get-linode-disks)
                          operations.
                        example: 81920
                        readOnly: true
                      gpus:
                        type: integer
                        description: |
                          __Read-only__ The number of gpus this Linode has access to.
                        example: 0
                        readOnly: true
                      memory:
                        type: integer
                        description: |-
                          __Read-only__ The amount of RAM, in MB, this Linode has access to.

                          Typically, a Linode boots with all of its available RAM, but this can be configured in a Config profile. See the [List config profiles](https://techdocs.akamai.com/linode-api/reference/get-linode-configs) operations and the LinodeConfig object for more information.
                        example: 4096
                        readOnly: true
                      transfer:
                        type: integer
                        description: |
                          __Read-only__ The amount of network transfer this Linode is allotted each
                          month.
                        example: 4000
                        readOnly: true
                      vcpus:
                        type: integer
                        description: |
                          __Read-only__ The number of vcpus this Linode has access to.
                        example: 2
                        readOnly: true
                  status:
                    type: string
                    description: |
                      __Read-only__ A brief description of this Linode's current state. This field may change
                      without direct action from you. For example, when a Linode goes
                      into maintenance mode its status will display "stopped".
                    example: running
                    readOnly: true
                    enum:
                    - running
                    - offline
                    - booting
                    - rebooting
                    - shutting_down
                    - provisioning
                    - deleting
                    - migrating
                    - rebuilding
                    - cloning
                    - restoring
                    - stopped
                    - billing_suspension
                    x-linode-cli-color:
                      billing_suspension: red
                      default_: yellow
                      offline: red
                      running: green
                    x-linode-cli-display: 6
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Linode was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  watchdog_enabled:
                    type: boolean
                    description: |
                      The watchdog, named Lassie, is a Shutdown Watchdog that monitors your Linode and
                      will reboot it if it powers off unexpectedly. It works by issuing
                      a boot job when your Linode powers off without a shutdown job
                      being responsible. To prevent a loop, Lassie will give up if
                      there have been more than 5 boot jobs issued within 15 minutes.
                    example: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-clone-linode-instance
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "region": "us-east",
                "type": "g6-standard-2",
                "linode_id": 124,
                "label": "cloned-linode",
                "group": "Linode-Group",
                "placement_group": {
                  "id": 528
                },
                "backups_enabled": true,
                "disks": [25674],
                "configs": [23456],
                "private_ip": true,
                "metadata": {
                  "user_data": "I2Nsb3VkLWNvbmZpZw=="
                }
              }' \
              https://api.linode.com/v4/linode/instances/123/clone
      - lang: CLI
        source: |-
          linode-cli linodes clone 123 \
            --linode_id 124 \
            --region us-east \
            --type g6-standard-2 \
            --label cloned-linode \
            --backups_enabled true \
            --placement_group.id 528 \
            --disks 25674 \
            --configs 23456 \
            --private_ip true \
            --metadata.user_data I2Nsb3VkLWNvbmZpZw==
      x-linode-charge: true
      x-linode-cli-action: clone
      x-linode-grant: add_linodes
      x-original-op-id: cloneLinodeInstance
      x-original-op-title: Linode Clone
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/configs:
    x-akamai:
      file-path: paths/linode-configs.yaml
      path-info: /linode/instances/{linodeId}/configs
    parameters:
    - name: linodeId
      in: path
      description: |
        ID of the Linode to look up Configuration profiles for.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path-1f0ac574.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-add-linode-config
      summary: Create a config profile
      tags:
      - Configurations
      description: |-
        Adds a new Configuration profile to a Linode.


        ---


        - __CLI__.

            ```
            linode-cli linodes config-create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes config-create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The parameters to set when creating the Configuration profile. This determines which kernel, devices, how much
          memory, etc. a Linode boots with.
        required: true
        content:
          application/json:
            schema:
              required:
              - label
              - devices
              allOf:
              - type: object
                x-akamai:
                  file-path: schemas/linode-config.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The ID of this Config.
                    example: 23456
                    readOnly: true
                    x-linode-cli-display: 1
                  comments:
                    type: string
                    description: |
                      Optional field for arbitrary User comments on this Config.
                    example: This is my main Config
                    nullable: true
                  devices:
                    type: object
                    description: |-
                      A dictionary of device disks to use as a device map in a Linode's configuration profile.

                      - An empty device disk dictionary or a dictionary with empty values for device slots is allowed.
                      - If no devices are specified, booting from this configuration will hold until a device exists that allows the boot process to start.
                    x-akamai:
                      file-path: schemas/devices.yaml
                    additionalProperties: false
                    properties:
                      sda:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sdb:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sdc:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sdd:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sde:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sdf:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sdg:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sdh:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                  helpers:
                    type: object
                    description: |
                      Helpers enabled when booting to this Linode Config.
                    additionalProperties: false
                    properties:
                      devtmpfs_automount:
                        type: boolean
                        description: |
                          Populates the /dev directory early during boot without udev.  Defaults to false.
                        example: false
                      distro:
                        type: boolean
                        description: |
                          Helps maintain correct inittab/upstart console device.
                        example: true
                      modules_dep:
                        type: boolean
                        description: |
                          Creates a modules dependency file for the Kernel you run.
                        example: true
                      network:
                        type: boolean
                        description: |
                          Automatically configures static networking.
                        example: true
                      updatedb_disabled:
                        type: boolean
                        description: |
                          Disables updatedb cron job to avoid disk thrashing.
                        example: true
                  interfaces:
                    type: array
                    description: |-
                      An array of Network Interfaces to add to this Linode's Configuration Profile. At least one and up to three Interface objects can exist in this array. The position in the array determines which of the Linode's network Interfaces is configured:

                      - First [0]:  eth0
                      - Second [1]: eth1
                      - Third [2]:  eth2

                      When updating a Linode's Interfaces, _each Interface must be redefined_. An empty `interfaces` array results in a default `public` type Interface configuration only.

                      If no public Interface is configured, public IP addresses are still assigned to the Linode but will not be usable without manual configuration.

                      __Note:__ Changes to Linode Interface configurations can be enabled by rebooting the Linode.

                      `vpc` details

                      See the [VPC documentation](https://www.linode.com/docs/products/networking/vpc/#technical-specifications) guide for its specifications and limitations.

                      `vlan` details

                      - Only Next Generation Network (NGN) data centers support VLANs. Run the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation to view the capabilities of data center regions. If a VLAN is attached to your Linode and you attempt to migrate or clone it to a non-NGN data center, the migration or cloning will not initiate. If a Linode cannot be migrated or cloned because of an incompatibility, you will be prompted to select a different data center or contact support.
                      - See the [VLANs Overview](https://www.linode.com/docs/products/networking/vlans/#technical-specifications) guide to view additional specifications and limitations.
                    example:
                    - id: 101
                      ipam_address: null
                      ipv4: null
                      label: null
                      primary: false
                      purpose: public
                      subnet_id: null
                      vpc_id: null
                    - id: 102
                      ipam_address: 10.0.0.1/24
                      ipv4:
                        nat_1_1: null
                        vpc: 10.0.0.2
                      label: vlan-1
                      primary: false
                      purpose: vlan
                      subnet_id: null
                      vpc_id: null
                    - id: 103
                      ipam_address: null
                      ipv4:
                        nat_1_1: 203.0.113.2
                        vpc: 10.0.1.2
                      label: null
                      primary: true
                      purpose: vpc
                      subnet_id: 101
                      vpc_id: 111
                    x-akamai:
                      file-path: schemas/linode-config-interfaces.yaml
                    required:
                    - purpose
                    minItems: 1
                    maxItems: 3
                    uniqueItems: true
                    items:
                      type: object
                      description: |
                        The Network Interface to apply to this Linode's configuration profile.
                      x-akamai:
                        file-path: schemas/linode-config-interface.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ The unique ID representing this Interface.
                          example: 101
                          readOnly: true
                          x-linode-cli-display: 1
                        active:
                          type: boolean
                          description: |
                            Returns `true` if the Interface is in use, meaning that Compute Instance
                            has been booted using the Configuration Profile to which
                            the Interface belongs. Otherwise returns `false`.
                          example: true
                        ip_ranges:
                          type: array
                          description: |-
                            An array of IPv4 CIDR VPC Subnet ranges that are routed to this Interface.

                            - Array items are only allowed for `vpc` type Interfaces.
                            - This must be empty for non-`vpc` type Interfaces.

                            For requests:

                            - Addresses in submitted ranges must not already be actively assigned.
                            - Submitting values replaces any existing values.
                            - Submitting an empty array removes any existing values.
                            - Omitting this property results in no change to existing values.
                          example:
                          - 10.0.0.64/26
                          - fd04:495a:691c:971c::1:0/112
                          nullable: true
                          items:
                            type: string
                            format: ip
                        ipam_address:
                          type: string
                          description: |-
                            This Network Interface's private IP address in Classless Inter-Domain Routing (CIDR) notation.

                            For `vlan` purpose Interfaces:

                            - Must be unique among the Linode's Interfaces to avoid conflicting addresses.
                            - Should be unique among devices attached to the VLAN to avoid conflict.
                            - The Linode is configured to use this address for the associated Interface upon reboot if Network Helper is enabled. If Network Helper is disabled, the address can be enabled with [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).

                            For `public` purpose Interfaces:

                            - In requests, must be an empty string (`""`) or `null` if included.
                            - In responses, always returns `null`.

                            For `vpc` purpose Interfaces:

                            - In requests, must be an empty string (`""`) or `null` if included.
                            - In responses, always returns `null`.
                          example: 10.0.0.1/24
                          nullable: true
                          format: ip/netmask
                          x-linode-cli-display: 4
                        ipv4:
                          type: object
                          description: |
                            IPv4 addresses configured for this Interface. Only allowed for `vpc`
                            type Interfaces. Returns `null` if no `vpc` Interface
                            is assigned.
                          additionalProperties: false
                          properties:
                            nat_1_1:
                              type: string
                              description: |-
                                The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the VPC Subnet IPv4 address assigned to this Interface.

                                - Only allowed for `vpc` type Interfaces.
                                - Returns `null` if no 1:1 NAT is set for a `vpc` type Interface.
                                - Returns an empty string (`""`) for non-`vpc` type Interfaces.

                                For requests:

                                - Setting this value to `any` enables the Linode's assigned public IPv4 address on this Interface and establishes a 1:1 NAT between the public IPv4 and VPC Subnet IPv4 addresses.
                                - Setting the value to a specific public IPv4 address that is assigned to the Linode enables a 1:1 NAT between that address and the VPC Subnet IPv4 address.
                                - The public IPv4 address can't be shared with another Linode.
                                - If omitted, set to `null`, or set to an empty string (`""`), no 1:1 NAT is established.

                                __Note__: When creating a new compute-instance, you can't set this to a specific IPv4 address. When a new compute instance is created, the network establishes a public IPv4 address for it. Since this address doesn't exist yet, you can't include a custom IPv4 address to change it. Once your compute instances is created, you can [update your configuration profile interface](https://www.linode.com/docs/api/linode-instances/#configuration-profile-interface-update) to change the `nat_1_1` address.
                              example: 203.0.113.2
                              nullable: true
                              format: ip
                            vpc:
                              type: string
                              description: |-
                                The VPC Subnet IPv4 address for this Interface.

                                - Only allowed for `vpc` type Interfaces.
                                - Returns an empty string (`""`) for non-`vpc` type Interfaces.

                                For requests:

                                - Must not already be actively assigned as an address or within a range to any Linodes.
                                - Must not be the first two or last two addresses in the Subnet IPv4 Range.
                                - If omitted, a valid address within the Subnet IPv4 range is automatically assigned.
                              example: 10.0.0.2
                              nullable: true
                              format: ip
                        label:
                          type: string
                          description: |-
                            The name of this Interface.

                            For `vlan` purpose Interfaces:

                            - Required.
                            - Must be unique among the Linode's Interfaces (a Linode cannot be attached to the same VLAN multiple times).
                            - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                            - If the VLAN label is new, a VLAN is created. Up to 10 VLANs can be created in each data center region. To view your active VLANs, run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans) operation.

                            For `public` purpose Interfaces:

                            - In requests, must be an empty string (`""`) or `null` if included.
                            - In responses, always returns `null`.

                            For `vpc` purpose Interfaces:

                            - In requests, must be an empty string (`""`) or `null` if included.
                            - In responses, always returns `null`.
                          example: example-interface
                          nullable: true
                          minLength: 1
                          maxLength: 64
                          pattern: '[a-zA-Z0-9-]+'
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        primary:
                          type: boolean
                          description: |-
                            The primary Interface is configured as the default route to the Linode.

                            Each Configuration Profile can have up to one `"primary": true` Interface at a time.

                            Must be `false` for `vlan` type Interfaces.

                            If no Interface is configured as the primary, the first non-`vlan` type Interface in the `interfaces` array is automatically treated as the primary Interface.
                          example: true
                        purpose:
                          type: string
                          description: |-
                            The type of Interface.

                            - `public`
                              - Only one `public` Interface per Linode can be defined.
                              - The Linode's default public IPv4 address is assigned to the `public` Interface.
                              - A Linode must have a public Interface in the first/eth0 position to be reachable via the public internet upon boot without additional system configuration. If no `public` Interface is configured, the Linode is not directly reachable via the public internet. In this case, access can only be established via [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/) or other Linodes connected to the same VLAN or VPC.

                            - `vlan`
                              - Configuring a `vlan` purpose Interface attaches this Linode to the VLAN with the specified `label`.
                              - The Linode is configured to use the specified `ipam_address`, if any.

                            - `vpc`
                              - Configuring a `vpc` purpose Interface attaches this Linode to the existing VPC Subnet with the specified `subnet_id`.
                              - When the Interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC Subnet. See `ipv4.vpc` for more information.
                          example: vlan
                          enum:
                          - public
                          - vlan
                          - vpc
                          x-linode-cli-display: 3
                        subnet_id:
                          type: integer
                          description: |-
                            The `id` of the VPC Subnet for this Interface.

                            In requests, this value is used to assign a Linode to a VPC Subnet.

                            - Required for `vpc` type Interfaces.
                            - Returns `null` for non-`vpc` type Interfaces.
                            - Once a VPC Subnet is assigned to an Interface, it cannot be updated.
                            - The Linode must be rebooted with the Interface's Configuration Profile to complete assignment to a VPC Subnet.
                          example: 101
                          nullable: true
                        vpc_id:
                          type: integer
                          description: |
                            __Read-only__ The `id` of the VPC configured for this Interface. Returns
                            `null` for non-`vpc` type Interfaces.
                          example: 111
                          nullable: true
                          readOnly: true
                  kernel:
                    type: string
                    description: |-
                      A Kernel ID to boot a Linode with. Here are examples of commonly-used kernels:

                      - `linode/latest-64bit` (default): Our latest kernel at the time of instance boot/reboot.
                      - `linode/grub2`: The upstream distribution-supplied kernel that is installed on the primary disk, or a custom kernel if installed.
                      - `linode/direct-disk`: The MBR (Master Boot Record) of the primary disk/root device, used instead of a Linux kernel.

                      For a complete list of options, run the [List kernels](https://techdocs.akamai.com/linode-api/reference/get-kernels) operation.
                    example: linode/latest-64bit
                    default: linode/latest-64bit
                    x-linode-cli-display: 3
                  label:
                    type: string
                    description: |
                      The Config's label is for display purposes only.
                    example: My Config
                    minLength: 1
                    maxLength: 48
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  memory_limit:
                    type: integer
                    description: |
                      Defaults to the total RAM of the Linode.
                    example: 2048
                  root_device:
                    type: string
                    description: |-
                      The root device to boot.

                      - If no value or an invalid value is provided, root device will default to `/dev/sda`.
                      - If the device specified at the root device location is not mounted, the Linode will not boot until a device is mounted.
                    example: /dev/sda
                    pattern: a-z, A-Z, 0-9, /, _, -
                  run_level:
                    type: string
                    description: |
                      Defines the state of your Linode after booting. Defaults to `default`.
                    example: default
                    enum:
                    - default
                    - single
                    - binbash
                  virt_mode:
                    type: string
                    description: |-
                      Controls the virtualization mode. Defaults to `paravirt`.

                      - `paravirt` is suitable for most cases. Linodes running in paravirt mode share some qualities with the host, ultimately making it run faster since there is less transition between it and the host.
                      - `fullvirt` affords more customization, but is slower because 100% of the VM is virtualized.
                    example: paravirt
                    enum:
                    - paravirt
                    - fullvirt
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A Configuration profile was created.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/linode-config.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The ID of this Config.
                    example: 23456
                    readOnly: true
                    x-linode-cli-display: 1
                  comments:
                    type: string
                    description: |
                      Optional field for arbitrary User comments on this Config.
                    example: This is my main Config
                    nullable: true
                  devices:
                    type: object
                    description: |-
                      A dictionary of device disks to use as a device map in a Linode's configuration profile.

                      - An empty device disk dictionary or a dictionary with empty values for device slots is allowed.
                      - If no devices are specified, booting from this configuration will hold until a device exists that allows the boot process to start.
                    x-akamai:
                      file-path: schemas/devices.yaml
                    additionalProperties: false
                    properties:
                      sda:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sdb:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sdc:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sdd:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sde:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sdf:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sdg:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sdh:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                  helpers:
                    type: object
                    description: |
                      Helpers enabled when booting to this Linode Config.
                    additionalProperties: false
                    properties:
                      devtmpfs_automount:
                        type: boolean
                        description: |
                          Populates the /dev directory early during boot without udev.  Defaults to false.
                        example: false
                      distro:
                        type: boolean
                        description: |
                          Helps maintain correct inittab/upstart console device.
                        example: true
                      modules_dep:
                        type: boolean
                        description: |
                          Creates a modules dependency file for the Kernel you run.
                        example: true
                      network:
                        type: boolean
                        description: |
                          Automatically configures static networking.
                        example: true
                      updatedb_disabled:
                        type: boolean
                        description: |
                          Disables updatedb cron job to avoid disk thrashing.
                        example: true
                  interfaces:
                    type: array
                    description: |-
                      An array of Network Interfaces to add to this Linode's Configuration Profile. At least one and up to three Interface objects can exist in this array. The position in the array determines which of the Linode's network Interfaces is configured:

                      - First [0]:  eth0
                      - Second [1]: eth1
                      - Third [2]:  eth2

                      When updating a Linode's Interfaces, _each Interface must be redefined_. An empty `interfaces` array results in a default `public` type Interface configuration only.

                      If no public Interface is configured, public IP addresses are still assigned to the Linode but will not be usable without manual configuration.

                      __Note:__ Changes to Linode Interface configurations can be enabled by rebooting the Linode.

                      `vpc` details

                      See the [VPC documentation](https://www.linode.com/docs/products/networking/vpc/#technical-specifications) guide for its specifications and limitations.

                      `vlan` details

                      - Only Next Generation Network (NGN) data centers support VLANs. Run the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation to view the capabilities of data center regions. If a VLAN is attached to your Linode and you attempt to migrate or clone it to a non-NGN data center, the migration or cloning will not initiate. If a Linode cannot be migrated or cloned because of an incompatibility, you will be prompted to select a different data center or contact support.
                      - See the [VLANs Overview](https://www.linode.com/docs/products/networking/vlans/#technical-specifications) guide to view additional specifications and limitations.
                    example:
                    - id: 101
                      ipam_address: null
                      ipv4: null
                      label: null
                      primary: false
                      purpose: public
                      subnet_id: null
                      vpc_id: null
                    - id: 102
                      ipam_address: 10.0.0.1/24
                      ipv4:
                        nat_1_1: null
                        vpc: 10.0.0.2
                      label: vlan-1
                      primary: false
                      purpose: vlan
                      subnet_id: null
                      vpc_id: null
                    - id: 103
                      ipam_address: null
                      ipv4:
                        nat_1_1: 203.0.113.2
                        vpc: 10.0.1.2
                      label: null
                      primary: true
                      purpose: vpc
                      subnet_id: 101
                      vpc_id: 111
                    x-akamai:
                      file-path: schemas/linode-config-interfaces.yaml
                    required:
                    - purpose
                    minItems: 1
                    maxItems: 3
                    uniqueItems: true
                    items:
                      type: object
                      description: |
                        The Network Interface to apply to this Linode's configuration profile.
                      x-akamai:
                        file-path: schemas/linode-config-interface.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ The unique ID representing this Interface.
                          example: 101
                          readOnly: true
                          x-linode-cli-display: 1
                        active:
                          type: boolean
                          description: |
                            Returns `true` if the Interface is in use, meaning that Compute Instance
                            has been booted using the Configuration Profile to which
                            the Interface belongs. Otherwise returns `false`.
                          example: true
                        ip_ranges:
                          type: array
                          description: |-
                            An array of IPv4 CIDR VPC Subnet ranges that are routed to this Interface.

                            - Array items are only allowed for `vpc` type Interfaces.
                            - This must be empty for non-`vpc` type Interfaces.

                            For requests:

                            - Addresses in submitted ranges must not already be actively assigned.
                            - Submitting values replaces any existing values.
                            - Submitting an empty array removes any existing values.
                            - Omitting this property results in no change to existing values.
                          example:
                          - 10.0.0.64/26
                          - fd04:495a:691c:971c::1:0/112
                          nullable: true
                          items:
                            type: string
                            format: ip
                        ipam_address:
                          type: string
                          description: |-
                            This Network Interface's private IP address in Classless Inter-Domain Routing (CIDR) notation.

                            For `vlan` purpose Interfaces:

                            - Must be unique among the Linode's Interfaces to avoid conflicting addresses.
                            - Should be unique among devices attached to the VLAN to avoid conflict.
                            - The Linode is configured to use this address for the associated Interface upon reboot if Network Helper is enabled. If Network Helper is disabled, the address can be enabled with [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).

                            For `public` purpose Interfaces:

                            - In requests, must be an empty string (`""`) or `null` if included.
                            - In responses, always returns `null`.

                            For `vpc` purpose Interfaces:

                            - In requests, must be an empty string (`""`) or `null` if included.
                            - In responses, always returns `null`.
                          example: 10.0.0.1/24
                          nullable: true
                          format: ip/netmask
                          x-linode-cli-display: 4
                        ipv4:
                          type: object
                          description: |
                            IPv4 addresses configured for this Interface. Only allowed for `vpc`
                            type Interfaces. Returns `null` if no `vpc` Interface
                            is assigned.
                          additionalProperties: false
                          properties:
                            nat_1_1:
                              type: string
                              description: |-
                                The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the VPC Subnet IPv4 address assigned to this Interface.

                                - Only allowed for `vpc` type Interfaces.
                                - Returns `null` if no 1:1 NAT is set for a `vpc` type Interface.
                                - Returns an empty string (`""`) for non-`vpc` type Interfaces.

                                For requests:

                                - Setting this value to `any` enables the Linode's assigned public IPv4 address on this Interface and establishes a 1:1 NAT between the public IPv4 and VPC Subnet IPv4 addresses.
                                - Setting the value to a specific public IPv4 address that is assigned to the Linode enables a 1:1 NAT between that address and the VPC Subnet IPv4 address.
                                - The public IPv4 address can't be shared with another Linode.
                                - If omitted, set to `null`, or set to an empty string (`""`), no 1:1 NAT is established.

                                __Note__: When creating a new compute-instance, you can't set this to a specific IPv4 address. When a new compute instance is created, the network establishes a public IPv4 address for it. Since this address doesn't exist yet, you can't include a custom IPv4 address to change it. Once your compute instances is created, you can [update your configuration profile interface](https://www.linode.com/docs/api/linode-instances/#configuration-profile-interface-update) to change the `nat_1_1` address.
                              example: 203.0.113.2
                              nullable: true
                              format: ip
                            vpc:
                              type: string
                              description: |-
                                The VPC Subnet IPv4 address for this Interface.

                                - Only allowed for `vpc` type Interfaces.
                                - Returns an empty string (`""`) for non-`vpc` type Interfaces.

                                For requests:

                                - Must not already be actively assigned as an address or within a range to any Linodes.
                                - Must not be the first two or last two addresses in the Subnet IPv4 Range.
                                - If omitted, a valid address within the Subnet IPv4 range is automatically assigned.
                              example: 10.0.0.2
                              nullable: true
                              format: ip
                        label:
                          type: string
                          description: |-
                            The name of this Interface.

                            For `vlan` purpose Interfaces:

                            - Required.
                            - Must be unique among the Linode's Interfaces (a Linode cannot be attached to the same VLAN multiple times).
                            - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                            - If the VLAN label is new, a VLAN is created. Up to 10 VLANs can be created in each data center region. To view your active VLANs, run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans) operation.

                            For `public` purpose Interfaces:

                            - In requests, must be an empty string (`""`) or `null` if included.
                            - In responses, always returns `null`.

                            For `vpc` purpose Interfaces:

                            - In requests, must be an empty string (`""`) or `null` if included.
                            - In responses, always returns `null`.
                          example: example-interface
                          nullable: true
                          minLength: 1
                          maxLength: 64
                          pattern: '[a-zA-Z0-9-]+'
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        primary:
                          type: boolean
                          description: |-
                            The primary Interface is configured as the default route to the Linode.

                            Each Configuration Profile can have up to one `"primary": true` Interface at a time.

                            Must be `false` for `vlan` type Interfaces.

                            If no Interface is configured as the primary, the first non-`vlan` type Interface in the `interfaces` array is automatically treated as the primary Interface.
                          example: true
                        purpose:
                          type: string
                          description: |-
                            The type of Interface.

                            - `public`
                              - Only one `public` Interface per Linode can be defined.
                              - The Linode's default public IPv4 address is assigned to the `public` Interface.
                              - A Linode must have a public Interface in the first/eth0 position to be reachable via the public internet upon boot without additional system configuration. If no `public` Interface is configured, the Linode is not directly reachable via the public internet. In this case, access can only be established via [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/) or other Linodes connected to the same VLAN or VPC.

                            - `vlan`
                              - Configuring a `vlan` purpose Interface attaches this Linode to the VLAN with the specified `label`.
                              - The Linode is configured to use the specified `ipam_address`, if any.

                            - `vpc`
                              - Configuring a `vpc` purpose Interface attaches this Linode to the existing VPC Subnet with the specified `subnet_id`.
                              - When the Interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC Subnet. See `ipv4.vpc` for more information.
                          example: vlan
                          enum:
                          - public
                          - vlan
                          - vpc
                          x-linode-cli-display: 3
                        subnet_id:
                          type: integer
                          description: |-
                            The `id` of the VPC Subnet for this Interface.

                            In requests, this value is used to assign a Linode to a VPC Subnet.

                            - Required for `vpc` type Interfaces.
                            - Returns `null` for non-`vpc` type Interfaces.
                            - Once a VPC Subnet is assigned to an Interface, it cannot be updated.
                            - The Linode must be rebooted with the Interface's Configuration Profile to complete assignment to a VPC Subnet.
                          example: 101
                          nullable: true
                        vpc_id:
                          type: integer
                          description: |
                            __Read-only__ The `id` of the VPC configured for this Interface. Returns
                            `null` for non-`vpc` type Interfaces.
                          example: 111
                          nullable: true
                          readOnly: true
                  kernel:
                    type: string
                    description: |-
                      A Kernel ID to boot a Linode with. Here are examples of commonly-used kernels:

                      - `linode/latest-64bit` (default): Our latest kernel at the time of instance boot/reboot.
                      - `linode/grub2`: The upstream distribution-supplied kernel that is installed on the primary disk, or a custom kernel if installed.
                      - `linode/direct-disk`: The MBR (Master Boot Record) of the primary disk/root device, used instead of a Linux kernel.

                      For a complete list of options, run the [List kernels](https://techdocs.akamai.com/linode-api/reference/get-kernels) operation.
                    example: linode/latest-64bit
                    default: linode/latest-64bit
                    x-linode-cli-display: 3
                  label:
                    type: string
                    description: |
                      The Config's label is for display purposes only.
                    example: My Config
                    minLength: 1
                    maxLength: 48
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  memory_limit:
                    type: integer
                    description: |
                      Defaults to the total RAM of the Linode.
                    example: 2048
                  root_device:
                    type: string
                    description: |-
                      The root device to boot.

                      - If no value or an invalid value is provided, root device will default to `/dev/sda`.
                      - If the device specified at the root device location is not mounted, the Linode will not boot until a device is mounted.
                    example: /dev/sda
                    pattern: a-z, A-Z, 0-9, /, _, -
                  run_level:
                    type: string
                    description: |
                      Defines the state of your Linode after booting. Defaults to `default`.
                    example: default
                    enum:
                    - default
                    - single
                    - binbash
                  virt_mode:
                    type: string
                    description: |-
                      Controls the virtualization mode. Defaults to `paravirt`.

                      - `paravirt` is suitable for most cases. Linodes running in paravirt mode share some qualities with the host, ultimately making it run faster since there is less transition between it and the host.
                      - `fullvirt` affords more customization, but is slower because 100% of the VM is virtualized.
                    example: paravirt
                    enum:
                    - paravirt
                    - fullvirt
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-add-linode-config
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "kernel": "linode/latest-64bit",
                "comments": "This is my main Config",
                "memory_limit": 2048,
                "run_level": "default",
                "virt_mode": "paravirt",
                "interfaces": [
                  {
                    "purpose": "public",
                    "label": "",
                    "ipam_address": ""
                  },
                  {
                    "purpose": "vlan",
                    "label": "vlan-1",
                    "ipam_address": "10.0.0.1/24"
                  }
                ],
                "helpers": {
                  "updatedb_disabled": true,
                  "distro": true,
                  "modules_dep": true,
                  "network": true,
                  "devtmpfs_automount": false
                },
                "label": "My Config",
                "devices": {
                  "sda": {
                    "disk_id": 123456,
                    "volume_id": null
                  },
                  "sdb": {
                    "disk_id": 123457,
                    "volume_id": null
                  }
                }
              }' \
              https://api.linode.com/v4/linode/instances/123/configs
      - lang: CLI
        source: |-
          linode-cli linodes config-create 7590910 \
            --label "My Config" \
            --devices.sda.disk_id 123456 \
            --devices.sdb.disk_id 123457
      x-linode-cli-action: config-create
      x-original-op-id: addLinodeConfig
      x-original-op-title: Configuration Profile Create
    get:
      operationId: get-linode-configs
      summary: List config profiles
      tags:
      - Configurations
      description: |-
        Lists Configuration profiles associated with a Linode.


        ---


        - __CLI__.

            ```
            linode-cli linodes configs-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes configs-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        200:
          description: |
            Returns an array of Configuration profiles associated with this Linode.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      x-akamai:
                        file-path: schemas/linode-config.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ The ID of this Config.
                          example: 23456
                          readOnly: true
                          x-linode-cli-display: 1
                        comments:
                          type: string
                          description: |
                            Optional field for arbitrary User comments on this Config.
                          example: This is my main Config
                          nullable: true
                        devices:
                          type: object
                          description: |-
                            A dictionary of device disks to use as a device map in a Linode's configuration profile.

                            - An empty device disk dictionary or a dictionary with empty values for device slots is allowed.
                            - If no devices are specified, booting from this configuration will hold until a device exists that allows the boot process to start.
                          x-akamai:
                            file-path: schemas/devices.yaml
                          additionalProperties: false
                          properties:
                            sda:
                              type: object
                              description: |
                                Device can be either a Disk or Volume identified by `disk_id`
                                or `volume_id`. Only one type per slot allowed. Can
                                be null. Devices mapped from _sde_ through _sdh_ are
                                unavailable in `fullvirt` virt_mode.
                              x-akamai:
                                file-path: schemas/device.yaml
                              additionalProperties: false
                              properties:
                                disk_id:
                                  type: integer
                                  description: |
                                    The Disk ID, or `null` if a Volume is assigned to this
                                    slot.
                                  example: 124458
                                volume_id:
                                  type: integer
                                  description: |
                                    The Volume ID, or `null` if a Disk is assigned to this slot.
                                  example: null
                            sdb:
                              type: object
                              description: |
                                Device can be either a Disk or Volume identified by `disk_id`
                                or `volume_id`. Only one type per slot allowed. Can
                                be null. Devices mapped from _sde_ through _sdh_ are
                                unavailable in `fullvirt` virt_mode.
                              x-akamai:
                                file-path: schemas/device.yaml
                              additionalProperties: false
                              properties:
                                disk_id:
                                  type: integer
                                  description: |
                                    The Disk ID, or `null` if a Volume is assigned to this
                                    slot.
                                  example: 124458
                                volume_id:
                                  type: integer
                                  description: |
                                    The Volume ID, or `null` if a Disk is assigned to this slot.
                                  example: null
                            sdc:
                              type: object
                              description: |
                                Device can be either a Disk or Volume identified by `disk_id`
                                or `volume_id`. Only one type per slot allowed. Can
                                be null. Devices mapped from _sde_ through _sdh_ are
                                unavailable in `fullvirt` virt_mode.
                              x-akamai:
                                file-path: schemas/device.yaml
                              additionalProperties: false
                              properties:
                                disk_id:
                                  type: integer
                                  description: |
                                    The Disk ID, or `null` if a Volume is assigned to this
                                    slot.
                                  example: 124458
                                volume_id:
                                  type: integer
                                  description: |
                                    The Volume ID, or `null` if a Disk is assigned to this slot.
                                  example: null
                            sdd:
                              type: object
                              description: |
                                Device can be either a Disk or Volume identified by `disk_id`
                                or `volume_id`. Only one type per slot allowed. Can
                                be null. Devices mapped from _sde_ through _sdh_ are
                                unavailable in `fullvirt` virt_mode.
                              x-akamai:
                                file-path: schemas/device.yaml
                              additionalProperties: false
                              properties:
                                disk_id:
                                  type: integer
                                  description: |
                                    The Disk ID, or `null` if a Volume is assigned to this
                                    slot.
                                  example: 124458
                                volume_id:
                                  type: integer
                                  description: |
                                    The Volume ID, or `null` if a Disk is assigned to this slot.
                                  example: null
                            sde:
                              type: object
                              description: |
                                Device can be either a Disk or Volume identified by `disk_id`
                                or `volume_id`. Only one type per slot allowed. Can
                                be null. Devices mapped from _sde_ through _sdh_ are
                                unavailable in `fullvirt` virt_mode.
                              x-akamai:
                                file-path: schemas/device.yaml
                              additionalProperties: false
                              properties:
                                disk_id:
                                  type: integer
                                  description: |
                                    The Disk ID, or `null` if a Volume is assigned to this
                                    slot.
                                  example: 124458
                                volume_id:
                                  type: integer
                                  description: |
                                    The Volume ID, or `null` if a Disk is assigned to this slot.
                                  example: null
                            sdf:
                              type: object
                              description: |
                                Device can be either a Disk or Volume identified by `disk_id`
                                or `volume_id`. Only one type per slot allowed. Can
                                be null. Devices mapped from _sde_ through _sdh_ are
                                unavailable in `fullvirt` virt_mode.
                              x-akamai:
                                file-path: schemas/device.yaml
                              additionalProperties: false
                              properties:
                                disk_id:
                                  type: integer
                                  description: |
                                    The Disk ID, or `null` if a Volume is assigned to this
                                    slot.
                                  example: 124458
                                volume_id:
                                  type: integer
                                  description: |
                                    The Volume ID, or `null` if a Disk is assigned to this slot.
                                  example: null
                            sdg:
                              type: object
                              description: |
                                Device can be either a Disk or Volume identified by `disk_id`
                                or `volume_id`. Only one type per slot allowed. Can
                                be null. Devices mapped from _sde_ through _sdh_ are
                                unavailable in `fullvirt` virt_mode.
                              x-akamai:
                                file-path: schemas/device.yaml
                              additionalProperties: false
                              properties:
                                disk_id:
                                  type: integer
                                  description: |
                                    The Disk ID, or `null` if a Volume is assigned to this
                                    slot.
                                  example: 124458
                                volume_id:
                                  type: integer
                                  description: |
                                    The Volume ID, or `null` if a Disk is assigned to this slot.
                                  example: null
                            sdh:
                              type: object
                              description: |
                                Device can be either a Disk or Volume identified by `disk_id`
                                or `volume_id`. Only one type per slot allowed. Can
                                be null. Devices mapped from _sde_ through _sdh_ are
                                unavailable in `fullvirt` virt_mode.
                              x-akamai:
                                file-path: schemas/device.yaml
                              additionalProperties: false
                              properties:
                                disk_id:
                                  type: integer
                                  description: |
                                    The Disk ID, or `null` if a Volume is assigned to this
                                    slot.
                                  example: 124458
                                volume_id:
                                  type: integer
                                  description: |
                                    The Volume ID, or `null` if a Disk is assigned to this slot.
                                  example: null
                        helpers:
                          type: object
                          description: |
                            Helpers enabled when booting to this Linode Config.
                          additionalProperties: false
                          properties:
                            devtmpfs_automount:
                              type: boolean
                              description: |
                                Populates the /dev directory early during boot without udev.  Defaults
                                to false.
                              example: false
                            distro:
                              type: boolean
                              description: |
                                Helps maintain correct inittab/upstart console device.
                              example: true
                            modules_dep:
                              type: boolean
                              description: |
                                Creates a modules dependency file for the Kernel you run.
                              example: true
                            network:
                              type: boolean
                              description: |
                                Automatically configures static networking.
                              example: true
                            updatedb_disabled:
                              type: boolean
                              description: |
                                Disables updatedb cron job to avoid disk thrashing.
                              example: true
                        interfaces:
                          type: array
                          description: |-
                            An array of Network Interfaces to add to this Linode's Configuration Profile. At least one and up to three Interface objects can exist in this array. The position in the array determines which of the Linode's network Interfaces is configured:

                            - First [0]:  eth0
                            - Second [1]: eth1
                            - Third [2]:  eth2

                            When updating a Linode's Interfaces, _each Interface must be redefined_. An empty `interfaces` array results in a default `public` type Interface configuration only.

                            If no public Interface is configured, public IP addresses are still assigned to the Linode but will not be usable without manual configuration.

                            __Note:__ Changes to Linode Interface configurations can be enabled by rebooting the Linode.

                            `vpc` details

                            See the [VPC documentation](https://www.linode.com/docs/products/networking/vpc/#technical-specifications) guide for its specifications and limitations.

                            `vlan` details

                            - Only Next Generation Network (NGN) data centers support VLANs. Run the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation to view the capabilities of data center regions. If a VLAN is attached to your Linode and you attempt to migrate or clone it to a non-NGN data center, the migration or cloning will not initiate. If a Linode cannot be migrated or cloned because of an incompatibility, you will be prompted to select a different data center or contact support.
                            - See the [VLANs Overview](https://www.linode.com/docs/products/networking/vlans/#technical-specifications) guide to view additional specifications and limitations.
                          example:
                          - id: 101
                            ipam_address: null
                            ipv4: null
                            label: null
                            primary: false
                            purpose: public
                            subnet_id: null
                            vpc_id: null
                          - id: 102
                            ipam_address: 10.0.0.1/24
                            ipv4:
                              nat_1_1: null
                              vpc: 10.0.0.2
                            label: vlan-1
                            primary: false
                            purpose: vlan
                            subnet_id: null
                            vpc_id: null
                          - id: 103
                            ipam_address: null
                            ipv4:
                              nat_1_1: 203.0.113.2
                              vpc: 10.0.1.2
                            label: null
                            primary: true
                            purpose: vpc
                            subnet_id: 101
                            vpc_id: 111
                          x-akamai:
                            file-path: schemas/linode-config-interfaces.yaml
                          required:
                          - purpose
                          minItems: 1
                          maxItems: 3
                          uniqueItems: true
                          items:
                            type: object
                            description: |
                              The Network Interface to apply to this Linode's configuration profile.
                            x-akamai:
                              file-path: schemas/linode-config-interface.yaml
                            additionalProperties: false
                            properties:
                              id:
                                type: integer
                                description: |
                                  __Read-only__ The unique ID representing this Interface.
                                example: 101
                                readOnly: true
                                x-linode-cli-display: 1
                              active:
                                type: boolean
                                description: |
                                  Returns `true` if the Interface is in use, meaning that
                                  Compute Instance has been booted using the Configuration
                                  Profile to which the Interface belongs. Otherwise
                                  returns `false`.
                                example: true
                              ip_ranges:
                                type: array
                                description: |-
                                  An array of IPv4 CIDR VPC Subnet ranges that are routed to this Interface.

                                  - Array items are only allowed for `vpc` type Interfaces.
                                  - This must be empty for non-`vpc` type Interfaces.

                                  For requests:

                                  - Addresses in submitted ranges must not already be actively assigned.
                                  - Submitting values replaces any existing values.
                                  - Submitting an empty array removes any existing values.
                                  - Omitting this property results in no change to existing values.
                                example:
                                - 10.0.0.64/26
                                - fd04:495a:691c:971c::1:0/112
                                nullable: true
                                items:
                                  type: string
                                  format: ip
                              ipam_address:
                                type: string
                                description: |-
                                  This Network Interface's private IP address in Classless Inter-Domain Routing (CIDR) notation.

                                  For `vlan` purpose Interfaces:

                                  - Must be unique among the Linode's Interfaces to avoid conflicting addresses.
                                  - Should be unique among devices attached to the VLAN to avoid conflict.
                                  - The Linode is configured to use this address for the associated Interface upon reboot if Network Helper is enabled. If Network Helper is disabled, the address can be enabled with [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).

                                  For `public` purpose Interfaces:

                                  - In requests, must be an empty string (`""`) or `null` if included.
                                  - In responses, always returns `null`.

                                  For `vpc` purpose Interfaces:

                                  - In requests, must be an empty string (`""`) or `null` if included.
                                  - In responses, always returns `null`.
                                example: 10.0.0.1/24
                                nullable: true
                                format: ip/netmask
                                x-linode-cli-display: 4
                              ipv4:
                                type: object
                                description: |
                                  IPv4 addresses configured for this Interface. Only allowed for
                                  `vpc` type Interfaces. Returns `null` if no `vpc`
                                  Interface is assigned.
                                additionalProperties: false
                                properties:
                                  nat_1_1:
                                    type: string
                                    description: |-
                                      The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the VPC Subnet IPv4 address assigned to this Interface.

                                      - Only allowed for `vpc` type Interfaces.
                                      - Returns `null` if no 1:1 NAT is set for a `vpc` type Interface.
                                      - Returns an empty string (`""`) for non-`vpc` type Interfaces.

                                      For requests:

                                      - Setting this value to `any` enables the Linode's assigned public IPv4 address on this Interface and establishes a 1:1 NAT between the public IPv4 and VPC Subnet IPv4 addresses.
                                      - Setting the value to a specific public IPv4 address that is assigned to the Linode enables a 1:1 NAT between that address and the VPC Subnet IPv4 address.
                                      - The public IPv4 address can't be shared with another Linode.
                                      - If omitted, set to `null`, or set to an empty string (`""`), no 1:1 NAT is established.

                                      __Note__: When creating a new compute-instance, you can't set this to a specific IPv4 address. When a new compute instance is created, the network establishes a public IPv4 address for it. Since this address doesn't exist yet, you can't include a custom IPv4 address to change it. Once your compute instances is created, you can [update your configuration profile interface](https://www.linode.com/docs/api/linode-instances/#configuration-profile-interface-update) to change the `nat_1_1` address.
                                    example: 203.0.113.2
                                    nullable: true
                                    format: ip
                                  vpc:
                                    type: string
                                    description: |-
                                      The VPC Subnet IPv4 address for this Interface.

                                      - Only allowed for `vpc` type Interfaces.
                                      - Returns an empty string (`""`) for non-`vpc` type Interfaces.

                                      For requests:

                                      - Must not already be actively assigned as an address or within a range to any Linodes.
                                      - Must not be the first two or last two addresses in the Subnet IPv4 Range.
                                      - If omitted, a valid address within the Subnet IPv4 range is automatically assigned.
                                    example: 10.0.0.2
                                    nullable: true
                                    format: ip
                              label:
                                type: string
                                description: |-
                                  The name of this Interface.

                                  For `vlan` purpose Interfaces:

                                  - Required.
                                  - Must be unique among the Linode's Interfaces (a Linode cannot be attached to the same VLAN multiple times).
                                  - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                                  - If the VLAN label is new, a VLAN is created. Up to 10 VLANs can be created in each data center region. To view your active VLANs, run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans) operation.

                                  For `public` purpose Interfaces:

                                  - In requests, must be an empty string (`""`) or `null` if included.
                                  - In responses, always returns `null`.

                                  For `vpc` purpose Interfaces:

                                  - In requests, must be an empty string (`""`) or `null` if included.
                                  - In responses, always returns `null`.
                                example: example-interface
                                nullable: true
                                minLength: 1
                                maxLength: 64
                                pattern: '[a-zA-Z0-9-]+'
                                x-linode-cli-display: 2
                                x-linode-filterable: true
                              primary:
                                type: boolean
                                description: |-
                                  The primary Interface is configured as the default route to the Linode.

                                  Each Configuration Profile can have up to one `"primary": true` Interface at a time.

                                  Must be `false` for `vlan` type Interfaces.

                                  If no Interface is configured as the primary, the first non-`vlan` type Interface in the `interfaces` array is automatically treated as the primary Interface.
                                example: true
                              purpose:
                                type: string
                                description: |-
                                  The type of Interface.

                                  - `public`
                                    - Only one `public` Interface per Linode can be defined.
                                    - The Linode's default public IPv4 address is assigned to the `public` Interface.
                                    - A Linode must have a public Interface in the first/eth0 position to be reachable via the public internet upon boot without additional system configuration. If no `public` Interface is configured, the Linode is not directly reachable via the public internet. In this case, access can only be established via [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/) or other Linodes connected to the same VLAN or VPC.

                                  - `vlan`
                                    - Configuring a `vlan` purpose Interface attaches this Linode to the VLAN with the specified `label`.
                                    - The Linode is configured to use the specified `ipam_address`, if any.

                                  - `vpc`
                                    - Configuring a `vpc` purpose Interface attaches this Linode to the existing VPC Subnet with the specified `subnet_id`.
                                    - When the Interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC Subnet. See `ipv4.vpc` for more information.
                                example: vlan
                                enum:
                                - public
                                - vlan
                                - vpc
                                x-linode-cli-display: 3
                              subnet_id:
                                type: integer
                                description: |-
                                  The `id` of the VPC Subnet for this Interface.

                                  In requests, this value is used to assign a Linode to a VPC Subnet.

                                  - Required for `vpc` type Interfaces.
                                  - Returns `null` for non-`vpc` type Interfaces.
                                  - Once a VPC Subnet is assigned to an Interface, it cannot be updated.
                                  - The Linode must be rebooted with the Interface's Configuration Profile to complete assignment to a VPC Subnet.
                                example: 101
                                nullable: true
                              vpc_id:
                                type: integer
                                description: |
                                  __Read-only__ The `id` of the VPC configured for this Interface.
                                  Returns `null` for non-`vpc` type Interfaces.
                                example: 111
                                nullable: true
                                readOnly: true
                        kernel:
                          type: string
                          description: |-
                            A Kernel ID to boot a Linode with. Here are examples of commonly-used kernels:

                            - `linode/latest-64bit` (default): Our latest kernel at the time of instance boot/reboot.
                            - `linode/grub2`: The upstream distribution-supplied kernel that is installed on the primary disk, or a custom kernel if installed.
                            - `linode/direct-disk`: The MBR (Master Boot Record) of the primary disk/root device, used instead of a Linux kernel.

                            For a complete list of options, run the [List kernels](https://techdocs.akamai.com/linode-api/reference/get-kernels) operation.
                          example: linode/latest-64bit
                          default: linode/latest-64bit
                          x-linode-cli-display: 3
                        label:
                          type: string
                          description: |
                            The Config's label is for display purposes only.
                          example: My Config
                          minLength: 1
                          maxLength: 48
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        memory_limit:
                          type: integer
                          description: |
                            Defaults to the total RAM of the Linode.
                          example: 2048
                        root_device:
                          type: string
                          description: |-
                            The root device to boot.

                            - If no value or an invalid value is provided, root device will default to `/dev/sda`.
                            - If the device specified at the root device location is not mounted, the Linode will not boot until a device is mounted.
                          example: /dev/sda
                          pattern: a-z, A-Z, 0-9, /, _, -
                        run_level:
                          type: string
                          description: |
                            Defines the state of your Linode after booting. Defaults to `default`.
                          example: default
                          enum:
                          - default
                          - single
                          - binbash
                        virt_mode:
                          type: string
                          description: |-
                            Controls the virtualization mode. Defaults to `paravirt`.

                            - `paravirt` is suitable for most cases. Linodes running in paravirt mode share some qualities with the host, ultimately making it run faster since there is less transition between it and the host.
                            - `fullvirt` affords more customization, but is slower because 100% of the VM is virtualized.
                          example: paravirt
                          enum:
                          - paravirt
                          - fullvirt
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-linode-configs
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/linode/instances/123/configs
      - lang: CLI
        source: linode-cli linodes configs-list 123
      x-linode-cli-action: configs-list
      x-original-op-id: getLinodeConfigs
      x-original-op-title: Configuration Profiles List
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/configs/{configId}:
    x-akamai:
      file-path: paths/linode-config.yaml
      path-info: /linode/instances/{linodeId}/configs/{configId}
    parameters:
    - name: linodeId
      in: path
      description: |
        The `id` of the Linode.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id.yaml
      required: true
      schema:
        type: integer
    - name: configId
      in: path
      description: |
        The `id` of the Configuration Profile.
      example: '{{configId}}'
      x-akamai:
        file-path: parameters/config-id-profile.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-linode-config
      summary: Get a config profile
      tags:
      - Configurations
      description: |-
        Returns information about a specific Configuration profile.


        ---


        - __CLI__.

            ```
            linode-cli linodes config-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes config-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A Configuration profile object.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/linode-config.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The ID of this Config.
                    example: 23456
                    readOnly: true
                    x-linode-cli-display: 1
                  comments:
                    type: string
                    description: |
                      Optional field for arbitrary User comments on this Config.
                    example: This is my main Config
                    nullable: true
                  devices:
                    type: object
                    description: |-
                      A dictionary of device disks to use as a device map in a Linode's configuration profile.

                      - An empty device disk dictionary or a dictionary with empty values for device slots is allowed.
                      - If no devices are specified, booting from this configuration will hold until a device exists that allows the boot process to start.
                    x-akamai:
                      file-path: schemas/devices.yaml
                    additionalProperties: false
                    properties:
                      sda:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sdb:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sdc:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sdd:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sde:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sdf:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sdg:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sdh:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                  helpers:
                    type: object
                    description: |
                      Helpers enabled when booting to this Linode Config.
                    additionalProperties: false
                    properties:
                      devtmpfs_automount:
                        type: boolean
                        description: |
                          Populates the /dev directory early during boot without udev.  Defaults to false.
                        example: false
                      distro:
                        type: boolean
                        description: |
                          Helps maintain correct inittab/upstart console device.
                        example: true
                      modules_dep:
                        type: boolean
                        description: |
                          Creates a modules dependency file for the Kernel you run.
                        example: true
                      network:
                        type: boolean
                        description: |
                          Automatically configures static networking.
                        example: true
                      updatedb_disabled:
                        type: boolean
                        description: |
                          Disables updatedb cron job to avoid disk thrashing.
                        example: true
                  interfaces:
                    type: array
                    description: |-
                      An array of Network Interfaces to add to this Linode's Configuration Profile. At least one and up to three Interface objects can exist in this array. The position in the array determines which of the Linode's network Interfaces is configured:

                      - First [0]:  eth0
                      - Second [1]: eth1
                      - Third [2]:  eth2

                      When updating a Linode's Interfaces, _each Interface must be redefined_. An empty `interfaces` array results in a default `public` type Interface configuration only.

                      If no public Interface is configured, public IP addresses are still assigned to the Linode but will not be usable without manual configuration.

                      __Note:__ Changes to Linode Interface configurations can be enabled by rebooting the Linode.

                      `vpc` details

                      See the [VPC documentation](https://www.linode.com/docs/products/networking/vpc/#technical-specifications) guide for its specifications and limitations.

                      `vlan` details

                      - Only Next Generation Network (NGN) data centers support VLANs. Run the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation to view the capabilities of data center regions. If a VLAN is attached to your Linode and you attempt to migrate or clone it to a non-NGN data center, the migration or cloning will not initiate. If a Linode cannot be migrated or cloned because of an incompatibility, you will be prompted to select a different data center or contact support.
                      - See the [VLANs Overview](https://www.linode.com/docs/products/networking/vlans/#technical-specifications) guide to view additional specifications and limitations.
                    example:
                    - id: 101
                      ipam_address: null
                      ipv4: null
                      label: null
                      primary: false
                      purpose: public
                      subnet_id: null
                      vpc_id: null
                    - id: 102
                      ipam_address: 10.0.0.1/24
                      ipv4:
                        nat_1_1: null
                        vpc: 10.0.0.2
                      label: vlan-1
                      primary: false
                      purpose: vlan
                      subnet_id: null
                      vpc_id: null
                    - id: 103
                      ipam_address: null
                      ipv4:
                        nat_1_1: 203.0.113.2
                        vpc: 10.0.1.2
                      label: null
                      primary: true
                      purpose: vpc
                      subnet_id: 101
                      vpc_id: 111
                    x-akamai:
                      file-path: schemas/linode-config-interfaces.yaml
                    required:
                    - purpose
                    minItems: 1
                    maxItems: 3
                    uniqueItems: true
                    items:
                      type: object
                      description: |
                        The Network Interface to apply to this Linode's configuration profile.
                      x-akamai:
                        file-path: schemas/linode-config-interface.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ The unique ID representing this Interface.
                          example: 101
                          readOnly: true
                          x-linode-cli-display: 1
                        active:
                          type: boolean
                          description: |
                            Returns `true` if the Interface is in use, meaning that Compute Instance
                            has been booted using the Configuration Profile to which
                            the Interface belongs. Otherwise returns `false`.
                          example: true
                        ip_ranges:
                          type: array
                          description: |-
                            An array of IPv4 CIDR VPC Subnet ranges that are routed to this Interface.

                            - Array items are only allowed for `vpc` type Interfaces.
                            - This must be empty for non-`vpc` type Interfaces.

                            For requests:

                            - Addresses in submitted ranges must not already be actively assigned.
                            - Submitting values replaces any existing values.
                            - Submitting an empty array removes any existing values.
                            - Omitting this property results in no change to existing values.
                          example:
                          - 10.0.0.64/26
                          - fd04:495a:691c:971c::1:0/112
                          nullable: true
                          items:
                            type: string
                            format: ip
                        ipam_address:
                          type: string
                          description: |-
                            This Network Interface's private IP address in Classless Inter-Domain Routing (CIDR) notation.

                            For `vlan` purpose Interfaces:

                            - Must be unique among the Linode's Interfaces to avoid conflicting addresses.
                            - Should be unique among devices attached to the VLAN to avoid conflict.
                            - The Linode is configured to use this address for the associated Interface upon reboot if Network Helper is enabled. If Network Helper is disabled, the address can be enabled with [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).

                            For `public` purpose Interfaces:

                            - In requests, must be an empty string (`""`) or `null` if included.
                            - In responses, always returns `null`.

                            For `vpc` purpose Interfaces:

                            - In requests, must be an empty string (`""`) or `null` if included.
                            - In responses, always returns `null`.
                          example: 10.0.0.1/24
                          nullable: true
                          format: ip/netmask
                          x-linode-cli-display: 4
                        ipv4:
                          type: object
                          description: |
                            IPv4 addresses configured for this Interface. Only allowed for `vpc`
                            type Interfaces. Returns `null` if no `vpc` Interface
                            is assigned.
                          additionalProperties: false
                          properties:
                            nat_1_1:
                              type: string
                              description: |-
                                The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the VPC Subnet IPv4 address assigned to this Interface.

                                - Only allowed for `vpc` type Interfaces.
                                - Returns `null` if no 1:1 NAT is set for a `vpc` type Interface.
                                - Returns an empty string (`""`) for non-`vpc` type Interfaces.

                                For requests:

                                - Setting this value to `any` enables the Linode's assigned public IPv4 address on this Interface and establishes a 1:1 NAT between the public IPv4 and VPC Subnet IPv4 addresses.
                                - Setting the value to a specific public IPv4 address that is assigned to the Linode enables a 1:1 NAT between that address and the VPC Subnet IPv4 address.
                                - The public IPv4 address can't be shared with another Linode.
                                - If omitted, set to `null`, or set to an empty string (`""`), no 1:1 NAT is established.

                                __Note__: When creating a new compute-instance, you can't set this to a specific IPv4 address. When a new compute instance is created, the network establishes a public IPv4 address for it. Since this address doesn't exist yet, you can't include a custom IPv4 address to change it. Once your compute instances is created, you can [update your configuration profile interface](https://www.linode.com/docs/api/linode-instances/#configuration-profile-interface-update) to change the `nat_1_1` address.
                              example: 203.0.113.2
                              nullable: true
                              format: ip
                            vpc:
                              type: string
                              description: |-
                                The VPC Subnet IPv4 address for this Interface.

                                - Only allowed for `vpc` type Interfaces.
                                - Returns an empty string (`""`) for non-`vpc` type Interfaces.

                                For requests:

                                - Must not already be actively assigned as an address or within a range to any Linodes.
                                - Must not be the first two or last two addresses in the Subnet IPv4 Range.
                                - If omitted, a valid address within the Subnet IPv4 range is automatically assigned.
                              example: 10.0.0.2
                              nullable: true
                              format: ip
                        label:
                          type: string
                          description: |-
                            The name of this Interface.

                            For `vlan` purpose Interfaces:

                            - Required.
                            - Must be unique among the Linode's Interfaces (a Linode cannot be attached to the same VLAN multiple times).
                            - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                            - If the VLAN label is new, a VLAN is created. Up to 10 VLANs can be created in each data center region. To view your active VLANs, run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans) operation.

                            For `public` purpose Interfaces:

                            - In requests, must be an empty string (`""`) or `null` if included.
                            - In responses, always returns `null`.

                            For `vpc` purpose Interfaces:

                            - In requests, must be an empty string (`""`) or `null` if included.
                            - In responses, always returns `null`.
                          example: example-interface
                          nullable: true
                          minLength: 1
                          maxLength: 64
                          pattern: '[a-zA-Z0-9-]+'
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        primary:
                          type: boolean
                          description: |-
                            The primary Interface is configured as the default route to the Linode.

                            Each Configuration Profile can have up to one `"primary": true` Interface at a time.

                            Must be `false` for `vlan` type Interfaces.

                            If no Interface is configured as the primary, the first non-`vlan` type Interface in the `interfaces` array is automatically treated as the primary Interface.
                          example: true
                        purpose:
                          type: string
                          description: |-
                            The type of Interface.

                            - `public`
                              - Only one `public` Interface per Linode can be defined.
                              - The Linode's default public IPv4 address is assigned to the `public` Interface.
                              - A Linode must have a public Interface in the first/eth0 position to be reachable via the public internet upon boot without additional system configuration. If no `public` Interface is configured, the Linode is not directly reachable via the public internet. In this case, access can only be established via [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/) or other Linodes connected to the same VLAN or VPC.

                            - `vlan`
                              - Configuring a `vlan` purpose Interface attaches this Linode to the VLAN with the specified `label`.
                              - The Linode is configured to use the specified `ipam_address`, if any.

                            - `vpc`
                              - Configuring a `vpc` purpose Interface attaches this Linode to the existing VPC Subnet with the specified `subnet_id`.
                              - When the Interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC Subnet. See `ipv4.vpc` for more information.
                          example: vlan
                          enum:
                          - public
                          - vlan
                          - vpc
                          x-linode-cli-display: 3
                        subnet_id:
                          type: integer
                          description: |-
                            The `id` of the VPC Subnet for this Interface.

                            In requests, this value is used to assign a Linode to a VPC Subnet.

                            - Required for `vpc` type Interfaces.
                            - Returns `null` for non-`vpc` type Interfaces.
                            - Once a VPC Subnet is assigned to an Interface, it cannot be updated.
                            - The Linode must be rebooted with the Interface's Configuration Profile to complete assignment to a VPC Subnet.
                          example: 101
                          nullable: true
                        vpc_id:
                          type: integer
                          description: |
                            __Read-only__ The `id` of the VPC configured for this Interface. Returns
                            `null` for non-`vpc` type Interfaces.
                          example: 111
                          nullable: true
                          readOnly: true
                  kernel:
                    type: string
                    description: |-
                      A Kernel ID to boot a Linode with. Here are examples of commonly-used kernels:

                      - `linode/latest-64bit` (default): Our latest kernel at the time of instance boot/reboot.
                      - `linode/grub2`: The upstream distribution-supplied kernel that is installed on the primary disk, or a custom kernel if installed.
                      - `linode/direct-disk`: The MBR (Master Boot Record) of the primary disk/root device, used instead of a Linux kernel.

                      For a complete list of options, run the [List kernels](https://techdocs.akamai.com/linode-api/reference/get-kernels) operation.
                    example: linode/latest-64bit
                    default: linode/latest-64bit
                    x-linode-cli-display: 3
                  label:
                    type: string
                    description: |
                      The Config's label is for display purposes only.
                    example: My Config
                    minLength: 1
                    maxLength: 48
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  memory_limit:
                    type: integer
                    description: |
                      Defaults to the total RAM of the Linode.
                    example: 2048
                  root_device:
                    type: string
                    description: |-
                      The root device to boot.

                      - If no value or an invalid value is provided, root device will default to `/dev/sda`.
                      - If the device specified at the root device location is not mounted, the Linode will not boot until a device is mounted.
                    example: /dev/sda
                    pattern: a-z, A-Z, 0-9, /, _, -
                  run_level:
                    type: string
                    description: |
                      Defines the state of your Linode after booting. Defaults to `default`.
                    example: default
                    enum:
                    - default
                    - single
                    - binbash
                  virt_mode:
                    type: string
                    description: |-
                      Controls the virtualization mode. Defaults to `paravirt`.

                      - `paravirt` is suitable for most cases. Linodes running in paravirt mode share some qualities with the host, ultimately making it run faster since there is less transition between it and the host.
                      - `fullvirt` affords more customization, but is slower because 100% of the VM is virtualized.
                    example: paravirt
                    enum:
                    - paravirt
                    - fullvirt
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-linode-config
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/linode/instances/123/configs/23456
      - lang: CLI
        source: linode-cli linodes config-view 123 23456
      x-linode-cli-action: config-view
      x-linode-grant: read_only
      x-original-op-id: getLinodeConfig
      x-original-op-title: Configuration Profile View
    put:
      operationId: put-linode-config
      summary: Update a config profile
      tags:
      - Configurations
      description: |-
        Updates a Configuration profile.


        ---


        - __CLI__.

            ```
            linode-cli linodes config-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes config-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The Configuration profile parameters to modify.
        required: true
        content:
          application/json:
            schema:
              type: object
              x-akamai:
                file-path: schemas/linode-config.yaml
              additionalProperties: false
              properties:
                id:
                  type: integer
                  description: |
                    __Read-only__ The ID of this Config.
                  example: '{{id}}'
                  readOnly: true
                  x-linode-cli-display: 1
                comments:
                  type: string
                  description: |
                    Optional field for arbitrary User comments on this Config.
                  example: '{{comments}}'
                  nullable: true
                devices:
                  type: object
                  description: |-
                    A dictionary of device disks to use as a device map in a Linode's configuration profile.

                    - An empty device disk dictionary or a dictionary with empty values for device slots is allowed.
                    - If no devices are specified, booting from this configuration will hold until a device exists that allows the boot process to start.
                  x-akamai:
                    file-path: schemas/devices.yaml
                  additionalProperties: false
                  properties:
                    sda:
                      type: object
                      description: |
                        Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only
                        one type per slot allowed. Can be null. Devices mapped from
                        _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                      x-akamai:
                        file-path: schemas/device.yaml
                      additionalProperties: false
                      properties:
                        disk_id:
                          type: integer
                          description: |
                            The Disk ID, or `null` if a Volume is assigned to this slot.
                          example: 124458
                        volume_id:
                          type: integer
                          description: |
                            The Volume ID, or `null` if a Disk is assigned to this slot.
                          example: null
                    sdb:
                      type: object
                      description: |
                        Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only
                        one type per slot allowed. Can be null. Devices mapped from
                        _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                      x-akamai:
                        file-path: schemas/device.yaml
                      additionalProperties: false
                      properties:
                        disk_id:
                          type: integer
                          description: |
                            The Disk ID, or `null` if a Volume is assigned to this slot.
                          example: 124458
                        volume_id:
                          type: integer
                          description: |
                            The Volume ID, or `null` if a Disk is assigned to this slot.
                          example: null
                    sdc:
                      type: object
                      description: |
                        Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only
                        one type per slot allowed. Can be null. Devices mapped from
                        _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                      x-akamai:
                        file-path: schemas/device.yaml
                      additionalProperties: false
                      properties:
                        disk_id:
                          type: integer
                          description: |
                            The Disk ID, or `null` if a Volume is assigned to this slot.
                          example: 124458
                        volume_id:
                          type: integer
                          description: |
                            The Volume ID, or `null` if a Disk is assigned to this slot.
                          example: null
                    sdd:
                      type: object
                      description: |
                        Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only
                        one type per slot allowed. Can be null. Devices mapped from
                        _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                      x-akamai:
                        file-path: schemas/device.yaml
                      additionalProperties: false
                      properties:
                        disk_id:
                          type: integer
                          description: |
                            The Disk ID, or `null` if a Volume is assigned to this slot.
                          example: 124458
                        volume_id:
                          type: integer
                          description: |
                            The Volume ID, or `null` if a Disk is assigned to this slot.
                          example: null
                    sde:
                      type: object
                      description: |
                        Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only
                        one type per slot allowed. Can be null. Devices mapped from
                        _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                      x-akamai:
                        file-path: schemas/device.yaml
                      additionalProperties: false
                      properties:
                        disk_id:
                          type: integer
                          description: |
                            The Disk ID, or `null` if a Volume is assigned to this slot.
                          example: 124458
                        volume_id:
                          type: integer
                          description: |
                            The Volume ID, or `null` if a Disk is assigned to this slot.
                          example: null
                    sdf:
                      type: object
                      description: |
                        Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only
                        one type per slot allowed. Can be null. Devices mapped from
                        _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                      x-akamai:
                        file-path: schemas/device.yaml
                      additionalProperties: false
                      properties:
                        disk_id:
                          type: integer
                          description: |
                            The Disk ID, or `null` if a Volume is assigned to this slot.
                          example: 124458
                        volume_id:
                          type: integer
                          description: |
                            The Volume ID, or `null` if a Disk is assigned to this slot.
                          example: null
                    sdg:
                      type: object
                      description: |
                        Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only
                        one type per slot allowed. Can be null. Devices mapped from
                        _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                      x-akamai:
                        file-path: schemas/device.yaml
                      additionalProperties: false
                      properties:
                        disk_id:
                          type: integer
                          description: |
                            The Disk ID, or `null` if a Volume is assigned to this slot.
                          example: 124458
                        volume_id:
                          type: integer
                          description: |
                            The Volume ID, or `null` if a Disk is assigned to this slot.
                          example: null
                    sdh:
                      type: object
                      description: |
                        Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only
                        one type per slot allowed. Can be null. Devices mapped from
                        _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                      x-akamai:
                        file-path: schemas/device.yaml
                      additionalProperties: false
                      properties:
                        disk_id:
                          type: integer
                          description: |
                            The Disk ID, or `null` if a Volume is assigned to this slot.
                          example: 124458
                        volume_id:
                          type: integer
                          description: |
                            The Volume ID, or `null` if a Disk is assigned to this slot.
                          example: null
                helpers:
                  type: object
                  description: |
                    Helpers enabled when booting to this Linode Config.
                  additionalProperties: false
                  properties:
                    devtmpfs_automount:
                      type: boolean
                      description: |
                        Populates the /dev directory early during boot without udev.  Defaults to false.
                      example: false
                    distro:
                      type: boolean
                      description: |
                        Helps maintain correct inittab/upstart console device.
                      example: true
                    modules_dep:
                      type: boolean
                      description: |
                        Creates a modules dependency file for the Kernel you run.
                      example: true
                    network:
                      type: boolean
                      description: |
                        Automatically configures static networking.
                      example: true
                    updatedb_disabled:
                      type: boolean
                      description: |
                        Disables updatedb cron job to avoid disk thrashing.
                      example: true
                interfaces:
                  type: array
                  description: |-
                    An array of Network Interfaces to add to this Linode's Configuration Profile. At least one and up to three Interface objects can exist in this array. The position in the array determines which of the Linode's network Interfaces is configured:

                    - First [0]:  eth0
                    - Second [1]: eth1
                    - Third [2]:  eth2

                    When updating a Linode's Interfaces, _each Interface must be redefined_. An empty `interfaces` array results in a default `public` type Interface configuration only.

                    If no public Interface is configured, public IP addresses are still assigned to the Linode but will not be usable without manual configuration.

                    __Note:__ Changes to Linode Interface configurations can be enabled by rebooting the Linode.

                    `vpc` details

                    See the [VPC documentation](https://www.linode.com/docs/products/networking/vpc/#technical-specifications) guide for its specifications and limitations.

                    `vlan` details

                    - Only Next Generation Network (NGN) data centers support VLANs. Run the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation to view the capabilities of data center regions. If a VLAN is attached to your Linode and you attempt to migrate or clone it to a non-NGN data center, the migration or cloning will not initiate. If a Linode cannot be migrated or cloned because of an incompatibility, you will be prompted to select a different data center or contact support.
                    - See the [VLANs Overview](https://www.linode.com/docs/products/networking/vlans/#technical-specifications) guide to view additional specifications and limitations.
                  example:
                  - id: 101
                    ipam_address: null
                    ipv4: null
                    label: null
                    primary: false
                    purpose: public
                    subnet_id: null
                    vpc_id: null
                  - id: 102
                    ipam_address: 10.0.0.1/24
                    ipv4:
                      nat_1_1: null
                      vpc: 10.0.0.2
                    label: vlan-1
                    primary: false
                    purpose: vlan
                    subnet_id: null
                    vpc_id: null
                  - id: 103
                    ipam_address: null
                    ipv4:
                      nat_1_1: 203.0.113.2
                      vpc: 10.0.1.2
                    label: null
                    primary: true
                    purpose: vpc
                    subnet_id: 101
                    vpc_id: 111
                  x-akamai:
                    file-path: schemas/linode-config-interfaces.yaml
                  required:
                  - purpose
                  minItems: 1
                  maxItems: 3
                  uniqueItems: true
                  items:
                    type: object
                    description: |
                      The Network Interface to apply to this Linode's configuration profile.
                    x-akamai:
                      file-path: schemas/linode-config-interface.yaml
                    additionalProperties: false
                    properties:
                      id:
                        type: integer
                        description: |
                          __Read-only__ The unique ID representing this Interface.
                        example: 101
                        readOnly: true
                        x-linode-cli-display: 1
                      active:
                        type: boolean
                        description: |
                          Returns `true` if the Interface is in use, meaning that Compute Instance
                          has been booted using the Configuration Profile to which
                          the Interface belongs. Otherwise returns `false`.
                        example: true
                      ip_ranges:
                        type: array
                        description: |-
                          An array of IPv4 CIDR VPC Subnet ranges that are routed to this Interface.

                          - Array items are only allowed for `vpc` type Interfaces.
                          - This must be empty for non-`vpc` type Interfaces.

                          For requests:

                          - Addresses in submitted ranges must not already be actively assigned.
                          - Submitting values replaces any existing values.
                          - Submitting an empty array removes any existing values.
                          - Omitting this property results in no change to existing values.
                        example:
                        - 10.0.0.64/26
                        - fd04:495a:691c:971c::1:0/112
                        nullable: true
                        items:
                          type: string
                          format: ip
                      ipam_address:
                        type: string
                        description: |-
                          This Network Interface's private IP address in Classless Inter-Domain Routing (CIDR) notation.

                          For `vlan` purpose Interfaces:

                          - Must be unique among the Linode's Interfaces to avoid conflicting addresses.
                          - Should be unique among devices attached to the VLAN to avoid conflict.
                          - The Linode is configured to use this address for the associated Interface upon reboot if Network Helper is enabled. If Network Helper is disabled, the address can be enabled with [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).

                          For `public` purpose Interfaces:

                          - In requests, must be an empty string (`""`) or `null` if included.
                          - In responses, always returns `null`.

                          For `vpc` purpose Interfaces:

                          - In requests, must be an empty string (`""`) or `null` if included.
                          - In responses, always returns `null`.
                        example: 10.0.0.1/24
                        nullable: true
                        format: ip/netmask
                        x-linode-cli-display: 4
                      ipv4:
                        type: object
                        description: |
                          IPv4 addresses configured for this Interface. Only allowed for `vpc` type
                          Interfaces. Returns `null` if no `vpc` Interface is assigned.
                        additionalProperties: false
                        properties:
                          nat_1_1:
                            type: string
                            description: |-
                              The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the VPC Subnet IPv4 address assigned to this Interface.

                              - Only allowed for `vpc` type Interfaces.
                              - Returns `null` if no 1:1 NAT is set for a `vpc` type Interface.
                              - Returns an empty string (`""`) for non-`vpc` type Interfaces.

                              For requests:

                              - Setting this value to `any` enables the Linode's assigned public IPv4 address on this Interface and establishes a 1:1 NAT between the public IPv4 and VPC Subnet IPv4 addresses.
                              - Setting the value to a specific public IPv4 address that is assigned to the Linode enables a 1:1 NAT between that address and the VPC Subnet IPv4 address.
                              - The public IPv4 address can't be shared with another Linode.
                              - If omitted, set to `null`, or set to an empty string (`""`), no 1:1 NAT is established.

                              __Note__: When creating a new compute-instance, you can't set this to a specific IPv4 address. When a new compute instance is created, the network establishes a public IPv4 address for it. Since this address doesn't exist yet, you can't include a custom IPv4 address to change it. Once your compute instances is created, you can [update your configuration profile interface](https://www.linode.com/docs/api/linode-instances/#configuration-profile-interface-update) to change the `nat_1_1` address.
                            example: 203.0.113.2
                            nullable: true
                            format: ip
                          vpc:
                            type: string
                            description: |-
                              The VPC Subnet IPv4 address for this Interface.

                              - Only allowed for `vpc` type Interfaces.
                              - Returns an empty string (`""`) for non-`vpc` type Interfaces.

                              For requests:

                              - Must not already be actively assigned as an address or within a range to any Linodes.
                              - Must not be the first two or last two addresses in the Subnet IPv4 Range.
                              - If omitted, a valid address within the Subnet IPv4 range is automatically assigned.
                            example: 10.0.0.2
                            nullable: true
                            format: ip
                      label:
                        type: string
                        description: |-
                          The name of this Interface.

                          For `vlan` purpose Interfaces:

                          - Required.
                          - Must be unique among the Linode's Interfaces (a Linode cannot be attached to the same VLAN multiple times).
                          - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                          - If the VLAN label is new, a VLAN is created. Up to 10 VLANs can be created in each data center region. To view your active VLANs, run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans) operation.

                          For `public` purpose Interfaces:

                          - In requests, must be an empty string (`""`) or `null` if included.
                          - In responses, always returns `null`.

                          For `vpc` purpose Interfaces:

                          - In requests, must be an empty string (`""`) or `null` if included.
                          - In responses, always returns `null`.
                        example: example-interface
                        nullable: true
                        minLength: 1
                        maxLength: 64
                        pattern: '[a-zA-Z0-9-]+'
                        x-linode-cli-display: 2
                        x-linode-filterable: true
                      primary:
                        type: boolean
                        description: |-
                          The primary Interface is configured as the default route to the Linode.

                          Each Configuration Profile can have up to one `"primary": true` Interface at a time.

                          Must be `false` for `vlan` type Interfaces.

                          If no Interface is configured as the primary, the first non-`vlan` type Interface in the `interfaces` array is automatically treated as the primary Interface.
                        example: true
                      purpose:
                        type: string
                        description: |-
                          The type of Interface.

                          - `public`
                            - Only one `public` Interface per Linode can be defined.
                            - The Linode's default public IPv4 address is assigned to the `public` Interface.
                            - A Linode must have a public Interface in the first/eth0 position to be reachable via the public internet upon boot without additional system configuration. If no `public` Interface is configured, the Linode is not directly reachable via the public internet. In this case, access can only be established via [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/) or other Linodes connected to the same VLAN or VPC.

                          - `vlan`
                            - Configuring a `vlan` purpose Interface attaches this Linode to the VLAN with the specified `label`.
                            - The Linode is configured to use the specified `ipam_address`, if any.

                          - `vpc`
                            - Configuring a `vpc` purpose Interface attaches this Linode to the existing VPC Subnet with the specified `subnet_id`.
                            - When the Interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC Subnet. See `ipv4.vpc` for more information.
                        example: vlan
                        enum:
                        - public
                        - vlan
                        - vpc
                        x-linode-cli-display: 3
                      subnet_id:
                        type: integer
                        description: |-
                          The `id` of the VPC Subnet for this Interface.

                          In requests, this value is used to assign a Linode to a VPC Subnet.

                          - Required for `vpc` type Interfaces.
                          - Returns `null` for non-`vpc` type Interfaces.
                          - Once a VPC Subnet is assigned to an Interface, it cannot be updated.
                          - The Linode must be rebooted with the Interface's Configuration Profile to complete assignment to a VPC Subnet.
                        example: 101
                        nullable: true
                      vpc_id:
                        type: integer
                        description: |
                          __Read-only__ The `id` of the VPC configured for this Interface. Returns `null`
                          for non-`vpc` type Interfaces.
                        example: 111
                        nullable: true
                        readOnly: true
                kernel:
                  type: string
                  description: |-
                    A Kernel ID to boot a Linode with. Here are examples of commonly-used kernels:

                    - `linode/latest-64bit` (default): Our latest kernel at the time of instance boot/reboot.
                    - `linode/grub2`: The upstream distribution-supplied kernel that is installed on the primary disk, or a custom kernel if installed.
                    - `linode/direct-disk`: The MBR (Master Boot Record) of the primary disk/root device, used instead of a Linux kernel.

                    For a complete list of options, run the [List kernels](https://techdocs.akamai.com/linode-api/reference/get-kernels) operation.
                  example: '{{kernel}}'
                  default: linode/latest-64bit
                  x-linode-cli-display: 3
                label:
                  type: string
                  description: |
                    The Config's label is for display purposes only.
                  example: '{{label}}'
                  minLength: 1
                  maxLength: 48
                  x-linode-cli-display: 2
                  x-linode-filterable: true
                memory_limit:
                  type: integer
                  description: |
                    Defaults to the total RAM of the Linode.
                  example: '{{memory_limit}}'
                root_device:
                  type: string
                  description: |-
                    The root device to boot.

                    - If no value or an invalid value is provided, root device will default to `/dev/sda`.
                    - If the device specified at the root device location is not mounted, the Linode will not boot until a device is mounted.
                  example: '{{root_device}}'
                  pattern: a-z, A-Z, 0-9, /, _, -
                run_level:
                  type: string
                  description: |
                    Defines the state of your Linode after booting. Defaults to `default`.
                  example: '{{run_level}}'
                  enum:
                  - default
                  - single
                  - binbash
                virt_mode:
                  type: string
                  description: |-
                    Controls the virtualization mode. Defaults to `paravirt`.

                    - `paravirt` is suitable for most cases. Linodes running in paravirt mode share some qualities with the host, ultimately making it run faster since there is less transition between it and the host.
                    - `fullvirt` affords more customization, but is slower because 100% of the VM is virtualized.
                  example: '{{virt_mode}}'
                  enum:
                  - paravirt
                  - fullvirt
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Configuration profile successfully updated.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/linode-config.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The ID of this Config.
                    example: 23456
                    readOnly: true
                    x-linode-cli-display: 1
                  comments:
                    type: string
                    description: |
                      Optional field for arbitrary User comments on this Config.
                    example: This is my main Config
                    nullable: true
                  devices:
                    type: object
                    description: |-
                      A dictionary of device disks to use as a device map in a Linode's configuration profile.

                      - An empty device disk dictionary or a dictionary with empty values for device slots is allowed.
                      - If no devices are specified, booting from this configuration will hold until a device exists that allows the boot process to start.
                    x-akamai:
                      file-path: schemas/devices.yaml
                    additionalProperties: false
                    properties:
                      sda:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sdb:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sdc:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sdd:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sde:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sdf:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sdg:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                      sdh:
                        type: object
                        description: |
                          Device can be either a Disk or Volume identified by `disk_id` or `volume_id`.
                          Only one type per slot allowed. Can be null. Devices mapped
                          from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                        x-akamai:
                          file-path: schemas/device.yaml
                        additionalProperties: false
                        properties:
                          disk_id:
                            type: integer
                            description: |
                              The Disk ID, or `null` if a Volume is assigned to this slot.
                            example: 124458
                          volume_id:
                            type: integer
                            description: |
                              The Volume ID, or `null` if a Disk is assigned to this slot.
                            example: null
                  helpers:
                    type: object
                    description: |
                      Helpers enabled when booting to this Linode Config.
                    additionalProperties: false
                    properties:
                      devtmpfs_automount:
                        type: boolean
                        description: |
                          Populates the /dev directory early during boot without udev.  Defaults to false.
                        example: false
                      distro:
                        type: boolean
                        description: |
                          Helps maintain correct inittab/upstart console device.
                        example: true
                      modules_dep:
                        type: boolean
                        description: |
                          Creates a modules dependency file for the Kernel you run.
                        example: true
                      network:
                        type: boolean
                        description: |
                          Automatically configures static networking.
                        example: true
                      updatedb_disabled:
                        type: boolean
                        description: |
                          Disables updatedb cron job to avoid disk thrashing.
                        example: true
                  interfaces:
                    type: array
                    description: |-
                      An array of Network Interfaces to add to this Linode's Configuration Profile. At least one and up to three Interface objects can exist in this array. The position in the array determines which of the Linode's network Interfaces is configured:

                      - First [0]:  eth0
                      - Second [1]: eth1
                      - Third [2]:  eth2

                      When updating a Linode's Interfaces, _each Interface must be redefined_. An empty `interfaces` array results in a default `public` type Interface configuration only.

                      If no public Interface is configured, public IP addresses are still assigned to the Linode but will not be usable without manual configuration.

                      __Note:__ Changes to Linode Interface configurations can be enabled by rebooting the Linode.

                      `vpc` details

                      See the [VPC documentation](https://www.linode.com/docs/products/networking/vpc/#technical-specifications) guide for its specifications and limitations.

                      `vlan` details

                      - Only Next Generation Network (NGN) data centers support VLANs. Run the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation to view the capabilities of data center regions. If a VLAN is attached to your Linode and you attempt to migrate or clone it to a non-NGN data center, the migration or cloning will not initiate. If a Linode cannot be migrated or cloned because of an incompatibility, you will be prompted to select a different data center or contact support.
                      - See the [VLANs Overview](https://www.linode.com/docs/products/networking/vlans/#technical-specifications) guide to view additional specifications and limitations.
                    example:
                    - id: 101
                      ipam_address: null
                      ipv4: null
                      label: null
                      primary: false
                      purpose: public
                      subnet_id: null
                      vpc_id: null
                    - id: 102
                      ipam_address: 10.0.0.1/24
                      ipv4:
                        nat_1_1: null
                        vpc: 10.0.0.2
                      label: vlan-1
                      primary: false
                      purpose: vlan
                      subnet_id: null
                      vpc_id: null
                    - id: 103
                      ipam_address: null
                      ipv4:
                        nat_1_1: 203.0.113.2
                        vpc: 10.0.1.2
                      label: null
                      primary: true
                      purpose: vpc
                      subnet_id: 101
                      vpc_id: 111
                    x-akamai:
                      file-path: schemas/linode-config-interfaces.yaml
                    required:
                    - purpose
                    minItems: 1
                    maxItems: 3
                    uniqueItems: true
                    items:
                      type: object
                      description: |
                        The Network Interface to apply to this Linode's configuration profile.
                      x-akamai:
                        file-path: schemas/linode-config-interface.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ The unique ID representing this Interface.
                          example: 101
                          readOnly: true
                          x-linode-cli-display: 1
                        active:
                          type: boolean
                          description: |
                            Returns `true` if the Interface is in use, meaning that Compute Instance
                            has been booted using the Configuration Profile to which
                            the Interface belongs. Otherwise returns `false`.
                          example: true
                        ip_ranges:
                          type: array
                          description: |-
                            An array of IPv4 CIDR VPC Subnet ranges that are routed to this Interface.

                            - Array items are only allowed for `vpc` type Interfaces.
                            - This must be empty for non-`vpc` type Interfaces.

                            For requests:

                            - Addresses in submitted ranges must not already be actively assigned.
                            - Submitting values replaces any existing values.
                            - Submitting an empty array removes any existing values.
                            - Omitting this property results in no change to existing values.
                          example:
                          - 10.0.0.64/26
                          - fd04:495a:691c:971c::1:0/112
                          nullable: true
                          items:
                            type: string
                            format: ip
                        ipam_address:
                          type: string
                          description: |-
                            This Network Interface's private IP address in Classless Inter-Domain Routing (CIDR) notation.

                            For `vlan` purpose Interfaces:

                            - Must be unique among the Linode's Interfaces to avoid conflicting addresses.
                            - Should be unique among devices attached to the VLAN to avoid conflict.
                            - The Linode is configured to use this address for the associated Interface upon reboot if Network Helper is enabled. If Network Helper is disabled, the address can be enabled with [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).

                            For `public` purpose Interfaces:

                            - In requests, must be an empty string (`""`) or `null` if included.
                            - In responses, always returns `null`.

                            For `vpc` purpose Interfaces:

                            - In requests, must be an empty string (`""`) or `null` if included.
                            - In responses, always returns `null`.
                          example: 10.0.0.1/24
                          nullable: true
                          format: ip/netmask
                          x-linode-cli-display: 4
                        ipv4:
                          type: object
                          description: |
                            IPv4 addresses configured for this Interface. Only allowed for `vpc`
                            type Interfaces. Returns `null` if no `vpc` Interface
                            is assigned.
                          additionalProperties: false
                          properties:
                            nat_1_1:
                              type: string
                              description: |-
                                The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the VPC Subnet IPv4 address assigned to this Interface.

                                - Only allowed for `vpc` type Interfaces.
                                - Returns `null` if no 1:1 NAT is set for a `vpc` type Interface.
                                - Returns an empty string (`""`) for non-`vpc` type Interfaces.

                                For requests:

                                - Setting this value to `any` enables the Linode's assigned public IPv4 address on this Interface and establishes a 1:1 NAT between the public IPv4 and VPC Subnet IPv4 addresses.
                                - Setting the value to a specific public IPv4 address that is assigned to the Linode enables a 1:1 NAT between that address and the VPC Subnet IPv4 address.
                                - The public IPv4 address can't be shared with another Linode.
                                - If omitted, set to `null`, or set to an empty string (`""`), no 1:1 NAT is established.

                                __Note__: When creating a new compute-instance, you can't set this to a specific IPv4 address. When a new compute instance is created, the network establishes a public IPv4 address for it. Since this address doesn't exist yet, you can't include a custom IPv4 address to change it. Once your compute instances is created, you can [update your configuration profile interface](https://www.linode.com/docs/api/linode-instances/#configuration-profile-interface-update) to change the `nat_1_1` address.
                              example: 203.0.113.2
                              nullable: true
                              format: ip
                            vpc:
                              type: string
                              description: |-
                                The VPC Subnet IPv4 address for this Interface.

                                - Only allowed for `vpc` type Interfaces.
                                - Returns an empty string (`""`) for non-`vpc` type Interfaces.

                                For requests:

                                - Must not already be actively assigned as an address or within a range to any Linodes.
                                - Must not be the first two or last two addresses in the Subnet IPv4 Range.
                                - If omitted, a valid address within the Subnet IPv4 range is automatically assigned.
                              example: 10.0.0.2
                              nullable: true
                              format: ip
                        label:
                          type: string
                          description: |-
                            The name of this Interface.

                            For `vlan` purpose Interfaces:

                            - Required.
                            - Must be unique among the Linode's Interfaces (a Linode cannot be attached to the same VLAN multiple times).
                            - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                            - If the VLAN label is new, a VLAN is created. Up to 10 VLANs can be created in each data center region. To view your active VLANs, run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans) operation.

                            For `public` purpose Interfaces:

                            - In requests, must be an empty string (`""`) or `null` if included.
                            - In responses, always returns `null`.

                            For `vpc` purpose Interfaces:

                            - In requests, must be an empty string (`""`) or `null` if included.
                            - In responses, always returns `null`.
                          example: example-interface
                          nullable: true
                          minLength: 1
                          maxLength: 64
                          pattern: '[a-zA-Z0-9-]+'
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        primary:
                          type: boolean
                          description: |-
                            The primary Interface is configured as the default route to the Linode.

                            Each Configuration Profile can have up to one `"primary": true` Interface at a time.

                            Must be `false` for `vlan` type Interfaces.

                            If no Interface is configured as the primary, the first non-`vlan` type Interface in the `interfaces` array is automatically treated as the primary Interface.
                          example: true
                        purpose:
                          type: string
                          description: |-
                            The type of Interface.

                            - `public`
                              - Only one `public` Interface per Linode can be defined.
                              - The Linode's default public IPv4 address is assigned to the `public` Interface.
                              - A Linode must have a public Interface in the first/eth0 position to be reachable via the public internet upon boot without additional system configuration. If no `public` Interface is configured, the Linode is not directly reachable via the public internet. In this case, access can only be established via [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/) or other Linodes connected to the same VLAN or VPC.

                            - `vlan`
                              - Configuring a `vlan` purpose Interface attaches this Linode to the VLAN with the specified `label`.
                              - The Linode is configured to use the specified `ipam_address`, if any.

                            - `vpc`
                              - Configuring a `vpc` purpose Interface attaches this Linode to the existing VPC Subnet with the specified `subnet_id`.
                              - When the Interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC Subnet. See `ipv4.vpc` for more information.
                          example: vlan
                          enum:
                          - public
                          - vlan
                          - vpc
                          x-linode-cli-display: 3
                        subnet_id:
                          type: integer
                          description: |-
                            The `id` of the VPC Subnet for this Interface.

                            In requests, this value is used to assign a Linode to a VPC Subnet.

                            - Required for `vpc` type Interfaces.
                            - Returns `null` for non-`vpc` type Interfaces.
                            - Once a VPC Subnet is assigned to an Interface, it cannot be updated.
                            - The Linode must be rebooted with the Interface's Configuration Profile to complete assignment to a VPC Subnet.
                          example: 101
                          nullable: true
                        vpc_id:
                          type: integer
                          description: |
                            __Read-only__ The `id` of the VPC configured for this Interface. Returns
                            `null` for non-`vpc` type Interfaces.
                          example: 111
                          nullable: true
                          readOnly: true
                  kernel:
                    type: string
                    description: |-
                      A Kernel ID to boot a Linode with. Here are examples of commonly-used kernels:

                      - `linode/latest-64bit` (default): Our latest kernel at the time of instance boot/reboot.
                      - `linode/grub2`: The upstream distribution-supplied kernel that is installed on the primary disk, or a custom kernel if installed.
                      - `linode/direct-disk`: The MBR (Master Boot Record) of the primary disk/root device, used instead of a Linux kernel.

                      For a complete list of options, run the [List kernels](https://techdocs.akamai.com/linode-api/reference/get-kernels) operation.
                    example: linode/latest-64bit
                    default: linode/latest-64bit
                    x-linode-cli-display: 3
                  label:
                    type: string
                    description: |
                      The Config's label is for display purposes only.
                    example: My Config
                    minLength: 1
                    maxLength: 48
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  memory_limit:
                    type: integer
                    description: |
                      Defaults to the total RAM of the Linode.
                    example: 2048
                  root_device:
                    type: string
                    description: |-
                      The root device to boot.

                      - If no value or an invalid value is provided, root device will default to `/dev/sda`.
                      - If the device specified at the root device location is not mounted, the Linode will not boot until a device is mounted.
                    example: /dev/sda
                    pattern: a-z, A-Z, 0-9, /, _, -
                  run_level:
                    type: string
                    description: |
                      Defines the state of your Linode after booting. Defaults to `default`.
                    example: default
                    enum:
                    - default
                    - single
                    - binbash
                  virt_mode:
                    type: string
                    description: |-
                      Controls the virtualization mode. Defaults to `paravirt`.

                      - `paravirt` is suitable for most cases. Linodes running in paravirt mode share some qualities with the host, ultimately making it run faster since there is less transition between it and the host.
                      - `fullvirt` affords more customization, but is slower because 100% of the VM is virtualized.
                    example: paravirt
                    enum:
                    - paravirt
                    - fullvirt
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-linode-config
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "kernel": "linode/latest-64bit",
                "comments": "This is my main Config",
                "memory_limit": 2048,
                "run_level": "default",
                "virt_mode": "paravirt",
                "interfaces": [
                  {
                    "purpose": "public",
                    "label": "",
                    "ipam_address": ""
                  },
                  {
                    "purpose": "vlan",
                    "label": "vlan-1",
                    "ipam_address": "10.0.0.1/24"
                  }
                ],
                "helpers": {
                  "updatedb_disabled": true,
                  "distro": true,
                  "modules_dep": true,
                  "network": true,
                  "devtmpfs_automount": false
                },
                "label": "My Config",
                "devices": {
                  "sda": {
                    "disk_id": 123456,
                    "volume_id": null
                  },
                  "sdb": {
                    "disk_id": 123457,
                    "volume_id": null
                  }
                }
              }' \
              https://api.linode.com/v4/linode/instances/123/configs/23456
      - lang: CLI
        source: |-
          linode-cli linodes config-update 123 23456 \
            --kernel "linode/latest-64bit" \
            --comments "This is my main Config" \
            --memory_limit 2048 \
            --run_level default \
            --virt_mode paravirt \
            --helpers.updatedb_disabled true \
            --helpers.distro true \
            --helpers.modules_dep true \
            --helpers.network true \
            --helpers.devtmpfs_automount false \
            --label "My Config" \
            --devices.sda.disk_id 123456 \
            --devices.sdb.disk_id 123457
      x-linode-cli-action: config-update
      x-linode-grant: read_write
      x-original-op-id: updateLinodeConfig
      x-original-op-title: Configuration Profile Update
    delete:
      operationId: delete-linode-config
      summary: Delete a config profile
      tags:
      - Configurations
      description: |-
        Deletes the specified Configuration profile from the specified Linode.


        ---


        - __CLI__.

            ```
            linode-cli linodes config-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes config-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Configuration profile successfully deleted.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-linode-config
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/linode/instances/123/configs/23456
      - lang: CLI
        source: linode-cli linodes config-delete 123 23456
      x-linode-cli-action: config-delete
      x-linode-grant: read_write
      x-original-op-id: deleteLinodeConfig
      x-original-op-title: Configuration Profile Delete
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/configs/{configId}/interfaces:
    x-akamai:
      file-path: paths/interfaces.yaml
      path-info: /linode/instances/{linodeId}/configs/{configId}/interfaces
    parameters:
    - name: linodeId
      in: path
      description: |
        The `id` of the Linode.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id.yaml
      required: true
      schema:
        type: integer
    - name: configId
      in: path
      description: |
        The `id` of the Configuration Profile.
      example: '{{configId}}'
      x-akamai:
        file-path: parameters/config-id-profile.yaml
      required: true
      schema:
        type: integer
    post:
      servers:
      - url: https://api.linode.com/v4
      operationId: post-linode-config-interface
      summary: Add a configuration profile interface
      tags:
      - Configurations
      description: |-
        Creates and appends a single Interface to the end of the `interfaces` array for an existing Configuration Profile.

        - The User accessing this operation must have `read_write` grants to the Linode.
        - A successful request triggers a `linode_config_update` event.
        - If the new Interface is added with `"primary": true`, then any existing primary Interface is changed to `"primary": false`.

        Reboot the Linode with this Configuration Profile to activate an Interface that was added with this operation.


        ---


        - __CLI__.

            ```
            linode-cli linodes config-interface-add
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes config-interface-add
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The Interface to add to the Configuration Profile.
        required: true
        content:
          application/json:
            schema:
              allOf:
              - required:
                - purpose
              - type: object
                description: |
                  The Network Interface to apply to this Linode's configuration profile.
                x-akamai:
                  file-path: schemas/linode-config-interface.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID representing this Interface.
                    example: 101
                    readOnly: true
                    x-linode-cli-display: 1
                  active:
                    type: boolean
                    description: |
                      Returns `true` if the Interface is in use, meaning that Compute Instance has been
                      booted using the Configuration Profile to which the Interface
                      belongs. Otherwise returns `false`.
                    example: true
                  ip_ranges:
                    type: array
                    description: |-
                      An array of IPv4 CIDR VPC Subnet ranges that are routed to this Interface.

                      - Array items are only allowed for `vpc` type Interfaces.
                      - This must be empty for non-`vpc` type Interfaces.

                      For requests:

                      - Addresses in submitted ranges must not already be actively assigned.
                      - Submitting values replaces any existing values.
                      - Submitting an empty array removes any existing values.
                      - Omitting this property results in no change to existing values.
                    example:
                    - 10.0.0.64/26
                    - fd04:495a:691c:971c::1:0/112
                    nullable: true
                    items:
                      type: string
                      format: ip
                  ipam_address:
                    type: string
                    description: |-
                      This Network Interface's private IP address in Classless Inter-Domain Routing (CIDR) notation.

                      For `vlan` purpose Interfaces:

                      - Must be unique among the Linode's Interfaces to avoid conflicting addresses.
                      - Should be unique among devices attached to the VLAN to avoid conflict.
                      - The Linode is configured to use this address for the associated Interface upon reboot if Network Helper is enabled. If Network Helper is disabled, the address can be enabled with [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).

                      For `public` purpose Interfaces:

                      - In requests, must be an empty string (`""`) or `null` if included.
                      - In responses, always returns `null`.

                      For `vpc` purpose Interfaces:

                      - In requests, must be an empty string (`""`) or `null` if included.
                      - In responses, always returns `null`.
                    example: 10.0.0.1/24
                    nullable: true
                    format: ip/netmask
                    x-linode-cli-display: 4
                  ipv4:
                    type: object
                    description: |
                      IPv4 addresses configured for this Interface. Only allowed for `vpc` type Interfaces.
                      Returns `null` if no `vpc` Interface is assigned.
                    additionalProperties: false
                    properties:
                      nat_1_1:
                        type: string
                        description: |-
                          The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the VPC Subnet IPv4 address assigned to this Interface.

                          - Only allowed for `vpc` type Interfaces.
                          - Returns `null` if no 1:1 NAT is set for a `vpc` type Interface.
                          - Returns an empty string (`""`) for non-`vpc` type Interfaces.

                          For requests:

                          - Setting this value to `any` enables the Linode's assigned public IPv4 address on this Interface and establishes a 1:1 NAT between the public IPv4 and VPC Subnet IPv4 addresses.
                          - Setting the value to a specific public IPv4 address that is assigned to the Linode enables a 1:1 NAT between that address and the VPC Subnet IPv4 address.
                          - The public IPv4 address can't be shared with another Linode.
                          - If omitted, set to `null`, or set to an empty string (`""`), no 1:1 NAT is established.

                          __Note__: When creating a new compute-instance, you can't set this to a specific IPv4 address. When a new compute instance is created, the network establishes a public IPv4 address for it. Since this address doesn't exist yet, you can't include a custom IPv4 address to change it. Once your compute instances is created, you can [update your configuration profile interface](https://www.linode.com/docs/api/linode-instances/#configuration-profile-interface-update) to change the `nat_1_1` address.
                        example: 203.0.113.2
                        nullable: true
                        format: ip
                      vpc:
                        type: string
                        description: |-
                          The VPC Subnet IPv4 address for this Interface.

                          - Only allowed for `vpc` type Interfaces.
                          - Returns an empty string (`""`) for non-`vpc` type Interfaces.

                          For requests:

                          - Must not already be actively assigned as an address or within a range to any Linodes.
                          - Must not be the first two or last two addresses in the Subnet IPv4 Range.
                          - If omitted, a valid address within the Subnet IPv4 range is automatically assigned.
                        example: 10.0.0.2
                        nullable: true
                        format: ip
                  label:
                    type: string
                    description: |-
                      The name of this Interface.

                      For `vlan` purpose Interfaces:

                      - Required.
                      - Must be unique among the Linode's Interfaces (a Linode cannot be attached to the same VLAN multiple times).
                      - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                      - If the VLAN label is new, a VLAN is created. Up to 10 VLANs can be created in each data center region. To view your active VLANs, run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans) operation.

                      For `public` purpose Interfaces:

                      - In requests, must be an empty string (`""`) or `null` if included.
                      - In responses, always returns `null`.

                      For `vpc` purpose Interfaces:

                      - In requests, must be an empty string (`""`) or `null` if included.
                      - In responses, always returns `null`.
                    example: example-interface
                    nullable: true
                    minLength: 1
                    maxLength: 64
                    pattern: '[a-zA-Z0-9-]+'
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  primary:
                    type: boolean
                    description: |-
                      The primary Interface is configured as the default route to the Linode.

                      Each Configuration Profile can have up to one `"primary": true` Interface at a time.

                      Must be `false` for `vlan` type Interfaces.

                      If no Interface is configured as the primary, the first non-`vlan` type Interface in the `interfaces` array is automatically treated as the primary Interface.
                    example: true
                  purpose:
                    type: string
                    description: |-
                      The type of Interface.

                      - `public`
                        - Only one `public` Interface per Linode can be defined.
                        - The Linode's default public IPv4 address is assigned to the `public` Interface.
                        - A Linode must have a public Interface in the first/eth0 position to be reachable via the public internet upon boot without additional system configuration. If no `public` Interface is configured, the Linode is not directly reachable via the public internet. In this case, access can only be established via [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/) or other Linodes connected to the same VLAN or VPC.

                      - `vlan`
                        - Configuring a `vlan` purpose Interface attaches this Linode to the VLAN with the specified `label`.
                        - The Linode is configured to use the specified `ipam_address`, if any.

                      - `vpc`
                        - Configuring a `vpc` purpose Interface attaches this Linode to the existing VPC Subnet with the specified `subnet_id`.
                        - When the Interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC Subnet. See `ipv4.vpc` for more information.
                    example: vlan
                    enum:
                    - public
                    - vlan
                    - vpc
                    x-linode-cli-display: 3
                  subnet_id:
                    type: integer
                    description: |-
                      The `id` of the VPC Subnet for this Interface.

                      In requests, this value is used to assign a Linode to a VPC Subnet.

                      - Required for `vpc` type Interfaces.
                      - Returns `null` for non-`vpc` type Interfaces.
                      - Once a VPC Subnet is assigned to an Interface, it cannot be updated.
                      - The Linode must be rebooted with the Interface's Configuration Profile to complete assignment to a VPC Subnet.
                    example: 101
                    nullable: true
                  vpc_id:
                    type: integer
                    description: |
                      __Read-only__ The `id` of the VPC configured for this Interface. Returns `null` for
                      non-`vpc` type Interfaces.
                    example: 111
                    nullable: true
                    readOnly: true
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Interface successfully added.
          content:
            application/json:
              schema:
                type: object
                description: |
                  The Network Interface to apply to this Linode's configuration profile.
                x-akamai:
                  file-path: schemas/linode-config-interface.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID representing this Interface.
                    example: 101
                    readOnly: true
                    x-linode-cli-display: 1
                  active:
                    type: boolean
                    description: |
                      Returns `true` if the Interface is in use, meaning that Compute Instance has been
                      booted using the Configuration Profile to which the Interface
                      belongs. Otherwise returns `false`.
                    example: true
                  ip_ranges:
                    type: array
                    description: |-
                      An array of IPv4 CIDR VPC Subnet ranges that are routed to this Interface.

                      - Array items are only allowed for `vpc` type Interfaces.
                      - This must be empty for non-`vpc` type Interfaces.

                      For requests:

                      - Addresses in submitted ranges must not already be actively assigned.
                      - Submitting values replaces any existing values.
                      - Submitting an empty array removes any existing values.
                      - Omitting this property results in no change to existing values.
                    example:
                    - 10.0.0.64/26
                    - fd04:495a:691c:971c::1:0/112
                    nullable: true
                    items:
                      type: string
                      format: ip
                  ipam_address:
                    type: string
                    description: |-
                      This Network Interface's private IP address in Classless Inter-Domain Routing (CIDR) notation.

                      For `vlan` purpose Interfaces:

                      - Must be unique among the Linode's Interfaces to avoid conflicting addresses.
                      - Should be unique among devices attached to the VLAN to avoid conflict.
                      - The Linode is configured to use this address for the associated Interface upon reboot if Network Helper is enabled. If Network Helper is disabled, the address can be enabled with [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).

                      For `public` purpose Interfaces:

                      - In requests, must be an empty string (`""`) or `null` if included.
                      - In responses, always returns `null`.

                      For `vpc` purpose Interfaces:

                      - In requests, must be an empty string (`""`) or `null` if included.
                      - In responses, always returns `null`.
                    example: 10.0.0.1/24
                    nullable: true
                    format: ip/netmask
                    x-linode-cli-display: 4
                  ipv4:
                    type: object
                    description: |
                      IPv4 addresses configured for this Interface. Only allowed for `vpc` type Interfaces.
                      Returns `null` if no `vpc` Interface is assigned.
                    additionalProperties: false
                    properties:
                      nat_1_1:
                        type: string
                        description: |-
                          The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the VPC Subnet IPv4 address assigned to this Interface.

                          - Only allowed for `vpc` type Interfaces.
                          - Returns `null` if no 1:1 NAT is set for a `vpc` type Interface.
                          - Returns an empty string (`""`) for non-`vpc` type Interfaces.

                          For requests:

                          - Setting this value to `any` enables the Linode's assigned public IPv4 address on this Interface and establishes a 1:1 NAT between the public IPv4 and VPC Subnet IPv4 addresses.
                          - Setting the value to a specific public IPv4 address that is assigned to the Linode enables a 1:1 NAT between that address and the VPC Subnet IPv4 address.
                          - The public IPv4 address can't be shared with another Linode.
                          - If omitted, set to `null`, or set to an empty string (`""`), no 1:1 NAT is established.

                          __Note__: When creating a new compute-instance, you can't set this to a specific IPv4 address. When a new compute instance is created, the network establishes a public IPv4 address for it. Since this address doesn't exist yet, you can't include a custom IPv4 address to change it. Once your compute instances is created, you can [update your configuration profile interface](https://www.linode.com/docs/api/linode-instances/#configuration-profile-interface-update) to change the `nat_1_1` address.
                        example: 203.0.113.2
                        nullable: true
                        format: ip
                      vpc:
                        type: string
                        description: |-
                          The VPC Subnet IPv4 address for this Interface.

                          - Only allowed for `vpc` type Interfaces.
                          - Returns an empty string (`""`) for non-`vpc` type Interfaces.

                          For requests:

                          - Must not already be actively assigned as an address or within a range to any Linodes.
                          - Must not be the first two or last two addresses in the Subnet IPv4 Range.
                          - If omitted, a valid address within the Subnet IPv4 range is automatically assigned.
                        example: 10.0.0.2
                        nullable: true
                        format: ip
                  label:
                    type: string
                    description: |-
                      The name of this Interface.

                      For `vlan` purpose Interfaces:

                      - Required.
                      - Must be unique among the Linode's Interfaces (a Linode cannot be attached to the same VLAN multiple times).
                      - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                      - If the VLAN label is new, a VLAN is created. Up to 10 VLANs can be created in each data center region. To view your active VLANs, run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans) operation.

                      For `public` purpose Interfaces:

                      - In requests, must be an empty string (`""`) or `null` if included.
                      - In responses, always returns `null`.

                      For `vpc` purpose Interfaces:

                      - In requests, must be an empty string (`""`) or `null` if included.
                      - In responses, always returns `null`.
                    example: example-interface
                    nullable: true
                    minLength: 1
                    maxLength: 64
                    pattern: '[a-zA-Z0-9-]+'
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  primary:
                    type: boolean
                    description: |-
                      The primary Interface is configured as the default route to the Linode.

                      Each Configuration Profile can have up to one `"primary": true` Interface at a time.

                      Must be `false` for `vlan` type Interfaces.

                      If no Interface is configured as the primary, the first non-`vlan` type Interface in the `interfaces` array is automatically treated as the primary Interface.
                    example: true
                  purpose:
                    type: string
                    description: |-
                      The type of Interface.

                      - `public`
                        - Only one `public` Interface per Linode can be defined.
                        - The Linode's default public IPv4 address is assigned to the `public` Interface.
                        - A Linode must have a public Interface in the first/eth0 position to be reachable via the public internet upon boot without additional system configuration. If no `public` Interface is configured, the Linode is not directly reachable via the public internet. In this case, access can only be established via [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/) or other Linodes connected to the same VLAN or VPC.

                      - `vlan`
                        - Configuring a `vlan` purpose Interface attaches this Linode to the VLAN with the specified `label`.
                        - The Linode is configured to use the specified `ipam_address`, if any.

                      - `vpc`
                        - Configuring a `vpc` purpose Interface attaches this Linode to the existing VPC Subnet with the specified `subnet_id`.
                        - When the Interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC Subnet. See `ipv4.vpc` for more information.
                    example: vlan
                    enum:
                    - public
                    - vlan
                    - vpc
                    x-linode-cli-display: 3
                  subnet_id:
                    type: integer
                    description: |-
                      The `id` of the VPC Subnet for this Interface.

                      In requests, this value is used to assign a Linode to a VPC Subnet.

                      - Required for `vpc` type Interfaces.
                      - Returns `null` for non-`vpc` type Interfaces.
                      - Once a VPC Subnet is assigned to an Interface, it cannot be updated.
                      - The Linode must be rebooted with the Interface's Configuration Profile to complete assignment to a VPC Subnet.
                    example: 101
                    nullable: true
                  vpc_id:
                    type: integer
                    description: |
                      __Read-only__ The `id` of the VPC configured for this Interface. Returns `null` for
                      non-`vpc` type Interfaces.
                    example: 111
                    nullable: true
                    readOnly: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-linode-config-interface
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl https://api.linode.com/v4/linode/instances/$linodeId/configs/$configId/interfaces \
            -H "Authorization: Bearer $TOKEN" \
            -H "Content-Type: application/json" \
            -X POST -d '{
              "purpose": "vpc",
              "primary": false,
              "label": "",
              "ipam_address": "",
              "subnet_id": 101,
              "ipv4": {
                "vpc": "10.0.1.2",
                "nat_1_1": "203.0.113.2"
              }
            }'
      - lang: CLI
        source: |-
          linode-cli linodes config-interface-add $linodeId $configId \
            --purpose vpc \
            --primary false \
            --subnet_id 101 \
            --ipv4.vpc "10.0.1.2" \
            --ipv4.nat_1_1 "203.0.113.2"
      x-linode-cli-action: config-interface-add
      x-linode-grant: read_write
      x-original-op-id: addLinodeConfigInterface
      x-original-op-title: Configuration Profile Interface
        Add
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-linode-config-interfaces
      summary: List configuration profile interfaces
      tags:
      - Configurations
      description: |-
        Returns an ordered array of all Interfaces associated with this Configuration Profile.

        - The User accessing this operation must have at least `read_only` grants to the Linode.


        ---


        - __OAuth__.

            ```
            linodes:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: linodes:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            An ordered array of Interface objects.
          content:
            application/json:
              schema:
                type: array
                description: |-
                  An array of Network Interfaces to add to this Linode's Configuration Profile. At least one and up to three Interface objects can exist in this array. The position in the array determines which of the Linode's network Interfaces is configured:

                  - First [0]:  eth0
                  - Second [1]: eth1
                  - Third [2]:  eth2

                  When updating a Linode's Interfaces, _each Interface must be redefined_. An empty `interfaces` array results in a default `public` type Interface configuration only.

                  If no public Interface is configured, public IP addresses are still assigned to the Linode but will not be usable without manual configuration.

                  __Note:__ Changes to Linode Interface configurations can be enabled by rebooting the Linode.

                  `vpc` details

                  See the [VPC documentation](https://www.linode.com/docs/products/networking/vpc/#technical-specifications) guide for its specifications and limitations.

                  `vlan` details

                  - Only Next Generation Network (NGN) data centers support VLANs. Run the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation to view the capabilities of data center regions. If a VLAN is attached to your Linode and you attempt to migrate or clone it to a non-NGN data center, the migration or cloning will not initiate. If a Linode cannot be migrated or cloned because of an incompatibility, you will be prompted to select a different data center or contact support.
                  - See the [VLANs Overview](https://www.linode.com/docs/products/networking/vlans/#technical-specifications) guide to view additional specifications and limitations.
                example:
                - id: 101
                  ipam_address: null
                  ipv4: null
                  label: null
                  primary: false
                  purpose: public
                  subnet_id: null
                  vpc_id: null
                - id: 102
                  ipam_address: 10.0.0.1/24
                  ipv4:
                    nat_1_1: null
                    vpc: 10.0.0.2
                  label: vlan-1
                  primary: false
                  purpose: vlan
                  subnet_id: null
                  vpc_id: null
                - id: 103
                  ipam_address: null
                  ipv4:
                    nat_1_1: 203.0.113.2
                    vpc: 10.0.1.2
                  label: null
                  primary: true
                  purpose: vpc
                  subnet_id: 101
                  vpc_id: 111
                x-akamai:
                  file-path: schemas/linode-config-interfaces.yaml
                required:
                - purpose
                minItems: 1
                maxItems: 3
                uniqueItems: true
                items:
                  type: object
                  description: |
                    The Network Interface to apply to this Linode's configuration profile.
                  x-akamai:
                    file-path: schemas/linode-config-interface.yaml
                  additionalProperties: false
                  properties:
                    id:
                      type: integer
                      description: |
                        __Read-only__ The unique ID representing this Interface.
                      example: 101
                      readOnly: true
                      x-linode-cli-display: 1
                    active:
                      type: boolean
                      description: |
                        Returns `true` if the Interface is in use, meaning that Compute Instance has
                        been booted using the Configuration Profile to which the Interface
                        belongs. Otherwise returns `false`.
                      example: true
                    ip_ranges:
                      type: array
                      description: |-
                        An array of IPv4 CIDR VPC Subnet ranges that are routed to this Interface.

                        - Array items are only allowed for `vpc` type Interfaces.
                        - This must be empty for non-`vpc` type Interfaces.

                        For requests:

                        - Addresses in submitted ranges must not already be actively assigned.
                        - Submitting values replaces any existing values.
                        - Submitting an empty array removes any existing values.
                        - Omitting this property results in no change to existing values.
                      example:
                      - 10.0.0.64/26
                      - fd04:495a:691c:971c::1:0/112
                      nullable: true
                      items:
                        type: string
                        format: ip
                    ipam_address:
                      type: string
                      description: |-
                        This Network Interface's private IP address in Classless Inter-Domain Routing (CIDR) notation.

                        For `vlan` purpose Interfaces:

                        - Must be unique among the Linode's Interfaces to avoid conflicting addresses.
                        - Should be unique among devices attached to the VLAN to avoid conflict.
                        - The Linode is configured to use this address for the associated Interface upon reboot if Network Helper is enabled. If Network Helper is disabled, the address can be enabled with [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).

                        For `public` purpose Interfaces:

                        - In requests, must be an empty string (`""`) or `null` if included.
                        - In responses, always returns `null`.

                        For `vpc` purpose Interfaces:

                        - In requests, must be an empty string (`""`) or `null` if included.
                        - In responses, always returns `null`.
                      example: 10.0.0.1/24
                      nullable: true
                      format: ip/netmask
                      x-linode-cli-display: 4
                    ipv4:
                      type: object
                      description: |
                        IPv4 addresses configured for this Interface. Only allowed for `vpc` type Interfaces.
                        Returns `null` if no `vpc` Interface is assigned.
                      additionalProperties: false
                      properties:
                        nat_1_1:
                          type: string
                          description: |-
                            The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the VPC Subnet IPv4 address assigned to this Interface.

                            - Only allowed for `vpc` type Interfaces.
                            - Returns `null` if no 1:1 NAT is set for a `vpc` type Interface.
                            - Returns an empty string (`""`) for non-`vpc` type Interfaces.

                            For requests:

                            - Setting this value to `any` enables the Linode's assigned public IPv4 address on this Interface and establishes a 1:1 NAT between the public IPv4 and VPC Subnet IPv4 addresses.
                            - Setting the value to a specific public IPv4 address that is assigned to the Linode enables a 1:1 NAT between that address and the VPC Subnet IPv4 address.
                            - The public IPv4 address can't be shared with another Linode.
                            - If omitted, set to `null`, or set to an empty string (`""`), no 1:1 NAT is established.

                            __Note__: When creating a new compute-instance, you can't set this to a specific IPv4 address. When a new compute instance is created, the network establishes a public IPv4 address for it. Since this address doesn't exist yet, you can't include a custom IPv4 address to change it. Once your compute instances is created, you can [update your configuration profile interface](https://www.linode.com/docs/api/linode-instances/#configuration-profile-interface-update) to change the `nat_1_1` address.
                          example: 203.0.113.2
                          nullable: true
                          format: ip
                        vpc:
                          type: string
                          description: |-
                            The VPC Subnet IPv4 address for this Interface.

                            - Only allowed for `vpc` type Interfaces.
                            - Returns an empty string (`""`) for non-`vpc` type Interfaces.

                            For requests:

                            - Must not already be actively assigned as an address or within a range to any Linodes.
                            - Must not be the first two or last two addresses in the Subnet IPv4 Range.
                            - If omitted, a valid address within the Subnet IPv4 range is automatically assigned.
                          example: 10.0.0.2
                          nullable: true
                          format: ip
                    label:
                      type: string
                      description: |-
                        The name of this Interface.

                        For `vlan` purpose Interfaces:

                        - Required.
                        - Must be unique among the Linode's Interfaces (a Linode cannot be attached to the same VLAN multiple times).
                        - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                        - If the VLAN label is new, a VLAN is created. Up to 10 VLANs can be created in each data center region. To view your active VLANs, run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans) operation.

                        For `public` purpose Interfaces:

                        - In requests, must be an empty string (`""`) or `null` if included.
                        - In responses, always returns `null`.

                        For `vpc` purpose Interfaces:

                        - In requests, must be an empty string (`""`) or `null` if included.
                        - In responses, always returns `null`.
                      example: example-interface
                      nullable: true
                      minLength: 1
                      maxLength: 64
                      pattern: '[a-zA-Z0-9-]+'
                      x-linode-cli-display: 2
                      x-linode-filterable: true
                    primary:
                      type: boolean
                      description: |-
                        The primary Interface is configured as the default route to the Linode.

                        Each Configuration Profile can have up to one `"primary": true` Interface at a time.

                        Must be `false` for `vlan` type Interfaces.

                        If no Interface is configured as the primary, the first non-`vlan` type Interface in the `interfaces` array is automatically treated as the primary Interface.
                      example: true
                    purpose:
                      type: string
                      description: |-
                        The type of Interface.

                        - `public`
                          - Only one `public` Interface per Linode can be defined.
                          - The Linode's default public IPv4 address is assigned to the `public` Interface.
                          - A Linode must have a public Interface in the first/eth0 position to be reachable via the public internet upon boot without additional system configuration. If no `public` Interface is configured, the Linode is not directly reachable via the public internet. In this case, access can only be established via [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/) or other Linodes connected to the same VLAN or VPC.

                        - `vlan`
                          - Configuring a `vlan` purpose Interface attaches this Linode to the VLAN with the specified `label`.
                          - The Linode is configured to use the specified `ipam_address`, if any.

                        - `vpc`
                          - Configuring a `vpc` purpose Interface attaches this Linode to the existing VPC Subnet with the specified `subnet_id`.
                          - When the Interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC Subnet. See `ipv4.vpc` for more information.
                      example: vlan
                      enum:
                      - public
                      - vlan
                      - vpc
                      x-linode-cli-display: 3
                    subnet_id:
                      type: integer
                      description: |-
                        The `id` of the VPC Subnet for this Interface.

                        In requests, this value is used to assign a Linode to a VPC Subnet.

                        - Required for `vpc` type Interfaces.
                        - Returns `null` for non-`vpc` type Interfaces.
                        - Once a VPC Subnet is assigned to an Interface, it cannot be updated.
                        - The Linode must be rebooted with the Interface's Configuration Profile to complete assignment to a VPC Subnet.
                      example: 101
                      nullable: true
                    vpc_id:
                      type: integer
                      description: |
                        __Read-only__ The `id` of the VPC configured for this Interface. Returns `null`
                        for non-`vpc` type Interfaces.
                      example: 111
                      nullable: true
                      readOnly: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-linode-config-interfaces
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl https://api.linode.com/v4/linode/instances/$linodeId/configs/$configId/interfaces \
            -H "Authorization: Bearer $TOKEN"
      - lang: CLI
        source: linode-cli linodes config-interfaces-list $linodeId $configId
      x-linode-cli-action:
      - config-interfaces-list
      - config-interfaces-ls
      x-linode-grant: read_only
      x-original-op-id: getLinodeConfigInterfaces
      x-original-op-title: Configuration Profile Interfaces
        List
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/configs/{configId}/interfaces/order:
    x-akamai:
      file-path: paths/order.yaml
      path-info: /linode/instances/{linodeId}/configs/{configId}/interfaces/order
    parameters:
    - name: linodeId
      in: path
      description: |
        The `id` of the Linode.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id.yaml
      required: true
      schema:
        type: integer
    - name: configId
      in: path
      description: |
        The `id` of the Configuration Profile.
      example: '{{configId}}'
      x-akamai:
        file-path: parameters/config-id-profile.yaml
      required: true
      schema:
        type: integer
    post:
      servers:
      - url: https://api.linode.com/v4
      operationId: post-linode-config-interfaces
      summary: Reorder configuration profile interfaces
      tags:
      - Configurations
      description: |-
        Reorders the existing Interfaces of a Configuration Profile.

        - The User accessing this operation must have `read_write` grants to the Linode.


        ---


        - __CLI__.

            ```
            linode-cli linodes config-interfaces-order
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes config-interfaces-order
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The desired Interface order for the Configuration Profile.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                Linode Configuration Interfaces Order request object.
              additionalProperties: false
              required:
              - ids
              properties:
                ids:
                  type: array
                  description: |-
                    An ordered array of existing Configuration Profile Interface `id`s.

                    - All current Interface `id`s must be present in the array.
                    - If the Configuration Profile contains Interfaces and is active on the Linode, the Linode must first be shut down prior to running this operation.
                    - Reordering takes effect after rebooting the Linode with this Configuration Profile.

                    The position in the array determines which of the Linode's network Interfaces is configured:

                    - First [0]:  eth0
                    - Second [1]: eth1
                    - Third [2]:  eth2
                  items:
                    type: integer
                    description: |
                      __Read-only__ The unique ID representing this Interface.
                    example: 101
                    readOnly: true
                    x-linode-cli-display: 1
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Interfaces successfully reordered.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-linode-config-interfaces
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl https://api.linode.com/v4/linode/instances/$linodeId/configs/$configId/interfaces/order \
            -H "Authorization: Bearer $TOKEN" \
            -H "Content-Type: application/json" \
            -X POST -d '{
              "ids": [
                101,
                102,
                103
              ]
            }'
      - lang: CLI
        source: |-
          linode-cli linodes config-interfaces-order $linodeId $configId \
            --ids 101 --ids 102 --ids 103
      x-linode-cli-action: config-interfaces-order
      x-linode-grant: read_write
      x-original-op-id: orderLinodeConfigInterfaces
      x-original-op-title: Configuration Profile Interfaces
        Order
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/configs/{configId}/interfaces/{interfaceId}:
    x-akamai:
      file-path: paths/interface.yaml
      path-info: /linode/instances/{linodeId}/configs/{configId}/interfaces/{interfaceId}
    parameters:
    - name: linodeId
      in: path
      description: |
        The `id` of the Linode.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id.yaml
      required: true
      schema:
        type: integer
    - name: configId
      in: path
      description: |
        The `id` of the Configuration Profile.
      example: '{{configId}}'
      x-akamai:
        file-path: parameters/config-id-profile.yaml
      required: true
      schema:
        type: integer
    - name: interfaceId
      in: path
      description: |
        The `id` of the Linode Configuration Profile Interface.
      example: '{{interfaceId}}'
      x-akamai:
        file-path: parameters/interface-id-path.yaml
      required: true
      schema:
        type: integer
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-linode-config-interface
      summary: Get a configuration profile interface
      tags:
      - Interfaces
      description: |-
        Returns a single Configuration Profile Interface.

        - The User accessing this operation must have at least `read_only` grants to the Linode.


        ---


        - __OAuth__.

            ```
            linodes:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: linodes:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            An Interface object.
          content:
            application/json:
              schema:
                type: object
                description: |
                  The Network Interface to apply to this Linode's configuration profile.
                x-akamai:
                  file-path: schemas/linode-config-interface.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID representing this Interface.
                    example: 101
                    readOnly: true
                    x-linode-cli-display: 1
                  active:
                    type: boolean
                    description: |
                      Returns `true` if the Interface is in use, meaning that Compute Instance has been
                      booted using the Configuration Profile to which the Interface
                      belongs. Otherwise returns `false`.
                    example: true
                  ip_ranges:
                    type: array
                    description: |-
                      An array of IPv4 CIDR VPC Subnet ranges that are routed to this Interface.

                      - Array items are only allowed for `vpc` type Interfaces.
                      - This must be empty for non-`vpc` type Interfaces.

                      For requests:

                      - Addresses in submitted ranges must not already be actively assigned.
                      - Submitting values replaces any existing values.
                      - Submitting an empty array removes any existing values.
                      - Omitting this property results in no change to existing values.
                    example:
                    - 10.0.0.64/26
                    - fd04:495a:691c:971c::1:0/112
                    nullable: true
                    items:
                      type: string
                      format: ip
                  ipam_address:
                    type: string
                    description: |-
                      This Network Interface's private IP address in Classless Inter-Domain Routing (CIDR) notation.

                      For `vlan` purpose Interfaces:

                      - Must be unique among the Linode's Interfaces to avoid conflicting addresses.
                      - Should be unique among devices attached to the VLAN to avoid conflict.
                      - The Linode is configured to use this address for the associated Interface upon reboot if Network Helper is enabled. If Network Helper is disabled, the address can be enabled with [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).

                      For `public` purpose Interfaces:

                      - In requests, must be an empty string (`""`) or `null` if included.
                      - In responses, always returns `null`.

                      For `vpc` purpose Interfaces:

                      - In requests, must be an empty string (`""`) or `null` if included.
                      - In responses, always returns `null`.
                    example: 10.0.0.1/24
                    nullable: true
                    format: ip/netmask
                    x-linode-cli-display: 4
                  ipv4:
                    type: object
                    description: |
                      IPv4 addresses configured for this Interface. Only allowed for `vpc` type Interfaces.
                      Returns `null` if no `vpc` Interface is assigned.
                    additionalProperties: false
                    properties:
                      nat_1_1:
                        type: string
                        description: |-
                          The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the VPC Subnet IPv4 address assigned to this Interface.

                          - Only allowed for `vpc` type Interfaces.
                          - Returns `null` if no 1:1 NAT is set for a `vpc` type Interface.
                          - Returns an empty string (`""`) for non-`vpc` type Interfaces.

                          For requests:

                          - Setting this value to `any` enables the Linode's assigned public IPv4 address on this Interface and establishes a 1:1 NAT between the public IPv4 and VPC Subnet IPv4 addresses.
                          - Setting the value to a specific public IPv4 address that is assigned to the Linode enables a 1:1 NAT between that address and the VPC Subnet IPv4 address.
                          - The public IPv4 address can't be shared with another Linode.
                          - If omitted, set to `null`, or set to an empty string (`""`), no 1:1 NAT is established.

                          __Note__: When creating a new compute-instance, you can't set this to a specific IPv4 address. When a new compute instance is created, the network establishes a public IPv4 address for it. Since this address doesn't exist yet, you can't include a custom IPv4 address to change it. Once your compute instances is created, you can [update your configuration profile interface](https://www.linode.com/docs/api/linode-instances/#configuration-profile-interface-update) to change the `nat_1_1` address.
                        example: 203.0.113.2
                        nullable: true
                        format: ip
                      vpc:
                        type: string
                        description: |-
                          The VPC Subnet IPv4 address for this Interface.

                          - Only allowed for `vpc` type Interfaces.
                          - Returns an empty string (`""`) for non-`vpc` type Interfaces.

                          For requests:

                          - Must not already be actively assigned as an address or within a range to any Linodes.
                          - Must not be the first two or last two addresses in the Subnet IPv4 Range.
                          - If omitted, a valid address within the Subnet IPv4 range is automatically assigned.
                        example: 10.0.0.2
                        nullable: true
                        format: ip
                  label:
                    type: string
                    description: |-
                      The name of this Interface.

                      For `vlan` purpose Interfaces:

                      - Required.
                      - Must be unique among the Linode's Interfaces (a Linode cannot be attached to the same VLAN multiple times).
                      - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                      - If the VLAN label is new, a VLAN is created. Up to 10 VLANs can be created in each data center region. To view your active VLANs, run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans) operation.

                      For `public` purpose Interfaces:

                      - In requests, must be an empty string (`""`) or `null` if included.
                      - In responses, always returns `null`.

                      For `vpc` purpose Interfaces:

                      - In requests, must be an empty string (`""`) or `null` if included.
                      - In responses, always returns `null`.
                    example: example-interface
                    nullable: true
                    minLength: 1
                    maxLength: 64
                    pattern: '[a-zA-Z0-9-]+'
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  primary:
                    type: boolean
                    description: |-
                      The primary Interface is configured as the default route to the Linode.

                      Each Configuration Profile can have up to one `"primary": true` Interface at a time.

                      Must be `false` for `vlan` type Interfaces.

                      If no Interface is configured as the primary, the first non-`vlan` type Interface in the `interfaces` array is automatically treated as the primary Interface.
                    example: true
                  purpose:
                    type: string
                    description: |-
                      The type of Interface.

                      - `public`
                        - Only one `public` Interface per Linode can be defined.
                        - The Linode's default public IPv4 address is assigned to the `public` Interface.
                        - A Linode must have a public Interface in the first/eth0 position to be reachable via the public internet upon boot without additional system configuration. If no `public` Interface is configured, the Linode is not directly reachable via the public internet. In this case, access can only be established via [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/) or other Linodes connected to the same VLAN or VPC.

                      - `vlan`
                        - Configuring a `vlan` purpose Interface attaches this Linode to the VLAN with the specified `label`.
                        - The Linode is configured to use the specified `ipam_address`, if any.

                      - `vpc`
                        - Configuring a `vpc` purpose Interface attaches this Linode to the existing VPC Subnet with the specified `subnet_id`.
                        - When the Interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC Subnet. See `ipv4.vpc` for more information.
                    example: vlan
                    enum:
                    - public
                    - vlan
                    - vpc
                    x-linode-cli-display: 3
                  subnet_id:
                    type: integer
                    description: |-
                      The `id` of the VPC Subnet for this Interface.

                      In requests, this value is used to assign a Linode to a VPC Subnet.

                      - Required for `vpc` type Interfaces.
                      - Returns `null` for non-`vpc` type Interfaces.
                      - Once a VPC Subnet is assigned to an Interface, it cannot be updated.
                      - The Linode must be rebooted with the Interface's Configuration Profile to complete assignment to a VPC Subnet.
                    example: 101
                    nullable: true
                  vpc_id:
                    type: integer
                    description: |
                      __Read-only__ The `id` of the VPC configured for this Interface. Returns `null` for
                      non-`vpc` type Interfaces.
                    example: 111
                    nullable: true
                    readOnly: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-linode-config-interface
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl https://api.linode.com/v4/linode/instances/$linodeId/configs/$configId/interfaces/$interfaceId \
            -H "Authorization: Bearer $TOKEN"
      - lang: CLI
        source: linode-cli linodes config-interface-view $linodeId $configId $interfaceId
      x-linode-cli-action:
      - config-interface-view
      x-linode-grant: read_only
      x-original-op-id: getLinodeConfigInterface
      x-original-op-title: Configuration Profile Interface
        View
    put:
      servers:
      - url: https://api.linode.com/v4
      operationId: put-linode-config-interface
      summary: Update a configuration profile interface
      tags:
      - Interfaces
      description: |-
        Updates a `vpc` or `public` purpose Interface for this Configuration Profile.

        - The User accessing this operation must have `read_write` grants to the Linode.
        - A successful request triggers a `linode_config_update` event.
        - The Interface `purpose` cannot be updated with this operation.
        - VPC Subnets cannot be updated on an Interface. A new `vpc` purpose Interface must be created to assign a different Subnet to a Configuration Profile.
        - Only `primary` can be updated for `public` purpose Interfaces.
        - This operation not currently allowed for `vlan` purpose Interfaces.


        ---


        - __CLI__.

            ```
            linode-cli linodes config-interface-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes config-interface-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The updated Interface.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                Linode Configuration Interface Update request object.
              additionalProperties: false
              properties:
                ip_ranges:
                  type: array
                  description: |-
                    An array of IPv4 CIDR VPC Subnet ranges that are routed to this Interface.

                    - Array items are only allowed for `vpc` type Interfaces.
                    - This must be empty for non-`vpc` type Interfaces.

                    For requests:

                    - Addresses in submitted ranges must not already be actively assigned.
                    - Submitting values replaces any existing values.
                    - Submitting an empty array removes any existing values.
                    - Omitting this property results in no change to existing values.
                  example:
                  - 10.0.0.64/26
                  - fd04:495a:691c:971c::1:0/112
                  nullable: true
                  items:
                    type: string
                    format: ip
                ipv4:
                  type: object
                  description: |
                    IPv4 addresses configured for this Interface. Only allowed for `vpc` type Interfaces. Returns
                    `null` if no `vpc` Interface is assigned.
                  additionalProperties: false
                  properties:
                    nat_1_1:
                      type: string
                      description: |-
                        The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the VPC Subnet IPv4 address assigned to this Interface.

                        - Only allowed for `vpc` type Interfaces.
                        - Returns `null` if no 1:1 NAT is set for a `vpc` type Interface.
                        - Returns an empty string (`""`) for non-`vpc` type Interfaces.

                        For requests:

                        - Setting this value to `any` enables the Linode's assigned public IPv4 address on this Interface and establishes a 1:1 NAT between the public IPv4 and VPC Subnet IPv4 addresses.
                        - Setting the value to a specific public IPv4 address that is assigned to the Linode enables a 1:1 NAT between that address and the VPC Subnet IPv4 address.
                        - The public IPv4 address can't be shared with another Linode.
                        - If omitted, set to `null`, or set to an empty string (`""`), no 1:1 NAT is established.

                        __Note__: When creating a new compute-instance, you can't set this to a specific IPv4 address. When a new compute instance is created, the network establishes a public IPv4 address for it. Since this address doesn't exist yet, you can't include a custom IPv4 address to change it. Once your compute instances is created, you can [update your configuration profile interface](https://www.linode.com/docs/api/linode-instances/#configuration-profile-interface-update) to change the `nat_1_1` address.
                      example: 203.0.113.2
                      nullable: true
                      format: ip
                    vpc:
                      type: string
                      description: |-
                        The VPC Subnet IPv4 address for this Interface.

                        - Only allowed for `vpc` type Interfaces.
                        - Returns an empty string (`""`) for non-`vpc` type Interfaces.

                        For requests:

                        - Must not already be actively assigned as an address or within a range to any Linodes.
                        - Must not be the first two or last two addresses in the Subnet IPv4 Range.
                        - If omitted, a valid address within the Subnet IPv4 range is automatically assigned.
                      example: 10.0.0.2
                      nullable: true
                      format: ip
                primary:
                  type: boolean
                  description: |-
                    The primary Interface is configured as the default route to the Linode.

                    Each Configuration Profile can have up to one `"primary": true` Interface at a time.

                    Must be `false` for `vlan` type Interfaces.

                    If no Interface is configured as the primary, the first non-`vlan` type Interface in the `interfaces` array is automatically treated as the primary Interface.
                  example: '{{primary}}'
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Interface successfully updated.
          content:
            application/json:
              schema:
                type: object
                description: |
                  The Network Interface to apply to this Linode's configuration profile.
                x-akamai:
                  file-path: schemas/linode-config-interface.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID representing this Interface.
                    example: 101
                    readOnly: true
                    x-linode-cli-display: 1
                  active:
                    type: boolean
                    description: |
                      Returns `true` if the Interface is in use, meaning that Compute Instance has been
                      booted using the Configuration Profile to which the Interface
                      belongs. Otherwise returns `false`.
                    example: true
                  ip_ranges:
                    type: array
                    description: |-
                      An array of IPv4 CIDR VPC Subnet ranges that are routed to this Interface.

                      - Array items are only allowed for `vpc` type Interfaces.
                      - This must be empty for non-`vpc` type Interfaces.

                      For requests:

                      - Addresses in submitted ranges must not already be actively assigned.
                      - Submitting values replaces any existing values.
                      - Submitting an empty array removes any existing values.
                      - Omitting this property results in no change to existing values.
                    example:
                    - 10.0.0.64/26
                    - fd04:495a:691c:971c::1:0/112
                    nullable: true
                    items:
                      type: string
                      format: ip
                  ipam_address:
                    type: string
                    description: |-
                      This Network Interface's private IP address in Classless Inter-Domain Routing (CIDR) notation.

                      For `vlan` purpose Interfaces:

                      - Must be unique among the Linode's Interfaces to avoid conflicting addresses.
                      - Should be unique among devices attached to the VLAN to avoid conflict.
                      - The Linode is configured to use this address for the associated Interface upon reboot if Network Helper is enabled. If Network Helper is disabled, the address can be enabled with [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).

                      For `public` purpose Interfaces:

                      - In requests, must be an empty string (`""`) or `null` if included.
                      - In responses, always returns `null`.

                      For `vpc` purpose Interfaces:

                      - In requests, must be an empty string (`""`) or `null` if included.
                      - In responses, always returns `null`.
                    example: 10.0.0.1/24
                    nullable: true
                    format: ip/netmask
                    x-linode-cli-display: 4
                  ipv4:
                    type: object
                    description: |
                      IPv4 addresses configured for this Interface. Only allowed for `vpc` type Interfaces.
                      Returns `null` if no `vpc` Interface is assigned.
                    additionalProperties: false
                    properties:
                      nat_1_1:
                        type: string
                        description: |-
                          The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the VPC Subnet IPv4 address assigned to this Interface.

                          - Only allowed for `vpc` type Interfaces.
                          - Returns `null` if no 1:1 NAT is set for a `vpc` type Interface.
                          - Returns an empty string (`""`) for non-`vpc` type Interfaces.

                          For requests:

                          - Setting this value to `any` enables the Linode's assigned public IPv4 address on this Interface and establishes a 1:1 NAT between the public IPv4 and VPC Subnet IPv4 addresses.
                          - Setting the value to a specific public IPv4 address that is assigned to the Linode enables a 1:1 NAT between that address and the VPC Subnet IPv4 address.
                          - The public IPv4 address can't be shared with another Linode.
                          - If omitted, set to `null`, or set to an empty string (`""`), no 1:1 NAT is established.

                          __Note__: When creating a new compute-instance, you can't set this to a specific IPv4 address. When a new compute instance is created, the network establishes a public IPv4 address for it. Since this address doesn't exist yet, you can't include a custom IPv4 address to change it. Once your compute instances is created, you can [update your configuration profile interface](https://www.linode.com/docs/api/linode-instances/#configuration-profile-interface-update) to change the `nat_1_1` address.
                        example: 203.0.113.2
                        nullable: true
                        format: ip
                      vpc:
                        type: string
                        description: |-
                          The VPC Subnet IPv4 address for this Interface.

                          - Only allowed for `vpc` type Interfaces.
                          - Returns an empty string (`""`) for non-`vpc` type Interfaces.

                          For requests:

                          - Must not already be actively assigned as an address or within a range to any Linodes.
                          - Must not be the first two or last two addresses in the Subnet IPv4 Range.
                          - If omitted, a valid address within the Subnet IPv4 range is automatically assigned.
                        example: 10.0.0.2
                        nullable: true
                        format: ip
                  label:
                    type: string
                    description: |-
                      The name of this Interface.

                      For `vlan` purpose Interfaces:

                      - Required.
                      - Must be unique among the Linode's Interfaces (a Linode cannot be attached to the same VLAN multiple times).
                      - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                      - If the VLAN label is new, a VLAN is created. Up to 10 VLANs can be created in each data center region. To view your active VLANs, run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans) operation.

                      For `public` purpose Interfaces:

                      - In requests, must be an empty string (`""`) or `null` if included.
                      - In responses, always returns `null`.

                      For `vpc` purpose Interfaces:

                      - In requests, must be an empty string (`""`) or `null` if included.
                      - In responses, always returns `null`.
                    example: example-interface
                    nullable: true
                    minLength: 1
                    maxLength: 64
                    pattern: '[a-zA-Z0-9-]+'
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  primary:
                    type: boolean
                    description: |-
                      The primary Interface is configured as the default route to the Linode.

                      Each Configuration Profile can have up to one `"primary": true` Interface at a time.

                      Must be `false` for `vlan` type Interfaces.

                      If no Interface is configured as the primary, the first non-`vlan` type Interface in the `interfaces` array is automatically treated as the primary Interface.
                    example: true
                  purpose:
                    type: string
                    description: |-
                      The type of Interface.

                      - `public`
                        - Only one `public` Interface per Linode can be defined.
                        - The Linode's default public IPv4 address is assigned to the `public` Interface.
                        - A Linode must have a public Interface in the first/eth0 position to be reachable via the public internet upon boot without additional system configuration. If no `public` Interface is configured, the Linode is not directly reachable via the public internet. In this case, access can only be established via [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/) or other Linodes connected to the same VLAN or VPC.

                      - `vlan`
                        - Configuring a `vlan` purpose Interface attaches this Linode to the VLAN with the specified `label`.
                        - The Linode is configured to use the specified `ipam_address`, if any.

                      - `vpc`
                        - Configuring a `vpc` purpose Interface attaches this Linode to the existing VPC Subnet with the specified `subnet_id`.
                        - When the Interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC Subnet. See `ipv4.vpc` for more information.
                    example: vlan
                    enum:
                    - public
                    - vlan
                    - vpc
                    x-linode-cli-display: 3
                  subnet_id:
                    type: integer
                    description: |-
                      The `id` of the VPC Subnet for this Interface.

                      In requests, this value is used to assign a Linode to a VPC Subnet.

                      - Required for `vpc` type Interfaces.
                      - Returns `null` for non-`vpc` type Interfaces.
                      - Once a VPC Subnet is assigned to an Interface, it cannot be updated.
                      - The Linode must be rebooted with the Interface's Configuration Profile to complete assignment to a VPC Subnet.
                    example: 101
                    nullable: true
                  vpc_id:
                    type: integer
                    description: |
                      __Read-only__ The `id` of the VPC configured for this Interface. Returns `null` for
                      non-`vpc` type Interfaces.
                    example: 111
                    nullable: true
                    readOnly: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-linode-config-interface
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl https://api.linode.com/v4/linode/instances/$linodeId/configs/$configId/interfaces/$interfaceId \
            -H "Authorization: Bearer $TOKEN" \
            -H "Content-Type: application/json" \
            -X PUT -d '{
              "primary": true,
              "ipv4": {
                "vpc": "10.0.1.2",
                "nat_1_1": "203.0.113.2"
              }
            }'
      - lang: CLI
        source: |-
          linode-cli linodes config-interface-update $linodeId $configId $interfaceId \
            --primary true \
            --ipv4.vpc "10.0.1.2" \
            --ipv4.nat_1_1 "203.0.113.2"
      x-linode-cli-action: config-interface-update
      x-linode-grant: read_write
      x-original-op-id: updateLinodeConfigInterface
      x-original-op-title: Configuration Profile Interface
        Update
    delete:
      servers:
      - url: https://api.linode.com/v4
      operationId: delete-linode-config-interface
      summary: Delete a configuration profile interface
      tags:
      - Interfaces
      description: |-
        Deletes an Interface from the Configuration Profile.

        - The User accessing this operation must have `read_write` grants to the Linode.
        - A successful request triggers a `linode_config_update` event.
        - Active Interfaces cannot be deleted. The associated Linode must first be shut down (or restarted using another Configuration Profile) before such Interfaces can be deleted from a Configuration Profile.


        ---


        - __CLI__.

            ```
            linode-cli linodes config-interface-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes config-interface-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Interface successfully deleted.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-linode-config-interface
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/linode/instances/$linodeId/configs/$configId/interfaces/$interfaceId
      - lang: CLI
        source: linode-cli linodes config-delete $linodeId $configId $interfaceId
      x-linode-cli-action: config-interface-delete
      x-linode-grant: read_write
      x-original-op-id: deleteLinodeConfigInterface
      x-original-op-title: Configuration Profile Interface
        Delete
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/disks:
    x-akamai:
      file-path: paths/disks.yaml
      path-info: /linode/instances/{linodeId}/disks
    parameters:
    - name: linodeId
      in: path
      description: |
        ID of the Linode to look up.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-add-linode-disk
      summary: Create a disk
      tags:
      - Disks
      description: |-
        Adds a new Disk to a Linode.

        - You can optionally create a Disk from an Image or an Empty Disk if no Image is provided with a request.

        - When creating an Empty Disk, providing a `label` is required.

        - If no `label` is provided, an `image` is required instead.

        - When creating a Disk from an Image, `root_pass` is required.

        - The default filesystem for new Disks is `ext4`. If creating a Disk from an Image, the filesystem of the Image is used unless otherwise specified.

        - When deploying a StackScript on a Disk:

          - Run [List StackScripts](https://techdocs.akamai.com/linode-api/reference/get-stack-scripts) for a list of available StackScripts.
          - Requires a compatible Image to be supplied.
            - Run [Get a StackScript](https://techdocs.akamai.com/linode-api/reference/get-stack-script) for compatible Images.
          - It is recommended to supply SSH keys for the root User using the `authorized_keys` field.
          - You may also supply a list of usernames via the `authorized_users` field.
            - These users must have an SSH Key associated with their Profiles first. See [Add an SSH key](https://techdocs.akamai.com/linode-api/reference/post-add-ssh-key) for more information.


        ---


        - __CLI__.

            ```
            linode-cli linodes disk-create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes disk-create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The parameters to set when creating the Disk.
        required: true
        content:
          application/json:
            schema:
              required:
              - size
              allOf:
              - type: object
                description: |
                  Disk object request.
                x-akamai:
                  file-path: schemas/disk-request.yaml
                additionalProperties: false
                properties:
                  authorized_keys:
                    type: array
                    description: |
                      __Write-only__ A list of public SSH keys that will be automatically appended to the
                      root user's `~/.ssh/authorized_keys` file when deploying from
                      an Image.
                    example:
                    - ssh-rsa AAAA_valid_public_ssh_key_123456785== user@their-computer
                    items:
                      type: string
                    writeOnly: true
                  authorized_users:
                    type: array
                    description: |
                      __Write-only__ A list of usernames. If the usernames have associated SSH keys, the
                      keys will be appended to the root users `~/.ssh/authorized_keys`
                      file automatically when deploying from an Image.
                    example:
                    - myUser
                    - secondaryUser
                    items:
                      type: string
                    writeOnly: true
                  filesystem:
                    type: string
                    description: |-
                      The Disk filesystem can be one of:

                        - raw - No filesystem, just a raw binary stream.
                        - swap - Linux swap area.
                        - ext3 - The ext3 journaling filesystem for Linux.
                        - ext4 - The ext4 journaling filesystem for Linux.
                        - initrd - initrd (uncompressed initrd, ext2, max 32 MB).
                    example: ext4
                    enum:
                    - raw
                    - swap
                    - ext3
                    - ext4
                    - initrd
                    x-linode-cli-display: 5
                  image:
                    type: string
                    description: |-
                      An Image ID to deploy the Linode Disk from.

                      Run the [List images](https://techdocs.akamai.com/linode-api/reference/get-images) operation with authentication to view all available Images. Official Linode Images start with `linode/`, while your Account's Images start with `private/`. Creating a disk from a Private Image requires `read_only` or `read_write` permissions for that Image. Run the [Update a user's grants](https://techdocs.akamai.com/linode-api/reference/put-user-grants) operation to adjust permissions for an Account Image.
                    example: linode/debian9
                  label:
                    type: string
                    description: |
                      The Disk's label is for display purposes only.
                    example: Debian 9 Disk
                    minLength: 1
                    maxLength: 48
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  root_pass:
                    type: string
                    description: |-
                      __Write-only__ This sets the root user's password on a newly-created Linode Disk when deploying from an Image.

                      - __Required__ when creating a Linode Disk from an Image, including when using a StackScript.

                      - Must meet a password strength score requirement that is calculated internally by the API. If the strength requirement is not met, you will receive a `Password does not meet strength requirement` error.
                    example: aComplexP@ssword
                    minLength: 7
                    maxLength: 128
                    format: password
                    writeOnly: true
                  size:
                    type: integer
                    description: |-
                      The size of the Disk in MB.

                      Images require a minimum size. Run the [Get an image](https://techdocs.akamai.com/linode-api/reference/get-image) operation to view its size.
                    example: 48640
                    x-linode-filterable: true
                  stackscript_data:
                    type: object
                    description: |-
                      This field is required only if the StackScript being deployed requires input data from the User for successful completion. See [User Defined Fields (UDFs)](https://www.linode.com/docs/products/tools/stackscripts/guides/write-a-custom-script/#declare-user-defined-fields-udfs) for more details.

                      This field is required to be valid JSON.

                      Total length cannot exceed 65,535 characters.
                    example:
                      gh_username: linode
                    maxLength: 65535
                  stackscript_id:
                    type: integer
                    description: |
                      A StackScript ID that will cause the referenced StackScript to be run during deployment
                      of this Linode. A compatible `image` is required to use a StackScript.
                      To get a list of available StackScript and their permitted Images,
                      run [List StackScripts](https://techdocs.akamai.com/linode-api/reference/get-stack-scripts).
                      This field cannot be used when deploying from a Backup or a
                      Private Image.
                    example: 10079
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Disk created.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/disk.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Disk's ID which must be provided for all operations impacting
                      this Disk.
                    example: 25674
                    readOnly: true
                    x-linode-cli-display: 1
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Disk was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  disk_encryption:
                    type: string
                    description: |
                      __Read-only__ Displays if encryption is enabled on this Disk.
                    example: enabled
                    readOnly: true
                    enum:
                    - enabled
                    - disabled
                  filesystem:
                    type: string
                    description: |-
                      The Disk filesystem can be one of:

                        - raw - No filesystem, just a raw binary stream.
                        - swap - Linux swap area.
                        - ext3 - The ext3 journaling filesystem for Linux.
                        - ext4 - The ext4 journaling filesystem for Linux.
                        - initrd - initrd (uncompressed initrd, ext2, max 32 MB).
                    example: ext4
                    enum:
                    - raw
                    - swap
                    - ext3
                    - ext4
                    - initrd
                    x-linode-cli-display: 5
                  label:
                    type: string
                    description: |
                      The Disk's label is for display purposes only.
                    example: Debian 9 Disk
                    minLength: 1
                    maxLength: 48
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  size:
                    type: integer
                    description: |
                      The size of the Disk in MB.
                    example: 48640
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  status:
                    type: string
                    description: |
                      __Read-only__ A brief description of this Disk's current state. This field may change
                      without direct action from you, as a result of operations performed
                      to the Disk or the Linode containing the Disk.
                    example: ready
                    readOnly: true
                    enum:
                    - ready
                    - not ready
                    - deleting
                    x-linode-cli-color:
                      not ready: red
                      default_: yellow
                      ready: green
                    x-linode-cli-display: 3
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Disk was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-add-linode-disk
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "label": "Debian 9 Disk",
                "image": "linode/debian9",
                "size": 1300,
                "authorized_keys": [
                  "ssh-rsa AAAA_valid_public_ssh_key_123456785== user@their-computer"
                ],
                "authorized_users": [
                  "myUser",
                  "secondaryUser"
                ],
                "root_pass": "aComplexP@ssword",
                "stackscript_id": 10079,
                "stackscript_data": {
                  "gh_username": "linode"
                }
              }' \
              https://api.linode.com/v4/linode/instances/123/disks
      - lang: CLI
        source: |-
          linode-cli linodes disk-create 123 \
            --size 1300 \
            --authorized_keys "ssh-rsa AAAA_valid_public_ssh_key_123456785== user@their-computer" \
            --authorized_users "myUser" \
            --authorized_users "secondaryUser" \
            --root_pass aComplex@Password \
            --image "linode/debian9" \
            --stackscript_id 10079 \
            --stackscript_data '{"gh_username": "linode"}'
      x-linode-cli-action: disk-create
      x-original-op-id: addLinodeDisk
      x-original-op-title: Disk Create
    get:
      operationId: get-linode-disks
      summary: List disks
      tags:
      - Disks
      description: |-
        View Disk information for Disks associated with this Linode.


        ---


        - __CLI__.

            ```
            linode-cli linodes disks-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes disks-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated list of disks associated with this Linode.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      x-akamai:
                        file-path: schemas/disk.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ This Disk's ID which must be provided for all operations
                            impacting this Disk.
                          example: 25674
                          readOnly: true
                          x-linode-cli-display: 1
                        created:
                          type: string
                          description: |
                            __Read-only__ When this Disk was created.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                        disk_encryption:
                          type: string
                          description: |
                            __Read-only__ Displays if encryption is enabled on this Disk.
                          example: enabled
                          readOnly: true
                          enum:
                          - enabled
                          - disabled
                        filesystem:
                          type: string
                          description: |-
                            The Disk filesystem can be one of:

                              - raw - No filesystem, just a raw binary stream.
                              - swap - Linux swap area.
                              - ext3 - The ext3 journaling filesystem for Linux.
                              - ext4 - The ext4 journaling filesystem for Linux.
                              - initrd - initrd (uncompressed initrd, ext2, max 32 MB).
                          example: ext4
                          enum:
                          - raw
                          - swap
                          - ext3
                          - ext4
                          - initrd
                          x-linode-cli-display: 5
                        label:
                          type: string
                          description: |
                            The Disk's label is for display purposes only.
                          example: Debian 9 Disk
                          minLength: 1
                          maxLength: 48
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        size:
                          type: integer
                          description: |
                            The size of the Disk in MB.
                          example: 48640
                          x-linode-cli-display: 4
                          x-linode-filterable: true
                        status:
                          type: string
                          description: |
                            __Read-only__ A brief description of this Disk's current state. This
                            field may change without direct action from you, as a
                            result of operations performed to the Disk or the Linode
                            containing the Disk.
                          example: ready
                          readOnly: true
                          enum:
                          - ready
                          - not ready
                          - deleting
                          x-linode-cli-color:
                            not ready: red
                            default_: yellow
                            ready: green
                          x-linode-cli-display: 3
                        updated:
                          type: string
                          description: |
                            __Read-only__ When this Disk was last updated.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-linode-disks
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/linode/instances/123/disks
      - lang: CLI
        source: linode-cli linodes disks-list 123
      x-linode-cli-action: disks-list
      x-linode-grant: read_only
      x-original-op-id: getLinodeDisks
      x-original-op-title: Disks List
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/disks/{diskId}:
    x-akamai:
      file-path: paths/disk.yaml
      path-info: /linode/instances/{linodeId}/disks/{diskId}
    parameters:
    - name: linodeId
      in: path
      description: |
        ID of the Linode to look up.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path.yaml
      required: true
      schema:
        type: integer
    - name: diskId
      in: path
      description: |
        ID of the Disk to look up.
      example: '{{diskId}}'
      x-akamai:
        file-path: parameters/disk-id-path-7d2fdd58.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-linode-disk
      summary: Get a disk
      tags:
      - Disks
      description: |-
        View Disk information for a Disk associated with this Linode.


        ---


        - __CLI__.

            ```
            linode-cli linodes disk-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes disk-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a single Disk object.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/disk.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Disk's ID which must be provided for all operations impacting
                      this Disk.
                    example: 25674
                    readOnly: true
                    x-linode-cli-display: 1
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Disk was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  disk_encryption:
                    type: string
                    description: |
                      __Read-only__ Displays if encryption is enabled on this Disk.
                    example: enabled
                    readOnly: true
                    enum:
                    - enabled
                    - disabled
                  filesystem:
                    type: string
                    description: |-
                      The Disk filesystem can be one of:

                        - raw - No filesystem, just a raw binary stream.
                        - swap - Linux swap area.
                        - ext3 - The ext3 journaling filesystem for Linux.
                        - ext4 - The ext4 journaling filesystem for Linux.
                        - initrd - initrd (uncompressed initrd, ext2, max 32 MB).
                    example: ext4
                    enum:
                    - raw
                    - swap
                    - ext3
                    - ext4
                    - initrd
                    x-linode-cli-display: 5
                  label:
                    type: string
                    description: |
                      The Disk's label is for display purposes only.
                    example: Debian 9 Disk
                    minLength: 1
                    maxLength: 48
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  size:
                    type: integer
                    description: |
                      The size of the Disk in MB.
                    example: 48640
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  status:
                    type: string
                    description: |
                      __Read-only__ A brief description of this Disk's current state. This field may change
                      without direct action from you, as a result of operations performed
                      to the Disk or the Linode containing the Disk.
                    example: ready
                    readOnly: true
                    enum:
                    - ready
                    - not ready
                    - deleting
                    x-linode-cli-color:
                      not ready: red
                      default_: yellow
                      ready: green
                    x-linode-cli-display: 3
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Disk was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-linode-disk
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/linode/instances/123/disks/25674
      - lang: CLI
        source: linode-cli linodes disk-view 123 25674
      x-linode-cli-action: disk-view
      x-linode-grant: read_only
      x-original-op-id: getLinodeDisk
      x-original-op-title: Disk View
    put:
      operationId: put-disk
      summary: Update a disk
      tags:
      - Disks
      description: |-
        Updates a Disk that you have permission to `read_write`.


        ---


        - __CLI__.

            ```
            linode-cli linodes disk-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes disk-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Updates the parameters of a single Disk.
        required: true
        content:
          application/json:
            schema:
              additionalProperties: false
              properties:
                label:
                  type: string
                  description: |
                    The Disk's label is for display purposes only.
                  example: Debian 9 Disk
                  minLength: 1
                  maxLength: 48
                  x-linode-cli-display: 2
                  x-linode-filterable: true
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The updated Disk.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/disk.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Disk's ID which must be provided for all operations impacting
                      this Disk.
                    example: 25674
                    readOnly: true
                    x-linode-cli-display: 1
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Disk was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  disk_encryption:
                    type: string
                    description: |
                      __Read-only__ Displays if encryption is enabled on this Disk.
                    example: enabled
                    readOnly: true
                    enum:
                    - enabled
                    - disabled
                  filesystem:
                    type: string
                    description: |-
                      The Disk filesystem can be one of:

                        - raw - No filesystem, just a raw binary stream.
                        - swap - Linux swap area.
                        - ext3 - The ext3 journaling filesystem for Linux.
                        - ext4 - The ext4 journaling filesystem for Linux.
                        - initrd - initrd (uncompressed initrd, ext2, max 32 MB).
                    example: ext4
                    enum:
                    - raw
                    - swap
                    - ext3
                    - ext4
                    - initrd
                    x-linode-cli-display: 5
                  label:
                    type: string
                    description: |
                      The Disk's label is for display purposes only.
                    example: Debian 9 Disk
                    minLength: 1
                    maxLength: 48
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  size:
                    type: integer
                    description: |
                      The size of the Disk in MB.
                    example: 48640
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  status:
                    type: string
                    description: |
                      __Read-only__ A brief description of this Disk's current state. This field may change
                      without direct action from you, as a result of operations performed
                      to the Disk or the Linode containing the Disk.
                    example: ready
                    readOnly: true
                    enum:
                    - ready
                    - not ready
                    - deleting
                    x-linode-cli-color:
                      not ready: red
                      default_: yellow
                      ready: green
                    x-linode-cli-display: 3
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Disk was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-disk
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "label": "Debian 9 Disk"
              }' \
              https://api.linode.com/v4/linode/instances/123/disks/25674
      - lang: CLI
        source: |-
          linode-cli linodes disk-update 123 25674 \
            --label "Debian 9 Disk"
      x-linode-cli-action: disk-update
      x-linode-grant: read_write
      x-original-op-id: updateDisk
      x-original-op-title: Disk Update
    delete:
      operationId: delete-disk
      summary: Delete a disk
      tags:
      - Disks
      description: |-
        Deletes a Disk you have permission to `read_write`.

        __Deleting a Disk is a destructive action and cannot be undone.__


        ---


        - __CLI__.

            ```
            linode-cli linodes disk-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes disk-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Delete successful
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-disk
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/linode/instances/123/disks/25674
      - lang: CLI
        source: linode-cli linodes disk-delete 123 24674
      x-linode-cli-action: disk-delete
      x-linode-grant: read_write
      x-original-op-id: deleteDisk
      x-original-op-title: Disk Delete
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/disks/{diskId}/clone:
    x-akamai:
      file-path: paths/disk-clone.yaml
      path-info: /linode/instances/{linodeId}/disks/{diskId}/clone
    parameters:
    - name: linodeId
      in: path
      description: |
        ID of the Linode to look up.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path.yaml
      required: true
      schema:
        type: integer
    - name: diskId
      in: path
      description: |
        ID of the Disk to clone.
      example: '{{diskId}}'
      x-akamai:
        file-path: parameters/disk-id-path-dee05ba9.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-clone-linode-disk
      summary: Clone a disk
      tags:
      - Disks
      description: |-
        Copies a disk, byte-for-byte, into a new Disk belonging to the same Linode. The Linode must have enough storage space available to accept a new Disk of the same size as this one or this operation will fail.


        ---


        - __CLI__.

            ```
            linode-cli linodes disk-clone
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes disk-clone
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Disk clone initiated.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/disk.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Disk's ID which must be provided for all operations impacting
                      this Disk.
                    example: 25674
                    readOnly: true
                    x-linode-cli-display: 1
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Disk was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  disk_encryption:
                    type: string
                    description: |
                      __Read-only__ Displays if encryption is enabled on this Disk.
                    example: enabled
                    readOnly: true
                    enum:
                    - enabled
                    - disabled
                  filesystem:
                    type: string
                    description: |-
                      The Disk filesystem can be one of:

                        - raw - No filesystem, just a raw binary stream.
                        - swap - Linux swap area.
                        - ext3 - The ext3 journaling filesystem for Linux.
                        - ext4 - The ext4 journaling filesystem for Linux.
                        - initrd - initrd (uncompressed initrd, ext2, max 32 MB).
                    example: ext4
                    enum:
                    - raw
                    - swap
                    - ext3
                    - ext4
                    - initrd
                    x-linode-cli-display: 5
                  label:
                    type: string
                    description: |
                      The Disk's label is for display purposes only.
                    example: Debian 9 Disk
                    minLength: 1
                    maxLength: 48
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  size:
                    type: integer
                    description: |
                      The size of the Disk in MB.
                    example: 48640
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  status:
                    type: string
                    description: |
                      __Read-only__ A brief description of this Disk's current state. This field may change
                      without direct action from you, as a result of operations performed
                      to the Disk or the Linode containing the Disk.
                    example: ready
                    readOnly: true
                    enum:
                    - ready
                    - not ready
                    - deleting
                    x-linode-cli-color:
                      not ready: red
                      default_: yellow
                      ready: green
                    x-linode-cli-display: 3
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Disk was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-clone-linode-disk
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-linode-cli-action: disk-clone
      x-linode-grant: read_write
      x-original-op-id: cloneLinodeDisk
      x-original-op-title: Disk Clone
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/disks/{diskId}/password:
    x-akamai:
      file-path: paths/disk-password.yaml
      path-info: /linode/instances/{linodeId}/disks/{diskId}/password
    parameters:
    - name: linodeId
      in: path
      description: |
        ID of the Linode to look up.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path.yaml
      required: true
      schema:
        type: integer
    - name: diskId
      in: path
      description: |
        ID of the Disk to look up.
      example: '{{diskId}}'
      x-akamai:
        file-path: parameters/disk-id-path.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-reset-disk-password
      summary: Reset a disk root password
      tags:
      - Disks
      description: |-
        Resets the password of a Disk you have permission to `read_write`.


        ---


        - __CLI__.

            ```
            linode-cli linodes disk-reset-password
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes disk-reset-password
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The new password.
        required: true
        content:
          application/json:
            schema:
              additionalProperties: false
              required:
              - password
              properties:
                password:
                  type: string
                  description: |
                    The new root password for the OS installed on this Disk. The password must meet the
                    complexity strength validation requirements for a strong password.
                  example: another@complex^Password123
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a single Disk object.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-reset-disk-password
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "password": "another@complex^Password123"
              }' \
              https://api.linode.com/v4/linode/instances/123/disks/25674/password
      - lang: CLI
        source: |-
          linode-cli linodes disk-reset-password \
            123 25674 \
            --password aComplex@Password
      x-linode-cli-action: disk-reset-password
      x-linode-grant: read_write
      x-original-op-id: resetDiskPassword
      x-original-op-title: Disk Root Password Reset
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/disks/{diskId}/resize:
    x-akamai:
      file-path: paths/disk-resize.yaml
      path-info: /linode/instances/{linodeId}/disks/{diskId}/resize
    parameters:
    - name: linodeId
      in: path
      description: |
        ID of the Linode to look up.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path.yaml
      required: true
      schema:
        type: integer
    - name: diskId
      in: path
      description: |
        ID of the Disk to look up.
      example: '{{diskId}}'
      x-akamai:
        file-path: parameters/disk-id-path.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-resize-disk
      summary: Resize a disk
      tags:
      - Disks
      description: |-
        Resizes a Disk you have permission to `read_write`.

        The Disk must not be in use. If the Disk is in use, the request will succeed but the resize will ultimately fail. For a request to succeed, the Linode must be shut down prior to resizing the Disk, or the Disk must not be assigned to the Linode's active Configuration Profile.

        If you are resizing the Disk to a smaller size, it cannot be made smaller than what is required by the total size of the files current on the Disk.


        ---


        - __CLI__.

            ```
            linode-cli linodes disk-resize
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes disk-resize
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The new size of the Disk.
        required: true
        content:
          application/json:
            schema:
              additionalProperties: false
              required:
              - size
              properties:
                size:
                  type: integer
                  description: |
                    The desired size, in MB, of the disk.
                  example: 2048
                  minimum: 1
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Resize started.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-resize-disk
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "size": 2048
              }' \
              https://api.linode.com/v4/linode/instances/123/disks/25674/resize
      - lang: CLI
        source: |-
          linode-cli linodes disk-resize 123 25674 \
            --size 2048
      x-linode-cli-action: disk-resize
      x-linode-grant: read_write
      x-original-op-id: resizeDisk
      x-original-op-title: Disk Resize
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/firewalls:
    x-akamai:
      file-path: paths/linode-firewalls.yaml
      path-info: /linode/instances/{linodeId}/firewalls
    parameters:
    - name: linodeId
      in: path
      description: |
        ID of the Linode to access.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path-177e235a.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-linode-firewalls
      summary: List a Linode's firewalls
      tags:
      - Firewalls
      description: |-
        View Firewall information for Firewalls assigned to this Linode.


        ---


        - __CLI__.

            ```
            linode-cli linodes firewalls-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes firewalls-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a paginated list of Firewalls assigned to this Linode.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        A resource that controls incoming and outgoing network traffic to a compute service.
                        Only one enabled Firewall can be attached to a particular
                        service at any given time. [Create a firewall device](https://techdocs.akamai.com/linode-api/reference/post-firewall-device)
                        to assign a Firewall to a service. Currently, Firewalls can
                        assigned to Linode compute instances and NodeBalancers.
                      x-akamai:
                        file-path: schemas/firewall.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ The Firewall's unique ID.
                          example: 123
                          readOnly: true
                          x-linode-cli-display: 1
                          x-linode-filterable: true
                        tags:
                          type: array
                          description: |
                            An array of tags applied to this object. Tags are for organizational purposes
                            only.
                          example:
                          - example tag
                          - another example
                          items:
                            type: string
                          x-linode-filterable: true
                        created:
                          type: string
                          description: |
                            __Read-only__ When this Firewall was created.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                          x-linode-cli-display: 4
                          x-linode-filterable: true
                        label:
                          type: string
                          description: |-
                            The Firewall's label, for display purposes only.

                            Firewall labels have the following constraints:

                              - Must begin and end with an alphanumeric character.
                              - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                              - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
                              - Must be between 3 and 32 characters.
                              - Must be unique.
                          example: firewall123
                          minLength: 3
                          maxLength: 32
                          pattern: ^[a-zA-Z]((?!--|__|..)[a-zA-Z0-9-_.])+$
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        rules:
                          type: object
                          description: |-
                            The inbound and outbound access rules to apply to the Firewall.

                            A Firewall may have up to 25 rules across its inbound and outbound rulesets.

                            Multiple rules are applied in order. If two rules conflict, the first rule takes precedence. For example, if the first rule accepts inbound traffic from an address, and the second rule drops inbound traffic the same address, the first rule applies and inbound traffic from that address is accepted.
                          additionalProperties: false
                          properties:
                            inbound:
                              type: array
                              description: |
                                The inbound rules for the firewall, as a JSON array.
                              items:
                                type: object
                                description: |
                                  One of a Firewall's inbound or outbound access rules. The
                                  `ports` property can be used to allow traffic on
                                  a comma-separated list of different ports.
                                x-akamai:
                                  file-path: schemas/firewall-rule-config.yaml
                                additionalProperties: false
                                properties:
                                  description:
                                    type: string
                                    description: |
                                      Used to describe this rule. For display purposes
                                      only.
                                    example: An example firewall rule description.
                                    minLength: 1
                                    maxLength: 100
                                  action:
                                    type: string
                                    description: |
                                      Controls whether traffic is accepted or dropped by this
                                      rule. Overrides the Firewall's `inbound_policy`
                                      if this is an inbound rule, or the `outbound_policy`
                                      if this is an outbound rule.
                                    example: ACCEPT
                                    enum:
                                    - ACCEPT
                                    - DROP
                                  addresses:
                                    type: object
                                    description: |-
                                      The IPv4 and/or IPv6 addresses affected by this rule. A Rule can have up to 255 total addresses or networks listed across its IPv4 and IPv6 arrays. A network and a single IP are treated as equivalent when accounting for this limit.

                                      Must contain `ipv4`, `ipv6`, or both.
                                    additionalProperties: false
                                    properties:
                                      ipv4:
                                        type: array
                                        description: |-
                                          A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.

                                          If "0.0.0.0/0" is included in this list, all IPv4 addresses are affected by this rule.
                                        example:
                                        - 192.0.2.0/24
                                        - 198.51.100.2/32
                                        items:
                                          type: string
                                      ipv6:
                                        type: array
                                        description: |-
                                          A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.

                                          If "::/0" is included in this list, all IPv6 addresses are affected by this rule.
                                        example:
                                        - 2001:DB8::/128
                                        items:
                                          type: string
                                  label:
                                    type: string
                                    description: |
                                      Used to identify this rule. For display purposes
                                      only.
                                    example: firewallrule123
                                    minLength: 3
                                    maxLength: 32
                                  ports:
                                    type: string
                                    description: |-
                                      A string representing the port or ports affected by this rule:

                                      - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                                      - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                                      - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port "080" is not allowed.
                                      - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                                      - If no ports are configured, all ports are affected.
                                      - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                                    example: 22-24, 80, 443
                                    nullable: true
                                  protocol:
                                    type: string
                                    description: |
                                      The type of network traffic affected by this rule.
                                    example: TCP
                                    enum:
                                    - TCP
                                    - UDP
                                    - ICMP
                                    - IPENCAP
                              x-linode-cli-format: json
                            inbound_policy:
                              type: string
                              description: |
                                The default behavior for inbound traffic. This setting can be
                                overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules)
                                the `inbound.action` property of the Firewall Rule.
                              example: DROP
                              enum:
                              - ACCEPT
                              - DROP
                            outbound:
                              type: array
                              description: |
                                The outbound rules for the firewall, as a JSON array.
                              items:
                                type: object
                                description: |
                                  One of a Firewall's inbound or outbound access rules. The
                                  `ports` property can be used to allow traffic on
                                  a comma-separated list of different ports.
                                x-akamai:
                                  file-path: schemas/firewall-rule-config.yaml
                                additionalProperties: false
                                properties:
                                  description:
                                    type: string
                                    description: |
                                      Used to describe this rule. For display purposes
                                      only.
                                    example: An example firewall rule description.
                                    minLength: 1
                                    maxLength: 100
                                  action:
                                    type: string
                                    description: |
                                      Controls whether traffic is accepted or dropped by this
                                      rule. Overrides the Firewall's `inbound_policy`
                                      if this is an inbound rule, or the `outbound_policy`
                                      if this is an outbound rule.
                                    example: ACCEPT
                                    enum:
                                    - ACCEPT
                                    - DROP
                                  addresses:
                                    type: object
                                    description: |-
                                      The IPv4 and/or IPv6 addresses affected by this rule. A Rule can have up to 255 total addresses or networks listed across its IPv4 and IPv6 arrays. A network and a single IP are treated as equivalent when accounting for this limit.

                                      Must contain `ipv4`, `ipv6`, or both.
                                    additionalProperties: false
                                    properties:
                                      ipv4:
                                        type: array
                                        description: |-
                                          A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.

                                          If "0.0.0.0/0" is included in this list, all IPv4 addresses are affected by this rule.
                                        example:
                                        - 192.0.2.0/24
                                        - 198.51.100.2/32
                                        items:
                                          type: string
                                      ipv6:
                                        type: array
                                        description: |-
                                          A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.

                                          If "::/0" is included in this list, all IPv6 addresses are affected by this rule.
                                        example:
                                        - 2001:DB8::/128
                                        items:
                                          type: string
                                  label:
                                    type: string
                                    description: |
                                      Used to identify this rule. For display purposes
                                      only.
                                    example: firewallrule123
                                    minLength: 3
                                    maxLength: 32
                                  ports:
                                    type: string
                                    description: |-
                                      A string representing the port or ports affected by this rule:

                                      - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                                      - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                                      - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port "080" is not allowed.
                                      - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                                      - If no ports are configured, all ports are affected.
                                      - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                                    example: 22-24, 80, 443
                                    nullable: true
                                  protocol:
                                    type: string
                                    description: |
                                      The type of network traffic affected by this rule.
                                    example: TCP
                                    enum:
                                    - TCP
                                    - UDP
                                    - ICMP
                                    - IPENCAP
                              x-linode-cli-format: json
                            outbound_policy:
                              type: string
                              description: |
                                The default behavior for outbound traffic. This setting can be
                                overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules)
                                the `outbound.action` property of the Firewall Rule.
                              example: DROP
                              enum:
                              - ACCEPT
                              - DROP
                        status:
                          type: string
                          description: |-
                            __Read-only__ The status of this Firewall.

                              - When a Firewall is first created its status is `enabled`.
                              - RUn the [Update a firewall](https://techdocs.akamai.com/linode-api/reference/put-firewall) operation to set a Firewall's status to `enabled` or `disabled`.
                              - RUn the [Delete a firewall](https://techdocs.akamai.com/linode-api/reference/delete-firewall) operation to delete a Firewall.
                          example: enabled
                          readOnly: true
                          enum:
                          - enabled
                          - disabled
                          - deleted
                          x-linode-cli-display: 3
                        updated:
                          type: string
                          description: |
                            __Read-only__ When this Firewall was last updated.
                          example: '2018-01-02T00:01:01'
                          readOnly: true
                          format: date-time
                          x-linode-cli-display: 5
                          x-linode-filterable: true
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-linode-firewalls
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/linode/instances/123/firewalls
      - lang: CLI
        source: linode-cli linodes firewalls-list 123
      x-linode-cli-action: firewalls-list
      x-linode-grant: read_only
      x-original-op-id: getLinodeFirewalls
      x-original-op-title: Firewalls List
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/ips:
    x-akamai:
      file-path: paths/linode-ips.yaml
      path-info: /linode/instances/{linodeId}/ips
    parameters:
    - name: linodeId
      in: path
      description: |
        ID of the Linode to look up.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-add-linode-ip
      summary: Allocate an IPv4 address
      tags:
      - IP addresses
      description: |-
        Allocates a public or private IPv4 address to a Linode. Public IP Addresses, after the one included with each Linode, incur an additional monthly charge. If you need an additional public IP Address you must request one - please [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket). You may not add more than one private IPv4 address to a single Linode.


        ---


        - __CLI__.

            ```
            linode-cli linodes ip-add
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes ip-add
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the address you are creating.
        required: true
        content:
          application/json:
            schema:
              additionalProperties: false
              required:
              - type
              - public
              properties:
                type:
                  type: string
                  description: |
                    The type of address you are allocating. Only IPv4 addresses may be allocated through this
                    operation.
                  example: ipv4
                  enum:
                  - ipv4
                public:
                  type: boolean
                  description: |
                    Whether to create a public or private IPv4 address.
                  example: true
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            IP address was successfully allocated.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An IP address that exists in Linode's system, either IPv4 or IPv6.
                x-akamai:
                  file-path: schemas/ip-address.yaml
                additionalProperties: false
                properties:
                  type:
                    type: string
                    description: |
                      __Read-only__ The type of address this is.
                    example: ipv4
                    readOnly: true
                    enum:
                    - ipv4
                    - ipv6
                    - ipv6/pool
                    - ipv6/range
                    x-linode-cli-display: 2
                  address:
                    type: string
                    description: |
                      __Read-only__ The IP address.
                    example: 97.107.143.141
                    readOnly: true
                    format: ip
                    x-linode-cli-display: 1
                  gateway:
                    type: string
                    description: |
                      __Read-only__ The default gateway for this address.
                    example: 97.107.143.1
                    nullable: true
                    readOnly: true
                    format: ip
                  linode_id:
                    type: integer
                    description: |
                      __Read-only__ The ID of the Linode this address currently belongs to. For IPv4 addresses,
                      this is by default the Linode that this address was assigned
                      to on creation, and these addresses my be moved using the [Assign
                      IPv4s to Linodes](https://techdocs.akamai.com/linode-api/reference/post-assign-ipv4s)
                      operation. For SLAAC and link-local addresses, this value may
                      not be changed.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 6
                  prefix:
                    type: integer
                    description: |
                      __Read-only__ The number of bits set in the subnet mask.
                    example: 24
                    readOnly: true
                  public:
                    type: boolean
                    description: |
                      __Read-only__ Whether this is a public or private IP address.
                    example: true
                    readOnly: true
                    x-linode-cli-display: 3
                  rdns:
                    type: string
                    description: |
                      The reverse DNS assigned to this address. For public IPv4 addresses, this will be set
                      to a default value provided by Linode if not explicitly set.
                    example: test.example.org
                    nullable: true
                    x-linode-cli-display: 4
                  region:
                    type: string
                    description: |
                      __Read-only__ The Region this IP address resides in.
                    example: us-east
                    readOnly: true
                    x-linode-cli-display: 5
                  subnet_mask:
                    type: string
                    description: |
                      __Read-only__ The mask that separates host bits from network bits for this address.
                    example: 255.255.255.0
                    readOnly: true
                    format: ip
                  vpc_nat_1_1:
                    type: object
                    description: |-
                      IPv4 address configured as a 1:1 NAT for this Interface. If no address is configured as a 1:1 NAT, `null` is returned.

                      __Note:__ Only allowed for `vpc` type Interfaces.
                    additionalProperties: false
                    properties:
                      address:
                        type: string
                        description: |
                          The IPv4 address that is configured as a 1:1 NAT for this VPC interface.
                        example: 192.168.0.42
                        format: ipv4
                      subnet_id:
                        type: integer
                        description: |
                          The `id` of the VPC Subnet for this Interface.
                        example: 101
                        nullable: false
                      vpc_id:
                        type: integer
                        description: |
                          __Read-only__ The `id` of the VPC configured for this Interface.
                        example: 111
                        nullable: false
                        readOnly: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-add-linode-ip
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "type": "ipv4",
                "public": true
              }' \
              https://api.linode.com/v4/linode/instances/123/ips
      - lang: CLI
        source: |-
          linode-cli linodes ip-add 123 \
            --type ipv4 \
            --public true
      x-linode-cli-action: ip-add
      x-linode-grant: read_write
      x-original-op-id: addLinodeIP
      x-original-op-title: IPv4 Address Allocate
    get:
      operationId: get-linode-ips
      summary: Get networking information
      tags:
      - IP addresses
      description: |-
        Returns networking information for a single Linode.

        __Note__. If the target Linode has several configuration profiles that include a Virtual Private Cloud (VPC) interface, address information for all of VPCs will be listed in the response.


        ---


        - __CLI__.

            ```
            linode-cli linodes ips-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes ips-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Requested Linode's networking configuration.
          content:
            application/json:
              schema:
                additionalProperties: false
                properties:
                  ipv4:
                    type: object
                    description: |
                      Information about this Linode's IPv4 addresses.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      private:
                        type: array
                        description: |
                          A list of private IP Address objects belonging to this Linode.
                        readOnly: true
                        items:
                          type: object
                          description: |
                            A private IPv4 address that exists in Linode's system.
                          x-akamai:
                            file-path: schemas/ip-address-private.yaml
                          additionalProperties: false
                          properties:
                            type:
                              type: string
                              description: |
                                __Read-only__ The type of address this is.
                              example: ipv4
                              readOnly: true
                              x-linode-cli-display: 2
                            address:
                              type: string
                              description: |
                                __Read-only__ The private IPv4 address.
                              example: 192.168.133.234
                              readOnly: true
                              format: ip
                              x-linode-cli-display: 1
                            gateway:
                              type: string
                              description: |
                                __Read-only__ The default gateway for this address.
                              example: null
                              readOnly: true
                              format: ip
                            linode_id:
                              type: integer
                              description: |
                                __Read-only__ The ID of the Linode this address currently belongs
                                to.
                              example: 123
                              readOnly: true
                              x-linode-cli-display: 6
                            prefix:
                              type: integer
                              description: |
                                __Read-only__ The number of bits set in the subnet mask.
                              example: 17
                              readOnly: true
                            public:
                              type: boolean
                              description: |
                                __Read-only__ Whether this is a public or private IP address.
                              example: false
                              readOnly: true
                              x-linode-cli-display: 3
                            rdns:
                              type: string
                              description: |
                                The reverse DNS assigned to this address.
                              example: null
                              x-linode-cli-display: 4
                            region:
                              type: string
                              description: |
                                __Read-only__ The Region this address resides in.
                              example: us-east
                              readOnly: true
                              x-linode-cli-display: 5
                            subnet_mask:
                              type: string
                              description: |
                                __Read-only__ The mask that separates host bits from network bits
                                for this address.
                              example: 255.255.128.0
                              readOnly: true
                              format: ip
                      public:
                        type: array
                        description: |
                          A list of public IP Address objects belonging to this Linode.
                        readOnly: true
                        items:
                          type: object
                          description: |
                            An IP address that exists in Linode's system, either IPv4 or IPv6.
                          x-akamai:
                            file-path: schemas/ip-address.yaml
                          additionalProperties: false
                          properties:
                            type:
                              type: string
                              description: |
                                __Read-only__ The type of address this is.
                              example: ipv4
                              readOnly: true
                              enum:
                              - ipv4
                              - ipv6
                              - ipv6/pool
                              - ipv6/range
                              x-linode-cli-display: 2
                            address:
                              type: string
                              description: |
                                __Read-only__ The IP address.
                              example: 97.107.143.141
                              readOnly: true
                              format: ip
                              x-linode-cli-display: 1
                            gateway:
                              type: string
                              description: |
                                __Read-only__ The default gateway for this address.
                              example: 97.107.143.1
                              nullable: true
                              readOnly: true
                              format: ip
                            linode_id:
                              type: integer
                              description: |
                                __Read-only__ The ID of the Linode this address currently belongs
                                to. For IPv4 addresses, this is by default the Linode
                                that this address was assigned to on creation, and
                                these addresses my be moved using the [Assign IPv4s
                                to Linodes](https://techdocs.akamai.com/linode-api/reference/post-assign-ipv4s)
                                operation. For SLAAC and link-local addresses, this
                                value may not be changed.
                              example: 123
                              readOnly: true
                              x-linode-cli-display: 6
                            prefix:
                              type: integer
                              description: |
                                __Read-only__ The number of bits set in the subnet mask.
                              example: 24
                              readOnly: true
                            public:
                              type: boolean
                              description: |
                                __Read-only__ Whether this is a public or private IP address.
                              example: true
                              readOnly: true
                              x-linode-cli-display: 3
                            rdns:
                              type: string
                              description: |
                                The reverse DNS assigned to this address. For public IPv4 addresses,
                                this will be set to a default value provided by Linode
                                if not explicitly set.
                              example: test.example.org
                              nullable: true
                              x-linode-cli-display: 4
                            region:
                              type: string
                              description: |
                                __Read-only__ The Region this IP address resides in.
                              example: us-east
                              readOnly: true
                              x-linode-cli-display: 5
                            subnet_mask:
                              type: string
                              description: |
                                __Read-only__ The mask that separates host bits from network bits
                                for this address.
                              example: 255.255.255.0
                              readOnly: true
                              format: ip
                            vpc_nat_1_1:
                              type: object
                              description: |-
                                IPv4 address configured as a 1:1 NAT for this Interface. If no address is configured as a 1:1 NAT, `null` is returned.

                                __Note:__ Only allowed for `vpc` type Interfaces.
                              additionalProperties: false
                              properties:
                                address:
                                  type: string
                                  description: |
                                    The IPv4 address that is configured as a 1:1 NAT for this
                                    VPC interface.
                                  example: 192.168.0.42
                                  format: ipv4
                                subnet_id:
                                  type: integer
                                  description: |
                                    The `id` of the VPC Subnet for this Interface.
                                  example: 101
                                  nullable: false
                                vpc_id:
                                  type: integer
                                  description: |
                                    __Read-only__ The `id` of the VPC configured for this Interface.
                                  example: 111
                                  nullable: false
                                  readOnly: true
                      reserved:
                        type: array
                        description: |
                          A list of reserved IP Address objects belonging to this Linode.
                        readOnly: true
                        items:
                          type: object
                          description: |
                            An IP address that exists in Linode's system, either IPv4 or IPv6.
                          x-akamai:
                            file-path: schemas/ip-address.yaml
                          additionalProperties: false
                          properties:
                            type:
                              type: string
                              description: |
                                __Read-only__ The type of address this is.
                              example: ipv4
                              readOnly: true
                              enum:
                              - ipv4
                              - ipv6
                              - ipv6/pool
                              - ipv6/range
                              x-linode-cli-display: 2
                            address:
                              type: string
                              description: |
                                __Read-only__ The IP address.
                              example: 97.107.143.141
                              readOnly: true
                              format: ip
                              x-linode-cli-display: 1
                            gateway:
                              type: string
                              description: |
                                __Read-only__ The default gateway for this address.
                              example: 97.107.143.1
                              nullable: true
                              readOnly: true
                              format: ip
                            linode_id:
                              type: integer
                              description: |
                                __Read-only__ The ID of the Linode this address currently belongs
                                to. For IPv4 addresses, this is by default the Linode
                                that this address was assigned to on creation, and
                                these addresses my be moved using the [Assign IPv4s
                                to Linodes](https://techdocs.akamai.com/linode-api/reference/post-assign-ipv4s)
                                operation. For SLAAC and link-local addresses, this
                                value may not be changed.
                              example: 123
                              readOnly: true
                              x-linode-cli-display: 6
                            prefix:
                              type: integer
                              description: |
                                __Read-only__ The number of bits set in the subnet mask.
                              example: 24
                              readOnly: true
                            public:
                              type: boolean
                              description: |
                                __Read-only__ Whether this is a public or private IP address.
                              example: true
                              readOnly: true
                              x-linode-cli-display: 3
                            rdns:
                              type: string
                              description: |
                                The reverse DNS assigned to this address. For public IPv4 addresses,
                                this will be set to a default value provided by Linode
                                if not explicitly set.
                              example: test.example.org
                              nullable: true
                              x-linode-cli-display: 4
                            region:
                              type: string
                              description: |
                                __Read-only__ The Region this IP address resides in.
                              example: us-east
                              readOnly: true
                              x-linode-cli-display: 5
                            subnet_mask:
                              type: string
                              description: |
                                __Read-only__ The mask that separates host bits from network bits
                                for this address.
                              example: 255.255.255.0
                              readOnly: true
                              format: ip
                            vpc_nat_1_1:
                              type: object
                              description: |-
                                IPv4 address configured as a 1:1 NAT for this Interface. If no address is configured as a 1:1 NAT, `null` is returned.

                                __Note:__ Only allowed for `vpc` type Interfaces.
                              additionalProperties: false
                              properties:
                                address:
                                  type: string
                                  description: |
                                    The IPv4 address that is configured as a 1:1 NAT for this
                                    VPC interface.
                                  example: 192.168.0.42
                                  format: ipv4
                                subnet_id:
                                  type: integer
                                  description: |
                                    The `id` of the VPC Subnet for this Interface.
                                  example: 101
                                  nullable: false
                                vpc_id:
                                  type: integer
                                  description: |
                                    __Read-only__ The `id` of the VPC configured for this Interface.
                                  example: 111
                                  nullable: false
                                  readOnly: true
                      shared:
                        type: array
                        description: |
                          A list of shared IP Address objects assigned to this Linode.
                        readOnly: true
                        items:
                          type: object
                          description: |
                            An IP address that exists in Linode's system, either IPv4 or IPv6.
                          x-akamai:
                            file-path: schemas/ip-address.yaml
                          additionalProperties: false
                          properties:
                            type:
                              type: string
                              description: |
                                __Read-only__ The type of address this is.
                              example: ipv4
                              readOnly: true
                              enum:
                              - ipv4
                              - ipv6
                              - ipv6/pool
                              - ipv6/range
                              x-linode-cli-display: 2
                            address:
                              type: string
                              description: |
                                __Read-only__ The IP address.
                              example: 97.107.143.141
                              readOnly: true
                              format: ip
                              x-linode-cli-display: 1
                            gateway:
                              type: string
                              description: |
                                __Read-only__ The default gateway for this address.
                              example: 97.107.143.1
                              nullable: true
                              readOnly: true
                              format: ip
                            linode_id:
                              type: integer
                              description: |
                                __Read-only__ The ID of the Linode this address currently belongs
                                to. For IPv4 addresses, this is by default the Linode
                                that this address was assigned to on creation, and
                                these addresses my be moved using the [Assign IPv4s
                                to Linodes](https://techdocs.akamai.com/linode-api/reference/post-assign-ipv4s)
                                operation. For SLAAC and link-local addresses, this
                                value may not be changed.
                              example: 123
                              readOnly: true
                              x-linode-cli-display: 6
                            prefix:
                              type: integer
                              description: |
                                __Read-only__ The number of bits set in the subnet mask.
                              example: 24
                              readOnly: true
                            public:
                              type: boolean
                              description: |
                                __Read-only__ Whether this is a public or private IP address.
                              example: true
                              readOnly: true
                              x-linode-cli-display: 3
                            rdns:
                              type: string
                              description: |
                                The reverse DNS assigned to this address. For public IPv4 addresses,
                                this will be set to a default value provided by Linode
                                if not explicitly set.
                              example: test.example.org
                              nullable: true
                              x-linode-cli-display: 4
                            region:
                              type: string
                              description: |
                                __Read-only__ The Region this IP address resides in.
                              example: us-east
                              readOnly: true
                              x-linode-cli-display: 5
                            subnet_mask:
                              type: string
                              description: |
                                __Read-only__ The mask that separates host bits from network bits
                                for this address.
                              example: 255.255.255.0
                              readOnly: true
                              format: ip
                            vpc_nat_1_1:
                              type: object
                              description: |-
                                IPv4 address configured as a 1:1 NAT for this Interface. If no address is configured as a 1:1 NAT, `null` is returned.

                                __Note:__ Only allowed for `vpc` type Interfaces.
                              additionalProperties: false
                              properties:
                                address:
                                  type: string
                                  description: |
                                    The IPv4 address that is configured as a 1:1 NAT for this
                                    VPC interface.
                                  example: 192.168.0.42
                                  format: ipv4
                                subnet_id:
                                  type: integer
                                  description: |
                                    The `id` of the VPC Subnet for this Interface.
                                  example: 101
                                  nullable: false
                                vpc_id:
                                  type: integer
                                  description: |
                                    __Read-only__ The `id` of the VPC configured for this Interface.
                                  example: 111
                                  nullable: false
                                  readOnly: true
                      vpc:
                        type: array
                        description: |
                          A list of Virtual Private Cloud (VPC)-specific addresses or ranges for the
                          Linode.
                        readOnly: true
                        items:
                          description: |
                            The response data for the VPC IP Addresses List and View operations.
                          x-akamai:
                            file-path: schemas/ip-addresses-vpc.yaml
                          allOf:
                          - type: object
                            description: |
                              An envelope for paginated response. When accessing a collection
                              through a GET endpoint, the results are wrapped in this
                              envelope which includes metadata about those results.
                              Results are presented within a `data` array. See [Pagination](https://techdocs.akamai.com/linode-api/reference/pagination)
                              for more information.
                            x-akamai:
                              file-path: schemas/pagination-envelope.yaml
                            additionalProperties: false
                            properties:
                              page:
                                type: integer
                                description: |
                                  __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                                example: 1
                                readOnly: true
                              pages:
                                type: integer
                                description: |
                                  __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                                example: 1
                                readOnly: true
                              results:
                                type: integer
                                description: |
                                  __Read-only__ The total number of results.
                                example: 1
                                readOnly: true
                          - type: object
                            description: |
                              A VPC IP address that exists in Linode's system, specific to the response
                              for the [List VPC IP addresses](https://techdocs.akamai.com/linode-api/reference/get-vpcs-ips)
                              operation. Returned as an empty set for Linodes that
                              are not part of a VPC.
                            properties:
                              active:
                                type: boolean
                                description: |
                                  __Read-only__ Returns `true` if the VPC interface is in use, meaning
                                  that the Linode was powered on using the `config_id`
                                  to which the interface belongs. Otherwise returns
                                  `false`.
                                example: true
                                readOnly: true
                                x-linode-filterable: true
                              address:
                                type: string
                                description: |
                                  __Read-only__ An IPv4 address configured for this VPC interface.
                                  These follow the [RFC 1918](https://datatracker.ietf.org/doc/html/rfc1918)
                                  private address format. Displayed as `null` if an
                                  `address_range`.
                                example: 192.0.2.141
                                nullable: true
                                readOnly: true
                                format: ip
                                x-linode-cli-display: 1
                              address_range:
                                type: string
                                description: |
                                  __Read-only__ A range of IPv4 addresses configured for this VPC
                                  interface. Displayed as `null` if a single `address`.
                                nullable: true
                                readOnly: true
                              config_id:
                                type: integer
                                description: |
                                  __Read-only__ The globally general entity identifier for the Linode
                                  configuration profile where the VPC is included.
                                example: 4567
                                readOnly: true
                                x-linode-filterable: true
                              gateway:
                                type: string
                                description: |
                                  __Read-only__ The default gateway for the VPC subnet that the
                                  IP or IP range belongs to.
                                example: 192.0.2.1
                                nullable: true
                                readOnly: true
                                format: ip
                              interface_id:
                                type: integer
                                description: |
                                  __Read-only__ The globally general API entity identifier for the
                                  Linode interface.
                                example: 2435
                                readOnly: true
                              linode_id:
                                type: integer
                                description: |
                                  __Read-only__ The identifier for the Linode the VPC interface
                                  currently belongs to.
                                example: 123
                                readOnly: true
                                x-linode-cli-display: 6
                                x-linode-filterable: true
                              nat_1_1:
                                type: string
                                description: |
                                  __Read-only__ The public IP address used for NAT 1:1 with the
                                  VPC. This is empty if NAT 1:1 isn't used.
                                example: 192.168.0.42
                                readOnly: true
                                format: ip
                              prefix:
                                type: integer
                                description: |
                                  __Read-only__ The number of bits set in the `subnet_mask`.
                                example: 24
                                nullable: true
                                readOnly: true
                              region:
                                type: string
                                description: |
                                  __Read-only__ The region of the VPC.
                                example: us-east
                                readOnly: true
                                x-linode-cli-display: 5
                                x-linode-filterable: true
                              subnet_id:
                                type: integer
                                description: |
                                  The `id` of the VPC Subnet for this interface.
                                example: 101
                                nullable: false
                              subnet_mask:
                                type: string
                                description: |
                                  __Read-only__ The mask that separates host bits from network bits
                                  for the `address` or `address_range`.
                                example: 255.255.255.0
                                readOnly: true
                                format: ip
                              vpc_id:
                                type: integer
                                description: |
                                  __Read-only__ The unique globally general API entity identifier
                                  for the VPC.
                                example: 7654
                                readOnly: true
                                x-linode-filterable: true
                  ipv6:
                    type: object
                    description: |
                      Information about this Linode's IPv6 addresses.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      global:
                        type: array
                        description: |
                          A list of IPv6 range objects assigned to this Linode.
                        items:
                          type: object
                          description: |
                            An object representing an IPv6 range.
                          x-akamai:
                            file-path: schemas/ipv6-range.yaml
                          additionalProperties: false
                          properties:
                            prefix:
                              type: integer
                              description: |
                                The prefix length of the address. The total number of addresses
                                that can be assigned from this range is calculated
                                as 2<sup>(128 - prefix length)</sup>.
                              example: 64
                              x-linode-cli-display: 2
                            range:
                              type: string
                              description: |
                                __Read-only__ The IPv6 address of this range.
                              example: '2600:3c01::'
                              readOnly: true
                              x-linode-cli-display: 1
                            region:
                              type: string
                              description: |
                                __Read-only__ The region for this range of IPv6 addresses.
                              example: us-east
                              readOnly: true
                              x-linode-cli-display: 3
                            route_target:
                              type: string
                              description: |
                                The IPv6 SLAAC address.
                              example: 2600:3c01::ffff:ffff:ffff:ffff
                      link_local:
                        type: object
                        description: |
                          A link-local IPv6 address that exists in Linode's system.
                        x-akamai:
                          file-path: schemas/ip-address-v6-link-local.yaml
                        additionalProperties: false
                        properties:
                          type:
                            type: string
                            description: |
                              __Read-only__ The type of address this is.
                            example: ipv6
                            readOnly: true
                            x-linode-cli-display: 2
                          address:
                            type: string
                            description: |
                              __Read-only__ The IPv6 link-local address.
                            example: fe80::f03c:91ff:fe24:3a2f
                            readOnly: true
                            format: ip
                            x-linode-cli-display: 1
                          gateway:
                            type: string
                            description: |
                              __Read-only__ The default gateway for this address.
                            example: fe80::1
                            readOnly: true
                          linode_id:
                            type: integer
                            description: |
                              __Read-only__ The ID of the Linode this address currently belongs
                              to.
                            example: 123
                            readOnly: true
                            x-linode-cli-display: 6
                          prefix:
                            type: integer
                            description: |
                              __Read-only__ The network prefix.
                            example: 64
                            readOnly: true
                          public:
                            type: boolean
                            description: |
                              __Read-only__ Whether this is a public or private IP address.
                            example: false
                            readOnly: true
                            x-linode-cli-display: 3
                          rdns:
                            type: string
                            description: |
                              The reverse DNS assigned to this address.
                            example: null
                            x-linode-cli-display: 4
                          region:
                            type: string
                            description: |
                              __Read-only__ The Region this address resides in.
                            example: us-east
                            readOnly: true
                            x-linode-cli-display: 5
                            x-linode-filterable: true
                          subnet_mask:
                            type: string
                            description: |
                              __Read-only__ The subnet mask.
                            example: 'ffff:ffff:ffff:ffff::'
                            readOnly: true
                            format: ip
                      slaac:
                        type: object
                        description: |
                          A SLAAC IPv6 address that exists in Linode's system.
                        x-akamai:
                          file-path: schemas/ip-address-v6-slaac.yaml
                        additionalProperties: false
                        properties:
                          type:
                            type: string
                            description: |
                              __Read-only__ The type of address this is.
                            example: ipv6
                            readOnly: true
                            x-linode-cli-display: 2
                          address:
                            type: string
                            description: |
                              __Read-only__ The address.
                            example: 2600:3c03::f03c:91ff:fe24:3a2f
                            readOnly: true
                            format: ip
                            x-linode-cli-display: 1
                          gateway:
                            type: string
                            description: |
                              __Read-only__ The default gateway for this address.
                            example: fe80::1
                            readOnly: true
                          linode_id:
                            type: integer
                            description: |
                              __Read-only__ The ID of the Linode this address currently belongs
                              to.
                            example: 123
                            readOnly: true
                            x-linode-cli-display: 6
                          prefix:
                            type: integer
                            description: |
                              __Read-only__ The network prefix.
                            example: 64
                            readOnly: true
                          public:
                            type: boolean
                            description: |
                              __Read-only__ Whether this is a public or private IP address.
                            example: true
                            readOnly: true
                            x-linode-cli-display: 3
                          rdns:
                            type: string
                            description: |
                              The reverse DNS assigned to this address.
                            example: null
                            x-linode-cli-display: 4
                          region:
                            type: string
                            description: |
                              __Read-only__ The Region this address resides in.
                            example: us-east
                            readOnly: true
                            x-linode-cli-display: 5
                            x-linode-filterable: true
                          subnet_mask:
                            type: string
                            description: |
                              __Read-only__ The subnet mask.
                            example: 'ffff:ffff:ffff:ffff::'
                            readOnly: true
                            format: ip
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
              x-linode-cli-subtables:
              - ipv4.public
              - ipv4.private
              - ipv4.shared
              - ipv4.reserved
              - ipv4.vpc
              - ipv6.link_local
              - ipv6.slaac
              - ipv6.global
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-linode-ips
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/linode/instances/123/ips
      - lang: CLI
        source: linode-cli linodes ips-list 123
      x-linode-cli-action: ips-list
      x-linode-grant: read_only
      x-original-op-id: getLinodeIPs
      x-original-op-title: Networking Information List
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/ips/{address}:
    x-akamai:
      file-path: paths/linode-ip-address.yaml
      path-info: /linode/instances/{linodeId}/ips/{address}
    parameters:
    - name: linodeId
      in: path
      description: |
        The ID of the Linode.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path-820d9f86.yaml
      required: true
      schema:
        type: integer
    - name: address
      in: path
      description: |
        The IP address.
      example: '{{address}}'
      x-akamai:
        file-path: parameters/address-path-59ea51e6.yaml
      required: true
      schema:
        type: string
        format: ip
    get:
      operationId: get-linode-ip
      summary: Get a Linode's IP address
      tags:
      - IP addresses
      description: |-
        View information about the specified IP address associated with the specified Linode.


        ---


        - __CLI__.

            ```
            linode-cli linodes ip-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes ip-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A single IP address.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An IP address that exists in Linode's system, either IPv4 or IPv6.
                x-akamai:
                  file-path: schemas/ip-address.yaml
                additionalProperties: false
                properties:
                  type:
                    type: string
                    description: |
                      __Read-only__ The type of address this is.
                    example: ipv4
                    readOnly: true
                    enum:
                    - ipv4
                    - ipv6
                    - ipv6/pool
                    - ipv6/range
                    x-linode-cli-display: 2
                  address:
                    type: string
                    description: |
                      __Read-only__ The IP address.
                    example: 97.107.143.141
                    readOnly: true
                    format: ip
                    x-linode-cli-display: 1
                  gateway:
                    type: string
                    description: |
                      __Read-only__ The default gateway for this address.
                    example: 97.107.143.1
                    nullable: true
                    readOnly: true
                    format: ip
                  linode_id:
                    type: integer
                    description: |
                      __Read-only__ The ID of the Linode this address currently belongs to. For IPv4 addresses,
                      this is by default the Linode that this address was assigned
                      to on creation, and these addresses my be moved using the [Assign
                      IPv4s to Linodes](https://techdocs.akamai.com/linode-api/reference/post-assign-ipv4s)
                      operation. For SLAAC and link-local addresses, this value may
                      not be changed.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 6
                  prefix:
                    type: integer
                    description: |
                      __Read-only__ The number of bits set in the subnet mask.
                    example: 24
                    readOnly: true
                  public:
                    type: boolean
                    description: |
                      __Read-only__ Whether this is a public or private IP address.
                    example: true
                    readOnly: true
                    x-linode-cli-display: 3
                  rdns:
                    type: string
                    description: |
                      The reverse DNS assigned to this address. For public IPv4 addresses, this will be set
                      to a default value provided by Linode if not explicitly set.
                    example: test.example.org
                    nullable: true
                    x-linode-cli-display: 4
                  region:
                    type: string
                    description: |
                      __Read-only__ The Region this IP address resides in.
                    example: us-east
                    readOnly: true
                    x-linode-cli-display: 5
                  subnet_mask:
                    type: string
                    description: |
                      __Read-only__ The mask that separates host bits from network bits for this address.
                    example: 255.255.255.0
                    readOnly: true
                    format: ip
                  vpc_nat_1_1:
                    type: object
                    description: |-
                      IPv4 address configured as a 1:1 NAT for this Interface. If no address is configured as a 1:1 NAT, `null` is returned.

                      __Note:__ Only allowed for `vpc` type Interfaces.
                    additionalProperties: false
                    properties:
                      address:
                        type: string
                        description: |
                          The IPv4 address that is configured as a 1:1 NAT for this VPC interface.
                        example: 192.168.0.42
                        format: ipv4
                      subnet_id:
                        type: integer
                        description: |
                          The `id` of the VPC Subnet for this Interface.
                        example: 101
                        nullable: false
                      vpc_id:
                        type: integer
                        description: |
                          __Read-only__ The `id` of the VPC configured for this Interface.
                        example: 111
                        nullable: false
                        readOnly: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-linode-ip
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/linode/instances/123/ips/97.107.143.141
      - lang: CLI
        source: linode-cli linodes ip-view 123 97.107.143.141
      x-linode-cli-action: ip-view
      x-linode-grant: read_only
      x-original-op-id: getLinodeIP
      x-original-op-title: IP Address View
    put:
      operationId: put-linode-ip
      summary: Update an IP address's RDNS for a Linode
      tags:
      - IP addresses
      description: |-
        Updates the reverse DNS (RDNS) for a Linode's IP Address. This may be done for both IPv4 and IPv6 addresses.

        Setting the RDNS to `null` for a public IPv4 address, resets it to the default "ip.linodeusercontent.com" RDNS value.


        ---


        - __CLI__.

            ```
            linode-cli linodes ip-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes ip-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The information to update for the IP address.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - rdns
              properties:
                rdns:
                  type: string
                  description: |
                    The reverse DNS assigned to this address. For public IPv4 addresses, this will be set to
                    a default value provided by Linode if not explicitly set.
                  example: '{{rdns}}'
                  nullable: true
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The updated IP address record.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An IP address that exists in Linode's system, either IPv4 or IPv6.
                x-akamai:
                  file-path: schemas/ip-address.yaml
                additionalProperties: false
                properties:
                  type:
                    type: string
                    description: |
                      __Read-only__ The type of address this is.
                    example: ipv4
                    readOnly: true
                    enum:
                    - ipv4
                    - ipv6
                    - ipv6/pool
                    - ipv6/range
                    x-linode-cli-display: 2
                  address:
                    type: string
                    description: |
                      __Read-only__ The IP address.
                    example: 97.107.143.141
                    readOnly: true
                    format: ip
                    x-linode-cli-display: 1
                  gateway:
                    type: string
                    description: |
                      __Read-only__ The default gateway for this address.
                    example: 97.107.143.1
                    nullable: true
                    readOnly: true
                    format: ip
                  linode_id:
                    type: integer
                    description: |
                      __Read-only__ The ID of the Linode this address currently belongs to. For IPv4 addresses,
                      this is by default the Linode that this address was assigned
                      to on creation, and these addresses my be moved using the [Assign
                      IPv4s to Linodes](https://techdocs.akamai.com/linode-api/reference/post-assign-ipv4s)
                      operation. For SLAAC and link-local addresses, this value may
                      not be changed.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 6
                  prefix:
                    type: integer
                    description: |
                      __Read-only__ The number of bits set in the subnet mask.
                    example: 24
                    readOnly: true
                  public:
                    type: boolean
                    description: |
                      __Read-only__ Whether this is a public or private IP address.
                    example: true
                    readOnly: true
                    x-linode-cli-display: 3
                  rdns:
                    type: string
                    description: |
                      The reverse DNS assigned to this address. For public IPv4 addresses, this will be set
                      to a default value provided by Linode if not explicitly set.
                    example: test.example.org
                    nullable: true
                    x-linode-cli-display: 4
                  region:
                    type: string
                    description: |
                      __Read-only__ The Region this IP address resides in.
                    example: us-east
                    readOnly: true
                    x-linode-cli-display: 5
                  subnet_mask:
                    type: string
                    description: |
                      __Read-only__ The mask that separates host bits from network bits for this address.
                    example: 255.255.255.0
                    readOnly: true
                    format: ip
                  vpc_nat_1_1:
                    type: object
                    description: |-
                      IPv4 address configured as a 1:1 NAT for this Interface. If no address is configured as a 1:1 NAT, `null` is returned.

                      __Note:__ Only allowed for `vpc` type Interfaces.
                    additionalProperties: false
                    properties:
                      address:
                        type: string
                        description: |
                          The IPv4 address that is configured as a 1:1 NAT for this VPC interface.
                        example: 192.168.0.42
                        format: ipv4
                      subnet_id:
                        type: integer
                        description: |
                          The `id` of the VPC Subnet for this Interface.
                        example: 101
                        nullable: false
                      vpc_id:
                        type: integer
                        description: |
                          __Read-only__ The `id` of the VPC configured for this Interface.
                        example: 111
                        nullable: false
                        readOnly: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-linode-ip
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "rdns": "test.example.org"
              }' \
              https://api.linode.com/v4/linode/instances/123/ips/203.0.113.1
      - lang: CLI
        source: |-
          linode-cli linodes ip-update 123 \
            203.0.113.1 \
            --rdns test.example.org
      x-linode-cli-action: ip-update
      x-linode-grant: read_write
      x-original-op-id: updateLinodeIP
      x-original-op-title: IP Address RDNS Update
    delete:
      operationId: delete-linode-ip
      summary: Delete an IPv4 address
      tags:
      - IP addresses
      description: |-
        Deletes a public or private IPv4 address associated with this Linode. This will fail if it is the Linode's last remaining public IPv4 address, or if the address has a 1:1 NAT with an active VPC Subnet address.


        ---


        - __CLI__.

            ```
            linode-cli linodes ip-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes ip-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            IP address successfully removed.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-linode-ip
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/linode/instances/123/ips/97.107.143.141
      - lang: CLI
        source: linode-cli linodes ip-delete 97.107.143.141
      x-linode-cli-action: ip-delete
      x-linode-grant: read_write
      x-original-op-id: removeLinodeIP
      x-original-op-title: IPv4 Address Delete
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/migrate:
    x-akamai:
      file-path: paths/migrate.yaml
      path-info: /linode/instances/{linodeId}/migrate
    parameters:
    - name: linodeId
      in: path
      description: |
        ID of the Linode to migrate.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path-1510d8f8.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-migrate-linode-instance
      summary: Initiate a DC migration/pending host migration
      tags:
      - Linode instances
      description: |-
        Initiate a pending host migration that has been scheduled by Linode or initiate a cross data center (DC) migration.  A list of pending migrations, if any, can be accessed from [List notifications](https://techdocs.akamai.com/linode-api/reference/get-notifications). When the migration begins, your Linode will be shutdown if not already off. If the migration initiated the shutdown, it will reboot the Linode when completed.

        To initiate a cross DC migration, you must pass a `region` parameter to the request body specifying the target data center region. You can view a list of all available regions and their feature capabilities from [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions). See our [Pricing Page](https://www.linode.com/pricing/) for Region-specific pricing, which applies after migration is complete. If your Linode has a DC migration already queued or you have initiated a previously scheduled migration, you will not be able to initiate a DC migration until it has completed.

        `vpc` details

        - Cross DC migrations are not allowed for Linodes that have a `vpc` purpose Configuration Profile Interface. Host migrations within the same DC are permitted.
        - See the [VPC documentation](https://www.linode.com/docs/products/networking/vpc/#technical-specifications) guide for its specifications and limitations.

        `vlan` details

        - Only Next Generation Network (NGN) data centers support VLANs. Run the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation to view the capabilities of data center regions. If a VLAN is attached to your Linode and you attempt to migrate or clone it to a non-NGN data center, the migration or cloning will not initiate. If a Linode cannot be migrated or cloned because of an incompatibility, you will be prompted to select a different data center or contact support.
        - Next Generation Network (NGN) data centers do not support IPv6 `/116` pools or IP Failover. If you have these features enabled on your Linode and attempt to migrate to an NGN data center, the migration will not initiate. If a Linode cannot be migrated because of an incompatibility, you will be prompted to select a different data center or contact support.
        - See the [VLANs Overview](https://www.linode.com/docs/products/networking/vlans/#technical-specifications) guide to view additional specifications and limitations.


        ---


        - __CLI__.

            ```
            linode-cli linodes migrate
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes migrate
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json:
            schema:
              additionalProperties: false
              properties:
                type:
                  type: string
                  description: |-
                    Type of migration used in moving to a new host or Linode type.

                    `warm`: the Linode will not power down until the migration is complete.
                    Warm migrations are not available for DC migrations.

                    `cold`: the Linode will be powered down and migrated. When the migration
                    is complete, the Linode will be powered on.
                  example: warm
                  default: cold
                  enum:
                  - warm
                  - cold
                placement_group:
                  type: object
                  description: |-
                    Include this to assign this Linode to an existing [placement group](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/) in the data center you're migrating to. These constraints apply:

                    - If the target Linode is in a placement group, it will be removed from it when migrating.
                    - The target placement group needs to be in the same `region` you're migrating to.
                    - The target placement group needs to have capacity. Run the [Get a region](https://techdocs.akamai.com/linode-api/reference/get-region) operation for the region you want to migrate to, and store the `maximum_linodes_per_pg` value. Run the [Get a placement group](https://techdocs.akamai.com/linode-api/reference/get-placement-group) operation for that same region to review how many Linodes are in that group.
                  additionalProperties: false
                  required:
                  - id
                  properties:
                    id:
                      type: integer
                      description: |
                        The placement group's ID. You need to provide it for all operations impacting it.
                      example: 528
                      nullable: false
                      x-linode-cli-display: 1
                region:
                  type: string
                  description: |
                    The region to which the Linode will be migrated. Must be a valid region slug. A list of
                    regions can be viewed by running the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions)
                    operation. A cross data center migration will cancel a pending
                    migration that has not yet been initiated. A cross data center
                    migration will initiate a `linode_migrate_datacenter_create` event.
                  example: us-east
                upgrade:
                  type: boolean
                  description: |
                    When initiating a cross DC migration, setting this value to true will also ensure that
                    the Linode is upgraded to the latest generation of hardware that
                    corresponds to your Linode's Type, if any free upgrades are available
                    for it. If no free upgrades are available, and this value is set
                    to true, then the endpoint will return a 400 error code and the
                    migration will not be performed. If the data center set in the
                    `region` field does not allow upgrades, then the endpoint will
                    return a 400 error code and the migration will not be performed.
                  example: false
                  default: false
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Scheduled migration started
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-migrate-linode-instance
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
            -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "region": "us-central",
                "placement_group": {
                  "id": 528
                }
              }' \
              https://api.linode.com/v4/linode/instances/123/migrate
      - lang: CLI
        source: linode-cli linodes migrate 123 \ --region us-central \ --placement_group.id 528 \
      x-linode-cli-action: migrate
      x-linode-grant: read_write
      x-original-op-id: migrateLinodeInstance
      x-original-op-title: DC Migration/Pending Host
        Migration Initiate
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/mutate:
    x-akamai:
      file-path: paths/mutate.yaml
      path-info: /linode/instances/{linodeId}/mutate
    parameters:
    - name: linodeId
      in: path
      description: |
        ID of the Linode to mutate.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path-ddb4b8aa.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-mutate-linode-instance
      summary: Upgrade a Linode
      tags:
      - Linode instances
      description: |-
        Linodes created with now-deprecated Types are entitled to a free upgrade to the next generation. A mutating Linode will be allocated any new resources the upgraded Type provides, and will be subsequently restarted if it was currently running. If any actions are currently running or queued, those actions must be completed first before you can initiate a mutate.


        ---


        - __CLI__.

            ```
            linode-cli linodes upgrade
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes upgrade
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Whether to automatically resize disks or not.
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                allow_auto_disk_resize:
                  type: boolean
                  description: |
                    Automatically resize disks when resizing a Linode. When resizing down to a smaller
                    plan your Linode's data must fit within the smaller disk size.
                  example: '{{allow_auto_disk_resize}}'
                  default: true
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Mutate started.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-mutate-linode-instance
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST \
              https://api.linode.com/v4/linode/instances/123/mutate
      - lang: CLI
        source: linode-cli linodes upgrade 123
      x-linode-cli-action: upgrade
      x-linode-grant: read_write
      x-original-op-id: mutateLinodeInstance
      x-original-op-title: Linode Upgrade
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/nodebalancers:
    x-akamai:
      file-path: paths/linode-node-balancers.yaml
      path-info: /linode/instances/{linodeId}/nodebalancers
    parameters:
    - name: linodeId
      in: path
      description: |
        ID of the Linode to look up
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path-32b5e821.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-linode-node-balancers
      summary: List Linode NodeBalancers
      tags:
      - Node balancers
      description: |-
        Returns a list of NodeBalancers that are assigned to this Linode and readable by the requesting User.

        Read permission to a NodeBalancer can be given to a User by accessing the [Update a user's grants](https://techdocs.akamai.com/linode-api/reference/put-user-grants) operation.


        ---


        - __CLI__.

            ```
            linode-cli linodes nodebalancers
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes nodebalancers
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        200:
          description: |
            Returns a paginated list of NodeBalancers.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        Linode's load balancing solution.  Can handle multiple ports, SSL termination, and
                        any number of backends.  NodeBalancer ports are configured
                        with NodeBalancer Configs, and each config is given one or
                        more NodeBalancer Node that accepts traffic.  The traffic
                        should be routed to the  NodeBalancer's ip address, the NodeBalancer
                        will handle routing individual requests to backends.
                      x-akamai:
                        file-path: schemas/node-balancer.yaml
                      title: NodeBalancer
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ This NodeBalancer's unique ID.
                          example: 12345
                          readOnly: true
                          x-linode-cli-display: 1
                        tags:
                          type: array
                          description: |
                            An array of Tags applied to this object.  Tags are for organizational purposes
                            only.
                          example:
                          - example tag
                          - another example
                          items:
                            type: string
                          x-linode-filterable: true
                        client_conn_throttle:
                          type: integer
                          description: |
                            Throttle connections per second.  Set to 0 (zero) to disable throttling.
                          example: 0
                          minimum: 0
                          maximum: 20
                          x-linode-cli-display: 6
                        created:
                          type: string
                          description: |
                            __Read-only__ When this NodeBalancer was created.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                        host:
                          type: string
                          description: |
                            __Read-only__ This NodeBalancer's hostname, beginning with its IP address
                            and ending with _.ip.linodeusercontent.com_.
                          example: 192.0.2.1.ip.linodeusercontent.com
                          readOnly: true
                          x-linode-cli-display: 4
                        ipv4:
                          type: string
                          description: |
                            __Read-only__ This NodeBalancer's public IPv4 address.
                          example: 203.0.113.1
                          readOnly: true
                          format: ip
                          x-linode-cli-display: 5
                          x-linode-filterable: true
                        ipv6:
                          type: string
                          description: |
                            __Read-only__ This NodeBalancer's public IPv6 address.
                          example: null
                          nullable: true
                          readOnly: true
                          format: ip
                          x-linode-cli-display: 6
                        label:
                          type: string
                          description: |
                            This NodeBalancer's label. These must be unique on your Account.
                          example: balancer12345
                          minLength: 3
                          maxLength: 32
                          pattern: '[a-zA-Z0-9-_]{3,32}'
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        region:
                          type: string
                          description: |
                            __Read-only__ The Region where this NodeBalancer is located. NodeBalancers
                            only support backends in the same Region.
                          example: us-east
                          readOnly: true
                          x-linode-cli-display: 3
                          x-linode-filterable: true
                        transfer:
                          type: object
                          description: |
                            __Read-only__ Information about the amount of transfer this NodeBalancer
                            has had so far this month.
                          additionalProperties: false
                          readOnly: true
                          properties:
                            in:
                              type: number
                              description: |
                                __Read-only__ The total outbound transfer, in MB, used for this
                                NodeBalancer this month.
                              example: 28.91200828552246
                              nullable: true
                              readOnly: true
                            out:
                              type: number
                              description: |
                                __Read-only__ The total inbound transfer, in MB, used for this NodeBalancer
                                this month.
                              example: 3.5487728118896484
                              nullable: true
                              readOnly: true
                            total:
                              type: number
                              description: |
                                __Read-only__ The total transfer, in MB, used by this NodeBalancer
                                this month.
                              example: 32.46078109741211
                              nullable: true
                              readOnly: true
                        updated:
                          type: string
                          description: |
                            __Read-only__ When this NodeBalancer was last updated.
                          example: '2018-03-01T00:01:01'
                          readOnly: true
                          format: date-time
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-linode-node-balancers
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/linode/instances/123/nodebalancers
      - lang: CLI
        source: linode-cli linodes nodebalancers 123
      x-linode-cli-action: nodebalancers
      x-linode-grant: read_only
      x-original-op-id: getLinodeNodeBalancers
      x-original-op-title: Linode NodeBalancers View
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/password:
    x-akamai:
      file-path: paths/linode-password.yaml
      path-info: /linode/instances/{linodeId}/password
    parameters:
    - name: linodeId
      in: path
      description: |
        ID of the Linode for which to reset its root password.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path-9dcaaf12.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-reset-linode-password
      summary: Reset a Linode's root password
      tags:
      - Linode instances
      description: |-
        Resets the root password for this Linode.

        - Your Linode must be [shut down](https://techdocs.akamai.com/linode-api/reference/post-shutdown-linode-instance) for a password reset to complete.
        - If your Linode has more than one disk (not counting its swap disk), run the [Reset a disk root password](https://techdocs.akamai.com/linode-api/reference/post-reset-disk-password) operation to update a specific disk's root password.
        - A `password_reset` event is generated when a root password reset is successful.


        ---


        - __CLI__.

            ```
            linode-cli linodes linode-reset-password
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes linode-reset-password
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          This Linode's new root password.
        content:
          application/json:
            schema:
              additionalProperties: false
              required:
              - root_pass
              properties:
                root_pass:
                  type: string
                  description: |
                    The root user's password on this Linode. Linode passwords must meet a password strength
                    score requirement that is calculated internally by the API. If
                    the strength requirement is not met, you will receive a Password
                    does not meet strength requirement error.
                  example: a$eCure4assw0rd!43v51
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Password Reset.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-reset-linode-password
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
            -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "root_pass": "a$eCure4assw0rd!43v51"
              }' \
              https://api.linode.com/v4/linode/instances/123/password
      - lang: CLI
        source: linode-cli linodes linode-reset-password 123 a$eCure4assw0rd!43v51
      x-linode-cli-action: linode-reset-password
      x-linode-grant: read_write
      x-original-op-id: resetLinodePassword
      x-original-op-title: Linode Root Password Reset
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/reboot:
    x-akamai:
      file-path: paths/reboot.yaml
      path-info: /linode/instances/{linodeId}/reboot
    parameters:
    - name: linodeId
      in: path
      description: |
        ID of the linode to reboot.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path-84c49e3c.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-reboot-linode-instance
      summary: Reboot a Linode
      tags:
      - Linode instances
      description: |-
        Reboots a Linode you have permission to modify. If any actions are currently running or queued, those actions must be completed first before you can initiate a reboot.


        ---


        - __CLI__.

            ```
            linode-cli linodes reboot
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes reboot
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Optional reboot parameters.
        content:
          application/json:
            schema:
              additionalProperties: false
              properties:
                config_id:
                  type: integer
                  description: |
                    The Linode Config ID to reboot into.  If null or omitted, the last booted config will
                    be used.  If there was no last booted config and this Linode only
                    has one config, it will be used.  If a config cannot be determined,
                    an error will be returned.
                  example: null
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Reboot started.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-reboot-linode-instance
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST \
              https://api.linode.com/v4/linode/instances/123/reboot
      - lang: CLI
        source: linode-cli linodes reboot 123
      x-linode-cli-action: reboot
      x-linode-grant: read_write
      x-original-op-id: rebootLinodeInstance
      x-original-op-title: Linode Reboot
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/rebuild:
    x-akamai:
      file-path: paths/linode-rebuild.yaml
      path-info: /linode/instances/{linodeId}/rebuild
    parameters:
    - name: linodeId
      in: path
      description: |
        ID of the Linode to rebuild.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path-2816c8d8.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-rebuild-linode-instance
      summary: Rebuild a Linode
      tags:
      - Linode instances
      description: |-
        Rebuilds a Linode you have the `read_write` permission to modify.

        A rebuild will first shut down the Linode, delete all disks and configs on the Linode, and then deploy a new `image` to the Linode with the given attributes. Additionally:

          - Requires an `image` be supplied.
          - Requires a `root_pass` be supplied to use for the root User's Account.
          - It is recommended to supply SSH keys for the root User using the `authorized_keys` field.
          - Linodes utilizing Metadata (`"has_user_data": true`) should include `metadata.user_data` in the rebuild request to continue using the service.

        During a rebuild, you can `enable` or `disable` local disk encryption. If disk encryption is not included in the request, the previous `disk_encryption` value is used. Disk encryption cannot be disabled if the compute instance is attached to a LKE nodepool.

        You also have the option to resize the Linode to a different plan by including the `type` parameter with your request. Note that resizing involves migrating the Linode to a new hardware host, while rebuilding without resizing maintains the same hardware host. Resizing also requires significantly more time for completion of this operation. The following additional conditions apply:

          - The Linode must not have a pending migration.
          - Your Account cannot have an outstanding balance.
          - The Linode must not have more disk allocation than the new Type allows.
            - In that situation, you must first delete or resize the disk to be smaller.


        ---


        - __CLI__.

            ```
            linode-cli linodes rebuild
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes rebuild
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The requested state your Linode will be rebuilt into.
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - image
              - root_pass
              allOf:
              - type: object
                description: |
                  Common properties for creating and rebuilding Linodes.
                x-akamai:
                  file-path: schemas/linode-request.yaml
                additionalProperties: false
                properties:
                  authorized_keys:
                    type: array
                    description: |
                      __Write-only__ A list of public SSH keys that will be automatically appended to the
                      root user's `~/.ssh/authorized_keys` file when deploying from
                      an Image.
                    example:
                    - ssh-rsa AAAA_valid_public_ssh_key_123456785== user@their-computer
                    items:
                      type: string
                    writeOnly: true
                  authorized_users:
                    type: array
                    description: |
                      __Write-only__ A list of usernames. If the usernames have associated SSH keys, the
                      keys will be appended to the root users `~/.ssh/authorized_keys`
                      file automatically when deploying from an Image.
                    example:
                    - myUser
                    - secondaryUser
                    items:
                      type: string
                    writeOnly: true
                  booted:
                    type: boolean
                    description: |
                      __Write-only__ This field defaults to `true` if the Linode is created with an Image
                      or from a Backup. If it is deployed from an Image or a Backup
                      and you wish it to remain `offline` after deployment, set this
                      to `false`.
                    default: true
                    writeOnly: true
                  disk_encryption:
                    type: string
                    description: |-
                      Local disk encryption ensures that your data stored on compute instances is encrypted. Encryption converts the data on the compute instance into unreadable code. Decryption of the the disk requires other systems within the datacenter. This requirement protects against data leakage if the disk is removed from a datacenter, lost, stolen, recycled or disposed.

                      By default, encryption is `enabled` on all compute instances but you can opt-out of implementing this feature. If you opted-out of encryption or if the compute instance was created prior to the introduction of the local disk encryption support, you can encrypt your data using Rebuild.
                    enum:
                    - enabled
                    - disabled
                  image:
                    type: string
                    description: |-
                      An Image ID to deploy the Linode Disk from.

                      Run the [List images](https://techdocs.akamai.com/linode-api/reference/get-images) operation with authentication to view all available Images. Official Linode Images start with `linode/`, while your Account's Images start with `private/`. Creating a disk from a Private Image requires `read_only` or `read_write` permissions for that Image. Run the [Update a user's grants](https://techdocs.akamai.com/linode-api/reference/put-user-grants) operation to adjust permissions for an Account Image.
                    example: linode/debian9
                  metadata:
                    type: object
                    description: |
                      __Write-only__ An object containing user-defined data relevant to the creation of Linodes.
                    additionalProperties: false
                    properties:
                      user_data:
                        type: string
                        description: |-
                          Base64-encoded [cloud-config](https://www.linode.com/docs/products/compute/compute-instances/guides/metadata-cloud-config/) data.

                          Cannot be modified after provisioning. To update, use either the [Clone a Linode](https://techdocs.akamai.com/linode-api/reference/post-clone-linode-instance) or [Rebuild a Linode](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance) operations.

                          Must not be included when cloning to an existing Linode.

                          Unencoded data must not exceed 65535 bytes, or about 16kb encoded.
                        example: I2Nsb3VkLWNvbmZpZwpwYWNrYWdlX3VwZGF0ZTogdHJ1ZQpwYWNrYWdlX3VwZ3JhZGU6IHRydWU=
                        format: byte
                    writeOnly: true
                  root_pass:
                    type: string
                    description: |-
                      __Write-only__ This sets the root user's password on a newly-created Linode Disk when deploying from an Image.

                      - __Required__ when creating a Linode Disk from an Image, including when using a StackScript.

                      - Must meet a password strength score requirement that is calculated internally by the API. If the strength requirement is not met, you will receive a `Password does not meet strength requirement` error.
                    example: aComplexP@ssword
                    minLength: 7
                    maxLength: 128
                    format: password
                    writeOnly: true
                  stackscript_data:
                    type: object
                    description: |-
                      This field is required only if the StackScript being deployed requires input data from the User for successful completion. See [User Defined Fields (UDFs)](https://www.linode.com/docs/products/tools/stackscripts/guides/write-a-custom-script/#declare-user-defined-fields-udfs) for more details.

                      This field is required to be valid JSON.

                      Total length cannot exceed 65,535 characters.
                    example:
                      gh_username: linode
                    maxLength: 65535
                  stackscript_id:
                    type: integer
                    description: |
                      A StackScript ID that will cause the referenced StackScript to be run during deployment
                      of this Linode. A compatible `image` is required to use a StackScript.
                      To get a list of available StackScript and their permitted Images,
                      run [List StackScripts](https://techdocs.akamai.com/linode-api/reference/get-stack-scripts).
                      This field cannot be used when deploying from a Backup or a
                      Private Image.
                    example: 10079
              - type: object
                additionalProperties: false
                properties:
                  type:
                    type: string
                    description: |
                      The ID of the [Linode type](https://techdocs.akamai.com/linode-api/reference/get-linode-types)
                      to resize to with this request.
                    example: g6-standard-2
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Rebuild started.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/linode.yaml
                title: Linode
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Linode's ID which must be provided for all operations impacting
                      this Linode.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  tags:
                    type: array
                    description: |
                      An array of tags applied to this object.  Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  type:
                    type: string
                    description: |
                      __Read-only__ This is the [Linode type](https://techdocs.akamai.com/linode-api/reference/get-linode-types)
                      that this Linode was deployed with. To change a Linode's Type,
                      use [Resize a Linode](https://techdocs.akamai.com/linode-api/reference/post-resize-linode-instance).
                    example: g6-standard-1
                    readOnly: true
                    x-linode-cli-display: 4
                  alerts:
                    type: object
                    additionalProperties: false
                    properties:
                      cpu:
                        type: integer
                        description: |-
                          The percentage of CPU usage required to trigger an alert. If the average CPU usage over two hours exceeds this value, we'll send you an alert. Your Linode's total CPU capacity is represented as 100%, multiplied by its number of cores.

                          For example, a two core Linode's CPU capacity is represented as 200%. If you want to be alerted at 90% of a two core Linode's CPU capacity, set the alert value to `180`.

                          The default value is 90% multiplied by the number of cores.

                          If the value is set to `0` (zero), the alert is disabled.
                        example: 180
                      io:
                        type: integer
                        description: |
                          The amount of disk IO operation per second required to trigger an alert. If
                          the average disk IO over two hours exceeds this value, we'll
                          send you an alert. If set to `0` (zero), this alert is disabled.
                        example: 10000
                      network_in:
                        type: integer
                        description: |
                          The amount of incoming traffic, in Mbit/s, required to trigger an alert. If the
                          average incoming traffic over two hours exceeds this value,
                          we'll send you an alert. If this is set to `0` (zero), the
                          alert is disabled.
                        example: 10
                      network_out:
                        type: integer
                        description: |
                          The amount of outbound traffic, in Mbit/s, required to trigger an alert. If the
                          average outbound traffic over two hours exceeds this value,
                          we'll send you an alert. If this is set to `0` (zero), the
                          alert is disabled.
                        example: 10
                      transfer_quota:
                        type: integer
                        description: |
                          The percentage of network transfer that may be used before an alert is
                          triggered. When this value is exceeded, we'll alert you.
                          If this is set to `0` (zero), the alert is disabled.
                        example: 80
                  backups:
                    type: object
                    description: |
                      Information about this Linode's backups status. For information about available backups,
                      run [List backups](https://techdocs.akamai.com/linode-api/reference/get-backups).
                    additionalProperties: false
                    properties:
                      available:
                        type: boolean
                        description: |-
                          __Read-only__ Whether Backups for this Linode are available for restoration.

                          Backups undergoing maintenance are not available for restoration.
                        example: true
                        readOnly: true
                      enabled:
                        type: boolean
                        description: |
                          __Read-only__ If this Linode has the Backup service enabled. To enable backups,
                          run [Enable backups](https://techdocs.akamai.com/linode-api/reference/post-enable-backups).
                        example: true
                        readOnly: true
                      last_successful:
                        type: string
                        description: |
                          __Read-only__ The last successful backup date. 'null' if there was no previous
                          backup.
                        example: '2018-01-01T00:01:01'
                        readOnly: true
                        format: date-time
                      schedule:
                        type: object
                        additionalProperties: false
                        properties:
                          day:
                            type: string
                            description: |-
                              The day of the week that your Linode's weekly Backup is taken. If not set manually, a day will be chosen for you. Backups are taken every day, but backups taken on this day are preferred when selecting backups to retain for a longer period.

                              If not set manually, then when backups are initially enabled, this may come back as `Scheduling` until the `day` is automatically selected.
                            example: Saturday
                            nullable: true
                            enum:
                            - Scheduling
                            - Sunday
                            - Monday
                            - Tuesday
                            - Wednesday
                            - Thursday
                            - Friday
                            - Saturday
                          window:
                            type: string
                            description: |-
                              The window in which your backups will be taken, in UTC. A backups window is a two-hour span of time in which the backup may occur.

                              For example, `W10` indicates that your backups should be taken between 10:00 and 12:00. If you do not choose a backup window, one will be selected for you automatically.

                              If not set manually, when backups are initially enabled this may come back as `Scheduling` until the `window` is automatically selected.
                            example: W22
                            nullable: true
                            enum:
                            - Scheduling
                            - W0
                            - W2
                            - W4
                            - W6
                            - W8
                            - W10
                            - W12
                            - W14
                            - W16
                            - W18
                            - W20
                            - W22
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Linode was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  disk_encryption:
                    type: string
                    description: |
                      Local disk encryption ensures that your data stored on compute instances is encrypted.
                      Encryption converts the data on the compute instance into unreadable
                      code. Decryption of the the disk requires other systems within
                      the datacenter. This requirement protects against data leakage
                      if the disk is removed from a datacenter, lost, stolen, recycled
                      or disposed.
                    example: enabled
                    default: enabled
                    enum:
                    - enabled
                    - disabled
                  group:
                    type: string
                    description: |
                      A deprecated property denoting a group label for this Linode.
                    example: Linode-Group
                    deprecated: true
                    x-linode-filterable: true
                  has_user_data:
                    type: boolean
                    description: |
                      __Read-only__ Whether this compute instance was provisioned utilizing `user_data`
                      provided via the Metadata service. See the [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
                      description for more information on Metadata.
                    example: true
                    readOnly: true
                  host_uuid:
                    type: string
                    description: |
                      __Read-only__ The Linode's host machine, as a UUID.
                    example: 3a3ddd59d9a78bb8de041391075df44de62bfec8
                    readOnly: true
                    format: uuid
                  hypervisor:
                    type: string
                    description: |
                      __Read-only__ The virtualization software powering this Linode.
                    example: kvm
                    readOnly: true
                    enum:
                    - kvm
                  image:
                    example: linode/debian10
                    nullable: true
                    readOnly: true
                    allOf:
                    - type: string
                      description: |-
                        An Image ID to deploy the Linode Disk from.

                        Run the [List images](https://techdocs.akamai.com/linode-api/reference/get-images) operation with authentication to view all available Images. Official Linode Images start with `linode/`, while your Account's Images start with `private/`. Creating a disk from a Private Image requires `read_only` or `read_write` permissions for that Image. Run the [Update a user's grants](https://techdocs.akamai.com/linode-api/reference/put-user-grants) operation to adjust permissions for an Account Image.
                      example: linode/debian9
                    x-linode-cli-display: 5
                    x-linode-filterable: true
                  ipv4:
                    type: array
                    description: |-
                      __Read-only__ This Linode's IPv4 Addresses. Each Linode is assigned a single public IPv4 address upon creation, and may get a single private IPv4 address if needed. You may need to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) to get additional IPv4 addresses.

                      IPv4 addresses may be reassigned between your Linodes, or shared with other Linodes. See the [networking](https://techdocs.akamai.com/linode-api/reference/post-firewalls) operations for details.
                    example:
                    - 203.0.113.1
                    - 192.0.2.1
                    readOnly: true
                    format: ipv4
                    items:
                      type: string
                    x-linode-cli-display: 7
                    x-linode-filterable: true
                  ipv6:
                    type: string
                    description: |
                      __Read-only__ This Linode's IPv6 SLAAC address. This address is specific to a Linode,
                      and may not be shared. If the Linode has not been assigned an
                      IPv6 address, the return value will be `null`.
                    example: c001:d00d::1337/128
                    nullable: true
                    readOnly: true
                    format: ipv6/128
                  label:
                    type: string
                    description: |-
                      The Linode's label is for display purposes only. If no label is provided for a Linode, a default will be assigned.

                      Linode labels have the following constraints:

                      - Must begin and end with an alphanumeric character.
                      - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                      - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
                    example: linode123
                    minLength: 3
                    maxLength: 64
                    pattern: ^[a-zA-Z]((?!--|__|..)[a-zA-Z0-9-_.])+$
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  placement_group:
                    type: object
                    description: |
                      __Read-only__ Details on the [placement group](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/)
                      that this Linode belongs to. Empty if the Linode isn't in a
                      placement group.
                    additionalProperties: false
                    nullable: true
                    readOnly: true
                    properties:
                      id:
                        type: integer
                        description: |
                          The placement group's ID. You need to provide it for all operations impacting
                          it.
                        example: 528
                        nullable: false
                        x-linode-cli-display: 1
                      affinity_type:
                        type: string
                        description: |-
                          __Read-only__ How compute instances are distributed in your placement group. `anti-affinity:local` places compute instances in separate fault domains, but still in the same region.

                          __Note__: With the limited availability release, only `anti_affinity:local` is available for `affinity_type`.
                        example: anti-affinity:local
                        readOnly: true
                        x-linode-cli-display: 1
                        x-linode-filterable: true
                      is_strict:
                        type: boolean
                        description: |-
                          How the API enforces your `affinity_type`:

                          - Set to `true`, your group is strict. You can't add more compute instances to your placement group if your preferred container lacks capacity or is unavailable.
                          - Set to `false`, your group is flexible. You can add more compute instances to it even if they violate the `affinity_type`. If you violate the `affinity_type` your placement group becomes non-compliant and you need to wait for our assistance.
                        example: true
                        nullable: false
                      label:
                        type: string
                        description: |-
                          The unique name set for the placement group. A label has these constraints:

                          - It needs to begin and end with an alphanumeric character.
                          - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                        example: PG_Miami_failover
                        minLength: 1
                        x-linode-cli-display: 1
                        x-linode-filterable: true
                  region:
                    type: string
                    description: |
                      __Read-only__ This is the [region](https://techdocs.akamai.com/linode-api/reference/get-regions)
                      where the Linode was deployed. A Linode's region can only be
                      changed by initiating a [cross data center migration](https://techdocs.akamai.com/linode-api/reference/post-migrate-linode-instance).
                    example: us-east
                    readOnly: true
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  specs:
                    type: object
                    description: |
                      __Read-only__ Information about the resources available to this Linode.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      disk:
                        type: integer
                        description: |
                          __Read-only__ The amount of storage space, in MB, this Linode has access to.
                          A typical Linode will divide this space between a primary
                          disk with an `image` deployed to it, and a swap disk, usually
                          512 MB. This is the default configuration created when deploying
                          a Linode with an `image` through [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance).
                          While this configuration is suitable for 99% of use cases,
                          if you need finer control over your Linode's disks, see
                          the [List disks](https://techdocs.akamai.com/linode-api/reference/get-linode-disks)
                          operations.
                        example: 81920
                        readOnly: true
                      gpus:
                        type: integer
                        description: |
                          __Read-only__ The number of gpus this Linode has access to.
                        example: 0
                        readOnly: true
                      memory:
                        type: integer
                        description: |-
                          __Read-only__ The amount of RAM, in MB, this Linode has access to.

                          Typically, a Linode boots with all of its available RAM, but this can be configured in a Config profile. See the [List config profiles](https://techdocs.akamai.com/linode-api/reference/get-linode-configs) operations and the LinodeConfig object for more information.
                        example: 4096
                        readOnly: true
                      transfer:
                        type: integer
                        description: |
                          __Read-only__ The amount of network transfer this Linode is allotted each
                          month.
                        example: 4000
                        readOnly: true
                      vcpus:
                        type: integer
                        description: |
                          __Read-only__ The number of vcpus this Linode has access to.
                        example: 2
                        readOnly: true
                  status:
                    type: string
                    description: |
                      __Read-only__ A brief description of this Linode's current state. This field may change
                      without direct action from you. For example, when a Linode goes
                      into maintenance mode its status will display "stopped".
                    example: running
                    readOnly: true
                    enum:
                    - running
                    - offline
                    - booting
                    - rebooting
                    - shutting_down
                    - provisioning
                    - deleting
                    - migrating
                    - rebuilding
                    - cloning
                    - restoring
                    - stopped
                    - billing_suspension
                    x-linode-cli-color:
                      billing_suspension: red
                      default_: yellow
                      offline: red
                      running: green
                    x-linode-cli-display: 6
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Linode was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  watchdog_enabled:
                    type: boolean
                    description: |
                      The watchdog, named Lassie, is a Shutdown Watchdog that monitors your Linode and
                      will reboot it if it powers off unexpectedly. It works by issuing
                      a boot job when your Linode powers off without a shutdown job
                      being responsible. To prevent a loop, Lassie will give up if
                      there have been more than 5 boot jobs issued within 15 minutes.
                    example: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "image": "linode/debian9",
                "root_pass": "aComplexP@ssword",
                "disk_encryption": disabled,
                "authorized_keys": [
                  "ssh-rsa AAAA_valid_public_ssh_key_123456785== user@their-computer"
                ],
                "authorized_users": [
                  "myUsername",
                  "secondaryUsername"
                ],
                "booted": true,
                "stackscript_id": 10079,
                "stackscript_data": {
                  "gh_username": "linode"
                },
                "type": "g6-standard-2",
                "metadata": {
                  "user_data": "I2Nsb3VkLWNvbmZpZw=="
                }
              }' \
              https://api.linode.com/v4/linode/instances/123/rebuild
      - lang: CLI
        source: |-
          linode-cli linodes rebuild 123 \
            --image "linode/debian9" \
            --root_pass aComplex@Password \
            --disk_encryption disabled \
            --authorized_keys "ssh-rsa AAAA_valid_public_ssh_key_123456785== user@their-computer" \
            --authorized_users "myUsername" \
            --authorized_users "secondaryUsername" \
            --booted true \
            --stackscript_id 10079 \
            --stackscript_data '{"gh_username": "linode"}' \
            --type "g6-standard-2" \
            --metadata.userdata "I2Nsb3VkLWNvbmZpZw=="
      x-linode-cli-action: rebuild
      x-linode-grant: read_write
      x-original-op-id: rebuildLinodeInstance
      x-original-op-title: Linode Rebuild
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/rescue:
    x-akamai:
      file-path: paths/rescue.yaml
      path-info: /linode/instances/{linodeId}/rescue
    parameters:
    - name: linodeId
      in: path
      description: |
        ID of the Linode to rescue.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path-7f941cb9.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-rescue-linode-instance
      summary: Boot a Linode into rescue mode
      tags:
      - Linode instances
      description: |-
        Rescue Mode is a safe environment for performing many system recovery and disk management tasks. Rescue Mode is based on the Finnix recovery distribution, a self-contained and bootable Linux distribution. You can also use Rescue Mode for tasks other than disaster recovery, such as formatting disks to use different filesystems, copying data between disks, and downloading files from a disk via SSH and SFTP.

        - Note that "sdh" is reserved and unavailable during rescue.


        ---


        - __CLI__.

            ```
            linode-cli linodes rescue
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes rescue
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Optional object of devices to be mounted.
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                devices:
                  type: object
                  x-akamai:
                    file-path: schemas/rescue-devices.yaml
                  additionalProperties: false
                  properties:
                    sda:
                      type: object
                      description: |
                        Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only
                        one type per slot allowed. Can be null. Devices mapped from
                        _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                      x-akamai:
                        file-path: schemas/device.yaml
                      additionalProperties: false
                      properties:
                        disk_id:
                          type: integer
                          description: |
                            The Disk ID, or `null` if a Volume is assigned to this slot.
                          example: 124458
                        volume_id:
                          type: integer
                          description: |
                            The Volume ID, or `null` if a Disk is assigned to this slot.
                          example: null
                    sdb:
                      type: object
                      description: |
                        Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only
                        one type per slot allowed. Can be null. Devices mapped from
                        _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                      x-akamai:
                        file-path: schemas/device.yaml
                      additionalProperties: false
                      properties:
                        disk_id:
                          type: integer
                          description: |
                            The Disk ID, or `null` if a Volume is assigned to this slot.
                          example: 124458
                        volume_id:
                          type: integer
                          description: |
                            The Volume ID, or `null` if a Disk is assigned to this slot.
                          example: null
                    sdc:
                      type: object
                      description: |
                        Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only
                        one type per slot allowed. Can be null. Devices mapped from
                        _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                      x-akamai:
                        file-path: schemas/device.yaml
                      additionalProperties: false
                      properties:
                        disk_id:
                          type: integer
                          description: |
                            The Disk ID, or `null` if a Volume is assigned to this slot.
                          example: 124458
                        volume_id:
                          type: integer
                          description: |
                            The Volume ID, or `null` if a Disk is assigned to this slot.
                          example: null
                    sdd:
                      type: object
                      description: |
                        Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only
                        one type per slot allowed. Can be null. Devices mapped from
                        _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                      x-akamai:
                        file-path: schemas/device.yaml
                      additionalProperties: false
                      properties:
                        disk_id:
                          type: integer
                          description: |
                            The Disk ID, or `null` if a Volume is assigned to this slot.
                          example: 124458
                        volume_id:
                          type: integer
                          description: |
                            The Volume ID, or `null` if a Disk is assigned to this slot.
                          example: null
                    sde:
                      type: object
                      description: |
                        Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only
                        one type per slot allowed. Can be null. Devices mapped from
                        _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                      x-akamai:
                        file-path: schemas/device.yaml
                      additionalProperties: false
                      properties:
                        disk_id:
                          type: integer
                          description: |
                            The Disk ID, or `null` if a Volume is assigned to this slot.
                          example: 124458
                        volume_id:
                          type: integer
                          description: |
                            The Volume ID, or `null` if a Disk is assigned to this slot.
                          example: null
                    sdf:
                      type: object
                      description: |
                        Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only
                        one type per slot allowed. Can be null. Devices mapped from
                        _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                      x-akamai:
                        file-path: schemas/device.yaml
                      additionalProperties: false
                      properties:
                        disk_id:
                          type: integer
                          description: |
                            The Disk ID, or `null` if a Volume is assigned to this slot.
                          example: 124458
                        volume_id:
                          type: integer
                          description: |
                            The Volume ID, or `null` if a Disk is assigned to this slot.
                          example: null
                    sdg:
                      type: object
                      description: |
                        Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only
                        one type per slot allowed. Can be null. Devices mapped from
                        _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                      x-akamai:
                        file-path: schemas/device.yaml
                      additionalProperties: false
                      properties:
                        disk_id:
                          type: integer
                          description: |
                            The Disk ID, or `null` if a Volume is assigned to this slot.
                          example: 124458
                        volume_id:
                          type: integer
                          description: |
                            The Volume ID, or `null` if a Disk is assigned to this slot.
                          example: null
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Rescue started.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-rescue-linode-instance
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "devices": {
                  "sda": {
                    "disk_id": 124458,
                    "volume_id": null
                  },
                  "sdb": {
                    "disk_id": null,
                    "volume_id": null
                  }
                }
              }' \
              https://api.linode.com/v4/linode/instances/123/rescue
      - lang: CLI
        source: |-
          linode-cli linodes rescue 123 \
            --devices.sda.disk_id 124458
      x-linode-cli-action: rescue
      x-linode-grant: read_write
      x-original-op-id: rescueLinodeInstance
      x-original-op-title: Linode Boot into Rescue Mode
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/resize:
    x-akamai:
      file-path: paths/linode-resize.yaml
      path-info: /linode/instances/{linodeId}/resize
    parameters:
    - name: linodeId
      in: path
      description: |
        ID of the Linode to resize.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path-0d41ac92.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-resize-linode-instance
      summary: Resize a Linode
      tags:
      - Linode instances
      description: |-
        Resizes a Linode you have the `read_write` permission to a different Type. If any actions are currently running or queued, those actions must be completed first before you can initiate a resize. Additionally, the following criteria must be met in order to resize a Linode:

          - The Linode must not have a pending migration.
          - Your Account cannot have an outstanding balance.
          - The Linode must not have more disk allocation than the new Type allows.
            - In that situation, you must first delete or resize the disk to be smaller.

        You can also resize a Linode when using the [Rebuild a Linode](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance) operation.


        ---


        - __CLI__.

            ```
            linode-cli linodes resize
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes resize
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The Type your current Linode will resize to, and whether to attempt to automatically resize the Linode's disks.
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - type
              properties:
                type:
                  type: string
                  description: |
                    The ID representing the Linode Type.
                  example: '{{type}}'
                  x-linode-cli-display: 1
                allow_auto_disk_resize:
                  type: boolean
                  description: |
                    Automatically resize disks when resizing a Linode. When resizing down to a smaller
                    plan your Linode's data must fit within the smaller disk size.
                  example: '{{allow_auto_disk_resize}}'
                  default: true
                migration_type:
                  type: string
                  description: |-
                    Type of migration used in moving to a new host or Linode type.

                    `warm`: the Linode will not power down until the migration is complete.
                    Warm migrations are not available for DC migrations.

                    `cold`: the Linode will be powered down and migrated. When the migration
                    is complete, the Linode will be powered on.
                  example: '{{migration_type}}'
                  default: cold
                  enum:
                  - warm
                  - cold
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Resize started.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-resize-linode-instance
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "type": "g6-standard-2"
              }' \
              https://api.linode.com/v4/linode/instances/123/resize
      - lang: CLI
        source: |-
          linode-cli linodes resize 123 \
            --type g6-standard-2
      x-linode-cli-action: resize
      x-linode-grant: read_write
      x-original-op-id: resizeLinodeInstance
      x-original-op-title: Linode Resize
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/shutdown:
    x-akamai:
      file-path: paths/shutdown.yaml
      path-info: /linode/instances/{linodeId}/shutdown
    parameters:
    - name: linodeId
      in: path
      description: |
        ID of the Linode to shutdown.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path-d40d3cd2.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-shutdown-linode-instance
      summary: Shut down a Linode
      tags:
      - Linode instances
      description: |-
        Shuts down a Linode you have permission to modify. If any actions are currently running or queued, those actions must be completed first before you can initiate a shutdown.


        ---


        - __CLI__.

            ```
            linode-cli linodes shutdown
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes shutdown
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Shutdown started.
          content:
            application/json:
              schema:
                type: object
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-shutdown-linode-instance
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST \
              https://api.linode.com/v4/linode/instances/123/shutdown
      - lang: CLI
        source: linode-cli linodes shutdown 123
      x-linode-cli-action: shutdown
      x-linode-grant: read_write
      x-original-op-id: shutdownLinodeInstance
      x-original-op-title: Linode Shut Down
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/stats:
    x-akamai:
      file-path: paths/linode-stats.yaml
      path-info: /linode/instances/{linodeId}/stats
    parameters:
    - name: linodeId
      in: path
      description: |
        ID of the Linode to look up.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-linode-stats
      summary: Get Linode statistics
      tags:
      - Statistics
      description: |-
        Returns CPU, IO, IPv4, and IPv6 statistics for your Linode for the past 24 hours.


        ---


        - __CLI__.

            ```
            linode-cli linodes stats
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes stats
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The Linode's stats for the past 24 hours.
          content:
            application/json:
              schema:
                type: object
                description: |
                  __Read-only__ CPU, IO, IPv4, and IPv6 statistics. Graph data, if available, is in "[timestamp,
                  reading]" array format. Timestamp is a UNIX timestamp in EST.
                x-akamai:
                  file-path: schemas/linode-stats.yaml
                additionalProperties: false
                readOnly: true
                properties:
                  title:
                    type: string
                    description: |
                      The title for this data set.
                    example: linode.com - my-linode (linode123456) - day (5 min avg)
                  cpu:
                    type: array
                    description: |
                      Percentage of CPU used.
                    items:
                      type: array
                      items:
                        type: number
                        example:
                        - 1521483600000
                        - 0.42
                  io:
                    type: object
                    description: |
                      Input/Output statistics.
                    additionalProperties: false
                    properties:
                      io:
                        type: array
                        description: |
                          Block/s written.
                        items:
                          type: array
                          items:
                            type: number
                            example:
                            - 1521484800000
                            - 0.19
                      swap:
                        type: array
                        description: |
                          Block/s written.
                        items:
                          type: array
                          items:
                            type: number
                            example:
                            - 1521484800000
                            - 0
                  netv4:
                    type: object
                    description: |
                      IPv4 statistics.
                    additionalProperties: false
                    properties:
                      in:
                        type: array
                        description: |
                          Input stats for IPv4, measured in bits/s (bits/second).
                        items:
                          type: array
                          items:
                            type: number
                            example:
                            - 1521484800000
                            - 2004.36
                      out:
                        type: array
                        description: |
                          Output stats for IPv4, measured in bits/s (bits/second).
                        items:
                          type: array
                          items:
                            type: number
                            example:
                            - 1521484800000
                            - 3928.91
                      private_in:
                        type: array
                        description: |
                          Private IPv4 input statistics, measured in bits/s (bits/second).
                        items:
                          type: array
                          items:
                            type: number
                            example:
                            - 1521484800000
                            - 0
                      private_out:
                        type: array
                        description: |
                          Private IPv4 output statistics, measured in bits/s (bits/second).
                        items:
                          type: array
                          items:
                            type: number
                            example:
                            - 1521484800000
                            - 5.6
                  netv6:
                    type: object
                    description: |
                      IPv6 statistics.
                    additionalProperties: false
                    properties:
                      in:
                        type: array
                        description: |
                          Input stats for IPv6, measured in bits/s (bits/second).
                        items:
                          type: array
                          items:
                            type: number
                            example:
                            - 1521484800000
                            - 0
                      out:
                        type: array
                        description: |
                          Output stats for IPv6, measured in bits/s (bits/second).
                        items:
                          type: array
                          items:
                            type: number
                            example:
                            - 1521484800000
                            - 0
                      private_in:
                        type: array
                        description: |
                          Private IPv6 input statistics, measured in bits/s (bits/second).
                        items:
                          type: array
                          items:
                            type: number
                            example:
                            - 1521484800000
                            - 195.18
                      private_out:
                        type: array
                        description: |
                          Private IPv6 output statistics, measured in bits/s (bits/second).
                        items:
                          type: array
                          items:
                            type: number
                            example:
                            - 1521484800000
                            - 5.6
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-linode-stats
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/linode/instances/123/stats
      x-linode-cli-action: stats
      x-linode-cli-skip: true
      x-original-op-id: getLinodeStats
      x-original-op-title: Linode Statistics View
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/stats/{year}/{month}:
    x-akamai:
      file-path: paths/linode-stats-month.yaml
      path-info: /linode/instances/{linodeId}/stats/{year}/{month}
    parameters:
    - name: linodeId
      in: path
      description: |
        ID of the Linode to look up.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path.yaml
      required: true
      schema:
        type: integer
    - name: year
      in: path
      description: |
        Numeric value representing the year to look up.
      example: '{{year}}'
      x-akamai:
        file-path: parameters/year-path-6f345986.yaml
      required: true
      schema:
        type: integer
    - name: month
      in: path
      description: |
        Numeric value representing the month to look up.
      example: '{{month}}'
      x-akamai:
        file-path: parameters/month-path.yaml
      required: true
      schema:
        type: integer
        minimum: 1
        maximum: 12
    get:
      operationId: get-linode-stats-by-year-month
      summary: Get monthly statistics
      tags:
      - Statistics
      description: |-
        Returns statistics for a specific month. The year/month values must be either a date in the past, or the current month. If the current month, statistics will be retrieved for the past 30 days.


        ---


        - __CLI__.

            ```
            linode-cli linodes stats-month
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes stats-month
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The Linode's statistics for the requested period.
          content:
            application/json:
              schema:
                type: object
                description: |
                  __Read-only__ CPU, IO, IPv4, and IPv6 statistics. Graph data, if available, is in "[timestamp,
                  reading]" array format. Timestamp is a UNIX timestamp in EST.
                x-akamai:
                  file-path: schemas/linode-stats.yaml
                additionalProperties: false
                readOnly: true
                properties:
                  title:
                    type: string
                    description: |
                      The title for this data set.
                    example: linode.com - my-linode (linode123456) - day (5 min avg)
                  cpu:
                    type: array
                    description: |
                      Percentage of CPU used.
                    items:
                      type: array
                      items:
                        type: number
                        example:
                        - 1521483600000
                        - 0.42
                  io:
                    type: object
                    description: |
                      Input/Output statistics.
                    additionalProperties: false
                    properties:
                      io:
                        type: array
                        description: |
                          Block/s written.
                        items:
                          type: array
                          items:
                            type: number
                            example:
                            - 1521484800000
                            - 0.19
                      swap:
                        type: array
                        description: |
                          Block/s written.
                        items:
                          type: array
                          items:
                            type: number
                            example:
                            - 1521484800000
                            - 0
                  netv4:
                    type: object
                    description: |
                      IPv4 statistics.
                    additionalProperties: false
                    properties:
                      in:
                        type: array
                        description: |
                          Input stats for IPv4, measured in bits/s (bits/second).
                        items:
                          type: array
                          items:
                            type: number
                            example:
                            - 1521484800000
                            - 2004.36
                      out:
                        type: array
                        description: |
                          Output stats for IPv4, measured in bits/s (bits/second).
                        items:
                          type: array
                          items:
                            type: number
                            example:
                            - 1521484800000
                            - 3928.91
                      private_in:
                        type: array
                        description: |
                          Private IPv4 input statistics, measured in bits/s (bits/second).
                        items:
                          type: array
                          items:
                            type: number
                            example:
                            - 1521484800000
                            - 0
                      private_out:
                        type: array
                        description: |
                          Private IPv4 output statistics, measured in bits/s (bits/second).
                        items:
                          type: array
                          items:
                            type: number
                            example:
                            - 1521484800000
                            - 5.6
                  netv6:
                    type: object
                    description: |
                      IPv6 statistics.
                    additionalProperties: false
                    properties:
                      in:
                        type: array
                        description: |
                          Input stats for IPv6, measured in bits/s (bits/second).
                        items:
                          type: array
                          items:
                            type: number
                            example:
                            - 1521484800000
                            - 0
                      out:
                        type: array
                        description: |
                          Output stats for IPv6, measured in bits/s (bits/second).
                        items:
                          type: array
                          items:
                            type: number
                            example:
                            - 1521484800000
                            - 0
                      private_in:
                        type: array
                        description: |
                          Private IPv6 input statistics, measured in bits/s (bits/second).
                        items:
                          type: array
                          items:
                            type: number
                            example:
                            - 1521484800000
                            - 195.18
                      private_out:
                        type: array
                        description: |
                          Private IPv6 output statistics, measured in bits/s (bits/second).
                        items:
                          type: array
                          items:
                            type: number
                            example:
                            - 1521484800000
                            - 5.6
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-linode-stats-by-year-month
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/linode/instances/123/stats/2018/01
      x-linode-cli-action: stats-month
      x-linode-cli-skip: true
      x-original-op-id: getLinodeStatsByYearMonth
      x-original-op-title: Statistics View (year/month)
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/transfer:
    x-akamai:
      file-path: paths/linode-transfer.yaml
      path-info: /linode/instances/{linodeId}/transfer
    parameters:
    - name: linodeId
      in: path
      description: |
        ID of the Linode to look up.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path-dc17dce5.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-linode-transfer
      summary: Get a network transfer
      tags:
      - Network transfers
      description: |-
        Returns a Linode's network transfer pool statistics for the current month.


        ---


        - __CLI__.

            ```
            linode-cli linodes transfer-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes transfer-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A collection of the specified Linode's network transfer statistics.
          content:
            application/json:
              schema:
                additionalProperties: false
                properties:
                  billable:
                    type: integer
                    description: |
                      The amount of network transfer this Linode has used, in GB, past your monthly quota.
                    example: 0
                    readOnly: true
                  quota:
                    type: integer
                    description: |
                      The amount of network transfer this Linode adds to your transfer pool, in GB, for
                      the current month's billing cycle.
                    example: 2000
                    readOnly: true
                  used:
                    type: integer
                    description: |
                      The amount of network transfer used by this Linode, in bytes, for the current month's
                      billing cycle.
                    example: 22956600198
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-linode-transfer
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/linode/instances/123/transfer
      - lang: CLI
        source: linode-cli linodes transfer-view 123
      x-linode-cli-action: transfer-view
      x-linode-grant: read_only
      x-original-op-id: getLinodeTransfer
      x-original-op-title: Network Transfer View
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/transfer/{year}/{month}:
    x-akamai:
      file-path: paths/linode-transfer-month.yaml
      path-info: /linode/instances/{linodeId}/transfer/{year}/{month}
    parameters:
    - name: linodeId
      in: path
      description: |
        ID of the Linode to look up.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path.yaml
      required: true
      schema:
        type: integer
    - name: year
      in: path
      description: |
        Numeric value representing the year to look up.
      example: '{{year}}'
      x-akamai:
        file-path: parameters/year-path-dda8c8e5.yaml
      required: true
      schema:
        type: integer
        minimum: 2000
        maximum: 2037
    - name: month
      in: path
      description: |
        Numeric value representing the month to look up.
      example: '{{month}}'
      x-akamai:
        file-path: parameters/month-path.yaml
      required: true
      schema:
        type: integer
        minimum: 1
        maximum: 12
    get:
      operationId: get-linode-transfer-by-year-month
      summary: Get monthly network transfer stats
      tags:
      - Network transfer statistics
      description: |-
        Returns a Linode's network transfer statistics for a specific month. The year/month values must be either a date in the past, or the current month.


        ---


        - __CLI__.

            ```
            linode-cli linodes transfer-month
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes transfer-month
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A collection of the specified Linode's network transfer statistics for the requested month.
          content:
            application/json:
              schema:
                additionalProperties: false
                properties:
                  bytes_in:
                    type: integer
                    description: |
                      The amount of inbound public network traffic received by this Linode, in bytes, for
                      a specific year/month.
                    example: 30471077120
                    readOnly: true
                  bytes_out:
                    type: integer
                    description: |
                      The amount of outbound public network traffic sent by this Linode, in bytes, for a
                      specific year/month.
                    example: 22956600198
                    readOnly: true
                  bytes_total:
                    type: integer
                    description: |
                      The total amount of public network traffic sent and received by this Linode, in
                      bytes, for a specific year/month.
                    example: 53427677318
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-linode-transfer-by-year-month
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/linode/instances/123/transfer/2018/01
      x-linode-cli-action: transfer-month
      x-linode-cli-skip: true
      x-linode-grant: read_only
      x-original-op-id: getLinodeTransferByYearMonth
      x-original-op-title: Network Transfer View (year/month)
    x-linode-cli-command: linodes
  /linode/instances/{linodeId}/volumes:
    x-akamai:
      file-path: paths/linode-volumes.yaml
      path-info: /linode/instances/{linodeId}/volumes
    parameters:
    - name: linodeId
      in: path
      description: |
        ID of the Linode to look up.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path-dc17dce5.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-linode-volumes
      summary: List a Linode's volumes
      tags:
      - Volumes
      description: |-
        View Block Storage Volumes attached to this Linode.


        ---


        - __CLI__.

            ```
            linode-cli linodes volumes
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            linodes:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli linodes volumes
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: linodes:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns an array of Block Storage Volumes attached to this Linode.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        A Block Storage Volume associated with your Account.
                      x-akamai:
                        file-path: schemas/volume.yaml
                      title: Volume
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ The unique ID of this Volume.
                          example: 12345
                          readOnly: true
                          x-linode-cli-display: 1
                        tags:
                          type: array
                          description: |
                            An array of Tags applied to this object.  Tags are for organizational purposes
                            only.
                          example:
                          - example tag
                          - another example
                          items:
                            type: string
                          x-linode-filterable: true
                        created:
                          type: string
                          description: |
                            __Read-only__ When this Volume was created.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                        filesystem_path:
                          type: string
                          description: |
                            __Read-only__ The full filesystem path for the Volume based on the Volume's
                            label. Path is /dev/disk/by-id/scsi-0Linode_Volume_ +
                            Volume label.
                          example: /dev/disk/by-id/scsi-0Linode_Volume_my-volume
                          readOnly: true
                        hardware_type:
                          type: string
                          description: |
                            __Read-only__ The storage type of this Volume.
                          example: nvme
                          readOnly: true
                          enum:
                          - hdd
                          - nvme
                        label:
                          type: string
                          description: |
                            The Volume's label is for display purposes only.
                          example: my-volume
                          minLength: 1
                          maxLength: 32
                          pattern: ^[a-zA-Z]((?!--|__)[a-zA-Z0-9-_])+$
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        linode_id:
                          type: integer
                          description: |
                            If a Volume is attached to a specific Linode, the ID of that Linode will
                            be displayed here.
                          example: 12346
                          nullable: true
                          x-linode-cli-display: 6
                        linode_label:
                          type: string
                          description: |
                            __Read-only__ If a Volume is attached to a specific Linode, the label
                            of that Linode will be displayed here.
                          example: linode123
                          nullable: true
                          readOnly: true
                          x-linode-cli-display: 7
                        region:
                          type: string
                          description: |
                            __Read-only__ The unique ID of this Region.
                          example: us-east
                          readOnly: true
                          x-linode-cli-display: 5
                        size:
                          type: integer
                          description: |
                            The Volume's size, in GiB.
                          example: 30
                          maximum: 10240
                          x-linode-cli-display: 4
                        status:
                          type: string
                          description: |-
                            __Read-only__ The current status of the volume.  Can be one of:

                              - `creating` - the Volume is being created and is not yet available
                                for use.
                              - `active` - the Volume is online and available for use.
                              - `resizing` - the Volume is in the process of upgrading
                                its current capacity.
                          example: active
                          readOnly: true
                          enum:
                          - creating
                          - active
                          - resizing
                          x-linode-cli-color:
                            active: green
                            contact_support: red
                            default_: yellow
                          x-linode-cli-display: 3
                        updated:
                          type: string
                          description: |
                            __Read-only__ When this Volume was last updated.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-linode-volumes
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/linode/instances/123/volumes
      - lang: CLI
        source: linode-cli linode volumes 123
      x-linode-cli-action: volumes
      x-original-op-id: getLinodeVolumes
      x-original-op-title: Linode's Volumes List
    x-linode-cli-command: linodes
  /linode/kernels:
    x-akamai:
      file-path: paths/kernels.yaml
      path-info: /linode/kernels
    get:
      operationId: get-kernels
      summary: List kernels
      tags:
      - Kernels
      description: |-
        Lists available Kernels.

        Due to the extensive list of available kernels, please keep [pagination](https://techdocs.akamai.com/linode-api/reference/pagination) controls in mind when managing responses to this operation.
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns an array of Kernels.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        Linux kernel object
                      x-akamai:
                        file-path: schemas/kernel.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: string
                          description: |
                            __Read-only__ The unique ID of this Kernel.
                          example: linode/latest-64bit
                          readOnly: true
                          x-linode-cli-display: 1
                        version:
                          type: string
                          description: |
                            __Read-only__ Linux Kernel version.
                          example: 4.15.7
                          readOnly: true
                          x-linode-cli-display: 3
                          x-linode-filterable: true
                        architecture:
                          type: string
                          description: |
                            __Read-only__ The architecture of this Kernel.
                          example: x86_64
                          readOnly: true
                          enum:
                          - x86_64
                          - i386
                          x-linode-cli-display: 4
                          x-linode-filterable: true
                        built:
                          type: string
                          description: |
                            __Read-only__ The date on which this Kernel was built.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                        deprecated:
                          type: boolean
                          description: |
                            __Read-only__ If this Kernel is marked as deprecated, this field has
                            a value of true; otherwise, this field is false.
                          example: false
                          readOnly: true
                          x-linode-filterable: true
                        kvm:
                          type: boolean
                          description: |
                            __Read-only__ If this Kernel is suitable for KVM Linodes.
                          example: true
                          readOnly: true
                          x-linode-filterable: true
                        label:
                          type: string
                          description: |
                            __Read-only__ The friendly name of this Kernel.
                          example: Latest 64 bit (4.15.7-x86_64-linode102)
                          readOnly: true
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        pvops:
                          type: boolean
                          description: |
                            __Read-only__ If this Kernel is suitable for paravirtualized operations.
                          example: false
                          readOnly: true
                          x-linode-filterable: true
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-kernels
      x-code-samples:
      - lang: Shell
        source: curl https://api.linode.com/v4/linode/kernels
      - lang: CLI
        source: linode-cli kernels list
      x-linode-cli-action:
      - list
      - ls
      x-original-op-id: getKernels
      x-original-op-title: Kernels List
    x-linode-cli-command: kernels
  /linode/kernels/{kernelId}:
    x-akamai:
      file-path: paths/kernel.yaml
      path-info: /linode/kernels/{kernelId}
    parameters:
    - name: kernelId
      in: path
      description: |
        ID of the Kernel to look up.
      example: '{{kernelId}}'
      x-akamai:
        file-path: parameters/kernel-id-path.yaml
      required: true
      schema:
        type: string
    get:
      operationId: get-kernel
      summary: Get a kernel
      tags:
      - Kernels
      description: |
        Returns information about a single Kernel.
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A single Kernel object.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Linux kernel object
                x-akamai:
                  file-path: schemas/kernel.yaml
                additionalProperties: false
                properties:
                  id:
                    type: string
                    description: |
                      __Read-only__ The unique ID of this Kernel.
                    example: linode/latest-64bit
                    readOnly: true
                    x-linode-cli-display: 1
                  version:
                    type: string
                    description: |
                      __Read-only__ Linux Kernel version.
                    example: 4.15.7
                    readOnly: true
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  architecture:
                    type: string
                    description: |
                      __Read-only__ The architecture of this Kernel.
                    example: x86_64
                    readOnly: true
                    enum:
                    - x86_64
                    - i386
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  built:
                    type: string
                    description: |
                      __Read-only__ The date on which this Kernel was built.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  deprecated:
                    type: boolean
                    description: |
                      __Read-only__ If this Kernel is marked as deprecated, this field has a value of true;
                      otherwise, this field is false.
                    example: false
                    readOnly: true
                    x-linode-filterable: true
                  kvm:
                    type: boolean
                    description: |
                      __Read-only__ If this Kernel is suitable for KVM Linodes.
                    example: true
                    readOnly: true
                    x-linode-filterable: true
                  label:
                    type: string
                    description: |
                      __Read-only__ The friendly name of this Kernel.
                    example: Latest 64 bit (4.15.7-x86_64-linode102)
                    readOnly: true
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  pvops:
                    type: boolean
                    description: |
                      __Read-only__ If this Kernel is suitable for paravirtualized operations.
                    example: false
                    readOnly: true
                    x-linode-filterable: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-kernel
      x-code-samples:
      - lang: Shell
        source: curl https://api.linode.com/v4/linode/kernels/linode/latest-64bit
      - lang: CLI
        source: linode-cli kernels view latest-64bit
      x-linode-cli-action: view
      x-original-op-id: getKernel
      x-original-op-title: Kernel View
    x-linode-cli-command: kernels
  /linode/stackscripts:
    x-akamai:
      file-path: paths/stack-scripts.yaml
      path-info: /linode/stackscripts
    post:
      operationId: post-add-stack-script
      summary: Create a StackScript
      tags:
      - StackScripts
      description: |-
        Creates a StackScript in your Account.


        ---


        - __CLI__.

            ```
            linode-cli stackscripts create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            stackscripts:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli stackscripts create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: stackscripts:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The properties to set for the new StackScript.
        required: true
        content:
          application/json:
            schema:
              required:
              - script
              - label
              - images
              allOf:
              - type: object
                description: |
                  A StackScript enables you to quickly deploy a fully-configured application in an automated
                  manner.
                x-akamai:
                  file-path: schemas/stack-script.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of this StackScript.
                    example: 10079
                    readOnly: true
                    x-linode-cli-display: 1
                  description:
                    type: string
                    description: |
                      A description for the StackScript.
                    example: This StackScript installs and configures MySQL
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      __Read-only__ The date this StackScript was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 6
                  deployments_active:
                    type: integer
                    description: |
                      __Read-only__ Count of currently active, deployed Linodes created from this StackScript.
                    example: 1
                    readOnly: true
                  deployments_total:
                    type: integer
                    description: |
                      __Read-only__ The total number of times this StackScript has been deployed.
                    example: 12
                    readOnly: true
                    x-linode-filterable: true
                  images:
                    type: array
                    description: |-
                      An array of Image IDs. These are the Images that can be deployed with this StackScript.

                      `any/all` indicates that all available Images, including private Images, are accepted.
                    example:
                    - linode/debian9
                    - linode/debian8
                    items:
                      type: string
                    x-linode-cli-display: 4
                  is_public:
                    type: boolean
                    description: |
                      This determines whether other users can use your StackScript. __Once a StackScript is
                      made public, it cannot be made private.__
                    example: true
                    x-linode-cli-display: 5
                    x-linode-filterable: true
                  label:
                    type: string
                    description: |
                      The StackScript's label is for display purposes only.
                    example: a-stackscript
                    minLength: 3
                    maxLength: 128
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  mine:
                    type: boolean
                    description: |
                      __Read-only__ Returns `true` if this StackScript is owned by the account of the user
                      making the request, and the user making the request is unrestricted
                      or has access to this StackScript.
                    example: true
                    readOnly: true
                    x-linode-filterable: true
                  rev_note:
                    type: string
                    description: |
                      This field allows you to add notes for the set of revisions made to this StackScript.
                    example: Set up MySQL
                    x-linode-filterable: true
                  script:
                    type: string
                    description: |
                      The script to execute when provisioning a new Linode with this StackScript.
                    example: \"#!/bin/bash\"
                    x-linode-cli-format: file
                  updated:
                    type: string
                    description: |
                      __Read-only__ The date this StackScript was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 7
                  user_defined_fields:
                    type: array
                    description: |
                      __Read-only__ This is a list of fields defined with a special syntax inside this
                      StackScript that allow for supplying customized parameters during
                      deployment. See [Declare User-Defined Fields (UDFs)](https://www.linode.com/docs/products/tools/stackscripts/guides/write-a-custom-script/#declare-user-defined-fields-udfs)
                      for more information.
                    example:
                      name: DB_PASSWORD
                      example: hunter2
                      label: Enter the DB password
                    readOnly: true
                    items:
                      type: object
                      description: |
                        A custom field defined by the User with a special syntax within a StackScript. Derived
                        from the contents of the script.
                      x-akamai:
                        file-path: schemas/user-defined-field.yaml
                      additionalProperties: false
                      required:
                      - label
                      - name
                      - example
                      properties:
                        name:
                          type: string
                          description: |
                            __Read-only__ The name of the field.
                          example: DB_PASSWORD
                          readOnly: true
                        example:
                          type: string
                          description: |
                            __Read-only__ An example value for the field.
                          example: hunter2
                          readOnly: true
                        default:
                          type: string
                          description: |
                            __Read-only__ The default value.  If not specified, this value will
                            be used.
                          readOnly: true
                        oneOf:
                          type: string
                          description: |
                            __Read-only__ A list of acceptable single values for the field.
                          example: avalue,anothervalue,thirdvalue
                          readOnly: true
                        label:
                          type: string
                          description: |
                            __Read-only__ A human-readable label for the field that will serve as
                            the input prompt for entering the value during deployment.
                          example: Enter the password
                          readOnly: true
                        manyOf:
                          type: string
                          description: |
                            __Read-only__ A list of acceptable values for the field in any quantity,
                            combination or order.
                          example: avalue,anothervalue,thirdvalue
                          readOnly: true
                  user_gravatar_id:
                    type: string
                    description: |
                      __Read-only__ The Gravatar ID for the User who created the StackScript.
                    example: a445b305abda30ebc766bc7fda037c37
                    readOnly: true
                  username:
                    type: string
                    description: |
                      __Read-only__ The User who created the StackScript.
                    example: myuser
                    readOnly: true
                    x-linode-cli-display: 2
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            StackScript successfully created.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A StackScript enables you to quickly deploy a fully-configured application in an automated
                  manner.
                x-akamai:
                  file-path: schemas/stack-script.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of this StackScript.
                    example: 10079
                    readOnly: true
                    x-linode-cli-display: 1
                  description:
                    type: string
                    description: |
                      A description for the StackScript.
                    example: This StackScript installs and configures MySQL
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      __Read-only__ The date this StackScript was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 6
                  deployments_active:
                    type: integer
                    description: |
                      __Read-only__ Count of currently active, deployed Linodes created from this StackScript.
                    example: 1
                    readOnly: true
                  deployments_total:
                    type: integer
                    description: |
                      __Read-only__ The total number of times this StackScript has been deployed.
                    example: 12
                    readOnly: true
                    x-linode-filterable: true
                  images:
                    type: array
                    description: |-
                      An array of Image IDs. These are the Images that can be deployed with this StackScript.

                      `any/all` indicates that all available Images, including private Images, are accepted.
                    example:
                    - linode/debian9
                    - linode/debian8
                    items:
                      type: string
                    x-linode-cli-display: 4
                  is_public:
                    type: boolean
                    description: |
                      This determines whether other users can use your StackScript. __Once a StackScript is
                      made public, it cannot be made private.__
                    example: true
                    x-linode-cli-display: 5
                    x-linode-filterable: true
                  label:
                    type: string
                    description: |
                      The StackScript's label is for display purposes only.
                    example: a-stackscript
                    minLength: 3
                    maxLength: 128
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  mine:
                    type: boolean
                    description: |
                      __Read-only__ Returns `true` if this StackScript is owned by the account of the user
                      making the request, and the user making the request is unrestricted
                      or has access to this StackScript.
                    example: true
                    readOnly: true
                    x-linode-filterable: true
                  rev_note:
                    type: string
                    description: |
                      This field allows you to add notes for the set of revisions made to this StackScript.
                    example: Set up MySQL
                    x-linode-filterable: true
                  script:
                    type: string
                    description: |
                      The script to execute when provisioning a new Linode with this StackScript.
                    example: \"#!/bin/bash\"
                    x-linode-cli-format: file
                  updated:
                    type: string
                    description: |
                      __Read-only__ The date this StackScript was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 7
                  user_defined_fields:
                    type: array
                    description: |
                      __Read-only__ This is a list of fields defined with a special syntax inside this
                      StackScript that allow for supplying customized parameters during
                      deployment. See [Declare User-Defined Fields (UDFs)](https://www.linode.com/docs/products/tools/stackscripts/guides/write-a-custom-script/#declare-user-defined-fields-udfs)
                      for more information.
                    example:
                      name: DB_PASSWORD
                      example: hunter2
                      label: Enter the DB password
                    readOnly: true
                    items:
                      type: object
                      description: |
                        A custom field defined by the User with a special syntax within a StackScript. Derived
                        from the contents of the script.
                      x-akamai:
                        file-path: schemas/user-defined-field.yaml
                      additionalProperties: false
                      required:
                      - label
                      - name
                      - example
                      properties:
                        name:
                          type: string
                          description: |
                            __Read-only__ The name of the field.
                          example: DB_PASSWORD
                          readOnly: true
                        example:
                          type: string
                          description: |
                            __Read-only__ An example value for the field.
                          example: hunter2
                          readOnly: true
                        default:
                          type: string
                          description: |
                            __Read-only__ The default value.  If not specified, this value will
                            be used.
                          readOnly: true
                        oneOf:
                          type: string
                          description: |
                            __Read-only__ A list of acceptable single values for the field.
                          example: avalue,anothervalue,thirdvalue
                          readOnly: true
                        label:
                          type: string
                          description: |
                            __Read-only__ A human-readable label for the field that will serve as
                            the input prompt for entering the value during deployment.
                          example: Enter the password
                          readOnly: true
                        manyOf:
                          type: string
                          description: |
                            __Read-only__ A list of acceptable values for the field in any quantity,
                            combination or order.
                          example: avalue,anothervalue,thirdvalue
                          readOnly: true
                  user_gravatar_id:
                    type: string
                    description: |
                      __Read-only__ The Gravatar ID for the User who created the StackScript.
                    example: a445b305abda30ebc766bc7fda037c37
                    readOnly: true
                  username:
                    type: string
                    description: |
                      __Read-only__ The User who created the StackScript.
                    example: myuser
                    readOnly: true
                    x-linode-cli-display: 2
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-add-stack-script
      security:
      - personalAccessToken: []
      - oauth:
        - stackscripts:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "label": "a-stackscript",
                "description": "This StackScript installs and configures MySQL",
                "images": [
                  "linode/debian9",
                  "linode/debian8"
                ],
                "is_public": true,
                "rev_note": "Set up MySQL",
                "script": "#!/bin/bash"
              }' \
              https://api.linode.com/v4/linode/stackscripts
      - lang: CLI
        source: |-
          linode-cli stackscripts create \
            --label a-stackscript \
            --description "This StackScript install and configures MySQL" \
            --images "linode/debian9" \
            --images "linode/debian8" \
            --is_public true \
            --rev_note "Set up MySQL" \
            --script '#!/bin/bash'
      x-linode-cli-action: create
      x-linode-grant: add_stackscripts
      x-original-op-id: addStackScript
      x-original-op-title: StackScript Create
    get:
      operationId: get-stack-scripts
      summary: List StackScripts
      tags:
      - StackScripts
      description: |-
        If the request is not authenticated, only public StackScripts are returned.

        For more information on StackScripts, please read our [StackScripts documentation](https://www.linode.com/docs/products/tools/stackscripts/).


        ---


        - __OAuth__.

            ```
            stackscripts:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: stackscripts:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A list of StackScripts available to the User, including private StackScripts owned by the User if the request
            is authenticated.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        A StackScript enables you to quickly deploy a fully-configured application in an
                        automated manner.
                      x-akamai:
                        file-path: schemas/stack-script.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ The unique ID of this StackScript.
                          example: 10079
                          readOnly: true
                          x-linode-cli-display: 1
                        description:
                          type: string
                          description: |
                            A description for the StackScript.
                          example: This StackScript installs and configures MySQL
                          x-linode-filterable: true
                        created:
                          type: string
                          description: |
                            __Read-only__ The date this StackScript was created.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                          x-linode-cli-display: 6
                        deployments_active:
                          type: integer
                          description: |
                            __Read-only__ Count of currently active, deployed Linodes created from
                            this StackScript.
                          example: 1
                          readOnly: true
                        deployments_total:
                          type: integer
                          description: |
                            __Read-only__ The total number of times this StackScript has been deployed.
                          example: 12
                          readOnly: true
                          x-linode-filterable: true
                        images:
                          type: array
                          description: |-
                            An array of Image IDs. These are the Images that can be deployed with this StackScript.

                            `any/all` indicates that all available Images, including private Images, are accepted.
                          example:
                          - linode/debian9
                          - linode/debian8
                          items:
                            type: string
                          x-linode-cli-display: 4
                        is_public:
                          type: boolean
                          description: |
                            This determines whether other users can use your StackScript. __Once a
                            StackScript is made public, it cannot be made private.__
                          example: true
                          x-linode-cli-display: 5
                          x-linode-filterable: true
                        label:
                          type: string
                          description: |
                            The StackScript's label is for display purposes only.
                          example: a-stackscript
                          minLength: 3
                          maxLength: 128
                          x-linode-cli-display: 3
                          x-linode-filterable: true
                        mine:
                          type: boolean
                          description: |
                            __Read-only__ Returns `true` if this StackScript is owned by the account
                            of the user making the request, and the user making the
                            request is unrestricted or has access to this StackScript.
                          example: true
                          readOnly: true
                          x-linode-filterable: true
                        rev_note:
                          type: string
                          description: |
                            This field allows you to add notes for the set of revisions made to this
                            StackScript.
                          example: Set up MySQL
                          x-linode-filterable: true
                        script:
                          type: string
                          description: |
                            The script to execute when provisioning a new Linode with this StackScript.
                          example: \"#!/bin/bash\"
                          x-linode-cli-format: file
                        updated:
                          type: string
                          description: |
                            __Read-only__ The date this StackScript was last updated.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                          x-linode-cli-display: 7
                        user_defined_fields:
                          type: array
                          description: |
                            __Read-only__ This is a list of fields defined with a special syntax
                            inside this StackScript that allow for supplying customized
                            parameters during deployment. See [Declare User-Defined
                            Fields (UDFs)](https://www.linode.com/docs/products/tools/stackscripts/guides/write-a-custom-script/#declare-user-defined-fields-udfs)
                            for more information.
                          example:
                            name: DB_PASSWORD
                            example: hunter2
                            label: Enter the DB password
                          readOnly: true
                          items:
                            type: object
                            description: |
                              A custom field defined by the User with a special syntax within a StackScript.
                              Derived from the contents of the script.
                            x-akamai:
                              file-path: schemas/user-defined-field.yaml
                            additionalProperties: false
                            required:
                            - label
                            - name
                            - example
                            properties:
                              name:
                                type: string
                                description: |
                                  __Read-only__ The name of the field.
                                example: DB_PASSWORD
                                readOnly: true
                              example:
                                type: string
                                description: |
                                  __Read-only__ An example value for the field.
                                example: hunter2
                                readOnly: true
                              default:
                                type: string
                                description: |
                                  __Read-only__ The default value.  If not specified, this value
                                  will be used.
                                readOnly: true
                              oneOf:
                                type: string
                                description: |
                                  __Read-only__ A list of acceptable single values for the field.
                                example: avalue,anothervalue,thirdvalue
                                readOnly: true
                              label:
                                type: string
                                description: |
                                  __Read-only__ A human-readable label for the field that will serve
                                  as the input prompt for entering the value during
                                  deployment.
                                example: Enter the password
                                readOnly: true
                              manyOf:
                                type: string
                                description: |
                                  __Read-only__ A list of acceptable values for the field in any
                                  quantity, combination or order.
                                example: avalue,anothervalue,thirdvalue
                                readOnly: true
                        user_gravatar_id:
                          type: string
                          description: |
                            __Read-only__ The Gravatar ID for the User who created the StackScript.
                          example: a445b305abda30ebc766bc7fda037c37
                          readOnly: true
                        username:
                          type: string
                          description: |
                            __Read-only__ The User who created the StackScript.
                          example: myuser
                          readOnly: true
                          x-linode-cli-display: 2
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-stack-scripts
      security:
      - personalAccessToken: []
      - oauth:
        - stackscripts:read_only
      x-code-samples:
      - lang: Shell
        source: curl https://api.linode.com/v4/linode/stackscripts
      - lang: CLI
        source: linode-cli stackscripts list
      x-linode-cli-action:
      - list
      - ls
      x-linode-grant: read_only
      x-original-op-id: getStackScripts
      x-original-op-title: StackScripts List
    x-linode-cli-command: stackscripts
  /linode/stackscripts/{stackscriptId}:
    x-akamai:
      file-path: paths/stack-script.yaml
      path-info: /linode/stackscripts/{stackscriptId}
    parameters:
    - name: stackscriptId
      in: path
      description: |
        The ID of the StackScript to look up.
      example: '{{stackscriptId}}'
      x-akamai:
        file-path: parameters/stackscript-id-path.yaml
      required: true
      schema:
        type: string
    get:
      operationId: get-stack-script
      summary: Get a StackScript
      tags:
      - StackScripts
      description: |-
        Returns all of the information about a specified StackScript, including the contents of the script.


        ---


        - __CLI__.

            ```
            linode-cli stackscripts view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            stackscripts:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli stackscripts view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: stackscripts:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A single StackScript.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A StackScript enables you to quickly deploy a fully-configured application in an automated
                  manner.
                x-akamai:
                  file-path: schemas/stack-script.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of this StackScript.
                    example: 10079
                    readOnly: true
                    x-linode-cli-display: 1
                  description:
                    type: string
                    description: |
                      A description for the StackScript.
                    example: This StackScript installs and configures MySQL
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      __Read-only__ The date this StackScript was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 6
                  deployments_active:
                    type: integer
                    description: |
                      __Read-only__ Count of currently active, deployed Linodes created from this StackScript.
                    example: 1
                    readOnly: true
                  deployments_total:
                    type: integer
                    description: |
                      __Read-only__ The total number of times this StackScript has been deployed.
                    example: 12
                    readOnly: true
                    x-linode-filterable: true
                  images:
                    type: array
                    description: |-
                      An array of Image IDs. These are the Images that can be deployed with this StackScript.

                      `any/all` indicates that all available Images, including private Images, are accepted.
                    example:
                    - linode/debian9
                    - linode/debian8
                    items:
                      type: string
                    x-linode-cli-display: 4
                  is_public:
                    type: boolean
                    description: |
                      This determines whether other users can use your StackScript. __Once a StackScript is
                      made public, it cannot be made private.__
                    example: true
                    x-linode-cli-display: 5
                    x-linode-filterable: true
                  label:
                    type: string
                    description: |
                      The StackScript's label is for display purposes only.
                    example: a-stackscript
                    minLength: 3
                    maxLength: 128
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  mine:
                    type: boolean
                    description: |
                      __Read-only__ Returns `true` if this StackScript is owned by the account of the user
                      making the request, and the user making the request is unrestricted
                      or has access to this StackScript.
                    example: true
                    readOnly: true
                    x-linode-filterable: true
                  rev_note:
                    type: string
                    description: |
                      This field allows you to add notes for the set of revisions made to this StackScript.
                    example: Set up MySQL
                    x-linode-filterable: true
                  script:
                    type: string
                    description: |
                      The script to execute when provisioning a new Linode with this StackScript.
                    example: \"#!/bin/bash\"
                    x-linode-cli-format: file
                  updated:
                    type: string
                    description: |
                      __Read-only__ The date this StackScript was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 7
                  user_defined_fields:
                    type: array
                    description: |
                      __Read-only__ This is a list of fields defined with a special syntax inside this
                      StackScript that allow for supplying customized parameters during
                      deployment. See [Declare User-Defined Fields (UDFs)](https://www.linode.com/docs/products/tools/stackscripts/guides/write-a-custom-script/#declare-user-defined-fields-udfs)
                      for more information.
                    example:
                      name: DB_PASSWORD
                      example: hunter2
                      label: Enter the DB password
                    readOnly: true
                    items:
                      type: object
                      description: |
                        A custom field defined by the User with a special syntax within a StackScript. Derived
                        from the contents of the script.
                      x-akamai:
                        file-path: schemas/user-defined-field.yaml
                      additionalProperties: false
                      required:
                      - label
                      - name
                      - example
                      properties:
                        name:
                          type: string
                          description: |
                            __Read-only__ The name of the field.
                          example: DB_PASSWORD
                          readOnly: true
                        example:
                          type: string
                          description: |
                            __Read-only__ An example value for the field.
                          example: hunter2
                          readOnly: true
                        default:
                          type: string
                          description: |
                            __Read-only__ The default value.  If not specified, this value will
                            be used.
                          readOnly: true
                        oneOf:
                          type: string
                          description: |
                            __Read-only__ A list of acceptable single values for the field.
                          example: avalue,anothervalue,thirdvalue
                          readOnly: true
                        label:
                          type: string
                          description: |
                            __Read-only__ A human-readable label for the field that will serve as
                            the input prompt for entering the value during deployment.
                          example: Enter the password
                          readOnly: true
                        manyOf:
                          type: string
                          description: |
                            __Read-only__ A list of acceptable values for the field in any quantity,
                            combination or order.
                          example: avalue,anothervalue,thirdvalue
                          readOnly: true
                  user_gravatar_id:
                    type: string
                    description: |
                      __Read-only__ The Gravatar ID for the User who created the StackScript.
                    example: a445b305abda30ebc766bc7fda037c37
                    readOnly: true
                  username:
                    type: string
                    description: |
                      __Read-only__ The User who created the StackScript.
                    example: myuser
                    readOnly: true
                    x-linode-cli-display: 2
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-stack-script
      security:
      - personalAccessToken: []
      - oauth:
        - stackscripts:read_only
      x-code-samples:
      - lang: Shell
        source: curl https://api.linode.com/v4/linode/stackscripts/10079
      - lang: CLI
        source: linode-cli stackscripts view 10079
      x-linode-cli-action: view
      x-linode-grant: read_only
      x-original-op-id: getStackScript
      x-original-op-title: StackScript View
    put:
      operationId: put-stack-script
      summary: Update a StackScript
      tags:
      - StackScripts
      description: |-
        Updates a StackScript.

        __Once a StackScript is made public, it cannot be made private.__


        ---


        - __CLI__.

            ```
            linode-cli stackscripts update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            stackscripts:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli stackscripts update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: stackscripts:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The fields to update.
        content:
          application/json:
            schema:
              type: object
              description: |
                A StackScript enables you to quickly deploy a fully-configured application in an automated manner.
              x-akamai:
                file-path: schemas/stack-script.yaml
              additionalProperties: false
              properties:
                id:
                  type: integer
                  description: |
                    __Read-only__ The unique ID of this StackScript.
                  example: '{{id}}'
                  readOnly: true
                  x-linode-cli-display: 1
                description:
                  type: string
                  description: |
                    A description for the StackScript.
                  example: '{{description}}'
                  x-linode-filterable: true
                created:
                  type: string
                  description: |
                    __Read-only__ The date this StackScript was created.
                  example: '{{created}}'
                  readOnly: true
                  format: date-time
                  x-linode-cli-display: 6
                deployments_active:
                  type: integer
                  description: |
                    __Read-only__ Count of currently active, deployed Linodes created from this StackScript.
                  example: '{{deployments_active}}'
                  readOnly: true
                deployments_total:
                  type: integer
                  description: |
                    __Read-only__ The total number of times this StackScript has been deployed.
                  example: '{{deployments_total}}'
                  readOnly: true
                  x-linode-filterable: true
                images:
                  type: array
                  description: |-
                    An array of Image IDs. These are the Images that can be deployed with this StackScript.

                    `any/all` indicates that all available Images, including private Images, are accepted.
                  example:
                  - linode/debian9
                  - linode/debian8
                  items:
                    type: string
                  x-linode-cli-display: 4
                is_public:
                  type: boolean
                  description: |
                    This determines whether other users can use your StackScript. __Once a StackScript is
                    made public, it cannot be made private.__
                  example: '{{is_public}}'
                  x-linode-cli-display: 5
                  x-linode-filterable: true
                label:
                  type: string
                  description: |
                    The StackScript's label is for display purposes only.
                  example: '{{label}}'
                  minLength: 3
                  maxLength: 128
                  x-linode-cli-display: 3
                  x-linode-filterable: true
                mine:
                  type: boolean
                  description: |
                    __Read-only__ Returns `true` if this StackScript is owned by the account of the user
                    making the request, and the user making the request is unrestricted
                    or has access to this StackScript.
                  example: '{{mine}}'
                  readOnly: true
                  x-linode-filterable: true
                rev_note:
                  type: string
                  description: |
                    This field allows you to add notes for the set of revisions made to this StackScript.
                  example: '{{rev_note}}'
                  x-linode-filterable: true
                script:
                  type: string
                  description: |
                    The script to execute when provisioning a new Linode with this StackScript.
                  example: '{{script}}'
                  x-linode-cli-format: file
                updated:
                  type: string
                  description: |
                    __Read-only__ The date this StackScript was last updated.
                  example: '{{updated}}'
                  readOnly: true
                  format: date-time
                  x-linode-cli-display: 7
                user_defined_fields:
                  type: array
                  description: |
                    __Read-only__ This is a list of fields defined with a special syntax inside this StackScript
                    that allow for supplying customized parameters during deployment.
                    See [Declare User-Defined Fields (UDFs)](https://www.linode.com/docs/products/tools/stackscripts/guides/write-a-custom-script/#declare-user-defined-fields-udfs)
                    for more information.
                  example:
                    name: DB_PASSWORD
                    example: hunter2
                    label: Enter the DB password
                  readOnly: true
                  items:
                    type: object
                    description: |
                      A custom field defined by the User with a special syntax within a StackScript. Derived
                      from the contents of the script.
                    x-akamai:
                      file-path: schemas/user-defined-field.yaml
                    additionalProperties: false
                    required:
                    - label
                    - name
                    - example
                    properties:
                      name:
                        type: string
                        description: |
                          __Read-only__ The name of the field.
                        example: DB_PASSWORD
                        readOnly: true
                      example:
                        type: string
                        description: |
                          __Read-only__ An example value for the field.
                        example: hunter2
                        readOnly: true
                      default:
                        type: string
                        description: |
                          __Read-only__ The default value.  If not specified, this value will be used.
                        readOnly: true
                      oneOf:
                        type: string
                        description: |
                          __Read-only__ A list of acceptable single values for the field.
                        example: avalue,anothervalue,thirdvalue
                        readOnly: true
                      label:
                        type: string
                        description: |
                          __Read-only__ A human-readable label for the field that will serve as the
                          input prompt for entering the value during deployment.
                        example: Enter the password
                        readOnly: true
                      manyOf:
                        type: string
                        description: |
                          __Read-only__ A list of acceptable values for the field in any quantity,
                          combination or order.
                        example: avalue,anothervalue,thirdvalue
                        readOnly: true
                user_gravatar_id:
                  type: string
                  description: |
                    __Read-only__ The Gravatar ID for the User who created the StackScript.
                  example: '{{user_gravatar_id}}'
                  readOnly: true
                username:
                  type: string
                  description: |
                    __Read-only__ The User who created the StackScript.
                  example: '{{username}}'
                  readOnly: true
                  x-linode-cli-display: 2
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            StackScript was successfully modified.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A StackScript enables you to quickly deploy a fully-configured application in an automated
                  manner.
                x-akamai:
                  file-path: schemas/stack-script.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The unique ID of this StackScript.
                    example: 10079
                    readOnly: true
                    x-linode-cli-display: 1
                  description:
                    type: string
                    description: |
                      A description for the StackScript.
                    example: This StackScript installs and configures MySQL
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      __Read-only__ The date this StackScript was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 6
                  deployments_active:
                    type: integer
                    description: |
                      __Read-only__ Count of currently active, deployed Linodes created from this StackScript.
                    example: 1
                    readOnly: true
                  deployments_total:
                    type: integer
                    description: |
                      __Read-only__ The total number of times this StackScript has been deployed.
                    example: 12
                    readOnly: true
                    x-linode-filterable: true
                  images:
                    type: array
                    description: |-
                      An array of Image IDs. These are the Images that can be deployed with this StackScript.

                      `any/all` indicates that all available Images, including private Images, are accepted.
                    example:
                    - linode/debian9
                    - linode/debian8
                    items:
                      type: string
                    x-linode-cli-display: 4
                  is_public:
                    type: boolean
                    description: |
                      This determines whether other users can use your StackScript. __Once a StackScript is
                      made public, it cannot be made private.__
                    example: true
                    x-linode-cli-display: 5
                    x-linode-filterable: true
                  label:
                    type: string
                    description: |
                      The StackScript's label is for display purposes only.
                    example: a-stackscript
                    minLength: 3
                    maxLength: 128
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  mine:
                    type: boolean
                    description: |
                      __Read-only__ Returns `true` if this StackScript is owned by the account of the user
                      making the request, and the user making the request is unrestricted
                      or has access to this StackScript.
                    example: true
                    readOnly: true
                    x-linode-filterable: true
                  rev_note:
                    type: string
                    description: |
                      This field allows you to add notes for the set of revisions made to this StackScript.
                    example: Set up MySQL
                    x-linode-filterable: true
                  script:
                    type: string
                    description: |
                      The script to execute when provisioning a new Linode with this StackScript.
                    example: \"#!/bin/bash\"
                    x-linode-cli-format: file
                  updated:
                    type: string
                    description: |
                      __Read-only__ The date this StackScript was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 7
                  user_defined_fields:
                    type: array
                    description: |
                      __Read-only__ This is a list of fields defined with a special syntax inside this
                      StackScript that allow for supplying customized parameters during
                      deployment. See [Declare User-Defined Fields (UDFs)](https://www.linode.com/docs/products/tools/stackscripts/guides/write-a-custom-script/#declare-user-defined-fields-udfs)
                      for more information.
                    example:
                      name: DB_PASSWORD
                      example: hunter2
                      label: Enter the DB password
                    readOnly: true
                    items:
                      type: object
                      description: |
                        A custom field defined by the User with a special syntax within a StackScript. Derived
                        from the contents of the script.
                      x-akamai:
                        file-path: schemas/user-defined-field.yaml
                      additionalProperties: false
                      required:
                      - label
                      - name
                      - example
                      properties:
                        name:
                          type: string
                          description: |
                            __Read-only__ The name of the field.
                          example: DB_PASSWORD
                          readOnly: true
                        example:
                          type: string
                          description: |
                            __Read-only__ An example value for the field.
                          example: hunter2
                          readOnly: true
                        default:
                          type: string
                          description: |
                            __Read-only__ The default value.  If not specified, this value will
                            be used.
                          readOnly: true
                        oneOf:
                          type: string
                          description: |
                            __Read-only__ A list of acceptable single values for the field.
                          example: avalue,anothervalue,thirdvalue
                          readOnly: true
                        label:
                          type: string
                          description: |
                            __Read-only__ A human-readable label for the field that will serve as
                            the input prompt for entering the value during deployment.
                          example: Enter the password
                          readOnly: true
                        manyOf:
                          type: string
                          description: |
                            __Read-only__ A list of acceptable values for the field in any quantity,
                            combination or order.
                          example: avalue,anothervalue,thirdvalue
                          readOnly: true
                  user_gravatar_id:
                    type: string
                    description: |
                      __Read-only__ The Gravatar ID for the User who created the StackScript.
                    example: a445b305abda30ebc766bc7fda037c37
                    readOnly: true
                  username:
                    type: string
                    description: |
                      __Read-only__ The User who created the StackScript.
                    example: myuser
                    readOnly: true
                    x-linode-cli-display: 2
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-stack-script
      security:
      - personalAccessToken: []
      - oauth:
        - stackscripts:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "label": "a-stackscript",
                "description": "This StackScript installs and configures MySQL",
                "images": [
                  "linode/debian9",
                  "linode/debian8"
                ],
                "is_public": true,
                "rev_note": "Set up MySQL",
                "script": "#!/bin/bash"
              }' \
              https://api.linode.com/v4/linode/stackscripts/10079
      - lang: CLI
        source: |-
          linode-cli stackscripts update 10079 \
            --label a-stackscript \
            --description "This StackScript installs \
              and configures MySQL" \
            --images "linode/debian9" \
            --images "linode/debian8" \
            --is_public true \
            --rev_note "Set up MySQL" \
            --script '#!/bin/bash'
      x-linode-cli-action: update
      x-linode-grant: read_write
      x-original-op-id: updateStackScript
      x-original-op-title: StackScript Update
    delete:
      operationId: delete-stack-script
      summary: Delete a StackScript
      tags:
      - StackScripts
      description: |-
        Deletes a private StackScript you have permission to `read_write`. You cannot delete a public StackScript.


        ---


        - __OAuth__.

            ```
            stackscripts:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: stackscripts:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            StackScript was deleted.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-stack-script
      security:
      - personalAccessToken: []
      - oauth:
        - stackscripts:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/linode/stackscripts/10079
      - lang: CLI
        source: linode-cli stackscripts delete 10079
      x-linode-cli-action:
      - delete
      - rm
      x-linode-grant: read_write
      x-original-op-id: deleteStackScript
      x-original-op-title: StackScript Delete
    x-linode-cli-command: stackscripts
  /linode/types:
    x-akamai:
      file-path: paths/linode-types.yaml
      path-info: /linode/types
    get:
      operationId: get-linode-types
      summary: List types
      tags:
      - Linode types
      description: |
        Returns collection of Linode Types, including pricing and specifications for each Type. These are used when [creating](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
        or [resizing](https://techdocs.akamai.com/linode-api/reference/post-resize-linode-instance)
        Linodes.
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A collection of Linode Types.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        Returns collection of Linode types, including pricing and specifications for
                        each type. These are used when [creating](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
                        or [resizing](https://techdocs.akamai.com/linode-api/reference/post-resize-linode-instance)
                        Linodes.
                      x-akamai:
                        file-path: schemas/linode-type.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: string
                          description: |
                            __Read-only__ The ID representing the Linode Type.
                          example: g6-standard-2
                          readOnly: true
                          x-linode-cli-display: 1
                        addons:
                          type: object
                          description: |
                            __Read-only__ A list of optional add-on services for Linodes and their
                            associated costs.
                          additionalProperties: false
                          readOnly: true
                          properties:
                            backups:
                              type: object
                              description: |
                                __Read-only__ Information about the optional Backup service offered
                                for Linodes.
                              additionalProperties: false
                              readOnly: true
                              properties:
                                price:
                                  type: object
                                  description: |-
                                    The default cost of enabling Backups for this Linode Type. Prices are in US dollars, broken down into hourly and monthly charges.

                                    Certain Regions have different prices from the default. For Region-specific prices, see `region_prices`.
                                  additionalProperties: false
                                  properties:
                                    hourly:
                                      type: number
                                      description: |
                                        The cost (in US dollars) per hour to add Backups
                                        service.
                                      example: 0.008
                                    monthly:
                                      type: number
                                      description: |
                                        The cost (in US dollars) per month to add Backups
                                        service.
                                      example: 5
                                region_prices:
                                  type: array
                                  items:
                                    type: object
                                    additionalProperties: false
                                    properties:
                                      id:
                                        type: string
                                        description: |
                                          The Region ID for these prices.
                                        example: us-east
                                      hourly:
                                        type: number
                                        description: |
                                          Cost (in US dollars) per hour to add Backups
                                          service in this Region.
                                        example: 0.0096
                                      monthly:
                                        type: number
                                        description: |
                                          Cost (in US dollars) per month to add Backups
                                          service in this Region.
                                        example: 6
                        class:
                          type: string
                          description: |-
                            __Read-only__ The class of the Linode Type.

                            We currently offer six classes of compute instances:

                              - `nanode` - Nanode instances are good for low-duty workloads, where performance isn't critical. __Note:__ As of June 16th, 2020, Nanodes became 1 GB Linodes in the Cloud Manager, however, the API, the CLI, and billing will continue to refer to these instances as Nanodes.
                              - `standard` - Standard Shared instances are good for medium-duty workloads and are a good mix of performance, resources, and price. __Note:__ As of June 16th, 2020, Standard Linodes in the Cloud Manager became Shared Linodes, however, the API, the CLI, and billing will continue to refer to these instances as Standard Linodes.
                              - `dedicated` - Dedicated CPU instances are good for full-duty workloads where consistent performance is important.
                              - `premium` (limited Regions) - In addition to the features of Dedicated instances, Premium instances come equipped with the latest AMD EPYC&trade; CPUs, ensuring your applications are running on the latest hardware with consistently high performance. Only available in [regions](https://techdocs.akamai.com/linode-api/reference/get-regions) with "Premium Plans" in their `capabilities`
                              - `gpu` (limited Regions) - Linodes with dedicated NVIDIA Quadro&reg; RTX 6000 GPUs accelerate highly specialized applications such as machine learning, AI, and video transcoding. Only available in [regions](https://techdocs.akamai.com/linode-api/reference/get-regions) with "GPU Linodes" in their `capabilities`
                              - `highmem` - High Memory instances favor RAM over other resources, and can be good for memory hungry use cases like caching and in-memory databases. All High Memory plans contain dedicated CPU cores.
                          example: standard
                          readOnly: true
                          enum:
                          - nanode
                          - standard
                          - dedicated
                          - premium
                          - gpu
                          - highmem
                          x-linode-cli-display: 3
                          x-linode-filterable: true
                        disk:
                          type: integer
                          description: |
                            __Read-only__ The Disk size, in MB, of the Linode Type.
                          example: 81920
                          readOnly: true
                          x-linode-cli-display: 4
                          x-linode-filterable: true
                        gpus:
                          type: integer
                          description: |
                            __Read-only__ The number of GPUs this Linode Type offers.
                          example: 0
                          readOnly: true
                          x-linode-cli-display: 6.5
                          x-linode-filterable: true
                        label:
                          type: string
                          description: |
                            __Read-only__ The Linode Type's label is for display purposes only.
                          example: Linode 4GB
                          readOnly: true
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        memory:
                          type: integer
                          description: |
                            __Read-only__ Amount of RAM included in this Linode Type.
                          example: 4096
                          readOnly: true
                          x-linode-cli-display: 5
                          x-linode-filterable: true
                        network_out:
                          type: integer
                          description: |
                            __Read-only__ The Mbits outbound bandwidth allocation.
                          example: 1000
                          readOnly: true
                          x-linode-cli-display: 7
                          x-linode-filterable: true
                        price:
                          type: object
                          description: |-
                            __Read-only__ The default cost of provisioning this Linode Type. Prices are in US dollars, broken down into hourly and monthly charges.

                            Certain Regions have different prices from the default. For Region-specific prices, see `region_prices`.
                          additionalProperties: false
                          readOnly: true
                          properties:
                            hourly:
                              type: number
                              description: |
                                Cost (in US dollars) per hour.
                              example: 0.03
                              x-linode-cli-display: 9
                            monthly:
                              type: number
                              description: |
                                Cost (in US dollars) per month.
                              example: 20
                              x-linode-cli-display: 10
                        region_prices:
                          type: array
                          items:
                            type: object
                            additionalProperties: false
                            properties:
                              id:
                                type: string
                                description: |
                                  The Region ID for these prices.
                                example: us-east
                              hourly:
                                type: number
                                description: |
                                  Cost (in US dollars) per hour for this Region.
                                example: 0.036
                              monthly:
                                type: number
                                description: |
                                  Cost (in US dollars) per month for this Region.
                                example: 24
                        successor:
                          type: string
                          description: |
                            __Read-only__ The Linode Type that a [mutate](https://techdocs.akamai.com/linode-api/reference/post-mutate-linode-instance)
                            will upgrade to for a Linode of this type.  If "null",
                            a Linode of this type may not mutate.
                          example: null
                          nullable: true
                          readOnly: true
                        transfer:
                          type: integer
                          description: |
                            __Read-only__ The monthly outbound transfer amount, in MB.
                          example: 4000
                          readOnly: true
                          x-linode-cli-display: 8
                          x-linode-filterable: true
                        vcpus:
                          type: integer
                          description: |
                            __Read-only__ The number of VCPU cores this Linode Type offers.
                          example: 2
                          readOnly: true
                          x-linode-cli-display: 6
                          x-linode-filterable: true
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-linode-types
      x-code-samples:
      - lang: Shell
        source: curl https://api.linode.com/v4/linode/types
      - lang: CLI
        source: linode-cli linodes types
      x-linode-cli-action: types
      x-linode-redoc-load-ids: true
      x-original-op-id: getLinodeTypes
      x-original-op-title: Types List
    x-linode-cli-command: linodes
  /linode/types/{typeId}:
    x-akamai:
      file-path: paths/linode-type.yaml
      path-info: /linode/types/{typeId}
    parameters:
    - name: typeId
      in: path
      description: |
        The ID of the Linode Type to look up.
      example: '{{typeId}}'
      x-akamai:
        file-path: parameters/type-id-path-ef9c5d29.yaml
      required: true
      schema:
        type: string
    get:
      operationId: get-linode-type
      summary: Get a type
      tags:
      - Linode types
      description: |
        Returns information about a specific Linode Type, including pricing and specifications. This is used when [creating](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
        or [resizing](https://techdocs.akamai.com/linode-api/reference/post-resize-linode-instance)
        Linodes.
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A single Linode Type.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Returns collection of Linode types, including pricing and specifications for each type. These
                  are used when [creating](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
                  or [resizing](https://techdocs.akamai.com/linode-api/reference/post-resize-linode-instance)
                  Linodes.
                x-akamai:
                  file-path: schemas/linode-type.yaml
                additionalProperties: false
                properties:
                  id:
                    type: string
                    description: |
                      __Read-only__ The ID representing the Linode Type.
                    example: g6-standard-2
                    readOnly: true
                    x-linode-cli-display: 1
                  addons:
                    type: object
                    description: |
                      __Read-only__ A list of optional add-on services for Linodes and their associated
                      costs.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      backups:
                        type: object
                        description: |
                          __Read-only__ Information about the optional Backup service offered for Linodes.
                        additionalProperties: false
                        readOnly: true
                        properties:
                          price:
                            type: object
                            description: |-
                              The default cost of enabling Backups for this Linode Type. Prices are in US dollars, broken down into hourly and monthly charges.

                              Certain Regions have different prices from the default. For Region-specific prices, see `region_prices`.
                            additionalProperties: false
                            properties:
                              hourly:
                                type: number
                                description: |
                                  The cost (in US dollars) per hour to add Backups service.
                                example: 0.008
                              monthly:
                                type: number
                                description: |
                                  The cost (in US dollars) per month to add Backups service.
                                example: 5
                          region_prices:
                            type: array
                            items:
                              type: object
                              additionalProperties: false
                              properties:
                                id:
                                  type: string
                                  description: |
                                    The Region ID for these prices.
                                  example: us-east
                                hourly:
                                  type: number
                                  description: |
                                    Cost (in US dollars) per hour to add Backups service
                                    in this Region.
                                  example: 0.0096
                                monthly:
                                  type: number
                                  description: |
                                    Cost (in US dollars) per month to add Backups service
                                    in this Region.
                                  example: 6
                  class:
                    type: string
                    description: |-
                      __Read-only__ The class of the Linode Type.

                      We currently offer six classes of compute instances:

                        - `nanode` - Nanode instances are good for low-duty workloads, where performance isn't critical. __Note:__ As of June 16th, 2020, Nanodes became 1 GB Linodes in the Cloud Manager, however, the API, the CLI, and billing will continue to refer to these instances as Nanodes.
                        - `standard` - Standard Shared instances are good for medium-duty workloads and are a good mix of performance, resources, and price. __Note:__ As of June 16th, 2020, Standard Linodes in the Cloud Manager became Shared Linodes, however, the API, the CLI, and billing will continue to refer to these instances as Standard Linodes.
                        - `dedicated` - Dedicated CPU instances are good for full-duty workloads where consistent performance is important.
                        - `premium` (limited Regions) - In addition to the features of Dedicated instances, Premium instances come equipped with the latest AMD EPYC&trade; CPUs, ensuring your applications are running on the latest hardware with consistently high performance. Only available in [regions](https://techdocs.akamai.com/linode-api/reference/get-regions) with "Premium Plans" in their `capabilities`
                        - `gpu` (limited Regions) - Linodes with dedicated NVIDIA Quadro&reg; RTX 6000 GPUs accelerate highly specialized applications such as machine learning, AI, and video transcoding. Only available in [regions](https://techdocs.akamai.com/linode-api/reference/get-regions) with "GPU Linodes" in their `capabilities`
                        - `highmem` - High Memory instances favor RAM over other resources, and can be good for memory hungry use cases like caching and in-memory databases. All High Memory plans contain dedicated CPU cores.
                    example: standard
                    readOnly: true
                    enum:
                    - nanode
                    - standard
                    - dedicated
                    - premium
                    - gpu
                    - highmem
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  disk:
                    type: integer
                    description: |
                      __Read-only__ The Disk size, in MB, of the Linode Type.
                    example: 81920
                    readOnly: true
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  gpus:
                    type: integer
                    description: |
                      __Read-only__ The number of GPUs this Linode Type offers.
                    example: 0
                    readOnly: true
                    x-linode-cli-display: 6.5
                    x-linode-filterable: true
                  label:
                    type: string
                    description: |
                      __Read-only__ The Linode Type's label is for display purposes only.
                    example: Linode 4GB
                    readOnly: true
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  memory:
                    type: integer
                    description: |
                      __Read-only__ Amount of RAM included in this Linode Type.
                    example: 4096
                    readOnly: true
                    x-linode-cli-display: 5
                    x-linode-filterable: true
                  network_out:
                    type: integer
                    description: |
                      __Read-only__ The Mbits outbound bandwidth allocation.
                    example: 1000
                    readOnly: true
                    x-linode-cli-display: 7
                    x-linode-filterable: true
                  price:
                    type: object
                    description: |-
                      __Read-only__ The default cost of provisioning this Linode Type. Prices are in US dollars, broken down into hourly and monthly charges.

                      Certain Regions have different prices from the default. For Region-specific prices, see `region_prices`.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      hourly:
                        type: number
                        description: |
                          Cost (in US dollars) per hour.
                        example: 0.03
                        x-linode-cli-display: 9
                      monthly:
                        type: number
                        description: |
                          Cost (in US dollars) per month.
                        example: 20
                        x-linode-cli-display: 10
                  region_prices:
                    type: array
                    items:
                      type: object
                      additionalProperties: false
                      properties:
                        id:
                          type: string
                          description: |
                            The Region ID for these prices.
                          example: us-east
                        hourly:
                          type: number
                          description: |
                            Cost (in US dollars) per hour for this Region.
                          example: 0.036
                        monthly:
                          type: number
                          description: |
                            Cost (in US dollars) per month for this Region.
                          example: 24
                  successor:
                    type: string
                    description: |
                      __Read-only__ The Linode Type that a [mutate](https://techdocs.akamai.com/linode-api/reference/post-mutate-linode-instance)
                      will upgrade to for a Linode of this type.  If "null", a Linode
                      of this type may not mutate.
                    example: null
                    nullable: true
                    readOnly: true
                  transfer:
                    type: integer
                    description: |
                      __Read-only__ The monthly outbound transfer amount, in MB.
                    example: 4000
                    readOnly: true
                    x-linode-cli-display: 8
                    x-linode-filterable: true
                  vcpus:
                    type: integer
                    description: |
                      __Read-only__ The number of VCPU cores this Linode Type offers.
                    example: 2
                    readOnly: true
                    x-linode-cli-display: 6
                    x-linode-filterable: true
              x-example:
                x-ref: ../examples/tbd.json
              x-linode-cli-subtables:
              - region_prices
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-linode-type
      x-code-samples:
      - lang: Shell
        source: curl https://api.linode.com/v4/linode/types/g6-standard-2
      - lang: CLI
        source: linode-cli linodes type-view g6-standard-2
      x-linode-cli-action: type-view
      x-original-op-id: getLinodeType
      x-original-op-title: Type View
    x-linode-cli-command: linodes
  /lke/clusters:
    x-akamai:
      file-path: paths/lke-clusters.yaml
      path-info: /lke/clusters
    post:
      operationId: post-lke-cluster
      summary: Create a Kubernetes cluster
      tags:
      - Clusters
      description: |-
        Creates a Kubernetes cluster. The Kubernetes cluster will be created asynchronously. You can use the events system to determine when the Kubernetes cluster is ready to use. Please note that it often takes 2-5 minutes before the [Kubernetes API endpoints](https://techdocs.akamai.com/linode-api/reference/get-lke-cluster-api-endpoints) and the [Kubeconfig file](https://techdocs.akamai.com/linode-api/reference/get-lke-cluster-kubeconfig) for the new cluster are ready.


        ---


        - __CLI__.

            ```
            linode-cli lke cluster-create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            lke:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli lke cluster-create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: lke:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Configuration for the Kubernetes cluster
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - label
              - region
              - k8s_version
              - node_pools
              properties:
                tags:
                  type: array
                  description: |
                    An array of tags applied to the Kubernetes cluster. Tags are for organizational purposes
                    only.
                  example:
                  - ecomm
                  - blogs
                  items:
                    type: string
                  x-linode-filterable: true
                control_plane:
                  type: object
                  description: |
                    Defines settings for the Kubernetes Control Plane. Allows for the enabling of High
                    Availability (HA) for Control Plane Components. Enabling High
                    Availability for LKE is an __irreversible__ change.
                  additionalProperties: false
                  properties:
                    high_availability:
                      type: boolean
                      description: |
                        Defines whether High Availability is enabled for the Control Plane Components of
                        the cluster. Defaults to `false`.
                      example: true
                k8s_version:
                  type: string
                  description: |
                    The desired Kubernetes version for this Kubernetes cluster in the format of &lt;major&gt;.&lt;minor&gt;,
                    and the latest supported patch version will be deployed.
                  example: '{{k8s_version}}'
                  x-linode-cli-display: 4
                  x-linode-filterable: true
                label:
                  type: string
                  description: |-
                    This Kubernetes cluster's unique label for display purposes only. Labels have the following constraints:

                      - UTF-8 characters will be returned by the API using escape sequences of their Unicode code points. For example, the Japanese character _か_ is 3 bytes in UTF-8 (`0xE382AB`). Its Unicode code point is 2 bytes (`0x30AB`). APIv4 supports this character and the API will return it as the escape sequence using six 1 byte characters which represent 2 bytes of Unicode code point (`"\u30ab"`).

                      - 4 byte UTF-8 characters are not supported.

                      - If the label is entirely composed of UTF-8 characters, the API response will return the code points using up to 193 1 byte characters.
                  example: '{{label}}'
                  minLength: 1
                  maxLength: 32
                  x-linode-cli-display: 2
                  x-linode-filterable: true
                node_pools:
                  type: array
                  required:
                  - type
                  - count
                  items:
                    type: object
                    description: |
                      Specifies a collection of Linodes which will be members of a Kubernetes cluster.
                    x-akamai:
                      file-path: schemas/lke-node-pool-request-body.yaml
                    additionalProperties: false
                    properties:
                      tags:
                        type: array
                        description: |
                          An array of tags applied to this object. Tags are for organizational purposes
                          only.
                        example:
                        - example tag
                        - another example
                        items:
                          type: string
                        x-linode-filterable: true
                      type:
                        type: string
                        description: |
                          The Linode Type for all of the nodes in the Node Pool.
                        example: g6-standard-4
                      autoscaler:
                        type: object
                        description: |-
                          When enabled, the number of nodes autoscales within the defined minimum and maximum values.

                          When making a request, `max` and `min` require each other.
                        additionalProperties: false
                        properties:
                          enabled:
                            type: boolean
                            description: |
                              Whether autoscaling is enabled for this Node Pool. Defaults to `false`.
                            example: true
                          max:
                            type: integer
                            description: |
                              The maximum number of nodes to autoscale to. Defaults to the Node
                              Pool's `count`. Defaults to the value provided by the
                              `count` field.
                            example: 12
                            minimum: 1
                            maximum: 100
                          min:
                            type: integer
                            description: |
                              The minimum number of nodes to autoscale to. Defaults to the Node
                              Pool's `count`.
                            example: 3
                            minimum: 1
                            maximum: 100
                      count:
                        type: integer
                        description: |
                          The number of nodes in the Node Pool.
                        example: 6
                        minimum: 1
                        maximum: 100
                      disks:
                        type: array
                        description: |-
                          __Note__: This field should be omitted except for special use cases. The disks specified here are partitions in _addition_ to the primary partition and reduce the size of the primary partition, which can lead to stability problems for the Node.

                          This Node Pool's custom disk layout. Each item in this array will create a new disk partition for each node in this Node Pool.

                            - The custom disk layout is applied to each node in this Node Pool.
                            - The maximum number of custom disk partitions that can be configured is 7.
                            - Once the requested disk partitions are allocated, the remaining disk space is allocated to the node's boot disk.
                            - A Node Pool's custom disk layout is immutable over the lifetime of the Node Pool.
                        items:
                          type: object
                          description: |
                            The values to assign to each partition in this Node Pool's custom disk
                            layout.
                          additionalProperties: false
                          properties:
                            type:
                              type: string
                              description: |
                                This custom disk partition's filesystem type.
                              example: ext-4
                              enum:
                              - raw
                              - ext4
                            size:
                              type: integer
                              description: |-
                                The size of this custom disk partition in MB.

                                  - The size of this disk partition must not exceed the capacity of the node's plan type.
                              example: 1024
                        x-linode-cli-format: json
                region:
                  type: string
                  description: |
                    This Kubernetes cluster's location.
                  example: '{{region}}'
                  x-linode-cli-display: 3
                  x-linode-filterable: true
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
        x-linode-cli-allowed-defaults:
        - region
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Kubernetes cluster creation has started.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A Kubernetes cluster.
                x-akamai:
                  file-path: schemas/lke-cluster.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Kubernetes cluster's unique ID.
                    example: 1234
                    readOnly: true
                    x-linode-cli-display: 1
                  tags:
                    type: array
                    description: |
                      An array of tags applied to the Kubernetes cluster. Tags are for organizational purposes
                      only.
                    example:
                    - ecomm
                    - blogs
                    items:
                      type: string
                    x-linode-filterable: true
                  control_plane:
                    type: object
                    description: |
                      Defines settings for the Kubernetes Control Plane. Allows for the enabling of High
                      Availability (HA) for Control Plane Components. Enabling High
                      Availability for LKE is an __irreversible__ change.
                    additionalProperties: false
                    properties:
                      high_availability:
                        type: boolean
                        description: |
                          Defines whether High Availability is enabled for the Control Plane Components
                          of the cluster. Defaults to `false`.
                        example: true
                        x-linode-cli-display: 5
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Kubernetes cluster was created.
                    example: '2019-09-12T21:25:30Z'
                    readOnly: true
                    format: date-time
                  k8s_version:
                    type: string
                    description: |
                      The desired Kubernetes version for this Kubernetes cluster in the format of &lt;major&gt;.&lt;minor&gt;,
                      and the latest supported patch version will be deployed.
                    example: '1.27'
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  label:
                    type: string
                    description: |-
                      This Kubernetes cluster's unique label for display purposes only. Labels have the following constraints:

                        - UTF-8 characters will be returned by the API using escape sequences of their Unicode code points. For example, the Japanese character _か_ is 3 bytes in UTF-8 (`0xE382AB`). Its Unicode code point is 2 bytes (`0x30AB`). APIv4 supports this character and the API will return it as the escape sequence using six 1 byte characters which represent 2 bytes of Unicode code point (`"\u30ab"`).

                        - 4 byte UTF-8 characters are not supported.

                        - If the label is entirely composed of UTF-8 characters, the API response will return the code points using up to 193 1 byte characters.
                    example: lkecluster12345
                    minLength: 1
                    maxLength: 32
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  region:
                    type: string
                    description: |
                      This Kubernetes cluster's location.
                    example: us-central
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Kubernetes cluster was updated.
                    example: '2019-09-13T21:24:16Z'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-lke-cluster
      security:
      - personalAccessToken: []
      - oauth:
        - lke:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
                -H "Authorization: Bearer $TOKEN" \
                -X POST -d '{
                  "label": "cluster12345",
                  "region": "us-central",
                  "k8s_version": "1.27",
                  "tags": ["ecomm", "blogs"],
                  "control_plane": {
                      "high_availability": true
                    },
                  "node_pools": [
                    {
                      "type": "g6-standard-4",
                      "count": 6,
                      "autoscaler": {
                        "enabled": true,
                        "max": 12,
                        "min": 3
                      }
                    },
                    {
                      "type": "g6-standard-8",
                      "count": 3
                    }
                  ]
                }' \
                https://api.linode.com/v4/lke/clusters
      - lang: CLI
        source: |-
          linode-cli lke cluster-create \
            --label cluster12345 \
            --region us-central \
            --k8s_version 1.27 \
            --control_plane.high_availability true \
            --node_pools.type g6-standard-4 --node_pools.count 6 \
            --node_pools.type g6-standard-8 --node_pools.count 3 \
            --node_pools.autoscaler.enabled true \
            --node_pools.autoscaler.max 12 \
            --node_pools.autoscaler.min 3 \
            --tags ecomm
      x-linode-charge: true
      x-linode-cli-action: cluster-create
      x-linode-grant: add_clusters
      x-original-op-id: createLKECluster
      x-original-op-title: Kubernetes Cluster Create
    get:
      operationId: get-lke-clusters
      summary: List Kubernetes clusters
      tags:
      - Clusters
      description: |-
        Lists current Kubernetes clusters available on your account.


        ---


        - __CLI__.

            ```
            linode-cli lke clusters-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            lke:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli lke clusters-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: lke:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns an array of all Kubernetes clusters on your Account.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        A Kubernetes cluster.
                      x-akamai:
                        file-path: schemas/lke-cluster.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ This Kubernetes cluster's unique ID.
                          example: 1234
                          readOnly: true
                          x-linode-cli-display: 1
                        tags:
                          type: array
                          description: |
                            An array of tags applied to the Kubernetes cluster. Tags are for organizational
                            purposes only.
                          example:
                          - ecomm
                          - blogs
                          items:
                            type: string
                          x-linode-filterable: true
                        control_plane:
                          type: object
                          description: |
                            Defines settings for the Kubernetes Control Plane. Allows for the enabling
                            of High Availability (HA) for Control Plane Components.
                            Enabling High Availability for LKE is an __irreversible__
                            change.
                          additionalProperties: false
                          properties:
                            high_availability:
                              type: boolean
                              description: |
                                Defines whether High Availability is enabled for the Control Plane
                                Components of the cluster. Defaults to `false`.
                              example: true
                              x-linode-cli-display: 5
                        created:
                          type: string
                          description: |
                            __Read-only__ When this Kubernetes cluster was created.
                          example: '2019-09-12T21:25:30Z'
                          readOnly: true
                          format: date-time
                        k8s_version:
                          type: string
                          description: |
                            The desired Kubernetes version for this Kubernetes cluster in the format of &lt;major&gt;.&lt;minor&gt;,
                            and the latest supported patch version will be deployed.
                          example: '1.27'
                          x-linode-cli-display: 4
                          x-linode-filterable: true
                        label:
                          type: string
                          description: |-
                            This Kubernetes cluster's unique label for display purposes only. Labels have the following constraints:

                              - UTF-8 characters will be returned by the API using escape sequences of their Unicode code points. For example, the Japanese character _か_ is 3 bytes in UTF-8 (`0xE382AB`). Its Unicode code point is 2 bytes (`0x30AB`). APIv4 supports this character and the API will return it as the escape sequence using six 1 byte characters which represent 2 bytes of Unicode code point (`"\u30ab"`).

                              - 4 byte UTF-8 characters are not supported.

                              - If the label is entirely composed of UTF-8 characters, the API response will return the code points using up to 193 1 byte characters.
                          example: lkecluster12345
                          minLength: 1
                          maxLength: 32
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        region:
                          type: string
                          description: |
                            This Kubernetes cluster's location.
                          example: us-central
                          x-linode-cli-display: 3
                          x-linode-filterable: true
                        updated:
                          type: string
                          description: |
                            __Read-only__ When this Kubernetes cluster was updated.
                          example: '2019-09-13T21:24:16Z'
                          readOnly: true
                          format: date-time
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-lke-clusters
      security:
      - personalAccessToken: []
      - oauth:
        - lke:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
                https://api.linode.com/v4/lke/clusters
      - lang: CLI
        source: linode-cli lke clusters-list
      x-linode-cli-action: clusters-list
      x-linode-grant: read_only
      x-original-op-id: getLKEClusters
      x-original-op-title: Kubernetes Clusters List
    x-linode-cli-command: lke
  /lke/clusters/{clusterId}:
    x-akamai:
      file-path: paths/cluster.yaml
      path-info: /lke/clusters/{clusterId}
    parameters:
    - name: clusterId
      in: path
      description: |
        ID of the Kubernetes cluster to look up.
      example: '{{clusterId}}'
      x-akamai:
        file-path: parameters/cluster-id-path.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-lke-cluster
      summary: Get a Kubernetes cluster
      tags:
      - Clusters
      description: |-
        Get a specific Cluster by ID.


        ---


        - __CLI__.

            ```
            linode-cli lke cluster-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            lke:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli lke cluster-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: lke:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a single Kubernetes cluster.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A Kubernetes cluster.
                x-akamai:
                  file-path: schemas/lke-cluster.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Kubernetes cluster's unique ID.
                    example: 1234
                    readOnly: true
                    x-linode-cli-display: 1
                  tags:
                    type: array
                    description: |
                      An array of tags applied to the Kubernetes cluster. Tags are for organizational purposes
                      only.
                    example:
                    - ecomm
                    - blogs
                    items:
                      type: string
                    x-linode-filterable: true
                  control_plane:
                    type: object
                    description: |
                      Defines settings for the Kubernetes Control Plane. Allows for the enabling of High
                      Availability (HA) for Control Plane Components. Enabling High
                      Availability for LKE is an __irreversible__ change.
                    additionalProperties: false
                    properties:
                      high_availability:
                        type: boolean
                        description: |
                          Defines whether High Availability is enabled for the Control Plane Components
                          of the cluster. Defaults to `false`.
                        example: true
                        x-linode-cli-display: 5
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Kubernetes cluster was created.
                    example: '2019-09-12T21:25:30Z'
                    readOnly: true
                    format: date-time
                  k8s_version:
                    type: string
                    description: |
                      The desired Kubernetes version for this Kubernetes cluster in the format of &lt;major&gt;.&lt;minor&gt;,
                      and the latest supported patch version will be deployed.
                    example: '1.27'
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  label:
                    type: string
                    description: |-
                      This Kubernetes cluster's unique label for display purposes only. Labels have the following constraints:

                        - UTF-8 characters will be returned by the API using escape sequences of their Unicode code points. For example, the Japanese character _か_ is 3 bytes in UTF-8 (`0xE382AB`). Its Unicode code point is 2 bytes (`0x30AB`). APIv4 supports this character and the API will return it as the escape sequence using six 1 byte characters which represent 2 bytes of Unicode code point (`"\u30ab"`).

                        - 4 byte UTF-8 characters are not supported.

                        - If the label is entirely composed of UTF-8 characters, the API response will return the code points using up to 193 1 byte characters.
                    example: lkecluster12345
                    minLength: 1
                    maxLength: 32
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  region:
                    type: string
                    description: |
                      This Kubernetes cluster's location.
                    example: us-central
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Kubernetes cluster was updated.
                    example: '2019-09-13T21:24:16Z'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-lke-cluster
      security:
      - personalAccessToken: []
      - oauth:
        - lke:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
                https://api.linode.com/v4/lke/clusters/12345
      - lang: CLI
        source: linode-cli lke cluster-view 12345
      x-linode-cli-action: cluster-view
      x-original-op-id: getLKECluster
      x-original-op-title: Kubernetes Cluster View
    put:
      operationId: put-lke-cluster
      summary: Update a Kubernetes cluster
      tags:
      - Clusters
      description: |-
        Updates a Kubernetes cluster.


        ---


        - __CLI__.

            ```
            linode-cli lke cluster-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            lke:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli lke cluster-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: lke:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The fields to update the Kubernetes cluster.
        content:
          application/json:
            schema:
              additionalProperties: false
              properties:
                tags:
                  type: array
                  description: |
                    An array of tags applied to the Kubernetes cluster. Tags are for organizational purposes
                    only. To delete a tag, exclude it from your `tags` array.
                  example:
                  - prod
                  - monitoring
                  - ecomm
                  - blog
                  items:
                    type: string
                control_plane:
                  type: object
                  description: |-
                    Defines settings for the Kubernetes Control Plane. Allows for the enabling of High Availability (HA) for Control Plane Components.

                    Enabling High Availability for LKE is an __irreversible__ change.

                    When upgrading pre-existing LKE clusters to use the HA Control Plane, the following changes will additionally occur:

                    - All nodes will be deleted and new nodes will be created to replace them.

                    - Any local storage (such as `hostPath` volumes) will be erased.

                    - The upgrade process may take several minutes to complete, as nodes will be replaced on a rolling basis.
                  additionalProperties: false
                  properties:
                    high_availability:
                      type: boolean
                      description: |
                        Defines whether High Availability is enabled for the Control Plane Components of
                        the cluster. Defaults to `false`.
                      example: true
                k8s_version:
                  type: string
                  description: |-
                    The desired Kubernetes version for this Kubernetes cluster in the format of &lt;major&gt;.&lt;minor&gt;. New and recycled Nodes in this cluster will be installed with the latest available patch for the Cluster's Kubernetes version.

                    When upgrading the Kubernetes version, only the next latest minor version following the current version can be deployed. For example, a cluster with Kubernetes version 1.19 can be upgraded to version 1.20, but not directly to 1.21.

                    The Kubernetes version of a cluster can not be downgraded.
                label:
                  type: string
                  description: |-
                    This Kubernetes cluster's unique label for display purposes only. Labels have the following constraints:

                      - UTF-8 characters will be returned by the API using escape sequences of their Unicode code points. For example, the Japanese character _か_ is 3 bytes in UTF-8 (`0xE382AB`). Its Unicode code point is 2 bytes (`0x30AB`). APIv4 supports this character and the API will return it as the escape sequence using six 1 byte characters which represent 2 bytes of Unicode code point (`"\u30ab"`).

                      - 4 byte UTF-8 characters are not supported.

                      - If the label is entirely composed of UTF-8 characters, the API response will return the code points using up to 193 1 byte characters.
                  example: lkecluster12345
                  minLength: 1
                  maxLength: 32
                  x-linode-cli-display: 2
                  x-linode-filterable: true
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        200:
          description: |
            Returns a single Kubernetes cluster.
          content:
            application/json:
              schema:
                additionalProperties: false
                properties:
                  tags:
                    type: array
                    description: |
                      An array of tags applied to the Kubernetes cluster. Tags are for organizational purposes
                      only. To delete a tag, exclude it from your `tags` array.
                    example:
                    - prod
                    - monitoring
                    - ecomm
                    - blog
                    items:
                      type: string
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Kubernetes cluster was created.
                    example: '2019-09-12T21:25:30Z'
                    readOnly: true
                    format: date-time
                  k8s_version:
                    type: string
                    description: |
                      The desired Kubernetes version for this Kubernetes cluster in the format of &lt;major&gt;.&lt;minor&gt;,
                      and the latest supported patch version will be deployed.
                    example: '1.27'
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  label:
                    type: string
                    description: |-
                      This Kubernetes cluster's unique label for display purposes only. Labels have the following constraints:

                        - UTF-8 characters will be returned by the API using escape sequences of their Unicode code points. For example, the Japanese character _か_ is 3 bytes in UTF-8 (`0xE382AB`). Its Unicode code point is 2 bytes (`0x30AB`). APIv4 supports this character and the API will return it as the escape sequence using six 1 byte characters which represent 2 bytes of Unicode code point (`"\u30ab"`).

                        - 4 byte UTF-8 characters are not supported.

                        - If the label is entirely composed of UTF-8 characters, the API response will return the code points using up to 193 1 byte characters.
                    example: lkecluster12345
                    minLength: 1
                    maxLength: 32
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  region:
                    type: string
                    description: |
                      This Kubernetes cluster's location.
                    example: us-central
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Kubernetes cluster was updated.
                    example: '2019-09-13T21:24:16Z'
                    readOnly: true
                    format: date-time
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-lke-cluster
      security:
      - personalAccessToken: []
      - oauth:
        - lke:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
                -H "Authorization: Bearer $TOKEN" \
                -X PUT -d '{
                  "label": "lkecluster54321",
                  "tags" : ["ecomm", "blog", "prod", "monitoring"],
                  "control_plane": {
                      "high_availability": true
                    },
                  "k8s_version": "1.27"
                }' \
                https://api.linode.com/v4/lke/clusters/12345
      - lang: CLI
        source: |-
          linode-cli lke cluster-update 12345 \
            --label lkecluster54321 \
            --control_plane.high_availability true \
            --k8s_version 1.27 \
            --tags ecomm \
            --tags blog \
            --tags prod \
            --tags monitoring
      x-linode-cli-action: cluster-update
      x-original-op-id: putLKECluster
      x-original-op-title: Kubernetes Cluster Update
    delete:
      operationId: delete-lke-cluster
      summary: Delete a Kubernetes cluster
      tags:
      - Clusters
      description: |-
        Deletes a Cluster you have permission to `read_write`.

        __Deleting a Cluster is a destructive action and cannot be undone.__

        Deleting a Cluster:

        - Deletes all Linodes in all pools within this Kubernetes cluster
        - Deletes all supporting Kubernetes services for this Kubernetes cluster (API server, etcd, etc)
        - Deletes all NodeBalancers created by this Kubernetes cluster
        - Does not delete any of the volumes created by this Kubernetes cluster


        ---


        - __CLI__.

            ```
            linode-cli lke cluster-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            lke:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli lke cluster-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: lke:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Delete successful
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-lke-cluster
      security:
      - personalAccessToken: []
      - oauth:
        - lke:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
                -X DELETE \
                https://api.linode.com/v4/lke/clusters/12345
      - lang: CLI
        source: linode-cli lke cluster-delete 12345
      x-linode-cli-action: cluster-delete
      x-original-op-id: deleteLKECluster
      x-original-op-title: Kubernetes Cluster Delete
    x-linode-cli-command: lke
  /lke/clusters/{clusterId}/api-endpoints:
    x-akamai:
      file-path: paths/api-endpoints.yaml
      path-info: /lke/clusters/{clusterId}/api-endpoints
    parameters:
    - name: clusterId
      in: path
      description: |
        ID of the Kubernetes cluster to look up.
      example: '{{clusterId}}'
      x-akamai:
        file-path: parameters/cluster-id-path.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-lke-cluster-api-endpoints
      summary: List Kubernetes API endpoints
      tags:
      - LKE API endpoints
      description: |-
        List the Kubernetes API server endpoints for this cluster. Please note that it often takes 2-5 minutes before the endpoint is ready after first [creating a new cluster](https://techdocs.akamai.com/linode-api/reference/post-lke-cluster).


        ---


        - __CLI__.

            ```
            linode-cli lke api-endpoints-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            lke:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli lke api-endpoints-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: lke:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns the Kubernetes API server endpoints for this cluster.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    description: |
                      The Kubernetes API server endpoints for this cluster.
                    items:
                      type: object
                      additionalProperties: false
                      properties:
                        endpoint:
                          type: string
                          description: |
                            A Kubernetes API server endpoint for this cluster.
                          example: https://192.0.2.1:6443
                          readOnly: true
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-lke-cluster-api-endpoints
      security:
      - personalAccessToken: []
      - oauth:
        - lke:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
                https://api.linode.com/v4/lke/clusters/12345/api-endpoints
      - lang: CLI
        source: linode-cli lke api-endpoints-list 12345
      x-linode-cli-action: api-endpoints-list
      x-original-op-id: getLKEClusterAPIEndpoints
      x-original-op-title: Kubernetes API Endpoints
        List
    x-linode-cli-command: lke
  /lke/clusters/{clusterId}/dashboard:
    x-akamai:
      file-path: paths/dashboard.yaml
      path-info: /lke/clusters/{clusterId}/dashboard
    parameters:
    - name: clusterId
      in: path
      description: |
        ID of the Kubernetes cluster to look up.
      example: '{{clusterId}}'
      x-akamai:
        file-path: parameters/cluster-id-path-28150853.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-lke-cluster-dashboard
      summary: Get a Kubernetes cluster dashboard URL
      tags:
      - Cluster dashboard
      description: |-
        Get a [Kubernetes Dashboard](https://github.com/kubernetes/dashboard) access URL for this Cluster, which enables performance of administrative tasks through a web interface.

        Dashboards are installed for Clusters by default.

        To access the Cluster Dashboard login prompt, enter the URL in a web browser. Select either __Token__ or __Kubeconfig__ authentication, then select __Sign in__.

        For additional guidance on using the Cluster Dashboard, see the [Navigating the Cluster Dashboard](https://www.linode.com/docs/guides/using-the-kubernetes-dashboard-on-lke/#navigating-the-cluster-dashboard) section of our guide on [Using the Kubernetes Dashboard on LKE](https://www.linode.com/docs/guides/using-the-kubernetes-dashboard-on-lke/).


        ---


        - __CLI__.

            ```
            linode-cli lke cluster-dashboard-url
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            lke:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli lke cluster-dashboard-url
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: lke:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a Kubernetes Cluster Dashboard URL.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  url:
                    type: string
                    description: |
                      The Cluster Dashboard access URL.
                    example: https://example.dashboard.linodelke.net
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-lke-cluster-dashboard
      security:
      - personalAccessToken: []
      - oauth:
        - lke:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
                https://api.linode.com/v4/lke/clusters/12345/dashboard
      - lang: CLI
        source: linode-cli lke cluster-dashboard-url 12345
      x-linode-cli-action: cluster-dashboard-url
      x-original-op-id: getLKEClusterDashboard
      x-original-op-title: Kubernetes Cluster Dashboard
        URL View
    x-linode-cli-command: lke
  /lke/clusters/{clusterId}/kubeconfig:
    x-akamai:
      file-path: paths/kubeconfig.yaml
      path-info: /lke/clusters/{clusterId}/kubeconfig
    parameters:
    - name: clusterId
      in: path
      description: |
        ID of the Kubernetes cluster to look up.
      example: '{{clusterId}}'
      x-akamai:
        file-path: parameters/cluster-id-path-28150853.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-lke-cluster-kubeconfig
      summary: Get a Kubeconfig
      tags:
      - Kubeconfigs
      description: |-
        Get the Kubeconfig file for a Cluster. Please note that it often takes 2-5 minutes before the Kubeconfig file is ready after first [creating a new cluster](https://techdocs.akamai.com/linode-api/reference/post-lke-cluster).


        ---


        - __CLI__.

            ```
            linode-cli lke kubeconfig-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            lke:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli lke kubeconfig-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: lke:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns the Base64-encoded Kubeconfig file for this Kubernetes cluster.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  kubeconfig:
                    type: string
                    description: |
                      The Base64-encoded Kubeconfig file for this Cluster.
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-lke-cluster-kubeconfig
      security:
      - personalAccessToken: []
      - oauth:
        - lke:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
                https://api.linode.com/v4/lke/clusters/12345/kubeconfig
      - lang: CLI
        source: linode-cli lke kubeconfig-view 12345
      x-linode-cli-action: kubeconfig-view
      x-original-op-id: getLKEClusterKubeconfig
      x-original-op-title: Kubeconfig View
    delete:
      operationId: delete-lke-cluster-kubeconfig
      summary: Delete a Kubeconfig
      tags:
      - Kubeconfigs
      description: |-
        Delete and regenerate the Kubeconfig file for a Cluster.


        ---


        - __CLI__.

            ```
            linode-cli lke kubeconfig-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            lke:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli lke kubeconfig-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: lke:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Kubeconfig file deleted and regenerated successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-lke-cluster-kubeconfig
      security:
      - personalAccessToken: []
      - oauth:
        - lke:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
                -X DELETE \
                https://api.linode.com/v4/lke/clusters/12345/kubeconfig
      - lang: CLI
        source: linode-cli lke kubeconfig-delete 12345
      x-linode-cli-action: kubeconfig-delete
      x-original-op-id: deleteLKEClusterKubeconfig
      x-original-op-title: Kubeconfig Delete
    x-linode-cli-command: lke
  /lke/clusters/{clusterId}/nodes/{nodeId}:
    x-akamai:
      file-path: paths/cluster-node.yaml
      path-info: /lke/clusters/{clusterId}/nodes/{nodeId}
    parameters:
    - name: clusterId
      in: path
      description: |
        ID of the Kubernetes cluster containing the Node.
      example: '{{clusterId}}'
      x-akamai:
        file-path: parameters/cluster-id-path-b0a6717a.yaml
      required: true
      schema:
        type: integer
    - name: nodeId
      in: path
      description: |
        The ID of the Node to access
      example: '{{nodeId}}'
      x-akamai:
        file-path: parameters/node-id.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-lke-cluster-node
      summary: Get a node
      tags:
      - Nodes
      description: |-
        Returns the values for a specified node object.


        ---


        - __CLI__.

            ```
            linode-cli lke node-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            lke:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli lke node-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: lke:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns the values of a node object in the form that it appears currently in the node pool array.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  id:
                    type: string
                    description: |
                      The Node's ID.
                    example: 12345-6aa78910bc
                    readOnly: true
                  instance_id:
                    type: integer
                    description: |
                      The Linode's ID. If no Linode is currently provisioned for this Node, this is `null`.
                    example: 123456
                  status:
                    type: string
                    description: |-
                      The creation status of this Node. This status is distinct from this Node's readiness as a Kubernetes Node Object as determined by the command `kubectl get nodes`.

                      `not_ready` indicates that the Linode is still being created.

                      `ready` indicates that the Linode has successfully been created and is running Kubernetes software.
                    example: ready
                    enum:
                    - ready
                    - not_ready
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-lke-cluster-node
      security:
      - personalAccessToken: []
      - oauth:
        - lke:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
                https://api.linode.com/v4/lke/clusters/12345/nodes/12345-6aa78910bc
      - lang: CLI
        source: linode-cli lke node-view 123456 12345-6aa78910bc
      x-linode-cli-action: node-view
      x-original-op-id: getLKEClusterNode
      x-original-op-title: Node View
    delete:
      operationId: delete-lke-cluster-node
      summary: Delete a node
      tags:
      - Nodes
      description: |-
        Deletes a specific Node from a Node Pool.

        __Deleting a Node is a destructive action and cannot be undone.__

        Deleting a Node will reduce the size of the Node Pool it belongs to.


        ---


        - __CLI__.

            ```
            linode-cli lke node-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            lke:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli lke node-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: lke:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Delete successful
          content:
            application/json:
              schema:
                type: object
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-lke-cluster-node
      security:
      - personalAccessToken: []
      - oauth:
        - lke:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
                -X DELETE \
                https://api.linode.com/v4/lke/clusters/12345/nodes/12345-6aa78910bc
      - lang: CLI
        source: linode-cli lke node-delete 12345 12345-6aa78910bc
      x-linode-cli-action: node-delete
      x-original-op-id: deleteLKEClusterNode
      x-original-op-title: Node Delete
    x-linode-cli-command: lke
  /lke/clusters/{clusterId}/nodes/{nodeId}/recycle:
    x-akamai:
      file-path: paths/node-recycle.yaml
      path-info: /lke/clusters/{clusterId}/nodes/{nodeId}/recycle
    parameters:
    - name: clusterId
      in: path
      description: |
        ID of the Kubernetes cluster containing the Node.
      example: '{{clusterId}}'
      x-akamai:
        file-path: parameters/cluster-id-path-b0a6717a.yaml
      required: true
      schema:
        type: integer
    - name: nodeId
      in: path
      description: |
        ID of the Node to be recycled.
      example: '{{nodeId}}'
      x-akamai:
        file-path: parameters/node-id-recycle.yaml
      required: true
      schema:
        type: string
    post:
      operationId: post-lke-cluster-node-recycle
      summary: Recycle a node
      tags:
      - Nodes
      description: |-
        Recycles an individual Node in the designated Kubernetes Cluster. The Node will be deleted and replaced with a new Linode, which may take a few minutes. Replacement Nodes are installed with the latest available patch for the Cluster's Kubernetes Version.

        __Any local storage on deleted Linodes (such as "hostPath" and "emptyDir" volumes, or "local" PersistentVolumes) will be erased.__


        ---


        - __CLI__.

            ```
            linode-cli lke node-recycle
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            lke:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli lke node-recycle
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: lke:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Recycle request succeeded and is in progress.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-lke-cluster-node-recycle
      security:
      - personalAccessToken: []
      - oauth:
        - lke:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
                -H "Authorization: Bearer $TOKEN" \
                -X POST \
                https://api.linode.com/v4/lke/clusters/12345/nodes/12345-6aa78910bc/recycle
      - lang: CLI
        source: linode-cli lke node-recycle 12345 12345-6aa78910bc
      x-linode-cli-action: node-recycle
      x-original-op-id: postLKEClusterNodeRecycle
      x-original-op-title: Node Recycle
    x-linode-cli-command: lke
  /lke/clusters/{clusterId}/pools:
    x-akamai:
      file-path: paths/cluster-pools.yaml
      path-info: /lke/clusters/{clusterId}/pools
    parameters:
    - name: clusterId
      in: path
      description: |
        ID of the Kubernetes cluster to look up.
      example: '{{clusterId}}'
      x-akamai:
        file-path: parameters/cluster-id-path.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-lke-cluster-pools
      summary: Create a node pool
      tags:
      - Node pools
      description: |-
        Creates a new Node Pool for the designated Kubernetes cluster.


        ---


        - __CLI__.

            ```
            linode-cli lke pool-create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            lke:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli lke pool-create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: lke:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Configuration for the Node Pool
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - type
              - count
              allOf:
              - type: object
                description: |
                  Specifies a collection of Linodes which will be members of a Kubernetes cluster.
                x-akamai:
                  file-path: schemas/lke-node-pool-request-body.yaml
                additionalProperties: false
                properties:
                  tags:
                    type: array
                    description: |
                      An array of tags applied to this object. Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  type:
                    type: string
                    description: |
                      The Linode Type for all of the nodes in the Node Pool.
                    example: g6-standard-4
                  autoscaler:
                    type: object
                    description: |-
                      When enabled, the number of nodes autoscales within the defined minimum and maximum values.

                      When making a request, `max` and `min` require each other.
                    additionalProperties: false
                    properties:
                      enabled:
                        type: boolean
                        description: |
                          Whether autoscaling is enabled for this Node Pool. Defaults to `false`.
                        example: true
                      max:
                        type: integer
                        description: |
                          The maximum number of nodes to autoscale to. Defaults to the Node Pool's `count`.
                          Defaults to the value provided by the `count` field.
                        example: 12
                        minimum: 1
                        maximum: 100
                      min:
                        type: integer
                        description: |
                          The minimum number of nodes to autoscale to. Defaults to the Node Pool's `count`.
                        example: 3
                        minimum: 1
                        maximum: 100
                  count:
                    type: integer
                    description: |
                      The number of nodes in the Node Pool.
                    example: 6
                    minimum: 1
                    maximum: 100
                  disks:
                    type: array
                    description: |-
                      __Note__: This field should be omitted except for special use cases. The disks specified here are partitions in _addition_ to the primary partition and reduce the size of the primary partition, which can lead to stability problems for the Node.

                      This Node Pool's custom disk layout. Each item in this array will create a new disk partition for each node in this Node Pool.

                        - The custom disk layout is applied to each node in this Node Pool.
                        - The maximum number of custom disk partitions that can be configured is 7.
                        - Once the requested disk partitions are allocated, the remaining disk space is allocated to the node's boot disk.
                        - A Node Pool's custom disk layout is immutable over the lifetime of the Node Pool.
                    items:
                      type: object
                      description: |
                        The values to assign to each partition in this Node Pool's custom disk layout.
                      additionalProperties: false
                      properties:
                        type:
                          type: string
                          description: |
                            This custom disk partition's filesystem type.
                          example: ext-4
                          enum:
                          - raw
                          - ext4
                        size:
                          type: integer
                          description: |-
                            The size of this custom disk partition in MB.

                              - The size of this disk partition must not exceed the capacity of the node's plan type.
                          example: 1024
                    x-linode-cli-format: json
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Node Pool has been created.
          content:
            application/json:
              schema:
                type: object
                description: |
                  The set of Node Pools which are members of the Kubernetes cluster. Node Pools consist of a Linode
                  type, the number of Linodes to deploy of that type, and additional
                  status information.
                x-akamai:
                  file-path: schemas/lke-node-pool.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      This Node Pool's unique ID.
                    example: 456
                    x-linode-filterable: true
                  tags:
                    type: array
                    description: |
                      An array of tags applied to this object. Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  type:
                    type: string
                    description: |
                      The Linode Type for all of the nodes in the Node Pool.
                    example: g6-standard-4
                  autoscaler:
                    type: object
                    description: |
                      When enabled, the number of nodes autoscales within the defined minimum and maximum values.
                    additionalProperties: false
                    properties:
                      enabled:
                        type: boolean
                        description: |
                          Whether autoscaling is enabled for this Node Pool. Defaults to `false`.
                        example: true
                      max:
                        type: integer
                        description: |
                          The maximum number of nodes to autoscale to. Defaults to the Node Pool's `count`.
                        example: 12
                        minimum: 1
                        maximum: 100
                      min:
                        type: integer
                        description: |
                          The minimum number of nodes to autoscale to. Defaults to the Node Pool's `count`.
                        example: 3
                        minimum: 1
                        maximum: 100
                  count:
                    type: integer
                    description: |
                      The number of nodes in the Node Pool.
                    example: 6
                    minimum: 1
                    maximum: 100
                  disk_encryption:
                    type: string
                    description: |
                      For LKE nodepools, `disk-encryption` is automatically `enabled` and cannot be `disabled`.
                    example: enabled
                    enum:
                    - enabled
                  disks:
                    type: array
                    description: |
                      This Node Pool's custom disk layout.
                    items:
                      type: object
                      description: |
                        The values to assign to each partition in this Node Pool's custom disk layout.
                      additionalProperties: false
                      properties:
                        type:
                          type: string
                          description: |
                            This custom disk partition's filesystem type.
                          example: ext-4
                          enum:
                          - raw
                          - ext4
                        size:
                          type: integer
                          description: |-
                            The size of this custom disk partition in MB.

                              - The size of this disk partition must not exceed the capacity of the node's plan type.
                          example: 1024
                    x-linode-cli-format: json
                  nodes:
                    type: array
                    description: |
                      Status information for the Nodes which are members of this Node Pool. If a Linode
                      has not been provisioned for a given Node slot, the instance_id
                      will be returned as null.
                    items:
                      type: object
                      description: |
                        __Read-only__ Status information for a Node which is a member of a Kubernetes cluster.
                      x-akamai:
                        file-path: schemas/lke-node-status.yaml
                      additionalProperties: false
                      readOnly: true
                      properties:
                        id:
                          type: string
                          description: |
                            The Node's ID.
                          example: '123456'
                        instance_id:
                          type: string
                          description: |
                            The Linode's ID. When no Linode is currently provisioned for this Node,
                            this will be null.
                          example: 123458
                        status:
                          type: string
                          description: |
                            The Node's status as it pertains to being a Kubernetes node.
                          example: ready
                          enum:
                          - ready
                          - not_ready
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-lke-cluster-pools
      security:
      - personalAccessToken: []
      - oauth:
        - lke:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
                -H "Authorization: Bearer $TOKEN" \
                -X POST -d '{
                  "type": "g6-standard-4",
                  "count": 6,
                  "tags": ["example-tag"],
                  "autoscaler": {
                    "enabled": true,
                    "max": 12,
                    "min": 3
                  }
                }' \
                https://api.linode.com/v4/lke/clusters/12345/pools
      - lang: CLI
        source: |-
          linode-cli lke pool-create 12345 \
            --type g6-standard-4 \
            --count 6 \
            --tags example-tag \
            --autoscaler.enabled true \
            --autoscaler.max 12 \
            --autoscaler.min 3
      x-linode-cli-action: pool-create
      x-original-op-id: postLKEClusterPools
      x-original-op-title: Node Pool Create
    get:
      operationId: get-lke-cluster-pools
      summary: List node pools
      tags:
      - Node pools
      description: |-
        Returns all active Node Pools on a Kubernetes cluster.


        ---


        - __CLI__.

            ```
            linode-cli lke pools-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            lke:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli lke pools-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: lke:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns an array of all Pools in this Kubernetes cluster.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        The set of Node Pools which are members of the Kubernetes cluster. Node Pools consist
                        of a Linode type, the number of Linodes to deploy of that
                        type, and additional status information.
                      x-akamai:
                        file-path: schemas/lke-node-pool.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            This Node Pool's unique ID.
                          example: 456
                          x-linode-filterable: true
                        tags:
                          type: array
                          description: |
                            An array of tags applied to this object. Tags are for organizational purposes
                            only.
                          example:
                          - example tag
                          - another example
                          items:
                            type: string
                          x-linode-filterable: true
                        type:
                          type: string
                          description: |
                            The Linode Type for all of the nodes in the Node Pool.
                          example: g6-standard-4
                        autoscaler:
                          type: object
                          description: |
                            When enabled, the number of nodes autoscales within the defined minimum
                            and maximum values.
                          additionalProperties: false
                          properties:
                            enabled:
                              type: boolean
                              description: |
                                Whether autoscaling is enabled for this Node Pool. Defaults to `false`.
                              example: true
                            max:
                              type: integer
                              description: |
                                The maximum number of nodes to autoscale to. Defaults to the Node
                                Pool's `count`.
                              example: 12
                              minimum: 1
                              maximum: 100
                            min:
                              type: integer
                              description: |
                                The minimum number of nodes to autoscale to. Defaults to the Node
                                Pool's `count`.
                              example: 3
                              minimum: 1
                              maximum: 100
                        count:
                          type: integer
                          description: |
                            The number of nodes in the Node Pool.
                          example: 6
                          minimum: 1
                          maximum: 100
                        disk_encryption:
                          type: string
                          description: |
                            For LKE nodepools, `disk-encryption` is automatically `enabled` and cannot
                            be `disabled`.
                          example: enabled
                          enum:
                          - enabled
                        disks:
                          type: array
                          description: |
                            This Node Pool's custom disk layout.
                          items:
                            type: object
                            description: |
                              The values to assign to each partition in this Node Pool's custom
                              disk layout.
                            additionalProperties: false
                            properties:
                              type:
                                type: string
                                description: |
                                  This custom disk partition's filesystem type.
                                example: ext-4
                                enum:
                                - raw
                                - ext4
                              size:
                                type: integer
                                description: |-
                                  The size of this custom disk partition in MB.

                                    - The size of this disk partition must not exceed the capacity of the node's plan type.
                                example: 1024
                          x-linode-cli-format: json
                        nodes:
                          type: array
                          description: |
                            Status information for the Nodes which are members of this Node Pool. If
                            a Linode has not been provisioned for a given Node slot,
                            the instance_id will be returned as null.
                          items:
                            type: object
                            description: |
                              __Read-only__ Status information for a Node which is a member of a
                              Kubernetes cluster.
                            x-akamai:
                              file-path: schemas/lke-node-status.yaml
                            additionalProperties: false
                            readOnly: true
                            properties:
                              id:
                                type: string
                                description: |
                                  The Node's ID.
                                example: '123456'
                              instance_id:
                                type: string
                                description: |
                                  The Linode's ID. When no Linode is currently provisioned for
                                  this Node, this will be null.
                                example: 123458
                              status:
                                type: string
                                description: |
                                  The Node's status as it pertains to being a Kubernetes node.
                                example: ready
                                enum:
                                - ready
                                - not_ready
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
              x-linode-cli-nested-list: nodes
              x-linode-cli-use-schema:
                type: object
                additionalProperties: false
                properties:
                  id:
                    x-linode-cli-display: 1
                  type:
                    x-linode-cli-display: 2
                  nodes.id:
                    x-linode-cli-display: 3
                  nodes.instance_id:
                    x-linode-cli-display: 4
                  nodes.status:
                    x-linode-cli-display: 5
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-lke-cluster-pools
      security:
      - personalAccessToken: []
      - oauth:
        - lke:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
                https://api.linode.com/v4/lke/clusters/12345/pools
      - lang: CLI
        source: linode-cli lke pools-list 12345
      x-linode-cli-action: pools-list
      x-original-op-id: getLKEClusterPools
      x-original-op-title: Node Pools List
    x-linode-cli-command: lke
  /lke/clusters/{clusterId}/pools/{poolId}:
    x-akamai:
      file-path: paths/pool.yaml
      path-info: /lke/clusters/{clusterId}/pools/{poolId}
    parameters:
    - name: clusterId
      in: path
      description: |
        ID of the Kubernetes cluster to look up.
      example: '{{clusterId}}'
      x-akamai:
        file-path: parameters/cluster-id-path-28150853.yaml
      required: true
      schema:
        type: integer
    - name: poolId
      in: path
      description: |
        ID of the Pool to look up
      example: '{{poolId}}'
      x-akamai:
        file-path: parameters/pool-id-path-8468a3cd.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-lke-node-pool
      summary: Get a node pool
      tags:
      - Node pools
      description: |-
        Get a specific Node Pool by ID.


        ---


        - __CLI__.

            ```
            linode-cli lke pool-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            lke:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli lke pool-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: lke:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        200:
          description: |
            Returns the requested Node Pool.
          content:
            application/json:
              schema:
                type: object
                description: |
                  The set of Node Pools which are members of the Kubernetes cluster. Node Pools consist of a Linode
                  type, the number of Linodes to deploy of that type, and additional
                  status information.
                x-akamai:
                  file-path: schemas/lke-node-pool.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      This Node Pool's unique ID.
                    example: 456
                    x-linode-filterable: true
                  tags:
                    type: array
                    description: |
                      An array of tags applied to this object. Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  type:
                    type: string
                    description: |
                      The Linode Type for all of the nodes in the Node Pool.
                    example: g6-standard-4
                  autoscaler:
                    type: object
                    description: |
                      When enabled, the number of nodes autoscales within the defined minimum and maximum values.
                    additionalProperties: false
                    properties:
                      enabled:
                        type: boolean
                        description: |
                          Whether autoscaling is enabled for this Node Pool. Defaults to `false`.
                        example: true
                      max:
                        type: integer
                        description: |
                          The maximum number of nodes to autoscale to. Defaults to the Node Pool's `count`.
                        example: 12
                        minimum: 1
                        maximum: 100
                      min:
                        type: integer
                        description: |
                          The minimum number of nodes to autoscale to. Defaults to the Node Pool's `count`.
                        example: 3
                        minimum: 1
                        maximum: 100
                  count:
                    type: integer
                    description: |
                      The number of nodes in the Node Pool.
                    example: 6
                    minimum: 1
                    maximum: 100
                  disk_encryption:
                    type: string
                    description: |
                      For LKE nodepools, `disk-encryption` is automatically `enabled` and cannot be `disabled`.
                    example: enabled
                    enum:
                    - enabled
                  disks:
                    type: array
                    description: |
                      This Node Pool's custom disk layout.
                    items:
                      type: object
                      description: |
                        The values to assign to each partition in this Node Pool's custom disk layout.
                      additionalProperties: false
                      properties:
                        type:
                          type: string
                          description: |
                            This custom disk partition's filesystem type.
                          example: ext-4
                          enum:
                          - raw
                          - ext4
                        size:
                          type: integer
                          description: |-
                            The size of this custom disk partition in MB.

                              - The size of this disk partition must not exceed the capacity of the node's plan type.
                          example: 1024
                    x-linode-cli-format: json
                  nodes:
                    type: array
                    description: |
                      Status information for the Nodes which are members of this Node Pool. If a Linode
                      has not been provisioned for a given Node slot, the instance_id
                      will be returned as null.
                    items:
                      type: object
                      description: |
                        __Read-only__ Status information for a Node which is a member of a Kubernetes cluster.
                      x-akamai:
                        file-path: schemas/lke-node-status.yaml
                      additionalProperties: false
                      readOnly: true
                      properties:
                        id:
                          type: string
                          description: |
                            The Node's ID.
                          example: '123456'
                        instance_id:
                          type: string
                          description: |
                            The Linode's ID. When no Linode is currently provisioned for this Node,
                            this will be null.
                          example: 123458
                        status:
                          type: string
                          description: |
                            The Node's status as it pertains to being a Kubernetes node.
                          example: ready
                          enum:
                          - ready
                          - not_ready
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-lke-node-pool
      security:
      - personalAccessToken: []
      - oauth:
        - lke:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
                https://api.linode.com/v4/lke/clusters/12345/pools/456
      - lang: CLI
        source: linode-cli lke pool-view 12345 456
      x-linode-cli-action: pool-view
      x-original-op-id: getLKENodePool
      x-original-op-title: Node Pool View
    put:
      operationId: put-lke-node-pool
      summary: Update a node pool
      tags:
      - Node pools
      description: |-
        Updates a Node Pool's count and autoscaler configuration.

        Linodes will be created or deleted to match changes to the Node Pool's count.

        __Any local storage on deleted Linodes (such as "hostPath" and "emptyDir" volumes, or "local" PersistentVolumes) will be erased.__


        ---


        - __CLI__.

            ```
            linode-cli lke pool-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            lke:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli lke pool-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: lke:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The fields to update
        content:
          application/json:
            schema:
              additionalProperties: false
              properties:
                autoscaler:
                  type: object
                  description: |-
                    When enabled, the number of nodes autoscales within the defined minimum and maximum values.

                    When making a request, `max` and `min` require each other.
                  additionalProperties: false
                  properties:
                    enabled:
                      type: boolean
                      description: |
                        Whether autoscaling is enabled for this Node Pool. Defaults to `false`.
                      example: true
                    max:
                      type: integer
                      description: |
                        The maximum number of nodes to autoscale to. Defaults to the Node Pool's `count`.
                        Defaults to the value provided by the `count` field.
                      example: 12
                      minimum: 1
                      maximum: 100
                    min:
                      type: integer
                      description: |
                        The minimum number of nodes to autoscale to. Defaults to the Node Pool's `count`.
                      example: 3
                      minimum: 1
                      maximum: 100
                count:
                  type: integer
                  description: |
                    The number of nodes in the Node Pool.
                  example: 6
                  minimum: 1
                  maximum: 100
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        200:
          description: |
            Node Pool was successfully modified.
          content:
            application/json:
              schema:
                type: object
                description: |
                  The set of Node Pools which are members of the Kubernetes cluster. Node Pools consist of a Linode
                  type, the number of Linodes to deploy of that type, and additional
                  status information.
                x-akamai:
                  file-path: schemas/lke-node-pool.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      This Node Pool's unique ID.
                    example: 456
                    x-linode-filterable: true
                  tags:
                    type: array
                    description: |
                      An array of tags applied to this object. Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  type:
                    type: string
                    description: |
                      The Linode Type for all of the nodes in the Node Pool.
                    example: g6-standard-4
                  autoscaler:
                    type: object
                    description: |
                      When enabled, the number of nodes autoscales within the defined minimum and maximum values.
                    additionalProperties: false
                    properties:
                      enabled:
                        type: boolean
                        description: |
                          Whether autoscaling is enabled for this Node Pool. Defaults to `false`.
                        example: true
                      max:
                        type: integer
                        description: |
                          The maximum number of nodes to autoscale to. Defaults to the Node Pool's `count`.
                        example: 12
                        minimum: 1
                        maximum: 100
                      min:
                        type: integer
                        description: |
                          The minimum number of nodes to autoscale to. Defaults to the Node Pool's `count`.
                        example: 3
                        minimum: 1
                        maximum: 100
                  count:
                    type: integer
                    description: |
                      The number of nodes in the Node Pool.
                    example: 6
                    minimum: 1
                    maximum: 100
                  disk_encryption:
                    type: string
                    description: |
                      For LKE nodepools, `disk-encryption` is automatically `enabled` and cannot be `disabled`.
                    example: enabled
                    enum:
                    - enabled
                  disks:
                    type: array
                    description: |
                      This Node Pool's custom disk layout.
                    items:
                      type: object
                      description: |
                        The values to assign to each partition in this Node Pool's custom disk layout.
                      additionalProperties: false
                      properties:
                        type:
                          type: string
                          description: |
                            This custom disk partition's filesystem type.
                          example: ext-4
                          enum:
                          - raw
                          - ext4
                        size:
                          type: integer
                          description: |-
                            The size of this custom disk partition in MB.

                              - The size of this disk partition must not exceed the capacity of the node's plan type.
                          example: 1024
                    x-linode-cli-format: json
                  nodes:
                    type: array
                    description: |
                      Status information for the Nodes which are members of this Node Pool. If a Linode
                      has not been provisioned for a given Node slot, the instance_id
                      will be returned as null.
                    items:
                      type: object
                      description: |
                        __Read-only__ Status information for a Node which is a member of a Kubernetes cluster.
                      x-akamai:
                        file-path: schemas/lke-node-status.yaml
                      additionalProperties: false
                      readOnly: true
                      properties:
                        id:
                          type: string
                          description: |
                            The Node's ID.
                          example: '123456'
                        instance_id:
                          type: string
                          description: |
                            The Linode's ID. When no Linode is currently provisioned for this Node,
                            this will be null.
                          example: 123458
                        status:
                          type: string
                          description: |
                            The Node's status as it pertains to being a Kubernetes node.
                          example: ready
                          enum:
                          - ready
                          - not_ready
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-lke-node-pool
      security:
      - personalAccessToken: []
      - oauth:
        - lke:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
                -H "Authorization: Bearer $TOKEN" \
                -X PUT -d '{
                  "count": 6,
                  "autoscaler": {
                    "enabled": true,
                    "max": 12,
                    "min": 3
                }' \
                https://api.linode.com/v4/lke/clusters/12345/pools/456
      - lang: CLI
        source: |-
          linode-cli lke pool-update 12345 456 \
            --count 6 \
            --autoscaler.enabled true \
            --autoscaler.max 12 \
            --autoscaler.min 3
      x-linode-cli-action: pool-update
      x-original-op-id: putLKENodePool
      x-original-op-title: Node Pool Update
    delete:
      operationId: delete-lke-node-pool
      summary: Delete a node pool
      tags:
      - Node pools
      description: |-
        Delete a specific Node Pool from a Kubernetes cluster.

        __Deleting a Node Pool is a destructive action and cannot be undone.__

        Deleting a Node Pool will delete all Linodes within that Pool.


        ---


        - __CLI__.

            ```
            linode-cli lke pool-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            lke:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli lke pool-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: lke:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Delete successful
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-lke-node-pool
      security:
      - personalAccessToken: []
      - oauth:
        - lke:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
                -X DELETE \
                https://api.linode.com/v4/lke/clusters/12345/pools/456
      - lang: CLI
        source: linode-cli lke pool-delete 12345 456
      x-linode-cli-action: pool-delete
      x-original-op-id: deleteLKENodePool
      x-original-op-title: Node Pool Delete
    x-linode-cli-command: lke
  /lke/clusters/{clusterId}/pools/{poolId}/recycle:
    x-akamai:
      file-path: paths/pool-recycle.yaml
      path-info: /lke/clusters/{clusterId}/pools/{poolId}/recycle
    parameters:
    - name: clusterId
      in: path
      description: |
        ID of the Kubernetes cluster this Node Pool is attached to.
      example: '{{clusterId}}'
      x-akamai:
        file-path: parameters/cluster-id-path-61c0e84e.yaml
      required: true
      schema:
        type: integer
    - name: poolId
      in: path
      description: |
        ID of the Node Pool to be recycled.
      example: '{{poolId}}'
      x-akamai:
        file-path: parameters/pool-id-path-bfac95e1.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-lke-cluster-pool-recycle
      summary: Recycle a node pool
      tags:
      - Node pools
      description: |-
        Recycles a Node Pool for the designated Kubernetes Cluster. All Linodes within the Node Pool will be deleted and replaced with new Linodes on a rolling basis, which may take several minutes. Replacement Nodes are installed with the latest available patch for the Cluster's Kubernetes Version.

        __Any local storage on deleted Linodes (such as "hostPath" and "emptyDir" volumes, or "local" PersistentVolumes) will be erased.__


        ---


        - __CLI__.

            ```
            linode-cli lke pool-recycle
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            lke:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli lke pool-recycle
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: lke:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Recycle request succeeded and is in progress.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-lke-cluster-pool-recycle
      security:
      - personalAccessToken: []
      - oauth:
        - lke:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
                -H "Authorization: Bearer $TOKEN" \
                -X POST \
                https://api.linode.com/v4/lke/clusters/12345/pools/456/recycle
      - lang: CLI
        source: linode-cli lke pool-recycle 12345 456
      x-linode-cli-action: pool-recycle
      x-original-op-id: postLKEClusterPoolRecycle
      x-original-op-title: Node Pool Recycle
    x-linode-cli-command: lke
  /lke/clusters/{clusterId}/recycle:
    x-akamai:
      file-path: paths/cluster-recycle.yaml
      path-info: /lke/clusters/{clusterId}/recycle
    parameters:
    - name: clusterId
      in: path
      description: |
        ID of the Kubernetes cluster which contains nodes to be recycled.
      example: '{{clusterId}}'
      x-akamai:
        file-path: parameters/cluster-id-path-c46e2301.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-lke-cluster-recycle
      summary: Recycle cluster nodes
      tags:
      - Clusters
      description: |-
        Recycles all nodes in all pools of a designated Kubernetes Cluster. All Linodes within the Cluster will be deleted and replaced with new Linodes on a rolling basis, which may take several minutes. Replacement Nodes are installed with the latest available [patch version](https://github.com/kubernetes/community/blob/master/contributors/design-proposals/release/versioning.md#kubernetes-release-versioning) for the Cluster's current Kubernetes minor release.

        __Any local storage on deleted Linodes (such as "hostPath" and "emptyDir" volumes, or "local" PersistentVolumes) will be erased.__


        ---


        - __CLI__.

            ```
            linode-cli lke cluster-nodes-recycle
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            lke:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli lke cluster-nodes-recycle
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: lke:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Recycle request succeeded and is in progress.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-lke-cluster-recycle
      security:
      - personalAccessToken: []
      - oauth:
        - lke:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
                -H "Authorization: Bearer $TOKEN" \
                -X POST \
                https://api.linode.com/v4/lke/clusters/12345/recycle
      - lang: CLI
        source: linode-cli lke cluster-nodes-recycle 12345
      x-linode-cli-action: cluster-nodes-recycle
      x-original-op-id: postLKEClusterRecycle
      x-original-op-title: Cluster Nodes Recycle
    x-linode-cli-command: lke
  /lke/clusters/{clusterId}/regenerate:
    x-akamai:
      file-path: paths/regenerate.yaml
      path-info: /lke/clusters/{clusterId}/regenerate
    parameters:
    - name: clusterId
      in: path
      description: |
        ID of the target Kubernetes cluster.
      example: '{{clusterId}}'
      x-akamai:
        file-path: parameters/cluster-id-path-c7286e6e.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-lke-cluster-regenerate
      summary: Regenerate a Kubernetes cluster
      tags:
      - Clusters
      description: |-
        Regenerate the Kubeconfig file and/or the service account token for a Cluster.

        This is a helper operation that allows performing both the [Delete a Kubeconfig](https://techdocs.akamai.com/linode-api/reference/delete-lke-cluster-kubeconfig) and the [Delete a service token](https://techdocs.akamai.com/linode-api/reference/delete-lke-service-token) operations with a single request.

        When using this operation, at least one of `kubeconfig` or `servicetoken` is required.

        __Note__. When regenerating a service account token, the Cluster's control plane components and Linode CSI drivers are also restarted and configured with the new token. High Availability Clusters should not experience any disruption, while standard Clusters may experience brief control plane downtime while components are restarted.


        ---


        - __CLI__.

            ```
            linode-cli lke regenerate
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            lke:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli lke regenerate
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: lke:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The Kubernetes Cluster Regenerate request object.
        content:
          application/json:
            schema:
              additionalProperties: false
              properties:
                kubeconfig:
                  type: boolean
                  description: |
                    Whether to delete and regenerate the Kubeconfig file for this Cluster.
                  example: true
                  default: false
                servicetoken:
                  type: boolean
                  description: |
                    Whether to delete and regenerate the service access token for this Cluster.
                  example: true
                  default: false
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Regenerate request successful.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
              x-tbd: externalize inline schemas; mark
                empty problem schemas
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-lke-cluster-regenerate
      security:
      - personalAccessToken: []
      - oauth:
        - lke:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
                -H "Authorization: Bearer $TOKEN" \
                -X POST -d '{
                  "kubeconfig": true,
                  "servicetoken": true
                }' \
                https://api.linode.com/v4/lke/clusters/12345/regenerate
      - lang: CLI
        source: |-
          linode-cli lke regenerate 12345 \
              --kubeconfig true \
              --servicetoken true
      x-linode-cli-action: regenerate
      x-original-op-id: postLKEClusterRegenerate
      x-original-op-title: Kubernetes Cluster Regenerate
    x-linode-cli-command: lke
  /lke/clusters/{clusterId}/servicetoken:
    x-akamai:
      file-path: paths/service-token.yaml
      path-info: /lke/clusters/{clusterId}/servicetoken
    parameters:
    - name: clusterId
      in: path
      description: |
        ID of the target Kubernetes cluster.
      example: '{{clusterId}}'
      x-akamai:
        file-path: parameters/cluster-id-path-c7286e6e.yaml
      required: true
      schema:
        type: integer
    delete:
      operationId: delete-lke-service-token
      summary: Delete a service token
      tags:
      - Service tokens
      description: |-
        Delete and regenerate the service account token for a Cluster.

        __Note__. When regenerating a service account token, the Cluster's control plane components and Linode CSI drivers are also restarted and configured with the new token. High Availability Clusters should not experience any disruption, while standard Clusters may experience brief control plane downtime while components are restarted.


        ---


        - __CLI__.

            ```
            linode-cli lke service-token-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            lke:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli lke service-token-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: lke:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Service token deleted and regenerated successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-lke-service-token
      security:
      - personalAccessToken: []
      - oauth:
        - lke:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
                -X DELETE \
                https://api.linode.com/v4/lke/clusters/12345/servicetoken
      - lang: CLI
        source: linode-cli lke service-token-delete 12345
      x-linode-cli-action: service-token-delete
      x-original-op-id: postLKECServiceTokenDelete
      x-original-op-title: Service Token Delete
    x-linode-cli-command: lke
  /lke/versions:
    x-akamai:
      file-path: paths/versions.yaml
      path-info: /lke/versions
    get:
      operationId: get-lke-versions
      summary: List Kubernetes versions
      tags:
      - LKE versions
      description: |-
        List the Kubernetes versions available for deployment to a Kubernetes cluster.


        ---


        - __CLI__.

            ```
            linode-cli lke versions-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            lke:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli lke versions-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: lke:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a list of Kubernetes versions available for deployment to a Kubernetes cluster.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        __Read-only__ LKE versions
                      x-akamai:
                        file-path: schemas/lke-version.yaml
                      additionalProperties: false
                      readOnly: true
                      properties:
                        id:
                          type: string
                          description: |
                            A Kubernetes version number available for deployment to a Kubernetes
                            cluster in the format of &lt;major&gt;.&lt;minor&gt;,
                            and the latest supported patch version.
                          example: '1.27'
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-lke-versions
      security:
      - personalAccessToken: []
      - oauth:
        - lke:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
                https://api.linode.com/v4/lke/versions
      - lang: CLI
        source: linode-cli lke versions-list
      x-linode-cli-action: versions-list
      x-linode-grant: read_only
      x-original-op-id: getLKEVersions
      x-original-op-title: Kubernetes Versions List
    x-linode-cli-command: lke
  /lke/versions/{version}:
    x-akamai:
      file-path: paths/version.yaml
      path-info: /lke/versions/{version}
    parameters:
    - name: version
      in: path
      description: |
        The LKE version to view.
      example: '{{version}}'
      x-akamai:
        file-path: parameters/version-path.yaml
      required: true
      schema:
        type: string
    get:
      operationId: get-lke-version
      summary: Get a Kubernetes version
      tags:
      - LKE versions
      description: |-
        View a Kubernetes version available for deployment to a Kubernetes cluster.


        ---


        - __CLI__.

            ```
            linode-cli lke version-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            lke:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli lke version-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: lke:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns a Kubernetes version object that is available for deployment to a Kubernetes cluster.
          content:
            application/json:
              schema:
                type: object
                description: |
                  __Read-only__ LKE versions
                x-akamai:
                  file-path: schemas/lke-version.yaml
                additionalProperties: false
                readOnly: true
                properties:
                  id:
                    type: string
                    description: |
                      A Kubernetes version number available for deployment to a Kubernetes cluster in the
                      format of &lt;major&gt;.&lt;minor&gt;, and the latest supported
                      patch version.
                    example: '1.27'
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-lke-version
      security:
      - personalAccessToken: []
      - oauth:
        - lke:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
                https://api.linode.com/v4/lke/versions/1.27
      - lang: CLI
        source: linode-cli lke version-view 1.27
      x-linode-cli-action: version-view
      x-linode-grant: read_only
      x-original-op-id: getLKEVersion
      x-original-op-title: Kubernetes Version View
    x-linode-cli-command: lke
  /longview/clients:
    x-akamai:
      file-path: paths/longview-clients.yaml
      path-info: /longview/clients
    post:
      operationId: post-longview-client
      summary: Create a Longview client
      tags:
      - Clients
      description: |-
        Creates a Longview Client.  This Client will not begin monitoring the status of your server until you configure the Longview Client application on your Linode using the returning `install_code` and `api_key`.


        ---


        - __CLI__.

            ```
            linode-cli longview create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            longview:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli longview create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: longview:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the LongviewClient to create.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                A LongviewClient is a single monitor set up to track statistics about one of your servers.
              x-akamai:
                file-path: schemas/longview-client.yaml
              additionalProperties: false
              properties:
                id:
                  type: integer
                  description: |
                    __Read-only__ This Client's unique ID.
                  example: '{{id}}'
                  readOnly: true
                  x-linode-cli-display: 1
                api_key:
                  type: string
                  description: |-
                    __Read-only__ The API key for this Client, used when configuring the Longview Client application on your Linode.

                    Returns as `[REDACTED]` if you do not have read-write access to this client.
                  example: '{{api_key}}'
                  readOnly: true
                apps:
                  type: object
                  description: |
                    __Read-only__ The apps this Client is monitoring on your Linode. This is configured when
                    you install the Longview Client application, and is present here
                    for information purposes only.
                  additionalProperties: false
                  readOnly: true
                  properties:
                    apache:
                      type: boolean
                      description: |
                        __Read-only__ If True, the Apache Longview Client module is monitoring Apache
                        on your server.
                      example: true
                      readOnly: true
                    mysql:
                      type: boolean
                      description: |
                        __Read-only__ If True, the MySQL Longview Client modules is monitoring MySQL
                        on your server.
                      example: true
                      readOnly: true
                    nginx:
                      type: boolean
                      description: |
                        __Read-only__ If True, the Nginx Longview Client module is monitoring Nginx
                        on your server.
                      example: false
                      readOnly: true
                created:
                  type: string
                  description: |
                    __Read-only__ When this Longview Client was created.
                  example: '{{created}}'
                  readOnly: true
                  format: date-time
                  x-linode-cli-display: 3
                install_code:
                  type: string
                  description: |-
                    __Read-only__ The install code for this Client, used when configuring the Longview Client application on your Linode.

                    Returns as `[REDACTED]` if you do not have read-write access to this client.
                  example: '{{install_code}}'
                  readOnly: true
                  x-linode-cli-display: 4
                label:
                  type: string
                  description: |
                    This Client's unique label. This is for display purposes only.
                  example: '{{label}}'
                  minLength: 3
                  maxLength: 32
                  pattern: '[a-zA-Z0-9-_]{3,32}'
                  x-linode-cli-display: 2
                  x-linode-filterable: true
                updated:
                  type: string
                  description: |
                    __Read-only__ When this Longview Client was last updated.
                  example: '{{updated}}'
                  readOnly: true
                  format: date-time
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Longview Client created successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A LongviewClient is a single monitor set up to track statistics about one of your servers.
                x-akamai:
                  file-path: schemas/longview-client.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Client's unique ID.
                    example: 789
                    readOnly: true
                    x-linode-cli-display: 1
                  api_key:
                    type: string
                    description: |-
                      __Read-only__ The API key for this Client, used when configuring the Longview Client application on your Linode.

                      Returns as `[REDACTED]` if you do not have read-write access to this client.
                    example: BD1B4B54-D752-A76D-5A9BD8A17F39DB61
                    readOnly: true
                  apps:
                    type: object
                    description: |
                      __Read-only__ The apps this Client is monitoring on your Linode. This is configured
                      when you install the Longview Client application, and is present
                      here for information purposes only.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      apache:
                        type: boolean
                        description: |
                          __Read-only__ If True, the Apache Longview Client module is monitoring Apache
                          on your server.
                        example: true
                        readOnly: true
                      mysql:
                        type: boolean
                        description: |
                          __Read-only__ If True, the MySQL Longview Client modules is monitoring MySQL
                          on your server.
                        example: true
                        readOnly: true
                      nginx:
                        type: boolean
                        description: |
                          __Read-only__ If True, the Nginx Longview Client module is monitoring Nginx
                          on your server.
                        example: false
                        readOnly: true
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Longview Client was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 3
                  install_code:
                    type: string
                    description: |-
                      __Read-only__ The install code for this Client, used when configuring the Longview Client application on your Linode.

                      Returns as `[REDACTED]` if you do not have read-write access to this client.
                    example: BD1B5605-BF5E-D385-BA07AD518BE7F321
                    readOnly: true
                    x-linode-cli-display: 4
                  label:
                    type: string
                    description: |
                      This Client's unique label. This is for display purposes only.
                    example: client789
                    minLength: 3
                    maxLength: 32
                    pattern: '[a-zA-Z0-9-_]{3,32}'
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Longview Client was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-longview-client
      security:
      - personalAccessToken: []
      - oauth:
        - longview:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "label": "client789"
              }' \
              https://api.linode.com/v4/longview/clients
      - lang: CLI
        source: |-
          linode-cli longview create \
            --label client789
      x-linode-cli-action: create
      x-linode-grant: add_longview
      x-original-op-id: createLongviewClient
      x-original-op-title: Longview Client Create
    get:
      operationId: get-longview-clients
      summary: List Longview clients
      tags:
      - Clients
      description: |-
        Returns a paginated list of Longview Clients you have access to. Longview Client is used to monitor stats on your Linode with the help of the Longview Client application.


        ---


        - __OAuth__.

            ```
            longview:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: longview:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of Longview Clients.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        A LongviewClient is a single monitor set up to track statistics about one of
                        your servers.
                      x-akamai:
                        file-path: schemas/longview-client.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ This Client's unique ID.
                          example: 789
                          readOnly: true
                          x-linode-cli-display: 1
                        api_key:
                          type: string
                          description: |-
                            __Read-only__ The API key for this Client, used when configuring the Longview Client application on your Linode.

                            Returns as `[REDACTED]` if you do not have read-write access to this client.
                          example: BD1B4B54-D752-A76D-5A9BD8A17F39DB61
                          readOnly: true
                        apps:
                          type: object
                          description: |
                            __Read-only__ The apps this Client is monitoring on your Linode. This
                            is configured when you install the Longview Client application,
                            and is present here for information purposes only.
                          additionalProperties: false
                          readOnly: true
                          properties:
                            apache:
                              type: boolean
                              description: |
                                __Read-only__ If True, the Apache Longview Client module is monitoring
                                Apache on your server.
                              example: true
                              readOnly: true
                            mysql:
                              type: boolean
                              description: |
                                __Read-only__ If True, the MySQL Longview Client modules is monitoring
                                MySQL on your server.
                              example: true
                              readOnly: true
                            nginx:
                              type: boolean
                              description: |
                                __Read-only__ If True, the Nginx Longview Client module is monitoring
                                Nginx on your server.
                              example: false
                              readOnly: true
                        created:
                          type: string
                          description: |
                            __Read-only__ When this Longview Client was created.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                          x-linode-cli-display: 3
                        install_code:
                          type: string
                          description: |-
                            __Read-only__ The install code for this Client, used when configuring the Longview Client application on your Linode.

                            Returns as `[REDACTED]` if you do not have read-write access to this client.
                          example: BD1B5605-BF5E-D385-BA07AD518BE7F321
                          readOnly: true
                          x-linode-cli-display: 4
                        label:
                          type: string
                          description: |
                            This Client's unique label. This is for display purposes only.
                          example: client789
                          minLength: 3
                          maxLength: 32
                          pattern: '[a-zA-Z0-9-_]{3,32}'
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        updated:
                          type: string
                          description: |
                            __Read-only__ When this Longview Client was last updated.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-longview-clients
      security:
      - personalAccessToken: []
      - oauth:
        - longview:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/longview/clients
      - lang: CLI
        source: linode-cli longview list
      x-linode-cli-action:
      - list
      - ls
      x-linode-grant: read_only
      x-original-op-id: getLongviewClients
      x-original-op-title: Longview Clients List
    x-linode-cli-command: longview
  /longview/clients/{clientId}:
    x-akamai:
      file-path: paths/longview-client.yaml
      path-info: /longview/clients/{clientId}
    parameters:
    - name: clientId
      in: path
      description: |
        The Longview Client ID to access.
      example: '{{clientId}}'
      x-akamai:
        file-path: parameters/client-id-path-0c50f505.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-longview-client
      summary: Get a Longview client
      tags:
      - Clients
      description: |-
        Returns a single Longview Client you can access.


        ---


        - __CLI__.

            ```
            linode-cli longview view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            longview:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli longview view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: longview:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The requested Longview Client.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A LongviewClient is a single monitor set up to track statistics about one of your servers.
                x-akamai:
                  file-path: schemas/longview-client.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Client's unique ID.
                    example: 789
                    readOnly: true
                    x-linode-cli-display: 1
                  api_key:
                    type: string
                    description: |-
                      __Read-only__ The API key for this Client, used when configuring the Longview Client application on your Linode.

                      Returns as `[REDACTED]` if you do not have read-write access to this client.
                    example: BD1B4B54-D752-A76D-5A9BD8A17F39DB61
                    readOnly: true
                  apps:
                    type: object
                    description: |
                      __Read-only__ The apps this Client is monitoring on your Linode. This is configured
                      when you install the Longview Client application, and is present
                      here for information purposes only.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      apache:
                        type: boolean
                        description: |
                          __Read-only__ If True, the Apache Longview Client module is monitoring Apache
                          on your server.
                        example: true
                        readOnly: true
                      mysql:
                        type: boolean
                        description: |
                          __Read-only__ If True, the MySQL Longview Client modules is monitoring MySQL
                          on your server.
                        example: true
                        readOnly: true
                      nginx:
                        type: boolean
                        description: |
                          __Read-only__ If True, the Nginx Longview Client module is monitoring Nginx
                          on your server.
                        example: false
                        readOnly: true
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Longview Client was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 3
                  install_code:
                    type: string
                    description: |-
                      __Read-only__ The install code for this Client, used when configuring the Longview Client application on your Linode.

                      Returns as `[REDACTED]` if you do not have read-write access to this client.
                    example: BD1B5605-BF5E-D385-BA07AD518BE7F321
                    readOnly: true
                    x-linode-cli-display: 4
                  label:
                    type: string
                    description: |
                      This Client's unique label. This is for display purposes only.
                    example: client789
                    minLength: 3
                    maxLength: 32
                    pattern: '[a-zA-Z0-9-_]{3,32}'
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Longview Client was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-longview-client
      security:
      - personalAccessToken: []
      - oauth:
        - longview:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/longview/clients/789
      - lang: CLI
        source: linode-cli longview view 789
      x-linode-cli-action: view
      x-linode-grant: read_only
      x-original-op-id: getLongviewClient
      x-original-op-title: Longview Client View
    put:
      operationId: put-longview-client
      summary: Update a Longview client
      tags:
      - Clients
      description: |-
        Updates a Longview Client.  This cannot update how it monitors your server; use the Longview Client application on your Linode for monitoring configuration.


        ---


        - __CLI__.

            ```
            linode-cli longview update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            longview:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli longview update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: longview:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The fields to update.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                A LongviewClient is a single monitor set up to track statistics about one of your servers.
              x-akamai:
                file-path: schemas/longview-client.yaml
              additionalProperties: false
              properties:
                id:
                  type: integer
                  description: |
                    __Read-only__ This Client's unique ID.
                  example: '{{id}}'
                  readOnly: true
                  x-linode-cli-display: 1
                api_key:
                  type: string
                  description: |-
                    __Read-only__ The API key for this Client, used when configuring the Longview Client application on your Linode.

                    Returns as `[REDACTED]` if you do not have read-write access to this client.
                  example: '{{api_key}}'
                  readOnly: true
                apps:
                  type: object
                  description: |
                    __Read-only__ The apps this Client is monitoring on your Linode. This is configured when
                    you install the Longview Client application, and is present here
                    for information purposes only.
                  additionalProperties: false
                  readOnly: true
                  properties:
                    apache:
                      type: boolean
                      description: |
                        __Read-only__ If True, the Apache Longview Client module is monitoring Apache
                        on your server.
                      example: true
                      readOnly: true
                    mysql:
                      type: boolean
                      description: |
                        __Read-only__ If True, the MySQL Longview Client modules is monitoring MySQL
                        on your server.
                      example: true
                      readOnly: true
                    nginx:
                      type: boolean
                      description: |
                        __Read-only__ If True, the Nginx Longview Client module is monitoring Nginx
                        on your server.
                      example: false
                      readOnly: true
                created:
                  type: string
                  description: |
                    __Read-only__ When this Longview Client was created.
                  example: '{{created}}'
                  readOnly: true
                  format: date-time
                  x-linode-cli-display: 3
                install_code:
                  type: string
                  description: |-
                    __Read-only__ The install code for this Client, used when configuring the Longview Client application on your Linode.

                    Returns as `[REDACTED]` if you do not have read-write access to this client.
                  example: '{{install_code}}'
                  readOnly: true
                  x-linode-cli-display: 4
                label:
                  type: string
                  description: |
                    This Client's unique label. This is for display purposes only.
                  example: '{{label}}'
                  minLength: 3
                  maxLength: 32
                  pattern: '[a-zA-Z0-9-_]{3,32}'
                  x-linode-cli-display: 2
                  x-linode-filterable: true
                updated:
                  type: string
                  description: |
                    __Read-only__ When this Longview Client was last updated.
                  example: '{{updated}}'
                  readOnly: true
                  format: date-time
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Longview Client updated successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A LongviewClient is a single monitor set up to track statistics about one of your servers.
                x-akamai:
                  file-path: schemas/longview-client.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Client's unique ID.
                    example: 789
                    readOnly: true
                    x-linode-cli-display: 1
                  api_key:
                    type: string
                    description: |-
                      __Read-only__ The API key for this Client, used when configuring the Longview Client application on your Linode.

                      Returns as `[REDACTED]` if you do not have read-write access to this client.
                    example: BD1B4B54-D752-A76D-5A9BD8A17F39DB61
                    readOnly: true
                  apps:
                    type: object
                    description: |
                      __Read-only__ The apps this Client is monitoring on your Linode. This is configured
                      when you install the Longview Client application, and is present
                      here for information purposes only.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      apache:
                        type: boolean
                        description: |
                          __Read-only__ If True, the Apache Longview Client module is monitoring Apache
                          on your server.
                        example: true
                        readOnly: true
                      mysql:
                        type: boolean
                        description: |
                          __Read-only__ If True, the MySQL Longview Client modules is monitoring MySQL
                          on your server.
                        example: true
                        readOnly: true
                      nginx:
                        type: boolean
                        description: |
                          __Read-only__ If True, the Nginx Longview Client module is monitoring Nginx
                          on your server.
                        example: false
                        readOnly: true
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Longview Client was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 3
                  install_code:
                    type: string
                    description: |-
                      __Read-only__ The install code for this Client, used when configuring the Longview Client application on your Linode.

                      Returns as `[REDACTED]` if you do not have read-write access to this client.
                    example: BD1B5605-BF5E-D385-BA07AD518BE7F321
                    readOnly: true
                    x-linode-cli-display: 4
                  label:
                    type: string
                    description: |
                      This Client's unique label. This is for display purposes only.
                    example: client789
                    minLength: 3
                    maxLength: 32
                    pattern: '[a-zA-Z0-9-_]{3,32}'
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Longview Client was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-longview-client
      security:
      - personalAccessToken: []
      - oauth:
        - longview:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "label": "client789"
              }' \
              https://api.linode.com/v4/longview/clients/789
      - lang: CLI
        source: |-
          linode-cli longview update 789 \
            --label client789
      x-linode-cli-action: update
      x-linode-grant: read_write
      x-original-op-id: updateLongviewClient
      x-original-op-title: Longview Client Update
    delete:
      operationId: delete-longview-client
      summary: Delete a Longview client
      tags:
      - Clients
      description: |-
        Deletes a Longview Client from your Account.

        __All information stored for this client will be lost.__

        This _does not_ uninstall the Longview Client application for your Linode - you must do that manually.


        ---


        - __OAuth__.

            ```
            longview:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: longview:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Longview Client deleted successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-longview-client
      security:
      - personalAccessToken: []
      - oauth:
        - longview:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/longview/clients/789
      - lang: CLI
        source: linode-cli longview delete 789
      x-linode-cli-action:
      - delete
      - rm
      x-linode-grant: read_write
      x-original-op-id: deleteLongviewClient
      x-original-op-title: Longview Client Delete
    x-linode-cli-command: longview
  /longview/plan:
    x-akamai:
      file-path: paths/plan.yaml
      path-info: /longview/plan
    get:
      operationId: get-longview-plan
      summary: Get a Longview plan
      tags:
      - Plans
      description: |-
        Get the details of your current Longview plan. This returns a `LongviewSubscription` object for your current Longview Pro plan, or an empty set `{}` if your current plan is Longview Free.

        You must have at least one of the following `global` [List a user's grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants) in order to run this operation:

          - `"account_access": read_write`
          - `"account_access": read_only`
          - `"longview_subscription": true`
          - `"add_longview": true`

        To update your subscription plan, send a request to [Update a Longview plan](https://techdocs.akamai.com/linode-api/reference/put-longview-plan).


        ---


        - __CLI__.

            ```
            linode-cli longview plan-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            longview:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli longview plan-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: longview:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The Longview plan details for this account.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A Longview Subscription represents a tier of Longview service you can subscribe to.
                x-akamai:
                  file-path: schemas/longview-subscription.yaml
                additionalProperties: false
                properties:
                  id:
                    type: string
                    description: |
                      __Read-only__ The unique ID of this Subscription tier.
                    example: longview-10
                    readOnly: true
                    enum:
                    - longview-3
                    - longview-10
                    - longview-40
                    - longview-100
                    x-linode-cli-display: 1
                  clients_included:
                    type: integer
                    description: |
                      __Read-only__ The number of Longview Clients that may be created with this Subscription
                      tier.
                    example: 10
                    readOnly: true
                    x-linode-cli-display: 3
                  label:
                    type: string
                    description: |
                      __Read-only__ A display name for this Subscription tier.
                    example: Longview Pro 10 pack
                    readOnly: true
                    x-linode-cli-display: 2
                  price:
                    type: object
                    description: |
                      __Read-only__ Pricing information about this Subscription tier.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      hourly:
                        type: number
                        description: |
                          __Read-only__ The hourly price, in US dollars, for this Subscription tier.
                        example: 0.06
                        readOnly: true
                      monthly:
                        type: number
                        description: |
                          __Read-only__ The maximum monthly price in US Dollars for this Subscription
                          tier. You will never be charged more than this amount per
                          month for this subscription.
                        example: 40
                        readOnly: true
                    x-linode-cli-display: 4
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-longview-plan
      security:
      - personalAccessToken: []
      - oauth:
        - longview:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/longview/plan
      - lang: CLI
        source: linode-cli longview plan-view
      x-linode-cli-action: plan-view
      x-original-op-id: getLongviewPlan
      x-original-op-title: Longview Plan View
    put:
      operationId: put-longview-plan
      summary: Update a Longview plan
      tags:
      - Plans
      description: |-
        Update your Longview plan to that of the given subscription ID. This returns a `LongviewSubscription` object for the updated Longview Pro plan, or an empty set `{}` if the updated plan is Longview Free.

        You must have `"longview_subscription": true` configured as a `global` [user grant](https://techdocs.akamai.com/linode-api/reference/get-user-grants) in order to run this operation.

        You can send a request to the [List Longview subscriptions](https://techdocs.akamai.com/linode-api/reference/get-longview-subscriptions) operation to receive the details, including `id`'s, of each plan.


        ---


        - __CLI__.

            ```
            linode-cli longview plan-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            longview:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli longview plan-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: longview:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Update your Longview subscription plan.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                Longview Plan object.
              x-akamai:
                file-path: schemas/longview-plan.yaml
              additionalProperties: false
              properties:
                longview_subscription:
                  type: string
                  description: |
                    The subscription ID for a particular Longview plan. A value of `null` corresponds to Longview
                    Free. You can send a request to the [List Longview subscriptions](https://techdocs.akamai.com/linode-api/reference/get-longview-subscriptions)
                    operation to receive the details of each plan.
                  example: '{{longview_subscription}}'
                  nullable: true
                  enum:
                  - longview-3
                  - longview-10
                  - longview-40
                  - longview-100
                  x-linode-cli-display: 1
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The updated Longview plan details for this account.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A Longview Subscription represents a tier of Longview service you can subscribe to.
                x-akamai:
                  file-path: schemas/longview-subscription.yaml
                additionalProperties: false
                properties:
                  id:
                    type: string
                    description: |
                      __Read-only__ The unique ID of this Subscription tier.
                    example: longview-10
                    readOnly: true
                    enum:
                    - longview-3
                    - longview-10
                    - longview-40
                    - longview-100
                    x-linode-cli-display: 1
                  clients_included:
                    type: integer
                    description: |
                      __Read-only__ The number of Longview Clients that may be created with this Subscription
                      tier.
                    example: 10
                    readOnly: true
                    x-linode-cli-display: 3
                  label:
                    type: string
                    description: |
                      __Read-only__ A display name for this Subscription tier.
                    example: Longview Pro 10 pack
                    readOnly: true
                    x-linode-cli-display: 2
                  price:
                    type: object
                    description: |
                      __Read-only__ Pricing information about this Subscription tier.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      hourly:
                        type: number
                        description: |
                          __Read-only__ The hourly price, in US dollars, for this Subscription tier.
                        example: 0.06
                        readOnly: true
                      monthly:
                        type: number
                        description: |
                          __Read-only__ The maximum monthly price in US Dollars for this Subscription
                          tier. You will never be charged more than this amount per
                          month for this subscription.
                        example: 40
                        readOnly: true
                    x-linode-cli-display: 4
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-longview-plan
      security:
      - personalAccessToken: []
      - oauth:
        - longview:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                  "longview_subscription": "longview-10"
              }' \
              https://api.linode.com/v4/longview/plan
      - lang: CLI
        source: linode-cli longview plan-update --longview_subscription longview-10
      x-linode-cli-action: plan-update
      x-original-op-id: updateLongviewPlan
      x-original-op-title: Longview Plan Update
    x-linode-cli-command: longview
  /longview/subscriptions:
    x-akamai:
      file-path: paths/subscriptions.yaml
      path-info: /longview/subscriptions
    get:
      operationId: get-longview-subscriptions
      summary: List Longview subscriptions
      tags:
      - Subscriptions
      description: |
        Returns a paginated list of available Longview Subscriptions. This is a public endpoint and requires no authentication.
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of Longview Subscriptions.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        A Longview Subscription represents a tier of Longview service you can subscribe to.
                      x-akamai:
                        file-path: schemas/longview-subscription.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: string
                          description: |
                            __Read-only__ The unique ID of this Subscription tier.
                          example: longview-10
                          readOnly: true
                          enum:
                          - longview-3
                          - longview-10
                          - longview-40
                          - longview-100
                          x-linode-cli-display: 1
                        clients_included:
                          type: integer
                          description: |
                            __Read-only__ The number of Longview Clients that may be created with
                            this Subscription tier.
                          example: 10
                          readOnly: true
                          x-linode-cli-display: 3
                        label:
                          type: string
                          description: |
                            __Read-only__ A display name for this Subscription tier.
                          example: Longview Pro 10 pack
                          readOnly: true
                          x-linode-cli-display: 2
                        price:
                          type: object
                          description: |
                            __Read-only__ Pricing information about this Subscription tier.
                          additionalProperties: false
                          readOnly: true
                          properties:
                            hourly:
                              type: number
                              description: |
                                __Read-only__ The hourly price, in US dollars, for this Subscription
                                tier.
                              example: 0.06
                              readOnly: true
                            monthly:
                              type: number
                              description: |
                                __Read-only__ The maximum monthly price in US Dollars for this Subscription
                                tier. You will never be charged more than this amount
                                per month for this subscription.
                              example: 40
                              readOnly: true
                          x-linode-cli-display: 4
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-longview-subscriptions
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/longview/subscriptions
      - lang: CLI
        source: linode-cli longview subscriptions-list
      x-linode-cli-action: subscriptions-list
      x-original-op-id: getLongviewSubscriptions
      x-original-op-title: Longview Subscriptions List
    x-linode-cli-command: longview
  /longview/subscriptions/{subscriptionId}:
    x-akamai:
      file-path: paths/subscription.yaml
      path-info: /longview/subscriptions/{subscriptionId}
    parameters:
    - name: subscriptionId
      in: path
      description: |
        The Longview Subscription to look up.
      example: '{{subscriptionId}}'
      x-akamai:
        file-path: parameters/subscription-id-path.yaml
      required: true
      schema:
        type: string
    get:
      operationId: get-longview-subscription
      summary: Get a Longview subscription
      tags:
      - Subscriptions
      description: |
        Get the Longview plan details as a single `LongviewSubscription` object for the provided subscription ID. This
        is a public endpoint and requires no authentication.
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The requested Longview Subscription details.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A Longview Subscription represents a tier of Longview service you can subscribe to.
                x-akamai:
                  file-path: schemas/longview-subscription.yaml
                additionalProperties: false
                properties:
                  id:
                    type: string
                    description: |
                      __Read-only__ The unique ID of this Subscription tier.
                    example: longview-10
                    readOnly: true
                    enum:
                    - longview-3
                    - longview-10
                    - longview-40
                    - longview-100
                    x-linode-cli-display: 1
                  clients_included:
                    type: integer
                    description: |
                      __Read-only__ The number of Longview Clients that may be created with this Subscription
                      tier.
                    example: 10
                    readOnly: true
                    x-linode-cli-display: 3
                  label:
                    type: string
                    description: |
                      __Read-only__ A display name for this Subscription tier.
                    example: Longview Pro 10 pack
                    readOnly: true
                    x-linode-cli-display: 2
                  price:
                    type: object
                    description: |
                      __Read-only__ Pricing information about this Subscription tier.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      hourly:
                        type: number
                        description: |
                          __Read-only__ The hourly price, in US dollars, for this Subscription tier.
                        example: 0.06
                        readOnly: true
                      monthly:
                        type: number
                        description: |
                          __Read-only__ The maximum monthly price in US Dollars for this Subscription
                          tier. You will never be charged more than this amount per
                          month for this subscription.
                        example: 40
                        readOnly: true
                    x-linode-cli-display: 4
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-longview-subscription
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/longview/subscriptions/longview-10
      - lang: CLI
        source: |-
          linode-cli longview subscription-view \
            longview-10
      x-linode-cli-action: subscription-view
      x-original-op-id: getLongviewSubscription
      x-original-op-title: Longview Subscription View
    x-linode-cli-command: longview
  /managed/contacts:
    x-akamai:
      file-path: paths/contacts.yaml
      path-info: /managed/contacts
    post:
      operationId: post-managed-contact
      summary: Create a managed contact
      tags:
      - Contacts
      description: |-
        Creates a Managed Contact.  A Managed Contact is someone Linode special forces can contact in the course of attempting to resolve an issue with a Managed Service.

        This operation can only be accessed by the unrestricted users of an account.


        ---


        - __CLI__.

            ```
            linode-cli managed contact-create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli managed contact-create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the contact to create.
        content:
          application/json:
            schema:
              type: object
              description: |
                Information about someone Linode's special forces may contact in case an issue is detected with
                a manager service.
              x-akamai:
                file-path: schemas/managed-contact.yaml
              additionalProperties: false
              properties:
                id:
                  type: integer
                  description: |
                    __Read-only__ This Contact's unique ID.
                  example: '{{id}}'
                  readOnly: true
                  x-linode-cli-display: 1
                name:
                  type: string
                  description: |
                    The name of this Contact.
                  example: '{{name}}'
                  minLength: 2
                  maxLength: 64
                  pattern: '[a-zA-Z0-9-_ ]{2,64}'
                  x-linode-cli-display: 2
                email:
                  type: string
                  description: |
                    The address to email this Contact to alert them of issues.
                  example: '{{email}}'
                  format: email
                  x-linode-cli-display: 3
                group:
                  type: string
                  description: |
                    A grouping for this Contact. This is for display purposes only.
                  example: '{{group}}'
                  nullable: true
                  minLength: 2
                  maxLength: 50
                  x-linode-cli-display: 5
                  x-linode-filterable: true
                phone:
                  type: object
                  description: |
                    Information about how to reach this Contact by phone.
                  additionalProperties: false
                  properties:
                    primary:
                      type: string
                      description: |
                        This Contact's primary phone number.
                      example: 123-456-7890
                      nullable: true
                      format: phone
                    secondary:
                      type: string
                      description: |
                        This Contact's secondary phone number.
                      example: null
                      nullable: true
                      format: phone
                  x-linode-cli-display: 4
                updated:
                  type: string
                  description: |
                    __Read-only__ When this Contact was last updated.
                  example: '{{updated}}'
                  readOnly: true
                  format: date-time
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Contact created.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Information about someone Linode's special forces may contact in case an issue is detected
                  with a manager service.
                x-akamai:
                  file-path: schemas/managed-contact.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Contact's unique ID.
                    example: 567
                    readOnly: true
                    x-linode-cli-display: 1
                  name:
                    type: string
                    description: |
                      The name of this Contact.
                    example: John Doe
                    minLength: 2
                    maxLength: 64
                    pattern: '[a-zA-Z0-9-_ ]{2,64}'
                    x-linode-cli-display: 2
                  email:
                    type: string
                    description: |
                      The address to email this Contact to alert them of issues.
                    example: john.doe@example.org
                    format: email
                    x-linode-cli-display: 3
                  group:
                    type: string
                    description: |
                      A grouping for this Contact. This is for display purposes only.
                    example: on-call
                    nullable: true
                    minLength: 2
                    maxLength: 50
                    x-linode-cli-display: 5
                    x-linode-filterable: true
                  phone:
                    type: object
                    description: |
                      Information about how to reach this Contact by phone.
                    additionalProperties: false
                    properties:
                      primary:
                        type: string
                        description: |
                          This Contact's primary phone number.
                        example: 123-456-7890
                        nullable: true
                        format: phone
                      secondary:
                        type: string
                        description: |
                          This Contact's secondary phone number.
                        example: null
                        nullable: true
                        format: phone
                    x-linode-cli-display: 4
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Contact was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-managed-contact
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "name": "John Doe",
                "email": "john.doe@example.org",
                "phone": {
                  "primary": "123-456-7890",
                  "secondary": null
                },
                "group": "on-call"
              }' \
              https://api.linode.com/v4/managed/contacts
      - lang: CLI
        source: |-
          linode-cli managed contact-create \
            --name "John Doe" \
            --email "john.doe@example.org" \
            --phone.primary "123-456-7890"
      x-linode-cli-action: contact-create
      x-linode-grant: unrestricted only
      x-original-op-id: createManagedContact
      x-original-op-title: Managed Contact Create
    get:
      operationId: get-managed-contacts
      summary: List managed contacts
      tags:
      - Contacts
      description: |-
        Returns a paginated list of Managed Contacts on your Account.

        This operation can only be accessed by the unrestricted users of an account.


        ---


        - __CLI__.

            ```
            linode-cli managed contacts-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli managed contacts-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of ManagedContacts
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        Information about someone Linode's special forces may contact in case an issue
                        is detected with a manager service.
                      x-akamai:
                        file-path: schemas/managed-contact.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ This Contact's unique ID.
                          example: 567
                          readOnly: true
                          x-linode-cli-display: 1
                        name:
                          type: string
                          description: |
                            The name of this Contact.
                          example: John Doe
                          minLength: 2
                          maxLength: 64
                          pattern: '[a-zA-Z0-9-_ ]{2,64}'
                          x-linode-cli-display: 2
                        email:
                          type: string
                          description: |
                            The address to email this Contact to alert them of issues.
                          example: john.doe@example.org
                          format: email
                          x-linode-cli-display: 3
                        group:
                          type: string
                          description: |
                            A grouping for this Contact. This is for display purposes only.
                          example: on-call
                          nullable: true
                          minLength: 2
                          maxLength: 50
                          x-linode-cli-display: 5
                          x-linode-filterable: true
                        phone:
                          type: object
                          description: |
                            Information about how to reach this Contact by phone.
                          additionalProperties: false
                          properties:
                            primary:
                              type: string
                              description: |
                                This Contact's primary phone number.
                              example: 123-456-7890
                              nullable: true
                              format: phone
                            secondary:
                              type: string
                              description: |
                                This Contact's secondary phone number.
                              example: null
                              nullable: true
                              format: phone
                          x-linode-cli-display: 4
                        updated:
                          type: string
                          description: |
                            __Read-only__ When this Contact was last updated.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-managed-contacts
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/managed/contacts
      - lang: CLI
        source: linode-cli managed contacts-list
      x-linode-cli-action: contacts-list
      x-linode-grant: unrestricted only
      x-original-op-id: getManagedContacts
      x-original-op-title: Managed Contacts List
    x-linode-cli-command: managed
  /managed/contacts/{contactId}:
    x-akamai:
      file-path: paths/contact.yaml
      path-info: /managed/contacts/{contactId}
    parameters:
    - name: contactId
      in: path
      description: |
        The ID of the contact to access.
      example: '{{contactId}}'
      x-akamai:
        file-path: parameters/contact-id-path.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-managed-contact
      summary: Get a managed contact
      tags:
      - Contacts
      description: |-
        Returns a single Managed Contact.

        This operation can only be accessed by the unrestricted users of an account.


        ---


        - __CLI__.

            ```
            linode-cli managed contact-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli managed contact-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The requested Managed Contact.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Information about someone Linode's special forces may contact in case an issue is detected
                  with a manager service.
                x-akamai:
                  file-path: schemas/managed-contact.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Contact's unique ID.
                    example: 567
                    readOnly: true
                    x-linode-cli-display: 1
                  name:
                    type: string
                    description: |
                      The name of this Contact.
                    example: John Doe
                    minLength: 2
                    maxLength: 64
                    pattern: '[a-zA-Z0-9-_ ]{2,64}'
                    x-linode-cli-display: 2
                  email:
                    type: string
                    description: |
                      The address to email this Contact to alert them of issues.
                    example: john.doe@example.org
                    format: email
                    x-linode-cli-display: 3
                  group:
                    type: string
                    description: |
                      A grouping for this Contact. This is for display purposes only.
                    example: on-call
                    nullable: true
                    minLength: 2
                    maxLength: 50
                    x-linode-cli-display: 5
                    x-linode-filterable: true
                  phone:
                    type: object
                    description: |
                      Information about how to reach this Contact by phone.
                    additionalProperties: false
                    properties:
                      primary:
                        type: string
                        description: |
                          This Contact's primary phone number.
                        example: 123-456-7890
                        nullable: true
                        format: phone
                      secondary:
                        type: string
                        description: |
                          This Contact's secondary phone number.
                        example: null
                        nullable: true
                        format: phone
                    x-linode-cli-display: 4
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Contact was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-managed-contact
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/managed/contacts/567
      - lang: CLI
        source: linode-cli managed contact-view 567
      x-linode-cli-action: contact-view
      x-linode-grant: unrestricted only
      x-original-op-id: getManagedContact
      x-original-op-title: Managed Contact View
    put:
      operationId: put-managed-contact
      summary: Update a managed contact
      tags:
      - Contacts
      description: |-
        Updates information about a Managed Contact. This operation can only be accessed by the unrestricted users of an account.


        ---


        - __CLI__.

            ```
            linode-cli managed contact-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli managed contact-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The fields to update.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                Information about someone Linode's special forces may contact in case an issue is detected with
                a manager service.
              x-akamai:
                file-path: schemas/managed-contact.yaml
              additionalProperties: false
              properties:
                id:
                  type: integer
                  description: |
                    __Read-only__ This Contact's unique ID.
                  example: '{{id}}'
                  readOnly: true
                  x-linode-cli-display: 1
                name:
                  type: string
                  description: |
                    The name of this Contact.
                  example: '{{name}}'
                  minLength: 2
                  maxLength: 64
                  pattern: '[a-zA-Z0-9-_ ]{2,64}'
                  x-linode-cli-display: 2
                email:
                  type: string
                  description: |
                    The address to email this Contact to alert them of issues.
                  example: '{{email}}'
                  format: email
                  x-linode-cli-display: 3
                group:
                  type: string
                  description: |
                    A grouping for this Contact. This is for display purposes only.
                  example: '{{group}}'
                  nullable: true
                  minLength: 2
                  maxLength: 50
                  x-linode-cli-display: 5
                  x-linode-filterable: true
                phone:
                  type: object
                  description: |
                    Information about how to reach this Contact by phone.
                  additionalProperties: false
                  properties:
                    primary:
                      type: string
                      description: |
                        This Contact's primary phone number.
                      example: 123-456-7890
                      nullable: true
                      format: phone
                    secondary:
                      type: string
                      description: |
                        This Contact's secondary phone number.
                      example: null
                      nullable: true
                      format: phone
                  x-linode-cli-display: 4
                updated:
                  type: string
                  description: |
                    __Read-only__ When this Contact was last updated.
                  example: '{{updated}}'
                  readOnly: true
                  format: date-time
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Contact updated successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Information about someone Linode's special forces may contact in case an issue is detected
                  with a manager service.
                x-akamai:
                  file-path: schemas/managed-contact.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Contact's unique ID.
                    example: 567
                    readOnly: true
                    x-linode-cli-display: 1
                  name:
                    type: string
                    description: |
                      The name of this Contact.
                    example: John Doe
                    minLength: 2
                    maxLength: 64
                    pattern: '[a-zA-Z0-9-_ ]{2,64}'
                    x-linode-cli-display: 2
                  email:
                    type: string
                    description: |
                      The address to email this Contact to alert them of issues.
                    example: john.doe@example.org
                    format: email
                    x-linode-cli-display: 3
                  group:
                    type: string
                    description: |
                      A grouping for this Contact. This is for display purposes only.
                    example: on-call
                    nullable: true
                    minLength: 2
                    maxLength: 50
                    x-linode-cli-display: 5
                    x-linode-filterable: true
                  phone:
                    type: object
                    description: |
                      Information about how to reach this Contact by phone.
                    additionalProperties: false
                    properties:
                      primary:
                        type: string
                        description: |
                          This Contact's primary phone number.
                        example: 123-456-7890
                        nullable: true
                        format: phone
                      secondary:
                        type: string
                        description: |
                          This Contact's secondary phone number.
                        example: null
                        nullable: true
                        format: phone
                    x-linode-cli-display: 4
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Contact was last updated.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-managed-contact
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "name": "John Doe",
                "email": "john.doe@example.org",
                "phone": {
                  "primary": "123-456-7890",
                  "secondary": null
                },
                "group": "on-call"
              }' \
              https://api.linode.com/v4/managed/contacts/567
      - lang: CLI
        source: |-
          linode-cli managed contact-update 567 \
            --name "John Doe" \
            --email "john.doe@example.org" \
            --phone.primary "123-456-7890"
      x-linode-cli-action: contact-update
      x-linode-grant: unrestricted only
      x-original-op-id: updateManagedContact
      x-original-op-title: Managed Contact Update
    delete:
      operationId: delete-managed-contact
      summary: Delete a managed contact
      tags:
      - Contacts
      description: |-
        Deletes a Managed Contact.

        This operation can only be accessed by the unrestricted users of an account.


        ---


        - __CLI__.

            ```
            linode-cli managed contact-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli managed contact-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Contact deleted successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-managed-contact
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/managed/contacts/567
      - lang: CLI
        source: linode-cli managed contact-delete 567
      x-linode-cli-action: contact-delete
      x-linode-grant: unrestricted only
      x-original-op-id: deleteManagedContact
      x-original-op-title: Managed Contact Delete
    x-linode-cli-command: managed
  /managed/credentials:
    x-akamai:
      file-path: paths/credentials.yaml
      path-info: /managed/credentials
    post:
      operationId: post-managed-credential
      summary: Create a managed credential
      tags:
      - Credentials
      description: |-
        Creates a Managed Credential. A Managed Credential is stored securely to allow Linode special forces to access your Managed Services and resolve issues.

        This operation can only be accessed by the unrestricted users of an account.


        ---


        - __CLI__.

            ```
            linode-cli managed credential-create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli managed credential-create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the Credential to create.
        content:
          application/json:
            schema:
              required:
              - label
              - password
              allOf:
              - type: object
                description: |
                  A securely-stored Credential that allows Linode's special forces to access a Managed server to
                  respond to Issues.
                x-akamai:
                  file-path: schemas/managed-credential.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Credential's unique ID.
                    example: 9991
                    readOnly: true
                    x-linode-cli-display: 1
                  label:
                    type: string
                    description: |
                      The unique label for this Credential. This is for display purposes only.
                    example: prod-password-1
                    minLength: 2
                    maxLength: 75
                    pattern: '[a-zA-Z0-9-_ \.]{2,75}'
                    x-linode-cli-display: 2
                  last_decrypted:
                    type: string
                    description: |
                      __Read-only__ The date this Credential was last decrypted by a member of Linode special
                      forces.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 3
              - type: object
                properties:
                  password:
                    type: string
                    description: |
                      The password to use when accessing the Managed Service.
                    example: s3cur3P@ssw0rd
                  username:
                    type: string
                    description: |
                      The username to use when accessing the Managed Service.
                    example: johndoe
                    minLength: 0
                    maxLength: 5000
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Credential created.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A securely-stored Credential that allows Linode's special forces to access a Managed server to
                  respond to Issues.
                x-akamai:
                  file-path: schemas/managed-credential.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Credential's unique ID.
                    example: 9991
                    readOnly: true
                    x-linode-cli-display: 1
                  label:
                    type: string
                    description: |
                      The unique label for this Credential. This is for display purposes only.
                    example: prod-password-1
                    minLength: 2
                    maxLength: 75
                    pattern: '[a-zA-Z0-9-_ \.]{2,75}'
                    x-linode-cli-display: 2
                  last_decrypted:
                    type: string
                    description: |
                      __Read-only__ The date this Credential was last decrypted by a member of Linode special
                      forces.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 3
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-managed-credential
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "label": "prod-password-1",
                "username": "johndoe",
                "password": "s3cur3P@ssw0rd"
              }' \
              https://api.linode.com/v4/managed/credentials
      - lang: CLI
        source: |-
          linode-cli managed credential-create \
            --label prod-password-1 \
            --username johndoe \
            --password s3cur3P@ssw0rd
      x-linode-cli-action: credential-create
      x-linode-grant: unrestricted only
      x-original-op-id: createManagedCredential
      x-original-op-title: Managed Credential Create
    get:
      operationId: get-managed-credentials
      summary: List managed credentials
      tags:
      - Credentials
      description: |-
        Returns a paginated list of Managed Credentials on your Account.

        This operation can only be accessed by the unrestricted users of an account.


        ---


        - __CLI__.

            ```
            linode-cli managed credentials-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli managed credentials-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of ManagedCredentials
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        A securely-stored Credential that allows Linode's special forces to access a
                        Managed server to respond to Issues.
                      x-akamai:
                        file-path: schemas/managed-credential.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ This Credential's unique ID.
                          example: 9991
                          readOnly: true
                          x-linode-cli-display: 1
                        label:
                          type: string
                          description: |
                            The unique label for this Credential. This is for display purposes only.
                          example: prod-password-1
                          minLength: 2
                          maxLength: 75
                          pattern: '[a-zA-Z0-9-_ \.]{2,75}'
                          x-linode-cli-display: 2
                        last_decrypted:
                          type: string
                          description: |
                            __Read-only__ The date this Credential was last decrypted by a member
                            of Linode special forces.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                          x-linode-cli-display: 3
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-managed-credentials
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/managed/credentials
      - lang: CLI
        source: linode-cli managed credentials-list
      x-linode-cli-action: credentials-list
      x-linode-grant: unrestricted only
      x-original-op-id: getManagedCredentials
      x-original-op-title: Managed Credentials List
    x-linode-cli-command: managed
  /managed/credentials/sshkey:
    x-akamai:
      file-path: paths/managed-ssh-key.yaml
      path-info: /managed/credentials/sshkey
    get:
      operationId: get-managed-ssh-key
      summary: Get a managed SSH key
      tags:
      - Managed SSH keys
      description: |-
        Returns the unique SSH public key assigned to your Linode account's Managed service. If you [add this public key](https://www.linode.com/docs/products/services/managed/get-started/#adding-the-public-key) to a Linode on your account, Linode special forces will be able to log in to the Linode with this key when attempting to resolve issues.

        This operation can only be accessed by the unrestricted users of an account.


        ---


        - __CLI__.

            ```
            linode-cli managed credential-sshkey-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli managed credential-sshkey-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The requested Managed SSH public key.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A unique SSH public key that allows Linode's special forces to access a Managed server to
                  respond to Issues.
                additionalProperties: false
                properties:
                  ssh_key:
                    type: string
                    description: |
                      The unique SSH public key assigned to your Linode account's Managed service.
                    example: ssh-rsa AAAAB...oD2ZQ== managedservices@linode
                    readOnly: true
                    x-linode-cli-display: 1
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-managed-ssh-key
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/managed/credentials/sshkey
      - lang: CLI
        source: linode-cli managed credential-sshkey-view
      x-linode-cli-action: credential-sshkey-view
      x-linode-grant: unrestricted only
      x-original-op-id: viewManagedSSHKey
      x-original-op-title: Managed SSH Key View
    x-linode-cli-command: managed
  /managed/credentials/{credentialId}:
    x-akamai:
      file-path: paths/credential.yaml
      path-info: /managed/credentials/{credentialId}
    parameters:
    - name: credentialId
      in: path
      description: |
        The ID of the Credential to access.
      example: '{{credentialId}}'
      x-akamai:
        file-path: parameters/credential-id-path.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-managed-credential
      summary: Get a managed credential
      tags:
      - Credentials
      description: |-
        Returns a single Managed Credential.

        This operation can only be accessed by the unrestricted users of an account.


        ---


        - __CLI__.

            ```
            linode-cli managed credential-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli managed credential-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The requested Managed Credential.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A securely-stored Credential that allows Linode's special forces to access a Managed server to
                  respond to Issues.
                x-akamai:
                  file-path: schemas/managed-credential.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Credential's unique ID.
                    example: 9991
                    readOnly: true
                    x-linode-cli-display: 1
                  label:
                    type: string
                    description: |
                      The unique label for this Credential. This is for display purposes only.
                    example: prod-password-1
                    minLength: 2
                    maxLength: 75
                    pattern: '[a-zA-Z0-9-_ \.]{2,75}'
                    x-linode-cli-display: 2
                  last_decrypted:
                    type: string
                    description: |
                      __Read-only__ The date this Credential was last decrypted by a member of Linode special
                      forces.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 3
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-managed-credential
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/managed/credentials/9991
      - lang: CLI
        source: linode-cli managed credential-view 9991
      x-linode-cli-action: credential-view
      x-linode-grant: unrestricted only
      x-original-op-id: getManagedCredential
      x-original-op-title: Managed Credential View
    put:
      operationId: put-managed-credential
      summary: Update a managed credential
      tags:
      - Credentials
      description: |-
        Updates the label of a Managed Credential. This operation does not update the username and password for a Managed Credential. To do this, run the [Update a managed credential's username and password](https://techdocs.akamai.com/linode-api/reference/post-managed-credential-username-password)) operation instead. This operation can only be accessed by the unrestricted users of an account.


        ---


        - __CLI__.

            ```
            linode-cli managed credential-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli managed credential-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The fields to update.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                A securely-stored Credential that allows Linode's special forces to access a Managed server to
                respond to Issues.
              x-akamai:
                file-path: schemas/managed-credential.yaml
              additionalProperties: false
              properties:
                id:
                  type: integer
                  description: |
                    __Read-only__ This Credential's unique ID.
                  example: '{{id}}'
                  readOnly: true
                  x-linode-cli-display: 1
                label:
                  type: string
                  description: |
                    The unique label for this Credential. This is for display purposes only.
                  example: '{{label}}'
                  minLength: 2
                  maxLength: 75
                  pattern: '[a-zA-Z0-9-_ \.]{2,75}'
                  x-linode-cli-display: 2
                last_decrypted:
                  type: string
                  description: |
                    __Read-only__ The date this Credential was last decrypted by a member of Linode special
                    forces.
                  example: '{{last_decrypted}}'
                  readOnly: true
                  format: date-time
                  x-linode-cli-display: 3
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Credential updated successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A securely-stored Credential that allows Linode's special forces to access a Managed server to
                  respond to Issues.
                x-akamai:
                  file-path: schemas/managed-credential.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Credential's unique ID.
                    example: 9991
                    readOnly: true
                    x-linode-cli-display: 1
                  label:
                    type: string
                    description: |
                      The unique label for this Credential. This is for display purposes only.
                    example: prod-password-1
                    minLength: 2
                    maxLength: 75
                    pattern: '[a-zA-Z0-9-_ \.]{2,75}'
                    x-linode-cli-display: 2
                  last_decrypted:
                    type: string
                    description: |
                      __Read-only__ The date this Credential was last decrypted by a member of Linode special
                      forces.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 3
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-managed-credential
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "label": "prod-password-1"
              }' \
              https://api.linode.com/v4/managed/credentials/9991
      - lang: CLI
        source: |-
          linode-cli managed credential-update 9991 \
            --label prod-password-1
      x-linode-cli-action: credential-update
      x-linode-grant: unrestricted only
      x-original-op-id: updateManagedCredential
      x-original-op-title: Managed Credential Update
    x-linode-cli-command: managed
  /managed/credentials/{credentialId}/revoke:
    x-akamai:
      file-path: paths/revoke.yaml
      path-info: /managed/credentials/{credentialId}/revoke
    parameters:
    - name: credentialId
      in: path
      description: |
        The ID of the Credential to access.
      example: '{{credentialId}}'
      x-akamai:
        file-path: parameters/credential-id-path-7162d62f.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-managed-credential-revoke
      summary: Delete a managed credential
      tags:
      - Credentials
      description: |-
        Deletes a Managed Credential.  Linode special forces will no longer have access to this Credential when attempting to resolve issues.

        This operation can only be accessed by the unrestricted users of an account.


        ---


        - __CLI__.

            ```
            linode-cli managed credential-revoke
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli managed credential-revoke
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Credential deleted successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-managed-credential-revoke
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/managed/credentials/9991
      - lang: CLI
        source: linode-cli managed credential-revoke 9991
      x-linode-cli-action: credential-revoke
      x-linode-grant: unrestricted only
      x-original-op-id: deleteManagedCredential
      x-original-op-title: Managed Credential Delete
    x-linode-cli-command: managed
  /managed/credentials/{credentialId}/update:
    x-akamai:
      file-path: paths/update.yaml
      path-info: /managed/credentials/{credentialId}/update
    parameters:
    - name: credentialId
      in: path
      description: |
        The ID of the Credential to update.
      example: '{{credentialId}}'
      x-akamai:
        file-path: parameters/credential-id-path-78b1aa7e.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-managed-credential-username-password
      summary: Update a managed credential's username and password
      tags:
      - Credentials
      description: |-
        Updates the username and password for a Managed Credential.

        This operation can only be accessed by the unrestricted users of an account.


        ---


        - __CLI__.

            ```
            linode-cli managed credential-update-username-password
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli managed credential-update-username-password
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The new username and password to assign to the Managed Credential.
        content:
          application/json:
            schema:
              additionalProperties: false
              required:
              - password
              properties:
                password:
                  type: string
                  description: |
                    The password to use when accessing the Managed Service.
                  example: s3cur3P@ssw0rd
                username:
                  type: string
                  description: |
                    The username to use when accessing the Managed Service.
                  example: johndoe
                  minLength: 0
                  maxLength: 5000
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Credential username and password updated.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-managed-credential-username-password
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "username": "johndoe",
                "password": "s3cur3P@ssw0rd"
              }' \
              https://api.linode.com/v4/managed/credentials/9991/update
      - lang: CLI
        source: |-
          linode-cli managed credential-update-username-password 9991 \
            --username johndoe \
            --password s3cur3P@ssw0rd
      x-linode-cli-action: credential-update-username-password
      x-linode-grant: unrestricted only
      x-original-op-id: updateManagedCredentialUsernamePassword
      x-original-op-title: Managed Credential Username
        and Password Update
    x-linode-cli-command: managed
  /managed/issues:
    x-akamai:
      file-path: paths/issues.yaml
      path-info: /managed/issues
    get:
      operationId: get-managed-issues
      summary: List managed issues
      tags:
      - Issues
      description: |-
        Returns a paginated list of recent and ongoing issues detected on your Managed Services.

        This operation can only be accessed by the unrestricted users of an account.


        ---


        - __CLI__.

            ```
            linode-cli managed issues-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli managed issues-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of open or ongoing Managed Issues.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        An Issue that was detected with a service Linode is managing.
                      x-akamai:
                        file-path: schemas/managed-issue.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ This Issue's unique ID.
                          example: 823
                          readOnly: true
                          x-linode-cli-display: 1
                        created:
                          type: string
                          description: |
                            __Read-only__ When this Issue was created. Issues are created in response
                            to issues detected with Managed Services, so this is also
                            when the Issue was detected.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                          x-linode-cli-display: 2
                        entity:
                          type: object
                          description: |
                            __Read-only__ The ticket this Managed Issue opened.
                          additionalProperties: false
                          readOnly: true
                          properties:
                            id:
                              type: integer
                              description: |
                                __Read-only__ This ticket's ID
                              example: 98765
                              readOnly: true
                            type:
                              type: string
                              description: |
                                __Read-only__ The type of entity this is. In this case, it is always
                                a Ticket.
                              example: ticket
                              readOnly: true
                              enum:
                              - ticket
                            label:
                              type: string
                              description: |
                                __Read-only__ The summary for this Ticket.
                              example: Managed Issue opened!
                              readOnly: true
                            url:
                              type: string
                              description: |
                                __Read-only__ The relative URL where you can access this Ticket.
                              example: /support/tickets/98765
                              readOnly: true
                              format: url
                          x-linode-cli-display: 4
                        services:
                          type: array
                          description: |
                            __Read-only__ An array of Managed Service IDs that were affected by
                            this Issue.
                          readOnly: true
                          items:
                            type: integer
                            example: 654
                          x-linode-cli-display: 3
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-managed-issues
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/managed/issues
      - lang: CLI
        source: linode-cli managed issues-list
      x-linode-cli-action: issues-list
      x-linode-grant: unrestricted only
      x-original-op-id: getManagedIssues
      x-original-op-title: Managed Issues List
    x-linode-cli-command: managed
  /managed/issues/{issueId}:
    x-akamai:
      file-path: paths/issue.yaml
      path-info: /managed/issues/{issueId}
    parameters:
    - name: issueId
      in: path
      description: |
        The Issue to look up.
      example: '{{issueId}}'
      x-akamai:
        file-path: parameters/issue-id-path.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-managed-issue
      summary: Get a managed issue
      tags:
      - Issues
      description: |-
        Returns a single Issue that is impacting or did impact one of your Managed Services.

        This operation can only be accessed by the unrestricted users of an account.


        ---


        - __CLI__.

            ```
            linode-cli managed issue-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli managed issue-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The requested issue.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An Issue that was detected with a service Linode is managing.
                x-akamai:
                  file-path: schemas/managed-issue.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Issue's unique ID.
                    example: 823
                    readOnly: true
                    x-linode-cli-display: 1
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Issue was created. Issues are created in response to issues
                      detected with Managed Services, so this is also when the Issue
                      was detected.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 2
                  entity:
                    type: object
                    description: |
                      __Read-only__ The ticket this Managed Issue opened.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      id:
                        type: integer
                        description: |
                          __Read-only__ This ticket's ID
                        example: 98765
                        readOnly: true
                      type:
                        type: string
                        description: |
                          __Read-only__ The type of entity this is. In this case, it is always a Ticket.
                        example: ticket
                        readOnly: true
                        enum:
                        - ticket
                      label:
                        type: string
                        description: |
                          __Read-only__ The summary for this Ticket.
                        example: Managed Issue opened!
                        readOnly: true
                      url:
                        type: string
                        description: |
                          __Read-only__ The relative URL where you can access this Ticket.
                        example: /support/tickets/98765
                        readOnly: true
                        format: url
                    x-linode-cli-display: 4
                  services:
                    type: array
                    description: |
                      __Read-only__ An array of Managed Service IDs that were affected by this Issue.
                    readOnly: true
                    items:
                      type: integer
                      example: 654
                    x-linode-cli-display: 3
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-managed-issue
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/managed/issues/823
      - lang: CLI
        source: linode-cli managed issue-view 823
      x-linode-cli-action: issue-view
      x-linode-grant: unrestricted only
      x-original-op-id: getManagedIssue
      x-original-op-title: Managed Issue View
    x-linode-cli-command: managed
  /managed/linode-settings:
    x-akamai:
      file-path: paths/linode-settings.yaml
      path-info: /managed/linode-settings
    get:
      operationId: get-managed-linode-settings
      summary: List managed Linode settings
      tags:
      - Linode settings
      description: |-
        Returns a paginated list of Managed Settings for your Linodes. There will be one entry per Linode on your Account.

        This operation can only be accessed by the unrestricted users of an account.


        ---


        - __CLI__.

            ```
            linode-cli managed linode-settings-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli managed linode-settings-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of Managed settings for your Linodes.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        Settings for a specific Linode related to Managed Services. There is one ManagedLinodeSettings
                        object for each Linode on your Account.
                      x-akamai:
                        file-path: schemas/managed-linode-settings.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ The ID of the Linode these Settings are for.
                          example: 123
                          readOnly: true
                          x-linode-cli-display: 1
                        group:
                          type: string
                          description: |
                            __Read-only__ The group of the Linode these Settings are for. This is
                            for display purposes only.
                          example: linodes
                          readOnly: true
                          deprecated: true
                          x-linode-cli-display: 3
                        label:
                          type: string
                          description: |
                            __Read-only__ The label of the Linode these Settings are for.
                          example: linode123
                          readOnly: true
                          x-linode-cli-display: 2
                        ssh:
                          type: object
                          description: |
                            The SSH settings for this Linode.
                          additionalProperties: false
                          properties:
                            access:
                              type: boolean
                              description: |
                                If true, Linode special forces may access this Linode over
                                ssh to respond to Issues.
                              example: true
                              default: true
                            ip:
                              type: string
                              description: |-
                                The IP Linode special forces should use to access this Linode when responding to an Issue.

                                By default, any of a Linode's IP addresses can be used for incident response access.
                              example: 203.0.113.1
                              default: any
                              format: ip
                            port:
                              type: integer
                              description: |-
                                The port Linode special forces should use to access this Linode over ssh to respond to an Issue.

                                The default `null` value corresponds to port 22.
                              example: 22
                              default: null
                              nullable: true
                              minimum: 1
                              maximum: 65535
                            user:
                              type: string
                              description: |-
                                The specific user, if any, Linode's special forces should use when accessing this Linode to respond to an issue.

                                The default `null` value corresponds to the root user.
                              example: linode
                              default: null
                              nullable: true
                              minLength: 0
                              maxLength: 32
                          x-linode-cli-display: 4
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-managed-linode-settings
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/managed/linode-settings
      - lang: CLI
        source: linode-cli managed linode-settings-list
      x-linode-cli-action: linode-settings-list
      x-linode-grant: unrestricted only
      x-original-op-id: getManagedLinodeSettings
      x-original-op-title: Managed Linode Settings List
    x-linode-cli-command: managed
  /managed/linode-settings/{linodeId}:
    x-akamai:
      file-path: paths/linode-settings-per-linode.yaml
      path-info: /managed/linode-settings/{linodeId}
    parameters:
    - name: linodeId
      in: path
      description: |
        The Linode ID whose settings we are accessing.
      example: '{{linodeId}}'
      x-akamai:
        file-path: parameters/linode-id-path-56000749.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-managed-linode-setting
      summary: Get a Linode's managed settings
      tags:
      - Managed settings
      description: |-
        Returns a single Linode's Managed settings.

        This operation can only be accessed by the unrestricted users of an account.


        ---


        - __CLI__.

            ```
            linode-cli managed linode-setting-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli managed linode-setting-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The requested Linode's Managed settings.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Settings for a specific Linode related to Managed Services. There is one ManagedLinodeSettings
                  object for each Linode on your Account.
                x-akamai:
                  file-path: schemas/managed-linode-settings.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The ID of the Linode these Settings are for.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                  group:
                    type: string
                    description: |
                      __Read-only__ The group of the Linode these Settings are for. This is for display purposes
                      only.
                    example: linodes
                    readOnly: true
                    deprecated: true
                    x-linode-cli-display: 3
                  label:
                    type: string
                    description: |
                      __Read-only__ The label of the Linode these Settings are for.
                    example: linode123
                    readOnly: true
                    x-linode-cli-display: 2
                  ssh:
                    type: object
                    description: |
                      The SSH settings for this Linode.
                    additionalProperties: false
                    properties:
                      access:
                        type: boolean
                        description: |
                          If true, Linode special forces may access this Linode over ssh to respond
                          to Issues.
                        example: true
                        default: true
                      ip:
                        type: string
                        description: |-
                          The IP Linode special forces should use to access this Linode when responding to an Issue.

                          By default, any of a Linode's IP addresses can be used for incident response access.
                        example: 203.0.113.1
                        default: any
                        format: ip
                      port:
                        type: integer
                        description: |-
                          The port Linode special forces should use to access this Linode over ssh to respond to an Issue.

                          The default `null` value corresponds to port 22.
                        example: 22
                        default: null
                        nullable: true
                        minimum: 1
                        maximum: 65535
                      user:
                        type: string
                        description: |-
                          The specific user, if any, Linode's special forces should use when accessing this Linode to respond to an issue.

                          The default `null` value corresponds to the root user.
                        example: linode
                        default: null
                        nullable: true
                        minLength: 0
                        maxLength: 32
                    x-linode-cli-display: 4
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-managed-linode-setting
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/managed/linode-settings/123
      - lang: CLI
        source: linode-cli managed linode-setting-view 123
      x-linode-cli-action: linode-setting-view
      x-linode-grant: unrestricted only
      x-original-op-id: getManagedLinodeSetting
      x-original-op-title: Linode's Managed Settings
        View
    put:
      operationId: put-managed-linode-setting
      summary: Update a Linode's managed settings
      tags:
      - Managed settings
      description: |-
        Updates a single Linode's Managed settings. This operation can only be accessed by the unrestricted users of an account.


        ---


        - __CLI__.

            ```
            linode-cli managed linode-setting-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli managed linode-setting-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The settings to update.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                Settings for a specific Linode related to Managed Services. There is one ManagedLinodeSettings object
                for each Linode on your Account.
              x-akamai:
                file-path: schemas/managed-linode-settings.yaml
              additionalProperties: false
              properties:
                id:
                  type: integer
                  description: |
                    __Read-only__ The ID of the Linode these Settings are for.
                  example: '{{id}}'
                  readOnly: true
                  x-linode-cli-display: 1
                group:
                  type: string
                  description: |
                    __Read-only__ The group of the Linode these Settings are for. This is for display purposes
                    only.
                  example: '{{group}}'
                  readOnly: true
                  deprecated: true
                  x-linode-cli-display: 3
                label:
                  type: string
                  description: |
                    __Read-only__ The label of the Linode these Settings are for.
                  example: '{{label}}'
                  readOnly: true
                  x-linode-cli-display: 2
                ssh:
                  type: object
                  description: |
                    The SSH settings for this Linode.
                  additionalProperties: false
                  properties:
                    access:
                      type: boolean
                      description: |
                        If true, Linode special forces may access this Linode over ssh to respond to Issues.
                      example: true
                      default: true
                    ip:
                      type: string
                      description: |-
                        The IP Linode special forces should use to access this Linode when responding to an Issue.

                        By default, any of a Linode's IP addresses can be used for incident response access.
                      example: 203.0.113.1
                      default: any
                      format: ip
                    port:
                      type: integer
                      description: |-
                        The port Linode special forces should use to access this Linode over ssh to respond to an Issue.

                        The default `null` value corresponds to port 22.
                      example: 22
                      default: null
                      nullable: true
                      minimum: 1
                      maximum: 65535
                    user:
                      type: string
                      description: |-
                        The specific user, if any, Linode's special forces should use when accessing this Linode to respond to an issue.

                        The default `null` value corresponds to the root user.
                      example: linode
                      default: null
                      nullable: true
                      minLength: 0
                      maxLength: 32
                  x-linode-cli-display: 4
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Settings updated successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Settings for a specific Linode related to Managed Services. There is one ManagedLinodeSettings
                  object for each Linode on your Account.
                x-akamai:
                  file-path: schemas/managed-linode-settings.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The ID of the Linode these Settings are for.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                  group:
                    type: string
                    description: |
                      __Read-only__ The group of the Linode these Settings are for. This is for display purposes
                      only.
                    example: linodes
                    readOnly: true
                    deprecated: true
                    x-linode-cli-display: 3
                  label:
                    type: string
                    description: |
                      __Read-only__ The label of the Linode these Settings are for.
                    example: linode123
                    readOnly: true
                    x-linode-cli-display: 2
                  ssh:
                    type: object
                    description: |
                      The SSH settings for this Linode.
                    additionalProperties: false
                    properties:
                      access:
                        type: boolean
                        description: |
                          If true, Linode special forces may access this Linode over ssh to respond
                          to Issues.
                        example: true
                        default: true
                      ip:
                        type: string
                        description: |-
                          The IP Linode special forces should use to access this Linode when responding to an Issue.

                          By default, any of a Linode's IP addresses can be used for incident response access.
                        example: 203.0.113.1
                        default: any
                        format: ip
                      port:
                        type: integer
                        description: |-
                          The port Linode special forces should use to access this Linode over ssh to respond to an Issue.

                          The default `null` value corresponds to port 22.
                        example: 22
                        default: null
                        nullable: true
                        minimum: 1
                        maximum: 65535
                      user:
                        type: string
                        description: |-
                          The specific user, if any, Linode's special forces should use when accessing this Linode to respond to an issue.

                          The default `null` value corresponds to the root user.
                        example: linode
                        default: null
                        nullable: true
                        minLength: 0
                        maxLength: 32
                    x-linode-cli-display: 4
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-managed-linode-setting
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "ssh": {
                  "access": true,
                  "user": "linode",
                  "ip": "203.0.113.1",
                  "port": 22
                }
              }' \
              https://api.linode.com/v4/managed/linode-settings/123
      - lang: CLI
        source: |-
          linode-cli managed linode-setting-update \
            7833234 \
            --ssh.access true \
            --ssh.user linode \
            --ssh.port 22 \
            --ssh.ip 203.0.113.1
      x-linode-cli-action: linode-setting-update
      x-linode-grant: unrestricted only
      x-original-op-id: updateManagedLinodeSetting
      x-original-op-title: Linode's Managed Settings
        Update
    x-linode-cli-command: managed
  /managed/services:
    x-akamai:
      file-path: paths/services.yaml
      path-info: /managed/services
    post:
      operationId: post-managed-service
      summary: Create a managed service
      tags:
      - Services
      description: |-
        Creates a Managed Service. Linode Managed will begin monitoring this service and reporting and attempting to resolve any Issues.

        This operation can only be accessed by the unrestricted users of an account.


        ---


        - __CLI__.

            ```
            linode-cli managed service-create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli managed service-create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the service to monitor.
        content:
          application/json:
            schema:
              required:
              - label
              - service_type
              - address
              - timeout
              allOf:
              - type: object
                description: |
                  A service that Linode is monitoring as part of your Managed services. If issues are detected
                  with this service, a ManagedIssue will be opened and, optionally,
                  Linode special forces will attempt to resolve the Issue.
                x-akamai:
                  file-path: schemas/managed-service.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Service's unique ID.
                    example: 9944
                    readOnly: true
                    x-linode-cli-display: 1
                  address:
                    type: string
                    description: |
                      The URL at which this Service is monitored. URL parameters such as `?no-cache=1` are
                      preserved. URL fragments/anchors such as `#monitor` are __not__
                      preserved.
                    example: https://example.org
                    minLength: 3
                    maxLength: 100
                    format: url
                    x-linode-cli-display: 5
                  body:
                    type: string
                    description: |
                      What to expect to find in the response body for the Service to be considered up.
                    example: it worked
                    nullable: true
                    minLength: 0
                    maxLength: 100
                  consultation_group:
                    type: string
                    description: |
                      The group of ManagedContacts who should be notified or consulted with when an Issue is detected.
                    example: on-call
                    minLength: 0
                    maxLength: 50
                    x-linode-cli-display: 6
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Managed Service was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  credentials:
                    type: array
                    description: |
                      An array of ManagedCredential IDs that should be used when attempting to resolve issues with
                      this Service.
                    items:
                      type: integer
                      example: 9991
                  label:
                    type: string
                    description: |
                      The label for this Service. This is for display purposes only.
                    example: prod-1
                    minLength: 3
                    maxLength: 64
                    pattern: '[a-zA-Z0-9-_ \.]{3,64}'
                    x-linode-cli-display: 4
                  notes:
                    type: string
                    description: |
                      Any information relevant to the Service that Linode special forces should know when attempting
                      to resolve Issues.
                    example: The service name is my-cool-application
                    nullable: true
                  region:
                    type: string
                    description: |
                      The Region in which this Service is located. This is required if address is a private
                      IP, and may not be set otherwise.
                    example: null
                  service_type:
                    type: string
                    description: |
                      How this Service is monitored.
                    example: url
                    enum:
                    - url
                    - tcp
                    x-linode-cli-display: 3
                  status:
                    type: string
                    description: |
                      __Read-only__ The current status of this Service.
                    example: ok
                    readOnly: true
                    enum:
                    - disabled
                    - pending
                    - ok
                    - problem
                    x-linode-cli-color:
                      default_: yellow
                      disabled: red
                      ok: green
                    x-linode-cli-display: 2
                  timeout:
                    type: integer
                    description: |
                      How long to wait, in seconds, for a response before considering the Service to be
                      down.
                    example: 30
                    minimum: 1
                    maximum: 255
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Managed Service was last updated.
                    example: '2018-03-01T00:01:01'
                    readOnly: true
                    format: date-time
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Service created.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A service that Linode is monitoring as part of your Managed services. If issues are detected
                  with this service, a ManagedIssue will be opened and, optionally,
                  Linode special forces will attempt to resolve the Issue.
                x-akamai:
                  file-path: schemas/managed-service.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Service's unique ID.
                    example: 9944
                    readOnly: true
                    x-linode-cli-display: 1
                  address:
                    type: string
                    description: |
                      The URL at which this Service is monitored. URL parameters such as `?no-cache=1` are
                      preserved. URL fragments/anchors such as `#monitor` are __not__
                      preserved.
                    example: https://example.org
                    minLength: 3
                    maxLength: 100
                    format: url
                    x-linode-cli-display: 5
                  body:
                    type: string
                    description: |
                      What to expect to find in the response body for the Service to be considered up.
                    example: it worked
                    nullable: true
                    minLength: 0
                    maxLength: 100
                  consultation_group:
                    type: string
                    description: |
                      The group of ManagedContacts who should be notified or consulted with when an Issue is detected.
                    example: on-call
                    minLength: 0
                    maxLength: 50
                    x-linode-cli-display: 6
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Managed Service was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  credentials:
                    type: array
                    description: |
                      An array of ManagedCredential IDs that should be used when attempting to resolve issues with
                      this Service.
                    items:
                      type: integer
                      example: 9991
                  label:
                    type: string
                    description: |
                      The label for this Service. This is for display purposes only.
                    example: prod-1
                    minLength: 3
                    maxLength: 64
                    pattern: '[a-zA-Z0-9-_ \.]{3,64}'
                    x-linode-cli-display: 4
                  notes:
                    type: string
                    description: |
                      Any information relevant to the Service that Linode special forces should know when attempting
                      to resolve Issues.
                    example: The service name is my-cool-application
                    nullable: true
                  region:
                    type: string
                    description: |
                      The Region in which this Service is located. This is required if address is a private
                      IP, and may not be set otherwise.
                    example: null
                  service_type:
                    type: string
                    description: |
                      How this Service is monitored.
                    example: url
                    enum:
                    - url
                    - tcp
                    x-linode-cli-display: 3
                  status:
                    type: string
                    description: |
                      __Read-only__ The current status of this Service.
                    example: ok
                    readOnly: true
                    enum:
                    - disabled
                    - pending
                    - ok
                    - problem
                    x-linode-cli-color:
                      default_: yellow
                      disabled: red
                      ok: green
                    x-linode-cli-display: 2
                  timeout:
                    type: integer
                    description: |
                      How long to wait, in seconds, for a response before considering the Service to be
                      down.
                    example: 30
                    minimum: 1
                    maximum: 255
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Managed Service was last updated.
                    example: '2018-03-01T00:01:01'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-managed-service
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "service_type": "url",
                "label": "prod-1",
                "address": "https://example.org",
                "timeout": 30,
                "body": "it worked",
                "consultation_group": "on-call",
                "notes": "The service name is my-cool-application",
                "credentials": [
                  9991
                ]
              }' \
              https://api.linode.com/v4/managed/services
      - lang: CLI
        source: |-
          linode-cli managed service-create \
            --service_type url \
            --label prod-1 \
            --address "https://example.org" \
            --timeout 30 \
            --body "it worked" \
            --consultation_group on-call \
            --notes "The service name is \
              my-cool-application" \
            --credentials 9991
      x-linode-cli-action: service-create
      x-linode-grant: unrestricted only
      x-original-op-id: createManagedService
      x-original-op-title: Managed Service Create
    get:
      operationId: get-managed-services
      summary: List managed services
      tags:
      - Services
      description: |-
        Returns a paginated list of Managed Services on your Account. These are the services Linode Managed is monitoring and will report and attempt to resolve issues with.

        This operation can only be accessed by the unrestricted users of an account.


        ---


        - __CLI__.

            ```
            linode-cli managed services-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli managed services-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of Managed Services
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        A service that Linode is monitoring as part of your Managed services. If issues
                        are detected with this service, a ManagedIssue will be opened
                        and, optionally, Linode special forces will attempt to resolve
                        the Issue.
                      x-akamai:
                        file-path: schemas/managed-service.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ This Service's unique ID.
                          example: 9944
                          readOnly: true
                          x-linode-cli-display: 1
                        address:
                          type: string
                          description: |
                            The URL at which this Service is monitored. URL parameters such as `?no-cache=1`
                            are preserved. URL fragments/anchors such as `#monitor`
                            are __not__ preserved.
                          example: https://example.org
                          minLength: 3
                          maxLength: 100
                          format: url
                          x-linode-cli-display: 5
                        body:
                          type: string
                          description: |
                            What to expect to find in the response body for the Service to be considered
                            up.
                          example: it worked
                          nullable: true
                          minLength: 0
                          maxLength: 100
                        consultation_group:
                          type: string
                          description: |
                            The group of ManagedContacts who should be notified or consulted with
                            when an Issue is detected.
                          example: on-call
                          minLength: 0
                          maxLength: 50
                          x-linode-cli-display: 6
                        created:
                          type: string
                          description: |
                            __Read-only__ When this Managed Service was created.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                        credentials:
                          type: array
                          description: |
                            An array of ManagedCredential IDs that should be used when attempting to resolve issues
                            with this Service.
                          items:
                            type: integer
                            example: 9991
                        label:
                          type: string
                          description: |
                            The label for this Service. This is for display purposes only.
                          example: prod-1
                          minLength: 3
                          maxLength: 64
                          pattern: '[a-zA-Z0-9-_ \.]{3,64}'
                          x-linode-cli-display: 4
                        notes:
                          type: string
                          description: |
                            Any information relevant to the Service that Linode special forces should
                            know when attempting to resolve Issues.
                          example: The service name is my-cool-application
                          nullable: true
                        region:
                          type: string
                          description: |
                            The Region in which this Service is located. This is required if address
                            is a private IP, and may not be set otherwise.
                          example: null
                        service_type:
                          type: string
                          description: |
                            How this Service is monitored.
                          example: url
                          enum:
                          - url
                          - tcp
                          x-linode-cli-display: 3
                        status:
                          type: string
                          description: |
                            __Read-only__ The current status of this Service.
                          example: ok
                          readOnly: true
                          enum:
                          - disabled
                          - pending
                          - ok
                          - problem
                          x-linode-cli-color:
                            default_: yellow
                            disabled: red
                            ok: green
                          x-linode-cli-display: 2
                        timeout:
                          type: integer
                          description: |
                            How long to wait, in seconds, for a response before considering the Service
                            to be down.
                          example: 30
                          minimum: 1
                          maximum: 255
                        updated:
                          type: string
                          description: |
                            __Read-only__ When this Managed Service was last updated.
                          example: '2018-03-01T00:01:01'
                          readOnly: true
                          format: date-time
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-managed-services
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/managed/services
      - lang: CLI
        source: linode-cli managed services-list
      x-linode-cli-action: services-list
      x-linode-grant: unrestricted only
      x-original-op-id: getManagedServices
      x-original-op-title: Managed Services List
    x-linode-cli-command: managed
  /managed/services/{serviceId}:
    x-akamai:
      file-path: paths/service.yaml
      path-info: /managed/services/{serviceId}
    parameters:
    - name: serviceId
      in: path
      description: |
        The ID of the Managed Service to access.
      example: '{{serviceId}}'
      x-akamai:
        file-path: parameters/service-id-path-d31c71b2.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-managed-service
      summary: Get a managed service
      tags:
      - Services
      description: |-
        Returns information about a single Managed Service on your Account.

        This operation can only be accessed by the unrestricted users of an account.


        ---


        - __CLI__.

            ```
            linode-cli managed service-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli managed service-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The requested Managed Service.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A service that Linode is monitoring as part of your Managed services. If issues are detected
                  with this service, a ManagedIssue will be opened and, optionally,
                  Linode special forces will attempt to resolve the Issue.
                x-akamai:
                  file-path: schemas/managed-service.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Service's unique ID.
                    example: 9944
                    readOnly: true
                    x-linode-cli-display: 1
                  address:
                    type: string
                    description: |
                      The URL at which this Service is monitored. URL parameters such as `?no-cache=1` are
                      preserved. URL fragments/anchors such as `#monitor` are __not__
                      preserved.
                    example: https://example.org
                    minLength: 3
                    maxLength: 100
                    format: url
                    x-linode-cli-display: 5
                  body:
                    type: string
                    description: |
                      What to expect to find in the response body for the Service to be considered up.
                    example: it worked
                    nullable: true
                    minLength: 0
                    maxLength: 100
                  consultation_group:
                    type: string
                    description: |
                      The group of ManagedContacts who should be notified or consulted with when an Issue is detected.
                    example: on-call
                    minLength: 0
                    maxLength: 50
                    x-linode-cli-display: 6
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Managed Service was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  credentials:
                    type: array
                    description: |
                      An array of ManagedCredential IDs that should be used when attempting to resolve issues with
                      this Service.
                    items:
                      type: integer
                      example: 9991
                  label:
                    type: string
                    description: |
                      The label for this Service. This is for display purposes only.
                    example: prod-1
                    minLength: 3
                    maxLength: 64
                    pattern: '[a-zA-Z0-9-_ \.]{3,64}'
                    x-linode-cli-display: 4
                  notes:
                    type: string
                    description: |
                      Any information relevant to the Service that Linode special forces should know when attempting
                      to resolve Issues.
                    example: The service name is my-cool-application
                    nullable: true
                  region:
                    type: string
                    description: |
                      The Region in which this Service is located. This is required if address is a private
                      IP, and may not be set otherwise.
                    example: null
                  service_type:
                    type: string
                    description: |
                      How this Service is monitored.
                    example: url
                    enum:
                    - url
                    - tcp
                    x-linode-cli-display: 3
                  status:
                    type: string
                    description: |
                      __Read-only__ The current status of this Service.
                    example: ok
                    readOnly: true
                    enum:
                    - disabled
                    - pending
                    - ok
                    - problem
                    x-linode-cli-color:
                      default_: yellow
                      disabled: red
                      ok: green
                    x-linode-cli-display: 2
                  timeout:
                    type: integer
                    description: |
                      How long to wait, in seconds, for a response before considering the Service to be
                      down.
                    example: 30
                    minimum: 1
                    maximum: 255
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Managed Service was last updated.
                    example: '2018-03-01T00:01:01'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-managed-service
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/managed/services/9994
      - lang: CLI
        source: linode-cli managed service-view 9994
      x-linode-cli-action: service-view
      x-linode-grant: unrestricted only
      x-original-op-id: getManagedService
      x-original-op-title: Managed Service View
    put:
      operationId: put-managed-service
      summary: Update a managed service
      tags:
      - Services
      description: |-
        Updates information about a Managed Service.

        This operation can only be accessed by the unrestricted users of an account.


        ---


        - __CLI__.

            ```
            linode-cli managed service-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli managed service-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The fields to update.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                A service that Linode is monitoring as part of your Managed services. If issues are detected
                with this service, a ManagedIssue will be opened and, optionally,
                Linode special forces will attempt to resolve the Issue.
              x-akamai:
                file-path: schemas/managed-service.yaml
              additionalProperties: false
              properties:
                id:
                  type: integer
                  description: |
                    __Read-only__ This Service's unique ID.
                  example: '{{id}}'
                  readOnly: true
                  x-linode-cli-display: 1
                address:
                  type: string
                  description: |
                    The URL at which this Service is monitored. URL parameters such as `?no-cache=1` are
                    preserved. URL fragments/anchors such as `#monitor` are __not__
                    preserved.
                  example: '{{address}}'
                  minLength: 3
                  maxLength: 100
                  format: url
                  x-linode-cli-display: 5
                body:
                  type: string
                  description: |
                    What to expect to find in the response body for the Service to be considered up.
                  example: '{{body}}'
                  nullable: true
                  minLength: 0
                  maxLength: 100
                consultation_group:
                  type: string
                  description: |
                    The group of ManagedContacts who should be notified or consulted with when an Issue is detected.
                  example: '{{consultation_group}}'
                  minLength: 0
                  maxLength: 50
                  x-linode-cli-display: 6
                created:
                  type: string
                  description: |
                    __Read-only__ When this Managed Service was created.
                  example: '{{created}}'
                  readOnly: true
                  format: date-time
                credentials:
                  type: array
                  description: |
                    An array of ManagedCredential IDs that should be used when attempting to resolve issues with
                    this Service.
                  items:
                    type: integer
                    example: 9991
                label:
                  type: string
                  description: |
                    The label for this Service. This is for display purposes only.
                  example: '{{label}}'
                  minLength: 3
                  maxLength: 64
                  pattern: '[a-zA-Z0-9-_ \.]{3,64}'
                  x-linode-cli-display: 4
                notes:
                  type: string
                  description: |
                    Any information relevant to the Service that Linode special forces should know when attempting
                    to resolve Issues.
                  example: '{{notes}}'
                  nullable: true
                region:
                  type: string
                  description: |
                    The Region in which this Service is located. This is required if address is a private IP,
                    and may not be set otherwise.
                  example: '{{region}}'
                service_type:
                  type: string
                  description: |
                    How this Service is monitored.
                  example: '{{service_type}}'
                  enum:
                  - url
                  - tcp
                  x-linode-cli-display: 3
                status:
                  type: string
                  description: |
                    __Read-only__ The current status of this Service.
                  example: '{{status}}'
                  readOnly: true
                  enum:
                  - disabled
                  - pending
                  - ok
                  - problem
                  x-linode-cli-color:
                    default_: yellow
                    disabled: red
                    ok: green
                  x-linode-cli-display: 2
                timeout:
                  type: integer
                  description: |
                    How long to wait, in seconds, for a response before considering the Service to be down.
                  example: '{{timeout}}'
                  minimum: 1
                  maximum: 255
                updated:
                  type: string
                  description: |
                    __Read-only__ When this Managed Service was last updated.
                  example: '{{updated}}'
                  readOnly: true
                  format: date-time
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Service updated successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A service that Linode is monitoring as part of your Managed services. If issues are detected
                  with this service, a ManagedIssue will be opened and, optionally,
                  Linode special forces will attempt to resolve the Issue.
                x-akamai:
                  file-path: schemas/managed-service.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Service's unique ID.
                    example: 9944
                    readOnly: true
                    x-linode-cli-display: 1
                  address:
                    type: string
                    description: |
                      The URL at which this Service is monitored. URL parameters such as `?no-cache=1` are
                      preserved. URL fragments/anchors such as `#monitor` are __not__
                      preserved.
                    example: https://example.org
                    minLength: 3
                    maxLength: 100
                    format: url
                    x-linode-cli-display: 5
                  body:
                    type: string
                    description: |
                      What to expect to find in the response body for the Service to be considered up.
                    example: it worked
                    nullable: true
                    minLength: 0
                    maxLength: 100
                  consultation_group:
                    type: string
                    description: |
                      The group of ManagedContacts who should be notified or consulted with when an Issue is detected.
                    example: on-call
                    minLength: 0
                    maxLength: 50
                    x-linode-cli-display: 6
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Managed Service was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  credentials:
                    type: array
                    description: |
                      An array of ManagedCredential IDs that should be used when attempting to resolve issues with
                      this Service.
                    items:
                      type: integer
                      example: 9991
                  label:
                    type: string
                    description: |
                      The label for this Service. This is for display purposes only.
                    example: prod-1
                    minLength: 3
                    maxLength: 64
                    pattern: '[a-zA-Z0-9-_ \.]{3,64}'
                    x-linode-cli-display: 4
                  notes:
                    type: string
                    description: |
                      Any information relevant to the Service that Linode special forces should know when attempting
                      to resolve Issues.
                    example: The service name is my-cool-application
                    nullable: true
                  region:
                    type: string
                    description: |
                      The Region in which this Service is located. This is required if address is a private
                      IP, and may not be set otherwise.
                    example: null
                  service_type:
                    type: string
                    description: |
                      How this Service is monitored.
                    example: url
                    enum:
                    - url
                    - tcp
                    x-linode-cli-display: 3
                  status:
                    type: string
                    description: |
                      __Read-only__ The current status of this Service.
                    example: ok
                    readOnly: true
                    enum:
                    - disabled
                    - pending
                    - ok
                    - problem
                    x-linode-cli-color:
                      default_: yellow
                      disabled: red
                      ok: green
                    x-linode-cli-display: 2
                  timeout:
                    type: integer
                    description: |
                      How long to wait, in seconds, for a response before considering the Service to be
                      down.
                    example: 30
                    minimum: 1
                    maximum: 255
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Managed Service was last updated.
                    example: '2018-03-01T00:01:01'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-managed-service
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "service_type": "url",
                "label": "prod-1",
                "address": "https://example.org",
                "timeout": 30,
                "body": "it worked",
                "consultation_group": "on-call",
                "notes": "The service name is my-cool-application",
                "credentials": [
                  9991
                ]
              }' \
              https://api.linode.com/v4/managed/services/9994
      - lang: CLI
        source: |-
          linode-cli managed service-update 9994 \
            --service_type url \
            --label prod-1 \
            --address "https://example.org" \
            --timeout 30 \
            --body "it worked" \
            --consultation_group on-call \
            --notes "The service name is my-cool-application" \
            --credentials 9991
      x-linode-cli-action: service-update
      x-linode-grant: unrestricted only
      x-original-op-id: updateManagedService
      x-original-op-title: Managed Service Update
    delete:
      operationId: delete-managed-service
      summary: Delete a managed service
      tags:
      - Services
      description: |-
        Deletes a Managed Service.  This service will no longer be monitored by Linode Managed.

        This operation can only be accessed by the unrestricted users of an account.


        ---


        - __CLI__.

            ```
            linode-cli managed service-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli managed service-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Service deleted successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-managed-service
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/managed/services/9994
      - lang: CLI
        source: linode-cli managed service-delete 9994
      x-linode-cli-action: service-delete
      x-linode-grant: unrestricted only
      x-original-op-id: deleteManagedService
      x-original-op-title: Managed Service Delete
    x-linode-cli-command: managed
  /managed/services/{serviceId}/disable:
    x-akamai:
      file-path: paths/disable.yaml
      path-info: /managed/services/{serviceId}/disable
    parameters:
    - name: serviceId
      in: path
      description: |
        The ID of the Managed Service to disable.
      example: '{{serviceId}}'
      x-akamai:
        file-path: parameters/service-id-path-67e33f47.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-disable-managed-service
      summary: Disable a managed service
      tags:
      - Services
      description: |-
        Temporarily disables monitoring of a Managed Service.

        This operation can only be accessed by the unrestricted users of an account.


        ---


        - __CLI__.

            ```
            linode-cli managed service-disable
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli managed service-disable
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Service disabled.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A service that Linode is monitoring as part of your Managed services. If issues are detected
                  with this service, a ManagedIssue will be opened and, optionally,
                  Linode special forces will attempt to resolve the Issue.
                x-akamai:
                  file-path: schemas/managed-service.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Service's unique ID.
                    example: 9944
                    readOnly: true
                    x-linode-cli-display: 1
                  address:
                    type: string
                    description: |
                      The URL at which this Service is monitored. URL parameters such as `?no-cache=1` are
                      preserved. URL fragments/anchors such as `#monitor` are __not__
                      preserved.
                    example: https://example.org
                    minLength: 3
                    maxLength: 100
                    format: url
                    x-linode-cli-display: 5
                  body:
                    type: string
                    description: |
                      What to expect to find in the response body for the Service to be considered up.
                    example: it worked
                    nullable: true
                    minLength: 0
                    maxLength: 100
                  consultation_group:
                    type: string
                    description: |
                      The group of ManagedContacts who should be notified or consulted with when an Issue is detected.
                    example: on-call
                    minLength: 0
                    maxLength: 50
                    x-linode-cli-display: 6
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Managed Service was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  credentials:
                    type: array
                    description: |
                      An array of ManagedCredential IDs that should be used when attempting to resolve issues with
                      this Service.
                    items:
                      type: integer
                      example: 9991
                  label:
                    type: string
                    description: |
                      The label for this Service. This is for display purposes only.
                    example: prod-1
                    minLength: 3
                    maxLength: 64
                    pattern: '[a-zA-Z0-9-_ \.]{3,64}'
                    x-linode-cli-display: 4
                  notes:
                    type: string
                    description: |
                      Any information relevant to the Service that Linode special forces should know when attempting
                      to resolve Issues.
                    example: The service name is my-cool-application
                    nullable: true
                  region:
                    type: string
                    description: |
                      The Region in which this Service is located. This is required if address is a private
                      IP, and may not be set otherwise.
                    example: null
                  service_type:
                    type: string
                    description: |
                      How this Service is monitored.
                    example: url
                    enum:
                    - url
                    - tcp
                    x-linode-cli-display: 3
                  status:
                    type: string
                    description: |
                      __Read-only__ The current status of this Service.
                    example: ok
                    readOnly: true
                    enum:
                    - disabled
                    - pending
                    - ok
                    - problem
                    x-linode-cli-color:
                      default_: yellow
                      disabled: red
                      ok: green
                    x-linode-cli-display: 2
                  timeout:
                    type: integer
                    description: |
                      How long to wait, in seconds, for a response before considering the Service to be
                      down.
                    example: 30
                    minimum: 1
                    maximum: 255
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Managed Service was last updated.
                    example: '2018-03-01T00:01:01'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-disable-managed-service
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST \
              https://api.linode.com/v4/managed/services/9994/disable
      - lang: CLI
        source: linode-cli managed service-disable 9994
      x-linode-cli-action: service-disable
      x-linode-grant: unrestricted only
      x-original-op-id: disableManagedService
      x-original-op-title: Managed Service Disable
    x-linode-cli-command: managed
  /managed/services/{serviceId}/enable:
    x-akamai:
      file-path: paths/service-enable.yaml
      path-info: /managed/services/{serviceId}/enable
    parameters:
    - name: serviceId
      in: path
      description: |
        The ID of the Managed Service to enable.
      example: '{{serviceId}}'
      x-akamai:
        file-path: parameters/service-id-path-ea3ecd21.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-enable-managed-service
      summary: Enable a managed service
      tags:
      - Services
      description: |-
        Enables monitoring of a Managed Service.

        This operation can only be accessed by the unrestricted users of an account.


        ---


        - __CLI__.

            ```
            linode-cli managed service-enable
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli managed service-enable
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Service enabled.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A service that Linode is monitoring as part of your Managed services. If issues are detected
                  with this service, a ManagedIssue will be opened and, optionally,
                  Linode special forces will attempt to resolve the Issue.
                x-akamai:
                  file-path: schemas/managed-service.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This Service's unique ID.
                    example: 9944
                    readOnly: true
                    x-linode-cli-display: 1
                  address:
                    type: string
                    description: |
                      The URL at which this Service is monitored. URL parameters such as `?no-cache=1` are
                      preserved. URL fragments/anchors such as `#monitor` are __not__
                      preserved.
                    example: https://example.org
                    minLength: 3
                    maxLength: 100
                    format: url
                    x-linode-cli-display: 5
                  body:
                    type: string
                    description: |
                      What to expect to find in the response body for the Service to be considered up.
                    example: it worked
                    nullable: true
                    minLength: 0
                    maxLength: 100
                  consultation_group:
                    type: string
                    description: |
                      The group of ManagedContacts who should be notified or consulted with when an Issue is detected.
                    example: on-call
                    minLength: 0
                    maxLength: 50
                    x-linode-cli-display: 6
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Managed Service was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  credentials:
                    type: array
                    description: |
                      An array of ManagedCredential IDs that should be used when attempting to resolve issues with
                      this Service.
                    items:
                      type: integer
                      example: 9991
                  label:
                    type: string
                    description: |
                      The label for this Service. This is for display purposes only.
                    example: prod-1
                    minLength: 3
                    maxLength: 64
                    pattern: '[a-zA-Z0-9-_ \.]{3,64}'
                    x-linode-cli-display: 4
                  notes:
                    type: string
                    description: |
                      Any information relevant to the Service that Linode special forces should know when attempting
                      to resolve Issues.
                    example: The service name is my-cool-application
                    nullable: true
                  region:
                    type: string
                    description: |
                      The Region in which this Service is located. This is required if address is a private
                      IP, and may not be set otherwise.
                    example: null
                  service_type:
                    type: string
                    description: |
                      How this Service is monitored.
                    example: url
                    enum:
                    - url
                    - tcp
                    x-linode-cli-display: 3
                  status:
                    type: string
                    description: |
                      __Read-only__ The current status of this Service.
                    example: ok
                    readOnly: true
                    enum:
                    - disabled
                    - pending
                    - ok
                    - problem
                    x-linode-cli-color:
                      default_: yellow
                      disabled: red
                      ok: green
                    x-linode-cli-display: 2
                  timeout:
                    type: integer
                    description: |
                      How long to wait, in seconds, for a response before considering the Service to be
                      down.
                    example: 30
                    minimum: 1
                    maximum: 255
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Managed Service was last updated.
                    example: '2018-03-01T00:01:01'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-enable-managed-service
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST \
              https://api.linode.com/v4/managed/services/9994/enable
      - lang: CLI
        source: linode-cli managed service-enable 9994
      x-linode-cli-action: service-enable
      x-linode-grant: unrestricted only
      x-original-op-id: enableManagedService
      x-original-op-title: Managed Service Enable
    x-linode-cli-command: managed
  /managed/stats:
    x-akamai:
      file-path: paths/managed-stats.yaml
      path-info: /managed/stats
    get:
      operationId: get-managed-stats
      summary: List managed stats
      tags:
      - Statistics
      description: |-
        Returns a list of Managed Stats on your Account in the form of x and y data points. You can use these data points to plot your own graph visualizations. These stats reflect the last 24 hours of combined usage across all managed Linodes on your account giving you a high-level snapshot of data for the following:

        - cpu
        - disk
        - swap
        - network in
        - network out

        This operation can only be accessed by the unrestricted users of an account.


        ---


        - __CLI__.

            ```
            linode-cli managed stats-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            account:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli managed stats-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: account:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A list of Managed Stats from the last 24 hours.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: object
                    discriminator:
                      propertyName: x-linode-ref-name
                    oneOf:
                    - type: object
                      description: |
                        A collection of graph data returned for managed stats.
                      x-akamai:
                        file-path: schemas/stats-data-available.yaml
                      title: Stats available
                      additionalProperties: false
                      properties:
                        cpu:
                          type: array
                          description: |
                            CPU usage stats from the last 24 hours.
                          items:
                            type: object
                            description: |
                              A stat data point.
                            x-akamai:
                              file-path: schemas/stats-data.yaml
                            additionalProperties: false
                            properties:
                              x:
                                type: integer
                                description: |
                                  __Read-only__ A stats graph data point.
                                example: 11513761600000
                                readOnly: true
                              y:
                                type: integer
                                description: |
                                  __Read-only__ A stats graph data point.
                                example: 29.94
                                readOnly: true
                        disk:
                          type: array
                          description: |
                            Disk usage stats from the last 24 hours.
                          items:
                            type: object
                            description: |
                              A stat data point.
                            x-akamai:
                              file-path: schemas/stats-data.yaml
                            additionalProperties: false
                            properties:
                              x:
                                type: integer
                                description: |
                                  __Read-only__ A stats graph data point.
                                example: 11513761600000
                                readOnly: true
                              y:
                                type: integer
                                description: |
                                  __Read-only__ A stats graph data point.
                                example: 29.94
                                readOnly: true
                        net_in:
                          type: array
                          description: |
                            Inbound network traffic stats from the last 24 hours.
                          items:
                            type: object
                            description: |
                              A stat data point.
                            x-akamai:
                              file-path: schemas/stats-data.yaml
                            additionalProperties: false
                            properties:
                              x:
                                type: integer
                                description: |
                                  __Read-only__ A stats graph data point.
                                example: 11513761600000
                                readOnly: true
                              y:
                                type: integer
                                description: |
                                  __Read-only__ A stats graph data point.
                                example: 29.94
                                readOnly: true
                        net_out:
                          type: array
                          description: |
                            Outbound network traffic stats from the last 24 hours.
                          items:
                            type: object
                            description: |
                              A stat data point.
                            x-akamai:
                              file-path: schemas/stats-data.yaml
                            additionalProperties: false
                            properties:
                              x:
                                type: integer
                                description: |
                                  __Read-only__ A stats graph data point.
                                example: 11513761600000
                                readOnly: true
                              y:
                                type: integer
                                description: |
                                  __Read-only__ A stats graph data point.
                                example: 29.94
                                readOnly: true
                        swap:
                          type: array
                          description: |
                            Swap usage stats from the last 24 hours.
                          items:
                            type: object
                            description: |
                              A stat data point.
                            x-akamai:
                              file-path: schemas/stats-data.yaml
                            additionalProperties: false
                            properties:
                              x:
                                type: integer
                                description: |
                                  __Read-only__ A stats graph data point.
                                example: 11513761600000
                                readOnly: true
                              y:
                                type: integer
                                description: |
                                  __Read-only__ A stats graph data point.
                                example: 29.94
                                readOnly: true
                      x-linode-ref-name: Stats Available
                    - type: array
                      description: |
                        __Read-only__ An array of error messages if managed stats are unavailable.
                      x-akamai:
                        file-path: schemas/stats-data-unavailable.yaml
                      title: Stats unavailable
                      readOnly: true
                      items:
                        type: string
                        example: Graphs are not yet available.
                      x-linode-ref-name: Stats Unavailable
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-managed-stats
      security:
      - personalAccessToken: []
      - oauth:
        - account:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/managed/stats
      - lang: CLI
        source: linode-cli managed stats-list
      x-linode-cli-action: stats-list
      x-linode-grant: unrestricted only
      x-original-op-id: getManagedStats
      x-original-op-title: Managed Stats List
    x-linode-cli-command: managed
  /networking/firewalls:
    x-akamai:
      file-path: paths/networking-firewalls.yaml
      path-info: /networking/firewalls
    post:
      servers:
      - url: https://api.linode.com/v4
      operationId: post-firewalls
      summary: Create a firewall
      tags:
      - Firewalls
      description: |-
        Creates a Firewall to filter network traffic.

        - Use the `rules` property to create inbound and outbound access rules.

        - Use the `devices` property to assign the Firewall to a service and apply its Rules to the device. Requires `read_write` [user grant](https://techdocs.akamai.com/linode-api/reference/get-user-grants) to the device. Currently, Firewalls can be assigned to Linode compute instances and NodeBalancers.

        - A Firewall can be assigned to multiple services at a time.

        - A Firewall can be assigned during Linode creation by utilizing the `firewall_id` [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance) property.

        - A service can have one active, assigned Firewall at a time.

        Additional disabled Firewalls can be assigned to a service, but they cannot be enabled if another active Firewall is already assigned to the same service.

        - Firewalls apply to all of a Linode's non-`vlan` purpose Configuration Profile Interfaces.

        - Assigned Linodes must not have any ongoing live migrations.

        - A `firewall_create` Event is generated when this operation succeeds.


        ---


        - __CLI__.

            ```
            linode-cli firewalls create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            firewall:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli firewalls create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: firewall:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Creates a Firewall object that can be applied to a service to filter the service's network traffic.
        content:
          application/json:
            schema:
              required:
              - label
              - rules
              allOf:
              - type: object
                description: |
                  A resource that controls incoming and outgoing network traffic to a compute service. Only
                  one enabled Firewall can be attached to a particular service at
                  any given time. [Create a firewall device](https://techdocs.akamai.com/linode-api/reference/post-firewall-device)
                  to assign a Firewall to a service. Currently, Firewalls can assigned
                  to Linode compute instances and NodeBalancers.
                x-akamai:
                  file-path: schemas/firewall.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The Firewall's unique ID.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  tags:
                    type: array
                    description: |
                      An array of tags applied to this object. Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Firewall was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  label:
                    type: string
                    description: |-
                      The Firewall's label, for display purposes only.

                      Firewall labels have the following constraints:

                        - Must begin and end with an alphanumeric character.
                        - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                        - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
                        - Must be between 3 and 32 characters.
                        - Must be unique.
                    example: firewall123
                    minLength: 3
                    maxLength: 32
                    pattern: ^[a-zA-Z]((?!--|__|..)[a-zA-Z0-9-_.])+$
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  rules:
                    type: object
                    description: |-
                      The inbound and outbound access rules to apply to the Firewall.

                      A Firewall may have up to 25 rules across its inbound and outbound rulesets.

                      Multiple rules are applied in order. If two rules conflict, the first rule takes precedence. For example, if the first rule accepts inbound traffic from an address, and the second rule drops inbound traffic the same address, the first rule applies and inbound traffic from that address is accepted.
                    additionalProperties: false
                    properties:
                      inbound:
                        type: array
                        description: |
                          The inbound rules for the firewall, as a JSON array.
                        items:
                          type: object
                          description: |
                            One of a Firewall's inbound or outbound access rules. The `ports` property
                            can be used to allow traffic on a comma-separated list
                            of different ports.
                          x-akamai:
                            file-path: schemas/firewall-rule-config.yaml
                          additionalProperties: false
                          properties:
                            description:
                              type: string
                              description: |
                                Used to describe this rule. For display purposes only.
                              example: An example firewall rule description.
                              minLength: 1
                              maxLength: 100
                            action:
                              type: string
                              description: |
                                Controls whether traffic is accepted or dropped by this rule.
                                Overrides the Firewall's `inbound_policy` if this
                                is an inbound rule, or the `outbound_policy` if this
                                is an outbound rule.
                              example: ACCEPT
                              enum:
                              - ACCEPT
                              - DROP
                            addresses:
                              type: object
                              description: |-
                                The IPv4 and/or IPv6 addresses affected by this rule. A Rule can have up to 255 total addresses or networks listed across its IPv4 and IPv6 arrays. A network and a single IP are treated as equivalent when accounting for this limit.

                                Must contain `ipv4`, `ipv6`, or both.
                              additionalProperties: false
                              properties:
                                ipv4:
                                  type: array
                                  description: |-
                                    A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.

                                    If "0.0.0.0/0" is included in this list, all IPv4 addresses are affected by this rule.
                                  example:
                                  - 192.0.2.0/24
                                  - 198.51.100.2/32
                                  items:
                                    type: string
                                ipv6:
                                  type: array
                                  description: |-
                                    A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.

                                    If "::/0" is included in this list, all IPv6 addresses are affected by this rule.
                                  example:
                                  - 2001:DB8::/128
                                  items:
                                    type: string
                            label:
                              type: string
                              description: |
                                Used to identify this rule. For display purposes only.
                              example: firewallrule123
                              minLength: 3
                              maxLength: 32
                            ports:
                              type: string
                              description: |-
                                A string representing the port or ports affected by this rule:

                                - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                                - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                                - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port "080" is not allowed.
                                - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                                - If no ports are configured, all ports are affected.
                                - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                              example: 22-24, 80, 443
                              nullable: true
                            protocol:
                              type: string
                              description: |
                                The type of network traffic affected by this rule.
                              example: TCP
                              enum:
                              - TCP
                              - UDP
                              - ICMP
                              - IPENCAP
                        x-linode-cli-format: json
                      inbound_policy:
                        type: string
                        description: |
                          The default behavior for inbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules)
                          the `inbound.action` property of the Firewall Rule.
                        example: DROP
                        enum:
                        - ACCEPT
                        - DROP
                      outbound:
                        type: array
                        description: |
                          The outbound rules for the firewall, as a JSON array.
                        items:
                          type: object
                          description: |
                            One of a Firewall's inbound or outbound access rules. The `ports` property
                            can be used to allow traffic on a comma-separated list
                            of different ports.
                          x-akamai:
                            file-path: schemas/firewall-rule-config.yaml
                          additionalProperties: false
                          properties:
                            description:
                              type: string
                              description: |
                                Used to describe this rule. For display purposes only.
                              example: An example firewall rule description.
                              minLength: 1
                              maxLength: 100
                            action:
                              type: string
                              description: |
                                Controls whether traffic is accepted or dropped by this rule.
                                Overrides the Firewall's `inbound_policy` if this
                                is an inbound rule, or the `outbound_policy` if this
                                is an outbound rule.
                              example: ACCEPT
                              enum:
                              - ACCEPT
                              - DROP
                            addresses:
                              type: object
                              description: |-
                                The IPv4 and/or IPv6 addresses affected by this rule. A Rule can have up to 255 total addresses or networks listed across its IPv4 and IPv6 arrays. A network and a single IP are treated as equivalent when accounting for this limit.

                                Must contain `ipv4`, `ipv6`, or both.
                              additionalProperties: false
                              properties:
                                ipv4:
                                  type: array
                                  description: |-
                                    A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.

                                    If "0.0.0.0/0" is included in this list, all IPv4 addresses are affected by this rule.
                                  example:
                                  - 192.0.2.0/24
                                  - 198.51.100.2/32
                                  items:
                                    type: string
                                ipv6:
                                  type: array
                                  description: |-
                                    A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.

                                    If "::/0" is included in this list, all IPv6 addresses are affected by this rule.
                                  example:
                                  - 2001:DB8::/128
                                  items:
                                    type: string
                            label:
                              type: string
                              description: |
                                Used to identify this rule. For display purposes only.
                              example: firewallrule123
                              minLength: 3
                              maxLength: 32
                            ports:
                              type: string
                              description: |-
                                A string representing the port or ports affected by this rule:

                                - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                                - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                                - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port "080" is not allowed.
                                - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                                - If no ports are configured, all ports are affected.
                                - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                              example: 22-24, 80, 443
                              nullable: true
                            protocol:
                              type: string
                              description: |
                                The type of network traffic affected by this rule.
                              example: TCP
                              enum:
                              - TCP
                              - UDP
                              - ICMP
                              - IPENCAP
                        x-linode-cli-format: json
                      outbound_policy:
                        type: string
                        description: |
                          The default behavior for outbound traffic. This setting can be overridden by
                          [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules)
                          the `outbound.action` property of the Firewall Rule.
                        example: DROP
                        enum:
                        - ACCEPT
                        - DROP
                  status:
                    type: string
                    description: |-
                      __Read-only__ The status of this Firewall.

                        - When a Firewall is first created its status is `enabled`.
                        - RUn the [Update a firewall](https://techdocs.akamai.com/linode-api/reference/put-firewall) operation to set a Firewall's status to `enabled` or `disabled`.
                        - RUn the [Delete a firewall](https://techdocs.akamai.com/linode-api/reference/delete-firewall) operation to delete a Firewall.
                    example: enabled
                    readOnly: true
                    enum:
                    - enabled
                    - disabled
                    - deleted
                    x-linode-cli-display: 3
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Firewall was last updated.
                    example: '2018-01-02T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 5
                    x-linode-filterable: true
              properties:
                devices:
                  type: object
                  description: |-
                    Devices to create for this Firewall. When a Device is created, the Firewall is assigned to its associated service. Currently, Devices can be created for Linode compute instances and NodeBalancers.

                    Additional devices can be assigned after Firewall creation by using the [Create a firewall device](https://techdocs.akamai.com/linode-api/reference/post-firewall-device) operation.
                  additionalProperties: false
                  properties:
                    linodes:
                      type: array
                      description: |
                        An array of Linode IDs. A Firewall Device is created for each ID.
                      example:
                      - 123
                      - 456
                      items:
                        type: integer
                    nodebalancers:
                      type: array
                      description: |-
                        An array containing a NodeBalancer ID. A Firewall Device is created for the ID.

                        - Only one NodeBalancer can be assigned to a Firewall at a time.
                        - Firewalls only apply to inbound TCP traffic to NodeBalancers.
                      example:
                      - 321
                      items:
                        type: integer
                rules:
                  additionalProperties: false
                  required:
                  - inbound_policy
                  - outbound_policy
                  properties:
                    inbound:
                      required:
                      - action
                      - addresses
                      - protocol
                    outbound:
                      required:
                      - action
                      - addresses
                      - protocol
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns information about the created Firewall.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A resource that controls incoming and outgoing network traffic to a compute service. Only
                  one enabled Firewall can be attached to a particular service at
                  any given time. [Create a firewall device](https://techdocs.akamai.com/linode-api/reference/post-firewall-device)
                  to assign a Firewall to a service. Currently, Firewalls can assigned
                  to Linode compute instances and NodeBalancers.
                x-akamai:
                  file-path: schemas/firewall.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The Firewall's unique ID.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  tags:
                    type: array
                    description: |
                      An array of tags applied to this object. Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Firewall was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  label:
                    type: string
                    description: |-
                      The Firewall's label, for display purposes only.

                      Firewall labels have the following constraints:

                        - Must begin and end with an alphanumeric character.
                        - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                        - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
                        - Must be between 3 and 32 characters.
                        - Must be unique.
                    example: firewall123
                    minLength: 3
                    maxLength: 32
                    pattern: ^[a-zA-Z]((?!--|__|..)[a-zA-Z0-9-_.])+$
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  rules:
                    type: object
                    description: |-
                      The inbound and outbound access rules to apply to the Firewall.

                      A Firewall may have up to 25 rules across its inbound and outbound rulesets.

                      Multiple rules are applied in order. If two rules conflict, the first rule takes precedence. For example, if the first rule accepts inbound traffic from an address, and the second rule drops inbound traffic the same address, the first rule applies and inbound traffic from that address is accepted.
                    additionalProperties: false
                    properties:
                      inbound:
                        type: array
                        description: |
                          The inbound rules for the firewall, as a JSON array.
                        items:
                          type: object
                          description: |
                            One of a Firewall's inbound or outbound access rules. The `ports` property
                            can be used to allow traffic on a comma-separated list
                            of different ports.
                          x-akamai:
                            file-path: schemas/firewall-rule-config.yaml
                          additionalProperties: false
                          properties:
                            description:
                              type: string
                              description: |
                                Used to describe this rule. For display purposes only.
                              example: An example firewall rule description.
                              minLength: 1
                              maxLength: 100
                            action:
                              type: string
                              description: |
                                Controls whether traffic is accepted or dropped by this rule.
                                Overrides the Firewall's `inbound_policy` if this
                                is an inbound rule, or the `outbound_policy` if this
                                is an outbound rule.
                              example: ACCEPT
                              enum:
                              - ACCEPT
                              - DROP
                            addresses:
                              type: object
                              description: |-
                                The IPv4 and/or IPv6 addresses affected by this rule. A Rule can have up to 255 total addresses or networks listed across its IPv4 and IPv6 arrays. A network and a single IP are treated as equivalent when accounting for this limit.

                                Must contain `ipv4`, `ipv6`, or both.
                              additionalProperties: false
                              properties:
                                ipv4:
                                  type: array
                                  description: |-
                                    A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.

                                    If "0.0.0.0/0" is included in this list, all IPv4 addresses are affected by this rule.
                                  example:
                                  - 192.0.2.0/24
                                  - 198.51.100.2/32
                                  items:
                                    type: string
                                ipv6:
                                  type: array
                                  description: |-
                                    A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.

                                    If "::/0" is included in this list, all IPv6 addresses are affected by this rule.
                                  example:
                                  - 2001:DB8::/128
                                  items:
                                    type: string
                            label:
                              type: string
                              description: |
                                Used to identify this rule. For display purposes only.
                              example: firewallrule123
                              minLength: 3
                              maxLength: 32
                            ports:
                              type: string
                              description: |-
                                A string representing the port or ports affected by this rule:

                                - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                                - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                                - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port "080" is not allowed.
                                - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                                - If no ports are configured, all ports are affected.
                                - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                              example: 22-24, 80, 443
                              nullable: true
                            protocol:
                              type: string
                              description: |
                                The type of network traffic affected by this rule.
                              example: TCP
                              enum:
                              - TCP
                              - UDP
                              - ICMP
                              - IPENCAP
                        x-linode-cli-format: json
                      inbound_policy:
                        type: string
                        description: |
                          The default behavior for inbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules)
                          the `inbound.action` property of the Firewall Rule.
                        example: DROP
                        enum:
                        - ACCEPT
                        - DROP
                      outbound:
                        type: array
                        description: |
                          The outbound rules for the firewall, as a JSON array.
                        items:
                          type: object
                          description: |
                            One of a Firewall's inbound or outbound access rules. The `ports` property
                            can be used to allow traffic on a comma-separated list
                            of different ports.
                          x-akamai:
                            file-path: schemas/firewall-rule-config.yaml
                          additionalProperties: false
                          properties:
                            description:
                              type: string
                              description: |
                                Used to describe this rule. For display purposes only.
                              example: An example firewall rule description.
                              minLength: 1
                              maxLength: 100
                            action:
                              type: string
                              description: |
                                Controls whether traffic is accepted or dropped by this rule.
                                Overrides the Firewall's `inbound_policy` if this
                                is an inbound rule, or the `outbound_policy` if this
                                is an outbound rule.
                              example: ACCEPT
                              enum:
                              - ACCEPT
                              - DROP
                            addresses:
                              type: object
                              description: |-
                                The IPv4 and/or IPv6 addresses affected by this rule. A Rule can have up to 255 total addresses or networks listed across its IPv4 and IPv6 arrays. A network and a single IP are treated as equivalent when accounting for this limit.

                                Must contain `ipv4`, `ipv6`, or both.
                              additionalProperties: false
                              properties:
                                ipv4:
                                  type: array
                                  description: |-
                                    A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.

                                    If "0.0.0.0/0" is included in this list, all IPv4 addresses are affected by this rule.
                                  example:
                                  - 192.0.2.0/24
                                  - 198.51.100.2/32
                                  items:
                                    type: string
                                ipv6:
                                  type: array
                                  description: |-
                                    A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.

                                    If "::/0" is included in this list, all IPv6 addresses are affected by this rule.
                                  example:
                                  - 2001:DB8::/128
                                  items:
                                    type: string
                            label:
                              type: string
                              description: |
                                Used to identify this rule. For display purposes only.
                              example: firewallrule123
                              minLength: 3
                              maxLength: 32
                            ports:
                              type: string
                              description: |-
                                A string representing the port or ports affected by this rule:

                                - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                                - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                                - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port "080" is not allowed.
                                - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                                - If no ports are configured, all ports are affected.
                                - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                              example: 22-24, 80, 443
                              nullable: true
                            protocol:
                              type: string
                              description: |
                                The type of network traffic affected by this rule.
                              example: TCP
                              enum:
                              - TCP
                              - UDP
                              - ICMP
                              - IPENCAP
                        x-linode-cli-format: json
                      outbound_policy:
                        type: string
                        description: |
                          The default behavior for outbound traffic. This setting can be overridden by
                          [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules)
                          the `outbound.action` property of the Firewall Rule.
                        example: DROP
                        enum:
                        - ACCEPT
                        - DROP
                  status:
                    type: string
                    description: |-
                      __Read-only__ The status of this Firewall.

                        - When a Firewall is first created its status is `enabled`.
                        - RUn the [Update a firewall](https://techdocs.akamai.com/linode-api/reference/put-firewall) operation to set a Firewall's status to `enabled` or `disabled`.
                        - RUn the [Delete a firewall](https://techdocs.akamai.com/linode-api/reference/delete-firewall) operation to delete a Firewall.
                    example: enabled
                    readOnly: true
                    enum:
                    - enabled
                    - disabled
                    - deleted
                    x-linode-cli-display: 3
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Firewall was last updated.
                    example: '2018-01-02T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 5
                    x-linode-filterable: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-firewalls
      security:
      - personalAccessToken: []
      - oauth:
        - firewall:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "label": "firewall123",
                "rules": {
                  "inbound_policy": "DROP",
                  "inbound": [
                    {
                      "protocol": "TCP",
                      "ports": "22, 80, 443",
                      "addresses": {
                        "ipv4": [
                          "192.0.2.0/24",
                          "198.51.100.2/32"
                        ],
                        "ipv6": [
                          "2001:DB8::/128"
                        ]
                      },
                    "action": "ACCEPT",
                    "label": "inbound-rule123",
                    "description": "An example inbound rule description."
                    }
                  ],
                  "outbound_policy": "DROP",
                  "outbound": [
                    {
                      "protocol": "TCP",
                      "ports": "49152-65535",
                      "addresses": {
                        "ipv4": [
                          "192.0.2.0/24",
                          "198.51.100.2/32"
                        ],
                        "ipv6": [
                          "2001:DB8::/128"
                        ]
                      },
                      "action": "ACCEPT",
                      "label": "outbound-rule123",
                      "description": "An example outbound rule description."
                    }
                  ]
                },
                "devices": {
                  "linodes": [
                    123,
                    456
                    ],
                  "nodebalancers": [
                    321
                  ]
                },
                "tags": [
                  "example tag",
                  "another example"
                ]
              }' \
              https://api.linode.com/v4/networking/firewalls
      - lang: CLI
        source: |-
          linode-cli firewalls create \
            --label example-firewall \
            --rules.outbound_policy ACCEPT \
            --rules.inbound_policy DROP \
            --rules.inbound '[{"protocol": "TCP", "ports": "22, 80, 8080, 443", "addresses": {"ipv4": ["192.0.2.0/24", "198.51.100.2/32"], "ipv6": ["2001:DB8::/128"]}, "action": "ACCEPT"}]' \
            --rules.outbound '[{"protocol": "TCP", "ports": "49152-65535", "addresses": {"ipv4": ["192.0.2.0/24", "198.51.100.2/32"],"ipv6": ["2001:DB8::/128"]}, "action": "DROP", "label": "outbound-rule123", "description": "An example outbound rule description."}]'
      x-linode-cli-action: create
      x-linode-grant: add_firewalls
      x-original-op-id: createFirewalls
      x-original-op-title: Firewall Create
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-firewalls
      summary: List firewalls
      tags:
      - Firewalls
      description: |-
        Returns a paginated list of accessible Firewalls.


        ---


        - __OAuth__.

            ```
            firewall:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: firewall:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns an array of Firewalls.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        A resource that controls incoming and outgoing network traffic to a compute service.
                        Only one enabled Firewall can be attached to a particular
                        service at any given time. [Create a firewall device](https://techdocs.akamai.com/linode-api/reference/post-firewall-device)
                        to assign a Firewall to a service. Currently, Firewalls can
                        assigned to Linode compute instances and NodeBalancers.
                      x-akamai:
                        file-path: schemas/firewall.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ The Firewall's unique ID.
                          example: 123
                          readOnly: true
                          x-linode-cli-display: 1
                          x-linode-filterable: true
                        tags:
                          type: array
                          description: |
                            An array of tags applied to this object. Tags are for organizational purposes
                            only.
                          example:
                          - example tag
                          - another example
                          items:
                            type: string
                          x-linode-filterable: true
                        created:
                          type: string
                          description: |
                            __Read-only__ When this Firewall was created.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                          x-linode-cli-display: 4
                          x-linode-filterable: true
                        label:
                          type: string
                          description: |-
                            The Firewall's label, for display purposes only.

                            Firewall labels have the following constraints:

                              - Must begin and end with an alphanumeric character.
                              - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                              - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
                              - Must be between 3 and 32 characters.
                              - Must be unique.
                          example: firewall123
                          minLength: 3
                          maxLength: 32
                          pattern: ^[a-zA-Z]((?!--|__|..)[a-zA-Z0-9-_.])+$
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        rules:
                          type: object
                          description: |-
                            The inbound and outbound access rules to apply to the Firewall.

                            A Firewall may have up to 25 rules across its inbound and outbound rulesets.

                            Multiple rules are applied in order. If two rules conflict, the first rule takes precedence. For example, if the first rule accepts inbound traffic from an address, and the second rule drops inbound traffic the same address, the first rule applies and inbound traffic from that address is accepted.
                          additionalProperties: false
                          properties:
                            inbound:
                              type: array
                              description: |
                                The inbound rules for the firewall, as a JSON array.
                              items:
                                type: object
                                description: |
                                  One of a Firewall's inbound or outbound access rules. The
                                  `ports` property can be used to allow traffic on
                                  a comma-separated list of different ports.
                                x-akamai:
                                  file-path: schemas/firewall-rule-config.yaml
                                additionalProperties: false
                                properties:
                                  description:
                                    type: string
                                    description: |
                                      Used to describe this rule. For display purposes
                                      only.
                                    example: An example firewall rule description.
                                    minLength: 1
                                    maxLength: 100
                                  action:
                                    type: string
                                    description: |
                                      Controls whether traffic is accepted or dropped by this
                                      rule. Overrides the Firewall's `inbound_policy`
                                      if this is an inbound rule, or the `outbound_policy`
                                      if this is an outbound rule.
                                    example: ACCEPT
                                    enum:
                                    - ACCEPT
                                    - DROP
                                  addresses:
                                    type: object
                                    description: |-
                                      The IPv4 and/or IPv6 addresses affected by this rule. A Rule can have up to 255 total addresses or networks listed across its IPv4 and IPv6 arrays. A network and a single IP are treated as equivalent when accounting for this limit.

                                      Must contain `ipv4`, `ipv6`, or both.
                                    additionalProperties: false
                                    properties:
                                      ipv4:
                                        type: array
                                        description: |-
                                          A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.

                                          If "0.0.0.0/0" is included in this list, all IPv4 addresses are affected by this rule.
                                        example:
                                        - 192.0.2.0/24
                                        - 198.51.100.2/32
                                        items:
                                          type: string
                                      ipv6:
                                        type: array
                                        description: |-
                                          A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.

                                          If "::/0" is included in this list, all IPv6 addresses are affected by this rule.
                                        example:
                                        - 2001:DB8::/128
                                        items:
                                          type: string
                                  label:
                                    type: string
                                    description: |
                                      Used to identify this rule. For display purposes
                                      only.
                                    example: firewallrule123
                                    minLength: 3
                                    maxLength: 32
                                  ports:
                                    type: string
                                    description: |-
                                      A string representing the port or ports affected by this rule:

                                      - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                                      - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                                      - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port "080" is not allowed.
                                      - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                                      - If no ports are configured, all ports are affected.
                                      - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                                    example: 22-24, 80, 443
                                    nullable: true
                                  protocol:
                                    type: string
                                    description: |
                                      The type of network traffic affected by this rule.
                                    example: TCP
                                    enum:
                                    - TCP
                                    - UDP
                                    - ICMP
                                    - IPENCAP
                              x-linode-cli-format: json
                            inbound_policy:
                              type: string
                              description: |
                                The default behavior for inbound traffic. This setting can be
                                overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules)
                                the `inbound.action` property of the Firewall Rule.
                              example: DROP
                              enum:
                              - ACCEPT
                              - DROP
                            outbound:
                              type: array
                              description: |
                                The outbound rules for the firewall, as a JSON array.
                              items:
                                type: object
                                description: |
                                  One of a Firewall's inbound or outbound access rules. The
                                  `ports` property can be used to allow traffic on
                                  a comma-separated list of different ports.
                                x-akamai:
                                  file-path: schemas/firewall-rule-config.yaml
                                additionalProperties: false
                                properties:
                                  description:
                                    type: string
                                    description: |
                                      Used to describe this rule. For display purposes
                                      only.
                                    example: An example firewall rule description.
                                    minLength: 1
                                    maxLength: 100
                                  action:
                                    type: string
                                    description: |
                                      Controls whether traffic is accepted or dropped by this
                                      rule. Overrides the Firewall's `inbound_policy`
                                      if this is an inbound rule, or the `outbound_policy`
                                      if this is an outbound rule.
                                    example: ACCEPT
                                    enum:
                                    - ACCEPT
                                    - DROP
                                  addresses:
                                    type: object
                                    description: |-
                                      The IPv4 and/or IPv6 addresses affected by this rule. A Rule can have up to 255 total addresses or networks listed across its IPv4 and IPv6 arrays. A network and a single IP are treated as equivalent when accounting for this limit.

                                      Must contain `ipv4`, `ipv6`, or both.
                                    additionalProperties: false
                                    properties:
                                      ipv4:
                                        type: array
                                        description: |-
                                          A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.

                                          If "0.0.0.0/0" is included in this list, all IPv4 addresses are affected by this rule.
                                        example:
                                        - 192.0.2.0/24
                                        - 198.51.100.2/32
                                        items:
                                          type: string
                                      ipv6:
                                        type: array
                                        description: |-
                                          A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.

                                          If "::/0" is included in this list, all IPv6 addresses are affected by this rule.
                                        example:
                                        - 2001:DB8::/128
                                        items:
                                          type: string
                                  label:
                                    type: string
                                    description: |
                                      Used to identify this rule. For display purposes
                                      only.
                                    example: firewallrule123
                                    minLength: 3
                                    maxLength: 32
                                  ports:
                                    type: string
                                    description: |-
                                      A string representing the port or ports affected by this rule:

                                      - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                                      - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                                      - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port "080" is not allowed.
                                      - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                                      - If no ports are configured, all ports are affected.
                                      - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                                    example: 22-24, 80, 443
                                    nullable: true
                                  protocol:
                                    type: string
                                    description: |
                                      The type of network traffic affected by this rule.
                                    example: TCP
                                    enum:
                                    - TCP
                                    - UDP
                                    - ICMP
                                    - IPENCAP
                              x-linode-cli-format: json
                            outbound_policy:
                              type: string
                              description: |
                                The default behavior for outbound traffic. This setting can be
                                overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules)
                                the `outbound.action` property of the Firewall Rule.
                              example: DROP
                              enum:
                              - ACCEPT
                              - DROP
                        status:
                          type: string
                          description: |-
                            __Read-only__ The status of this Firewall.

                              - When a Firewall is first created its status is `enabled`.
                              - RUn the [Update a firewall](https://techdocs.akamai.com/linode-api/reference/put-firewall) operation to set a Firewall's status to `enabled` or `disabled`.
                              - RUn the [Delete a firewall](https://techdocs.akamai.com/linode-api/reference/delete-firewall) operation to delete a Firewall.
                          example: enabled
                          readOnly: true
                          enum:
                          - enabled
                          - disabled
                          - deleted
                          x-linode-cli-display: 3
                        updated:
                          type: string
                          description: |
                            __Read-only__ When this Firewall was last updated.
                          example: '2018-01-02T00:01:01'
                          readOnly: true
                          format: date-time
                          x-linode-cli-display: 5
                          x-linode-filterable: true
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-firewalls
      security:
      - personalAccessToken: []
      - oauth:
        - firewall:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/networking/firewalls
      - lang: CLI
        source: linode-cli firewalls list
      x-linode-cli-action:
      - list
      - ls
      x-linode-grant: read_only
      x-original-op-id: getFirewalls
      x-original-op-title: Firewalls List
    x-linode-cli-command: firewalls
  /networking/firewalls/{firewallId}:
    x-akamai:
      file-path: paths/firewall.yaml
      path-info: /networking/firewalls/{firewallId}
    parameters:
    - name: firewallId
      in: path
      description: |
        ID of the Firewall to access.
      example: '{{firewallId}}'
      x-akamai:
        file-path: parameters/firewall-id.yaml
      required: true
      schema:
        type: integer
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-firewall
      summary: Get a firewall
      tags:
      - Firewalls
      description: |-
        Get a specific Firewall resource by its ID. The Firewall's Devices will not be returned in the response. Instead, run the [List firewall devices](https://techdocs.akamai.com/linode-api/reference/get-firewall-devices) operation to review them.


        ---


        - __CLI__.

            ```
            linode-cli firewalls view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            firewall:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli firewalls view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: firewall:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns information about this Firewall.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A resource that controls incoming and outgoing network traffic to a compute service. Only
                  one enabled Firewall can be attached to a particular service at
                  any given time. [Create a firewall device](https://techdocs.akamai.com/linode-api/reference/post-firewall-device)
                  to assign a Firewall to a service. Currently, Firewalls can assigned
                  to Linode compute instances and NodeBalancers.
                x-akamai:
                  file-path: schemas/firewall.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The Firewall's unique ID.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  tags:
                    type: array
                    description: |
                      An array of tags applied to this object. Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Firewall was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  label:
                    type: string
                    description: |-
                      The Firewall's label, for display purposes only.

                      Firewall labels have the following constraints:

                        - Must begin and end with an alphanumeric character.
                        - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                        - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
                        - Must be between 3 and 32 characters.
                        - Must be unique.
                    example: firewall123
                    minLength: 3
                    maxLength: 32
                    pattern: ^[a-zA-Z]((?!--|__|..)[a-zA-Z0-9-_.])+$
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  rules:
                    type: object
                    description: |-
                      The inbound and outbound access rules to apply to the Firewall.

                      A Firewall may have up to 25 rules across its inbound and outbound rulesets.

                      Multiple rules are applied in order. If two rules conflict, the first rule takes precedence. For example, if the first rule accepts inbound traffic from an address, and the second rule drops inbound traffic the same address, the first rule applies and inbound traffic from that address is accepted.
                    additionalProperties: false
                    properties:
                      inbound:
                        type: array
                        description: |
                          The inbound rules for the firewall, as a JSON array.
                        items:
                          type: object
                          description: |
                            One of a Firewall's inbound or outbound access rules. The `ports` property
                            can be used to allow traffic on a comma-separated list
                            of different ports.
                          x-akamai:
                            file-path: schemas/firewall-rule-config.yaml
                          additionalProperties: false
                          properties:
                            description:
                              type: string
                              description: |
                                Used to describe this rule. For display purposes only.
                              example: An example firewall rule description.
                              minLength: 1
                              maxLength: 100
                            action:
                              type: string
                              description: |
                                Controls whether traffic is accepted or dropped by this rule.
                                Overrides the Firewall's `inbound_policy` if this
                                is an inbound rule, or the `outbound_policy` if this
                                is an outbound rule.
                              example: ACCEPT
                              enum:
                              - ACCEPT
                              - DROP
                            addresses:
                              type: object
                              description: |-
                                The IPv4 and/or IPv6 addresses affected by this rule. A Rule can have up to 255 total addresses or networks listed across its IPv4 and IPv6 arrays. A network and a single IP are treated as equivalent when accounting for this limit.

                                Must contain `ipv4`, `ipv6`, or both.
                              additionalProperties: false
                              properties:
                                ipv4:
                                  type: array
                                  description: |-
                                    A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.

                                    If "0.0.0.0/0" is included in this list, all IPv4 addresses are affected by this rule.
                                  example:
                                  - 192.0.2.0/24
                                  - 198.51.100.2/32
                                  items:
                                    type: string
                                ipv6:
                                  type: array
                                  description: |-
                                    A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.

                                    If "::/0" is included in this list, all IPv6 addresses are affected by this rule.
                                  example:
                                  - 2001:DB8::/128
                                  items:
                                    type: string
                            label:
                              type: string
                              description: |
                                Used to identify this rule. For display purposes only.
                              example: firewallrule123
                              minLength: 3
                              maxLength: 32
                            ports:
                              type: string
                              description: |-
                                A string representing the port or ports affected by this rule:

                                - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                                - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                                - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port "080" is not allowed.
                                - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                                - If no ports are configured, all ports are affected.
                                - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                              example: 22-24, 80, 443
                              nullable: true
                            protocol:
                              type: string
                              description: |
                                The type of network traffic affected by this rule.
                              example: TCP
                              enum:
                              - TCP
                              - UDP
                              - ICMP
                              - IPENCAP
                        x-linode-cli-format: json
                      inbound_policy:
                        type: string
                        description: |
                          The default behavior for inbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules)
                          the `inbound.action` property of the Firewall Rule.
                        example: DROP
                        enum:
                        - ACCEPT
                        - DROP
                      outbound:
                        type: array
                        description: |
                          The outbound rules for the firewall, as a JSON array.
                        items:
                          type: object
                          description: |
                            One of a Firewall's inbound or outbound access rules. The `ports` property
                            can be used to allow traffic on a comma-separated list
                            of different ports.
                          x-akamai:
                            file-path: schemas/firewall-rule-config.yaml
                          additionalProperties: false
                          properties:
                            description:
                              type: string
                              description: |
                                Used to describe this rule. For display purposes only.
                              example: An example firewall rule description.
                              minLength: 1
                              maxLength: 100
                            action:
                              type: string
                              description: |
                                Controls whether traffic is accepted or dropped by this rule.
                                Overrides the Firewall's `inbound_policy` if this
                                is an inbound rule, or the `outbound_policy` if this
                                is an outbound rule.
                              example: ACCEPT
                              enum:
                              - ACCEPT
                              - DROP
                            addresses:
                              type: object
                              description: |-
                                The IPv4 and/or IPv6 addresses affected by this rule. A Rule can have up to 255 total addresses or networks listed across its IPv4 and IPv6 arrays. A network and a single IP are treated as equivalent when accounting for this limit.

                                Must contain `ipv4`, `ipv6`, or both.
                              additionalProperties: false
                              properties:
                                ipv4:
                                  type: array
                                  description: |-
                                    A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.

                                    If "0.0.0.0/0" is included in this list, all IPv4 addresses are affected by this rule.
                                  example:
                                  - 192.0.2.0/24
                                  - 198.51.100.2/32
                                  items:
                                    type: string
                                ipv6:
                                  type: array
                                  description: |-
                                    A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.

                                    If "::/0" is included in this list, all IPv6 addresses are affected by this rule.
                                  example:
                                  - 2001:DB8::/128
                                  items:
                                    type: string
                            label:
                              type: string
                              description: |
                                Used to identify this rule. For display purposes only.
                              example: firewallrule123
                              minLength: 3
                              maxLength: 32
                            ports:
                              type: string
                              description: |-
                                A string representing the port or ports affected by this rule:

                                - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                                - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                                - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port "080" is not allowed.
                                - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                                - If no ports are configured, all ports are affected.
                                - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                              example: 22-24, 80, 443
                              nullable: true
                            protocol:
                              type: string
                              description: |
                                The type of network traffic affected by this rule.
                              example: TCP
                              enum:
                              - TCP
                              - UDP
                              - ICMP
                              - IPENCAP
                        x-linode-cli-format: json
                      outbound_policy:
                        type: string
                        description: |
                          The default behavior for outbound traffic. This setting can be overridden by
                          [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules)
                          the `outbound.action` property of the Firewall Rule.
                        example: DROP
                        enum:
                        - ACCEPT
                        - DROP
                  status:
                    type: string
                    description: |-
                      __Read-only__ The status of this Firewall.

                        - When a Firewall is first created its status is `enabled`.
                        - RUn the [Update a firewall](https://techdocs.akamai.com/linode-api/reference/put-firewall) operation to set a Firewall's status to `enabled` or `disabled`.
                        - RUn the [Delete a firewall](https://techdocs.akamai.com/linode-api/reference/delete-firewall) operation to delete a Firewall.
                    example: enabled
                    readOnly: true
                    enum:
                    - enabled
                    - disabled
                    - deleted
                    x-linode-cli-display: 3
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Firewall was last updated.
                    example: '2018-01-02T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 5
                    x-linode-filterable: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-firewall
      security:
      - personalAccessToken: []
      - oauth:
        - firewall:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/networking/firewalls/123
      - lang: CLI
        source: linode-cli firewalls view 123
      x-linode-cli-action: view
      x-linode-grant: read_only
      x-original-op-id: getFirewall
      x-original-op-title: Firewall View
    put:
      servers:
      - url: https://api.linode.com/v4
      operationId: put-firewall
      summary: Update a firewall
      tags:
      - Firewalls
      description: |-
        Updates information for a Firewall.

        - Assigned Linodes must not have any ongoing live migrations.

        - If a Firewall's status is changed with this operation, a corresponding `firewall_enable` or `firewall_disable` Event will be generated.

        Some parts of a Firewall's configuration cannot be manipulated by this operation:

        - A Firewall's Devices cannot be set with this operation. Instead, run the [Create a firewall device](https://techdocs.akamai.com/linode-api/reference/post-firewall-device) and [Delete a firewall device](https://techdocs.akamai.com/linode-api/reference/delete-firewall-device) operations to assign and remove this Firewall from services.

        - A Firewall's Rules cannot be changed with this operation. Instead, run the [Update firewall rules](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) operation to update your Rules.

        - A Firewall's status can be set to `enabled` or `disabled` by this operation, but it cannot be set to `deleted`. Instead, run the [Delete a firewall](https://techdocs.akamai.com/linode-api/reference/delete-firewall) operation to delete a Firewall.


        ---


        - __CLI__.

            ```
            linode-cli firewalls update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            firewall:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli firewalls update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: firewall:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The Firewall information to update.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                tags:
                  type: array
                  description: |
                    An array of tags applied to this object. Tags are for organizational purposes only.
                  example:
                  - example tag
                  - another example
                  items:
                    type: string
                  x-linode-filterable: true
                label:
                  type: string
                  description: |-
                    The Firewall's label, for display purposes only.

                    Firewall labels have the following constraints:

                      - Must begin and end with an alphanumeric character.
                      - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                      - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
                      - Must be between 3 and 32 characters.
                      - Must be unique.
                  example: '{{label}}'
                  minLength: 3
                  maxLength: 32
                  pattern: ^[a-zA-Z]((?!--|__|..)[a-zA-Z0-9-_.])+$
                  x-linode-cli-display: 2
                  x-linode-filterable: true
                status:
                  type: string
                  description: |-
                    The status to be applied to this Firewall.

                     - When a Firewall is first created its status is `enabled`.
                     - Run the [Delete a firewall](https://techdocs.akamai.com/linode-api/reference/delete-firewall) operation to delete a Firewall.
                  example: '{{status}}'
                  enum:
                  - enabled
                  - disabled
                  x-linode-cli-display: 3
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Firewall updated successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  A resource that controls incoming and outgoing network traffic to a compute service. Only
                  one enabled Firewall can be attached to a particular service at
                  any given time. [Create a firewall device](https://techdocs.akamai.com/linode-api/reference/post-firewall-device)
                  to assign a Firewall to a service. Currently, Firewalls can assigned
                  to Linode compute instances and NodeBalancers.
                x-akamai:
                  file-path: schemas/firewall.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ The Firewall's unique ID.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 1
                    x-linode-filterable: true
                  tags:
                    type: array
                    description: |
                      An array of tags applied to this object. Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Firewall was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 4
                    x-linode-filterable: true
                  label:
                    type: string
                    description: |-
                      The Firewall's label, for display purposes only.

                      Firewall labels have the following constraints:

                        - Must begin and end with an alphanumeric character.
                        - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                        - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
                        - Must be between 3 and 32 characters.
                        - Must be unique.
                    example: firewall123
                    minLength: 3
                    maxLength: 32
                    pattern: ^[a-zA-Z]((?!--|__|..)[a-zA-Z0-9-_.])+$
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  rules:
                    type: object
                    description: |-
                      The inbound and outbound access rules to apply to the Firewall.

                      A Firewall may have up to 25 rules across its inbound and outbound rulesets.

                      Multiple rules are applied in order. If two rules conflict, the first rule takes precedence. For example, if the first rule accepts inbound traffic from an address, and the second rule drops inbound traffic the same address, the first rule applies and inbound traffic from that address is accepted.
                    additionalProperties: false
                    properties:
                      inbound:
                        type: array
                        description: |
                          The inbound rules for the firewall, as a JSON array.
                        items:
                          type: object
                          description: |
                            One of a Firewall's inbound or outbound access rules. The `ports` property
                            can be used to allow traffic on a comma-separated list
                            of different ports.
                          x-akamai:
                            file-path: schemas/firewall-rule-config.yaml
                          additionalProperties: false
                          properties:
                            description:
                              type: string
                              description: |
                                Used to describe this rule. For display purposes only.
                              example: An example firewall rule description.
                              minLength: 1
                              maxLength: 100
                            action:
                              type: string
                              description: |
                                Controls whether traffic is accepted or dropped by this rule.
                                Overrides the Firewall's `inbound_policy` if this
                                is an inbound rule, or the `outbound_policy` if this
                                is an outbound rule.
                              example: ACCEPT
                              enum:
                              - ACCEPT
                              - DROP
                            addresses:
                              type: object
                              description: |-
                                The IPv4 and/or IPv6 addresses affected by this rule. A Rule can have up to 255 total addresses or networks listed across its IPv4 and IPv6 arrays. A network and a single IP are treated as equivalent when accounting for this limit.

                                Must contain `ipv4`, `ipv6`, or both.
                              additionalProperties: false
                              properties:
                                ipv4:
                                  type: array
                                  description: |-
                                    A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.

                                    If "0.0.0.0/0" is included in this list, all IPv4 addresses are affected by this rule.
                                  example:
                                  - 192.0.2.0/24
                                  - 198.51.100.2/32
                                  items:
                                    type: string
                                ipv6:
                                  type: array
                                  description: |-
                                    A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.

                                    If "::/0" is included in this list, all IPv6 addresses are affected by this rule.
                                  example:
                                  - 2001:DB8::/128
                                  items:
                                    type: string
                            label:
                              type: string
                              description: |
                                Used to identify this rule. For display purposes only.
                              example: firewallrule123
                              minLength: 3
                              maxLength: 32
                            ports:
                              type: string
                              description: |-
                                A string representing the port or ports affected by this rule:

                                - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                                - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                                - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port "080" is not allowed.
                                - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                                - If no ports are configured, all ports are affected.
                                - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                              example: 22-24, 80, 443
                              nullable: true
                            protocol:
                              type: string
                              description: |
                                The type of network traffic affected by this rule.
                              example: TCP
                              enum:
                              - TCP
                              - UDP
                              - ICMP
                              - IPENCAP
                        x-linode-cli-format: json
                      inbound_policy:
                        type: string
                        description: |
                          The default behavior for inbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules)
                          the `inbound.action` property of the Firewall Rule.
                        example: DROP
                        enum:
                        - ACCEPT
                        - DROP
                      outbound:
                        type: array
                        description: |
                          The outbound rules for the firewall, as a JSON array.
                        items:
                          type: object
                          description: |
                            One of a Firewall's inbound or outbound access rules. The `ports` property
                            can be used to allow traffic on a comma-separated list
                            of different ports.
                          x-akamai:
                            file-path: schemas/firewall-rule-config.yaml
                          additionalProperties: false
                          properties:
                            description:
                              type: string
                              description: |
                                Used to describe this rule. For display purposes only.
                              example: An example firewall rule description.
                              minLength: 1
                              maxLength: 100
                            action:
                              type: string
                              description: |
                                Controls whether traffic is accepted or dropped by this rule.
                                Overrides the Firewall's `inbound_policy` if this
                                is an inbound rule, or the `outbound_policy` if this
                                is an outbound rule.
                              example: ACCEPT
                              enum:
                              - ACCEPT
                              - DROP
                            addresses:
                              type: object
                              description: |-
                                The IPv4 and/or IPv6 addresses affected by this rule. A Rule can have up to 255 total addresses or networks listed across its IPv4 and IPv6 arrays. A network and a single IP are treated as equivalent when accounting for this limit.

                                Must contain `ipv4`, `ipv6`, or both.
                              additionalProperties: false
                              properties:
                                ipv4:
                                  type: array
                                  description: |-
                                    A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.

                                    If "0.0.0.0/0" is included in this list, all IPv4 addresses are affected by this rule.
                                  example:
                                  - 192.0.2.0/24
                                  - 198.51.100.2/32
                                  items:
                                    type: string
                                ipv6:
                                  type: array
                                  description: |-
                                    A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.

                                    If "::/0" is included in this list, all IPv6 addresses are affected by this rule.
                                  example:
                                  - 2001:DB8::/128
                                  items:
                                    type: string
                            label:
                              type: string
                              description: |
                                Used to identify this rule. For display purposes only.
                              example: firewallrule123
                              minLength: 3
                              maxLength: 32
                            ports:
                              type: string
                              description: |-
                                A string representing the port or ports affected by this rule:

                                - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                                - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                                - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port "080" is not allowed.
                                - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                                - If no ports are configured, all ports are affected.
                                - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                              example: 22-24, 80, 443
                              nullable: true
                            protocol:
                              type: string
                              description: |
                                The type of network traffic affected by this rule.
                              example: TCP
                              enum:
                              - TCP
                              - UDP
                              - ICMP
                              - IPENCAP
                        x-linode-cli-format: json
                      outbound_policy:
                        type: string
                        description: |
                          The default behavior for outbound traffic. This setting can be overridden by
                          [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules)
                          the `outbound.action` property of the Firewall Rule.
                        example: DROP
                        enum:
                        - ACCEPT
                        - DROP
                  status:
                    type: string
                    description: |-
                      __Read-only__ The status of this Firewall.

                        - When a Firewall is first created its status is `enabled`.
                        - RUn the [Update a firewall](https://techdocs.akamai.com/linode-api/reference/put-firewall) operation to set a Firewall's status to `enabled` or `disabled`.
                        - RUn the [Delete a firewall](https://techdocs.akamai.com/linode-api/reference/delete-firewall) operation to delete a Firewall.
                    example: enabled
                    readOnly: true
                    enum:
                    - enabled
                    - disabled
                    - deleted
                    x-linode-cli-display: 3
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Firewall was last updated.
                    example: '2018-01-02T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 5
                    x-linode-filterable: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-firewall
      security:
      - personalAccessToken: []
      - oauth:
        - firewall:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "status": "disabled"
              }' \
              https://api.linode.com/v4/networking/firewalls/123
      - lang: CLI
        source: |-
          linode-cli firewalls update 123 \
            --status disabled
      x-linode-cli-action: update
      x-linode-grant: read_write
      x-original-op-id: updateFirewall
      x-original-op-title: Firewall Update
    delete:
      servers:
      - url: https://api.linode.com/v4
      operationId: delete-firewall
      summary: Delete a firewall
      tags:
      - Firewalls
      description: |-
        Delete a Firewall resource by its ID. This removes all of the Firewall's Rules from any services that the Firewall was assigned to.

        - Assigned Linodes must not have any ongoing live migrations.

        - A `firewall_delete` Event is generated when this operation returns successfully.


        ---


        - __OAuth__.

            ```
            firewall:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: firewall:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Delete Successful.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-firewall
      security:
      - personalAccessToken: []
      - oauth:
        - firewall:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/networking/firewalls/123
      - lang: CLI
        source: linode-cli firewalls delete 123
      x-linode-cli-action:
      - delete
      - rm
      x-linode-grant: read_write
      x-original-op-id: deleteFirewall
      x-original-op-title: Firewall Delete
    x-linode-cli-command: firewalls
  /networking/firewalls/{firewallId}/devices:
    x-akamai:
      file-path: paths/firewall-devices.yaml
      path-info: /networking/firewalls/{firewallId}/devices
    parameters:
    - name: firewallId
      in: path
      description: |
        ID of the Firewall to access.
      example: '{{firewallId}}'
      x-akamai:
        file-path: parameters/firewall-id.yaml
      required: true
      schema:
        type: integer
    post:
      servers:
      - url: https://api.linode.com/v4
      operationId: post-firewall-device
      summary: Create a firewall device
      tags:
      - Devices
      description: |-
        Creates a Firewall Device, which assigns a Firewall to a service (referred to as the Device's `entity`) and applies the Firewall's Rules to the device.

        - Currently, Devices with `linode` and `nodebalancer` entity types are accepted.

        - Firewalls only apply to inbound TCP traffic to NodeBalancers.

        - A Firewall can be assigned to multiple services at a time.

        - A service can have one active, assigned Firewall at a time. Additional disabled Firewalls can be assigned to a service, but they cannot be enabled if another active Firewall is already assigned to the same service.

        - Assigned Linodes must not have any ongoing live migrations.

        - A `firewall_device_add` Event is generated when the Firewall Device is added successfully.


        ---


        - __CLI__.

            ```
            linode-cli firewalls device-create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            firewall:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli firewalls device-create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: firewall:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required:
              - id
              - type
              allOf:
              - type: object
                description: |
                  __Read-only__ The compute service that this Firewall has been applied to.
                additionalProperties: false
                readOnly: true
                properties:
                  id:
                    type: integer
                    description: |
                      The entity's ID
                    example: 123
                  type:
                    type: string
                    description: |
                      The entity's type.
                    example: linode
                    enum:
                    - linode
                    - nodebalancer
                  label:
                    type: string
                    description: |
                      __Read-only__ The entity's label.
                    example: my-linode
                    readOnly: true
                  url:
                    type: string
                    description: |
                      __Read-only__ The API URL path you can use to access this entity.
                    example: /v4/linode/instances/123
                    readOnly: true
                    format: url
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns information about the created Firewall Device.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Associates a Firewall with a Linode or NodeBalancer service. A Firewall can be assigned to
                  a single entity at a time. Additional disabled Firewalls can be
                  assigned to a service, but they cannot be enabled if another active
                  Firewall is already assigned to the same service.
                x-akamai:
                  file-path: schemas/firewall-devices.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      The Device's unique ID
                    example: 123
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Device was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  entity:
                    type: object
                    description: |
                      __Read-only__ The compute service that this Firewall has been applied to.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      id:
                        type: integer
                        description: |
                          The entity's ID
                        example: 123
                      type:
                        type: string
                        description: |
                          The entity's type.
                        example: linode
                        enum:
                        - linode
                        - nodebalancer
                      label:
                        type: string
                        description: |
                          __Read-only__ The entity's label.
                        example: my-linode
                        readOnly: true
                      url:
                        type: string
                        description: |
                          __Read-only__ The API URL path you can use to access this entity.
                        example: /v4/linode/instances/123
                        readOnly: true
                        format: url
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Device was last updated.
                    example: '2018-01-02T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 4
                    x-linode-filterable: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-firewall-device
      security:
      - personalAccessToken: []
      - oauth:
        - firewall:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "type": "linode",
                "id": 123
              }' \
              https://api.linode.com/v4/networking/firewalls/123/devices
      - lang: CLI
        source: |-
          linode-cli firewalls device-create 123 \
            --id 456 \
            --type "linode"
      x-linode-cli-action: device-create
      x-linode-grant: read_write
      x-original-op-id: createFirewallDevice
      x-original-op-title: Firewall Device Create
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-firewall-devices
      summary: List firewall devices
      tags:
      - Devices
      description: |-
        Returns a paginated list of a Firewall's Devices. A Firewall Device assigns a Firewall to a service (referred to as the Device's `entity`).


        ---


        - __CLI__.

            ```
            linode-cli firewalls devices-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            firewall:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli firewalls devices-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: firewall:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of Firewall Devices
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    example:
                    - id: 456
                      created: 2018-01-01T00:01:01.000Z
                      entity:
                        id: 123
                        type: linode
                        label: my-linode
                        url: /v4/linode/instances/123
                      updated: 2018-01-02T00:01:01.000Z
                    - id: 654
                      created: 2018-01-01T00:01:01.000Z
                      entity:
                        id: 321
                        type: nodebalancer
                        label: my-nodebalancer
                        url: /v4/nodebalancers/123
                      updated: 2018-01-02T00:01:01.000Z
                    items:
                      type: object
                      description: |
                        Associates a Firewall with a Linode or NodeBalancer service. A Firewall can be assigned
                        to a single entity at a time. Additional disabled Firewalls
                        can be assigned to a service, but they cannot be enabled if
                        another active Firewall is already assigned to the same service.
                      x-akamai:
                        file-path: schemas/firewall-devices.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            The Device's unique ID
                          example: 123
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        created:
                          type: string
                          description: |
                            __Read-only__ When this Device was created.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                          x-linode-cli-display: 3
                          x-linode-filterable: true
                        entity:
                          type: object
                          description: |
                            __Read-only__ The compute service that this Firewall has been applied
                            to.
                          additionalProperties: false
                          readOnly: true
                          properties:
                            id:
                              type: integer
                              description: |
                                The entity's ID
                              example: 123
                            type:
                              type: string
                              description: |
                                The entity's type.
                              example: linode
                              enum:
                              - linode
                              - nodebalancer
                            label:
                              type: string
                              description: |
                                __Read-only__ The entity's label.
                              example: my-linode
                              readOnly: true
                            url:
                              type: string
                              description: |
                                __Read-only__ The API URL path you can use to access this entity.
                              example: /v4/linode/instances/123
                              readOnly: true
                              format: url
                        updated:
                          type: string
                          description: |
                            __Read-only__ When this Device was last updated.
                          example: '2018-01-02T00:01:01'
                          readOnly: true
                          format: date-time
                          x-linode-cli-display: 4
                          x-linode-filterable: true
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-firewall-devices
      security:
      - personalAccessToken: []
      - oauth:
        - firewall:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/networking/firewalls/123/devices
      - lang: CLI
        source: linode-cli firewalls devices-list 123
      x-linode-cli-action: devices-list
      x-linode-grant: read_only
      x-original-op-id: getFirewallDevices
      x-original-op-title: Firewall Devices List
    x-linode-cli-command: firewalls
  /networking/firewalls/{firewallId}/devices/{deviceId}:
    x-akamai:
      file-path: paths/firewall-device.yaml
      path-info: /networking/firewalls/{firewallId}/devices/{deviceId}
    parameters:
    - name: firewallId
      in: path
      description: |
        ID of the Firewall to access.
      example: '{{firewallId}}'
      x-akamai:
        file-path: parameters/firewall-id.yaml
      required: true
      schema:
        type: integer
    - name: deviceId
      in: path
      description: |
        ID of the Firewall Device to access.
      example: '{{deviceId}}'
      x-akamai:
        file-path: parameters/device-id-path-b7287cbb.yaml
      required: true
      schema:
        type: integer
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-firewall-device
      summary: Get a firewall device
      tags:
      - Devices
      description: |-
        Returns information for a Firewall Device, which assigns a Firewall to a service (referred to as the Device's `entity`).


        ---


        - __CLI__.

            ```
            linode-cli firewalls device-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            firewall:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli firewalls device-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: firewall:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The requested Firewall Device.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Associates a Firewall with a Linode or NodeBalancer service. A Firewall can be assigned to
                  a single entity at a time. Additional disabled Firewalls can be
                  assigned to a service, but they cannot be enabled if another active
                  Firewall is already assigned to the same service.
                x-akamai:
                  file-path: schemas/firewall-devices.yaml
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      The Device's unique ID
                    example: 123
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  created:
                    type: string
                    description: |
                      __Read-only__ When this Device was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  entity:
                    type: object
                    description: |
                      __Read-only__ The compute service that this Firewall has been applied to.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      id:
                        type: integer
                        description: |
                          The entity's ID
                        example: 123
                      type:
                        type: string
                        description: |
                          The entity's type.
                        example: linode
                        enum:
                        - linode
                        - nodebalancer
                      label:
                        type: string
                        description: |
                          __Read-only__ The entity's label.
                        example: my-linode
                        readOnly: true
                      url:
                        type: string
                        description: |
                          __Read-only__ The API URL path you can use to access this entity.
                        example: /v4/linode/instances/123
                        readOnly: true
                        format: url
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this Device was last updated.
                    example: '2018-01-02T00:01:01'
                    readOnly: true
                    format: date-time
                    x-linode-cli-display: 4
                    x-linode-filterable: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-firewall-device
      security:
      - personalAccessToken: []
      - oauth:
        - firewall:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/networking/firewalls/123/devices/456
      - lang: CLI
        source: |-
          linode-cli firewalls device-view \
            123 456
      x-linode-cli-action: device-view
      x-linode-grant: read_only
      x-original-op-id: getFirewallDevice
      x-original-op-title: Firewall Device View
    delete:
      servers:
      - url: https://api.linode.com/v4
      operationId: delete-firewall-device
      summary: Delete a firewall device
      tags:
      - Devices
      description: |-
        Removes a Firewall Device, which removes a Firewall from the service it was assigned to by the Device. This removes all of the Firewall's Rules from the service. If any other Firewalls have been assigned to the service, then those Rules remain in effect.

        - Assigned Linodes must not have any ongoing live migrations.

        - A `firewall_device_remove` Event is generated when the Firewall Device is removed successfully.


        ---


        - __CLI__.

            ```
            linode-cli firewalls device-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            firewall:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli firewalls device-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: firewall:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Delete Successful.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-firewall-device
      security:
      - personalAccessToken: []
      - oauth:
        - firewall:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
               -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/networking/firewalls/123/devices/456
      - lang: CLI
        source: linode-cli firewalls device-delete 123 456
      x-linode-cli-action: device-delete
      x-linode-grant: read_write
      x-original-op-id: deleteFirewallDevice
      x-original-op-title: Firewall Device Delete
    x-linode-cli-command: firewalls
  /networking/firewalls/{firewallId}/rules:
    x-akamai:
      file-path: paths/rules.yaml
      path-info: /networking/firewalls/{firewallId}/rules
    parameters:
    - name: firewallId
      in: path
      description: |
        ID of the Firewall to access.
      example: '{{firewallId}}'
      x-akamai:
        file-path: parameters/firewall-id.yaml
      required: true
      schema:
        type: integer
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-firewall-rules
      summary: List firewall rules
      tags:
      - Firewalls
      description: |-
        Returns the inbound and outbound Rules for a Firewall.


        ---


        - __CLI__.

            ```
            linode-cli firewalls rules-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            firewall:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli firewalls rules-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: firewall:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The requested Firewall Rules.
          content:
            application/json:
              schema:
                type: object
                description: |-
                  The inbound and outbound access rules to apply to the Firewall.

                  A Firewall may have up to 25 rules across its inbound and outbound rulesets.

                  Multiple rules are applied in order. If two rules conflict, the first rule takes precedence. For example, if the first rule accepts inbound traffic from an address, and the second rule drops inbound traffic the same address, the first rule applies and inbound traffic from that address is accepted.
                additionalProperties: false
                properties:
                  inbound:
                    type: array
                    description: |
                      The inbound rules for the firewall, as a JSON array.
                    items:
                      type: object
                      description: |
                        One of a Firewall's inbound or outbound access rules. The `ports` property can
                        be used to allow traffic on a comma-separated list of different
                        ports.
                      x-akamai:
                        file-path: schemas/firewall-rule-config.yaml
                      additionalProperties: false
                      properties:
                        description:
                          type: string
                          description: |
                            Used to describe this rule. For display purposes only.
                          example: An example firewall rule description.
                          minLength: 1
                          maxLength: 100
                        action:
                          type: string
                          description: |
                            Controls whether traffic is accepted or dropped by this rule. Overrides
                            the Firewall's `inbound_policy` if this is an inbound
                            rule, or the `outbound_policy` if this is an outbound
                            rule.
                          example: ACCEPT
                          enum:
                          - ACCEPT
                          - DROP
                        addresses:
                          type: object
                          description: |-
                            The IPv4 and/or IPv6 addresses affected by this rule. A Rule can have up to 255 total addresses or networks listed across its IPv4 and IPv6 arrays. A network and a single IP are treated as equivalent when accounting for this limit.

                            Must contain `ipv4`, `ipv6`, or both.
                          additionalProperties: false
                          properties:
                            ipv4:
                              type: array
                              description: |-
                                A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.

                                If "0.0.0.0/0" is included in this list, all IPv4 addresses are affected by this rule.
                              example:
                              - 192.0.2.0/24
                              - 198.51.100.2/32
                              items:
                                type: string
                            ipv6:
                              type: array
                              description: |-
                                A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.

                                If "::/0" is included in this list, all IPv6 addresses are affected by this rule.
                              example:
                              - 2001:DB8::/128
                              items:
                                type: string
                        label:
                          type: string
                          description: |
                            Used to identify this rule. For display purposes only.
                          example: firewallrule123
                          minLength: 3
                          maxLength: 32
                        ports:
                          type: string
                          description: |-
                            A string representing the port or ports affected by this rule:

                            - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                            - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                            - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port "080" is not allowed.
                            - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                            - If no ports are configured, all ports are affected.
                            - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                          example: 22-24, 80, 443
                          nullable: true
                        protocol:
                          type: string
                          description: |
                            The type of network traffic affected by this rule.
                          example: TCP
                          enum:
                          - TCP
                          - UDP
                          - ICMP
                          - IPENCAP
                    x-linode-cli-format: json
                  inbound_policy:
                    type: string
                    description: |
                      The default behavior for inbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules)
                      the `inbound.action` property of the Firewall Rule.
                    example: DROP
                    enum:
                    - ACCEPT
                    - DROP
                  outbound:
                    type: array
                    description: |
                      The outbound rules for the firewall, as a JSON array.
                    items:
                      type: object
                      description: |
                        One of a Firewall's inbound or outbound access rules. The `ports` property can
                        be used to allow traffic on a comma-separated list of different
                        ports.
                      x-akamai:
                        file-path: schemas/firewall-rule-config.yaml
                      additionalProperties: false
                      properties:
                        description:
                          type: string
                          description: |
                            Used to describe this rule. For display purposes only.
                          example: An example firewall rule description.
                          minLength: 1
                          maxLength: 100
                        action:
                          type: string
                          description: |
                            Controls whether traffic is accepted or dropped by this rule. Overrides
                            the Firewall's `inbound_policy` if this is an inbound
                            rule, or the `outbound_policy` if this is an outbound
                            rule.
                          example: ACCEPT
                          enum:
                          - ACCEPT
                          - DROP
                        addresses:
                          type: object
                          description: |-
                            The IPv4 and/or IPv6 addresses affected by this rule. A Rule can have up to 255 total addresses or networks listed across its IPv4 and IPv6 arrays. A network and a single IP are treated as equivalent when accounting for this limit.

                            Must contain `ipv4`, `ipv6`, or both.
                          additionalProperties: false
                          properties:
                            ipv4:
                              type: array
                              description: |-
                                A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.

                                If "0.0.0.0/0" is included in this list, all IPv4 addresses are affected by this rule.
                              example:
                              - 192.0.2.0/24
                              - 198.51.100.2/32
                              items:
                                type: string
                            ipv6:
                              type: array
                              description: |-
                                A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.

                                If "::/0" is included in this list, all IPv6 addresses are affected by this rule.
                              example:
                              - 2001:DB8::/128
                              items:
                                type: string
                        label:
                          type: string
                          description: |
                            Used to identify this rule. For display purposes only.
                          example: firewallrule123
                          minLength: 3
                          maxLength: 32
                        ports:
                          type: string
                          description: |-
                            A string representing the port or ports affected by this rule:

                            - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                            - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                            - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port "080" is not allowed.
                            - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                            - If no ports are configured, all ports are affected.
                            - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                          example: 22-24, 80, 443
                          nullable: true
                        protocol:
                          type: string
                          description: |
                            The type of network traffic affected by this rule.
                          example: TCP
                          enum:
                          - TCP
                          - UDP
                          - ICMP
                          - IPENCAP
                    x-linode-cli-format: json
                  outbound_policy:
                    type: string
                    description: |
                      The default behavior for outbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules)
                      the `outbound.action` property of the Firewall Rule.
                    example: DROP
                    enum:
                    - ACCEPT
                    - DROP
              x-example:
                x-ref: ../examples/tbd.json
              x-linode-cli-subtables:
              - inbound
              - outbound
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-firewall-rules
      security:
      - personalAccessToken: []
      - oauth:
        - firewall:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/networking/firewalls/123/rules
      - lang: CLI
        source: linode-cli firewalls rules-list 123
      x-linode-cli-action: rules-list
      x-linode-grant: read_only
      x-original-op-id: getFirewallRules
      x-original-op-title: Firewall Rules List
    put:
      servers:
      - url: https://api.linode.com/v4
      operationId: put-firewall-rules
      summary: Update firewall rules
      tags:
      - Firewalls
      description: |-
        Updates the inbound and outbound Rules for a Firewall.

        - Assigned Linodes must not have any ongoing live migrations.

        - __Note__. This operation replaces all of a Firewall's `inbound` and `outbound` rulesets with the values specified in your request.


        ---


        - __CLI__.

            ```
            linode-cli firewalls rules-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            firewall:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli firewalls rules-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: firewall:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The Firewall Rules information to update.
        content:
          application/json:
            schema:
              allOf:
              - type: object
                description: |-
                  The inbound and outbound access rules to apply to the Firewall.

                  A Firewall may have up to 25 rules across its inbound and outbound rulesets.

                  Multiple rules are applied in order. If two rules conflict, the first rule takes precedence. For example, if the first rule accepts inbound traffic from an address, and the second rule drops inbound traffic the same address, the first rule applies and inbound traffic from that address is accepted.
                additionalProperties: false
                properties:
                  inbound:
                    type: array
                    description: |
                      The inbound rules for the firewall, as a JSON array.
                    items:
                      type: object
                      description: |
                        One of a Firewall's inbound or outbound access rules. The `ports` property can
                        be used to allow traffic on a comma-separated list of different
                        ports.
                      x-akamai:
                        file-path: schemas/firewall-rule-config.yaml
                      additionalProperties: false
                      properties:
                        description:
                          type: string
                          description: |
                            Used to describe this rule. For display purposes only.
                          example: An example firewall rule description.
                          minLength: 1
                          maxLength: 100
                        action:
                          type: string
                          description: |
                            Controls whether traffic is accepted or dropped by this rule. Overrides
                            the Firewall's `inbound_policy` if this is an inbound
                            rule, or the `outbound_policy` if this is an outbound
                            rule.
                          example: ACCEPT
                          enum:
                          - ACCEPT
                          - DROP
                        addresses:
                          type: object
                          description: |-
                            The IPv4 and/or IPv6 addresses affected by this rule. A Rule can have up to 255 total addresses or networks listed across its IPv4 and IPv6 arrays. A network and a single IP are treated as equivalent when accounting for this limit.

                            Must contain `ipv4`, `ipv6`, or both.
                          additionalProperties: false
                          properties:
                            ipv4:
                              type: array
                              description: |-
                                A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.

                                If "0.0.0.0/0" is included in this list, all IPv4 addresses are affected by this rule.
                              example:
                              - 192.0.2.0/24
                              - 198.51.100.2/32
                              items:
                                type: string
                            ipv6:
                              type: array
                              description: |-
                                A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.

                                If "::/0" is included in this list, all IPv6 addresses are affected by this rule.
                              example:
                              - 2001:DB8::/128
                              items:
                                type: string
                        label:
                          type: string
                          description: |
                            Used to identify this rule. For display purposes only.
                          example: firewallrule123
                          minLength: 3
                          maxLength: 32
                        ports:
                          type: string
                          description: |-
                            A string representing the port or ports affected by this rule:

                            - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                            - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                            - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port "080" is not allowed.
                            - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                            - If no ports are configured, all ports are affected.
                            - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                          example: 22-24, 80, 443
                          nullable: true
                        protocol:
                          type: string
                          description: |
                            The type of network traffic affected by this rule.
                          example: TCP
                          enum:
                          - TCP
                          - UDP
                          - ICMP
                          - IPENCAP
                    x-linode-cli-format: json
                  inbound_policy:
                    type: string
                    description: |
                      The default behavior for inbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules)
                      the `inbound.action` property of the Firewall Rule.
                    example: DROP
                    enum:
                    - ACCEPT
                    - DROP
                  outbound:
                    type: array
                    description: |
                      The outbound rules for the firewall, as a JSON array.
                    items:
                      type: object
                      description: |
                        One of a Firewall's inbound or outbound access rules. The `ports` property can
                        be used to allow traffic on a comma-separated list of different
                        ports.
                      x-akamai:
                        file-path: schemas/firewall-rule-config.yaml
                      additionalProperties: false
                      properties:
                        description:
                          type: string
                          description: |
                            Used to describe this rule. For display purposes only.
                          example: An example firewall rule description.
                          minLength: 1
                          maxLength: 100
                        action:
                          type: string
                          description: |
                            Controls whether traffic is accepted or dropped by this rule. Overrides
                            the Firewall's `inbound_policy` if this is an inbound
                            rule, or the `outbound_policy` if this is an outbound
                            rule.
                          example: ACCEPT
                          enum:
                          - ACCEPT
                          - DROP
                        addresses:
                          type: object
                          description: |-
                            The IPv4 and/or IPv6 addresses affected by this rule. A Rule can have up to 255 total addresses or networks listed across its IPv4 and IPv6 arrays. A network and a single IP are treated as equivalent when accounting for this limit.

                            Must contain `ipv4`, `ipv6`, or both.
                          additionalProperties: false
                          properties:
                            ipv4:
                              type: array
                              description: |-
                                A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.

                                If "0.0.0.0/0" is included in this list, all IPv4 addresses are affected by this rule.
                              example:
                              - 192.0.2.0/24
                              - 198.51.100.2/32
                              items:
                                type: string
                            ipv6:
                              type: array
                              description: |-
                                A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.

                                If "::/0" is included in this list, all IPv6 addresses are affected by this rule.
                              example:
                              - 2001:DB8::/128
                              items:
                                type: string
                        label:
                          type: string
                          description: |
                            Used to identify this rule. For display purposes only.
                          example: firewallrule123
                          minLength: 3
                          maxLength: 32
                        ports:
                          type: string
                          description: |-
                            A string representing the port or ports affected by this rule:

                            - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                            - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                            - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port "080" is not allowed.
                            - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                            - If no ports are configured, all ports are affected.
                            - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                          example: 22-24, 80, 443
                          nullable: true
                        protocol:
                          type: string
                          description: |
                            The type of network traffic affected by this rule.
                          example: TCP
                          enum:
                          - TCP
                          - UDP
                          - ICMP
                          - IPENCAP
                    x-linode-cli-format: json
                  outbound_policy:
                    type: string
                    description: |
                      The default behavior for outbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules)
                      the `outbound.action` property of the Firewall Rule.
                    example: DROP
                    enum:
                    - ACCEPT
                    - DROP
              properties:
                inbound:
                  required:
                  - action
                  - addresses
                  - protocol
                outbound:
                  required:
                  - action
                  - addresses
                  - protocol
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
            x-linode-cli-subtables:
            - inbound
            - outbound
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Firewall Rules updated successfully.
          content:
            application/json:
              schema:
                type: object
                description: |-
                  The inbound and outbound access rules to apply to the Firewall.

                  A Firewall may have up to 25 rules across its inbound and outbound rulesets.

                  Multiple rules are applied in order. If two rules conflict, the first rule takes precedence. For example, if the first rule accepts inbound traffic from an address, and the second rule drops inbound traffic the same address, the first rule applies and inbound traffic from that address is accepted.
                additionalProperties: false
                properties:
                  inbound:
                    type: array
                    description: |
                      The inbound rules for the firewall, as a JSON array.
                    items:
                      type: object
                      description: |
                        One of a Firewall's inbound or outbound access rules. The `ports` property can
                        be used to allow traffic on a comma-separated list of different
                        ports.
                      x-akamai:
                        file-path: schemas/firewall-rule-config.yaml
                      additionalProperties: false
                      properties:
                        description:
                          type: string
                          description: |
                            Used to describe this rule. For display purposes only.
                          example: An example firewall rule description.
                          minLength: 1
                          maxLength: 100
                        action:
                          type: string
                          description: |
                            Controls whether traffic is accepted or dropped by this rule. Overrides
                            the Firewall's `inbound_policy` if this is an inbound
                            rule, or the `outbound_policy` if this is an outbound
                            rule.
                          example: ACCEPT
                          enum:
                          - ACCEPT
                          - DROP
                        addresses:
                          type: object
                          description: |-
                            The IPv4 and/or IPv6 addresses affected by this rule. A Rule can have up to 255 total addresses or networks listed across its IPv4 and IPv6 arrays. A network and a single IP are treated as equivalent when accounting for this limit.

                            Must contain `ipv4`, `ipv6`, or both.
                          additionalProperties: false
                          properties:
                            ipv4:
                              type: array
                              description: |-
                                A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.

                                If "0.0.0.0/0" is included in this list, all IPv4 addresses are affected by this rule.
                              example:
                              - 192.0.2.0/24
                              - 198.51.100.2/32
                              items:
                                type: string
                            ipv6:
                              type: array
                              description: |-
                                A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.

                                If "::/0" is included in this list, all IPv6 addresses are affected by this rule.
                              example:
                              - 2001:DB8::/128
                              items:
                                type: string
                        label:
                          type: string
                          description: |
                            Used to identify this rule. For display purposes only.
                          example: firewallrule123
                          minLength: 3
                          maxLength: 32
                        ports:
                          type: string
                          description: |-
                            A string representing the port or ports affected by this rule:

                            - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                            - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                            - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port "080" is not allowed.
                            - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                            - If no ports are configured, all ports are affected.
                            - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                          example: 22-24, 80, 443
                          nullable: true
                        protocol:
                          type: string
                          description: |
                            The type of network traffic affected by this rule.
                          example: TCP
                          enum:
                          - TCP
                          - UDP
                          - ICMP
                          - IPENCAP
                    x-linode-cli-format: json
                  inbound_policy:
                    type: string
                    description: |
                      The default behavior for inbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules)
                      the `inbound.action` property of the Firewall Rule.
                    example: DROP
                    enum:
                    - ACCEPT
                    - DROP
                  outbound:
                    type: array
                    description: |
                      The outbound rules for the firewall, as a JSON array.
                    items:
                      type: object
                      description: |
                        One of a Firewall's inbound or outbound access rules. The `ports` property can
                        be used to allow traffic on a comma-separated list of different
                        ports.
                      x-akamai:
                        file-path: schemas/firewall-rule-config.yaml
                      additionalProperties: false
                      properties:
                        description:
                          type: string
                          description: |
                            Used to describe this rule. For display purposes only.
                          example: An example firewall rule description.
                          minLength: 1
                          maxLength: 100
                        action:
                          type: string
                          description: |
                            Controls whether traffic is accepted or dropped by this rule. Overrides
                            the Firewall's `inbound_policy` if this is an inbound
                            rule, or the `outbound_policy` if this is an outbound
                            rule.
                          example: ACCEPT
                          enum:
                          - ACCEPT
                          - DROP
                        addresses:
                          type: object
                          description: |-
                            The IPv4 and/or IPv6 addresses affected by this rule. A Rule can have up to 255 total addresses or networks listed across its IPv4 and IPv6 arrays. A network and a single IP are treated as equivalent when accounting for this limit.

                            Must contain `ipv4`, `ipv6`, or both.
                          additionalProperties: false
                          properties:
                            ipv4:
                              type: array
                              description: |-
                                A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.

                                If "0.0.0.0/0" is included in this list, all IPv4 addresses are affected by this rule.
                              example:
                              - 192.0.2.0/24
                              - 198.51.100.2/32
                              items:
                                type: string
                            ipv6:
                              type: array
                              description: |-
                                A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.

                                If "::/0" is included in this list, all IPv6 addresses are affected by this rule.
                              example:
                              - 2001:DB8::/128
                              items:
                                type: string
                        label:
                          type: string
                          description: |
                            Used to identify this rule. For display purposes only.
                          example: firewallrule123
                          minLength: 3
                          maxLength: 32
                        ports:
                          type: string
                          description: |-
                            A string representing the port or ports affected by this rule:

                            - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                            - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                            - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port "080" is not allowed.
                            - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                            - If no ports are configured, all ports are affected.
                            - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                          example: 22-24, 80, 443
                          nullable: true
                        protocol:
                          type: string
                          description: |
                            The type of network traffic affected by this rule.
                          example: TCP
                          enum:
                          - TCP
                          - UDP
                          - ICMP
                          - IPENCAP
                    x-linode-cli-format: json
                  outbound_policy:
                    type: string
                    description: |
                      The default behavior for outbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules)
                      the `outbound.action` property of the Firewall Rule.
                    example: DROP
                    enum:
                    - ACCEPT
                    - DROP
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-firewall-rules
      security:
      - personalAccessToken: []
      - oauth:
        - firewall:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                  "inbound_policy": "DROP",
                  "inbound": [
                    {
                      "protocol": "TCP",
                      "ports": "22, 80, 443",
                      "addresses": {
                        "ipv4": [
                          "192.0.2.0/24",
                          "198.51.100.2/32"
                        ],
                        "ipv6": [
                          "2001:DB8::/128"
                        ]
                      },
                    "action": "ACCEPT",
                    "label": "inbound-rule123",
                    "description": "An example inbound rule description."
                    }
                  ],
                  "outbound_policy": "DROP",
                  "outbound": [
                    {
                      "protocol": "TCP",
                      "ports": "49152-65535",
                      "addresses": {
                        "ipv4": [
                          "192.0.2.0/24",
                          "198.51.100.2/32"
                        ],
                        "ipv6": [
                          "2001:DB8::/128"
                        ]
                      },
                      "action": "ACCEPT",
                      "label": "outbound-rule123",
                      "description": "An example outbound rule description."
                    }
                  ]
              }' \
              https://api.linode.com/v4/networking/firewalls/123/rules
      - lang: CLI
        source: |-
          linode-cli firewalls rules-update 123 \
            --inbound '[{"action":"ACCEPT", "protocol": "TCP", "ports": "22, 80, 8080, 443", "addresses": {"ipv4": ["192.0.2.0/24", "198.51.100.2/32"], "ipv6": ["2001:DB8::/128"]}}]' \
            --outbound '[{"action":"DROP","protocol": "TCP", "ports": "49152-65535", "addresses": {"ipv4": ["192.0.2.0/24", "198.51.100.2/32"], "ipv6": ["2001:DB8::/128`"]}}]'
      x-linode-cli-action: rules-update
      x-linode-grant: read_write
      x-original-op-id: updateFirewallRules
      x-original-op-title: Firewall Rules Update
    x-linode-cli-command: firewalls
  /networking/ips:
    x-akamai:
      file-path: paths/networking-ips.yaml
      path-info: /networking/ips
    post:
      operationId: post-allocate-ip
      summary: Allocate an IP address
      tags:
      - IP addresses
      description: |-
        Allocates a new IPv4 Address on your Account. The Linode must be configured to support additional addresses - please [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) requesting additional addresses before attempting allocation.


        ---


        - __CLI__.

            ```
            linode-cli networking ip-add
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            ips:read_write
        linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli networking ip-add
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: |-
            ips:read_write
            linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the address you are creating.
        required: true
        content:
          application/json:
            schema:
              additionalProperties: false
              required:
              - type
              - public
              - linode_id
              properties:
                type:
                  type: string
                  description: |
                    The type of address you are requesting. Only IPv4 addresses may be allocated through this
                    operation.
                  example: ipv4
                  enum:
                  - ipv4
                linode_id:
                  type: integer
                  description: |
                    The ID of a Linode you you have access to that this address will be allocated to.
                  example: 123
                public:
                  type: boolean
                  description: |
                    Whether to create a public or private IPv4 address.
                  example: true
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            IP Address allocated successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An IP address that exists in Linode's system, either IPv4 or IPv6.
                x-akamai:
                  file-path: schemas/ip-address.yaml
                additionalProperties: false
                properties:
                  type:
                    type: string
                    description: |
                      __Read-only__ The type of address this is.
                    example: ipv4
                    readOnly: true
                    enum:
                    - ipv4
                    - ipv6
                    - ipv6/pool
                    - ipv6/range
                    x-linode-cli-display: 2
                  address:
                    type: string
                    description: |
                      __Read-only__ The IP address.
                    example: 97.107.143.141
                    readOnly: true
                    format: ip
                    x-linode-cli-display: 1
                  gateway:
                    type: string
                    description: |
                      __Read-only__ The default gateway for this address.
                    example: 97.107.143.1
                    nullable: true
                    readOnly: true
                    format: ip
                  linode_id:
                    type: integer
                    description: |
                      __Read-only__ The ID of the Linode this address currently belongs to. For IPv4 addresses,
                      this is by default the Linode that this address was assigned
                      to on creation, and these addresses my be moved using the [Assign
                      IPv4s to Linodes](https://techdocs.akamai.com/linode-api/reference/post-assign-ipv4s)
                      operation. For SLAAC and link-local addresses, this value may
                      not be changed.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 6
                  prefix:
                    type: integer
                    description: |
                      __Read-only__ The number of bits set in the subnet mask.
                    example: 24
                    readOnly: true
                  public:
                    type: boolean
                    description: |
                      __Read-only__ Whether this is a public or private IP address.
                    example: true
                    readOnly: true
                    x-linode-cli-display: 3
                  rdns:
                    type: string
                    description: |
                      The reverse DNS assigned to this address. For public IPv4 addresses, this will be set
                      to a default value provided by Linode if not explicitly set.
                    example: test.example.org
                    nullable: true
                    x-linode-cli-display: 4
                  region:
                    type: string
                    description: |
                      __Read-only__ The Region this IP address resides in.
                    example: us-east
                    readOnly: true
                    x-linode-cli-display: 5
                  subnet_mask:
                    type: string
                    description: |
                      __Read-only__ The mask that separates host bits from network bits for this address.
                    example: 255.255.255.0
                    readOnly: true
                    format: ip
                  vpc_nat_1_1:
                    type: object
                    description: |-
                      IPv4 address configured as a 1:1 NAT for this Interface. If no address is configured as a 1:1 NAT, `null` is returned.

                      __Note:__ Only allowed for `vpc` type Interfaces.
                    additionalProperties: false
                    properties:
                      address:
                        type: string
                        description: |
                          The IPv4 address that is configured as a 1:1 NAT for this VPC interface.
                        example: 192.168.0.42
                        format: ipv4
                      subnet_id:
                        type: integer
                        description: |
                          The `id` of the VPC Subnet for this Interface.
                        example: 101
                        nullable: false
                      vpc_id:
                        type: integer
                        description: |
                          __Read-only__ The `id` of the VPC configured for this Interface.
                        example: 111
                        nullable: false
                        readOnly: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-allocate-ip
      security:
      - personalAccessToken: []
      - oauth:
        - ips:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "type": "ipv4",
                "public": true,
                "linode_id": 123
              }' \
              https://api.linode.com/v4/networking/ips
      - lang: CLI
        source: |-
          linode-cli networking ip-add \
            --type ipv4 \
            --public true \
            --linode_id 123
      x-linode-cli-action: ip-add
      x-linode-grant: read_write
      x-original-op-id: allocateIP
      x-original-op-title: IP Address Allocate
    get:
      operationId: get-ips
      summary: List IP addresses
      tags:
      - IP addresses
      description: |-
        Returns a paginated list of IP addresses on your account, excluding private addresses.

        __Note__. Use the `skip_ipv6_rdns` query string to improve performance if your application frequently accesses this operation and doesn't require IPv6 RDNS data.


        ---


        - __CLI__.

            ```
            linode-cli networking ips-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            ips:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli networking ips-list
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: ips:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: skip_ipv6_rdns
        in: query
        description: |
          When `true`, the `rdns` property for any `ipv6` type addresses always returns `null` regardless of whether
          RDNS data exists for the address.
        example: '{{skip_ipv6_rdns}}'
        x-akamai:
          file-path: parameters/skip-ipv6-rdns-query.yaml
        schema:
          type: boolean
          default: false
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of IP Addresses.
          content:
            application/json:
              schema:
                description: |
                  The response data for the [List IP addresses](https://techdocs.akamai.com/linode-api/reference/get-ips)
                  operation.
                x-akamai:
                  file-path: schemas/ip-addresses-list-response.yaml
                allOf:
                - type: object
                  description: |
                    An envelope for paginated response. When accessing a collection through a GET endpoint,
                    the results are wrapped in this envelope which includes metadata
                    about those results. Results are presented within a `data` array.
                    See [Pagination](https://techdocs.akamai.com/linode-api/reference/pagination)
                    for more information.
                  x-akamai:
                    file-path: schemas/pagination-envelope.yaml
                  additionalProperties: false
                  properties:
                    page:
                      type: integer
                      description: |
                        __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    pages:
                      type: integer
                      description: |
                        __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                      example: 1
                      readOnly: true
                    results:
                      type: integer
                      description: |
                        __Read-only__ The total number of results.
                      example: 1
                      readOnly: true
                - type: object
                  properties:
                    data:
                      type: array
                      description: |
                        IP addresses that exist in Linode's system, either IPv4 or IPv6, specific to
                        the response for the [List IP addresses](https://techdocs.akamai.com/linode-api/reference/get-ips)
                        operation.
                      items:
                        type: object
                        description: |
                          An IP address that exists in Linode's system, either IPv4 or IPv6, specific
                          to the response for the [List IP addresses](https://techdocs.akamai.com/linode-api/reference/get-ips)
                          operation.
                        additionalProperties: false
                        properties:
                          type:
                            type: string
                            description: |
                              __Read-only__ The type of address this is.
                            example: ipv4
                            readOnly: true
                            enum:
                            - ipv4
                            - ipv6
                            - ipv6/pool
                            - ipv6/range
                            x-linode-cli-display: 2
                            x-linode-filterable: true
                          address:
                            type: string
                            description: |
                              __Read-only__ The IP address.
                            example: 192.0.2.141
                            readOnly: true
                            format: ip
                            x-linode-cli-display: 1
                            x-linode-filterable: true
                          gateway:
                            type: string
                            description: |
                              __Read-only__ The default gateway for this address.
                            example: 192.0.2.1
                            nullable: true
                            readOnly: true
                            format: ip
                          linode_id:
                            type: integer
                            description: |
                              __Read-only__ The ID of the Linode this address currently belongs
                              to. For IPv4 addresses, this defaults to the Linode
                              that this address was assigned to on creation. IPv4
                              addresses may be moved using the [Assign IPv4s to Linodes](https://techdocs.akamai.com/linode-api/reference/post-assign-ipv4s)
                              operation. For SLAAC and link-local addresses, this
                              value may not be changed.
                            example: 123
                            readOnly: true
                            x-linode-cli-display: 6
                          prefix:
                            type: integer
                            description: |
                              __Read-only__ The number of bits set in the subnet mask.
                            example: 24
                            readOnly: true
                            x-linode-filterable: true
                          public:
                            type: boolean
                            description: |
                              __Read-only__ Whether this is a public or private IP address.
                            example: true
                            readOnly: true
                            x-linode-cli-display: 3
                          rdns:
                            type: string
                            description: |
                              The reverse DNS assigned to this address. For public IPv4 addresses,
                              this will be set to a default value provided by Linode
                              if not explicitly set.
                            example: test.example.org
                            nullable: true
                            x-linode-cli-display: 4
                            x-linode-filterable: true
                          region:
                            type: string
                            description: |
                              __Read-only__ The Region this IP address resides in.
                            example: us-east
                            readOnly: true
                            x-linode-cli-display: 5
                            x-linode-filterable: true
                          subnet_mask:
                            type: string
                            description: |
                              __Read-only__ The mask that separates host bits from network bits
                              for this address.
                            example: 255.255.255.0
                            readOnly: true
                            format: ip
                          vpc_nat_1_1:
                            type: object
                            description: |-
                              IPv4 address configured as a 1:1 NAT for this Interface. Empty if no address is configured as a 1:1 NAT.

                              __Note:__ Only allowed for `vpc` type Interfaces.
                            additionalProperties: false
                            properties:
                              address:
                                type: string
                                description: |
                                  The IPv4 address that is configured as a 1:1 NAT for this
                                  VPC interface.
                                example: 192.168.0.42
                                format: ipv4
                              subnet_id:
                                type: integer
                                description: |
                                  The `id` of the VPC Subnet for this Interface.
                                example: 101
                                nullable: false
                              vpc_id:
                                type: integer
                                description: |
                                  __Read-only__ The `id` of the VPC configured for this Interface.
                                example: 111
                                nullable: false
                                readOnly: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-ips
      security:
      - personalAccessToken: []
      - oauth:
        - ips:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/networking/ips?skip_ipv6_rdns=true
      - lang: CLI
        source: linode-cli networking ips-list
      x-linode-cli-action: ips-list
      x-linode-grant: read_only
      x-original-op-id: getIPs
      x-original-op-title: IP Addresses List
    x-linode-cli-command: networking
  /networking/ips/assign:
    x-akamai:
      file-path: paths/ips-assign.yaml
      path-info: /networking/ips/assign
    post:
      operationId: post-assign-ips
      summary: Assign IP addresses
      tags:
      - IP addresses
      description: |-
        Assign multiple IPv4 addresses and/or IPv6 ranges to multiple Linodes in one Region. This allows swapping, shuffling, or otherwise reorganizing IPs to your Linodes.

        The following restrictions apply:

        - All Linodes involved must have at least one public IPv4 address after assignment.
        - Linodes may have no more than one assigned private IPv4 address.
        - Linodes may have no more than one assigned IPv6 range.
        - Shared IP addresses cannot be swapped between Linodes.

        [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) to request additional IPv4 addresses or IPv6 ranges beyond standard account limits.

        __Note__. Removing an IP address that has been set as a Managed Linode's `ssh.ip` causes the Managed Linode's SSH access settings to reset to their default values.

        To view and configure Managed Linode SSH settings, use the following operations:

        - [Get a Linode's managed settings](https://techdocs.akamai.com/linode-api/reference/get-managed-linode-setting)
        - [Update a Linode's managed settings](https://techdocs.akamai.com/linode-api/reference/put-managed-linode-setting)

        __Note__. Addresses with an active 1:1 NAT to a VPC Interface address cannot be assigned to other Linodes.


        ---


        - __CLI__.

            ```
            linode-cli networking ip-assign
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            ips:read_write
        linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli networking ip-assign
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: |-
            ips:read_write
            linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about what IPv4 address or IPv6 range to assign, and to which Linode.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                Request object for IP Addresses Assign (POST /networking/ips/assign).
              x-akamai:
                file-path: schemas/ip-addresses-assign-request.yaml
              additionalProperties: false
              required:
              - region
              - assignments
              properties:
                assignments:
                  type: array
                  description: |
                    The list of assignments to make. You must have read_write access to all IPs being assigned
                    and all Linodes being assigned to in order for the assignments
                    to succeed.
                  required:
                  - address
                  - linode_id
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      address:
                        type: string
                        description: |-
                          The IPv4 address or IPv6 range for this assignment.

                          - Must be an IPv4 address or an IPv6 range you can access in the Region specified.
                          - IPv6 ranges must include a prefix length of `/56` or `/64`, for example: `2001:db8:3c4d:15::/64`.
                          - Assignment of an IPv6 range to a Linode updates the route target of the range to the assigned Linode's SLAAC address.
                          - May be a public or private address.
                        example: 192.0.2.1
                        format: ipv4|ipv6/prefix_length
                      linode_id:
                        type: integer
                        description: |
                          The ID of the Linode to assign this address to. The IP's previous Linode will
                          lose this address, and must end up with at least one public
                          address and no more than one private address once all assignments
                          have been made.
                        example: 123
                region:
                  type: string
                  description: |
                    The ID of the Region in which these assignments are to take place. All IPs and Linodes
                    must exist in this Region.
                  example: '{{region}}'
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            All assignments completed successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-assign-ips
      security:
      - personalAccessToken: []
      - oauth:
        - ips:read_write
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "region": "us-east",
                "assignments": [
                  {
                    "address": "192.0.2.1",
                    "linode_id": 123
                  },
                  {
                    "address": "2001:db8:3c4d:15::/64",
                    "linode_id": 234
                  }
                ]
              }' \
              https://api.linode.com/v4/networking/ips/assign
      - lang: CLI
        source: |-
          linode-cli networking ip-assign \
            --region us-east \
            --assignments.address 192.0.2.1 \
            --assignments.linode_id 123 \
            --assignments.address 2001:db8:3c4d:15::/64 \
            --assignments.linode_id 234
      x-linode-cli-action: ip-assign
      x-linode-grant: read_write
      x-original-op-id: assignIPs
      x-original-op-title: IP Addresses Assign
    x-linode-cli-command: networking
  /networking/ips/share:
    x-akamai:
      file-path: paths/ips-share.yaml
      path-info: /networking/ips/share
    post:
      servers:
      - url: https://api.linode.com/v4
      operationId: post-share-ips
      summary: Share IP addresses
      tags:
      - IP addresses
      description: |-
        Configure shared IPs.

        IP sharing allows IP address reassignment (also referred to as IP failover) from one Linode to another if the primary Linode becomes unresponsive. This means that requests to the primary Linode's IP address can be automatically rerouted to secondary Linodes at the configured shared IP addresses.

        IP failover requires configuration of a failover service (such as [Keepalived](https://www.linode.com/docs/guides/ip-failover-keepalived)) within the internal system of the primary Linode.

        __Note__. A public IPv4 address cannot be shared if it is configured for a 1:1 NAT on a `vpc` purpose Configuration Profile Interface.


        ---


        - __CLI__.

            ```
            linode-cli networking ip-share
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            ips:read_write
        linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli networking ip-share
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: |-
            ips:read_write
            linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about what IPs to share with which Linode.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                A request object IP Addresses Share (POST /networking/ips/share)
              x-akamai:
                file-path: schemas/ip-addresses-share-request.yaml
              additionalProperties: false
              required:
              - linode_id
              - ips
              properties:
                ips:
                  type: array
                  description: |-
                    A list of secondary Linode IPs to share with the primary Linode.

                    - Can include both IPv4 addresses and IPv6 ranges (omit /56 and /64 prefix lengths)
                    - Can include both private and public IPv4 addresses.
                    - You must have access to all of these addresses and they must be in the same Region as the primary Linode.
                    - Enter an empty array to remove all shared IP addresses.
                  example:
                  - 192.0.2.1
                  - '2001:db8:3c4d:15::'
                  items:
                    type: string
                    format: ip
                linode_id:
                  type: integer
                  description: |
                    The ID of the primary Linode that the addresses will be shared with.
                  example: '{{linode_id}}'
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            IP Address sharing successful.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-share-ips
      security:
      - personalAccessToken: []
      - oauth:
        - ips:read_write
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "linode_id": 123,
                "ips": [
                  "192.0.2.1",
                  "2001:db8:3c4d:15::"
                ]
              }' \
              https://api.linode.com/v4beta/networking/ips/share
      - lang: CLI
        source: |-
          linode-cli networking ip-share \
            --linode_id 123 \
            --ips 192.0.2.1 \
            --ips 2001:db8:3c4d:15::
      x-linode-cli-action: ip-share
      x-linode-grant: read_write
      x-original-op-id: shareIPs
      x-original-op-title: IP Addresses Share
    x-linode-cli-command: networking
  /networking/ips/{address}:
    x-akamai:
      file-path: paths/networking-ip-address.yaml
      path-info: /networking/ips/{address}
    parameters:
    - name: address
      in: path
      description: |
        The address to operate on.
      example: '{{address}}'
      x-akamai:
        file-path: parameters/address-path-e209a4cb.yaml
      required: true
      schema:
        type: string
        format: ip
    get:
      operationId: get-ip
      summary: Get an IP address
      tags:
      - IP addresses
      description: |-
        Returns information about a single IP Address on your Account.


        ---


        - __CLI__.

            ```
            linode-cli networking ip-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            ips:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli networking ip-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: ips:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The requested IP Address.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An IP address that exists in Linode's system, either IPv4 or IPv6.
                x-akamai:
                  file-path: schemas/ip-address.yaml
                additionalProperties: false
                properties:
                  type:
                    type: string
                    description: |
                      __Read-only__ The type of address this is.
                    example: ipv4
                    readOnly: true
                    enum:
                    - ipv4
                    - ipv6
                    - ipv6/pool
                    - ipv6/range
                    x-linode-cli-display: 2
                  address:
                    type: string
                    description: |
                      __Read-only__ The IP address.
                    example: 97.107.143.141
                    readOnly: true
                    format: ip
                    x-linode-cli-display: 1
                  gateway:
                    type: string
                    description: |
                      __Read-only__ The default gateway for this address.
                    example: 97.107.143.1
                    nullable: true
                    readOnly: true
                    format: ip
                  linode_id:
                    type: integer
                    description: |
                      __Read-only__ The ID of the Linode this address currently belongs to. For IPv4 addresses,
                      this is by default the Linode that this address was assigned
                      to on creation, and these addresses my be moved using the [Assign
                      IPv4s to Linodes](https://techdocs.akamai.com/linode-api/reference/post-assign-ipv4s)
                      operation. For SLAAC and link-local addresses, this value may
                      not be changed.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 6
                  prefix:
                    type: integer
                    description: |
                      __Read-only__ The number of bits set in the subnet mask.
                    example: 24
                    readOnly: true
                  public:
                    type: boolean
                    description: |
                      __Read-only__ Whether this is a public or private IP address.
                    example: true
                    readOnly: true
                    x-linode-cli-display: 3
                  rdns:
                    type: string
                    description: |
                      The reverse DNS assigned to this address. For public IPv4 addresses, this will be set
                      to a default value provided by Linode if not explicitly set.
                    example: test.example.org
                    nullable: true
                    x-linode-cli-display: 4
                  region:
                    type: string
                    description: |
                      __Read-only__ The Region this IP address resides in.
                    example: us-east
                    readOnly: true
                    x-linode-cli-display: 5
                  subnet_mask:
                    type: string
                    description: |
                      __Read-only__ The mask that separates host bits from network bits for this address.
                    example: 255.255.255.0
                    readOnly: true
                    format: ip
                  vpc_nat_1_1:
                    type: object
                    description: |-
                      IPv4 address configured as a 1:1 NAT for this Interface. If no address is configured as a 1:1 NAT, `null` is returned.

                      __Note:__ Only allowed for `vpc` type Interfaces.
                    additionalProperties: false
                    properties:
                      address:
                        type: string
                        description: |
                          The IPv4 address that is configured as a 1:1 NAT for this VPC interface.
                        example: 192.168.0.42
                        format: ipv4
                      subnet_id:
                        type: integer
                        description: |
                          The `id` of the VPC Subnet for this Interface.
                        example: 101
                        nullable: false
                      vpc_id:
                        type: integer
                        description: |
                          __Read-only__ The `id` of the VPC configured for this Interface.
                        example: 111
                        nullable: false
                        readOnly: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-ip
      security:
      - personalAccessToken: []
      - oauth:
        - ips:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/networking/ips/97.107.143.141
      - lang: CLI
        source: linode-cli networking ip-view 97.107.143.141
      x-linode-cli-action: ip-view
      x-linode-grant: read_only
      x-original-op-id: getIP
      x-original-op-title: IP Address View
    put:
      operationId: put-ip
      summary: Update an IP address's RDNS
      tags:
      - IP addresses
      description: |-
        Sets RDNS on an IP Address. Forward DNS must already be set up for reverse DNS to be applied. If you set the RDNS to `null` for public IPv4 addresses, it will be reset to the default _ip.linodeusercontent.com_ RDNS value.


        ---


        - __CLI__.

            ```
            linode-cli networking ip-update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            ips:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli networking ip-update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: ips:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The information to update.
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
              - rdns
              properties:
                rdns:
                  type: string
                  description: |
                    The reverse DNS assigned to this address. For public IPv4 addresses, this will be set to
                    a default value provided by Linode if not explicitly set.
                  example: '{{rdns}}'
                  nullable: true
                  x-linode-cli-display: 4
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            RDNS set successfully
          content:
            application/json:
              schema:
                type: object
                description: |
                  An IP address that exists in Linode's system, either IPv4 or IPv6.
                x-akamai:
                  file-path: schemas/ip-address.yaml
                additionalProperties: false
                properties:
                  type:
                    type: string
                    description: |
                      __Read-only__ The type of address this is.
                    example: ipv4
                    readOnly: true
                    enum:
                    - ipv4
                    - ipv6
                    - ipv6/pool
                    - ipv6/range
                    x-linode-cli-display: 2
                  address:
                    type: string
                    description: |
                      __Read-only__ The IP address.
                    example: 97.107.143.141
                    readOnly: true
                    format: ip
                    x-linode-cli-display: 1
                  gateway:
                    type: string
                    description: |
                      __Read-only__ The default gateway for this address.
                    example: 97.107.143.1
                    nullable: true
                    readOnly: true
                    format: ip
                  linode_id:
                    type: integer
                    description: |
                      __Read-only__ The ID of the Linode this address currently belongs to. For IPv4 addresses,
                      this is by default the Linode that this address was assigned
                      to on creation, and these addresses my be moved using the [Assign
                      IPv4s to Linodes](https://techdocs.akamai.com/linode-api/reference/post-assign-ipv4s)
                      operation. For SLAAC and link-local addresses, this value may
                      not be changed.
                    example: 123
                    readOnly: true
                    x-linode-cli-display: 6
                  prefix:
                    type: integer
                    description: |
                      __Read-only__ The number of bits set in the subnet mask.
                    example: 24
                    readOnly: true
                  public:
                    type: boolean
                    description: |
                      __Read-only__ Whether this is a public or private IP address.
                    example: true
                    readOnly: true
                    x-linode-cli-display: 3
                  rdns:
                    type: string
                    description: |
                      The reverse DNS assigned to this address. For public IPv4 addresses, this will be set
                      to a default value provided by Linode if not explicitly set.
                    example: test.example.org
                    nullable: true
                    x-linode-cli-display: 4
                  region:
                    type: string
                    description: |
                      __Read-only__ The Region this IP address resides in.
                    example: us-east
                    readOnly: true
                    x-linode-cli-display: 5
                  subnet_mask:
                    type: string
                    description: |
                      __Read-only__ The mask that separates host bits from network bits for this address.
                    example: 255.255.255.0
                    readOnly: true
                    format: ip
                  vpc_nat_1_1:
                    type: object
                    description: |-
                      IPv4 address configured as a 1:1 NAT for this Interface. If no address is configured as a 1:1 NAT, `null` is returned.

                      __Note:__ Only allowed for `vpc` type Interfaces.
                    additionalProperties: false
                    properties:
                      address:
                        type: string
                        description: |
                          The IPv4 address that is configured as a 1:1 NAT for this VPC interface.
                        example: 192.168.0.42
                        format: ipv4
                      subnet_id:
                        type: integer
                        description: |
                          The `id` of the VPC Subnet for this Interface.
                        example: 101
                        nullable: false
                      vpc_id:
                        type: integer
                        description: |
                          __Read-only__ The `id` of the VPC configured for this Interface.
                        example: 111
                        nullable: false
                        readOnly: true
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-ip
      security:
      - personalAccessToken: []
      - oauth:
        - ips:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "rdns": "test.example.org"
              }' \
              https://api.linode.com/v4/networking/ips/203.0.113.1
      - lang: CLI
        source: |-
          linode-cli networking ip-update \
            203.0.113.1 \
            --rdns "test.example.org"
      x-linode-cli-action: ip-update
      x-linode-grant: read_write
      x-original-op-id: updateIP
      x-original-op-title: IP Address RDNS Update
    x-linode-cli-command: networking
  /networking/ipv4/assign:
    x-akamai:
      file-path: paths/ipv4-assign.yaml
      path-info: /networking/ipv4/assign
    post:
      operationId: post-assign-ipv4s
      summary: Assign IPv4s to Linodes
      tags:
      - IPv4 addresses
      description: |-
        This operation is equivalent to [Assign IP addresses](https://techdocs.akamai.com/linode-api/reference/post-assign-ips).

        Assign multiple IPv4 addresses and/or IPv6 ranges to multiple Linodes in one Region. This allows swapping, shuffling, or otherwise reorganizing IPs to your Linodes.

        The following restrictions apply:

        - All Linodes involved must have at least one public IPv4 address after assignment.
        - Linodes may have no more than one assigned private IPv4 address.
        - Linodes may have no more than one assigned IPv6 range.

        [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) to request additional IPv4 addresses or IPv6 ranges beyond standard account limits.

        __Note__. Removing an IP address that has been set as a Managed Linode's `ssh.ip` causes the Managed Linode's SSH access settings to reset to their default values.

        To view and configure Managed Linode SSH settings, use the following operations:
        - [Get a Linode's managed settings](https://techdocs.akamai.com/linode-api/reference/get-managed-linode-setting)
        - [Update a Linode's managed settings](https://techdocs.akamai.com/linode-api/reference/put-managed-linode-setting)


        ---


        - __OAuth__.

            ```
            ips:read_write
        linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: |-
            ips:read_write
            linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about what IPv4 address to assign, and to which Linode.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                Request object for IP Addresses Assign (POST /networking/ips/assign).
              x-akamai:
                file-path: schemas/ip-addresses-assign-request.yaml
              additionalProperties: false
              required:
              - region
              - assignments
              properties:
                assignments:
                  type: array
                  description: |
                    The list of assignments to make. You must have read_write access to all IPs being assigned
                    and all Linodes being assigned to in order for the assignments
                    to succeed.
                  required:
                  - address
                  - linode_id
                  items:
                    type: object
                    additionalProperties: false
                    properties:
                      address:
                        type: string
                        description: |-
                          The IPv4 address or IPv6 range for this assignment.

                          - Must be an IPv4 address or an IPv6 range you can access in the Region specified.
                          - IPv6 ranges must include a prefix length of `/56` or `/64`, for example: `2001:db8:3c4d:15::/64`.
                          - Assignment of an IPv6 range to a Linode updates the route target of the range to the assigned Linode's SLAAC address.
                          - May be a public or private address.
                        example: 192.0.2.1
                        format: ipv4|ipv6/prefix_length
                      linode_id:
                        type: integer
                        description: |
                          The ID of the Linode to assign this address to. The IP's previous Linode will
                          lose this address, and must end up with at least one public
                          address and no more than one private address once all assignments
                          have been made.
                        example: 123
                region:
                  type: string
                  description: |
                    The ID of the Region in which these assignments are to take place. All IPs and Linodes
                    must exist in this Region.
                  example: '{{region}}'
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            All assignments completed successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-assign-ipv4s
      security:
      - personalAccessToken: []
      - oauth:
        - ips:read_write
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "region": "us-east",
                "assignments": [
                  {
                    "address": "192.0.2.1",
                    "linode_id": 123
                  },
                  {
                    "address": "2001:db8:3c4d:15::/64",
                    "linode_id": 234
                  }
                ]
              }' \
              https://api.linode.com/v4/networking/ipv4/assign
      - lang: CLI
        source: |-
          linode-cli networking ip-assign \
            --region us-east \
            --assignments.address 192.0.2.1 \
            --assignments.linode_id 123 \
            --assignments.address 2001:db8:3c4d:15::/64 \
            --assignments.linode_id 234
      x-linode-cli-skip: true
      x-linode-grant: read_write
      x-original-op-id: assignIPv4s
      x-original-op-title: Linodes Assign IPv4s
    x-linode-cli-command: networking
  /networking/ipv4/share:
    x-akamai:
      file-path: paths/ipv4-share.yaml
      path-info: /networking/ipv4/share
    post:
      operationId: post-share-ipv4s
      summary: Configure IPv4 sharing
      tags:
      - IPv4 addresses
      description: |-
        This operation is equivalent to [Share IP addresses](https://techdocs.akamai.com/linode-api/reference/post-share-ips).

        Configure shared IPs.

        IP sharing allows IP address reassignment (also referred to as IP failover) from one Linode to another if the primary Linode becomes unresponsive. This means that requests to the primary Linode's IP address can be automatically rerouted to secondary Linodes at the configured shared IP addresses.

        IP failover requires configuration of a failover service (such as [Keepalived](https://www.linode.com/docs/guides/ip-failover-keepalived)) within the internal system of the primary Linode.


        ---


        - __OAuth__.

            ```
            ips:read_write
        linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: |-
            ips:read_write
            linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about what IPs to share with which Linode.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                A request object IP Addresses Share (POST /networking/ips/share)
              x-akamai:
                file-path: schemas/ip-addresses-share-request.yaml
              additionalProperties: false
              required:
              - linode_id
              - ips
              properties:
                ips:
                  type: array
                  description: |-
                    A list of secondary Linode IPs to share with the primary Linode.

                    - Can include both IPv4 addresses and IPv6 ranges (omit /56 and /64 prefix lengths)
                    - Can include both private and public IPv4 addresses.
                    - You must have access to all of these addresses and they must be in the same Region as the primary Linode.
                    - Enter an empty array to remove all shared IP addresses.
                  example:
                  - 192.0.2.1
                  - '2001:db8:3c4d:15::'
                  items:
                    type: string
                    format: ip
                linode_id:
                  type: integer
                  description: |
                    The ID of the primary Linode that the addresses will be shared with.
                  example: '{{linode_id}}'
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Sharing configured successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-share-ipv4s
      security:
      - personalAccessToken: []
      - oauth:
        - ips:read_write
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "linode_id": 123,
                "ips": [
                  "192.0.2.1",
                  "192.0.2.2"
                ]
              }' \
              https://api.linode.com/v4/networking/ipv4/share
      - lang: CLI
        source: |-
          linode-cli networking ip-share \
            --linode_id 123 \
            --ips 192.0.2.1 \
            --ips 192.0.2.2
      x-linode-cli-skip: true
      x-linode-grant: read_write
      x-original-op-id: shareIPv4s
      x-original-op-title: IPv4 Sharing Configure
    x-linode-cli-command: networking
  /networking/ipv6/pools:
    x-akamai:
      file-path: paths/ipv6-pools.yaml
      path-info: /networking/ipv6/pools
    get:
      operationId: get-ipv6-pools
      summary: List IPv6 pools
      tags:
      - IPv6 pools
      description: |-
        Displays the IPv6 pools on your Account. A pool of IPv6 addresses are routed to all of your Linodes in a single [region](https://techdocs.akamai.com/linode-api/reference/get-regions). Any Linode on your Account may bring up any address in this pool at any time, with no external configuration required.


        ---


        - __CLI__.

            ```
            linode-cli networking v6-pools
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            ips:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli networking v6-pools
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: ips:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The IPv6 pools on your Account.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        An object representing an IPv6 pool.
                      x-akamai:
                        file-path: schemas/ipv6-pool.yaml
                      additionalProperties: false
                      properties:
                        prefix:
                          type: integer
                          description: |
                            The prefix length of the address. The total number of addresses that can
                            be assigned from this range is calculated as 2<sup>(128
                            - prefix length)</sup>.
                          example: 124
                          x-linode-cli-display: 2
                        range:
                          type: string
                          description: |
                            __Read-only__ The IPv6 range of addresses in this pool.
                          example: 2600:3c01::2:5000:0
                          readOnly: true
                          x-linode-cli-display: 1
                        region:
                          type: string
                          description: |
                            __Read-only__ The region for this pool of IPv6 addresses.
                          example: us-east
                          readOnly: true
                          x-linode-cli-display: 3
                          x-linode-filterable: true
                        route_target:
                          type: string
                          description: |
                            The last address in this block of IPv6 addresses.
                          example: 2600:3c01::2:5000:f
                          nullable: true
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-ipv6-pools
      security:
      - personalAccessToken: []
      - oauth:
        - ips:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/networking/ipv6/pools
      - lang: CLI
        source: linode-cli networking v6-pools
      x-linode-cli-action: v6-pools
      x-original-op-id: getIPv6Pools
      x-original-op-title: IPv6 Pools List
    x-linode-cli-command: networking
  /networking/ipv6/ranges:
    x-akamai:
      file-path: paths/ranges.yaml
      path-info: /networking/ipv6/ranges
    post:
      operationId: post-ipv6-range
      summary: Create an IPv6 range
      tags:
      - IPv6 ranges
      description: |-
        Creates an IPv6 Range and assigns it based on the provided Linode or route target IPv6 SLAAC address. See the `ipv6` property when running the [Get a Linode](https://techdocs.akamai.com/linode-api/reference/get-linode-instance) operation to view a Linode's IPv6 SLAAC address.

        - Either `linode_id` or `route_target` is required in a request.
        - `linode_id` and `route_target` are mutually exclusive. Submitting values for both properties in a request results in an error.
        - Upon a successful request, an IPv6 range is created in the [region](https://techdocs.akamai.com/linode-api/reference/get-regions) that corresponds to the provided `linode_id` or `route_target`.
        - Your Linode is responsible for routing individual addresses in the range, or handling traffic for all the addresses in the range.
        - Run the [Assign IP addresses](https://techdocs.akamai.com/linode-api/reference/post-assign-ips) operation to re-assign IPv6 Ranges to your Linodes.

        __Note__. The following restrictions apply:

          - A Linode can only have one IPv6 range targeting its SLAAC address.
          - An account can only have one IPv6 range in each [region](https://techdocs.akamai.com/linode-api/reference/get-regions).
          - [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) to request expansion of these restrictions.


        ---


        - __CLI__.

            ```
            linode-cli networking v6-range-create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            ips:read_write
        linodes:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli networking v6-range-create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: |-
            ips:read_write
            linodes:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the IPv6 range to create.
        required: true
        content:
          application/json:
            schema:
              additionalProperties: false
              required:
              - prefix_length
              properties:
                linode_id:
                  type: integer
                  description: |-
                    The ID of the Linode to assign this range to. The SLAAC address for the provided Linode is used as the range's `route_target`.

                    - __Required__ if `route_target` is omitted from the request.

                    - Mutually exclusive with `route_target`. Submitting values for both properties in a request results in an error.
                  example: 123
                prefix_length:
                  type: integer
                  description: |
                    The prefix length of the IPv6 range.
                  enum:
                  - 56
                  - 64
                route_target:
                  type: string
                  description: |-
                    The IPv6 SLAAC address to assign this range to.

                    - __Required__ if `linode_id` is omitted from the request.

                    - Mutually exclusive with `linode_id`. Submitting values for both properties in a request results in an error.

                    - __Note__. Omit the `/128` prefix length of the SLAAC address when using this property.
                  example: 2001:0db8::1
                  format: ipv6
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            IPv6 range created successfully.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  range:
                    type: string
                    description: |
                      The IPv6 network range, including subnet and prefix length.
                    example: 2001:0db8::/64
                    format: ipv6/prefix_length
                  route_target:
                    type: string
                    description: |
                      The route target IPV6 SLAAC address for this range. Does not include the prefix length.
                    example: 2001:0db8::1
                    format: ipv6
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-ipv6-range
      security:
      - personalAccessToken: []
      - oauth:
        - ips:read_write
        - linodes:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                  "linode_id": 123,
                  "prefix_length": 64
              }' \
              https://api.linode.com/v4/networking/ipv6/ranges
      - lang: CLI
        source: |-
          linode-cli networking v6-range-create \
            --linode_id 123 \
            --prefix_length 64
      x-linode-cli-action: v6-range-create
      x-original-op-id: postIPv6Range
      x-original-op-title: IPv6 Range Create
    get:
      operationId: get-ipv6-ranges
      summary: List IPv6 ranges
      tags:
      - IPv6 ranges
      description: |-
        Displays the IPv6 ranges on your Account.

          - An IPv6 range is a `/64` or `/54` block of IPv6 addresses routed to a single Linode in a given [region](https://techdocs.akamai.com/linode-api/reference/get-regions).

          - Your Linode is responsible for routing individual addresses in the range, or handling traffic for all the addresses in the range.

          - Run the [Create an IPv6 range](https://techdocs.akamai.com/linode-api/reference/post-ipv6-range) operation to add a `/64` or `/56` block of IPv6 addresses to your account.


        ---


        - __CLI__.

            ```
            linode-cli networking v6-ranges
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            ips:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli networking v6-ranges
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: ips:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The IPv6 ranges on your Account.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        An object representing an IPv6 range.
                      x-akamai:
                        file-path: schemas/ipv6-range.yaml
                      additionalProperties: false
                      properties:
                        prefix:
                          type: integer
                          description: |
                            The prefix length of the address. The total number of addresses that can
                            be assigned from this range is calculated as 2<sup>(128
                            - prefix length)</sup>.
                          example: 64
                          x-linode-cli-display: 2
                        range:
                          type: string
                          description: |
                            __Read-only__ The IPv6 address of this range.
                          example: '2600:3c01::'
                          readOnly: true
                          x-linode-cli-display: 1
                        region:
                          type: string
                          description: |
                            __Read-only__ The region for this range of IPv6 addresses.
                          example: us-east
                          readOnly: true
                          x-linode-cli-display: 3
                        route_target:
                          type: string
                          description: |
                            The IPv6 SLAAC address.
                          example: 2600:3c01::ffff:ffff:ffff:ffff
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-ipv6-ranges
      security:
      - personalAccessToken: []
      - oauth:
        - ips:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/networking/ipv6/ranges
      - lang: CLI
        source: linode-cli networking v6-ranges
      x-linode-cli-action: v6-ranges
      x-original-op-id: getIPv6Ranges
      x-original-op-title: IPv6 Ranges List
    x-linode-cli-command: networking
  /networking/ipv6/ranges/{range}:
    x-akamai:
      file-path: paths/range.yaml
      path-info: /networking/ipv6/ranges/{range}
    parameters:
    - name: range
      in: path
      description: |-
        The IPv6 range to access. Corresponds to the `range` property of objects returned from the [List IPv6 ranges](https://techdocs.akamai.com/linode-api/reference/get-ipv6-ranges) operation.

        __Note__. Omit the prefix length of the IPv6 range.
      example: '{{range}}'
      x-akamai:
        file-path: parameters/range-path.yaml
      required: true
      schema:
        type: string
        format: ipv6
    get:
      operationId: get-ipv6-range
      summary: Get an IPv6 range
      tags:
      - IPv6 ranges
      description: |-
        View IPv6 range information.


        ---


        - __CLI__.

            ```
            linode-cli networking v6-range-view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            ips:read
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli networking v6-range-view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: ips:read
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Returns IPv6 range information.
          content:
            application/json:
              schema:
                type: object
                description: |
                  An object representing an IPv6 range.
                x-akamai:
                  file-path: schemas/ipv6-range-bgp.yaml
                additionalProperties: false
                properties:
                  is_bgp:
                    type: boolean
                    description: |
                      __Read-only__ Whether this IPv6 range is shared.
                    example: false
                    readOnly: true
                  linodes:
                    type: array
                    description: |
                      __Read-only__ A list of Linodes targeted by this IPv6 range. Includes Linodes with
                      IP sharing.
                    example:
                    - 123
                    readOnly: true
                    items:
                      type: integer
                    x-linode-cli-display: 4
                  prefix:
                    type: integer
                    description: |
                      The prefix length of the address. The total number of addresses that can be assigned
                      from this range is calculated as 2<sup>(128 - prefix length)</sup>.
                    example: 64
                    x-linode-cli-display: 2
                  range:
                    type: string
                    description: |
                      __Read-only__ The IPv6 address of this range.
                    example: '2600:3c01::'
                    readOnly: true
                    x-linode-cli-display: 1
                  region:
                    type: string
                    description: |
                      __Read-only__ The region for this range of IPv6 addresses.
                    example: us-east
                    readOnly: true
                    x-linode-cli-display: 3
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-ipv6-range
      security:
      - personalAccessToken: []
      - oauth:
        - ips:read
      x-code-samples:
      - lang: Shell
        source: 'curl -H "Authorization: Bearer $TOKEN" https://api.linode.com/v4/networking/ipv6/ranges/2001:0db8::'
      - lang: CLI
        source: 'linode-cli networking v6-range-view 2001:0db8::'
      x-linode-cli-action: v6-range-view
      x-original-op-id: getIPv6Range
      x-original-op-title: IPv6 Range View
    delete:
      operationId: delete-ipv6-range
      summary: Delete an IPv6 range
      tags:
      - IPv6 ranges
      description: |-
        Removes this IPv6 range from your account and disconnects the range from any assigned Linodes.

        __Note__. Shared IPv6 ranges cannot be deleted at this time. Please contact Customer Support for assistance.


        ---


        - __CLI__.

            ```
            linode-cli networking v6-range-delete
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            ips:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli networking v6-range-delete
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: ips:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            IPv6 Range deleted.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-ipv6-range
      security:
      - personalAccessToken: []
      - oauth:
        - ips:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/networking/ipv6/ranges/2001:0db8::
      - lang: CLI
        source: 'linode-cli networking v6-range-delete 2001:0db8::'
      x-linode-cli-action: v6-range-delete
      x-original-op-id: deleteIPv6Range
      x-original-op-title: IPv6 Range Delete
    x-linode-cli-command: networking
  /networking/vlans:
    x-akamai:
      file-path: paths/vlans.yaml
      path-info: /networking/vlans
    get:
      servers:
      - url: https://api.linode.com/v4
      operationId: get-vlans
      summary: List VLANs
      tags:
      - VLANs
      description: |-
        Returns a list of all Virtual Local Area Networks (VLANs) on your Account. VLANs provide a mechanism for secure communication between two or more Linodes that are assigned to the same VLAN and are both within the same Layer 2 broadcast domain.

        VLANs are created and attached to Linodes by using the `interfaces` property for the following operations:

        - [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
        - [Create a config profile](https://techdocs.akamai.com/linode-api/reference/post-add-linode-config)
        - [Update a config profile](https://techdocs.akamai.com/linode-api/reference/put-linode-config)

        There are several ways to detach a VLAN from a Linode:

        - [Update](https://techdocs.akamai.com/linode-api/reference/put-linode-config) the active Configuration Profile to remove the VLAN Interface, then [reboot](https://techdocs.akamai.com/linode-api/reference/post-reboot-linode-instance) the Linode.
        - [Create a config profile](https://techdocs.akamai.com/linode-api/reference/post-add-linode-config) without the VLAN Interface, then [reboot](https://techdocs.akamai.com/linode-api/reference/post-reboot-linode-instance) the Linode into the new Configuration Profile.
        - [Delete](https://techdocs.akamai.com/linode-api/reference/delete-linode-instance) the Linode.

        __Note__. Only Next Generation Network (NGN) data centers support VLANs. Run the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation to view the capabilities of data center regions. If a VLAN is attached to your Linode and you attempt to migrate or clone it to a non-NGN data center, the migration or cloning will not initiate. If a Linode cannot be migrated because of an incompatibility, you will be prompted to select a different data center or contact support.

        __Note__. See the [VLANs Overview](https://www.linode.com/docs/products/networking/vlans/#technical-specifications) to view additional specifications and limitations.


        ---


        - __OAuth__.

            ```
            linodes:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: linodes:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The VLANs available on this Account.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        A virtual local area network (VLAN) associated with your Account.
                      x-akamai:
                        file-path: schemas/vlans.yaml
                      additionalProperties: false
                      properties:
                        created:
                          type: string
                          description: |
                            __Read-only__ The date this VLAN was created.
                          example: '2020-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                        label:
                          type: string
                          description: |
                            __Read-only__ The name of this VLAN.
                          example: vlan-example
                          readOnly: true
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        linodes:
                          type: array
                          description: |
                            __Read-only__ An array of Linode IDs attached to this VLAN.
                          example:
                          - 111
                          - 222
                          readOnly: true
                          items:
                            type: integer
                          x-linode-cli-display: 3
                        region:
                          type: string
                          description: |-
                            __Read-only__ This VLAN's data center region.

                            __Note:__ Currently, a VLAN can only be assigned to a Linode
                            within the same data center region.
                          example: ap-west
                          readOnly: true
                          x-linode-cli-display: 1
                          x-linode-filterable: true
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-vlans
      security:
      - personalAccessToken: []
      - oauth:
        - linodes:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/networking/vlans/
      - lang: CLI
        source: linode-cli vlans list
      x-linode-cli-action:
      - list
      - ls
      x-linode-grant: read_only
      x-original-op-id: getVLANs
      x-original-op-title: VLANs List
    x-linode-cli-command: vlans
  /nodebalancers:
    x-akamai:
      file-path: paths/node-balancers.yaml
      path-info: /nodebalancers
    post:
      operationId: post-node-balancer
      summary: Create a node balancer
      tags:
      - NodeBalancers
      description: |-
        Creates a NodeBalancer in the requested Region. Only available in [regions](https://techdocs.akamai.com/linode-api/reference/get-regions) with "NodeBalancers" in their `capabilities`.

        NodeBalancers require a port Config with at least one backend Node to start serving requests.

        When using the Linode CLI to create a NodeBalancer, first create a NodeBalancer without any Configs. Then, create Configs and Nodes for that NodeBalancer with the respective [Create a config](https://techdocs.akamai.com/linode-api/reference/post-node-balancer-config) and [Create a node](https://techdocs.akamai.com/linode-api/reference/post-node-balancer-node) operations.


        ---


        - __CLI__.

            ```
            linode-cli nodebalancers create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            nodebalancers:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli nodebalancers create
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: nodebalancers:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          Information about the NodeBalancer to create.
        required: true
        content:
          application/json:
            schema:
              additionalProperties: false
              required:
              - region
              properties:
                tags:
                  type: array
                  description: |
                    An array of Tags applied to this object. Tags are for organizational purposes only.
                  example:
                  - test
                  - web-dev-team
                  items:
                    type: string
                client_conn_throttle:
                  type: integer
                  description: |
                    Throttle connections per second.  Set to 0 (zero) to disable throttling.
                  example: 0
                  minimum: 0
                  maximum: 20
                  x-linode-cli-display: 6
                configs:
                  type: array
                  description: |-
                    The port Config(s) to create for this NodeBalancer.

                    Each Config must have a unique port and at least one Node.
                  required:
                  - nodes
                  items:
                    type: object
                    description: |
                      A request object representing a NodeBalancer Config, including Nodes.
                    additionalProperties: false
                    properties:
                      algorithm:
                        type: string
                        description: |
                          What algorithm this NodeBalancer should use for routing traffic to backends.
                        example: roundrobin
                        default: roundrobin
                        enum:
                        - roundrobin
                        - leastconn
                        - source
                        x-linode-cli-display: 4
                      check:
                        type: string
                        description: |-
                          The type of check to perform against backends to ensure they are serving requests. This is used to determine if backends are up or down.

                          - If `none` no check is performed.
                          - `connection` requires only a connection to the backend to succeed.
                          - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
                        example: http_body
                        default: none
                        enum:
                        - none
                        - connection
                        - http
                        - http_body
                      check_attempts:
                        type: integer
                        description: |
                          How many times to attempt a check before considering a backend to be down.
                        example: 3
                        default: 3
                        minimum: 1
                        maximum: 30
                      check_body:
                        type: string
                        description: |
                          This value must be present in the response body of the check in order for
                          it to pass. If this value is not present in the response
                          body of a check request, the backend is considered to be
                          down.
                        example: it works
                      check_interval:
                        type: integer
                        description: |-
                          How often, in seconds, to check that backends are up and serving requests.

                          Must be greater than `check_timeout`.
                        example: 90
                        default: 31
                      check_passive:
                        type: boolean
                        description: |
                          If true, any response from this backend with a `5xx` status code will be enough
                          for it to be considered unhealthy and taken out of rotation.
                        example: true
                        default: true
                        x-linode-cli-display: 6
                      check_path:
                        type: string
                        description: |
                          The URL path to check on each backend. If the backend does not respond to
                          this request it is considered to be down.
                        example: /test
                        pattern: ^[a-zA-Z0-9\/\-%?&=.]*$
                      check_timeout:
                        type: integer
                        description: |-
                          How long, in seconds, to wait for a check attempt before considering it failed.

                          Must be less than `check_interval`.
                        example: 10
                        default: 30
                        minimum: 1
                        maximum: 30
                      cipher_suite:
                        type: string
                        description: |-
                          What ciphers to use for SSL connections served by this NodeBalancer.

                          - `legacy` is considered insecure and should only be used if necessary.
                        example: recommended
                        default: recommended
                        enum:
                        - recommended
                        - legacy
                        x-linode-cli-color:
                          default_: white
                          legacy: red
                        x-linode-cli-display: 7
                      nodes:
                        type: array
                        description: |
                          The NodeBalancer Node(s) that serve this Config.
                        items:
                          type: object
                          description: |
                            A NodeBalancerNode represents a single backend serving requests for a single
                            port of a NodeBalancer.  Nodes are specific to NodeBalancer
                            Configs, and serve traffic over their private IP.  If
                            the same Linode is serving traffic for more than one port
                            on the same NodeBalancer, one NodeBalancer Node is required
                            for each config (port) it should serve requests on.  For
                            example, if you have four backends, and each should response
                            to both HTTP and HTTPS requests, you will need two NodeBalancerConfigs
                            (port 80 and port 443) and four backends each - one for
                            each of the Linodes serving requests for that port.
                          x-akamai:
                            file-path: schemas/node-balancer-node.yaml
                          additionalProperties: false
                          properties:
                            id:
                              type: integer
                              description: |
                                __Read-only__ This node's unique ID.
                              example: 54321
                              readOnly: true
                              x-linode-cli-display: 1
                            address:
                              type: string
                              description: |
                                The private IP Address where this backend can be reached. This
                                _must_ be a private IP address.
                              example: 192.168.210.120:80
                              format: ip
                              x-linode-cli-display: 3
                            config_id:
                              type: integer
                              description: |
                                __Read-only__ The NodeBalancer Config ID that this Node belongs
                                to.
                              example: 4567
                              readOnly: true
                            label:
                              type: string
                              description: |
                                The label for this node.  This is for display purposes only.
                              example: node54321
                              minLength: 3
                              maxLength: 32
                              pattern: '[a-zA-Z0-9-_.]{3,32}'
                              x-linode-cli-display: 2
                            mode:
                              type: string
                              description: |-
                                The mode this NodeBalancer should use when sending traffic to this backend.

                                - If set to `accept` this backend is accepting traffic.
                                - If set to `reject` this backend will not receive traffic.
                                - If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.
                                - If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
                              example: accept
                              enum:
                              - accept
                              - reject
                              - drain
                              - backup
                              x-linode-cli-display: 6
                            nodebalancer_id:
                              type: integer
                              description: |
                                __Read-only__ The NodeBalancer ID that this Node belongs to.
                              example: 12345
                              readOnly: true
                            status:
                              type: string
                              description: |
                                __Read-only__ The current status of this node, based on the configured
                                checks of its NodeBalancer Config.
                              example: UP
                              readOnly: true
                              enum:
                              - unknown
                              - UP
                              - DOWN
                              x-linode-cli-color:
                                DOWN: red
                                UP: green
                                default_: white
                                unknown: yellow
                              x-linode-cli-display: 4
                            weight:
                              type: integer
                              description: |
                                Used when picking a backend to serve a request and is not pinned
                                to a single backend yet.  Nodes with a higher weight
                                will receive more traffic.
                              example: 50
                              minimum: 1
                              maximum: 255
                              x-linode-cli-display: 5
                      port:
                        type: integer
                        description: |
                          The port this Config is for. These values must be unique across configs on a
                          single NodeBalancer (you can't have two configs for port
                          80, for example).  While some ports imply some protocols,
                          no enforcement is done and you may configure your NodeBalancer
                          however is useful to you. For example, while port 443 is
                          generally used for HTTPS, you do not need SSL configured
                          to have a NodeBalancer listening on port 443.
                        example: 80
                        default: 80
                        minimum: 1
                        maximum: 65535
                        x-linode-cli-display: 2
                      protocol:
                        type: string
                        description: |-
                          The protocol this port is configured to serve.

                          - The `http` and `tcp` protocols do not support `ssl_cert` and `ssl_key`.

                          - The `https` protocol is mutually required with `ssl_cert` and `ssl_key`.

                          Review our guide on [Available Protocols](https://www.linode.com/docs/products/networking/nodebalancers/guides/protocols/) for information on protocol features.
                        example: http
                        default: http
                        enum:
                        - http
                        - https
                        - tcp
                        x-linode-cli-display: 3
                      proxy_protocol:
                        type: string
                        description: |-
                          ProxyProtocol is a TCP extension that sends initial TCP connection information such as source/destination IPs and ports to backend devices. This information would be lost otherwise. Backend devices must be configured to work with ProxyProtocol if enabled.

                          - If omitted, or set to `none`, the NodeBalancer doesn't send any auxiliary data over TCP connections. This is the default.
                          - If set to `v1`, the human-readable header format (Version 1) is used. Requires `tcp` protocol.
                          - If set to `v2`, the binary header format (Version 2) is used. Requires `tcp` protocol.
                        example: none
                        default: none
                        enum:
                        - none
                        - v1
                        - v2
                      ssl_cert:
                        type: string
                        description: |2-

                          The PEM-formatted public SSL certificate (or the combined PEM-formatted SSL certificate and Certificate Authority chain) that should be served on this NodeBalancerConfig's port.

                          Line breaks must be represented as "\n" in the string for requests (but not when using the Linode CLI).

                          [Diffie-Hellman Parameters](https://www.linode.com/docs/products/networking/nodebalancers/guides/ssl-termination/#diffie-hellman-parameters) can be included in this value to enable forward secrecy.

                          The contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.

                          The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.
                        example: <REDACTED>
                        nullable: true
                        format: ssl-cert
                      ssl_key:
                        type: string
                        description: |-
                          The PEM-formatted private key for the SSL certificate set in the `ssl_cert` field.

                          Line breaks must be represented as "\n" in the string for requests (but not when using the Linode CLI).

                          The contents of this field will not be shown in any responses that display
                          the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field
                          appears.

                          The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig
                          response are automatically derived from your certificate. Please refer to these fields to
                          verify that the appropriate certificate was assigned to your NodeBalancerConfig.
                        example: <REDACTED>
                        nullable: true
                        format: ssl-key
                      stickiness:
                        type: string
                        description: |-
                          Controls how session stickiness is handled on this port.

                          - If set to `none` connections will always be assigned a backend based on the algorithm configured.
                          - If set to `table` sessions from the same remote address will be routed to the same backend.
                          - For HTTP or HTTPS clients, `http_cookie` allows sessions to be routed to the same backend based on a cookie set by the NodeBalancer.
                        example: http_cookie
                        default: none
                        enum:
                        - none
                        - table
                        - http_cookie
                        x-linode-cli-display: 5
                firewall_id:
                  type: integer
                  description: |-
                    The ID of the Firewall to assign to the NodeBalancer.

                    - Only one NodeBalancer can be assigned to a Firewall at a time.
                    - Firewalls only apply to inbound TCP traffic to NodeBalancers.
                label:
                  type: string
                  description: |
                    This NodeBalancer's label. These must be unique on your Account.
                  example: balancer12345
                  minLength: 3
                  maxLength: 32
                  pattern: '[a-zA-Z0-9-_]{3,32}'
                  x-linode-cli-display: 2
                  x-linode-filterable: true
                region:
                  type: string
                  description: |
                    The ID of the Region to create this NodeBalancer in.
                  example: us-east
              x-ref: ../schemas/added-tbd.yaml
            x-example:
              x-ref: ../examples/tbd.json
        x-linode-cli-allowed-defaults:
        - region
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            NodeBalancer created successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Linode's load balancing solution.  Can handle multiple ports, SSL termination, and any number
                  of backends.  NodeBalancer ports are configured with NodeBalancer
                  Configs, and each config is given one or more NodeBalancer Node
                  that accepts traffic.  The traffic should be routed to the  NodeBalancer's
                  ip address, the NodeBalancer will handle routing individual requests
                  to backends.
                x-akamai:
                  file-path: schemas/node-balancer.yaml
                title: NodeBalancer
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This NodeBalancer's unique ID.
                    example: 12345
                    readOnly: true
                    x-linode-cli-display: 1
                  tags:
                    type: array
                    description: |
                      An array of Tags applied to this object.  Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  client_conn_throttle:
                    type: integer
                    description: |
                      Throttle connections per second.  Set to 0 (zero) to disable throttling.
                    example: 0
                    minimum: 0
                    maximum: 20
                    x-linode-cli-display: 6
                  created:
                    type: string
                    description: |
                      __Read-only__ When this NodeBalancer was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  host:
                    type: string
                    description: |
                      __Read-only__ This NodeBalancer's hostname, beginning with its IP address and ending
                      with _.ip.linodeusercontent.com_.
                    example: 192.0.2.1.ip.linodeusercontent.com
                    readOnly: true
                    x-linode-cli-display: 4
                  ipv4:
                    type: string
                    description: |
                      __Read-only__ This NodeBalancer's public IPv4 address.
                    example: 203.0.113.1
                    readOnly: true
                    format: ip
                    x-linode-cli-display: 5
                    x-linode-filterable: true
                  ipv6:
                    type: string
                    description: |
                      __Read-only__ This NodeBalancer's public IPv6 address.
                    example: null
                    nullable: true
                    readOnly: true
                    format: ip
                    x-linode-cli-display: 6
                  label:
                    type: string
                    description: |
                      This NodeBalancer's label. These must be unique on your Account.
                    example: balancer12345
                    minLength: 3
                    maxLength: 32
                    pattern: '[a-zA-Z0-9-_]{3,32}'
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  region:
                    type: string
                    description: |
                      __Read-only__ The Region where this NodeBalancer is located. NodeBalancers only support
                      backends in the same Region.
                    example: us-east
                    readOnly: true
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  transfer:
                    type: object
                    description: |
                      __Read-only__ Information about the amount of transfer this NodeBalancer has had so far
                      this month.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      in:
                        type: number
                        description: |
                          __Read-only__ The total outbound transfer, in MB, used for this NodeBalancer
                          this month.
                        example: 28.91200828552246
                        nullable: true
                        readOnly: true
                      out:
                        type: number
                        description: |
                          __Read-only__ The total inbound transfer, in MB, used for this NodeBalancer
                          this month.
                        example: 3.5487728118896484
                        nullable: true
                        readOnly: true
                      total:
                        type: number
                        description: |
                          __Read-only__ The total transfer, in MB, used by this NodeBalancer this month.
                        example: 32.46078109741211
                        nullable: true
                        readOnly: true
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this NodeBalancer was last updated.
                    example: '2018-03-01T00:01:01'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-node-balancer
      security:
      - personalAccessToken: []
      - oauth:
        - nodebalancers:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "region": "us-east",
                "label": "balancer12345",
                "client_conn_throttle": 0,
                "configs": [
                  {
                    "port": 443,
                    "protocol": "https",
                    "algorithm": "roundrobin",
                    "stickiness": "http_cookie",
                    "check": "http_body",
                    "check_interval": 90,
                    "check_timeout": 10,
                    "check_attempts": 3,
                    "check_path": "/test",
                    "check_body": "it works",
                    "check_passive": true,
                    "proxy_protocol": "none",
                    "cipher_suite": "recommended",
                    "ssl_cert": "-----BEGIN CERTIFICATE-----\nCERTIFICATE_INFORMATION\n-----END CERTIFICATE-----",
                    "ssl_key": "-----BEGIN PRIVATE KEY-----\nPRIVATE_KEY_INFORMATION\n-----END PRIVATE KEY-----",
                    "nodes": [
                      {
                        "address": "192.168.210.120:80",
                        "label": "node1",
                        "weight": 50,
                        "mode": "accept"
                      },
                      {
                        "address": "192.168.210.122:81",
                        "label": "node2",
                        "weight": 50,
                        "mode": "accept"
                      }
                    ]
                  }
                ]
              }' \
              https://api.linode.com/v4/nodebalancers
      - lang: CLI
        source: |-
          linode-cli nodebalancers create \
            --region us-east \
            --label balancer12345 \
            --client_conn_throttle 0
      x-linode-cli-action: create
      x-linode-grant: add_nodebalancers
      x-original-op-id: createNodeBalancer
      x-original-op-title: NodeBalancer Create
    get:
      operationId: get-node-balancers
      summary: List node balancers
      tags:
      - NodeBalancers
      description: |-
        Returns a paginated list of NodeBalancers you have access to.


        ---


        - __OAuth__.

            ```
            nodebalancers:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: nodebalancers:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            A paginated list of NodeBalancers.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        Linode's load balancing solution.  Can handle multiple ports, SSL termination, and
                        any number of backends.  NodeBalancer ports are configured
                        with NodeBalancer Configs, and each config is given one or
                        more NodeBalancer Node that accepts traffic.  The traffic
                        should be routed to the  NodeBalancer's ip address, the NodeBalancer
                        will handle routing individual requests to backends.
                      x-akamai:
                        file-path: schemas/node-balancer.yaml
                      title: NodeBalancer
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ This NodeBalancer's unique ID.
                          example: 12345
                          readOnly: true
                          x-linode-cli-display: 1
                        tags:
                          type: array
                          description: |
                            An array of Tags applied to this object.  Tags are for organizational purposes
                            only.
                          example:
                          - example tag
                          - another example
                          items:
                            type: string
                          x-linode-filterable: true
                        client_conn_throttle:
                          type: integer
                          description: |
                            Throttle connections per second.  Set to 0 (zero) to disable throttling.
                          example: 0
                          minimum: 0
                          maximum: 20
                          x-linode-cli-display: 6
                        created:
                          type: string
                          description: |
                            __Read-only__ When this NodeBalancer was created.
                          example: '2018-01-01T00:01:01'
                          readOnly: true
                          format: date-time
                        host:
                          type: string
                          description: |
                            __Read-only__ This NodeBalancer's hostname, beginning with its IP address
                            and ending with _.ip.linodeusercontent.com_.
                          example: 192.0.2.1.ip.linodeusercontent.com
                          readOnly: true
                          x-linode-cli-display: 4
                        ipv4:
                          type: string
                          description: |
                            __Read-only__ This NodeBalancer's public IPv4 address.
                          example: 203.0.113.1
                          readOnly: true
                          format: ip
                          x-linode-cli-display: 5
                          x-linode-filterable: true
                        ipv6:
                          type: string
                          description: |
                            __Read-only__ This NodeBalancer's public IPv6 address.
                          example: null
                          nullable: true
                          readOnly: true
                          format: ip
                          x-linode-cli-display: 6
                        label:
                          type: string
                          description: |
                            This NodeBalancer's label. These must be unique on your Account.
                          example: balancer12345
                          minLength: 3
                          maxLength: 32
                          pattern: '[a-zA-Z0-9-_]{3,32}'
                          x-linode-cli-display: 2
                          x-linode-filterable: true
                        region:
                          type: string
                          description: |
                            __Read-only__ The Region where this NodeBalancer is located. NodeBalancers
                            only support backends in the same Region.
                          example: us-east
                          readOnly: true
                          x-linode-cli-display: 3
                          x-linode-filterable: true
                        transfer:
                          type: object
                          description: |
                            __Read-only__ Information about the amount of transfer this NodeBalancer
                            has had so far this month.
                          additionalProperties: false
                          readOnly: true
                          properties:
                            in:
                              type: number
                              description: |
                                __Read-only__ The total outbound transfer, in MB, used for this
                                NodeBalancer this month.
                              example: 28.91200828552246
                              nullable: true
                              readOnly: true
                            out:
                              type: number
                              description: |
                                __Read-only__ The total inbound transfer, in MB, used for this NodeBalancer
                                this month.
                              example: 3.5487728118896484
                              nullable: true
                              readOnly: true
                            total:
                              type: number
                              description: |
                                __Read-only__ The total transfer, in MB, used by this NodeBalancer
                                this month.
                              example: 32.46078109741211
                              nullable: true
                              readOnly: true
                        updated:
                          type: string
                          description: |
                            __Read-only__ When this NodeBalancer was last updated.
                          example: '2018-03-01T00:01:01'
                          readOnly: true
                          format: date-time
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
                x-ref: ../schemas/added-tbd.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-node-balancers
      security:
      - personalAccessToken: []
      - oauth:
        - nodebalancers:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/nodebalancers
      - lang: CLI
        source: linode-cli nodebalancers list
      x-linode-cli-action:
      - list
      - ls
      x-linode-grant: read_only
      x-original-op-id: getNodeBalancers
      x-original-op-title: NodeBalancers List
    x-linode-cli-command: nodebalancers
  /nodebalancers/{nodeBalancerId}:
    x-akamai:
      file-path: paths/node-balancer.yaml
      path-info: /nodebalancers/{nodeBalancerId}
    parameters:
    - name: nodeBalancerId
      in: path
      description: |
        The ID of the NodeBalancer to access.
      example: '{{nodeBalancerId}}'
      x-akamai:
        file-path: parameters/node-balancer-id.yaml
      required: true
      schema:
        type: integer
    get:
      operationId: get-node-balancer
      summary: Get a node balancer
      tags:
      - NodeBalancers
      description: |-
        Returns a single NodeBalancer you can access.


        ---


        - __CLI__.

            ```
            linode-cli nodebalancers view
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            nodebalancers:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli nodebalancers view
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: nodebalancers:read_only
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            The requested NodeBalancer object.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Linode's load balancing solution.  Can handle multiple ports, SSL termination, and any number
                  of backends.  NodeBalancer ports are configured with NodeBalancer
                  Configs, and each config is given one or more NodeBalancer Node
                  that accepts traffic.  The traffic should be routed to the  NodeBalancer's
                  ip address, the NodeBalancer will handle routing individual requests
                  to backends.
                x-akamai:
                  file-path: schemas/node-balancer.yaml
                title: NodeBalancer
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This NodeBalancer's unique ID.
                    example: 12345
                    readOnly: true
                    x-linode-cli-display: 1
                  tags:
                    type: array
                    description: |
                      An array of Tags applied to this object.  Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  client_conn_throttle:
                    type: integer
                    description: |
                      Throttle connections per second.  Set to 0 (zero) to disable throttling.
                    example: 0
                    minimum: 0
                    maximum: 20
                    x-linode-cli-display: 6
                  created:
                    type: string
                    description: |
                      __Read-only__ When this NodeBalancer was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  host:
                    type: string
                    description: |
                      __Read-only__ This NodeBalancer's hostname, beginning with its IP address and ending
                      with _.ip.linodeusercontent.com_.
                    example: 192.0.2.1.ip.linodeusercontent.com
                    readOnly: true
                    x-linode-cli-display: 4
                  ipv4:
                    type: string
                    description: |
                      __Read-only__ This NodeBalancer's public IPv4 address.
                    example: 203.0.113.1
                    readOnly: true
                    format: ip
                    x-linode-cli-display: 5
                    x-linode-filterable: true
                  ipv6:
                    type: string
                    description: |
                      __Read-only__ This NodeBalancer's public IPv6 address.
                    example: null
                    nullable: true
                    readOnly: true
                    format: ip
                    x-linode-cli-display: 6
                  label:
                    type: string
                    description: |
                      This NodeBalancer's label. These must be unique on your Account.
                    example: balancer12345
                    minLength: 3
                    maxLength: 32
                    pattern: '[a-zA-Z0-9-_]{3,32}'
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  region:
                    type: string
                    description: |
                      __Read-only__ The Region where this NodeBalancer is located. NodeBalancers only support
                      backends in the same Region.
                    example: us-east
                    readOnly: true
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  transfer:
                    type: object
                    description: |
                      __Read-only__ Information about the amount of transfer this NodeBalancer has had so far
                      this month.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      in:
                        type: number
                        description: |
                          __Read-only__ The total outbound transfer, in MB, used for this NodeBalancer
                          this month.
                        example: 28.91200828552246
                        nullable: true
                        readOnly: true
                      out:
                        type: number
                        description: |
                          __Read-only__ The total inbound transfer, in MB, used for this NodeBalancer
                          this month.
                        example: 3.5487728118896484
                        nullable: true
                        readOnly: true
                      total:
                        type: number
                        description: |
                          __Read-only__ The total transfer, in MB, used by this NodeBalancer this month.
                        example: 32.46078109741211
                        nullable: true
                        readOnly: true
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this NodeBalancer was last updated.
                    example: '2018-03-01T00:01:01'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-node-balancer
      security:
      - personalAccessToken: []
      - oauth:
        - nodebalancers:read_only
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/nodebalancers/12345
      - lang: CLI
        source: linode-cli nodebalancers view 12345
      x-linode-cli-action: view
      x-linode-grant: read_only
      x-original-op-id: getNodeBalancer
      x-original-op-title: NodeBalancer View
    put:
      operationId: put-node-balancer
      summary: Update a node balancer
      tags:
      - NodeBalancers
      description: |-
        Updates information about a NodeBalancer you can access.


        ---


        - __CLI__.

            ```
            linode-cli nodebalancers update
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            nodebalancers:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: CLI
          syntax: linode-cli nodebalancers update
          url: https://www.linode.com/docs/products/tools/cli/get-started/
        - title: OAuth
          syntax: nodebalancers:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        description: |
          The information to update.
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                Linode's load balancing solution.  Can handle multiple ports, SSL termination, and any number of backends.  NodeBalancer
                ports are configured with NodeBalancer Configs, and each config is
                given one or more NodeBalancer Node that accepts traffic.  The traffic
                should be routed to the  NodeBalancer's ip address, the NodeBalancer
                will handle routing individual requests to backends.
              x-akamai:
                file-path: schemas/node-balancer.yaml
              title: NodeBalancer
              additionalProperties: false
              properties:
                id:
                  type: integer
                  description: |
                    __Read-only__ This NodeBalancer's unique ID.
                  example: '{{id}}'
                  readOnly: true
                  x-linode-cli-display: 1
                tags:
                  type: array
                  description: |
                    An array of Tags applied to this object.  Tags are for organizational purposes only.
                  example:
                  - example tag
                  - another example
                  items:
                    type: string
                  x-linode-filterable: true
                client_conn_throttle:
                  type: integer
                  description: |
                    Throttle connections per second.  Set to 0 (zero) to disable throttling.
                  example: '{{client_conn_throttle}}'
                  minimum: 0
                  maximum: 20
                  x-linode-cli-display: 6
                created:
                  type: string
                  description: |
                    __Read-only__ When this NodeBalancer was created.
                  example: '{{created}}'
                  readOnly: true
                  format: date-time
                host:
                  type: string
                  description: |
                    __Read-only__ This NodeBalancer's hostname, beginning with its IP address and ending with _.ip.linodeusercontent.com_.
                  example: '{{host}}'
                  readOnly: true
                  x-linode-cli-display: 4
                ipv4:
                  type: string
                  description: |
                    __Read-only__ This NodeBalancer's public IPv4 address.
                  example: '{{ipv4}}'
                  readOnly: true
                  format: ip
                  x-linode-cli-display: 5
                  x-linode-filterable: true
                ipv6:
                  type: string
                  description: |
                    __Read-only__ This NodeBalancer's public IPv6 address.
                  example: '{{ipv6}}'
                  nullable: true
                  readOnly: true
                  format: ip
                  x-linode-cli-display: 6
                label:
                  type: string
                  description: |
                    This NodeBalancer's label. These must be unique on your Account.
                  example: '{{label}}'
                  minLength: 3
                  maxLength: 32
                  pattern: '[a-zA-Z0-9-_]{3,32}'
                  x-linode-cli-display: 2
                  x-linode-filterable: true
                region:
                  type: string
                  description: |
                    __Read-only__ The Region where this NodeBalancer is located. NodeBalancers only support
                    backends in the same Region.
                  example: '{{region}}'
                  readOnly: true
                  x-linode-cli-display: 3
                  x-linode-filterable: true
                transfer:
                  type: object
                  description: |
                    __Read-only__ Information about the amount of transfer this NodeBalancer has had so far
                    this month.
                  additionalProperties: false
                  readOnly: true
                  properties:
                    in:
                      type: number
                      description: |
                        __Read-only__ The total outbound transfer, in MB, used for this NodeBalancer
                        this month.
                      example: 28.91200828552246
                      nullable: true
                      readOnly: true
                    out:
                      type: number
                      description: |
                        __Read-only__ The total inbound transfer, in MB, used for this NodeBalancer
                        this month.
                      example: 3.5487728118896484
                      nullable: true
                      readOnly: true
                    total:
                      type: number
                      description: |
                        __Read-only__ The total transfer, in MB, used by this NodeBalancer this month.
                      example: 32.46078109741211
                      nullable: true
                      readOnly: true
                updated:
                  type: string
                  description: |
                    __Read-only__ When this NodeBalancer was last updated.
                  example: '{{updated}}'
                  readOnly: true
                  format: date-time
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            NodeBalancer updated successfully.
          content:
            application/json:
              schema:
                type: object
                description: |
                  Linode's load balancing solution.  Can handle multiple ports, SSL termination, and any number
                  of backends.  NodeBalancer ports are configured with NodeBalancer
                  Configs, and each config is given one or more NodeBalancer Node
                  that accepts traffic.  The traffic should be routed to the  NodeBalancer's
                  ip address, the NodeBalancer will handle routing individual requests
                  to backends.
                x-akamai:
                  file-path: schemas/node-balancer.yaml
                title: NodeBalancer
                additionalProperties: false
                properties:
                  id:
                    type: integer
                    description: |
                      __Read-only__ This NodeBalancer's unique ID.
                    example: 12345
                    readOnly: true
                    x-linode-cli-display: 1
                  tags:
                    type: array
                    description: |
                      An array of Tags applied to this object.  Tags are for organizational purposes only.
                    example:
                    - example tag
                    - another example
                    items:
                      type: string
                    x-linode-filterable: true
                  client_conn_throttle:
                    type: integer
                    description: |
                      Throttle connections per second.  Set to 0 (zero) to disable throttling.
                    example: 0
                    minimum: 0
                    maximum: 20
                    x-linode-cli-display: 6
                  created:
                    type: string
                    description: |
                      __Read-only__ When this NodeBalancer was created.
                    example: '2018-01-01T00:01:01'
                    readOnly: true
                    format: date-time
                  host:
                    type: string
                    description: |
                      __Read-only__ This NodeBalancer's hostname, beginning with its IP address and ending
                      with _.ip.linodeusercontent.com_.
                    example: 192.0.2.1.ip.linodeusercontent.com
                    readOnly: true
                    x-linode-cli-display: 4
                  ipv4:
                    type: string
                    description: |
                      __Read-only__ This NodeBalancer's public IPv4 address.
                    example: 203.0.113.1
                    readOnly: true
                    format: ip
                    x-linode-cli-display: 5
                    x-linode-filterable: true
                  ipv6:
                    type: string
                    description: |
                      __Read-only__ This NodeBalancer's public IPv6 address.
                    example: null
                    nullable: true
                    readOnly: true
                    format: ip
                    x-linode-cli-display: 6
                  label:
                    type: string
                    description: |
                      This NodeBalancer's label. These must be unique on your Account.
                    example: balancer12345
                    minLength: 3
                    maxLength: 32
                    pattern: '[a-zA-Z0-9-_]{3,32}'
                    x-linode-cli-display: 2
                    x-linode-filterable: true
                  region:
                    type: string
                    description: |
                      __Read-only__ The Region where this NodeBalancer is located. NodeBalancers only support
                      backends in the same Region.
                    example: us-east
                    readOnly: true
                    x-linode-cli-display: 3
                    x-linode-filterable: true
                  transfer:
                    type: object
                    description: |
                      __Read-only__ Information about the amount of transfer this NodeBalancer has had so far
                      this month.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      in:
                        type: number
                        description: |
                          __Read-only__ The total outbound transfer, in MB, used for this NodeBalancer
                          this month.
                        example: 28.91200828552246
                        nullable: true
                        readOnly: true
                      out:
                        type: number
                        description: |
                          __Read-only__ The total inbound transfer, in MB, used for this NodeBalancer
                          this month.
                        example: 3.5487728118896484
                        nullable: true
                        readOnly: true
                      total:
                        type: number
                        description: |
                          __Read-only__ The total transfer, in MB, used by this NodeBalancer this month.
                        example: 32.46078109741211
                        nullable: true
                        readOnly: true
                  updated:
                    type: string
                    description: |
                      __Read-only__ When this NodeBalancer was last updated.
                    example: '2018-03-01T00:01:01'
                    readOnly: true
                    format: date-time
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/put-node-balancer
      security:
      - personalAccessToken: []
      - oauth:
        - nodebalancers:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X PUT -d '{
                "label": "balancer12345",
                "client_conn_throttle": 0
              }' \
              https://api.linode.com/v4/nodebalancers/12345
      - lang: CLI
        source: |-
          linode-cli nodebalancers update 12345 \
            --label balancer12345 \
            --client_conn_throttle 0
      x-linode-cli-action: update
      x-linode-grant: read_write
      x-original-op-id: updateNodeBalancer
      x-original-op-title: NodeBalancer Update
    delete:
      operationId: delete-node-balancer
      summary: Delete a node balancer
      tags:
      - NodeBalancers
      description: |-
        Deletes a NodeBalancer.

        __This is a destructive action and cannot be undone.__

        Deleting a NodeBalancer will also delete all associated Configs and Nodes, although the backend servers represented by the Nodes will not be changed or removed. Deleting a NodeBalancer will cause you to lose access to the IP Addresses assigned to this NodeBalancer.


        ---


        - __OAuth__.

            ```
            nodebalancers:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      x-akamai:
        tabs:
        - title: OAuth
          syntax: nodebalancers:read_write
          url: https://techdocs.akamai.com/linode-api/reference/oauth
      parameters: []
      requestBody:
        content:
          application/json: {}
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            NodeBalancer deleted successfully.
          content:
            application/json:
              schema:
                type: object
                x-akamai:
                  file-path: schemas/added-empty-obj.yaml
              x-example:
                x-ref: ../examples/tbd.json
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/delete-node-balancer
      security:
      - personalAccessToken: []
      - oauth:
        - nodebalancers:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              -X DELETE \
              https://api.linode.com/v4/nodebalancers/12345
      - lang: CLI
        source: linode-cli nodebalancers delete 12345
      x-linode-cli-action:
      - delete
      - rm
      x-linode-grant: read_write
      x-original-op-id: deleteNodeBalancer
      x-original-op-title: NodeBalancer Delete
    x-linode-cli-command: nodebalancers
  /nodebalancers/{nodeBalancerId}/configs:
    x-akamai:
      file-path: paths/node-balancer-configs.yaml
      path-info: /nodebalancers/{nodeBalancerId}/configs
    parameters:
    - name: nodeBalancerId
      in: path
      description: |
        The ID of the NodeBalancer to access.
      example: '{{nodeBalancerId}}'
      x-akamai:
        file-path: parameters/node-balancer-id.yaml
      required: true
      schema:
        type: integer
    post:
      operationId: post-node-balancer-config
      summary: Create a config
      tags:
      - Configurations
      description: |-
        Creates a NodeBalancer Config, which allows the NodeBalancer to accept traffic on a new port. You will need to add NodeBalancer Nodes to the new Config before it can actually serve requests.


        ---


        - __CLI__.

            ```
            linode-cli nodebalancers config-create
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            nodebalancers:read_write
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      security:
      - personalAccessToken: []
      - oauth:
        - nodebalancers:read_write
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Content-Type: application/json" \
              -H "Authorization: Bearer $TOKEN" \
              -X POST -d '{
                "port": 443,
                "protocol": "https",
                "algorithm": "roundrobin",
                "stickiness": "http_cookie",
                "check": "http_body",
                "check_interval": 90,
                "check_timeout": 10,
                "check_attempts": 3,
                "check_path": "/test",
                "check_body": "it works",
                "check_passive": true,
                "proxy_protocol": "none",
                "ssl_cert": "-----BEGIN CERTIFICATE-----\nCERTIFICATE_INFORMATION\n-----END CERTIFICATE-----",
                "ssl_key": "-----BEGIN PRIVATE KEY-----\nPRIVATE_KEY_INFORMATION\n-----END PRIVATE KEY-----",
                "cipher_suite": "recommended"
              }' \
              https://api.linode.com/v4/nodebalancers/12345/configs
      - lang: CLI
        source: |-
          linode-cli nodebalancers config-create 12345 \
            --port 443 \
            --protocol https \
            --algorithm roundrobin \
            --stickiness http_cookie \
            --check http_body \
            --check_interval 90 \
            --check_timeout 10 \
            --check_attempts 3 \
            --check_path "/test" \
            --check_body "it works" \
            --check_passive true \
            --proxy_protocol "none" \
            --ssl_cert "-----BEGIN CERTIFICATE-----
                        CERTIFICATE_INFORMATION
                        -----END CERTIFICATE-----" \
            --ssl_key "-----BEGIN PRIVATE KEY-----
                       PRIVATE_KEY_INFORMATION
                       -----END PRIVATE KEY-----" \
            --cipher_suite recommended
      x-linode-cli-action: config-create
      x-linode-grant: read_write
      x-original-op-id: createNodeBalancerConfig
      x-original-op-title: Config Create
      x-akamai:
        tabs:
        - url: https://www.linode.com/docs/products/tools/cli/get-started/
          title: CLI
          syntax: linode-cli nodebalancers config-create
        - url: https://techdocs.akamai.com/linode-api/reference/oauth
          title: OAuth
          syntax: nodebalancers:read_write
      parameters: []
      requestBody:
        description: |
          Information about the port to configure.
        content:
          application/json:
            schema:
              type: object
              description: |
                A NodeBalancer config represents the configuration of this NodeBalancer on a single port.  For example,
                a NodeBalancer Config on port 80 would typically represent how this
                NodeBalancer response to HTTP requests. NodeBalancer configs have
                a list of backends, called "nodes," that they forward requests between
                based on their configuration.
              x-akamai:
                file-path: schemas/node-balancer-config.yaml
              additionalProperties: false
              properties:
                id:
                  type: integer
                  description: |
                    __Read-only__ This config's unique ID
                  example: '{{id}}'
                  readOnly: true
                  x-linode-cli-display: 1
                algorithm:
                  type: string
                  description: |
                    What algorithm this NodeBalancer should use for routing traffic to backends.
                  example: '{{algorithm}}'
                  default: roundrobin
                  enum:
                  - roundrobin
                  - leastconn
                  - source
                  x-linode-cli-display: 4
                check:
                  type: string
                  description: |-
                    The type of check to perform against backends to ensure they are serving requests. This is used to determine if backends are up or down.

                    - If `none` no check is performed.
                    - `connection` requires only a connection to the backend to succeed.
                    - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
                  example: '{{check}}'
                  default: none
                  enum:
                  - none
                  - connection
                  - http
                  - http_body
                check_attempts:
                  type: integer
                  description: |
                    How many times to attempt a check before considering a backend to be down.
                  example: '{{check_attempts}}'
                  default: 3
                  minimum: 1
                  maximum: 30
                check_body:
                  type: string
                  description: |
                    This value must be present in the response body of the check in order for it to pass. If
                    this value is not present in the response body of a check request,
                    the backend is considered to be down.
                  example: '{{check_body}}'
                check_interval:
                  type: integer
                  description: |-
                    How often, in seconds, to check that backends are up and serving requests.

                    Must be greater than `check_timeout`.
                  example: '{{check_interval}}'
                  default: 31
                check_passive:
                  type: boolean
                  description: |
                    If true, any response from this backend with a `5xx` status code will be enough for
                    it to be considered unhealthy and taken out of rotation.
                  example: '{{check_passive}}'
                  default: true
                  x-linode-cli-display: 6
                check_path:
                  type: string
                  description: |
                    The URL path to check on each backend. If the backend does not respond to this request
                    it is considered to be down.
                  example: '{{check_path}}'
                  pattern: ^[a-zA-Z0-9\/\-%?&=.]*$
                check_timeout:
                  type: integer
                  description: |-
                    How long, in seconds, to wait for a check attempt before considering it failed.

                    Must be less than `check_interval`.
                  example: '{{check_timeout}}'
                  default: 30
                  minimum: 1
                  maximum: 30
                cipher_suite:
                  type: string
                  description: |-
                    What ciphers to use for SSL connections served by this NodeBalancer.

                    - `legacy` is considered insecure and should only be used if necessary.
                  example: '{{cipher_suite}}'
                  default: recommended
                  enum:
                  - recommended
                  - legacy
                  x-linode-cli-color:
                    default_: white
                    legacy: red
                  x-linode-cli-display: 7
                nodebalancer_id:
                  type: integer
                  description: |
                    __Read-only__ The ID for the NodeBalancer this config belongs to.
                  example: '{{nodebalancer_id}}'
                  readOnly: true
                nodes_status:
                  type: object
                  description: |
                    __Read-only__ A structure containing information about the health of the backends for this
                    port.  This information is updated periodically as checks are
                    performed against backends.
                  additionalProperties: false
                  readOnly: true
                  properties:
                    down:
                      type: integer
                      description: |
                        __Read-only__ The number of backends considered to be "DOWN" and unhealthy.  These
                        are not in rotation, and not serving requests.
                      example: 0
                      readOnly: true
                    up:
                      type: integer
                      description: |
                        __Read-only__ The number of backends considered to be "UP" and healthy, and
                        that are serving requests.
                      example: 4
                      readOnly: true
                  x-linode-cli-display: 10
                port:
                  type: integer
                  description: |
                    The port this Config is for. These values must be unique across configs on a single
                    NodeBalancer (you can't have two configs for port 80, for example).  While
                    some ports imply some protocols, no enforcement is done and you
                    may configure your NodeBalancer however is useful to you. For
                    example, while port 443 is generally used for HTTPS, you do not
                    need SSL configured to have a NodeBalancer listening on port 443.
                  example: '{{port}}'
                  default: 80
                  minimum: 1
                  maximum: 65535
                  x-linode-cli-display: 2
                protocol:
                  type: string
                  description: |-
                    The protocol this port is configured to serve.

                    - The `http` and `tcp` protocols do not support `ssl_cert` and `ssl_key`.

                    - The `https` protocol is mutually required with `ssl_cert` and `ssl_key`.

                    Review our guide on [Available Protocols](https://www.linode.com/docs/products/networking/nodebalancers/guides/protocols/) for information on protocol features.
                  example: '{{protocol}}'
                  default: http
                  enum:
                  - http
                  - https
                  - tcp
                  x-linode-cli-display: 3
                proxy_protocol:
                  type: string
                  description: |-
                    ProxyProtocol is a TCP extension that sends initial TCP connection information such as source/destination IPs and ports to backend devices. This information would be lost otherwise. Backend devices must be configured to work with ProxyProtocol if enabled.

                    - If omitted, or set to `none`, the NodeBalancer doesn't send any auxiliary data over TCP connections. This is the default.
                    - If set to `v1`, the human-readable header format (Version 1) is used. Requires `tcp` protocol.
                    - If set to `v2`, the binary header format (Version 2) is used. Requires `tcp` protocol.
                  example: '{{proxy_protocol}}'
                  default: none
                  enum:
                  - none
                  - v1
                  - v2
                ssl_cert:
                  type: string
                  description: |2-

                    The PEM-formatted public SSL certificate (or the combined PEM-formatted SSL certificate and Certificate Authority chain) that should be served on this NodeBalancerConfig's port.

                    Line breaks must be represented as "\n" in the string for requests (but not when using the Linode CLI).

                    [Diffie-Hellman Parameters](https://www.linode.com/docs/products/networking/nodebalancers/guides/ssl-termination/#diffie-hellman-parameters) can be included in this value to enable forward secrecy.

                    The contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.

                    The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.
                  example: '{{ssl_cert}}'
                  nullable: true
                  format: ssl-cert
                ssl_commonname:
                  type: string
                  description: |
                    __Read-only__ The read-only common name automatically derived from the SSL certificate assigned
                    to this NodeBalancerConfig. Please refer to this field to verify
                    that the appropriate certificate is assigned to your NodeBalancerConfig.
                  example: '{{ssl_commonname}}'
                  readOnly: true
                  x-linode-cli-display: 8
                ssl_fingerprint:
                  type: string
                  description: |
                    __Read-only__ The read-only SHA1-encoded fingerprint automatically derived from the SSL certificate
                    assigned to this NodeBalancerConfig. Please refer to this field
                    to verify that the appropriate certificate is assigned to your
                    NodeBalancerConfig.
                  example: '{{ssl_fingerprint}}'
                  readOnly: true
                  x-linode-cli-display: 9
                ssl_key:
                  type: string
                  description: |-
                    The PEM-formatted private key for the SSL certificate set in the `ssl_cert` field.

                    Line breaks must be represented as "\n" in the string for requests (but not when using the Linode CLI).

                    The contents of this field will not be shown in any responses that display
                    the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field
                    appears.

                    The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig
                    response are automatically derived from your certificate. Please refer to these fields to
                    verify that the appropriate certificate was assigned to your NodeBalancerConfig.
                  example: '{{ssl_key}}'
                  nullable: true
                  format: ssl-key
                stickiness:
                  type: string
                  description: |-
                    Controls how session stickiness is handled on this port.

                    - If set to `none` connections will always be assigned a backend based on the algorithm configured.
                    - If set to `table` sessions from the same remote address will be routed to the same backend.
                    - For HTTP or HTTPS clients, `http_cookie` allows sessions to be routed to the same backend based on a cookie set by the NodeBalancer.
                  example: '{{stickiness}}'
                  default: none
                  enum:
                  - none
                  - table
                  - http_cookie
                  x-linode-cli-display: 5
            x-example:
              x-ref: ../examples/tbd.json
      responses:
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
        200:
          description: |
            Config created successfully.
          content:
            application/json:
              x-example:
                x-ref: ../examples/tbd.json
              schema:
                type: object
                description: |
                  A NodeBalancer config represents the configuration of this NodeBalancer on a single port.  For example,
                  a NodeBalancer Config on port 80 would typically represent how this
                  NodeBalancer response to HTTP requests. NodeBalancer configs have
                  a list of backends, called "nodes," that they forward requests between
                  based on their configuration.
                x-akamai:
                  file-path: schemas/node-balancer-config.yaml
                additionalProperties: false
                properties:
                  check_passive:
                    x-linode-cli-display: 6
                    type: boolean
                    description: |
                      If true, any response from this backend with a `5xx` status code will be enough for
                      it to be considered unhealthy and taken out of rotation.
                    example: true
                    default: true
                  check_path:
                    type: string
                    description: |
                      The URL path to check on each backend. If the backend does not respond to this request
                      it is considered to be down.
                    example: /test
                    pattern: ^[a-zA-Z0-9\/\-%?&=.]*$
                  check_timeout:
                    type: integer
                    description: |-
                      How long, in seconds, to wait for a check attempt before considering it failed.

                      Must be less than `check_interval`.
                    example: 10
                    default: 30
                    minimum: 1
                    maximum: 30
                  cipher_suite:
                    x-linode-cli-color:
                      default_: white
                      legacy: red
                    x-linode-cli-display: 7
                    type: string
                    description: |-
                      What ciphers to use for SSL connections served by this NodeBalancer.

                      - `legacy` is considered insecure and should only be used if necessary.
                    example: recommended
                    default: recommended
                    enum:
                    - recommended
                    - legacy
                  nodebalancer_id:
                    type: integer
                    description: |
                      __Read-only__ The ID for the NodeBalancer this config belongs to.
                    example: 12345
                    readOnly: true
                  nodes_status:
                    x-linode-cli-display: 10
                    type: object
                    description: |
                      __Read-only__ A structure containing information about the health of the backends for
                      this port.  This information is updated periodically as checks
                      are performed against backends.
                    additionalProperties: false
                    readOnly: true
                    properties:
                      down:
                        type: integer
                        description: |
                          __Read-only__ The number of backends considered to be "DOWN" and unhealthy.  These
                          are not in rotation, and not serving requests.
                        example: 0
                        readOnly: true
                      up:
                        type: integer
                        description: |
                          __Read-only__ The number of backends considered to be "UP" and healthy, and
                          that are serving requests.
                        example: 4
                        readOnly: true
                  id:
                    x-linode-cli-display: 1
                    type: integer
                    description: |
                      __Read-only__ This config's unique ID
                    example: 4567
                    readOnly: true
                  port:
                    x-linode-cli-display: 2
                    type: integer
                    description: |
                      The port this Config is for. These values must be unique across configs on a single
                      NodeBalancer (you can't have two configs for port 80, for example).  While
                      some ports imply some protocols, no enforcement is done and
                      you may configure your NodeBalancer however is useful to you.
                      For example, while port 443 is generally used for HTTPS, you
                      do not need SSL configured to have a NodeBalancer listening
                      on port 443.
                    example: 80
                    default: 80
                    minimum: 1
                    maximum: 65535
                  protocol:
                    x-linode-cli-display: 3
                    type: string
                    description: |-
                      The protocol this port is configured to serve.

                      - The `http` and `tcp` protocols do not support `ssl_cert` and `ssl_key`.

                      - The `https` protocol is mutually required with `ssl_cert` and `ssl_key`.

                      Review our guide on [Available Protocols](https://www.linode.com/docs/products/networking/nodebalancers/guides/protocols/) for information on protocol features.
                    example: http
                    default: http
                    enum:
                    - http
                    - https
                    - tcp
                  proxy_protocol:
                    type: string
                    description: |-
                      ProxyProtocol is a TCP extension that sends initial TCP connection information such as source/destination IPs and ports to backend devices. This information would be lost otherwise. Backend devices must be configured to work with ProxyProtocol if enabled.

                      - If omitted, or set to `none`, the NodeBalancer doesn't send any auxiliary data over TCP connections. This is the default.
                      - If set to `v1`, the human-readable header format (Version 1) is used. Requires `tcp` protocol.
                      - If set to `v2`, the binary header format (Version 2) is used. Requires `tcp` protocol.
                    example: none
                    default: none
                    enum:
                    - none
                    - v1
                    - v2
                  ssl_cert:
                    type: string
                    description: |2-

                      The PEM-formatted public SSL certificate (or the combined PEM-formatted SSL certificate and Certificate Authority chain) that should be served on this NodeBalancerConfig's port.

                      Line breaks must be represented as "\n" in the string for requests (but not when using the Linode CLI).

                      [Diffie-Hellman Parameters](https://www.linode.com/docs/products/networking/nodebalancers/guides/ssl-termination/#diffie-hellman-parameters) can be included in this value to enable forward secrecy.

                      The contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.

                      The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.
                    example: <REDACTED>
                    nullable: true
                    format: ssl-cert
                  ssl_commonname:
                    x-linode-cli-display: 8
                    type: string
                    description: |
                      __Read-only__ The read-only common name automatically derived from the SSL certificate assigned
                      to this NodeBalancerConfig. Please refer to this field to verify
                      that the appropriate certificate is assigned to your NodeBalancerConfig.
                    example: www.example.com
                    readOnly: true
                  ssl_fingerprint:
                    x-linode-cli-display: 9
                    type: string
                    description: |
                      __Read-only__ The read-only SHA1-encoded fingerprint automatically derived from the SSL
                      certificate assigned to this NodeBalancerConfig. Please refer
                      to this field to verify that the appropriate certificate is
                      assigned to your NodeBalancerConfig.
                    example: 00:01:02:03:04:05:06:07:08:09:0A:0B:0C:0D:0E:0F:10:11:12:13
                    readOnly: true
                  ssl_key:
                    type: string
                    description: |-
                      The PEM-formatted private key for the SSL certificate set in the `ssl_cert` field.

                      Line breaks must be represented as "\n" in the string for requests (but not when using the Linode CLI).

                      The contents of this field will not be shown in any responses that display
                      the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field
                      appears.

                      The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig
                      response are automatically derived from your certificate. Please refer to these fields to
                      verify that the appropriate certificate was assigned to your NodeBalancerConfig.
                    example: <REDACTED>
                    nullable: true
                    format: ssl-key
                  stickiness:
                    x-linode-cli-display: 5
                    type: string
                    description: |-
                      Controls how session stickiness is handled on this port.

                      - If set to `none` connections will always be assigned a backend based on the algorithm configured.
                      - If set to `table` sessions from the same remote address will be routed to the same backend.
                      - For HTTP or HTTPS clients, `http_cookie` allows sessions to be routed to the same backend based on a cookie set by the NodeBalancer.
                    example: http_cookie
                    default: none
                    enum:
                    - none
                    - table
                    - http_cookie
                  algorithm:
                    type: string
                    description: |
                      What algorithm this NodeBalancer should use for routing traffic to backends.
                    example: roundrobin
                    default: roundrobin
                    enum:
                    - roundrobin
                    - leastconn
                    - source
                    x-linode-cli-display: 4
                  check:
                    type: string
                    description: |-
                      The type of check to perform against backends to ensure they are serving requests. This is used to determine if backends are up or down.

                      - If `none` no check is performed.
                      - `connection` requires only a connection to the backend to succeed.
                      - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
                    example: http_body
                    default: none
                    enum:
                    - none
                    - connection
                    - http
                    - http_body
                  check_attempts:
                    type: integer
                    description: |
                      How many times to attempt a check before considering a backend to be down.
                    example: 3
                    default: 3
                    minimum: 1
                    maximum: 30
                  check_body:
                    type: string
                    description: |
                      This value must be present in the response body of the check in order for it to
                      pass. If this value is not present in the response body of a
                      check request, the backend is considered to be down.
                    example: it works
                  check_interval:
                    type: integer
                    description: |-
                      How often, in seconds, to check that backends are up and serving requests.

                      Must be greater than `check_timeout`.
                    example: 90
                    default: 31
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/post-node-balancer-config
    get:
      operationId: get-node-balancer-configs
      summary: List configs
      tags:
      - Configurations
      description: |-
        Returns a paginated list of NodeBalancer Configs associated with this NodeBalancer. NodeBalancer Configs represent individual ports that this NodeBalancer will accept traffic on, one Config per port.

        For example, if you wanted to accept standard HTTP traffic, you would need a Config listening on port 80.


        ---


        - __CLI__.

            ```
            linode-cli nodebalancers configs-list
            ```

            [Learn more...](https://www.linode.com/docs/products/tools/cli/get-started/)

        - __OAuth__.

            ```
            nodebalancers:read_only
            ```

            [Learn more...](https://techdocs.akamai.com/linode-api/reference/oauth)
      externalDocs:
        description: |
          See documentation for this operation in Akamai's Linode API
        url: https://techdocs.akamai.com/linode-api/reference/get-node-balancer-configs
      x-akamai:
        tabs:
        - title: CLI
          url: https://www.linode.com/docs/products/tools/cli/get-started/
          syntax: linode-cli nodebalancers configs-list
        - title: OAuth
          url: https://techdocs.akamai.com/linode-api/reference/oauth
          syntax: nodebalancers:read_only
      parameters:
      - name: page
        in: query
        description: |
          The page of a collection to return.
        example: '{{page}}'
        x-akamai:
          file-path: parameters/page-offset.yaml
        required: false
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        description: |
          The number of items to return per page.
        example: '{{page_size}}'
        x-akamai:
          file-path: parameters/page-size.yaml
        schema:
          type: integer
          default: 100
          minimum: 25
          maximum: 500
      security:
      - personalAccessToken: []
      - oauth:
        - nodebalancers:read_only
      requestBody:
        content:
          application/json: {}
      x-code-samples:
      - lang: Shell
        source: |-
          curl -H "Authorization: Bearer $TOKEN" \
              https://api.linode.com/v4/nodebalancers/12345/configs
      - lang: CLI
        source: linode-cli nodebalancers configs-list 12345
      x-linode-cli-action: configs-list
      responses:
        200:
          description: |
            A paginated list of NodeBalancer Configs
          content:
            application/json:
              x-example:
                x-ref: ../examples/tbd.json
              schema:
                type: object
                additionalProperties: false
                x-ref: ../schemas/added-tbd.yaml
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      description: |
                        A NodeBalancer config represents the configuration of this NodeBalancer on a single
                        port.  For example, a NodeBalancer Config on port 80 would
                        typically represent how this NodeBalancer response to HTTP
                        requests. NodeBalancer configs have a list of backends, called
                        "nodes," that they forward requests between based on their
                        configuration.
                      x-akamai:
                        file-path: schemas/node-balancer-config.yaml
                      additionalProperties: false
                      properties:
                        id:
                          type: integer
                          description: |
                            __Read-only__ This config's unique ID
                          example: 4567
                          x-linode-cli-display: 1
                          readOnly: true
                        algorithm:
                          type: string
                          description: |
                            What algorithm this NodeBalancer should use for routing traffic to backends.
                          example: roundrobin
                          x-linode-cli-display: 4
                          default: roundrobin
                          enum:
                          - roundrobin
                          - leastconn
                          - source
                        check:
                          type: string
                          description: |-
                            The type of check to perform against backends to ensure they are serving requests. This is used to determine if backends are up or down.

                            - If `none` no check is performed.
                            - `connection` requires only a connection to the backend to succeed.
                            - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
                          example: http_body
                          default: none
                          enum:
                          - none
                          - connection
                          - http
                          - http_body
                        check_attempts:
                          type: integer
                          description: |
                            How many times to attempt a check before considering a backend to be
                            down.
                          example: 3
                          default: 3
                          minimum: 1
                          maximum: 30
                        check_body:
                          type: string
                          description: |
                            This value must be present in the response body of the check in order
                            for it to pass. If this value is not present in the response
                            body of a check request, the backend is considered to
                            be down.
                          example: it works
                        check_interval:
                          type: integer
                          description: |-
                            How often, in seconds, to check that backends are up and serving requests.

                            Must be greater than `check_timeout`.
                          example: 90
                          default: 31
                        check_passive:
                          type: boolean
                          description: |
                            If true, any response from this backend with a `5xx` status code will
                            be enough for it to be considered unhealthy and taken
                            out of rotation.
                          example: true
                          x-linode-cli-display: 6
                          default: true
                        check_path:
                          type: string
                          description: |
                            The URL path to check on each backend. If the backend does not respond
                            to this request it is considered to be down.
                          example: /test
                          pattern: ^[a-zA-Z0-9\/\-%?&=.]*$
                        check_timeout:
                          type: integer
                          description: |-
                            How long, in seconds, to wait for a check attempt before considering it failed.

                            Must be less than `check_interval`.
                          example: 10
                          default: 30
                          minimum: 1
                          maximum: 30
                        cipher_suite:
                          type: string
                          description: |-
                            What ciphers to use for SSL connections served by this NodeBalancer.

                            - `legacy` is considered insecure and should only be used if necessary.
                          example: recommended
                          x-linode-cli-color:
                            default_: white
                            legacy: red
                          x-linode-cli-display: 7
                          default: recommended
                          enum:
                          - recommended
                          - legacy
                        nodebalancer_id:
                          type: integer
                          description: |
                            __Read-only__ The ID for the NodeBalancer this config belongs to.
                          example: 12345
                          readOnly: true
                        nodes_status:
                          type: object
                          description: |
                            __Read-only__ A structure containing information about the health of
                            the backends for this port.  This information is updated
                            periodically as checks are performed against backends.
                          x-linode-cli-display: 10
                          additionalProperties: false
                          readOnly: true
                          properties:
                            down:
                              type: integer
                              description: |
                                __Read-only__ The number of backends considered to be "DOWN" and
                                unhealthy.  These are not in rotation, and not serving
                                requests.
                              example: 0
                              readOnly: true
                            up:
                              type: integer
                              description: |
                                __Read-only__ The number of backends considered to be "UP" and healthy,
                                and that are serving requests.
                              example: 4
                              readOnly: true
                        port:
                          type: integer
                          description: |
                            The port this Config is for. These values must be unique across configs
                            on a single NodeBalancer (you can't have two configs for
                            port 80, for example).  While some ports imply some protocols,
                            no enforcement is done and you may configure your NodeBalancer
                            however is useful to you. For example, while port 443
                            is generally used for HTTPS, you do not need SSL configured
                            to have a NodeBalancer listening on port 443.
                          example: 80
                          x-linode-cli-display: 2
                          default: 80
                          minimum: 1
                          maximum: 65535
                        protocol:
                          type: string
                          description: |-
                            The protocol this port is configured to serve.

                            - The `http` and `tcp` protocols do not support `ssl_cert` and `ssl_key`.

                            - The `https` protocol is mutually required with `ssl_cert` and `ssl_key`.

                            Review our guide on [Available Protocols](https://www.linode.com/docs/products/networking/nodebalancers/guides/protocols/) for information on protocol features.
                          example: http
                          x-linode-cli-display: 3
                          default: http
                          enum:
                          - http
                          - https
                          - tcp
                        proxy_protocol:
                          type: string
                          description: |-
                            ProxyProtocol is a TCP extension that sends initial TCP connection information such as source/destination IPs and ports to backend devices. This information would be lost otherwise. Backend devices must be configured to work with ProxyProtocol if enabled.

                            - If omitted, or set to `none`, the NodeBalancer doesn't send any auxiliary data over TCP connections. This is the default.
                            - If set to `v1`, the human-readable header format (Version 1) is used. Requires `tcp` protocol.
                            - If set to `v2`, the binary header format (Version 2) is used. Requires `tcp` protocol.
                          example: none
                          default: none
                          enum:
                          - none
                          - v1
                          - v2
                        ssl_cert:
                          type: string
                          description: |2-

                            The PEM-formatted public SSL certificate (or the combined PEM-formatted SSL certificate and Certificate Authority chain) that should be served on this NodeBalancerConfig's port.

                            Line breaks must be represented as "\n" in the string for requests (but not when using the Linode CLI).

                            [Diffie-Hellman Parameters](https://www.linode.com/docs/products/networking/nodebalancers/guides/ssl-termination/#diffie-hellman-parameters) can be included in this value to enable forward secrecy.

                            The contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.

                            The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.
                          example: <REDACTED>
                          nullable: true
                          format: ssl-cert
                        ssl_commonname:
                          type: string
                          description: |
                            __Read-only__ The read-only common name automatically derived from the
                            SSL certificate assigned to this NodeBalancerConfig. Please
                            refer to this field to verify that the appropriate certificate
                            is assigned to your NodeBalancerConfig.
                          example: www.example.com
                          x-linode-cli-display: 8
                          readOnly: true
                        ssl_fingerprint:
                          type: string
                          description: |
                            __Read-only__ The read-only SHA1-encoded fingerprint automatically derived
                            from the SSL certificate assigned to this NodeBalancerConfig.
                            Please refer to this field to verify that the appropriate
                            certificate is assigned to your NodeBalancerConfig.
                          example: 00:01:02:03:04:05:06:07:08:09:0A:0B:0C:0D:0E:0F:10:11:12:13
                          x-linode-cli-display: 9
                          readOnly: true
                        ssl_key:
                          type: string
                          description: |-
                            The PEM-formatted private key for the SSL certificate set in the `ssl_cert` field.

                            Line breaks must be represented as "\n" in the string for requests (but not when using the Linode CLI).

                            The contents of this field will not be shown in any responses that display
                            the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field
                            appears.

                            The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig
                            response are automatically derived from your certificate. Please refer to these fields to
                            verify that the appropriate certificate was assigned to your NodeBalancerConfig.
                          example: <REDACTED>
                          nullable: true
                          format: ssl-key
                        stickiness:
                          type: string
                          description: |-
                            Controls how session stickiness is handled on this port.

                            - If set to `none` connections will always be assigned a backend based on the algorithm configured.
                            - If set to `table` sessions from the same remote address will be routed to the same backend.
                            - For HTTP or HTTPS clients, `http_cookie` allows sessions to be routed to the same backend based on a cookie set by the NodeBalancer.
                          example: http_cookie
                          x-linode-cli-display: 5
                          default: none
                          enum:
                          - none
                          - table
                          - http_cookie
                  page:
                    type: integer
                    description: |
                      __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  pages:
                    type: integer
                    description: |
                      __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
                    example: 1
                    readOnly: true
                  results:
                    type: integer
                    description: |
                      __Read-only__ The total number of results.
                    example: 1
                    readOnly: true
        default:
          description: |
            Error
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  errors:
                    type: array
                    items:
                      type: object
                      description: |
                        An object for describing a single error that occurred during the processing of a request.
                      x-akamai:
                        file-path: schemas/error-object.yaml
                      additionalProperties: false
                      properties:
                        field:
                          type: string
                          description: |
                            The field in the request that caused this error. This may be a path,
                            separated by periods in the case of nested fields. In
                            some cases this may come back as "null" if the error is
                            not specific to any single element of the request.
                          example: fieldname
                        reason:
                          type: string
                          description: |
                            What happened to cause this error. In most cases, this can be fixed
                            immediately by changing the data you sent in the request,
                            but in some cases you will be instructed to [Open a support
                            ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket)
                            or perform some other action before you can complete the
                            request successfully.
                          example: fieldname must be a valid value
      x-linode-grant: read_only
      x-original-op-id: getNodeBalancerConfigs
      x-original-op-title: Configs List
    x-linode-cli-command: nodebalancers
components:
  securitySchemes:
    oauth:
      type: oauth2
      flows:
        authorizationCode:
          authorizationUrl: https://login.linode.com/oauth/authorize
          scopes:
            account:read_only: Allows access to GET
              information about your Account.
            account:read_write: Allows access to all
              endpoints related to your Account.
            domains:read_only: Allows access to GET
              Domains on your Account.
            domains:read_write: Allows access to all
              Domain endpoints.
            events:read_only: Allows access to GET your
              Events.
            events:read_write: Allows access to all
              endpoints related to your Events.
            firewall:read_only: Allows access to GET
              information about your Firewalls.
            firewall:read_write: Allows access to all
              Firewall endpoints.
            images:read_only: Allows access to GET your
              Images.
            images:read_write: Allows access to all
              endpoints related to your Images.
            ips:read_only: Allows access to GET your
              ips.
            ips:read_write: Allows access to all endpoints
              related to your ips.
            linodes:read_only: Allows access to GET
              Linodes on your Account.
            linodes:read_write: Allow access to all
              endpoints related to your Linodes.
            lke:read_only: Allows access to GET LKE
              Clusters on your Account.
            lke:read_write: Allows access to all endpoints
              related to LKE Clusters on your Account.
            longview:read_only: Allows access to GET
              your Longview Clients.
            longview:read_write: Allows access to all
              endpoints related to your Longview Clients.
            nodebalancers:read_only: Allows access to
              GET NodeBalancers on your Account.
            nodebalancers:read_write: Allows access
              to all NodeBalancer endpoints.
            object_storage:read_only: Allows access
              to GET information related to your Object Storage.
            object_storage:read_write: Allows access
              to all Object Storage endpoints.
            stackscripts:read_only: Allows access to
              GET your StackScripts.
            stackscripts:read_write: Allows access to
              all endpoints related to your StackScripts.
            volumes:read_only: Allows access to GET
              your Volumes.
            volumes:read_write: Allows access to all
              endpoints related to your Volumes.
            vpc:read_write: Allows access to all endpoints
              related to VPC and Subnet creation, updating, and deletion.
          tokenUrl: https://login.linode.com/oauth/token
    personalAccessToken:
      type: http
      scheme: bearer
externalDocs:
  description: |
    See documentation for Akamai's Linode API
  url: https://techdocs.akamai.com/linode-api/reference