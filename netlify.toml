[context.deploy-preview.environment]
HUGO_VERSION = "0.40.2"

[context.deploy-preview]
command = "hugo env && hugo --buildDrafts --buildFuture --baseURL $DEPLOY_PRIME_URL && echo '/docs/* /:splat' >> public/_redirects"
